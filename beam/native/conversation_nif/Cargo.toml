[package]
name = "conversation_nif"
version = "0.1.0"
edition = "2021"

[lib]
name = "conversation_nif"
crate-type = ["cdylib"]

[dependencies]
abyss = { path = "../../..", package = "abyss", features = ["spectral"] }
rustler = "0.37"
