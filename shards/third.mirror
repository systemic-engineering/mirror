in @order

# @third — RE-EXPORT SHIM. Content migrated to `shards/order/third.mirror`
# per Alex 2026-07-20 Round 2 Q1 direct-transcript adjudication:
# "@order top-level marker; @third COLLAPSES into @order/third. Clean
# 5-level tower under one marker."
#
# === Migration ===
#
# The prior @third marker (Loki 2026-07-01 landing) at this path
# carried the recursion-depth substrate-decl with four bilateral
# predicates (depth_at_least, observer_observes_observing,
# recursion_folds_back, mechanism_visible), the composed third_order_
# active bilateral, the witness_third_order action, and the
# observation_depth carrier.
#
# ALL of this content now lives at `shards/order/third.mirror` — the
# third species-decl of the @order family. @order/third is the new
# canonical home; consumers should import @order/third directly for
# new code.
#
# This shim preserves existing consumer imports (`shards/reflection.
# mirror`, `shards/pack/metalogue.mirror`, `shards/cogito.mirror`,
# etc.) by re-exporting @order/third's carriers under the @third
# name. When downstream consumers migrate to `in @order/third`, this
# shim may be removed at a future substrate-pull tick.
#
# === Adjudication reasoning ===
#
# Per Round-2 brief §T1.7 Mara adjudication: option (b) re-export
# shim over option (a) delete + audit (substrate-hostile for dozens
# of downstream imports) and option (c) recognition-carrier stub
# (risks duplicate substrate-decl at two family-roots).
#
# Two-tick discipline per [[feedback-legibility-over-foundation-
# when-collapsing]]: THIS tick lands migration + shim; future tick
# MAY remove shim after downstream imports migrate.
#
# === Substrate-decl content preserved ===
#
# See `shards/order/third.mirror` for the full substrate-decl content:
#   - observation_depth carrier
#   - depth_at_least, observer_observes_observing, recursion_folds_
#     back, mechanism_visible predicates
#   - third_order_active composed bilateral
#   - witness_third_order action
#
# The Loki 2026-06-22 essay grounding, Bateson Level III + Foerster
# second-order + Kauffman eigenform + Lawvere fixed-point substrate
# authority chain is preserved at @order/third.
#
# === Substrate authority chain (migration) ===
#
# - **Alex Round 2 Q1** (2026-07-20 direct-transcript): "@third
#   COLLAPSES into @order/third."
# - **Loki 2026-06-22 essay** — the original naming preserved at
#   new canonical home.
# - **Round-1 math root** (`docs/math/the-tower/recognition-third-
#   cognition-is-the-metalogue-between-void-narcissus-and-void-
#   splinter.md`) — Mara 2026-07-20 substrate-decl'd @third's
#   Bateson-III + Foerster + Kauffman + Loki content at metalogue-
#   rhythm altitude; the same content lands at @order/third.
#
# === Related shards ===
#
# - `shards/order.mirror` — @order family-root marker (this tick).
# - `shards/order/third.mirror` — canonical @order/third species-
#   decl (migrated content home).
# - `shards/order/{first,second,fourth,fifth}.mirror` — sibling
#   species-decls under @order.
# - `shards/reflection.mirror` — downstream consumer of @third;
#   continues to work via this shim; migration to `in @order/third`
#   forward-promised.
# - `shards/pack/metalogue.mirror` — Level-2 metalogue landing
#   (`b0af0cd`); consumer of @third's metalogue-rhythm reading.
#
# === Path-namespace ===
#
# Path-namespace property: this file at `shards/third.mirror`
# re-exports `@order/third` under the `@third` name for backward-
# compatibility.

source @arxiv/cybernetics/bateson-1972
source @arxiv/cybernetics/foerster-1974

# Re-exports from @order/third for backward-compatibility. Consumers
# importing @third receive the migrated carriers under the @third
# name; new code should import @order/third directly.
out @third
