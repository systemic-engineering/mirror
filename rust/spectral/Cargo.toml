[package]
# `rust/spectral/` — math substrate crate at rust/ altitude.
# Migrated 2026-07-28 from `rust/src/{liquid.rs,spectral.rs}` per Alex
# 2026-07-25 four-crate decomposition + Mara `9bb1f57` naming discipline.
# Second cell to materialize as its own crate boundary; carries
# property/pillar surface + shard manifest + (post-Migration 4b)
# gauge-attractor singularity + magic gauge.
#
# Karen Spärck Jones citation lands at src/lib.rs docblock in Migration
# 4b per Mara `9bb1f57` introduction-site convention.
name = "spectral"
version = "0.1.0"
edition = "2021"
description = "mirror spectral crate — (A, H, D) triple + pillar surface + shard manifest"
license = "MIT OR Apache-2.0"

[dependencies]
# prismqueer with lapack + bundle features for pillar predicates +
# apply_h operator action + Bundle trait tower (Connes A, H, D).
prismqueer = { path = "../../../prism/prismqueer", features = ["lapack", "bundle"] }

[dev-dependencies]
terni = { path = "../../../prism/imperfect" }
tempfile = "3"
