-- Types and constructs from different programming paradigms.
type result(t, e) = ok(t) | err(e)
type option(t) = some(t) | none

async fn handle_request(req) -> result
impl Display for Error
match status {
  active => proceed
  inactive => reject
}

property total_function(handle_request) = iso
