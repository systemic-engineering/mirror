in @prism
in @kintsugi
in @code
in @io

grammar @kintsugi/lift {
  # lift Rust into grammar. The inverse of migrate.
  # read through @code lens. emit as .mirror.
  # the loss is the @io residual.
  lift(ast, target) -> imperfect { \ }
}

out lift
