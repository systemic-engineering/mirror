in @prism

grammar @git/hooks {
  type hook = pre_commit | commit_msg | pre_push | post_commit

  check(hook) -> imperfect { \ }
  untracked_loss -> loss { \ }
  format(hook) -> imperfect { \ }
}

out hook
out check
out untracked_loss
out format
