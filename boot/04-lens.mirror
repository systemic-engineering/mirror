form @lens {
  in @form

  lens lens(ref)
  lens action(ref)

  traversal optic_kind = iso | lens | prism | traversal | fold | setter
  traversal composition = then | compose | identity

  traversal preview = satisfiable | unsatisfiable | partial
  traversal delta = converged | diverged
}
