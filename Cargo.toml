[package]
name = "conversation"
version = "0.1.0"
edition = "2021"
license-file = "LICENSE.md"
description = "Gradients over trees"

[features]
default = []
git = ["fragmentation/git"]

[dependencies]
fragmentation = { path = "../fragmentation" }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tempfile = "3"
