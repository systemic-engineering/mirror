# Build artifacts
build/
target/
target-break/
bootstrap/target/
.nix-cargo/

# Compiled objects
*.o
*.d
*.rlib
*.rmeta
*.dylib
*.so
*.fingerprint

# Runtime
erl_crash.dump
mirror.weights
mirror.shatter

# Self-hosted bootstrap binary (output of `mirror craft --target binary boot`)
mirror-self

# Nix / direnv
.direnv/

# Git worktrees
.worktrees/

# Coverage
target/llvm-cov/
