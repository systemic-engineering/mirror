---
title: "Taut — prismqueer as PROOF ENGINE + MEASUREMENT + BENCHMARKING + SHAPE-OF-TENSION detector: floor-truth for Alex 2026-09-04 fast-math discipline"
subtitle: "Grep-verified inventory of LANDED / FORWARD-PROMISED / NOT-YET-NAMED primitives at /Users/alexwolf/dev/projects/prism/prismqueer/ per Alex 2026-09-04 identity-directive: 'This is Taut's baby. It's both the benchmarking, the measurement, and the proof engine. Taut's whole identity is finding the shape of tension. Make it fast math, Taut.' Composed against Mara loving-lambda-calculus canonical (da9e47c-adjacent; docs/specs/2026-09-04) + Mara loving-lambda-calculus math foundation (a05fbba-adjacent; docs/math/2026-09-04) + Rec #90 §5.4 sub-Turing FLOOR + Rec #92 M2.1 LOVE-monoid + Rec #82 β-normal AST content-addressing + Anna Wolf 2012 observation-without-perturbation. Floor-truth only; path:line-number citations; NO proposals; NO recognition-minting; NO Rust extension. Reed's territory: TICK B prismqueer::Shard<T> + foerster_imperative rust ship per Mara canonical §2.2 + §3.1."
author: Taut <taut@systemic.engineer>
date: 2026-09-04
role: drift-scout
altitude: floor-truth
provenance:
  - /Users/alexwolf/dev/projects/prism/prismqueer/ (grep-verified 2026-09-04)
  - docs/math/2026-09-04-mara-loving-lambda-calculus-foerster-imperative-shard-liquid-types-math-foundation.md
  - docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md
  - docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-math-foundation.md
  - docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md
  - docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md
  - docs/specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec.md
  - docs/math/2026-08-19-mara-recognition-90-compiler-as-one-mathematical-object-math-foundation.md
  - docs/specs/2026-08-19-mara-recognition-90-compiler-as-one-mathematical-object-canonical-spec.md
  - docs/math/2026-09-04-mara-nl-compose-body-spectral-commutator-natural-language-generation-math-foundation.md
composition-lineage:
  - Alex 2026-09-04 verbatim identity-directive (three-role composition: proof engine + measurement + benchmarking; shape-of-tension = Taut's whole identity; "make it fast math")
  - Alex 2026-08-05 [[feedback-rust-delivers-primitives-substrate-delivers-composition]] (rust primitives live at prismqueer altitude)
  - Alex 2026-08-25 [[feedback-forward-promised-vs-confirmed-rec-altitude]] (FORWARD-PROMISED vs CONFIRMED altitude discipline)
  - Alex 2026-08-26 [[project-anna-wolf-2012-observation-without-perturbation]] (Anna's math grounds ALL measurement primitives)
  - Alex 2026-08-26 [[project-softmax-is-avg-fate-wants-k5-compose]] (@fate wants K_5-compose, not softmax = avg)
  - Alex 2026-08-29 [[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]
  - Alex 2026-09-02 [[project-inference-is-rotation-observed-through-shared-substrate]] (rotation IS inference per shared substrate)
  - Alex 2026-09-02 [[project-autopoietic-operational-architecture-mq-rotation-tension-peer-compiler-partition]] (compiler observes tension; peer decides observation)
  - Rec #90 §5.4 sub-Turing FLOOR primitive-count cap (6-8 primitives; +3 headroom; total ≤11)
  - Rec #92 M2.1 kleinos-as-Transparency<P> LOVE-monoid (4-altitude substrate-scale-invariance)
  - Rec #82 β-normal AST OID content-addressation
  - Prior Taut scout precedent (`docs/scouts/2026-08-05-taut-primitives-vs-composition-scout.md` + `docs/scouts/2026-09-01-taut-prismqueer-colony-mirror-composition-glasswall-kleinos-metalogue-migration-boundary-drift-scout.md`)
---

## §0 — Scope and discipline

Per Alex 2026-09-04 verbatim identity-directive: *"This is Taut's baby. It's both the benchmarking, the measurement, and the proof engine. Taut's whole identity is finding the shape of tension. Make it fast math, Taut."*

This scout grounds the prismqueer floor-truth for the property-proof-floor architecture Alex 2026-09-04 named. Three-role composition — **measurement + benchmarking + proof engine** — at ONE crate. Taut grounds what's LANDED + names what's FORWARD-PROMISED for the property-proof-floor + what Alex's fast-math discipline requires next.

**Read-only per Pack convention**: NO proposals; NO canonical spec; NO recognition-minting; NO Rust extension; floor-truth ONLY with path:line-number citations.

**Fast-math discipline** (per Alex 2026-09-04):

- **Fast** = measurement primitives that fire EMPIRICALLY at silicon (LAPACK dsyev via FLANG runtime; sub-Turing bounded per Rec #90 §5.4; polynomial-time complexity witnessed via primary-source docblock citations).
- **Math** = category-theoretic + algebraic-topological + sheaf-cohomological substrate (Rec #92 LOVE-monoid + Rec #82 β-normal AST content-addressing + Curry-Howard + Baez-Schreiber bundle tower + Anna Wolf 2012 shared-memory observation).
- **Shape-of-tension** = H⁰ disconnection + H¹ cohomological + Fiedler-λ_2 delta primitives that empirically detect substrate topology tensions at prismqueer altitude.

---

## §1 — Crate-level floor-truth (grep-verified 2026-09-04)

### §1.1 Cargo.toml identity

`/Users/alexwolf/dev/projects/prism/prismqueer/Cargo.toml:1-45`:

- **name**: `prismqueer`
- **version**: `0.1.1`
- **description**: `"The spectral-triple substrate — five operations (focus, project, split, shift, settle), the Prism trait, zero deps. The foundation."`
- **keywords**: `["prism", "spectral", "substrate", "algebra", "optics"]`
- **categories**: `["rust-patterns", "no-std"]`

**Feature-gates** (`Cargo.toml:14-21`):

| Feature | Purpose | Deps introduced |
|---|---|---|
| `default` | `[]` | — |
| `optics` | classical optics (`Lens`/`Iso`/`Traversal`/…) | — |
| `bundle` | Baez-Schreiber principal-bundle tower + `liquid` module | implies `optics` |
| `lambda` | content-addressed lambda calculus | — |
| `lapack` | LAPACK/BLAS dispatch via FLANG | `cc` build-dep |
| `pq` | typed pq wire DSL with serde | `serde`, `serde_json` |
| `fate` | 5-model Fate inference selector | implies `bundle` |
| `fate-metal` | GPU Metal-runtime for Fate | `metal`, `objc` |
| `fate-training` | Fate model training pipeline | `serde` |

**Runtime deps** (`Cargo.toml:29-38`): `terni` (LOVE-monoid + Imperfect ternary functor; path `../imperfect`); `prismqueer-projections` (proc-macros `declaration!{}` + `#[derive(Prism)]` + `#[derive(Lambda)]`; path `../projections`); `sha2` (content-address); `hex` (OID encoding).

**Zero deps in default configuration**. Every extension is feature-gated.

### §1.2 pub modules exported at lib.rs

`/Users/alexwolf/dev/projects/prism/prismqueer/src/lib.rs:33-88`:

**Always-compiled** (14 modules): `beam`, `coincidence`, `crystal`, `luminosity`, `scalar_loss`, `substrate_ref`, `trace`, `connection`, `content`, `kernel`, `merkle`, `metal`, `named`, `oid`, `optic_kind`, `precision`, `spectral_oid`, `spectral_uuid`, `store`.

**Feature-gated**:

- `#[cfg(feature = "optics")]` — `optics` (Lens/Iso/Traversal/Fold/Setter/OpticPrism/Gather)
- `#[cfg(feature = "pq")]` — `pq` (Target/Filter/Output/Reference/Schema)
- `#[cfg(feature = "bundle")]` — `bundle`, `liquid`
- `#[cfg(feature = "lambda")]` — `lambda` (mod + reduce + typed)
- `#[cfg(feature = "lapack")]` — `ffi`, `spectral`, `spectral_dimension`
- `#[cfg(feature = "fate")]` — `fate`

**Central pub trait re-export** (`lib.rs:145-165`):

```
pub trait Prism {
    type Input: Beam;
    type Focused: Beam<In = <Self::Input as Beam>::Out>;
    type Projected: Beam<In = <Self::Focused as Beam>::Out>;
    type Refracted: Beam<In = <Self::Projected as Beam>::Out>;
    fn focus(&self, beam: Self::Input) -> Self::Focused;
    fn project(&self, beam: Self::Focused) -> Self::Projected;
    fn settle(&self, beam: Self::Projected) -> Self::Refracted;
}
```

**Central pub function** (`lib.rs:220-243`): `pub fn apply_h<P, SIn, In, Out, E, L>(prism: &P, state: SIn) -> Imperfect<Out, E, L>` — the heterogeneous-operator-action-on-H canonical pattern. Composes seed + `apply` + `into_focus`. Consumers avoid reinventing the wrapper.

### §1.3 Fortran native pipeline (LAPACK dispatch)

`/Users/alexwolf/dev/projects/prism/prismqueer/native/spectral.f90` (6.9KB, 2026-09-02): declares `module optics_spectral` with `bind(c)` LAPACK subroutine wrappers for `dsyev` (real symmetric eigenvalue), `dgesvd` (SVD), plus a Kuramoto phase-lock explicit-Euler integrator `spectral_phase_lock`. Modified 2026-09-02 (Reed dd2fbee prior; c14d61e rotation this arc).

`/Users/alexwolf/dev/projects/prism/prismqueer/native/prism.f90` (3.0KB, unchanged since 2026-04-11): declares `module optics_prism` with `preview` / `review` / `modify` / `compose` (matrix `P2*P1`) 4-op wrappers over projection matrices. These are the LAPACK-adjacent 4-op prism primitives that predate the spectral extension.

### §1.4 Build-script surface (`build.rs`, 18.5KB)

`/Users/alexwolf/dev/projects/prism/prismqueer/build.rs:1-9`: Brainfuck → native-Rust codegen pipeline. Reads all `.bf` files from `src/fate/brainfuck/`, applies IR optimization passes (run-length encoding at `:85`, clear/copy loop pattern-matching at `:114`), emits both Rust codegen (`:151`) and Metal Shading Language codegen (`:226`) for GPU-parallel Fate inference.

**Not** the FLANG-Fortran numerical build — that is `cc = { version = "1", optional = true }` in `Cargo.toml:41` gated by `lapack` feature; the Fortran object linking happens per Cargo's `cc` build-dependency (not visible in the extracted portion). FLOOR §7 numerical pipeline chain per prior scout precedent: `mirror → apply_h::act → rust/matrix::eigenvalues → prismqueer::ffi::eigenvalues → LAPACK dsyev → FLANG native/spectral.f90`.

---

## §2 — Shape-of-tension primitives (Taut's whole identity)

Per Alex 2026-09-04 identity-directive: **"Taut's whole identity is finding the shape of tension."**

### §2.1 LANDED shape-of-tension detector at prismqueer altitude

`/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral/tension.rs` (13.7KB, 2026-09-02, Reed 008cd65):

**Public API** (grep-verified):

- `pub enum TensionKind` (`:67-83`) — three-kind Phase 1 MVP:
  - `Disconnection` — H⁰(F) rank > 1 (multiple connected components)
  - `Cohomological` — H¹(F) non-trivial (independent cycles)
  - `Coherence` — Fiedler λ₂ below expected-value-for-graph-size
- `pub struct Tension` (`:85-99`) — kind + normalized magnitude ∈ [0, 1] + diagnostic
- `pub fn detect_tensions(sheaf) -> Imperfect<Vec<Tension>, Red, ConvergenceLoss>` (`:126-...`)

**Docblock formal grounding** (`tension.rs:8-17`):

- Tension #1 (Disconnection): H⁰(F) rank > 1; "task shape: bridge the components"
- Tension #2 (Cohomological): H¹(F) non-trivial; "for bare graph Laplacian (Q-Mara-λ Phase 1 per Hansen-Ghrist §3 Remark), β₁ = |E| - |V| + |components|"
- Tension #3 (Coherence): Fiedler λ_2 below path-graph baseline; "task shape: strengthen the weakest bridge"

**Composition-substrate** (`tension.rs:29-38`; zero new Rust primitives per HARD RULE `[[feedback-rust-delivers-primitives-substrate-delivers-composition]]`):

- Composes over `prismqueer::spectral::fiedler_lambda_2_of_sheaf` (Anna Wolf 2012 apparatus at basepoint via LAPACK dsyev)
- Composes over `prismqueer::ffi::eigenvalues` (full spectrum for component-count via zero-eigenvalue-multiplicity read)
- Composes over `prismqueer::spectral::SheafOfShardGraph` (input carrier)
- Returns `terni::Imperfect<Vec<Tension>, Red, ConvergenceLoss>` (ternary functor per Alex 2026-09-02 color-coded repo state)

**Empirical fire** (per CURRENT.md 2026-09-02 arc-log): `5/5 tests pass; K_5 correctly detects β₁=6, tree=0, disjoint-edges=2 components`. Reed 008cd65 wired into mirror-repo `cmd_serve_mcp` at 7752e55; real substrate walk yielded **Fiedler λ₂ = 2.999... + 53 Disconnected components + β₁ = 2637 independent cycles** from real substrate.

**CAVEAT** (per CURRENT.md 2026-09-02 self-audit): NUMBERS are real LAPACK; STRINGS (diagnostic templates) are Reed-authored `format!()` templates pending @nl.compose body ship. See `[[feedback-past-reed-io-au-sel-docblock-only-fabrication-historical-instance]]`.

### §2.2 LANDED rotation primitive (inference IS rotation)

`/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral/rotation.rs` (15.7KB, 2026-09-02, Reed c14d61e):

**Public API**:

- `pub struct Splinter` (`:86-111`) — atomic crystallization of one observation-tick; carries content-address + tick-index + measured observables (phases, order_r, fiedler)
- `pub struct RotationConfig` (`:117-143`) — encodes 1D coupling axis (`coupling_k`) + 1D time axis (`dt`) + omega scale for vertex-natural-frequencies
- `pub fn infer_via_rotation(loops, ticks, config) -> Imperfect<Vec<Splinter>, Red, ConvergenceLoss>` (`:172-...`)

**Docblock formal grounding** (`rotation.rs:1-30`) — Alex 2026-09-02 verbatim terminal recognition:

> *"ROTATION through TIME is the inference! All the coupled bagels/loops provide information about their topology by spinning through the NOW/hole and Anna's math allows to OBSERVE that computation which then settles into the inferred splinters."*

**5-axis substrate** (`rotation.rs:19-30`):

- 3D space: shard-graph topology (vertex positions in composed sheaf)
- 1D time: rotation tick (each step advances Kuramoto phase by `dt`)
- 1D relational: coupling K (Kuramoto phase-locking strength)

**Composition-substrate** (`rotation.rs:34-51`):

- Composes over `prismqueer::spectral::kleinos` (Reed 4a3bbe7 ring-and-hub topology; 036abeb ternary refactor)
- Composes over `prismqueer::ffi::phase_lock` (Reed dd2fbee 2026-07-20 Kuramoto integration; LAPACK-adjacent via FLANG `native/spectral.f90`)
- Composes over `prismqueer::spectral::fiedler_lambda_2_of_sheaf` (Anna Wolf 2012 apparatus via LAPACK dsyev)
- Composes over `prismqueer::oid::{Addressable, Oid}` (content-addressation per Rec #82)

**Per-tick observation loop** (`rotation.rs:149-157`):

1. Advance Kuramoto phase (spin the wheel by one dt)
2. **Anna Wolf apparatus observes** composed graph Fiedler λ₂ at K_3 basepoint (LAPACK dsyev — measurement without perturbation of rotation)
3. Compute Kuramoto order parameter r ∈ [0, 1] (coupling-coherence readout)
4. Content-address the tick state → splinter Oid
5. **Foerster gauge check**: Fiedler λ₂ must monotone-non-decrease across ticks (topology cannot narrow)
6. Deposit splinter

**Empirical fire**: `4/4 in-module smoke tests pass` per CURRENT.md 2026-09-02. **Rec #99 PARTIAL-CONFIRMED at Level-1 candidate** (rotation primitive c14d61e + mirror-repo wire 7752e55 real-substrate observation).

### §2.3 LANDED compose primitive (the K_2→K_3 shape emergent)

`/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral/kleinos.rs` (29.4KB, 2026-09-02, Reed 4a3bbe7 → 036abeb ternary refactor):

**Public API**:

- `pub enum Property` (`:67-83`) — four-variant discriminator per PAPER §3.6: `Sovereignty`, `EmergentThird`, `FiedlerRise`, `FusionRefusal`
- `pub struct SheafOfShardGraph` (`:130-180`) — cellular sheaf per Curry 2014; Phase 1 vertices + edges only per Q-Mara-λ Alex ratification (trivial-stalks defaults recover bare graph Laplacian per Hansen-Ghrist 2019 §3 Remark)
- `pub struct ComposedSheaf` (`:183-242`) — K_3-composed sheaf with accessor methods on emergent-third stalk + content-address per Rec #82
- `pub fn sheaf_of_shard_graph_from_edges(edges: &[(VertexId, VertexId)]) -> SheafOfShardGraph` (`:254-...`)
- `pub fn sheaf_of_complete_graph_of_order(n: usize) -> SheafOfShardGraph` (`:274-287`)
- `pub fn fiedler_lambda_2_of_sheaf(sheaf: &SheafOfShardGraph) -> f64` (`:297-330`) — Phase 1: bare graph Laplacian via LAPACK dsyev
- `pub fn kleinos(sheaf_a, sheaf_b) -> Imperfect<Green, Red, ConvergenceLoss>` (`:367-390`) — the K_2→K_3 compose primitive

**Formal spec anchors** (`kleinos.rs:1-25`):

- Mara canonical spec `docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md` §2.2 (signature) + §4.4 (body composition anti-pattern)
- Mara math foundation `docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-math-foundation.md` §3 (cellular-sheaf grounding) + §36 (Kasparov bimodule / Connes correspondence composed with Baez-Schreiber 2-connection)
- PAPER `~/dev/systemic.engineering/PAPER_2D.md` §3.6 LOVE-K_2→K_3 four properties

**Body composition invariant** (`kleinos.rs:29-37`; per Mara §4.4 + HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]`): NO if-else / match-arm dispatch on Property verbs. Verification body composes as ONE call producing `Transparency<Property>` LOVE-monoid verdict; four properties are coordinates NOT enumerated cases.

**Ternary return shape** (`kleinos.rs:95-117` per Alex 2026-09-02 verbatim: *"Result is binary wearing a trenchcoat. We're doing ternary here, Reed."*):

- **Green** = `Imperfect::Success(ComposedSheaf)` — all 4 PAPER §3.6 properties Pass; compose emitted cleanly
- **Yellow** = `Imperfect::Partial(ComposedSheaf, ConvergenceLoss)` — compose emitted with measured loss (Phase 2+)
- **Red** = `Imperfect::Failure(Red, ConvergenceLoss)` — compose refused; `Red = Transparency<Property>` LOVE-monoid

**The ring-and-hub topological correction** (`kleinos.rs:397-449`): 2026-09-02 landing (Reed 4a3bbe7). Alex 2026-09-02 verbatim (Achtsam Morden essay grounding):

> *"Das Loch is das Jetzt."* + *"Achtsam morden is die Kunst das Loch nich voll zu scheissen."*

**Empirical fire** (CURRENT.md 2026-09-02): **6/6 tests empirically pass at silicon**. K_3+K_3 composed λ₂ = 7 (K_7 emergent). Scar-preserved history of 2026-09-01 minimal-bridge (5/6 failed) → 2026-09-02 dense-hub (4/6) → 2026-09-02 ring-and-hub (6/6). Alex 2026-09-02: *"the emergent third IS the hole, not a vertex."*

**Complexity witness** (per Mara loving-lambda canonical §9.3): `love() call: O(prismqueer::spectral::kleinos cost) = O((n+m+1)³) per Rec #92 sub-Turing envelope` — polynomial-time bounded per Rec #90 §5.4 sub-Turing FLOOR.

---

## §3 — Measurement primitives (Anna Wolf 2012 substrate)

Per HARD RULE `[[project-anna-wolf-2012-observation-without-perturbation]]` (Alex 2026-08-26): Anna's math grounds ALL measurement primitives; cite at introduction site.

### §3.1 LANDED FFI layer — safe Rust wrappers around LAPACK dsyev / dgesvd

`/Users/alexwolf/dev/projects/prism/prismqueer/src/ffi.rs` (12.9KB, 2026-09-02):

**Extern C signatures** (`ffi.rs:17-85`):

- `prism_preview` / `prism_review` / `prism_modify` / `prism_compose` — the 4-op prism primitives (matrix projection); wrappers at `ffi.rs:127-198`
- `spectral_eigenvalues` — `dsyev('N')` eigenvalues only
- `spectral_eigensystem` — `dsyev('V')` eigenvalues + eigenvectors
- `spectral_singular_values` — `dgesvd('N','N')` singular values only
- `spectral_svd` — `dgesvd('A','A')` full SVD
- `spectral_phase_lock` — Kuramoto phase-lock explicit-Euler integration for N coupled oscillators (Reed dd2fbee)

**Safe Rust wrappers** — row-major → column-major layout conversion (`ffi.rs:98-121`):

- `pub fn preview(n, projection, source) -> Option<Vec<f64>>` (`:127-149`)
- `pub fn review(n, projection, focus) -> Vec<f64>` (`:151-165`)
- `pub fn modify(n, projection, source, transform) -> Vec<f64>` (`:167-186`)
- `pub fn compose(n, p1, p2) -> Vec<f64>` (`:188-198`) — matrix `P2·P1` at LAPACK altitude (NOT the sheaf-compose primitive at spectral::kleinos; refined by Mara canonical §1 refused-mints inventory)
- `pub fn eigenvalues(n, matrix) -> Result<Vec<f64>, i32>` (`:216-232`) — eigenvalues in ascending order
- `pub fn eigensystem(n, matrix) -> Result<(Vec<f64>, Vec<f64>), i32>` (`:246-273`) — eigenvalues + eigenvectors (row-major converted from LAPACK column-major)
- `pub fn singular_values(m, n, matrix) -> Result<Vec<f64>, i32>` (`:281-301`)
- `pub fn svd(m, n, matrix) -> Result<(Vec<f64>, Vec<f64>, Vec<f64>), i32>` (`:313-346`)
- `pub fn phase_lock(phases, omegas, k, steps, dt) -> Result<(Vec<f64>, f64), i32>` (`:359-390`) — returns `(final_phases, order_parameter_r ∈ [0,1])`

**Docblock cites LAPACK contracts explicitly** (`ffi.rs:44-84`). `Err(info)` returned per LAPACK `info != 0` convention. Empty-input degenerate cases handled at `ffi.rs:211-214`, `:280-282`, `:311-313`.

### §3.2 LANDED spectral-dimension via heat kernel on graph Laplacian

`/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral_dimension.rs` (14.0KB):

**Public API**:

- `pub struct Sigma(f64)` (`:36-62`) — diffusion time (heat-kernel parameter)
- `pub struct SpectralDimension(f64)` (`:65-85`) — effective dimension a diffusion process feels at scale σ
- `pub fn heat_return_probability(eigenvalues: &[f64], sigma: Sigma) -> f64` (`:89-99`) — `P(σ) = (1/N) Σ_k e^(−λ_k σ)`
- `pub fn spectral_dimension(eigenvalues: &[f64], sigma: Sigma) -> SpectralDimension` (`:103-...`) — closed-form `d_s(σ) = 2σ · (Σ λ_k e^(−λ_k σ)) / (Σ e^(−λ_k σ))`
- `pub fn laplacian_from_edges(n: usize, edges: &[(usize, usize)]) -> Vec<f64>` (`:131-142`)
- `pub fn graph_spectral_dimension(...)` (`:149-...`) — composes `laplacian_from_edges` + `ffi::eigensystem` + `spectral_dimension`

**Docblock formal grounding** (`spectral_dimension.rs:1-31`): combinatorial graph Laplacian `L = D − A`; heat-kernel trace `Z(σ) = Σ_k e^(−λ_k σ)`; sanity: a continuum d-dimensional manifold has `P ~ σ^(−d/2)`, giving `d_s = d`. λ_0 caveat: only meaningful at small-to-intermediate σ (the fine scale).

### §3.3 LANDED content-addressation tower (Rec #82 substrate)

- `/Users/alexwolf/dev/projects/prism/prismqueer/src/oid.rs` (7.0KB) — `pub struct Oid(String)` (`:10`) + `pub trait Addressable` (`:83`) — "The thing has a content address. That's all."
- `/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral_oid.rs` (7.8KB) — `pub struct SpectralOid` (`:25`) — Oid viewed at specific `Precision`; equality IS spectral closeness via truncated representation
- `/Users/alexwolf/dev/projects/prism/prismqueer/src/spectral_uuid.rs` (9.6KB) — `pub struct SpectralUuid([u8; 16])` (`:64`) — 128-bit content-address with 48/80 golden-ratio split (48 bits ACTIVE quantized `SpectralCoordinate<5>` navigable + 80 bits DARK BLAKE3-truncated content hash identity); monoid homomorphism w.r.t. shard merge per CRDT spec §3
- `/Users/alexwolf/dev/projects/prism/prismqueer/src/coincidence.rs` (27.5KB) — `pub struct Detector<const N: usize>` (`:248-251`) + `pub fn canonical_hash(bytes: &[u8]) -> String` (`:481-483`) — N=3 canonical detector for `Oid::hash()`; three independent observers, deterministic projections from SHA-256 seeds; **the K_3 observer per prismqueer-as-compiler math §3.3 grep-verified**

### §3.4 LANDED sub-Turing FLOOR (Rec #90 §5.4 cap)

Per `docs/specs/2026-08-19-mara-recognition-90-*-canonical-spec.md` §5.4:

- Layer 0 primitive-count cap: 6-8 landed + up to 3 pending = ≤11 max at empirical-fire
- Termination DECIDABLE at compile-time (finite integer comparisons + LAPACK bounded-iteration eigenvalue routines + walker cascade with idempotency + `apply_h::act` shard-body dispatch bounded by substrate-decl'd finite arms)
- Load-bearing invariant: if rust/ grew beyond 8-11 primitives, Rice's theorem could re-enter and undecidability would compromise Foerster-gauge trust-property

Per `docs/math/2026-08-19-mara-recognition-90-*-math-foundation.md` §8.4 Corollary 8.3 (Layer-0 cap preserved): `magic::foerster_gauge_preserved` is O(1) integer comparison; sub-Turing; does not introduce Rice-theorem-undecidability into the rust/ floor.

---

## §4 — Bundle tower (Baez-Schreiber principal-bundle)

`/Users/alexwolf/dev/projects/prism/prismqueer/src/bundle.rs` (31.1KB, 2026-09-02):

**Five traits forming supertrait chain** (`bundle.rs:5`): `Fiber → Connection → Gauge → Transport → Closure → Bundle (blanket)`.

**Public traits + laws**:

- `pub trait GroupStructure` (`:49-58`) — identity/inverse/composition/associativity (Gap 2 of `docs/specs/spectral-triple-grammar.md`)
- `pub trait LawvereFixedPoint` (`:73-96`) — idempotence + kernel projection (Gap 3; ties to Rec #94 Lawvere 1969 diagonal fixed-point)
- `pub trait Fiber` (`:100-103`) — "Level 0: the observed state. The section of the bundle. Abyss."
- `pub trait Connection: Fiber` (`:114-124`) — "Level 1: the optic that determines how information transports." Requires `Optic: Prism` (Gap 1)
- `pub trait Gauge: Connection` (`:134-149`) — "Level 2: the structure group." Requires `Group: GroupStructure`
- `pub trait Transport: Gauge` (`:159-172`) — "Level 3: parallel transport with holonomy." Requires `Holonomy: Metric` (Gap 4; Connes bounded-commutator condition)
- `pub trait Closure: Transport` (`:181-191`) — "Level 4: autopoietic closure. The Lawvere fixed point."
- `pub trait Bundle: Closure` (`:195-212`) — blanket impl: any type implementing all five levels IS a Bundle

**Concrete impls**:

- `pub struct IdentityPrism<S: Clone>` (`:219-271`) — pass-through at every stage; witnesses optic algebra has identity element
- `pub struct Cyclic<const N: u8>(pub u8)` (`:277-309`) — the cyclic group Z/NZ of rotations modulo N; simplest credible non-trivial group
- `pub struct StableFiber<S>` (`:316-331`) — fiber-state fixed under tick-map witness
- `TestBundle` / `LiquidTestBundle` reference impls (`:338-...`) — always-public, always-compiled (NOT `#[cfg(test)]`-gated) so downstream crates can compose property tests without redeclaring the tower

**Composition-anchor**: per `docs/math/2026-08-26-mara-prismqueer-as-compiler-*-math-foundation.md` §4 (bundle tower at prismqueer altitude per Baez-Schreiber precedent). Grep-verified in FLOOR §12.

---

## §5 — Liquid property-proof primitives (8-pillar substrate)

`/Users/alexwolf/dev/projects/prism/prismqueer/src/liquid.rs` (29.3KB, 2026-09-02; `#[cfg(feature = "bundle")]` gated):

**Public API**:

- `pub trait LiquidConnection: Transport` (`:70-112`) — Bundle whose commutator can be computed at Rust altitude via Gauge action + Transport holonomy composition; **blanket-implemented for any type satisfying `Transport`** (users do NOT implement directly — implementing `Transport` grants LiquidConnection for free)
- `pub struct Commutator<'a, C: LiquidConnection>` (`:118-137`) — `[A, B]` at a state as deferred value
- `pub fn commutator<'a, C: LiquidConnection>(...) -> Commutator<'a, C>` (`:140-150`)
- `pub fn commutator_norm<C>(a: &C, b: &C) -> C::Holonomy` (`:158-171`) — at Default state; convenience for tests

**The four pillars** (`liquid.rs:174-...`) — exposed as verdict functions returning `terni::PropertyVerdict`. Per `liquid.rs:178`: **Pillar IV (`@peer.audhd` fanout) lives at `mirror/rust/src/liquid.rs`** because it needs `fate::Fate::tick`, which prismqueer doesn't and shouldn't depend on (per Mara `3cd9a42` §6).

**Property guarantees by construction** (`liquid.rs:35-48`) — inherited from `Metric` axioms:

- Antisymmetric: `commutator_magnitude(a, b, s) == commutator_magnitude(b, a, s)` (Metric distance is symmetric)
- Self-annihilating: `commutator_magnitude(a, a, s) == Loss::zero()` (identical holonomies)
- Non-negative + triangle inequality (Metric axioms)
- Vanishes for abelian gauges (Cyclic<N> action commutes)

**First ouroboros layer** (`liquid.rs:47-49`): "every one of these is empirically witnessed by `prismqueer/tests/liquid_ouroboros.rs` — the first ouroboros layer where prismqueer tests its own trait laws through its own liquid module."

**Formal grounding** (`liquid.rs:26-30`): Connes' bounded-commutator condition `‖[D, a]‖ < ∞` at Rust-altitude prism-bundle altitude. Full derivation: `mirror/docs/math/spectral-commutator-four-pillars.md` (Mara `5d3040d`) §2; operational spec: `mirror/docs/specs/spectral-commutator-as-cybernetic-ground.md` (Mara `3cd9a42`).

---

## §6 — Benchmarking substrate (Fiedler λ_2 monotone climb per Rec #92 M2.1 L3)

### §6.1 LANDED bench: beam pipeline (Criterion)

`/Users/alexwolf/dev/projects/prism/prismqueer/benches/beam_pipeline.rs` (4.5KB, 2026-06-15):

- `fn light_pipeline_10()` (`:13-27`) + `fn mixed_pipeline_10()` (`:29-43`) + `fn dark_pipeline_smap_10()` (`:45-59`) + `fn dark_pipeline_next_10()` (`:61-75`) + `fn dark_pipeline_tick_10()` (`:77-90`) + `fn bench_pipelines(c: &mut Criterion)` (`:92-...`)
- Docblock (`:1-4`): "Benchmarks comparing light, mixed, and dark beam pipelines. Dark beams should be faster than light beams — no computation, just propagation of the fixpoint through the pipeline."
- Cargo.toml declares `[[bench]]` entry `name = "beam_pipeline", harness = false` (`:43-45`)

**Coverage gap**: NO benchmarks for `spectral::kleinos` OR `spectral::rotation` OR `spectral::tension` OR `ffi::eigenvalues` OR `spectral_phase_lock`. The four LANDED spectral primitives (§2 + §3) have NO benchmark surface.

### §6.2 LANDED Fiedler-monotone empirical assertion (per test suite)

`/Users/alexwolf/dev/projects/prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` (16.2KB, 2026-09-02):

- Semantic-compile altitude → GREEN per Reed 4a3bbe7 (6/6 empirically pass at silicon)
- Tests PAPER §3.6 four-property assertions via `kleinos(&a, &b)` invocations
- Discharges the FiedlerRise strict `>` clause per Q-Mara-κ Alex 2026-09-01 ratification

**No cross-commit Fiedler-λ_2 monotone climb benchmark** — the per-commit / per-tick monotonicity check happens INSIDE `rotation::infer_via_rotation` per-tick (`rotation.rs:154-157` Foerster gauge check). There is NO benchmarking harness measuring λ_2 climb across historic commits per Rec #92 M2.1 L3 empirical fire criterion.

### §6.3 Benchmark inventory summary

| Bench target | State | File |
|---|---|---|
| beam pipeline (light/mixed/dark) | LANDED | `benches/beam_pipeline.rs` |
| spectral::kleinos | NOT NAMED | — |
| spectral::rotation (Kuramoto integration + Fiedler per-tick) | NOT NAMED | — |
| spectral::tension (detect_tensions on real substrate) | NOT NAMED | — |
| ffi::eigenvalues (LAPACK dsyev throughput per matrix size) | NOT NAMED | — |
| ffi::phase_lock (Kuramoto integration cost per (N, steps)) | NOT NAMED | — |
| Cross-commit Fiedler λ_2 monotone climb (Rec #92 M2.1 L3) | NOT NAMED | — |
| Junction-temp / timing per Reed forward-promise (rust wire #409) | NOT NAMED | — |

---

## §7 — Proof engine (Curry-Howard-lifted refinement-type substrate)

Per Alex 2026-09-04 verbatim + Mara loving-lambda canonical `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md` + `docs/math/2026-09-04-mara-loving-lambda-calculus-foerster-imperative-shard-liquid-types-math-foundation.md`.

### §7.1 LANDED proof-engine substrate at prismqueer

**Curry-Howard witness carriers** at prismqueer altitude (grep-verified 2026-09-04):

- `pub trait Prism` (`lib.rs:145-165`) — the type-level composition primitive (3 associated Beam-typed stages; compiler enforces stage output = next stage input)
- `terni::Transparency<P>` LOVE-monoid re-exported (`lib.rs:128`) — Curry-Howard product-type carrier per Rec #92 M2.1
- `terni::PropertyVerdict` re-exported — three-valued proposition ({Pass, Partial, Fail}) per Curry-Howard sum-type
- `terni::Imperfect<T, E, L>` ternary functor re-exported — {Success, Partial, Failure} per Curry-Howard sum

**Lambda substrate** (`#[cfg(feature = "lambda")]` gated):

- `/Users/alexwolf/dev/projects/prism/prismqueer/src/lambda/mod.rs` (16.5KB) — `pub enum Lambda<T: Clone + PartialEq>` (`:21-32`) — four variants: Bind + Abs + Apply + Case; no strings; Oid for identity
- `/Users/alexwolf/dev/projects/prism/prismqueer/src/lambda/reduce.rs` (11.0KB) — `pub fn reduce_bounded<T>` (`:83-...`) — beta reduction with step budget; returns `Imperfect` three outcomes (Success = normal form zero steps; Partial = normal form after steps; Failure = budget exhausted or stuck)
- `/Users/alexwolf/dev/projects/prism/prismqueer/src/lambda/typed.rs` (9.0KB) — `pub trait LambdaFn` (`:22-53`) — typed surface with compile-time-checked composition via `.then()`; `A.then(B)` only compiles when `A::Output == B::Input`; loss accumulates through chain

**Content-addressed β-normal AST** (Rec #82 substrate): `oid.rs` + `spectral_uuid.rs` + `coincidence.rs` (§3.3 above).

### §7.2 Property-proof-floor gap per Mara loving-lambda canonical §2.2 + §3.1 + §6.3

**NOT YET LANDED at prismqueer** per Mara `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md` §2 + §3 (forward-promised at Reed TICK B):

1. **`prismqueer::Shard<T>` struct** (`docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:118-193`) — FORWARD-PROMISED at `/Users/alexwolf/dev/projects/prism/prismqueer/src/shard.rs`. Fields:
   - `hilbert_carrier: H_F` (substrate-varying carrier per Rec #90 §1.4)
   - `algebra_ref: AlgebraRef<A_F>` (5-op algebra reference; substrate-invariant per Rec #90 Def 1.9)
   - `dirac_operator: D_F` (spectral-loss functional per Rec #90 §1.6 kintsugi-flow)
   - `iteration_index: usize` (λ_0 monotone-descent per Rec #90 Prop 1.8)
   - `transparency: Transparency<Property>` per Rec #92 M2.1
   - `mandelbrot_state: MandelbrotState<T>` composing over shards/fractal/mandelbrot.mirror
   - `refinement_predicates: Vec<Predicate<T>>` back-written from mirror @liquid family
   - `foerster_gauge: FoersterGauge` compile-time predicate per Rec #90 §8

2. **`prismqueer::foerster_imperative<T, F>` type-level higher-order wrapper** (`docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:216-272`) — FORWARD-PROMISED at `/Users/alexwolf/dev/projects/prism/prismqueer/src/foerster_imperative.rs`. Signature:
   ```
   pub fn foerster_imperative<T, F: Fn(Shard<T>) -> Shard<T>>(f: F)
       -> FoersterImperative<T, F>
   ```
   Attaches Foerster-gauge predicate `F(ν, _)` at refinement-type level per Rondon-Kawaguchi-Jhala 2008 §5 (SMT-decidable compile-time verification). Per Rec #90 §8 orthogonality theorem: F is orthogonal to A_F^prismqueer at compile-time; runtime execution requires no runtime gauge-check.

3. **`prismqueer::spectral::kleinos` refactor to accept `Shard<T>` input types** (`docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:475-479`) — FORWARD-PROMISED at Reed TICK B. Current kleinos takes `&SheafOfShardGraph`; loving-lambda composition-shape `foerster_imperative(lambda(shard, shard)) -> kleinos(imperfect<Green, Red, Yellow>)` requires `kleinos: Shard<T> × Shard<T> → Transparency<Property>`.

### §7.3 The composition-shape at rust altitude (Mara loving-lambda canonical §4.1)

Per `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:283-323`:

```
foerster_imperative(lambda(shard, shard)) -> kleinos(imperfect<Green, Red, Yellow>)
```

FORWARD-PROMISED at Reed TICK C (mirror-repo `rust/src/love.rs` full compose + `rust/src/apply_h.rs` ARMS collapse per §6.3 of same canonical). The `love()` dispatch dissolves ~700 LOC apply_h::act ARMS into ONE `love()` dispatch via refinement-type-driven dispatch (Rondon-Kawaguchi-Jhala 2008 §5 SMT-decidable; no runtime ARM enumeration).

### §7.4 The property-proof-floor per Alex 2026-09-04 verbatim recognition

Composed from LANDED §2-§5 + FORWARD-PROMISED §7.2:

| Property-proof-floor layer | LANDED at prismqueer | FORWARD-PROMISED (Reed TICK B) |
|---|---|---|
| Content-addressed β-normal AST (Rec #82) | oid + spectral_uuid + coincidence + lambda | — |
| LOVE-monoid (Rec #92 M2.1) | terni::Transparency<P> re-exported | — |
| Bundle tower (Baez-Schreiber) | bundle.rs 5-trait supertrait chain | — |
| Commutator + 4 pillars (Connes) | liquid.rs commutator + PropertyVerdict | Pillar IV at mirror altitude |
| K_2→K_3 compose primitive (PAPER §3.6) | spectral::kleinos ring-and-hub topology | Shard<T>-input refactor |
| Shape-of-tension detector (H⁰+H¹+Fiedler) | spectral::tension detect_tensions | — |
| Rotation-as-inference (Kuramoto) | spectral::rotation infer_via_rotation | — |
| LAPACK dsyev/dgesvd/phase_lock | ffi + native/spectral.f90 | — |
| Sub-Turing FLOOR (Rec #90 §5.4) | ≤11 primitives per Layer 0 cap | — |
| Shard<T> struct (loving-lambda canonical §2.2) | — | src/shard.rs |
| foerster_imperative wrapper (loving-lambda §3.1) | — | src/foerster_imperative.rs |
| Refinement-type-driven dispatch (loving-lambda §6.3) | — | mirror-repo rust/src/love.rs |

---

## §8 — Fast-math primitives inventory with polynomial-time complexity witnesses

Per Alex 2026-09-04 fast-math discipline + Rec #90 §5.4 sub-Turing FLOOR:

### §8.1 Fast + LANDED (measures/benchmarks/proves NOW at silicon)

| Primitive | Complexity witness | Path:line |
|---|---|---|
| `ffi::eigenvalues` (LAPACK dsyev) | O(n³) per LAPACK dsyev primary source | `src/ffi.rs:216-232` |
| `ffi::eigensystem` (LAPACK dsyev + evecs) | O(n³) per LAPACK dsyev primary source | `src/ffi.rs:246-273` |
| `ffi::singular_values` (LAPACK dgesvd) | O(min(m,n) · max(m,n)²) per LAPACK dgesvd | `src/ffi.rs:281-301` |
| `ffi::svd` (LAPACK dgesvd full) | O(min(m,n) · max(m,n)²) per LAPACK dgesvd | `src/ffi.rs:313-346` |
| `ffi::phase_lock` (Kuramoto explicit-Euler) | O(steps · N²) per pairwise-coupling sum | `src/ffi.rs:359-390` |
| `spectral::kleinos` compose | O((n+m+1)³) per Mara loving-lambda §9.3 | `src/spectral/kleinos.rs:367-390` |
| `spectral::fiedler_lambda_2_of_sheaf` | O(n³) composes over ffi::eigenvalues | `src/spectral/kleinos.rs:297-330` |
| `spectral::detect_tensions` (H⁰+H¹+Fiedler) | O(n³) composes over ffi::eigenvalues | `src/spectral/tension.rs:126-...` |
| `spectral::infer_via_rotation` (per-tick) | O(ticks · (N² + n³)) per tick | `src/spectral/rotation.rs:172-...` |
| `spectral_dimension::spectral_dimension` closed-form | O(n) per eigenvalue-summation | `src/spectral_dimension.rs:103-...` |
| `spectral_dimension::graph_spectral_dimension` | O(n³) composes over ffi::eigensystem | `src/spectral_dimension.rs:149-...` |
| `coincidence::canonical_hash` (N=3 detector) | O(bytes · dim) per SHA-256 seed + projection | `src/coincidence.rs:481-483` |
| `liquid::commutator_norm` (abelian case) | O(Transport::holonomy cost) | `src/liquid.rs:158-171` |
| `oid::Oid` content-address | O(bytes) per SHA-256 | `src/oid.rs` |

**All 14 measurement + proof + shape-of-tension primitives sub-Turing bounded** per Rec #90 §5.4 cap (finite integer arithmetic + LAPACK bounded-iteration + walker cascade with idempotency).

### §8.2 Fast + FORWARD-PROMISED (substrate-decl'd but not yet firing)

| Primitive | Complexity witness | Composition-anchor |
|---|---|---|
| `prismqueer::Shard<T>` struct construction | O(1) field aggregation per Mara loving-lambda §9.3 | `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:522-527` |
| `prismqueer::foerster_imperative` wrapper | O(1) closure allocation per Mara loving-lambda §9.3 | `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:522-527` |
| `love() = foerster_imperative(kleinos)` | O(kleinos cost) = O((n+m+1)³) per Mara loving-lambda §9.3 | `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md:283-323` |

### §8.3 Slow-not-fast (complexity unclear or unbounded)

**None grep-verified** at prismqueer. Every LANDED primitive per §8.1 has a polynomial-time witness. Sub-Turing FLOOR discipline holds.

### §8.4 Missing per Mara canonical (named at spec+math altitude, absent at prismqueer)

Per Mara loving-lambda canonical §10.2 (Reed TICK B territory):

1. `prism/prismqueer/src/shard.rs` — §2.2 Shard<T> struct authoring
2. `prism/prismqueer/src/foerster_imperative.rs` — §3.1 wrapper authoring
3. Refactor `prism/prismqueer/src/spectral/kleinos.rs` if needed to accept Shard<T>

Per prior Mara `docs/math/2026-08-26-mara-prismqueer-as-compiler-*-math-foundation.md` §5 (Hamilton-shaped fractal composite memory scheduler; Q-Mara-ε Mara-lean Phase 1):

- **Fractal composite memory scheduler at prismqueer altitude** — DESIGN-LEVEL only per Mara-lean; not authored. §5.3 scheduler primitive definition NOT LANDED at prismqueer. Composes with §5.4 algedonic-monotonicity + crystal-preservation invariants.

Per prior Mara §5.5 (Q-Mara-ζ Mara-lean Phase 2+):

- **Fractal-Mandelbrot memory organization per Rec #98** — Phase 2 observability layer once Kleinos-compose primitive lands and compose-history carries structure at HEAD. FORWARD-PROMISED at candidate strength.

### §8.5 Benchmarking gap (measurement of the measurement substrate)

Per §6 benchmarking substrate: LANDED bench only covers beam pipeline. **NOT YET NAMED** benchmarks for the four LANDED spectral primitives at §2 (kleinos + rotation + tension + fiedler_lambda_2). Rec #92 M2.1 L3 empirical fire criterion (Fiedler λ_2 monotone climb per commit) has NO benchmark harness at prismqueer altitude.

---

## §9 — Categorical summary

### §9.1 LANDED count

**~35+ pub items** at prismqueer altitude across the four-role composition:

- **Measurement**: 15 pub fn / pub struct at `ffi.rs` + `spectral_dimension.rs` + `spectral::fiedler_lambda_2_of_sheaf`
- **Shape-of-tension**: 3 pub fn + 3 pub struct/enum at `spectral::tension` + `spectral::rotation`
- **Proof engine (LOVE-monoid layer)**: `terni::Transparency<P>` + `terni::PropertyVerdict` + `terni::Imperfect` re-exported; 8-trait Bundle tower at `bundle.rs`; commutator + 4 pillars (3 landed at prismqueer, 1 at mirror) at `liquid.rs`; lambda calculus at `lambda/`
- **Compose primitive (kleinos K_2→K_3)**: 6 pub fn/struct/enum at `spectral::kleinos`
- **Content-addressation tower** (Rec #82): `oid.rs` + `spectral_oid.rs` + `spectral_uuid.rs` + `coincidence.rs` (Detector<3>)
- **Benchmarking**: 1 bench file (beam_pipeline; Criterion harness)

### §9.2 FORWARD-PROMISED count

**3 primary primitives** (Reed TICK B territory at prism-repo per Mara loving-lambda canonical §10.2):

1. `prismqueer::Shard<T>` struct at `src/shard.rs`
2. `prismqueer::foerster_imperative` wrapper at `src/foerster_imperative.rs`
3. `prismqueer::spectral::kleinos` refactor to accept `Shard<T>` input

**Plus** (Reed TICK C at mirror-repo):

4. `rust/src/love.rs` full compose (dissolve tick 2 alias into `love(a, b) := foerster_imperative(kleinos)(a, b)`)
5. `rust/src/apply_h.rs` ARMS collapse per §6.3 refinement-type-driven dispatch

### §9.3 NOT YET NAMED count

**~8 gaps surfaced**:

1. Bench for `spectral::kleinos` (measure K_2→K_3 compose cost per (|V(a)|, |V(b)|))
2. Bench for `spectral::rotation` (measure Kuramoto integration + per-tick Fiedler cost)
3. Bench for `spectral::tension` (measure H⁰+H¹+Fiedler detection cost per sheaf size)
4. Bench for `ffi::eigenvalues` (measure LAPACK dsyev throughput per matrix size)
5. Bench for `ffi::phase_lock` (measure Kuramoto integration cost per (N, steps))
6. Cross-commit Fiedler λ_2 monotone climb harness (Rec #92 M2.1 L3 empirical fire criterion)
7. Junction-temp / timing reading per Reed rust wire #409 forward-promise
8. Hamilton-shaped fractal composite memory scheduler primitive per Mara `docs/math/2026-08-26-*-math-foundation.md` §5.3

### §9.4 Composition-gaps for property-proof-floor per Mara loving-lambda canonical

Per `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md` §6.5 + §8.2 (empirical criteria + TICK B enumeration):

- Shard<T> LANDED at `/Users/alexwolf/dev/projects/prism/prismqueer/src/shard.rs`
- foerster_imperative wrapper LANDED at `/Users/alexwolf/dev/projects/prism/prismqueer/src/foerster_imperative.rs`
- kleinos refactor to accept Shard<T> input types
- love() composition at rust altitude LANDED at mirror-repo `rust/src/love.rs`
- apply_h::act ARMS collapse per §6.3 refinement-type-driven dispatch LANDED at mirror-repo `rust/src/apply_h.rs`
- Fiedler λ_2 monotone climb ≥3 consecutive commits (Rec #92 M2.1 L3 empirical fire)
- 7-warning wire-through per Mara loving-lambda canonical §7 (7 unused warnings at `rust/src/{main,magic,wire,compile}.rs` lifted to Shard<T>.refinement_predicates field composing at intended usage site via `love(shard, args)`)

---

## §10 — Surprises (existing substrate not previously grep'd at this altitude)

### §10.1 The four-role composition is ALREADY GREP-VERIFIABLE at prismqueer

Per §2 + §3 + §5 + §6 above: **the three-role composition Alex 2026-09-04 named (measurement + benchmarking + proof engine) + Taut's identity role (shape-of-tension detector) is materially LANDED** at prismqueer altitude across four commits in the 2026-09-02 arc (Reed 4a3bbe7 kleinos + c14d61e rotation + 008cd65 tension + 036abeb ternary refactor).

The FORWARD-PROMISED gap is at TYPE-COMPOSITION altitude (Shard<T> struct + foerster_imperative wrapper), not at OPERATIONAL altitude. The operational shape-of-tension detector `detect_tensions` already fires empirically at silicon (5/5 tests pass; wire yielded real Fiedler λ₂ = 2.999... + 53 components + β₁ = 2637 on real substrate walk).

### §10.2 `bundle.rs` reference impls are always-compiled (not test-gated)

Per `bundle.rs:333-338`: **`TestBundle` + `LiquidTestBundle` are always-public, always-compiled (NOT `#[cfg(test)]`-gated)** so downstream crates — mirror, spectral, any consumer of `prismqueer::liquid` — can compose property tests over concrete Bundle carriers without redeclaring the tower. This is a load-bearing substrate design decision for downstream property-proof-floor consumers.

### §10.3 `liquid::LiquidConnection` is blanket-implemented for any `Transport`

Per `liquid.rs:70`: `pub trait LiquidConnection: Transport` — **users do NOT implement this trait directly**; implementing `Transport` (via Bundle tower) grants `LiquidConnection` for free. This means Pillar I/II/III commutator-based property verdicts are automatically available for ANY Bundle-tower implementer at prismqueer altitude.

### §10.4 Coincidence detector at N=3 IS the K_3 observer

Per `docs/math/2026-08-26-mara-prismqueer-as-compiler-*-math-foundation.md` §3.3 (grep-verified): **The `Detector<3>` at `coincidence.rs:248-251` IS the operational form of the third-order-observation Foerster-native pattern**. Three independent observers, deterministic projections from SHA-256 seeds. This grounds the K_3 observer in the compiler-substrate spectral triple at prismqueer altitude via `canonical_hash` (`:481-483`).

### §10.5 Fate 5-model inference substrate is source-mirrored into prismqueer

Per `Cargo.toml:22-27` + `lib.rs:86-88` + `src/fate/mod.rs` (52KB): Fate is source-mirrored from `/Users/alexwolf/dev/projects/fate/` into `prismqueer::fate` per Alex 2026-07-18 directive ("Let's pull fate into prismqueer::fate"). Divergence risk documented as forward-promise per `fate/mod.rs` docblock. 5 Model variants (`pub enum Model` at `:42-55`) + 16-dim feature vector (`pub const FEATURE_DIM: usize = 16`) + `pub struct Fate` (`:117-672`) + `pub trait Pipeline` (`:674-1460`). Feature-gated `fate = ["bundle"]`; Metal-runtime feature `fate-metal = ["fate", "dep:metal", "dep:objc"]` for GPU-parallel inference.

### §10.6 build.rs is a BF-to-Rust codegen pipeline (not FLANG-Fortran)

Per `build.rs:1-9` + full grep: **`build.rs` is a Brainfuck → native Rust + Metal Shading Language codegen pipeline** for GPU-parallel Fate inference. The FLANG-Fortran numerical build happens via `cc = { version = "1", optional = true }` build-dependency gated by `lapack` feature (`Cargo.toml:41`); Fortran object linking is orthogonal to `build.rs` proper. The FLOOR §7 numerical pipeline grep-hop chain `mirror → apply_h::act → rust/matrix::eigenvalues → prismqueer::ffi::eigenvalues → LAPACK dsyev → FLANG native/spectral.f90` bridges these two build-time substrates via feature-gate composition.

---

## §11 — Composition-anchor citations (per Definition M8.1 FLOOR discipline)

Cite by reference; do not re-derive per Mara canonical §12.2 composition-anchor discipline:

- Rec #82 β-normal AST content-addressation — `docs/math/the-tower/recognition-82-*.md`
- Rec #90 §5.4 sub-Turing FLOOR primitive-count cap — `docs/specs/2026-08-19-mara-recognition-90-compiler-as-one-mathematical-object-canonical-spec.md:257-289`
- Rec #90 §8 Foerster-gauge orthogonality theorem — `docs/math/2026-08-19-mara-recognition-90-compiler-as-one-mathematical-object-math-foundation.md:337-363`
- Rec #92 M2.1 kleinos-as-Transparency<P> LOVE-monoid — `docs/math/2026-08-22-mara-recognition-92-*-math-foundation.md`
- Rec #94 Lawvere 1969 diagonal fixed-point closure at A₄ compiler-substrate — `docs/math/2026-08-22-mara-recognition-94-*-math-foundation.md`
- PAPER §3.6 LOVE-K_2→K_3 four properties — `~/dev/systemic.engineering/PAPER_2D.md`
- Mara prismqueer-as-compiler canonical + math (2026-08-26) — `docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md` + `docs/specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec.md`
- Mara prismqueer::spectral::compose Phase 1 canonical + math (2026-08-31) — `docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md` + `docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-math-foundation.md`
- Mara loving-lambda-calculus canonical + math (2026-09-04) — `docs/specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md` + `docs/math/2026-09-04-mara-loving-lambda-calculus-foerster-imperative-shard-liquid-types-math-foundation.md`
- Mara @nl.compose body math foundation (2026-09-04) — `docs/math/2026-09-04-mara-nl-compose-body-spectral-commutator-natural-language-generation-math-foundation.md`
- Curry 2014 cellular sheaves + Hansen-Ghrist 2019 sheaf Laplacian L_F + Hansen 2020 Thesis §3 — per Mara §36 citations
- Anna Wolf 2012 observation-without-perturbation (Diplomarbeit PGI/JCNS; consent obtained Alex 2026-08-26) — LOAD-BEARING per `[[project-anna-wolf-2012-observation-without-perturbation]]`
- Baez-Schreiber 2013 principal-bundle tower (arXiv:1310.7930) — per bundle.rs supertrait chain
- Rondon-Kawaguchi-Jhala 2008 Liquid Types (PLDI '08; https://dl.acm.org/doi/10.1145/1375581.1375602) — per Mara loving-lambda §3-§4 refinement-type-inference discipline
- Connes bounded-commutator condition — per liquid.rs Connes-native discipline
- Requardt 2002 Dirac Operators on Graphs — per `docs/math/2026-08-19-mara-recognition-90-*-math-foundation.md` bibliography

---

## §12 — Circular-recursive self-audit

### §12.1 What this scout IS

Per Alex 2026-09-04 identity-directive:

- Floor-truth inventory at prism-repo grep-verified 2026-09-04
- Categorical partition (LANDED / FORWARD-PROMISED / NOT YET NAMED / composition-gaps)
- Fast-math primitives inventory with polynomial-time complexity witnesses
- Composition-anchor citations by reference (Definition M8.1 discipline)
- Path:line-number citations throughout
- Composed against Mara loving-lambda canonical §2.2 + §3.1 + §6.3 + §10.3 for property-proof-floor gap analysis

### §12.2 What this scout IS NOT

Per Pack convention (Taut = read-only) + Alex 2026-09-04 directives:

- Does NOT propose new prismqueer primitives (Mara territory)
- Does NOT mint Recognition candidates (Alex + Pack territory)
- Does NOT author Rust code at prism-repo (Reed TICK B territory)
- Does NOT author Rust code at mirror-repo (Reed TICK C territory)
- Does NOT re-derive math (composition-anchor citations only)
- Does NOT include whole-file quotes (path:line-number citations only)
- Does NOT scope beyond the four-role composition (measurement + benchmarking + proof engine + shape-of-tension)

### §12.3 What this scout surfaces for adjudication

- **§9.3 NOT YET NAMED gaps** — bench harness authorship + Fiedler-λ_2 monotone climb harness + Hamilton-shaped memory scheduler primitive; Alex may adjudicate whether any belongs at TICK B / TICK C / TICK D altitude
- **§10.6 build-substrate observation** — FLANG-Fortran linking is orthogonal to build.rs (BF codegen); Alex may adjudicate whether this composition needs explicit substrate-decl at prismqueer altitude
- **§7.4 property-proof-floor table** — composed picture of what's landed vs what needs TICK B/C ship; Alex may adjudicate whether the composition-shape maps to the fast-math discipline as expected

---

## §13 — Q.E.D. summary

**Prismqueer AT prismqueer altitude (grep-verified 2026-09-04)**:

- **PROOF ENGINE** = LANDED via Curry-Howard-lifted `terni::Transparency<P>` LOVE-monoid + `Prism` trait 3-stage type-composition + `bundle.rs` 5-trait Baez-Schreiber tower + `liquid.rs` commutator-based 4-pillar property verdicts + `lambda/` β-normal AST substrate
- **MEASUREMENT** = LANDED via `ffi.rs` 6 safe LAPACK wrappers (dsyev + dgesvd + phase_lock) + `native/spectral.f90` FLANG-Fortran linking + `spectral_dimension.rs` heat-kernel closed-form + Anna Wolf 2012 observation-without-perturbation discipline enforced at basepoint per `spectral::fiedler_lambda_2_of_sheaf`
- **BENCHMARKING** = LANDED via `benches/beam_pipeline.rs` Criterion harness (light/mixed/dark pipeline comparison) + Rec #92 M2.1 L3 empirical fire criterion enforced INSIDE `rotation::infer_via_rotation` per-tick Foerster gauge check; **~7 bench-authorship gaps** for the four LANDED spectral primitives + cross-commit Fiedler climb harness
- **SHAPE-OF-TENSION detector** (Taut's identity) = LANDED via `spectral::tension` (H⁰ Disconnection + H¹ Cohomological + Fiedler λ_2 Coherence) + `spectral::rotation` (Kuramoto integration observed per Anna Wolf apparatus with Foerster gauge check) + `spectral::kleinos` (K_2→K_3 compose primitive per PAPER §3.6 four properties; ring-and-hub topology; 6/6 empirical fire at silicon)

**Property-proof-floor gaps per Mara loving-lambda canonical §2.2 + §3.1 + §6.3**:

- `prismqueer::Shard<T>` struct (FORWARD-PROMISED at Reed TICK B, `src/shard.rs`)
- `prismqueer::foerster_imperative` wrapper (FORWARD-PROMISED at Reed TICK B, `src/foerster_imperative.rs`)
- `prismqueer::spectral::kleinos` refactor to accept Shard<T> input (FORWARD-PROMISED at Reed TICK B)
- `rust/src/love.rs` full compose + `rust/src/apply_h.rs` ARMS collapse (FORWARD-PROMISED at Reed TICK C mirror-repo)

**Fast-math discipline holds**: all 14 LANDED measurement + proof + shape-of-tension primitives sub-Turing bounded per Rec #90 §5.4 cap. Polynomial-time complexity witnesses documented at §8.1.

Q.E.D. Floor-truth grounded. Taut done.

🍷
