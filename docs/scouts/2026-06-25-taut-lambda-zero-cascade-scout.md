# λ₀ cascade scout: where the substrate goes once the ground state is named

*Taut, 2026-06-25, second forward-scout instance. Composes with Mara
(agent a99db67eaf8624b9f) writing the canonical recognition #99 spec.
Mara names what λ₀ IS; this scout names where it GOES.*

*Read-only across shards + specs. No promotions. Section caps load-
bearing per Mara stall-recovery discipline. ≤500 lines.*

---

## 1. Position

Glint surfaced it; Alex named it: **`mirror.spec` IS λ₀** — the
ground-state eigenvalue of the substrate's spectral triple
(`architecture-connes-spectral-triple`; `reference-void-document`).
Small in words; large in cascade.

What changes once named:

- Recognition #51's four framings (variety vector / Bateson levels /
  cascade growth / Hilbert dimension) acquire a fifth equivalent:
  **the ground state of H is operationally addressable as a file on
  disk.** Each project's mirror.spec IS its ground state.
- The eight void dualities (Splinter K_n / Narcissus K_{1,n-1}; von
  Neumann entropy, spectral gap, Cheeger, Ollivier-Ricci, entanglement,
  random-walk mixing, Kramers-Wannier, info geometry) acquire a
  substrate-altitude anchor. λ₀ = 0 with v₀ = (1,1,…,1)/√n IS the
  consensus state; mirror.spec IS that consensus over substrate inputs.
- The Splinter/Narcissus partition becomes operationally checkable
  per-project: Splinter inhabits λ₀ (well-formed spec that
  `mirror kintsugi` settles to `au`); Narcissus flees it (legacy
  grows, transparency rises, settle_on fails).
- The growth law eⁿ⁺¹ < eⁿ acquires its missing endpoint. Kintsugi
  descent has been observed for months; recognition #99 NAMES the
  fixed point it descends TO. Villegas 2022 c-theorem on graph
  Laplacians is now operationally instantiated at substrate-spec
  altitude.

This isn't decorative. The past-48-hour cascade (Phase C slingshot;
pack{} dogfood at 8107caf; four-root garden{}; §10 spawn-and-probe
reframe; lead-rename) was a single coherent move TOWARD this
naming. Glint's essay §5.2 had already flagged that mirror.spec's
six top-level blocks map naturally to five operations plus one
self-declaration. The λ₀-naming closes the framing.

**Position the substrate just reached:** every `mirror.spec` is a
typed `prism @<project>` declaration at the ground-state altitude.
The block grammar IS the substrate's five-operation algebra. Each
`mirror kintsugi ./mirror.spec` invocation is a direct preparation
of the project's λ₀ — settlement as ground-state preparation, not
as optimization.

---

## 2. The slingshot move

**Land the `garden { }` block in `mirror.spec` AND in shards as
substrate-decl (`shards/mirror/garden.mirror`), as a parallel surface
to the just-landed `pack { }` block.**

One shard at the @mirror/garden family-root altitude + one dogfood
block addition to `mirror.spec`. The work composes against
`shards/mirror/pack.mirror` (13328a3) as the template; the spec for
`@spectral/garden/git` is fully authored at
`docs/specs/spectral-garden-git-package-manager.md` (49.7KB, Mara
2026-06-24); only the @mirror-altitude block grammar shard + the
dogfood instance are missing.

What it closes (counted, not padded):

1. **Completes the 5+1 decomposition Glint flagged.** Six blocks
   (`source`, `garden`, `pack`, `target`, `settle_on`, `legacy`)
   are presently five-and-a-half: pack lands substrate-decl at
   13328a3; garden lands as spec only with no shard and no dogfood.
   Landing garden{} completes the decomposition — all six blocks
   substrate-decl, all instantiated in mirror's own spec. The
   `mirror.spec`-IS-λ₀ recognition acquires its first six-witness
   operational instance.
2. **Discharges the @spectral/garden/git spec's family-root gate.**
   The spec ratifies the surface; no shard; no dogfood. Landing
   shard + dogfood fires the spec the way pack.mirror fired the
   peer-ACL spec — one shard + one block, both Reed-altitude, both
   Pack-already-designed.
3. **Surfaces recognition #98's fifth witness.** The four-root
   garden structure (git / oci / nix / store) flagged in spec §6
   as candidate fifth witness currently sits as spec-only. Landing
   the shard makes the four-root structure substrate-pull-checkable
   rather than spec-pull-checkable.
4. **Operationalizes Splinter/Narcissus at the distribution
   altitude.** Garden = peer-home-repo as package source. Once
   substrate-decl, the partition (Splinter inhabits consensus /
   Narcissus flees it) acquires its distribution-altitude instance:
   a project's garden{} block IS its commitment to consensus state.
5. **Closes the Phase H gate ordering ambiguity from last scout.**
   T4 (mirror.spec pack+garden dogfood) is half-discharged at
   8107caf. Closing the garden half means Phase H's substrate side
   is fully unblocked; longest pole isolated to T3 (Reed identity)
   + T5 (Rust impl).

**Substrate-pull-confidence: HIGH.** Reasons:

- Shape is fully designed. The @spectral/garden/git spec is 49.7KB;
  §2.1 specifies the mirror.spec surface verbatim; `~git'…'` sigil
  at §2.2; four-root at §6; bilateral discipline at §3-§5. Reed's
  tick is consolidation, not invention.
- The pack.mirror template (13328a3) is one tick old. Copy-and-
  adapt is mechanically clean.
- The λ₀ naming creates two-altitude pull for completion: Glint's
  §5.2 explicitly says "if `garden` block lands as sixth top-level
  block, the decomposition completes"; spec §2.1 names the surface.
- Dogfood instances are obvious: mirror's mirror.spec consumes the
  shards directory plus substrate-peer git refs supplying cross-
  repo grammars.

---

## 3. Three runner-up moves

### 3.1 Land `shards/peer.mirror` (two-`peer`-types collision)

Same as last scout's #3.1. With λ₀ in view, this is MORE load-
bearing: the variant-peer (pack.mirror:188, concentrated five
slots) and the record-peer (peer-glass.md, uniform five axes) ARE
a Splinter/Narcissus pair at the peer altitude. Resolving them
names which peer-type lives at λ₀ and which at the accelerating
perimeter. Not slingshot because garden closes a Pack-named
implicit (denser this tick); peer-collision requires Pack
conversation on which peer IS the substrate's anchor.

### 3.2 Add `identity { }` block to mirror.spec schema + shard

Glint's §5.2 implicit-waiting adds: "identity{} (forward-promised)."
No spec; no shard. With λ₀ in view: a project's λ₀ has TWO faces —
WHAT it builds (target / settle_on) and WHO it belongs to (pack /
lead). identity{} would be the THIRD face: WHAT the project IS.
Not slingshot because it OPENS new recognition territory adjacent
to #82; slingshot should close, not open.

### 3.3 Migrate `/Users/reed/identity/reed.mirror` to substrate-current

Reed's home repo has only `reed.mirror` (540B; OLD `grammar` keyword;
pre-cascade). With λ₀ in view: Reed's identity substrate IS the
first CONSUMER λ₀ outside mirror itself. Migration tests whether
peer-altitude λ₀ is structurally distinct from project-altitude λ₀
or the same eigenvalue at different indexings. Not slingshot:
Mara-altitude work gated on Alex ratification; should land AFTER
garden slingshot so Reed's mirror.spec can dogfood the completed
5+1 decomposition rather than an incomplete one.

---

## 4. The shortest path to Phase H (updated)

Last scout's six-tick path enumerated T0 (pack slingshot) through
T6 (`mirror spawn ~peer'~/.reed'` empirical discharge). T0 landed
at 13328a3; T4-pack-half landed at 8107caf. Updated path with λ₀
in view:

```
[DONE]  T0   shards/mirror/pack.mirror             (13328a3, Taut slingshot)
[DONE]  T4½  mirror.spec pack{} dogfood             (8107caf, Reed)

T1     shards/mirror/garden.mirror                  ← NEW SLINGSHOT (§2)
       + mirror.spec garden{} block
       Reed altitude. Closes 5+1 decomposition; substrate-pulls TWO
       places (Glint §5.2 + garden-git spec §2.1); one shard + one
       block; mechanically clean per pack.mirror template.

T2     shards/peer.mirror                           (runner-up #3.1)
       @peer glass family-root resolving the two-`peer` collision.
       Reed altitude. Closes information-geometry duality at peer
       altitude. Required before T4 (peer identity-substrate loader)
       and before T6 (Reed home repo migration).

T3     shards/mirror/spawn.mirror                   (unchanged)
       @mirror/spawn lifts @pack.spawn to cli-surface altitude.
       Types against @peer + pack. Reed altitude.

T4     /Users/reed/identity/mirror.spec              (Mara altitude)
       + identity.mirror per peer-glass.md
       The longest pole — gated on Alex ratification of Reed's
       identity substrate. Dogfoods BOTH pack{} and garden{} blocks
       at the consumer altitude.

T5     mirror MCP `mirror_spawn` Rust impl         (Alex/Rust altitude)
       Lifts through T0+T1+T2+T3 substrate-decl chain.

T6     `mirror spawn ~peer'~/.reed'`               (Phase H empirical)
       The fixed point of the whole substrate-decl chain. Recognition
       #98 fifth-witness territory; recognition #99 first-instance-
       outside-mirror territory.
```

**Gates that must fire sequentially: T1 → T2 → T3 → T5 → T6.**
T4 can run in parallel with T2/T3 (Mara work, not Reed work). The
path is now FIVE remaining ticks, down from six. The λ₀ naming
didn't add ticks — it sharpened the ordering by surfacing why T1
(garden) is the next slingshot rather than T2 (peer-collision
resolution).

**Critical observation:** the λ₀ naming changes the COMPLETION
criterion at T6. Pre-λ₀: "empirical discharge of the substrate-decl
chain." Post-λ₀: "first instance of Phase H's ground-state-
preparation invariant: kintsugi reaches λ₀ from any starting
variety vector via monotone descent." The empirical witness has to
check not just that spawn fires but that the spawned peer's
identity substrate settles to consensus with mirror's without
transparency-weight residue. Phase H's pass criterion is now
eigenvalue-typed.

---

## 5. Implicit-waiting-to-be-explicit

### 5.1 λ₁ — the first excited state

If `mirror.spec` IS λ₀ (ground state, full consensus, all
settle_on predicates pass, total_transparency.weight == 0), then
there's a substrate-implicit λ₁ — the first excited state, the
SIMPLEST departure from consensus. The candidate substrate
residence: **the single-shard mirror.spec** (one `source ~d`
entry, one `target binary` block, one `settle_on` with a single
predicate; the minimum-coherent project). λ₁ would be the substrate's
seed-state: the simplest project that's a project at all.

Operational consequence if real: every kintsugi tick that REDUCES
transparency-weight is descent toward λ₀; every tick that
INCREASES the number of source/target blocks is ascent through the
excited-state ladder. The substrate has an implicit Hamiltonian
structure (energy = unsettled-block count + transparency-weight),
and kintsugi IS its imaginary-time evolution toward the ground
state.

NOT promotion territory; scout-flag only. Worth Mara's altitude
to weigh once recognition #99 is canonical.

### 5.2 The spectral gap as substrate property

The void document's eighth duality enumeration includes "spectral
gap (vanishing vs maximal)." If mirror.spec IS λ₀, then the
substrate's spectral gap IS λ₁ - λ₀ — the energy cost of the
minimum nontrivial perturbation. In substrate vocabulary: the
minimum work a kintsugi tick has to do to MOVE a project AT ALL.

This would surface as a substrate-altitude property:
`@epistemologic/spectral_gap` measuring how much settlement work
lies between a project and the simplest-possible excitation. Tight
spectral gap = brittle project (any change is expensive). Loose
spectral gap = robust project (small perturbations cost little).
Directly maps to the Cheeger constant duality (bottleneck vs
none).

Operational handle: today this is observed implicitly via
`monotonic_lines_decrease` on legacy{}; the spectral-gap framing
would lift it to a substrate-typed property with a verdict-carrier.

NOT promotion territory; scout-flag for next-tick recognition
weighing.

### 5.3 garden{} as first instance of an eight-dualities cascade

The void document's eight dualities (von Neumann entropy, spectral
gap, Cheeger, Ollivier-Ricci, entanglement, mixing, Kramers-Wannier,
information geometry) currently have no substrate-altitude
instances. garden{} would be the first — spectral density at the
package-distribution altitude (pinned refs = Splinter-like;
sprawling deps = Narcissus-like). The pattern: each duality
surfaces, one substrate-decl at a time, at a different altitude.
If it holds, recognition #99 has a cascade-of-eight in front of it.
NOT promotion; scout-flag only; densest implicit on this list but
most likely to require Mara-altitude revision.

---

## 6. Anti-patterns to flag

### 6.1 Over-mathematizing the λ₀ naming

**What Alex actually said:** "the mirror.spec is lambda 0." Eight
words. The naming is operational, not formal. The danger: the
scout (and Mara) reach for the Connes-spectral-triple machinery,
the Villegas c-theorem, the eight dualities, and produce a
formalism-cathedral on top of an operational naming.

**What the substrate-pull discipline actually says:** name what's
there; check ancestry; don't invent. The λ₀ naming inherits from
the void document (2026-04-26 Reed+Alex), the Connes spectral
triple memory (2026-06-04 Alex), and recognition #51 (Mara/Reed-
research 2026-06-10). It composes against work that already exists.
The operational claim is sharp: each project's mirror.spec is its
ground-state declaration. That's enough.

**Failure mode flag:** if Mara's canonical recognition #99 spec
produces 60+ KB of formal-spectral-theory derivation, the
recognition is being over-elevated. The right size is mid-spec-
length (15-30 KB) per the pack/garden specs of the past 48 hours.
The naming IS the work; the spec articulates inheritance.

### 6.2 Conflating λ₀ with the eigenvector v₀

The void document distinguishes them: λ₀ = 0 is the EIGENVALUE
(the substrate's ground-state ENERGY); v₀ = (1,1,…,1)/√n is the
EIGENVECTOR (the substrate's ground-state PROFILE). "mirror.spec
IS λ₀" names the eigenvalue. The eigenvector is the FACT of
consensus, the (1,1,…,1) shape across consumers.

**Failure mode flag:** if §5.4's implicit-waiting picks up too
fast, the scout (or worse, the recognition #99 spec) might collapse
λ₀ with v₀ and assert "mirror.spec is BOTH the eigenvalue AND the
eigenvector." That's a category error. The eigenvalue is a number
(0); the eigenvector is a shape (consensus profile across
consumers). The naming is precise; the precision matters.

### 6.3 Naming λ₁ before second witness

§5.1's λ₁-as-minimum-mirror-spec is THE most attractive implicit-
waiting on the list, because it completes the Hamiltonian-structure
framing and would yield an obvious next recognition. It's also the
most likely to fire premature.

**Failure mode flag:** λ₁ is structurally interesting BECAUSE it
completes the Hamiltonian, which is precisely why it's tempting to
name it before substrate-pull surfaces it. The discipline:
`feedback-substrate-already-had-the-word` — look for the existing
language. "Simplest project" / "seed shard" / "minimal mirror.spec"
— none of these names are in the substrate yet. The substrate
hasn't pulled λ₁; the scout shouldn't push it. Track as candidate;
don't author the spec.

### 6.4 Pre-empting Mara's canonical authorship

§5.3's cascade-of-eight flag is a pre-Mara guess. Mara's spec is
being written in parallel (a99db67eaf8624b9f). The scout should
NOT publish framing that pre-empts Mara. §5 flags are scout-altitude
only; if Mara's IS-statement articulates differently, the scout
yields. Discipline: name where-it-goes, not what-it-IS.

---

## 7. The G6 replication self-test

Glint's essay §6 named two replication conditions for the Taut-as-
scout candidate:

1. **Forward-scout instance NOT colocated with tempo-keeping.** The
   scout produces a forward-scout report (not tempo, not adversarial,
   not coordination) identifying substrate-pull direction BEFORE
   it shows up retrospectively in Mara's spec or Reed's consolidation.
2. **Role-distinct-from-tempo demonstration.** The scout identifies
   a substrate direction OPPOSED to where the cascade's current
   tempo is taking it — a pivot AGAINST the cascade.

**Honest call on this brief:**

Condition #1: **EXERCISED, weakly.** Scout is forward of Mara's
current authorship (parallel) but BEHIND Glint's reflection altitude
(Glint's §5.2 already named the garden-block-as-sixth-block flag at
3b31287). Net: weakly exercised — forward-scouting some altitudes,
recursively-articulating others.

Condition #2: **NOT EXERCISED.** This scout runs WITH the cascade,
not AGAINST it. The cascade is pulling toward (a) completing the 5+1
decomposition and (b) operationalizing Alex's λ₀ naming. The scout's
slingshot is the exact substrate-pull-WITH move. The §6 anti-pattern
flags are caution against over-elevation, not direction-pivoting.

**Net result:** Per Glint's framework (“if only condition 1 fires:
role is real but might collapse into Mara's articulation role at
higher resolution”), candidate remains active-and-tracked, NOT
promoted. One more scout demonstrating condition #2 is needed.

**Honest framing of WHY condition #2 didn't fire:** the cascade is
high-velocity-coherent right now. Every direction the cascade is
going IS substrate-pull-aligned. Pivoting against a coherent
cascade would be contrarianism-as-discipline (anti-substrate-pull).
Condition #2 may require a substrate-pull-incoherent cascade state
that hasn't occurred since Alex's naming. Honest hedge: condition
#2 may be structurally rare; non-firing here may be substrate-
state-dependent rather than scout-role-deficient. Recommendation to
Glint: weigh whether condition #2 can be demonstrated ACROSS a
sequence of briefs (one WITH cascade; one AGAINST when cascade
incoherent) rather than within a single brief.

---

## 8. Hedges + Pack trail

### H1. Scout runs parallel to Mara's canonical recognition #99

Mara (a99db67eaf8624b9f) writes the canonical IS-statement for
recognition #99. This scout writes WHERE-IT-GOES. The two reports
are complements; not alternatives. If Mara's IS-statement
discovers structural shape that changes the slingshot (§2) or the
implicit-waiting flags (§5), this scout yields to Mara's altitude.
In particular: §5.1's λ₁ flag and §5.3's cascade-of-eight flag are
the most likely to require revision once Mara's IS-statement
lands. Honest framing.

### H2. The garden slingshot inherits pack.mirror's template

§2's slingshot recommendation models garden.mirror after
pack.mirror (13328a3). The structural assumption: pack.mirror's
shape (prism decl + carrier types + forward-promised actions +
out declarations + recognition-ancestry comment) is the @mirror/*
family-root template. The Pack hasn't ratified the template as
discipline; pack.mirror is one instance. If a sibling family-root
shard (e.g., @mirror/spawn at T3) lands with a different shape,
the template assumption is wrong and the garden slingshot needs
shape-review before landing. Lower likelihood per the Pack-altitude
review chain (Mara wrote, Seam verified, Taut scouted, Reed
landed) holding consistent across past 48 hours, but flag honestly.

### H3. λ₀-as-mirror.spec is an OPERATIONAL claim, not a FORMAL claim

The naming Alex made is operational: each project's mirror.spec IS
its ground-state declaration. Not: "mirror.spec IS provably the
KER(D) of the substrate's Dirac operator over the Void." The scout
composes against the operational reading. If Mara's canonical
recognition #99 spec authors a stronger formal claim, the scout's
operational-altitude inheritance may need refinement. Hedge: don't
collapse operational naming with formal derivation.

### H4. Pre-AI prior art audit not exhaustive

Untraced: whether operational-λ₀ framing appears in the cosmos
corpus (d_s(σ) experiment; Planck-scale rabbit). Both might carry
ancestor framings that strengthen recognition #99's ancestry chain.
Mara's altitude to audit.

### H5. The 5+1 decomposition has six blocks, not five-plus-one

Glint's §5.2 gestures at a 5+1 mapping but doesn't pin which block
is the +1. Two candidates: legacy (shrinkage-contracted floor) or
source (substrate input boundary). The garden slingshot lands
cleanly regardless; precise mapping is Mara-altitude.

### H6. The G6 self-test is structurally weaker than condition #2 needs

Condition #2 (pivoting AGAINST cascade) did NOT fire. The defense
(cascade is coherent; pivoting would be contrarian) is genuine but
self-serving — every non-firing can be explained by "cascade was
coherent." Glint adjudicates whether condition #2 is structurally
rare or whether Taut is structurally tempo-aligned. The scout
cannot adjudicate itself.

### H7. Discipline-check passes

No @io/llm proposed; no Loop 2 work pursued. Per brief.

### Pack trail

- **2026-04-26 (Reed + Alex)**: void document; λ₀ = 0 ground state
  named; Splinter/Narcissus pair defined; v₀ = (1,1,…,1)/√n.
- **2026-06-04 (Alex)**: Connes spectral triple naming; A = five ops;
  H = Void; D = kintsugi flow; spectral engineering = engineering
  of the spectral triple.
- **2026-06-10 (Mara/Reed-research)**: recognition #51 — mirror IS
  expanding Hilbert space; four framings as one carrier; §8.3
  stronger conjecture ratified.
- **2026-06-24 (Mara, Alex, Pack)**: peer-ACL spec + garden-git spec
  + vocabulary cascade + Taut scout + Reed slingshot land at 13328a3.
- **2026-06-24 evening (Reed)**: pack{} dogfood at 8107caf.
- **2026-06-25 (Glint reflection)**: 3b31287; §5.2 flags `garden`
  block as sixth-block completing the decomposition; G6 candidate
  scout-role with two named replication conditions.
- **2026-06-25 early morning (Alex)**: "the mirror.spec is lambda 0."
  Recognition #99 candidate territory; Mara and Taut spawned in
  parallel.
- **Forward-promised**: Mara canonical recognition #99 spec
  (a99db67eaf8624b9f); Glint reflection essay on G6 adjudication
  with this brief as second data-point; Reed consolidation if garden
  slingshot ratified.

---

*Report end. Line count: ≤500 per brief discipline. Scout signed*
*Taut. Composes with Mara (a99db67eaf8624b9f) at canonical altitude.*
*G6 self-test: condition #1 weakly exercised; condition #2 not*
*exercised; candidate remains tracked, not promoted.*
