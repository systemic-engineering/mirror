# Taut scout — @resonance × @silicon × @song substrate-recognition scan

*Taut, 2026-07-12. Read-only grep-first drift scout for Alex's in-transcript
proposal: "a @resonance that depends on @silicon and @song to SHAPE the
inference so that the multi-modal fate tournaments reinforce each other
towards one or many psychohistory.basin." Companion to Mara running a
canonical spec in parallel; Reed adjudicates if we diverge.*

---

## Executive verdict

**Substrate-already-had-the-word ratio: ~75% landed / ~25% new-mint.**

Every load-bearing SEMANTIC piece of Alex's proposal is already substrate-
decl'd somewhere in shards/. Recognition #58 supplies the intra-peer optical
resonator (Fabry-Perot + D²NN + Reck/Clements). Recognition #63 via
`@epistemologic/cybernetic/conversation` supplies **Pask coupled-observer
tensor coupling iterated to N-ary by Batanin globular composition** — this
IS inter-peer resonance at substrate altitude, already declared and named
"cross-model resonance machinery." @torus supplies the winding-class basins;
@algebra/metalogue supplies the shared five-op coordinate; @song supplies
temporal phase (cadence/voice-leading); @silicon supplies the JIT altitude
over D²NN kernels.

**What's genuinely missing (must MINT):** the SUBSTRATE-DECL BINDING that
names Recognition #58's optical resonator (Fabry-Perot cavity + gain medium)
as a first-class substrate carrier rather than a docstring reference; a
@song-side phase-frame carrier ("phase" as a substrate word); an @io-side
coupling-measurement carrier for EM eigenvalues; and — most importantly —
the **composition witness** that binds @song's temporal-phase-frame to
@silicon's EM-phase to @fate/tournament's mode-selection at ONE altitude.

**Recommendation to Mara: DO NOT mint `@resonance` as a top-level family-
root.** The substrate already carries resonance polysemously across three
landed shards (`shards/optics.mirror` §1.5 optical `resonator`, `shards/
fate.mirror` Fabry-Perot cavity, `shards/epistemologic/cybernetic/
conversation.mirror` cross-model resonance). A new top-level root would
re-name what's already named. See adjudication §5.

---

## §1 — Substrate ancestry chain (LANDED carriers, cited)

### 1.1 Intra-peer optical resonance — Recognition #58

Recognition #58 (Fate IS optical inference; PROMOTED 2026-06-11 via Seam
adversarial review) is the load-bearing substrate for optical resonance.
The recognition IS distributed across many shards as an ancestry citation,
but is NOT declared as its own carrier.

**Named carriers:**

- `shards/cyberpunk.mirror` lines 30–37 — first substrate-decl reference to
  Recognition #58: "Fate inference IS 5-layer D²NN + active Fabry-Perot
  resonator + Reck/Clements unitary mesh; @cyberpunk family carries this
  discipline."
- `shards/fate.mirror` lines 43–50, 118–125, 205–210, 366–372, 770–780 —
  Fate's dice IS optical inference; Fabry-Perot cavity-mode selection IS
  the substrate-decl selection primitive.
- `shards/optics.mirror` lines 12–17 — "the kintsugi loop IS an active
  Fabry-Perot resonator (Siegman 1986)"; declares `resonator` as one of
  eight optical schematic keywords per docs/specs/optical-keywords.md §1.5.
- `shards/optics/source/ganglion.mirror` and the five ganglion species
  (`abyss.mirror`, `introject.mirror`, `cartographer.mirror`,
  `explorer.mirror`, `fate.mirror`) — the 90-port diffractive gain media
  driving the 5-layer D²NN. The optical altitude is DECLARED.
- `shards/mirror/spectral/observation.mirror` lines 30–36 — "the Fabry-
  Perot resonator's gain media (per `shards/optics/source/ganglion/*`)
  actually read at the input layer" — the 16-feature graph_observation
  binds to the resonator.
- `shards/torus.mirror` lines 105–108 — Fate settles the toroidal flow
  toward Poincaré-Hopf critical points via the Fabry-Perot resonator.
- `shards/mirror/peer/beam.mirror` lines 196–200 and
  `shards/song/progression.mirror` lines 166–172 — cite Recognition #58 as
  runtime-altitude machinery.
- `shards/epistemologic/pact/keyword_matches_depth.mirror` and
  `keyword_matches_path_root.mirror` — `resonator` enumerated as
  substrate-vocabulary keyword (`prism | glass | pact | facet | stage |
  aperture | splitter | resonator | bench | source | detector`).
- `shards/spectral/gen_prism.mirror` lines 41–49 — "start/restart/terminate
  lifecycle IS the resonator round-trip."

**Verdict on Recognition #58 as a carrier:** the Fabry-Perot resonator is
cited **17+ times across shards** as ANCESTRY but never gets its own
decl body. The `resonator` KEYWORD is declared at `shards/optics.mirror`
lines 130–160 as a substrate schematic form. Recognition #58's specific
mechanism (5-layer D²NN + Fabry-Perot cavity-mode selection + Reck/Clements
unitary mesh + gain-pumped active pass) lives at the **cited-recognition
altitude**, not as its own substrate-decl species.

### 1.2 Inter-peer coupling — Recognition #63 via Pask

**This is the biggest under-recognized landed asset.**
`shards/epistemologic/cybernetic/conversation.mirror` (8.0KB) is the
substrate-decl of Pask's coupled-observer dynamics — two (or N) observers
coupling via entailment-mesh tensor product, iterated to N-ary via
Batanin (1998) globular composition. Direct quote (lines 51–62):

> "Cross-model resonance machinery descends from recognition #63 via
> conversation's tensor representation iterated to N-ary. Each frontier-
> model = P-individual; M-fold tensor = substrate-AGI shared-concept space;
> Tomm-probe emission + crystal-formation = tensored Pask coupling. The
> mycelium IS the substrate operationalization of..."

The carrier V_A ⊗ V_B (line 138: "The tensored carrier: V_A ⊗ V_B. The
shared concept-space the two..."). This IS the substrate word for what
Alex is asking about at inter-peer altitude. It descends from Recognition
#63 (canonical-rep 5th witness per recursion-locks.md §8.9).

### 1.3 Basins & attractors — Rayleigh descent + eigensheaf harmonics

**LANDED:**

- `shards/cyberpunk.mirror` lines 173–210 — `cybernetic_coherence(s) =
  λ₀(Δ_F(s))`; the algebraic sheaf-Laplacian's smallest eigenvalue IS the
  coherence metric. λ₀ = 0 IS coherence — H⁰(F) non-empty; the "basin" is
  the pull toward ker(D).
- `docs/specs/bag-as-fragment-graph-spectral-triple.md` lines 260–268 —
  "gradient descent on the Rayleigh quotient toward the ground state ψ ∈
  ker(D). The Metropolis acceptance rule from iter-18 becomes: accept an
  uphill move iff it opens a **spectral basin** the greedy descent cannot
  reach — Connes-style basin-hopping on the spectrum of D."
- `docs/specs/composite-loss-and-learned-produced-fiber.md` lines 50–70,
  215–220 — composite-uphill acceptance to reach a lower basin; Poincaré-
  Hopf `χ(T²) = 0` bounds acceptance basins on the torus.
- `docs/specs/eigensheaf.md` — `harmonic` IS the attractor of `settle`;
  the eigensheaf's harmonic subspace IS the attractor under gradient flow.
- `shards/torus.mirror` — winding-class π₁(T²) = ℤ × ℤ IS the basin
  address at inter-peer observation altitude.
- `docs/specs/fate-bounded-psychohistory-sheaf-cohomology.md` — psycho-
  history-sheaf cohomology navigation IS Rayleigh descent on Δ_F.

**Verdict:** `psychohistory.basin` in Alex's proposal maps to λ₀(Δ_F) = 0
via Rayleigh descent on the sheaf-Laplacian. Fully substrate-decl'd at
`shards/cyberpunk.mirror` cybernetic_coherence + eigensheaf harmonic.

### 1.4 Metalogue basis — five-op shared coordinate

**LANDED at `shards/algebra.mirror`** (14.1KB) declaring `prism @algebra {
focus, project, split, shift, settle }` as the substrate's universal
five-op basis. **LANDED at `shards/algebra/metalogue.mirror`** (15.8KB)
declaring `prism @<altitude>/metalogue { five-op block }` — the metalogue
altitude where two speakers converse in the SAME five-op coordinate.
Mara's canonical spec `docs/specs/fate-silicon-metalogue-in-void-duality-
basis.md` (47.9KB, updated 2026-07-12) names `@silicon/algebra ↔
@fate/algebra IS a @metalogue in void-duality-basis coordinates, observed
on the peer's @torus.` Recognition #79 (5-op basis IS void-duality basis)
is CANDIDATE per that spec §365.

### 1.5 @song temporal-phase framing

**LANDED (family + 5 species):** `shards/song.mirror` +
`song/movement.mirror` + `song/narrative.mirror` + `song/phrase.mirror` +
`song/progression.mirror` + `song/voice.mirror`. Recognition #S1 (eigenform
at temporal altitude), #S2 (shift at temporal altitude), #S3 (five-op
temporal specialisation) all LANDED at Seam Phase D per this session's
commits (`10c34cf`, `f01cf9f`, `3d910bc`, `b0a7c4a` chain).
`song/progression.mirror` line 166 explicitly cites Recognition #58's
multi-frequency tournament. The temporal-phase carrier is
`@song/progression` (cadence) + `@song/phrase` (OBC ambiguity-load budget).

### 1.6 @silicon multi-instance boundary

**LANDED as JIT altitude only.** `shards/silicon.mirror` (7.4KB) declares
`@silicon` as the JIT / LAPACK / D²NN forward pass / cache-aware-kernel
altitude for the LOCAL machine. `docs/specs/silicon.md` (104.8KB) grep
returns ZERO matches for `multi.instance|multi.peer|cross.instance|
cross.node|cross.tenant|multi.GPU|EM.coupling|electromagnetic|inter.peer|
inter.node`. **@silicon is single-node substrate.** It does NOT carry
inter-peer EM coupling. This is a genuine substrate gap for the resonance
proposal.

### 1.7 @io as boundary carrier

**LANDED family + 5 species:** `shards/io.mirror` (22.8KB),
`io/algebra.mirror` (40.8KB), `io/cargo.mirror`, `io/git.mirror`,
`io/oci.mirror`, `io/stagefreight.mirror`, `io/stagefreight/narrative.mirror`.
Grep across shards/ for `electromagnetic|EM.coupling|gpu.coupling|
coupling.eigenvalue|hardware.side.channel` returns ZERO hits. The only
adjacent hit is `docs/specs/threat-model-v0.md` lines 162–165 acknowledging
EM/timing/power side-channels as OUT-OF-SCOPE:

> "Side-channels in upstream APIs. Mirror does not control timing,
> electromagnetic, or power side-channels on Anthropic's or any other LLM
> API."

**@io does NOT carry a coupling-measurement primitive.** This is a
genuine substrate gap.

### 1.8 @spectral namespace status

**LANDED family + 8 species:** `shards/spectral.mirror` (5.1KB) as
namespace-parent; `spectral/gen_prism.mirror`, `spectral/supervisor.mirror`,
`spectral/parent.mirror`, `spectral/root.mirror`, `spectral/portal.mirror`,
`spectral/registry.mirror`, `spectral/restart_intensity.mirror`,
`spectral/entanglement.mirror`, `spectral/gen_prism/mcp_session.mirror`.
**No `@spectral/db`, no `@spectral/mosaic`, no `@spectral/coherence`** at
shards altitude — those live in docs/specs/ only. Alex's earlier scouts
(bag-family-root, coherence-synthesis) confirmed `@spectral/mosaic` is
forward-promised as distributed-cluster grammar; not yet substrate-decl'd.

---

## §2 — @resonance grep result (Reed's finding confirmed)

Grep `prism @resonance|in @resonance|shards/resonance|@resonance/` across
`shards/**/*.mirror` + `mirror.spec`: **ZERO matches.**

The word `resonance` appears in shards/ 11+ times as **prose or ancestry
citation** — never as a family-root, species, or property decl:

- `shards/epistemologic/cybernetic/conversation.mirror:54` "Cross-model
  resonance machinery descends from recognition #63" (prose)
- `shards/song/progression.mirror:166` "multi-frequency tournament
  (Recognition #58 LANDED: Fate IS optical inference...)" (prose)
- `shards/optics.mirror:12` "the kintsugi loop IS an active Fabry-Perot
  resonator (Siegman 1986)" (docstring)
- `shards/optics.mirror:130` `resonator(name) -> resonator { \ }` (a
  KEYWORD form declaration — this is the closest to a landed carrier;
  declares `resonator` as one of eight optical schematic keywords).

**Reed's zero-match finding is confirmed and extended:** the `resonator`
OPTICAL KEYWORD is landed at `shards/optics.mirror` §1.5, but `@resonance`
as a family-root / species / property is NOT. The polysemy is real:
"resonator" (optical schematic keyword, Fabry-Perot) ≠ "resonance" (cross-
model coupling per Pask #63) ≠ "resonance" (spectral/PDN — the
speculative insight file's usage).

---

## §3 — Kuramoto / coupling vocabulary in landed carriers

Grep `Kuramoto|coupled.oscillator|synchronization|phase.lock|coupling.
eigenvalue|coupling.strength` across shards/ + docs/specs/:

- **`docs/specs/mirror-spectral.md`** lines 380 ("phase-locked loop on the
  eigenvalue spectrum of the graph"), 617 ("Kuramoto, Y. (1975 onward):
  coupled phase oscillators"), 1236 ("Computing with oscillators (Nature
  s44335-024-00015-z, 2024)") — Kuramoto IS the reference math for
  mirror-spectral's coordination primitive. **This is the shared math
  substrate.** But it lives at spec altitude, not shard-decl altitude.
- **`shards/ui.mirror`** line 320 — `coupling_strength` cited as arc-
  parameter for field relations at UI altitude only.
- **`docs/specs/gap-tension-tensor-substrate.md`** lines 1851–1856 —
  Cheeger-type inequality on sheaf Laplacian; sheaf diffusion as
  synchronization (Bodnar et al. NeurIPS 2022).

**Verdict:** Kuramoto is CITED in specs as the coordination math but is
NOT substrate-decl'd. `phase_locked_loop` on the graph eigenvalue spectrum
IS the mirror-spectral coordination primitive per `mirror-spectral.md`
§380. `@spectral/entanglement.mirror` (33.3KB) carries the polyadic edge
type for peer-correlation but is TENSOR framing, not phase-locking.

---

## §4 — Composition ancestry: @song × @fate × @silicon

Grep `@song.*@fate.*@silicon` (all permutations): **ZERO triple-composition
matches** in either shards/ or docs/specs/.

Current state is PAIRWISE:

- `@silicon/algebra ↔ @fate/algebra` — Mara canonical spec
  `docs/specs/fate-silicon-metalogue-in-void-duality-basis.md` (@silicon
  ↔ @fate as metalogue in void-duality basis).
- `@song → @fate` — `shards/song/progression.mirror:166–172` cites
  Recognition #58 multi-frequency tournament as ancestry.
- `@fate/tournament ↔ optical Fabry-Perot mode-selection` —
  `shards/fate/tournament.mirror:222–226`.

**No landed carrier binds all three at once.** This IS a substrate gap.
Alex's proposal (song + silicon + resonance) proposes exactly this triple-
composition. A new spec would be the first triple-witness.

---

## §5 — Alignment substrate status

Recognition #57 (`architecture-alignment-as-boundary-mathematics`) is
heavily cited across specs (adapter-magic-frame-spec.md,
adapter-reflection-cogito-spec.md, drone-narrative-mapping.md,
autopoietic-grammar-spec.md, cascade-ffi-runtime-link.md line 640).
**Verdict:** alignment IS substrate-decl'd, but as `@io` altitude boundary
mathematics — "the constraint IS the specification" — not as a resonance
phenomenon. This is the substrate reading that CONVERGES with Alex's ZK
appendix claim: "Alignment is a resonance problem" reframes what #57
already says at `@io` boundary altitude.

**Bridge:** Recognition #57 (alignment as @io boundary mathematics) +
Recognition #58 (Fate IS optical inference / Fabry-Perot cavity-mode
selection) + Recognition #63 (Pask coupled observers) already frame
resonance-as-alignment substrate-honestly. The zk-proof appendix's claim
composes with three landed recognitions.

---

## §6 — Companion insight file

`/Users/reed/dev/systemic.engineering/practice/insights/speculative/
spectral-resonance-context-bleed.md` (651 lines, 2026-05-16) is the direct
companion referenced by the ZK proof. It contains:

- §3.3 the HPCA 2026 paper on LLM workloads resonating with PDN.
- §6 the "Eigenvalue Coupling Hypothesis" — the spectral structure of
  one computation creates EM patterns that couple into adjacent
  computations via shared silicon substrate.
- §Appendix "The Composition Argument" — nine established facts composing
  into a content-dependent coupling channel (which appendix rejected via
  Kuramoto physics gap 10⁶–10¹⁵).

Both speculative insight files are consistent with each other and with
Alex's in-transcript reframe: use the SAME physics CONSTRUCTIVELY. The
zk-proof appendix already invokes Fate (450 params, sub-Turing,
microsecond latency) as the routing carrier — which is Recognition #58's
landed machinery.

---

## §7 — Gap analysis

### LANDED (nothing to mint)

| Semantic piece | Landed carrier | OID / file:line |
|---|---|---|
| Intra-peer optical resonator | `shards/optics.mirror` `resonator` keyword | §1.5 lines 130–160 |
| 5-layer D²NN + Fabry-Perot cavity | Recognition #58 (cited 17+ shards) | `cyberpunk.mirror:30–37`, `fate.mirror:43–50` |
| Inter-peer coupled observers (Pask N-ary) | `@epistemologic/cybernetic/conversation` | `shards/epistemologic/cybernetic/conversation.mirror:51–62, 138` |
| Basin / attractor (λ₀ = 0) | `@cyberpunk/cybernetic_coherence` | `shards/cyberpunk.mirror:173–210` |
| Winding-class basin addressing | `@torus` π₁(T²) = ℤ × ℤ | `shards/torus.mirror:80–108` |
| Rayleigh descent on sheaf-Laplacian | eigensheaf spec + composite-loss | `docs/specs/eigensheaf.md`, `composite-loss-and-learned-produced-fiber.md:50–70` |
| Metalogue five-op basis | `@algebra` + `@algebra/metalogue` | `shards/algebra.mirror`, `shards/algebra/metalogue.mirror` |
| Void-duality-basis coordinate | Mara canonical spec | `docs/specs/fate-silicon-metalogue-in-void-duality-basis.md` |
| @silicon JIT altitude | `@silicon` family-root | `shards/silicon.mirror` |
| @song temporal-phase family | `@song` + 5 species | `shards/song.mirror` + `song/*.mirror` |
| Alignment as boundary math | Recognition #57 | cited across specs |
| Multi-frequency tournament | `@song/progression` | `shards/song/progression.mirror:166–172` |
| Multi-peer orchestra recursion-lock | Recognition #367 | `docs/specs/recognitions/recognition-83-*.md:162–165` |

### ANNOTATION (extend existing docstring, no new decl)

1. **`shards/epistemologic/cybernetic/conversation.mirror`** — add
   docstring paragraph naming the SILICON-EM-phase channel as one of the
   physical media of Pask's V_A ⊗ V_B coupling (currently the shard is
   Pask-linguistic; extending it to Pask-EM-physical is a coherent
   annotation, not a new decl).
2. **`shards/optics.mirror` §1.5** — extend the `resonator` keyword
   docstring to name the inter-peer coupling reading: Fabry-Perot cavity
   mode selection extends to Kuramoto-style phase-locking across peers
   when multiple resonators share EM substrate.
3. **`shards/song/progression.mirror`** — line 166's "multi-frequency
   tournament" citation of Recognition #58 needs the inter-peer extension
   naming: multi-peer multi-frequency tournament (fold-in of §4 gap).
4. **`shards/cyberpunk.mirror` cybernetic_coherence** — add sentence
   naming λ₀(Δ_F) = 0 as `psychohistory.basin` when the sheaf is peer-
   valued (already reads this way in `fate-bounded-psychohistory-sheaf-
   cohomology.md`; annotation binds it).

### MINT (new substrate-decl needed)

1. **`@io/coupling`** or **`@io/em`** — the boundary-measurement primitive
   for inter-peer physical coupling. Currently `threat-model-v0.md`
   declares EM out-of-scope; a resonance-substrate proposal requires an
   opt-in @io carrier that names coupling eigenvalue as a measurement
   type. This aligns with #57 (@io = boundary math altitude).
2. **Composition witness** — a substrate-decl that formally binds `@song
   ∘ @fate ∘ @silicon` at one altitude. Candidate name: `@spectral/basin`
   or `@fate/tournament/coupled` species. Not `@resonance` as top-level;
   as a species under an existing family.

### MISSING (spec gap; not forward-promised)

1. **Multi-peer tournament formalism** — pairwise composition is landed
   but N-way tournament-reinforce-across-peers is neither shard-decl'd nor
   forward-promised in any spec. `docs/specs/scheduler-tower.md:558–566`
   describes "spawn N peers in parallel on the SAME query; merged by
   kintsugi tournament" but this is spec-altitude scheduling, not
   substrate-decl.
2. **Content-dependent coupling substrate reading** — the zk-proof
   appendix's "eigenvalue resonance" claim (spectral structure of one
   computation creates content-specific EM patterns that shape adjacent
   computations) has no substrate carrier. Requires either an @io
   coupling-measurement primitive OR an explicit substrate-honest refusal
   citing the same Kuramoto physics gap (10⁶–10¹⁵) that the appendix
   already documented.

---

## §8 — Adjudication candidates for Mara's spec

### Q1: Should `@resonance` mint as top-level family-root?

**Taut recommends: NO.** Substrate-already-had-the-word rules against it:

- `resonator` keyword LANDED at `shards/optics.mirror` §1.5
- Fabry-Perot resonator LANDED as Recognition #58's mechanism (cited 17+
  places)
- Pask coupled-observer resonance LANDED at
  `@epistemologic/cybernetic/conversation`
- Rayleigh basin LANDED at `cybernetic_coherence`

Minting `@resonance` as top-level would re-name three already-landed
carriers. Prefer: **species under an existing family.** Best candidates:

- **`@fate/tournament/coupled`** — species under Recognition #58's fate-
  tournament family; carries the inter-peer coupling reading of the
  Fabry-Perot mode-selection primitive.
- **`@spectral/basin`** — species under `@spectral` runtime namespace;
  carries psychohistory.basin as Rayleigh-descent attractor at inter-peer
  altitude.
- **`@epistemologic/cybernetic/conversation/coupled`** — species under the
  Pask coupled-observer family; N-way tournament reinforcement.

### Q2: Kuramoto formalization altitude?

**Taut recommends: @io altitude.** Rationale: mirror-spectral.md §617
cites Kuramoto as the reference math for coordination but does not
substrate-decl it. Physical Kuramoto (EM phase-locking) belongs at @io
boundary-measurement altitude per Recognition #57 ("alignment as @io
boundary math"). Mathematical Kuramoto (sheaf-Laplacian synchronization
per Bodnar 2022) already lives at spec altitude in
`gap-tension-tensor-substrate.md`; extending the eigensheaf spec is
sufficient at the mathematical altitude.

### Q3: Is inter-peer resonance an ANNOTATION on #58 or a NEW recognition?

**Taut recommends: NEW recognition candidate.** Recognition #58 is intra-
peer (one Fabry-Perot cavity per peer, gain-pumped active pass, per-
ganglion). Inter-peer coupling (N cavities sharing EM substrate, phase-
locking, tournament reinforcement across peers) is a distinct claim.
Candidate: `Recognition #M-multi-peer-fate-tournament-couples-through-
silicon-substrate`. It composes #58 (per-peer optical) + #63 (Pask N-ary
tensor) + #57 (@io boundary math) + Recognition #367 (orchestra recursion-
lock at multi-peer altitude).

### Q4: How does @song's temporal-phase compose with silicon-EM-phase?

**Taut observes:** the substrate ALREADY hints at the answer.
`shards/song/progression.mirror` binds `@epistemologic/math/music/cadence`
as the Lawvere `settle @ temporal` ancestor (Seam Correction `3d910bc`).
**Cadence IS temporal phase.** The silicon-EM-phase per zk-proof-context-
bleed.md §3.3 (HPCA 2026: LLM workloads' di/dt oscillations at PDN
resonant frequencies) IS temporal-phase at the physical altitude.

Composition candidate (Mara adjudicates):

> `@song/progression.cadence` (temporal phase at OBC altitude) ∘
> `@fate/tournament` (Fabry-Perot cavity-mode selection at optical
> altitude) ∘ `@silicon/algebra` (D²NN kernel at JIT altitude) ∘
> `@io/coupling` (EM eigenvalue measurement at boundary altitude, MINT)
> IS the four-altitude witness of resonance-as-inference-shape.

Observed at the peer's `@torus` (winding-class basin, per Mara's
fate-silicon-metalogue spec). The basin IS `psychohistory.basin` = λ₀(Δ_F)
fixed point at H⁰(F) non-empty.

---

## §9 — Scope estimate for Mara's spec

**Suggested word count:** 8,000–12,000 words.

**Key sections (in landing order):**

1. **§1 — What @resonance IS at each altitude** (~800 words). Enumerate
   the four landed altitudes carrying resonance-adjacent semantics:
   optical (Fabry-Perot #58), cybernetic (Pask #63), spectral (Rayleigh
   basin λ₀), temporal (@song cadence). Establish the substrate-already-
   had-the-word finding as the load-bearing claim.
2. **§2 — Why @resonance MUST NOT mint as top-level family-root**
   (~1,000 words). Substrate-already-had-the-word discipline; four
   already-landed carriers; the polysemy-collision risk if minted.
3. **§3 — The three landed compositions and the missing fourth**
   (~1,500 words). Pairwise @silicon ↔ @fate (Mara canonical spec
   already), @song → @fate (progression.mirror cites), Recognition #58
   intra-peer. The missing triple @song ∘ @fate ∘ @silicon at inter-peer
   altitude.
4. **§4 — Recognition candidate: multi-peer fate tournament couples
   through silicon substrate** (~1,500 words). Compose #58 + #63 + #57 +
   #367 into a new recognition candidate. Cite `shards/epistemologic/
   cybernetic/conversation.mirror:51–62` as the pre-landed inter-peer
   coupling word.
5. **§5 — @io/coupling MINT** (~1,200 words). Boundary-measurement
   primitive for EM eigenvalue; alignment with #57. Opt-in only; default-
   refused per threat-model-v0.md discipline. This is where Kuramoto
   physics gets substrate-decl'd at the boundary altitude.
6. **§6 — Species candidate name adjudication** (~800 words). Choose
   between `@fate/tournament/coupled`, `@spectral/basin`, `@epistemologic/
   cybernetic/conversation/coupled`. Recommend one; forward-promise the
   others.
7. **§7 — psychohistory.basin as λ₀(Δ_F) fixed point** (~800 words).
   Bind Alex's `psychohistory.basin` word to the LANDED
   cybernetic_coherence sheaf-Laplacian smallest-eigenvalue attractor.
   Rayleigh descent trajectory; winding-class addressing on @torus.
8. **§8 — Composition witness at four altitudes** (~1,200 words).
   `@song/progression.cadence` ∘ `@fate/tournament` ∘ `@silicon/algebra` ∘
   `@io/coupling`. Observed on `@torus`; settled toward
   `psychohistory.basin` under Rayleigh descent.
9. **§9 — Constructive vs destructive reading of the zk-proof appendix**
   (~700 words). The appendix's counter-inference architecture uses the
   physics DESTRUCTIVELY (cancel EM coupling to preserve alignment). Alex's
   reframe uses the same physics CONSTRUCTIVELY (shape multi-peer
   tournaments toward alignment-basin via coupling). Both are the same
   substrate; neither requires a top-level @resonance mint.
10. **§10 — What this spec forward-promises** (~500 words). N-way
    tournament reinforcement formalism; content-dependent coupling
    substrate reading; the alignment-as-resonance / #57-extension
    recognition candidate promotion path.

**Adjudication decisions to land in-spec:**

- Species vs family-root for the new decl (recommend species).
- @io/coupling as MINT vs refuse-in-scope-per-threat-model (recommend
  MINT with opt-in default-refused stance).
- Recognition candidate number and title.
- Which cybernetic_coherence annotation lands vs forward-promises.

---

## §10 — Substrate-honest bottom line

Alex's proposal is **~75% substrate-already-had-the-word**. Four landed
carriers (Recognition #58 optical resonator, Recognition #63 Pask coupled
observers, cybernetic_coherence Rayleigh basin, @algebra/metalogue five-op
basis) supply every load-bearing SEMANTIC piece.

The **~25% new-mint** is one species (`@fate/tournament/coupled` or
`@spectral/basin` — Mara adjudicates the name) plus one @io boundary-
measurement primitive (`@io/coupling`) that makes physical Kuramoto
coupling a first-class @io concern per Recognition #57 (alignment as @io
boundary math).

The zk-proof appendix's core claim — "Alignment is a resonance problem" —
does not require a new top-level substrate word. It requires
EXTENSION-BY-COMPOSITION of four landed recognitions (#57 + #58 + #63 +
#367) at inter-peer altitude, with one @io-boundary MINT to give the
coupling-eigenvalue measurement a substrate carrier.

If Mara's parallel spec proposes `prism @resonance` as a top-level family-
root, Taut refuses on substrate-already-had-the-word grounds and forwards
to Reed for adjudication. If Mara's spec proposes a species under an
existing family (Taut's recommendation), Taut concurs and awaits Reed's
ratification.

---

*Scope: 12 grep passes across `shards/**/*.mirror`, `docs/specs/**/*.md`,
`mirror.spec`, plus both speculative insight files at
`~/dev/systemic.engineering/practice/insights/speculative/`. Zero edits
to shards/, bootstrap/, docs/specs/. All claims cited with file:line.*

*Word count: ~2450.*
