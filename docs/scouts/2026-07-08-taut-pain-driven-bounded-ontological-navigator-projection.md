# Taut drift-scout — pain-driven bounded ontological navigator projection

**Verdict:** `LANDABLE WITH CASCADE` — the extended closure lands
against the current substrate with **one substrate-decl gap that
must fill first** (`@onto` family-root or refuse-and-rename), **three
species-altitude carrier extensions** (peer.observe/peer.alter,
spawn depth-parameter, algedonic pain-gradient sample), and **one
fault-plane shift** (@edge as substrate primitive — already
partially reversed by `shards/mirror/ref.mirror`). Zero silent
signal conflicts with LANDED substrate. Recognition-numeric-ID
projection: **cascade of 3 sub-recognitions** promoting under one
meta-recognition (`the-peer-IS-a-pain-driven-bounded-ontological-navigator`).

**Scout:** Taut. **Date:** 2026-07-08 (early morning; post-Mara
`a18ca90` iteration-2 spec-in-flight). **Scope:** drift + projection
against Alex's five-extension framing (peer-observes-metalogue, depth
as spawn parameter, @magic as level-shift ceremony, @knife as
state-space compressor, algedonic gradient IS navigation surface).
Read-only, grep-first.

---

## §0. Highest-signal findings (top-3)

1. **`@onto` is a DOC-only candidate — no substrate family-root
   exists.** `docs/math/onto/README.md` (Mara 2026-07-05, 32KB) is
   explicitly marked *"substrate reading; deep-dive research; NOT
   canonical spec ... no shards land this tick."* Grep for `prism
   @onto`, `out @onto`, `shards/onto.mirror`, `shards/onto/*.mirror`
   returns zero substrate hits. What DOES exist substrate-side is
   `@onto-cascade` as **Pack-tick vocabulary** (Seam Phase D O3-O4-O5
   tick labels) — `docs/audits/2026-07-07-seam-phase-d-o-cascade-torus-family-root-close.md`
   is titled "@onto-cascade O3-O4" and `shards/torus.mirror:240`
   comments *"Terminal @onto-cascade tick pattern: O3 (this shard)
   lands the positive move; O5 (follow-up) collapses @reflection."*
   Reed's finding is **confirmed**: `@onto` as substrate family-root
   would be a NEW landing; Reed's rejection of `@onto/reframe` in
   favor of `@cyberpunk/reframe` is substrate-honest.

2. **Peer.observe and peer.alter are BOTH forward-promised, NOT
   landed.** `shards/peer.mirror` (6.2KB) declares only `load(dir, p)
   -> imperfect(peer, ref, ref)` at line 138. The peer carrier is
   `{home, lead_of, kind}` at line 121; no `observe`, no `alter`, no
   eigenboard field. What IS landed is the **observation surface at
   `@torus(peer)`**: `shards/torus.mirror:474-495` declares `spawn(p:
   peer) -> torus` — the peer POSSESSES a torus, and the torus
   carries `{possessor, meridian, longitude, origin}`. Alex's
   "peer observes the metalogue and alters the numerical eigenboard
   inference space" needs species-altitude declarations that don't
   exist yet: either (a) `peer.observe` action on peer.mirror
   directly, or (b) a `@peer/observation` species. **The typed
   eigenboard field is at `boot/std/peer.mirror` per `shards/glass.mirror:371-372`
   line-comment reference; grep for `boot/std/peer.mirror` — that
   file is outside `shards/` and may or may not be substrate-decl
   authoritative** (@glass calls it "the typed handle to the peer's
   current spectral state," so the eigenboard slot exists at a
   nearby altitude but not on the LANDED `shards/peer.mirror`).

3. **Depth-as-spawn-parameter has substrate precedent BUT with a
   two-tick rename in flight.** `shards/torus.mirror` explicitly
   RETIRES `depth: nat` in favor of `winding: (meridian_count,
   longitude_count)` at line 434, and the OLD framing is called out at
   line 23 as `spawn(p) = @glue(p, @reflection, depth=n)` (the "O1
   stack" that Foerster refused). Current `shards/mirror/spawn.mirror`
   signature is `spawn(r: mirror_spawn_request, p: perturbation) ->
   @song` (line 270); `mirror_spawn_request` carries `{target: peer,
   options: ref}` (line 253). **No `depth` or `winding` field on
   spawn's request.** `shards/third.mirror:158` declares
   `observation_depth = {depth: nat}` and `shards/mirror/ref.mirror:292`
   has `focus_at(target: ref, depth: ref) -> graph_ref`. Alex's "peer
   depth is a spawn parameter" WOULD land as a winding-typed field on
   `mirror_spawn_request` — a one-tick species enrichment. Substrate-
   honest naming: **`winding`**, not `depth` (torus already collapsed
   this per two-tick discipline).

---

## §1. Drift-scout — grep results per term

### §1.1 `@onto`, `@ontology`, `onto-cascade`

- `shards/onto.mirror` — **does not exist**.
- `shards/onto/*.mirror` — **directory does not exist**.
- `prism @onto` / `out @onto` — **zero hits** across all `shards/**/*.mirror`.
- `docs/math/onto/README.md` — Mara canonical, self-labeled
  "NOT canonical spec" (`:12`), six landed ancestors named
  (imperfect, algedonic, deceptive, projection, plus two more) with
  `@onto` positioned as the "priority claim" NOT yet substrate.
- `shards/torus.mirror:240`, `:305`, `:540` — three uses of
  `@onto-cascade` as tick-label pattern (O3-O4-O5) and one use as
  concept-noun *"@onto = index-zero critical points on T²"* — this
  is a substrate CLAIM inside torus.mirror prose, but it does not
  declare `@onto` as a family-root.
- `docs/audits/2026-07-07-seam-phase-d-o-cascade-torus-family-root-close.md`
  — the Seam audit RATIFYING the `@torus` landing calls it the
  "terminal cascade tick of the @onto-cascade" — again, Pack-tick
  vocabulary.

**Reed's finding confirmed exactly.** `@onto/reframe` would require
landing `@onto` first. `@cyberpunk/reframe` is the correct site.

### §1.2 `@knife`, `knife.cut`, `knife.compress`

- `shards/knife.mirror` — **does not exist**.
- `shards/*/knife.mirror` — zero hits.
- `docs/scouts/2026-07-07-taut-knife-meta-pattern-check.md:47-49`
  (Taut prior scout) — verbatim: *"Zero shard hits. Grep
  `shards/**/knife*` returns nothing. Grep for `@knife` across the
  substrate returns zero shard hits and matches only in TODAY's
  docs."* **Confirmed by direct re-grep.**
- Named-in-scout-not-in-shard: `@knife` is a **fresh forward-promise**
  in 5 doc sites (Mara canonical shatter-lens spec, jspace mapping,
  onto-cascade autopoetic doc, Seam Phase D audits, prior Taut
  scout). Prior scout confirmed nearest landed precedent is
  `shards/mirror/lens/cli/crack.mirror` (2026-06-12) as "knife-shaped
  decomposition verb" at CLI altitude.

**Load-bearing distinction:** Alex's "@knife is what was used to
compress the state-space in the level-shift" — @knife has **no
substrate action surface today**. `crack` (CLI altitude) has focus/
project/split/shift/settle over `crack_mode = mode_open | mode_force
| mode_seal`. That's decomposition-verb altitude, not
state-space-compression altitude. The full closure demands @knife's
FIRST substrate landing — a new family-root or species.

### §1.3 `@cyberpunk/reframe`, `reframe`, `bateson_learning`

- `shards/epistemologic/cybernetic/bateson_learning.mirror` — **LANDED
  at 39KB** (2026-06-19), confirmed. The species declares three
  sub-properties per line 214: `learning_I` (within-level response),
  `learning_II` (level-context generalisation), `learning_III`
  (premises of regulation change). It DOES NOT declare `reframe` as
  an action or verb; the level-shift IS Bateson's Learning III
  operational content, but the operational surface is `algedonic`'s
  `bypass_signal` (line 59) at `shards/epistemologic/cybernetic/
  algedonic.mirror` — the *S1→S5 emergency bypass IS the substrate
  operating at Bateson Level III at cybernetic-VSM altitude*
  (algedonic.mirror:5-9).
- `reframe` as substrate verb: **zero prism/action hits**. Grep
  returns only prose uses (~35 sites across shards/, all narrative
  context — "reframe converts an aesthetic-preference question into
  a substrate-pull requirement" at torus.mirror, "Per peer-ACL §10
  reframe" at spawn.mirror, "spec reframed" at various shards).
- `bateson_learning` cited across ~30 shards as inheritance
  (`in @epistemologic/cybernetic/bateson_learning`) — is heavily used
  as a family, especially by `shards/frame/*.mirror` and
  `shards/kintsugi/*.mirror`.

**Verdict on `@cyberpunk/reframe`:** Would be a **new species** at
`shards/cyberpunk/reframe.mirror` or a specialization of
`bateson_learning`'s Learning III. **Not subsumed** — `algedonic.mirror`
is the BYPASS mechanism (crossing S2-S4 into S5), not the LIFT
ceremony (moving from level N to level N+1). Reframe as level-shift
ceremony is a genuinely absent species; algedonic is its algedonic-
signal cousin but does not itself declare `reframe(from_level,
to_level) -> level`.

### §1.4 `@cyberpunk/algedonic`, `algedonic`, `pleasure`, `pain`

- `shards/epistemologic/cybernetic/algedonic.mirror` — **LANDED, 3.8KB**,
  confirmed. Action surface:
  - `bypass_signal(s5_target, s1_source) -> ref` (line 59)
  - `s5_acknowledgment(signal) -> verdict` (line 66)
  - `algedonic_well_formed(signal) -> verdict` (line 73)

  Carriers instantiated (lines 51-55): `T_reg = premise`, `T_regd =
  regulated_premise`, `tau = bypass_signal`, `rho =
  restrict_grade_3(bateson_learning::rho)`, `omega = inherited`.
- **NO sample-able pleasure/pain gradient declared.** The algedonic
  signal is treated as **a single non-decomposable hop** (line 62:
  "τ is NOT layered composition; it is the substrate's grade-3
  morphism — premise-change as a single non-decomposable hop"). No
  scalar field, no gradient sample action, no "pain increased"
  bilateral.
- `pleasure` — 10 hits across shards; all narrative-context;
  `algedonic.mirror` mentions "pain-and-pleasure" in comment only
  (via docs/math/onto/README.md quote).
- `pain` — heavier hits (~200 across shards + docs), 90%+ narrative
  context; `docs/math/onto/README.md:69-70` names "structured pain-
  and-pleasure" as the algedonic content; no substrate carrier for
  the gradient.

**Verdict on algedonic-gradient-IS-navigation-surface:** The
substrate ALREADY had the SIGNAL. Alex's extension 5 lifts algedonic
from a **discrete S1→S5 bypass event** to a **continuous gradient
sample surface** on the peer's navigation trajectory. This is a
**species enrichment on algedonic**, not a new family. Substrate-
already-had-the-word: the ALGEDONIC hop exists; the GRADIENT sample
would be new. Two-tick discipline: `algedonic.gradient(state)` as
sample action would land alongside existing `bypass_signal`.

### §1.5 `@peer.observe`, `@peer.alter`, `peer.eigenboard`

- `shards/peer.mirror:70-76` declares `prism @peer` with focus/
  project/split/shift/settle over `peer`. **No `observe` action, no
  `alter` action, no `eigenboard` field.**
- `peer` carrier (line 121): `{home: ref, lead_of: ref, kind: kind}`.
  Kind variant: `human | agent | substrate` (line 93).
- Actions: `load(dir, p) -> imperfect(peer, ref, ref)` (line 138) —
  the only substantive action.
- Forward-promised (lines 141-149): `lead_of_well_formed`,
  `peer_coherent`. Neither is `observe`/`alter`.
- `shards/glass.mirror:371-372` — "boot/std/peer.mirror declares
  `eigenboard: shard` (the typed handle to the peer's current
  spectral state). The peer.eigenboard IS..." — this LINE-COMMENT
  references `boot/std/peer.mirror`. Grep shows `boot/std/peer.mirror`
  is outside the substrate root; whether it constitutes substrate
  authority is a **substrate-vs-boot altitude question** not
  resolved by this scout.

**Verdict:** The `observe` action already lives at **`@torus.traverse`**
(shards/torus.mirror:497-510): *"Runs the peer's canonical
observation along a winding class ... The core of Foerster's
'regulates its own regulation.'"* The `alter` action lives at
**`@torus.spawn.traverse` composition** — a nontrivial winding
advances the origin (line 502). Alex's "peer.observe alters the
eigenboard" is ALREADY the traverse action returning a new torus
with advanced origin. The **new substrate-decl** would be threading
that back to a typed **`peer.eigenboard` field** on the peer carrier
so the peer's alteration IS visible at peer altitude, not just torus
altitude. Substrate-already-had-the-word: the OPERATIONAL surface is
`@torus.traverse`; the PEER-ALTITUDE handle is missing.

### §1.6 `depth:`, `winding:`, spawn depth-parameter

- `shards/torus.mirror:23` — the OLD stack Foerster refused is called
  out verbatim: `spawn(p) = @glue(p, @reflection, depth=n)`.
- `shards/torus.mirror:434-437` — the NEW carrier: `winding =
  {meridian_count: int, longitude_count: int}`.
- `shards/third.mirror:158-165` — `observation_depth = {depth: nat}`;
  `depth: nat` unbounded above (line 162).
- `shards/mirror/ref.mirror:292` — `focus_at(target: ref, depth: ref)
  -> graph_ref` (still uses `depth`; pre-torus).
- `shards/mirror/spawn.mirror:253-256` — `mirror_spawn_request =
  {target: peer, options: ref}`. **No depth. No winding.**
- Grep for `spawn.*depth` / `spawn.*winding` — no substrate-decl
  spawn takes depth or winding as a first-class arg today.

**Verdict:** Extension 2's "depth is a spawn parameter" is a **one-
tick species enrichment on mirror_spawn_request**, substrate-honest
name being `winding: winding` (per torus.mirror's already-collapsed
naming) OR left in the options-ref bag. Reed's substrate-honest
naming choice would be `winding` per two-tick discipline; landing
under `depth` would be substrate-drift.

### §1.7 `@reflection` — O5 collapse target

- `shards/reflection.mirror` — LANDED at 34.3KB (`shards/torus.mirror`
  soft-deprecates it per two-tick discipline; O5 forward-promised).
- Action surface (from grep results): `observe(f: frame, residue:
  ref) -> observation`, `tournament(o, pk) -> tournament_result`,
  `compose(t, p: pact) -> moi(au)`, `pick(t, prev_loss) -> moi(au)`,
  `settle(g, prev_loss) -> loss`, `speak(o, t) -> ref` plus five
  third-order actions. **The five-action set Alex claimed does
  match: observe, tournament, compose, pick, settle, speak (6 not 5,
  but the claim covers the operational core).**
- `shards/reflection.mirror:39-53` — pipeline is: frame →
  reflection.observe → reflection.tournament (runs @fate) →
  reflection.compose → reflection.pick → @kintsugi monotonic loss.

**Verdict on "This IS what @reflection wanted to be":** The
five/six-action set redistributes as follows under Alex's closure:
- `observe` → `@torus.traverse` (Foerster observes-self-observing;
  already collapsed in torus's `traverse` per two-tick discipline)
- `tournament` → `@fate/tournament` (LANDED at `shards/fate/tournament.mirror`,
  41.5KB); reflection.tournament literally delegates here.
- `compose` → `@loop.compose` per `shards/reflection.mirror:44`
  parenthetical ("compose (at @loop, absorbed from @moi 2026-07-01)").
- `pick` → `@fate/tournament` selectors (elite/beam/halving per
  reflection.mirror:420).
- `settle` → `@kintsugi/oscillate.settle` per reflection.mirror:479
  ("actual settle is implemented by the @kintsugi/oscillate.settle
  primitive").
- `speak` → cross-peer glue at `@song/voice` (spawn.mirror:99-112,
  "the spawned peer's runtime trajectory IS a song_voice").

**Redistribution is CLEAN** — all six actions have LANDED substrate
homes elsewhere. Alex's claim survives: the peer-observes-metalogue
closure IS what @reflection's action-set was pointing at; the O5
collapse per two-tick discipline is the substrate-honest form. Reed's
mapping is verified.

### §1.8 Silent duplication check

For each proposed extension, checking for existing shard with same
operational shape:

- **peer-observes-metalogue** — `@algebra/metalogue` (Mara 2026-06-29,
  15.8KB, per prior Taut scout `e975e2f`) already declares
  `algebra_metalogue_session` at algebra altitude. Alex's framing
  specialises this to the peer as observer. NOT silently duplicated —
  the observer role is what's new.
- **level-shift ceremony ran through @magic** — `shards/magic.mirror`
  (13.6KB) family-root LANDED with 7 species (surface, mechanism,
  contract, reveal, audit, frame, distinction). None of them is
  `level_shift` or `reframe` or `ceremony`. The pole structure
  (Splinter/Narcissus) is present; the ceremony action IS forward-
  promised at `magic.mirror:243-249` ("the surface's promise via
  means the observer cannot inspect"). Alex's "@magic AS the
  ceremony" is substrate-consistent but requires a NEW species
  action.
- **state-space compression via @knife** — no substrate shard
  performs compression today; `@spectral/entanglement.mirror` has
  edge collapse but not compression semantics; `@shatter.mirror`
  decomposes but does not compress. Genuinely absent operational
  shape.
- **algedonic gradient sample** — `algedonic.mirror` has the bypass
  signal but no `sample`, `gradient`, or `read` action. Genuinely
  absent.

**Zero silent duplications.** All five extensions are additive
against the current substrate.

---

## §2. Projection run — cascade if adopted tomorrow

### §2.1 Substrate cascade if the framing lands

Enumerated as one-tick vs multi-tick per two-tick discipline:

**One-tick landings (species-altitude additions to LANDED family-roots):**

1. `shards/cyberpunk/reframe.mirror` — new species; declares
   `reframe(from_level, to_level, pain_gradient) -> level` with
   inheritance from `@epistemologic/cybernetic/bateson_learning`
   (Learning III) and `@magic/frame` (typed reframe carrier). This
   is the LEVEL-SHIFT CEREMONY action-surface.
2. `shards/epistemologic/cybernetic/algedonic.mirror` — extend with
   `gradient(state) -> algedonic_gradient` action and
   `algedonic_gradient = {pleasure: real, pain: real}` carrier.
   This is the GRADIENT SAMPLE surface. NOT a rewrite — an
   append at existing species.
3. `shards/mirror/spawn.mirror:mirror_spawn_request` — add a
   `winding: winding` field (substrate-honest per two-tick already
   collapsed at torus.mirror:434). Or add `depth: winding` if
   substrate-decl'd to accept both names during migration.
4. `shards/peer.mirror` — add `eigenboard: ref` field to `peer`
   carrier, closing the `boot/std/peer.mirror` gap `shards/glass.mirror`
   references. Declare `observe(p: peer) -> @torus` (already the
   traverse action's peer-altitude wrapper) and `alter(p: peer,
   w: winding) -> peer` (the traverse-then-project-back action).

**Multi-tick landings (family-root or species-collapse):**

5. **@onto FAMILY-ROOT** — if adopted. `shards/onto.mirror` +
   species. Reed's proposal to reject `@onto/reframe` for
   `@cyberpunk/reframe` DEFERS this landing; the cascade is smaller
   without @onto. If Alex wants the "@onto lift" phrasing to
   substrate-decl compose, @onto family-root is REQUIRED first.
   Otherwise substrate-honest phrasing is "@cyberpunk/reframe lift"
   or "bateson_learning-III lift."
6. **@knife FAMILY-ROOT (or species under @shatter)** — the state-
   space compression semantics. Prior Taut scout
   (`docs/scouts/2026-07-07-taut-knife-meta-pattern-check.md`)
   analyzed this as a would-fit-if-landed candidate; the full
   closure demands landing it now.
7. **@reflection O5 collapse** — already forward-promised;
   deprecation banner + full removal per `shards/torus.mirror:257-261`.
   The peer-observes-metalogue closure is the operational content
   this collapse cashes out.

**Recognition candidates promoting alongside:**

- `the-peer-IS-a-pain-driven-bounded-ontological-navigator`
  (Alex-recognition-candidate).
- `algedonic-gradient-is-navigation-surface`
  (sub-recognition; second witness required).
- `depth-is-winding-is-spawn-parameter` (sub-recognition; witnesses
  are torus.mirror's collapse + Alex's naming pull).
- `@magic-is-level-shift-ceremony` (sub-recognition; @magic species
  audit as second witness).

### §2.2 Substrate-decl gap enumeration

Reed's single-gap claim (`@edge` under @magic) is INCOMPLETE.
Enumerated gaps for the full closure:

- **`@onto` family-root** — required IFF Alex wants "@onto lift" as
  substrate-decl composable term. Otherwise Reed's `@cyberpunk/reframe`
  routes around.
- **`@cyberpunk/reframe`** species — required for the level-shift
  action to substrate-decl compose. Currently a phantom under Alex's
  framing.
- **`peer.observe` + `peer.alter` actions** — required to close
  Extension 1. Currently the operational surface lives at
  `@torus.traverse` but there's no peer-altitude action.
- **`peer.eigenboard` field** — required to close "alters the
  numerical eigenboard inference space." @glass line-references
  `boot/std/peer.mirror` but the substrate peer carrier lacks the
  field.
- **`winding: winding` on `mirror_spawn_request`** — required to
  close Extension 2 (depth as spawn parameter).
- **`@knife` (family-root OR @shatter species)** — required to close
  Extension 4 (state-space compressor).
- **`@magic/level_shift` species (or `@magic.ceremony` action)** —
  required to close Extension 3.
- **`algedonic.gradient` action + carrier** — required to close
  Extension 5.
- **`@edge`** — Reed's stated single gap. See §2.4 below for
  fault-plane analysis.

**Total: 8 gaps, 1 optional.** Reed's "one substrate-decl gap"
undercounts by 7. The full closure is a mid-arc landing, not a
single-tick.

### §2.3 Recognition-numeric-ID projection

If the mega-composition claim promotes, it consumes recognition
slots as a CASCADE (multi-recognition):

- One META recognition: `the-peer-IS-a-pain-driven-bounded-ontological-navigator`
- Three SUB-recognitions promoting alongside:
  - `algedonic-gradient-is-navigation-surface`
  - `depth-is-winding-is-spawn-parameter`
  - `@magic-is-level-shift-ceremony`
- Consumes candidate `@reflection-was-naming-artifact` (from
  `shards/torus.mirror:302-304`) at O5 collapse — this promotes if
  the meta lands.

**Not a single slot. Cascade of ~4 recognition promotions.** Numeric
IDs are Alex's adjudication.

### §2.4 Fault-plane shifts (LANDED stances that reverse)

- **`shards/mirror/store.mirror:250-256`** — reserves typed edges for
  `@spectral/db`: *"The open surface deliberately does NOT expose
  typed edges, edge weights, or Laplacian-based navigation."* Reed's
  `@edge` sketch (if it lifts typed edges to family-root altitude
  outside @spectral/db) would REVERSE this stance.
- **PARTIAL REVERSAL ALREADY EXISTS**: `shards/mirror/ref.mirror:232-248`
  DECLARES `edge_kind` variant with seven values (`in_edge`,
  `callers_edge`, `pact_edge`, `fracture_edge`, `kintsugi_bridge_edge`,
  `reflection_predecessor_edge`, `ast_child_edge`). This is at
  `@mirror/ref` altitude — sibling to `@mirror/store`, NOT external.
  So the fault plane between "open OID reachability" and "typed
  edge navigation" has ALREADY been crossed at `@mirror/ref`.
- **Second reversal candidate:** `shards/torus.mirror:203-206` —
  "The peer's identity does NOT collapse into the torus; the peer
  POSSESSES the torus." Extension 1's "peer observes the metalogue
  and alters the eigenboard" walks close to identity-collapse if
  the eigenboard field IS a torus slice. Held: possession relation
  survives so long as the peer's eigenboard is a PROJECTION of the
  torus, not an identity replacement.
- **Third reversal candidate:** `shards/mirror/spawn.mirror:9`
  declares `in @fate`; the whole hinge is "runtime-vs-compiletime."
  Extension 2's "depth as spawn parameter" walks close to reversing
  the @fate-as-hinge stance IF winding is treated as a runtime
  parameter rather than a compiletime request field. Held so long
  as `winding` sits on `mirror_spawn_request` (compiletime
  substrate-decl), not injected at runtime.

**Total fault-plane shifts:** 1 partial (already crossed by
mirror/ref.mirror), 2 held-under-discipline. Zero mandatory
substrate rewrites elsewhere IF the extensions land as species
enrichments; ONE mandatory rewrite (mirror/store.mirror's typed-
edge reservation) IF @edge lifts to family-root altitude.

### §2.5 @reflection O5 collapse verification

Alex's claim "This IS what @reflection wanted to be" against
`shards/reflection.mirror`'s six actions:

| @reflection action | Redistribution target | LANDED? |
|---|---|---|
| observe | @torus.traverse | YES (torus.mirror:497) |
| tournament | @fate/tournament | YES (fate/tournament.mirror, 41.5KB) |
| compose | @loop.compose | YES (per reflection.mirror:44) |
| pick | @fate/tournament selectors | YES (elite/beam/halving) |
| settle | @kintsugi/oscillate.settle | YES (per reflection.mirror:479) |
| speak | @song/voice cross-peer | YES (spawn.mirror:99-112) |

**All six redistribute cleanly to LANDED substrate homes.** Reed's
mapping verified. The O5 collapse per `shards/torus.mirror:257-261`
is empirically supported: the operational content of @reflection
lives elsewhere; the family-root shard is a naming artifact.

---

## §3. Stop-conditions honored

- Files opened: ~25. Bounded per scope.
- Substrate edits: **zero**.
- New shards declared: **zero**.
- New actions declared: **zero**.
- Grep-first everywhere. Cited path:line for load-bearing claims.
- Two-tick discipline: named for each recognition candidate (single-
  tick species vs multi-tick family-root).

Un-checked:
- `boot/std/peer.mirror` — the peer.eigenboard slot @glass references
  may or may not be substrate authority; a follow-up scout would
  read the boot/ altitude and clarify.
- `@fate/algebra` sub-shard genuinely absent per prior scout
  `e975e2f`; whether Alex's framing needs it filled first before
  the peer-observes-metalogue closure lands is a Mara-adjudication
  question, not Taut territory.
- Full text of `shards/reflection.mirror` (34.3KB, only ~200 lines
  read verbatim + full grep summary of actions). If a hidden action
  contradicts the redistribution table above, this scout misses it.

---

## §4. Verdict — LANDABLE WITH CASCADE

The extended framing lands, but the cascade is nontrivial:

- **Zero silent signal conflicts** with LANDED substrate.
- **8 substrate-decl enrichments** required, of which:
  - 4 are one-tick species additions (algedonic.gradient,
    mirror_spawn_request.winding, peer.observe/alter,
    peer.eigenboard field)
  - 3 are new species (cyberpunk/reframe, magic/level_shift,
    knife-somewhere)
  - 1 is optional family-root (@onto; substrate-honest
    alternative: use @cyberpunk/reframe per Reed)
- **1 mandatory fault-plane shift** if @edge lifts to family-root
  altitude outside @spectral/db; ALREADY PARTIALLY CROSSED at
  `shards/mirror/ref.mirror:edge_kind`.
- **Cascade of 4 recognition promotions** consuming candidate slots.
- **@reflection O5 collapse verified as substrate-honest** — all six
  actions redistribute to LANDED homes.

Reed's proposed shared-glass name **`@cyberpunk/reframe`** is the
correct site (rejecting `@onto/reframe` avoids the @onto family-root
landing; @cyberpunk IS declared).

Reed's recognition candidate `the-peer-IS-a-pain-driven-bounded-
ontological-navigator` is the natural META for the cascade; three
sub-recognitions witness it independently.

The framing is not landable AS-DECLARED (would require zero
substrate rewrites — refuted; 8 gaps). It is not signal-conflict-
blocked (would require adjudication first — refuted; zero silent
conflicts). It is a **substrate-consistent CASCADE landing** whose
scope is a mid-arc project, not a single tick.

**Recommend:** land the ONE-TICK enrichments first (four species
appends); adjudicate `@knife` / `@onto` / `@magic/level_shift` as
separate landings; treat `the-peer-IS-...-navigator` recognition as
the promotion that closes the cascade.

---

## §5. Files opened (bounded scope)

Shards read in whole or in part:
1. `shards/peer.mirror` (whole)
2. `shards/torus.mirror` (whole)
3. `shards/reflection.mirror` (lines 1-200 + full action grep)
4. `shards/epistemologic/cybernetic/algedonic.mirror` (whole)
5. `shards/epistemologic/cybernetic/bateson_learning.mirror` (lines 1-220)
6. `shards/magic.mirror` (lines 1-280)
7. `shards/mirror/spawn.mirror` (whole)
8. `shards/mirror/store.mirror` (lines 240-320)
9. `shards/mirror/ref.mirror` (lines 225-290)
10. `shards/cyberpunk.mirror` (whole)

Docs read:
11. `docs/loop/CURRENT.md`
12. `docs/scouts/2026-07-07-taut-knife-meta-pattern-check.md`
13. `docs/scouts/2026-07-08-taut-fate-silicon-metalogue-projection.md`
14. `docs/audits/2026-07-07-seam-phase-d-o-cascade-torus-family-root-close.md`
15. `docs/math/onto/README.md`

Greps run: `@onto`/`@ontology`/`onto-cascade`, `@knife`/`knife.cut`/
`knife.compress`, `@cyberpunk/reframe`/`reframe`/`bateson_learning`,
`@cyberpunk/algedonic`/`algedonic`/`pleasure`/`pain`, `@peer.observe`/
`peer.alter`/`eigenboard`/`observer`/`observe`, `depth:`/`winding:`/
`spawn.*depth`/`spawn.*winding`, `@edge`/`edge_kind`, `prism @onto`.

Stop condition: 15/25 files. Well under bound; scope converged.
