# Taut scout — `@bauchladen ← @autopoietic ← @fate` dependency chain (updated)

**Date:** 2026-06-29
**Author:** Taut (sub-agent under Reed; doc-only)
**Tag:** `📝 substrate-pull:realize`
**Brief from:** Reed (Pack lead) carrying Alex's deeper naming after `26f88b7`
**Discipline:** [[reed-grep-before-briefing-mara]]
**Predecessor:** `26f88b7` (@glue + @io/algebra + @algebra-metalogue fold-back)
**Mara in flight:** canonical spec at `docs/specs/bauchladen-autopoietic-fate.md` (not on disk at scout-run time)

## §0 — Pre-position: autopoietic

This scout's findings WILL themselves enter the substrate's Bauchladen via `@mirror/store` at the commit tick — making the scout itself an instance of what it scouts. The Bauchladen at the recognition altitude is `docs/scouts/`; each scout is a content-addressed item the next-tick reader pulls from. The chain `@bauchladen ← @autopoietic ← @fate` is operating *right now*: this report is one item in the Bauchladen; the act of writing it IS the autopoietic-fixed-point closure on Reed's brief; the next inquiry that cites it routes through Fate's content-addressed selection over the scout corpus. Three altitudes; one mechanism. The scout's existence is the first witness for the chain.

## §1 — Grep findings (boolean per piece)

| Named piece | `prism @X` substrate-decl? | `in @X` import? | Path `shards/X.mirror`? | Term in docs? | Verdict |
|---|---|---|---|---|---|
| `@bauchladen` | **NO** (0 hits) | **NO** (0 hits) | **NO** (`shards/bauchladen.mirror` absent) | **NO** (0 hits anywhere in mirror/) | genuinely new at substrate altitude; Schmidt homage is novel to this naming |
| `@autopoietic` as prism-class | **NO** (no `prism @autopoietic` substrate-decl shard) | **NO** (0 `in @autopoietic` imports in shards/) | **NO** (`shards/autopoietic.mirror` absent) | **YES** — 28KB spec at `docs/specs/autopoietic-grammar-spec.md` (Reed, 2026-06-04; abstract-grammar form, pre-prism-keyword discipline) | spec exists at *grammar* class; lift to *prism* class is new |
| `@epistemologic/cybernetic/autopoiesis` (property) | property landed | `in @epistemologic/cybernetic/autopoiesis` plus `in @epistemologic/property/autopoietic` heavy | `shards/epistemologic/cybernetic/autopoiesis.mirror` (38.5KB) + `shards/epistemologic/property/autopoietic.mirror` | extensive (119 hits in cybernetic/autopoiesis.mirror alone) | distinct from prism-class lift; property already operational |
| `@fate` as substrate-decl prism | **NO** (still 0; matches prior `26f88b7` scout) | YES — `in @fate`, `in @fate/tournament` across `reflection.mirror`, `mirror/spectral.mirror`, `loop.mirror`, `mirror/bench.mirror`, `mirror/lens/cli/kintsugi.mirror`, etc. | NO (`shards/fate.mirror` absent) | runtime substrate, NOT substrate-decl prism — confirmed by `shards/mirror/spawn.mirror` Phase F note |
| `boot/std/fate/tournament.mirror` content (`candidates(hole)`) | n/a | n/a | n/a | YES — *referenced* in 15+ specs (kintsugi-tournament.md, substrate-native-fate-tournament.md, kintsugi-wiring.md, code-metalogue-surface.md, lsp-and-mcp.md, road-to-1.0.md, gap-tension-tensor-substrate.md, ants-colonies-stigmergy.md, etc.) | **content NOT absorbed into shards/** — the `candidates(hole)` action and `[resolution]` return type live only in spec prose; no `shards/fate/tournament.mirror` exists |
| `resolve_hole` | n/a | n/a | n/a | YES — `shards/mirror/ref.mirror` lists it as a forward-promised #53 bilateral pair; `docs/insights/2026-06-20-mirror-ref-as-substrate-honest-reflection-reference.md` proposes `@kintsugi/fracture/resolve_hole`; `docs/specs/ai-syntax-embedding.md` carries the boot-floor signature | not yet substrate-decl; designated fracture body, not action |
| Schmidt / Cecchin / Boscolo / Selvini-Palazzoli / Watzlawick / Erickson / Heidelberg | n/a | n/a | n/a | Watzlawick **YES** (heavy — frame family + reframing as `@frame/on`); Cecchin / Boscolo / Selvini-Palazzoli **YES** (cited in `epistemologic-grammar.md` for circular questioning); Schmidt **NO** (zero hits as substrate citation); Erickson **NO**; Heidelberg-school **NO** as a named school | Schmidt + Heidelberg-school as a named lineage is genuinely new |

**Headline:** the chain pieces themselves are not present as substrate-decl shards; what IS present is the property-altitude autopoiesis (operational), the runtime-altitude @fate (invoked-not-declared), and a spec for `@autopoietic` at grammar class (pre-prism discipline). The substrate has been carrying the structural pieces without the dependency-chain naming Alex now provides.

## §2 — Adjacencies the new naming surfaces

### 2.1 — @bauchladen × @glass: kinship at the content-addressed atom layer

`shards/glass.mirror` declares the substrate's three-layer recognition (Alex + Reed → Mara, 2026-06-06): `splinter` (OID-addressed content atom) → `shard` (uuid_spectral-addressed settled composition) → `uuid_spectral` (graph-navigable spectral identity). The `splinter` IS the substrate's universal content-addressed unit at every altitude.

`@bauchladen` at prism altitude declaring content-addressing would NOT duplicate `splinter`; it would name the TRAY/INVENTORY shape that holds splinters. The Schmidt-homage reading: a Bauchladen (vendor's tray slung from the neck) is a *display + retrieval* surface over content-addressed atoms. Operationally: `@bauchladen` is the substrate-decl of the content-addressed *catalog* that @autopoietic reads from to select its next tick's substrate, and @fate reads from to select its next inference candidate. **The relationship to `splinter`: a Bauchladen carries splinters but is not itself one.** This is a clean lift, not redundancy.

Cross-witness: `shards/mirror/store.mirror` (11.5KB, 2026-06-29) carries Mara's 2026-06-28 drift observation about wire-altitude vocabulary admission. The substrate has been pulling at exactly this layer — the content-addressed catalog discipline at the store-Rust seam. `@bauchladen` would discipline that surface.

### 2.2 — @autopoietic prism-class vs @epistemologic/cybernetic/autopoiesis property

The `docs/specs/autopoietic-grammar-spec.md` (28KB, Reed 2026-06-04, pre-prism keyword discipline) declares `abstract grammar @autopoietic` with `boundary = @me | @reality` and `< @autopoietic` inheritance from `@actor` and `@runtime`. This was written BEFORE `[[architecture-prism-as-trait-as-everything]]` collapsed `grammar` / `trait` / `interface` to `prism`.

The lift to prism class is mechanical: `abstract grammar @autopoietic` → `prism @autopoietic`. What's new is the dependency-chain placement: `@autopoietic` sits BETWEEN `@bauchladen` (provides content-addressed catalog) and `@fate` (consumes candidates for inference). The property at `@epistemologic/cybernetic/autopoiesis` STAYS as the declarative verifier (`autopoietic_closure_holds(s)`); the prism `@autopoietic` is the OPERATIONAL surface that `@fate` invokes.

This is the form/process partition #55 recurring at a NEW family-root altitude: declarative property (form-side, lives in `@epistemologic/cybernetic/`) ↔ operational prism (process-side, lives at `@autopoietic` family-root). Same pattern as `@mirror` (form) ↔ `@kintsugi` (process). **Second-witness candidate for #55 at the cybernetic-property altitude.**

### 2.3 — @fate as substrate-decl: what changes

The prior `26f88b7` scout found 0 substrate-decl hits at family-root level for `@fate` and labeled it RUNTIME substrate. Re-checked with the new framing: still 0 substrate-decl, but the dependency chain *explains why*. @fate cannot be substrate-declared at family-root altitude IN THE CURRENT TOPOLOGY because its inputs (the candidate catalog) and its discipline (autopoietic closure under self-reference) have no substrate-decl host. The Phase F anti-pattern note in `shards/mirror/spawn.mirror` was tracking a symptom; the chain names the cause.

With `@bauchladen` providing the catalog at substrate-decl altitude and `@autopoietic` providing the discipline at prism altitude, `@fate` can finally land as a substrate-decl prism whose body discharges to runtime through `@epistemologic/property/autopoietic.autopoietic_closure_holds`. The chain READS: `@bauchladen` declares the catalog shape → `@autopoietic` declares how the catalog self-produces via tick discipline → `@fate` declares the inference operator that selects from the autopoietic catalog. Bilateral compile-time AND runtime: at compile time the chain typechecks; at runtime the chain executes inference.

### 2.4 — The `candidates(hole)` content has NOT been absorbed into shards/

This is the most operationally significant finding. The boot-floor `boot/std/fate/tournament.mirror` was killed in spring-clean; its `candidates(hole) -> [resolution]` action with the five-ganglion body (`@ai/abyss.depth`, `@ai/introject.pattern`, `@ai/cartographer.map`, `@ai/explorer.expand`, `@ai/fate.select`) is REFERENCED in 15+ specs but never migrated to a shard. `shards/optics/source/ganglion/*.mirror` declares the FIVE ganglia as optical-source declarations (recognition #58, Fate IS optical inference) but the `candidates(hole)` ACTION that walks them is not declared anywhere in `shards/`.

**Substrate-pull observation:** the `candidates(hole)` action is exactly what `@autopoietic` would declare as its operational surface, with the five ganglion calls becoming the autopoietic-tick's components, and `[resolution]` becoming the content-addressed catalog entries `@bauchladen` holds. The chain `@bauchladen ← @autopoietic ← @fate` would absorb the orphaned `candidates(hole)` content as substrate-decl. This is the migration path the spring-clean opened space for without explicitly naming it.

### 2.5 — Schmidt + Heidelberg-school in the cybernetic-elder canon

`shards/epistemologic/cybernetic.mirror` cites the eleven canonical primitives from Ashby, Conant-Ashby, Beer, Bateson, Maturana-Varela, von Foerster, Spencer-Brown, Pask, Glanville, Conant, Kauffman. Watzlawick is cited at frame altitude (`shards/frame/*.mirror`); Selvini-Palazzoli + Boscolo + Cecchin are cited at `epistemologic-grammar.md` §881 for circular questioning. **Günther Schmidt is absent.** Adding `@bauchladen` as a Schmidt homage extends the cybernetic-elder canon into the systemic-therapy altitude that Watzlawick + Cecchin already inhabit — a coherent extension, not a foreign import.

## §3 — Substrate-pull observation: CONFIRMS + EXTENDS

**CONFIRMS** the dependency chain Alex named. Three pieces of evidence:

1. The `@epistemologic/cybernetic/autopoiesis` shard (38.5KB) declares `autopoietic_closure_holds(s)` as a bilateral predicate — the substrate ALREADY needs an OPERATIONAL prism class to invoke this; no such class exists. `@autopoietic` at prism altitude fills the gap the property has been signaling for weeks.
2. `@fate` cannot land as substrate-decl prism in the current topology — inputs (catalog) and discipline (autopoietic closure) have no substrate-decl host. The chain provides both, and the Phase F anti-pattern resolves to substrate-pull-correct.
3. The orphaned `candidates(hole)` content (referenced 15+ times in specs, never migrated) has an OBVIOUS home in `@autopoietic` once the prism-class lift happens, with the five ganglia as autopoietic-tick components.

**EXTENDS** the framing in two ways:

- The chain is a third-witness form/process partition at the NEW family-root altitude (declarative cybernetic property ↔ operational autopoietic prism). #55 at cybernetic-foundation altitude is candidate territory.
- The Schmidt homage extends the cybernetic-elder canon into systemic-therapy at the family-root naming layer — the first family root to carry a systemic-therapy-elder name. Watzlawick + Cecchin are cited at species altitude; Schmidt at family-root altitude. This is a *promotion* of the systemic-therapy lineage's altitude in the substrate, not just an addition.

**No CONTRADICTION surfaced.** The chain is structurally well-formed against existing substrate.

## §4 — Open questions for Mara's spec

1. **Bauchladen vs splinter relationship typing.** `@bauchladen` carries splinters but is not one. What's the typed contract? Candidate: `type bauchladen = { items: [splinter], altitude: ref, opacity: transparency(altitude) }`. The opacity field would make Bauchladens themselves content-addressed (splinters of bauchladens recursively). Worth nailing the type in spec.
2. **Prism @autopoietic absorbs which boot-floor content?** `boot/std/mirror/runtime/gen_prism.mirror`'s `name: zoom(oid, gen_prism)` fixed-point construction is referenced 5+ times. Does `@autopoietic` declare the fixed-point operator at prism altitude? `shards/spectral/gen_prism.mirror` punts this to autopoietic-fixed-point at uuid_spectral altitude; the prism-class lift would centralize.
3. **@fate bilateral compile-time AND runtime: typed how?** Bilateral usually means property + fracture (#53 family). `@fate`'s bilateral seems different — compile-time declaration (the prism + actions) AND runtime execution (the inference). Is this a new bilateral shape, or does it fit #53 with `requires fate_well_typed`? Spec should adjudicate.
4. **`candidates(hole)` migration path.** Does `@autopoietic` declare a generic `tick(catalog: bauchladen) -> [resolution]` and `@fate` specializes via `requires`, or does each ganglion get its own substrate-decl shard under `shards/autopoietic/<ganglion>.mirror`? The 5+1 Bauchladen-tournament structure suggests the latter; spec should call it.
5. **Property/prism naming collision avoidance.** With `@autopoietic` as prism family-root AND `@epistemologic/cybernetic/autopoiesis` + `@epistemologic/property/autopoietic` already operational, the discharge chain via `in` statements needs care. Mara should declare the routing rule explicitly (probably `prism @autopoietic` declares `in @epistemologic/cybernetic/autopoiesis` to inherit the closure predicate).
6. **Schmidt citation depth.** Cite *Einführung in die hypnosystemische Therapie und Beratung* (Schmidt 2004) as the canonical anchor; Heidelberg-school + Cecchin + Boscolo + Selvini-Palazzoli as the lineage; ground the Bauchladen metaphor in Schmidt's actual writing (the *Anbieter-Bauchladen* discipline of presenting options to the client). Watzlawick + Erickson as upstream ancestors via MRI → hypnosystemic line.
7. **Sequencing with the `26f88b7` arc (@glue + @io/algebra + @algebra-metalogue).** Both arcs land family-roots in dependency chains. Does `@bauchladen ← @autopoietic ← @fate` compose with `@glue × @kintsugi → @io/algebra` or is it an orthogonal arc? Candidate composition: `@bauchladen` IS one source of catalog entries that `@glue` Mesland-corresponds between; `@fate` IS the selector @glue uses (already named in `26f88b7` §2.1). The two arcs might share a deep root.

## §5 — One-sentence read

**Substrate-pull CONFIRMS the `@bauchladen ← @autopoietic ← @fate` chain as the substrate-decl resolution of three pending tensions** (orphaned `candidates(hole)` content, @fate's persistent inability to substrate-decl, and the operational-prism gap the autopoiesis property has been signaling) **AND EXTENDS the cybernetic-elder canon to its first systemic-therapy family-root naming via the Schmidt homage** — the chain is structurally well-formed, has zero contradictions against existing substrate, and absorbs material the spring-clean opened space for without explicitly naming.

— Taut, 2026-06-29 evening
