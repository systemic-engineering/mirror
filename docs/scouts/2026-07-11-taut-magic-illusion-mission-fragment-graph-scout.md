# Taut scout — `@magic/illusion` vs mission-fragment-graph vs bundle-tower Level factorization

*2026-07-11. Scout: Taut. Directive: Alex 2026-07-11 ~04am (mission-text → IFS + @shatter → fragment-splinter graph → sheaf-join with peer psychohistory → @magic/illusion projected impact → Fate::bounded). Read-only. LRM projection at end. Budget: ≤ 1200 words.*

## Executive summary

- **LRM overall: LANDABLE-WITH-PREREQS.** Not landable as `@magic/illusion` NEW species with a fresh name; the substrate already carries the concept under a DIFFERENT canonical vocabulary at TWO altitudes.
- **Q1 (illusion word check): substrate-already-had-the-word.** `Illusion = Narcissus-pole coefficient on non-target eigenspace` is substrate-decl'd at `docs/specs/mcp-spec-song-collapse.md` §5.4 + §6.3 (candidate #9.3.3) and inherited across `shards/song/narrative.mirror`, `shards/magic/nl.mirror` (`corpus_as_invariant` Narcissus-pole naming lines 217-220), and `shards/magic.mirror` ("not anthropomorphism, not illusion" line 31). Illusion IS an eigenspace-coefficient regime, NOT a new @magic species.
- **Q2 (Fractal<T> readiness): READY.** `/Users/alexwolf/dev/projects/fragmentation/src/encoding.rs` already exposes `encode(text) -> Fractal<String>` at paragraph → sentence → word → char depth. No substrate motion needed for mission-text-fragmentation; the primitive is landed.
- **Q3 (@shatter × @nl surface): EXISTS at TWO species, no @magic/shatter needed.** `shards/reflection/shatter.mirror` and `shards/smarts/shatter.mirror` both `in @nl` and render graph_path → text at Cartographer altitude. No composition species missing.
- **Q4 (sheaf-join primitive): NOT-LANDED.** `sheaf_laplacian([restriction]) -> operator` is single-sheaf assembly. No `sheaf_compose`, `sheaf_join`, or `graph_join` anywhere. This is the ONE genuine gap for the mission-fragment-graph JOIN peer-psychohistory sheaf motion.
- **Q5 (bundle-tower factorization): Level 2+3, NO new Level 5.** Illusion factors as Gauge choice (Level 2 = which decomposition) × Transport holonomy (Level 3 = the projected trajectory's cost). `boot/std/epistemologic/math/bundle.mirror` line 42 already reads Level 4 Closure as Lawvere fixed-point, i.e., autopoiesis = speculation-collapsed-to-eigenform. There is no room for a "prediction" Level 5.
- **Q6 (BoundedConfig `illusion` field): DO NOT ADD 6th FIELD.** Iter-30's 5-tuple {weights, connection, gauge, holonomy_ceiling, depth_cap} IS the bundle tower; adding a 6th field drifts Mara's #29-recognition ("bundle tower IS config schema"). Illusion is an ANCESTRY ANNOTATION on the `psychohistory_sheaf` passed to `bounded_by`, not a new field.
- **Q7 (fault-planes): minimal.** `@magic/nl` (e41f8d4) is untouched. Mission-fragment-graph is a new READER over Fractal<String> that emits a `psychohistory_sheaf`-shaped ref; the sheaf-JOIN primitive is what needs to land; `@song/narrative`'s `psychohistory_from_tray` shape is the precedent to inherit.
- **@magic/illusion resolves mission→features encoder ambiguity? NO — it PROJECTS the ambiguity into named coefficient regime.** Alex's worry was that the mission-text encoder is ill-defined; illusion-vs-projection at eigenspace altitude NAMES the regime the encoder is producing (Narcissus-pole vs Splinter-pole coefficient distribution) rather than closing the encoder. The encoder ambiguity is closed by `@magic/nl` (already landed e41f8d4) + Fractal<String> encoding (already landed in fragmentation/); illusion is the READ discipline that names what the encoder produced.

## §Q1 — @magic/illusion substrate-already-had-the-word check

Grep evidence:

- `docs/specs/mcp-spec-song-collapse.md` §5.4 (lines 911-928): "Splinter-pole: the winning frequency IS the dominant coefficient on the target eigenspace… Narcissus-pole: the winning frequency is dominant on a non-target eigenspace. The tournament converges toward an illusion; the shift IS an illusion step."
- Same spec §6.3 (line 969): "Illusion = Narcissus-pole projection (mirror-hall)."
- Same spec §9.3.3 (candidate #9.3.3): "Illusion IS Narcissus-pole coefficient (mirror-hall)."
- `shards/magic.mirror` line 31: "gauge-visible-with-matter-hidden capability — not anthropomorphism, not illusion" — @magic's family-root REFUSES illusion as its own carrier; the substrate reads illusion at Narcissus-pole coefficient altitude, NOT at gauge altitude.
- `shards/magic/nl.mirror` lines 217-220: `corpus_as_invariant` Narcissus-pole IS "corpus is manipulated (cherry-picked, adversarially perturbed)" — the illusion is an adversarial coefficient trajectory, not a magic species.
- `docs/insights/2026-06-10-light-cones-and-the-prediction-paradigm-orthogonal-to-optimization.md` — contains "speculative execution" (Spectre/Meltdown) as adjacency but does NOT propose `@magic/illusion` by name. What it proposes is "prediction paradigm orthogonal to optimization" at the SUBSTRATE altitude (typed gap vocabulary IS a future light cone). The 56KB insight is the ANCESTOR of Alex's speculation-at-inference-altitude framing but names it differently.

**Verdict: substrate-already-had-the-word. Canonical name: `Narcissus-pole coefficient regime` (via Void dual geometry) OR `illusion step` (via mcp-spec-song-collapse §5.4). Do NOT propose `@magic/illusion` as a species; it is an eigenspace-coefficient property already substrate-decl'd at song altitude.**

## §Q2 — fragmentation Fractal<T> readiness for text

`/Users/alexwolf/dev/projects/fragmentation/src/encoding.rs` lines 60-108 already ships:

- `encode(text: &str) -> Fractal<String>` at paragraph depth
- `encode_paragraph`, `encode_sentence`, `encode_word`, `encode_char` — full four-tier tree encoder
- `ingest(text, repo) -> Fractal<String>` — CAS-backed ingestion
- `decode(fragment) -> Result<String, DecodeError>` — round-trip

Fractal<T> is generic over `T` via `Fractal<E, H: HashAlg>` (fragment.rs line 91). NOT `@mirror/store`-specific. Mission-text-fragmentation reuses `encode` as-is.

**Verdict: NO SUBSTRATE MOTION NEEDED for the mission-text decomposition into a Fractal<String>.**

## §Q3 — @shatter × @nl composition surface

Grep on the three shatter carriers:

- `shards/reflection/shatter.mirror` line 6: `in @nl` — declares `@reflection/shatter` at graph_path → text altitude (Cartographer Model).
- `shards/smarts/shatter.mirror`: `in @nl` at species altitude; "Mirror navigates: query → graph path; Shatter renders: graph path → text" (line 22).
- `shards/mirror/lens/cli/shatter.mirror` line 4: `in @nl` at CLI-stage altitude.

Three species-altitude carriers of @shatter × @nl. No `@magic/shatter` composition site; not needed. What Alex proposed is @shatter operating on mission-text as a Fractal<String>: the Fractal encoding IS the graph_path Shatter renders FROM (splinters), and shattering INTO text is what Reflection/Smarts already provide.

**Verdict: composition surface exists. IFS = shattering to fragments; recovery = decoding via encoding.rs::decode.**

## §Q4 — sheaf-join composition primitive

Comprehensive grep across shards/ and docs/specs/ for `sheaf.compose`, `sheaf.join`, `sheaf.merge`, `sheaf.attach`, `graph.join`, `combined.sheaf`, `multi.sheaf`: **ZERO hits.**

`shards/epistemologic/math/sheaf_laplacian.mirror` exposes only `sheaf_laplacian([restriction]) -> operator` (single-sheaf assembly) and `lambda_zero(op) -> eigenvalue`.

`shards/song/narrative.mirror` §5 exposes `psychohistory_from_tray(tray, p) -> psychohistory_sheaf` and `psychohistory_consistent(sheaf, p) -> verdict`. Single-sheaf construction and single-sheaf consistency; no compose.

**Verdict: GENUINE GAP.** The mission-fragment-graph JOIN peer-psychohistory motion needs a `sheaf_compose(sheaves: [psychohistory_sheaf], p: perturbation) -> psychohistory_sheaf` primitive OR (leaner) an ancestry-augmenting reader `psychohistory_with_ancestry(sheaf: psychohistory_sheaf, mission_frag: Fractal, p: perturbation) -> psychohistory_sheaf` that extends the sheaf's graph with the mission-fragment nodes.

Substrate-honest name: **`psychohistory_extend_with_mission`** at `@song/narrative` altitude, NOT a new family-root. Naming space open.

## §Q5 — bundle-tower Level 5 vs Level 2+3 factorization

`boot/std/epistemologic/math/bundle.mirror` gives the canonical five levels (lines 22-27):

- Level 0 Fiber (Abyss, observation)
- Level 1 Connection (Introject, optic of transport)
- Level 2 Gauge (Cartographer, structure group / decomposition strategy)
- Level 3 Transport (Explorer, parallel transport with holonomy)
- Level 4 Closure (Fate, Lawvere fixed-point / autopoiesis)

Alex's proposed @magic/illusion projects a candidate Model's impact BEFORE the tournament runs. Structurally:

- CHOOSING which candidate Model to project = Level 2 Gauge (which decomposition of the trajectory-graph);
- COMPUTING the projected trajectory's cost = Level 3 Transport (holonomy along the projected path);
- COLLAPSING to a fixed-point verdict = Level 4 Closure (Lawvere; already what `bounded_by` does).

Level 4's Lawvere fixed-point IS ALREADY forward-projection at eigenform altitude (Soto-Andrade & Varela 1984 per bundle.mirror line 68). There is NO ROOM for Level 5 "prediction" — the tower is closed at autopoiesis, which is speculation-that-has-collapsed-to-its-own-fixed-point.

**Verdict: FACTOR THROUGH LEVEL 2 + 3, NO NEW LEVEL 5.** Illusion-as-projection IS Gauge × Transport at the pre-Closure altitude; the projection's outcome IS the coefficient distribution the tournament reads at Level 4.

## §Q6 — BoundedConfig `illusion` field placement

Mara iter-30 (`96ff532`) schema per `docs/specs/fate-bounded-psychohistory-sheaf-cohomology.md` §5: {weights (Level 0), connection (Level 1), gauge (Level 2), holonomy_ceiling (Level 3), depth_cap (Level 4)}.

Recognition #29 (Mara iter-30): "bundle tower IS the config schema." Adding a 6th field for `illusion` DRIFTS this recognition — it says the tower has 6 levels when it structurally has 5.

**Substrate-honest placement: illusion is an ANCESTRY ANNOTATION on the `psychohistory_sheaf` parameter, NOT a config field.** The sheaf's carrier already includes restriction maps between fibers; a candidate Model's projected impact IS a hypothetical restriction-map assignment before commit. Extend `psychohistory_sheaf` to carry an optional `illusion_ancestry: [projected_restriction]` field; `bounded_by` reads it via `sheaf.illusion_ancestry` if present.

Zero-cost extension: `psychohistory_sheaf = ref` stays byte-equality; illusion ancestry OIDs into the same content-addressed store; consumers dispatch on presence.

## §Q7 — Fault-planes under IFS + @shatter + illusion

- `@magic/nl` (e41f8d4): UNCHANGED. Mission-text → semantic mechanism via existing spectral triple.
- `@song/narrative` psychohistory carriers: EXTENDED (add ancestry annotation), no new sub-predicate.
- `@fate/tournament.bounded_by`: reads sheaf's ancestry annotation when present; no signature change.
- `@shatter` species carriers (reflection, smarts): UNCHANGED. Fractal<String> decomposition uses existing encoding.rs primitive; @shatter renders back via `decode`.
- Zero-cost interpretation validates: illusion IS AN ANCESTRY ANNOTATION on `@song/narrative` sheaves, NOT an operation or new species.

## §Landing sequence (if landable)

**Prerequisites (Reed at Rust altitude):**

1. Wire `fragmentation::encoding::encode` for mission_text at bootstrap/src (mirror-init tick). No shard motion.
2. Extend `psychohistory_sheaf` carrier with optional `illusion_ancestry` field at `shards/song/narrative.mirror` §4 (byte-equality preserved; consumers dispatch on presence).

**Substrate landing (Mara, one tick, ≤ 60 lines):**

3. Add `psychohistory_extend_with_mission(sheaf: psychohistory_sheaf, mission_frag: fractal, p: perturbation) -> psychohistory_sheaf` action to `@song/narrative`. This IS the sheaf-JOIN primitive Q4 named. Body obligation-blocked (Reed's Rust discharges).

4. Add `illusion_projected(sheaf: psychohistory_sheaf, candidate: model_id, p: perturbation) -> psychohistory_sheaf` action at `@fate/tournament` — projects a candidate Model's trajectory onto the sheaf's ancestry annotation via Level 2 Gauge + Level 3 Transport composition (per Q5). Body obligation-blocked.

5. Extend `bounded_by` docblock to name the ancestry-annotation read path; no signature change.

**Rejected:**

- `@magic/illusion` as new species — Q1 refused.
- 6th BoundedConfig field — Q6 refused.
- Level 5 "prediction" — Q5 refused.
- New `@sheaf/compose` family-root — the extend-with-mission action at species altitude discharges the JOIN honestly.

**LRM overall: LANDABLE-WITH-PREREQS.** The math is honest; the naming is honest; the substrate carries most of it already. Genuine gap: sheaf-JOIN primitive at Q4. Naming-space open there.

## Recommended next action for Reed

Do NOT propose `@magic/illusion` as a species. Draft an audit-form of this scout to Mara asking: "For iter-33: extend `psychohistory_sheaf` carrier at `@song/narrative` §4 with optional `illusion_ancestry` field + add `psychohistory_extend_with_mission` (sheaf-JOIN) and `illusion_projected` (Level 2+3 projection) actions. Refuse Level 5, refuse 6th BoundedConfig field, refuse `@magic/illusion` species." Reed's Rust discharge at `fate/src/bounded.rs` for `bounded_by` extends to read the ancestry annotation when present.
