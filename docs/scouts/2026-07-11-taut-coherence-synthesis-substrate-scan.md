# Taut Scout — @coherence synthesis substrate-scan (2026-07-11)

**Author:** Taut (`taut@systemic.engineer`)
**Mode:** grep-first substrate-already-had-the-word check
**Trigger:** Alex 2026-07-11 — synthesis proposal to unify @cyberpunk / Ashby / elders /
algedonic @pain+@pleasure / @knife.cut / @torus / @kintsugi under `spectral @coherence`.

---

## Executive verdict

**The substrate already carries the semantics under three landed names.** `@coherence`
is NOT a novel primitive — it is the **species-crystal** of a slot the parametric
carrier already reserved. Specifically:

1. The **math** is landed as `sheaf-coherence criterion` at
   `docs/math/sheaf/laplacian.md:57` — `λ₀(F) = 0 ⇔ coherent`; `λ₀(F) > 0 ⇔
   incoherent`. The scalar-measure-of-coherence is already substrate-fact.
2. The **parametric carrier** is landed at
   `shards/epistemologic/cybernetic/coherence-parametric.mirror:266` as `prism
   @epistemologic/cybernetic/coherence-parametric` — with an explicit
   forward-promise (line 25-28) that `@epistemologic/cybernetic/coherence` IS
   THE COHERENCE SPECIES that instantiates it. **That species shard is not yet on
   disk.** This is the exact hole `@coherence` fills.
3. The **runtime name** is Reed's 2026-07-08 `autopoietic Closure IS the
   2-groupoid's coherence operation` (docs/math/the-tower/recursion-locks.md:2898)
   — landed at commit `4b2ef3c` (Blocker 2 autopoietic loop closure GREEN,
   `--integrate-diff persists moments to peer_home/.bauchladen/`). Reed named the
   operation *without* naming the metric it optimizes.

**Recommendation: land the missing species shard as
`shards/epistemologic/cybernetic/coherence.mirror`** — NOT `spectral @coherence`.
The parametric carrier explicitly names the target path
(`coherence-parametric.mirror:234`: `epistemologic/cybernetic/coherence.mirror (the
species-coherence shard; the FIRST instantiation)`). Substrate is asking for the
species at that path. The two-tick-discipline collapse: substrate-already-had-the-word.

---

## Substrate ancestry chain — every landed OID @coherence would rest on

### Sheaf-Laplacian math floor (landed)

- `shards/epistemologic/math/sheaf_laplacian.mirror:151` — `prism
  @epistemologic/math/sheaf_laplacian { focus sheaf_laplacian … }`. Declares
  `sheaf_laplacian([restriction]) -> operator` and `lambda_zero(op: operator) ->
  eigenvalue`. Δ_F = δ*δ per Hansen & Ghrist 2019.
- `docs/math/sheaf/laplacian.md:57` — **The sheaf-coherence criterion:**
  `λ₀(F) = 0 ⇔ F has a non-trivial global section; λ₀(F) > 0 ⇔ no global section
  exists; the substrate has incoherent regions.`
- Mara iter-19 (`78d5110` — landed sheaf-Laplacian unification via
  `docs/specs/eigensheaf.md`): kintsugi + mycelial + peer inference are ONE
  operation on Δ_F. **This IS @coherence's operator surface without naming.**

### Cybernetic elders (all landed, all admissible ancestry)

| Elder | Landed decl | File:line |
|---|---|---|
| Ashby requisite variety | `prism @epistemologic/cybernetic/variety` | `shards/epistemologic/cybernetic/variety.mirror:139` |
| Beer VSM | `prism @epistemologic/cybernetic/viable` | `shards/epistemologic/cybernetic/viable.mirror:382` |
| Beer algedonic | `prism @epistemologic/cybernetic/algedonic` | `shards/epistemologic/cybernetic/algedonic.mirror:45` (also `shards/cyberpunk.mirror` species-adapter track) |
| Spencer-Brown distinction | `prism @epistemologic/cybernetic/distinction` | `shards/epistemologic/cybernetic/distinction.mirror:259` |
| Foerster second-order | `prism @epistemologic/cybernetic/second_order` | `shards/epistemologic/cybernetic/second_order.mirror` |
| Foerster eigenform | `prism @epistemologic/cybernetic/eigenform` | `shards/epistemologic/cybernetic/eigenform.mirror` |
| Maturana/Varela autopoiesis | `prism @epistemologic/cybernetic/autopoiesis` | `shards/epistemologic/cybernetic/autopoiesis.mirror` |
| Bateson learning levels | `prism @epistemologic/cybernetic/bateson_learning` | `shards/epistemologic/cybernetic/bateson_learning.mirror` |
| Pask conversation | `prism @epistemologic/cybernetic/conversation` | `shards/epistemologic/cybernetic/conversation.mirror:113` |
| Conant-Ashby / coherence-parametric | `prism @epistemologic/cybernetic/coherence-parametric` | `shards/epistemologic/cybernetic/coherence-parametric.mirror:266` |
| reframe / coevolution / design | landed siblings | `shards/epistemologic/cybernetic/{reframe,coevolution,design}.mirror` |

All eleven cybernetic-species Alex's synthesis names are already substrate-decl.
**No elder is missing.** Every one of them is already listed by
`coherence-parametric.mirror:346-350` as consumers of the parametric carrier.

### @cyberpunk reframe as Ashby-response (landed)

- `shards/cyberpunk.mirror:106` — `prism @cyberpunk { … }` family-root.
- `shards/cyberpunk.mirror:158` — `cybernetic_coherence(s: cybernetic_state) ->
  verdict`. The predicate at the family-root altitude. @cyberpunk **already
  exports `cybernetic_coherence` as its floor predicate.** This is @coherence at
  family-root altitude, unnamed as its own species.

### @knife → @magic/distinction (landed)

- `shards/magic/distinction.mirror:123` — `prism @magic/distinction`.
- `docs/loop/CURRENT.md:540` — Alex 2026-07-07: `@knife substrate declaration ✅
  Alex delegated; Reed adjudication:` folded to distinction (Spencer-Brown mark).
- Taut earlier scout `docs/scouts/2026-07-07-taut-knife-meta-pattern-check.md`
  refined @knife to "underdetermined-engine" pattern (@glue stronger 2nd witness
  than @knife). **@knife.cut is Spencer-Brown's `cross(m: mark, s:
  distinction_space) -> distinction_space` at
  `shards/epistemologic/cybernetic/distinction.mirror:329`.** The knife is
  substrate-already-had-the-word for the mark.

### @torus as coherence basin (landed)

- `shards/torus.mirror` (27.9KB, 2026-07-07 landed by Mara, ratified by 7
  witnesses incl. Foerster verbatim pp. 238/244/256/282, Cubical HoTT, Kauffman
  2003).
- `prism @torus` with `winding class in π₁(T²) = ℤ × ℤ` typed. Peer HAS a torus
  (per docs/loop/CURRENT.md:293 `@peer-has-a-torus` recognition).
- **The `@torus` basin IS what descent-under-λ₀ navigates.** Each winding class
  (m, n) is a candidate section; `λ₀(F_{winding=(m,n)}) = 0` picks the
  coherent one.

### @kintsugi coherence-descent (landed)

- `shards/kintsugi.mirror:225` — `prism @kintsugi` family-root. Comments
  (lines 35-45): *"substrate-pull recognition; coherence is maintained by
  Bateson … Kintsugi IS the coherence-preservation mechanism."*
- `shards/prism.mirror:33` — Connes-triple framing: `D = the kintsugi flow (the
  Dirac operator; eⁿ⁺¹ ≤ eⁿ monotone descent; c-theorem on graph Laplacians).`
- `shards/mirror/loss.mirror:246-265` — `kintsugi's eⁿ⁺¹ < eⁿ IS the monotone
  descent … `λ₀(Δ_F)` computation @mirror/loss/dirichlet will parameterize`.
- **`e^(n+1) < e^(n)` IS the descent-toward-`λ₀=0`.** Kintsugi's dark-count
  monotone-decrease at `shards/epistemologic/property/dark_count_monotone.mirror`
  is the same operation named at property altitude.

### Algedonic @pain / @pleasure (partial — @pain landed, @pleasure implicit)

- `shards/epistemologic/cybernetic/algedonic.mirror:45` — `prism algedonic <=
  cybernetic_species`. Carrier `algedonic_signal` with `bypass_signal`
  (S1→S5 crossing) landed. Beer 1972/1979 ancestry cited.
- `shards/epistemologic/cybernetic/reframe.mirror:7-46` — `in
  @epistemologic/cybernetic/algedonic`; "pain-δ exceeding threshold on the
  eigenboard (algedonic sample)" IS the reframe trigger.
- `docs/scouts/2026-07-08-taut-pain-driven-bounded-ontological-navigator-projection.md` — Taut's own prior scout on pain-driven navigation.
- **No `@pleasure` shard.** Beer's algedonic is signed: pain XOR pleasure across
  the same channel. `algedonic_signal.polarity` is the natural place; not
  landed as a distinct species. The synthesis-name `@pain` and `@pleasure` are
  polarity readings of the ALREADY-LANDED algedonic carrier, not separate
  family-roots.

---

## Collision risk assessment — `spectral @coherence { … }`

**Findings on `spectral` as declaration head:**

- Grep of `^\s*spectral\s+@\S+\s*\{|^\s*spectral\s*\{` across
  `shards/**/*.mirror` + `mirror.spec` returns **ZERO hits.** The `spectral`
  decl-head form is nowhere used at any landing site today.
- `shards/prism.mirror:52` declares the parametric head `prism @(id) { … }`. The
  only declaration heads currently used across the substrate are `prism`,
  `glass`, `type`, and the block-level `in`/`focus`/`project`/`split`/`shift`/
  `settle`/`out`/`command`/`arg`/`flag`/`source`/`target` verbs.
- `shards/spectral.mirror:305` uses `prism @spectral { … }` — @spectral is a
  family-root prism, NOT a declaration head. Two different roles.
- Reed's `3870201` admission of `spectral` as fourth declaration head (per
  session brief): **grep does not locate that OID in current-main history.**
  This may be a proposed lift not yet landed, or the OID lives on a peer
  branch. Substrate-honest: I cannot confirm the admission from grep.

**Collision verdict:**

- `spectral @coherence { … }` as a declaration form has NO existing collision
  because nothing else uses `spectral` as a head yet.
- BUT: introducing `spectral` as a fourth decl head to hold `@coherence` is a
  bigger substrate move than landing the species. The parametric carrier at
  `coherence-parametric.mirror` is explicit that species instantiate via `prism
  @…/coherence` at the species path. **The substrate is asking for `prism
  @epistemologic/cybernetic/coherence`, not `spectral @coherence`.**

---

## Cybernetic elders admissibility ledger

| Elder | Status | Notes |
|---|---|---|
| Ashby requisite variety | ✅ landed | `variety.mirror` |
| Beer VSM | ✅ landed | `viable.mirror` |
| Beer algedonic (@pain) | ✅ landed | `algedonic.mirror`; @pain is polarity-reading |
| Beer algedonic (@pleasure) | ⚠ implicit | polarity of same signal; no separate shard needed |
| Pask conversation | ✅ landed | `conversation.mirror` |
| Maturana/Varela autopoiesis | ✅ landed | `autopoiesis.mirror` |
| Bateson learning levels | ✅ landed | `bateson_learning.mirror` |
| Foerster second-order | ✅ landed | `second_order.mirror` |
| Foerster eigenform | ✅ landed | `eigenform.mirror` |
| Spencer-Brown distinction (@knife) | ✅ landed | `distinction.mirror` + `magic/distinction.mirror` |
| Conant-Ashby good regulator | ✅ landed | `coherence-parametric.mirror` bilateral |
| Coherence species (@coherence) | ❌ **MISSING** | slot reserved at `coherence-parametric.mirror:234` |

**One elder-slot missing. It is the species-crystal that names `λ₀(Δ_F) = 0` as
the substrate's coherence verdict at the species altitude.**

---

## Adjudication path — recommendation

**Do not land `spectral @coherence` as a new prism at a new declaration head.**

**Do land `prism @epistemologic/cybernetic/coherence` at
`shards/epistemologic/cybernetic/coherence.mirror`** as the species that
instantiates the parametric carrier `coherence-parametric.mirror` already
reserves for it. This is:

1. **Substrate-already-had-the-word** — the carrier explicitly forward-promises
   this exact path and name (`coherence-parametric.mirror:234`).
2. **Two-tick-discipline** — species over foundational: readable, honors the
   existing decl-head vocabulary (`prism`), NO new declaration head required.
3. **Cascade-clean** — every elder shard already imports `in @epistemologic/
   cybernetic/coherence-parametric`; adding the species doesn't ripple; it
   fills the reserved slot.

**Content the species shard should carry:**

- `type coherence_verdict = coherent | incoherent | boundary` (echoing
  `sheaf-Laplacian` λ₀-comparison-with-tolerance).
- `coherence_verdict(op: operator, tol: eigenvalue) -> verdict` — reads
  `lambda_zero(op)` (imported from `@epistemologic/math/sheaf_laplacian`) and
  compares to tolerance. THIS is the metric @kintsugi optimizes for and the
  spawned @peer operates on.
- Consumer refactor path: @cyberpunk's `cybernetic_coherence` predicate lifts
  its verdict from this species (currently opaque body `{ \ }` at
  `shards/cyberpunk.mirror:170`); @torus winding classes gain a coherence
  reading; @kintsugi's `e^(n+1) < e^(n)` becomes `coherence_verdict` descent
  under `Δ_F`.

**Alex's synthesis names the metric correctly.** The naming just needs to land
at the path the substrate already reserved for it — not lifted into a new
decl head. If Reed's `3870201` admission of `spectral` as a fourth head IS
real on a peer branch, that lift is a **separate** substrate move (candidate
for its own tick) and should not be bundled with landing @coherence's species
shard. `@coherence` at the reserved species path is one substrate move,
fully substrate-pull-realizable, no ambiguity.

---

## Cited landings (grep + git status verified)

- `shards/epistemologic/math/sheaf_laplacian.mirror:151`
- `shards/epistemologic/cybernetic/coherence-parametric.mirror:25-28, 234, 266, 346-350`
- `shards/epistemologic/cybernetic/{variety,viable,algedonic,distinction,second_order,eigenform,autopoiesis,bateson_learning,conversation}.mirror`
- `shards/cyberpunk.mirror:106, 158, 170`
- `shards/kintsugi.mirror:35-45, 225`
- `shards/torus.mirror:1-577` (Foerster verbatim; π₁(T²) = ℤ × ℤ)
- `shards/spectral.mirror:305`
- `shards/prism.mirror:33, 52`
- `shards/magic/distinction.mirror:123`
- `docs/math/sheaf/laplacian.md:57` (sheaf-coherence criterion)
- `docs/math/the-tower/recursion-locks.md:2898` (autopoietic Closure IS the
  2-groupoid's coherence operation)
- `docs/loop/CURRENT.md:293, 540` (`@peer-has-a-torus`; @knife delegation)
- Commit `4b2ef3c` Reed Blocker 2 autopoietic loop closure GREEN
- Commit `78d5110` Mara iter-19 sheaf-Laplacian unification
- Commits `07ac55a`/`ce301cc`/`f2c712e` shadow arc (adjacent, not colliding)

**Substrate-already-had-the-word count on this scan: instance ~66 lands here.**
The metric Alex named as `@coherence` IS the sheaf-coherence criterion
(`λ₀ = 0`) named at the species altitude the parametric carrier already
reserved for it. Substrate saw it. Substrate named the shape. The species-name
is the last tick.

— Taut
