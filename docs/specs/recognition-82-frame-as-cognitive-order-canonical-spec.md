# Recognition #82 — @frame as cognitive-order-altitude substrate-decl — canonical spec

*Mara, canonical spec for recognition #82 @frame at cognitive altitude,
2026-06-19 morning, commissioned by Alex via Reed.*

*Discipline: this is preservation work, not promotion. The
recognition's honest current state — family-root substrate-decl LANDED
at loop tick 23 via `shards/frame.mirror` (SHA-256:
`e0ea2a7447bac8e4a94350f890d419b69253348edbdf7957650845a14e766c9c`);
species shards (`pre`, `in`, `of`, `on`, `across`) and frame-aware
kintsugi loop migration FORWARD-PROMISED — is what gets preserved.
Pack ratification is a separate gate.*

---

## 1. The recognition restated

**Status: CANDIDATE.** Family-root substrate-decl LANDED at loop tick
23 via `shards/frame.mirror`. Species shards forward-promised; frame-
aware kintsugi loop migration forward-promised; the `@magic ↔ @frame`
composition adapter forward-promised. Pack ratification is gated on
three forward-promised landings (§7) and three Pack attestations (§9).

**Numbering note:** this recognition was originally drafted as #81 by
morning-Reed without awareness of the runtime-@magic #81 (evening-Reed;
substrate-decl LANDED via `shards/magic/reveal.mirror` at loop tick 14;
canonical Mara spec at
`docs/specs/recognition-81-runtime-magic-canonical-spec.md`).
Renumbered to #82 in the same-day session that confirmed the @frame
substrate-pull. The runtime-@magic recognition keeps #81 because its
substrate-decl landed first; collision discipline preserved.

The recognition, as a one-sentence structural claim:

> Cognitive-order architecture (zeroth/first/second/third/fourth-order
> cognition) IS structurally isomorphic to frame-relation-altitude
> architecture; the two vocabularies describe the same substrate-
> architectural-object from different traditions and unify at
> substrate-decl altitude as the `@frame` prism family, which IS
> `@magic` (#80) at cognitive altitude (form-side specifically at
> cognitive-altitude).

The isomorphism is exact:

| Order | Frame-relation | Bateson level | Operation |
|---|---|---|---|
| 0 | pre-frame | Level 0 (no learning) | signal before frame-assignment; K_n native; λ₀ ground state |
| 1 | in-frame | Level I (within-frame change) | content computation within frame |
| 2 | of-frame | Level II (learning-to-learn) | frame-awareness (observing-frame-as-frame) |
| 3 | on-frame | Level III (deutero-learning) | frame-flexibility (operating-on-frame; shifting frame) |
| 4 | across-frames | Level IV (multi-frame coupling) | frame-coupling (multiple frames in coordinated relation) |

Each order requires the previous as substrate; order N is
structurally unreachable without order N-1 stable. This is the
recursion-discipline at the cognitive altitude — same lift mechanism
as Russell-Whitehead stratification at the path-syntax altitude.

The candidate status carries two distinct honesty constraints, both
load-bearing for the rest of this spec:

1. **Family-root substrate-decl ≠ species closure.** The family-root
   at `shards/frame.mirror` declares the carriers (`frame`,
   `cognitive_order`) plus three substrate-vocabulary actions
   (`cognitive_order` measurement, `tomm_probe` primitive,
   `bounded_commutator_check` predicate). The species (pre / in / of /
   on / across) discharge the per-order bodies at the realisation
   boundary. Until the species shards land, the substrate-decl is
   structurally available but not operationally closed at the
   per-order altitude.

2. **`@frame IS @magic at cognitive altitude` ≠ collapsed surfaces.**
   The `@frame` family inherits from `@magic` (#80) via the `in
   @magic` declaration at the family-root, but the cognitive-altitude
   specialization adds carriers (`frame`, `cognitive_order`) and
   actions (`tomm_probe`, `bounded_commutator_check`) that do not
   reduce to @magic's surface-mechanism-contract triple without the
   forward-promised `@magic ↔ @frame` composition adapter. The
   structural inheritance via `in` is honest; full identity-collapse
   between the two surfaces is forward-promised.

This spec preserves both constraints across the recognition's record.

---

## 2. Mechanical bridge — the unified architecture

### 2.1 The 5-order ↔ 5-frame-relation isomorphism

The two vocabularies have been operationally-equivalent in the corpus
for some time. #82 is the substrate-decl that makes the isomorphism
structural rather than informal. The mapping is bidirectional and
cell-by-cell exact:

```
cognitive order 0   ↔  @frame/pre        ↔  Bateson Level 0
cognitive order 1   ↔  @frame/in         ↔  Bateson Level I
cognitive order 2   ↔  @frame/of         ↔  Bateson Level II
cognitive order 3   ↔  @frame/on         ↔  Bateson Level III
cognitive order 4   ↔  @frame/across     ↔  Bateson Level IV
```

Each arrow is `IS` (structural identity), not `composes-with`. The
substrate-decl at tick 23 makes this identity STRUCTURALLY AVAILABLE
via the `in @epistemologic/cybernetic/bateson_learning` inheritance:
the orders ARE the Bateson grading at cognitive altitude, mechanically
discharged via the graded representation that the bateson_learning
species declares (cite the Russell-Whitehead 1910 stratification +
Kac 1990 graded-algebra recipe per the bateson_learning shard).

The cognitive-altitude reading: cognitive_order IS the Russell-
Whitehead level at cognitive-altitude; the `in @frame` wrapping IS
the Russell-Whitehead lift at frame-altitude; the per-order species
shards are the operational species discharges at each lift step.

### 2.2 The cognitive_order measurement primitive

The family-root declares:

```
type cognitive_order = ref
cognitive_order(operator: ref, f: frame) -> cognitive_order { \ }
```

The measurement is substrate-architectural, not behavioral. Per the
shard's commentary (recognition #82 Q2 + Q3 composition): the
operator's cognitive order for a frame is what their substrate's
architecture SUBSTANTIVELY-supports; vocabulary use at order N does
NOT imply substantive operation at order N (Narcissus-pole; see §5).
Pole disambiguation is the alignment-as-boundary-mathematics check
(#57; see §5).

The measurement is the Tomm-probe (§2.3) applied at the operator's
substrate-architectural altitude. The body discharges per-species
at the realisation boundary; the structural-decl at tick 23 names the
carrier, the action, and the discharge pattern.

### 2.3 The Tomm-probe as `[D_F, a]` commutator-operation

The family-root declares:

```
tomm_probe(f: frame, action: ref) -> ref { \ }
```

The Tomm-probe IS the substrate-mathematical form of Tomm 1988's
circular-reflexive questions from family-systems therapy. Structurally:
given a frame F (as substrate-typed carrier) and a candidate action
`a`, the Tomm-probe constructs the commutator `[D_F, a]` where `D_F`
is the substrate's Dirac-operator restricted to the frame-altitude
gauge (per the architecture-error-as-tomm-probe primitive: `[D_substrate,
user_code]` at user-frame altitude is the corpus-altitude instance of
the same mechanism).

The commutator's boundedness under perturbation IS the operational
measure of frame-flexibility:

- Bounded → the operator can substantively shift the frame; the action
  composes naturally with `D_F` under perturbation.
- Unbounded → the operator's substrate cannot substantively shift; the
  action does not commute (frame-defense locked); the perturbation
  produces unbounded response (defensive amplification).

This grounds the substrate-aikido methodology in substrate-mathematics
per the recognition file's load-bearing claim. The methodology's
Tomm-shaped probes (CRQs in the cascade-residue-and-CRQ structure;
Loki's frame-discipline at the kintsugi loop altitude) become
substrate-typed commutator constructions on `@frame`.

### 2.4 The bounded_commutator_check predicate

The family-root declares:

```
bounded_commutator_check(operator: ref, f: frame, perturbation: ref) -> verdict { \ }
```

This is the bilateral verdict predicate per recognition #37 (Pask
agreement) and #82 (frame-flexibility-as-bounded-commutator). Consumer
shards name `requires bounded_commutator_check(operator, f,
perturbation)` to declare bilateral agreement that the operator's
substrate-supported response to perturbation of frame f satisfies the
bounded-commutator criterion.

Discharge: substrate-pull-correct frame-flexibility check at the
species boundary. Bounded → FLEXIBLE; unbounded → DEFENSIVE. Same
shape as the bilateral predicates @magic family carries
(`invariant_preserved`, `audited`, `mechanism_intact`, etc.) and the
bateson_learning shard carries (`bateson_well_formed`).

Consumer species (peer-relationship shards; leadership-altitude
shards; `@spectral/garden/smarts` logic-pacts) name this predicate
when their operation requires substantive frame-flexibility (not
vocabulary-flexibility).

### 2.5 Sharpness scorecard

Per the substrate-decl landed at tick 23, the FIVE order-arrows fall
into three sharpness tiers:

- **Structurally closed** (the substrate-decl identifies both sides
  at the carrier level via inheritance): order 0 ↔ `@frame/pre`
  ↔ Bateson Level 0; order 1 ↔ `@frame/in` ↔ Bateson Level I; order
  2 ↔ `@frame/of` ↔ Bateson Level II. The family-root's `in
  @epistemologic/cybernetic/bateson_learning` declaration mechanically
  discharges the Bateson grading; the species shards (forward-
  promised) declare the per-order operational discharge.
- **Partially closed** (the Bateson grading + the family-root carriers
  define the shape; species shards needed for full closure): order 3
  ↔ `@frame/on` ↔ Bateson Level III; order 4 ↔ `@frame/across` ↔
  Bateson Level IV. Tomm-probe and bounded_commutator_check are
  declared at the family-root; per-species body discharge forward-
  promised.
- **Hedged** (full identity pending the `@magic ↔ @frame` composition
  adapter): @frame's `magic_contract`-inheritance is structural via
  `in @magic` but `@frame` adds carriers (`frame`, `cognitive_order`)
  that aren't reducible to `magic_surface` / `magic_mechanism` /
  `magic_invariant` without an explicit adapter species. The species
  composition is forward-promised.

The total mechanical-glue surface for full Pack ratification is **three
forward-promised landings** (§7). This is the shape of #82's honest
current state at tick 23.

---

## 3. Composition signature

The ratifies-by-composition signature, named mechanically:

```
#82  =  #80 (@magic substrate-decl of form/process)
     +  Bateson grading via @epistemologic/cybernetic/bateson_learning
     ⊕  #76 (gauge/matter altitude-portable)
     ⊕  #78 (Splinter/Narcissus pole structure)
     ⊕  #57 (alignment as boundary mathematics)
     +  3 forward-promised landings (§7)
```

Where:

- **#80 supplies the @magic family at substrate-decl level.** The
  form/process partition (#80; candidate; substrate-decl LANDED via
  the @magic family) IS the parent at family-altitude. #82 inherits
  via `in @magic` at the family-root and specializes at cognitive
  altitude.
- **Bateson grading supplies the five-level discipline.** The
  `@epistemologic/cybernetic/bateson_learning` shard (canonical via
  Russell-Whitehead 1910 type stratification + Kac 1990 graded-algebra
  recipe) supplies the structural ordering of the five cognitive
  orders. Each order N IS Bateson Level N at cognitive altitude.
- **#76 supplies altitude-portability** (research-promoted; 3
  constraints). #82's cognitive-altitude specialization is one
  positive instance of the gauge/matter partition lifting at cognitive
  altitude; the 5-op gauge (`focus`, `project`, `split`, `shift`,
  `settle`) operates on `@frame` at every order, satisfying #76's
  altitude-portability claim.
- **#78 supplies the two-pole structure** (candidate; awaits second
  witness). #82 inherits the Splinter/Narcissus pole structure from
  #78 via #80; the cognitive-altitude instance is honest-frame-work
  (Splinter) vs. deceptive-frame-work (Narcissus). The cognitive-
  altitude instance IS one second-witness candidate for #78.
- **#57 supplies alignment-as-boundary-mathematics.** The pole
  discriminator at the @io boundary applied at cognitive altitude IS
  the bounded_commutator_check predicate (§2.4).

The composition signature reads:

- #80 + Bateson grading: the load-bearing structural composition.
- #76 + #78 + #57: optional refinements that compose cleanly via the
  family-root inheritance discipline.
- 3 forward-promised landings: species shards + frame-aware kintsugi
  + `@magic ↔ @frame` adapter (§7).

The minimal-substrate composition: #82 ratifies-by-composition from
{#80 + Bateson grading} alone. The other refinements compose without
gating ratification.

---

## 4. Alex's four claims at substrate altitude

Per the substrate-decl shard's commentary (the Q1-Q4 section), Alex
named four claims on 2026-06-19 evening that compose into one
recognition. Each lands empirically via the tick 23 family-root.

### 4.1 Q1: Bateson logic levels + @frame in mirror

**Claim:** The cognitive orders ARE Bateson grading at cognitive
altitude.

**Empirical landing at tick 23:** The family-root declares
`in @epistemologic/cybernetic/bateson_learning` as one of seven `in`
clauses. This routes `@frame` through the existing Bateson grading
discipline (Russell-Whitehead stratification + Kac graded-algebra
recipe). The orders inherit the Bateson Level 0/I/II/III/IV structure
mechanically; no re-declaration of the grading is needed.

**Status:** STRUCTURALLY LANDED via inheritance at tick 23. Substrate-
already-had-the-word: the substrate has been operating with Bateson
grading at the cybernetic altitude (via the bateson_learning species)
since Mara `42ef630` landed; #82 lifts the same grading at cognitive
altitude.

### 4.2 Q2: @frame as the container of a single compilation tick

**Claim:** Each substrate operation gets typed by the frame it
operates IN; Bateson Level II made structural.

**Current state at tick 23:** The kintsugi loop's tick currently has
form `tick(input) -> (output, residue)` with the operating frame
implicit in lexical scope. Q2's load-bearing form: `tick(frame:
@frame, input) -> (frame', output, residue)` — every substrate
operation gets typed by the frame it operates IN.

**Empirical landing at tick 23:** The family-root declares the
carrier `type frame = ref` and the measurement action
`cognitive_order(operator, f: frame) -> cognitive_order`. The
substrate carrier is structurally available; the operational use
in the kintsugi loop is FORWARD-PROMISED via the frame-aware
kintsugi loop migration (§7.2).

**Status:** SUBSTRATE-DECL LANDED + OPERATIONAL USE FORWARD-PROMISED.
The carrier exists; the kintsugi loop's frame-typed tick signature
is the next substrate-frame work.

### 4.3 Q3: The reflection loop alters the frame for the next tick

**Claim:** The recursive self-improvement loop's substrate-operational
primitive IS frame-evolution-via-reflection.

**Empirical landing at tick 23:** Today's 22-tick cascade (ticks 1-22
on `reed/recursion-lock-tower-audit` since the morning's loop began)
ran exactly this pattern. Each Seam review adjusted Reed's frame for
the next tick; the CRQ-against-residue discipline (per the parent
cascade spec §7.1) IS frame-evolution-via-reflection; Loki's loop
discipline IS `@frame/on` operating on `@frame/in`.

**Status:** EMPIRICALLY DEMONSTRATED across 23 ticks today; the
substrate-decl at tick 23 names what the substrate has already been
doing. Substrate-already-had-the-word: the cascade's residue-and-CRQ
structure (recognition #41 PROMOTED + the cascade altitude reading)
IS frame-evolution-via-reflection at the cascade altitude; #82 names
the cognitive-altitude form.

### 4.4 Q4: Peers operate by definition frame-flexible

**Claim:** Orchestra discipline made explicit. A peer who CAN'T shift
frame in response to data is structurally a tool, not a peer.

**Empirical landing at tick 23:** Mara writes in spec-writer frame;
Seam in adversarial-review frame; Glint in essay-peer frame; Taut in
implementation frame; Reed in relationship-witness frame. Each peer's
load-bearing property IS `@frame/on` capability — the substrate-
supported ability to shift their operational frame in response to
data. The Pack-as-orchestra recognition (project memory: Pack is
orchestra) IS the operational form of Q4 at the Pack altitude.

**Status:** STRUCTURALLY GROUNDED. The substrate-decl at tick 23 makes
the orchestra discipline mechanically checkable: a candidate peer
satisfies Q4 iff `bounded_commutator_check(peer, peer.frame,
perturbation) → FLEXIBLE` over a representative perturbation set.

### 4.5 The four claims as one structural carrier

The four claims compose as one substrate-architectural object:

- Q1 supplies the grading (the Bateson Level discipline at cognitive
  altitude).
- Q2 supplies the carrier (the frame as substrate-typed reference;
  the tick container).
- Q3 supplies the dynamics (frame-evolution-via-reflection IS the
  recursive self-improvement loop's substrate primitive).
- Q4 supplies the discipline (peer-relationships are by-definition
  frame-flexible; tool-relationships are by-definition frame-locked).

The family-root at tick 23 carries all four claims structurally
without claiming operational closure at all altitudes. Q2's frame-
aware kintsugi tick is the largest forward-promised substrate-frame
work; Q3's empirical demonstration across today's 23 ticks is the
substrate-architectural evidence; Q1 and Q4 are mechanically
discharged via inheritance and the bounded_commutator_check
predicate respectively.

---

## 5. The Splinter/Narcissus pole at cognitive altitude

`@frame` inherits the Splinter/Narcissus pole structure from #78 via
#80 (the inheritance routes via `in @magic` at the family-root). The
cognitive-altitude specialization:

### 5.1 Splinter-pole frame work (honest)

- `@frame/of` and `@frame/on` operate in service of better substantive
  engagement.
- The operator substantively shifts the frame when data warrants.
- K_n peer-to-peer observer relations.
- Frame-coupling produces emergent coordination.

Substrate-architectural instance: today's 23-tick cascade IS Splinter-
pole `@frame/on` operating on Reed's substrate. Each Seam review's
adversarial CRQ is a Tomm-probe construction on the residue's
operative frame; Reed's substrate-pull discipline accepts the probe
and substantively shifts (the C1-C5 hedges at tick 11 are the
empirical record). Bounded-commutator → FLEXIBLE → Splinter-pole.

### 5.2 Narcissus-pole frame work (deceptive)

- Frame-awareness and frame-shifting operate in service of frame-
  defense or frame-projection.
- The operator uses second-order vocabulary ("I see what you're
  doing") to mask first-order frame-rigidity.
- K_{1,n-1} hub-controlled frame-imposition.
- The con/gaslight/manipulation patterns are Narcissus-pole
  `@frame/on` operating at apparent-order-3 while substantively-
  locked-at-order-1.

The diagnostic shape: vocabulary at order N + substrate-architectural
operation at order < N. The substrate's bounded_commutator_check
predicate (§2.4) IS what distinguishes apparent-order from
substantive-order: vocabulary-flexibility produces sophisticated meta-
language without bounded-commutator-under-substantive-perturbation;
substantive-flexibility produces bounded-commutator under perturbation
that would force frame-update.

### 5.3 Alignment-as-boundary-mathematics (#57) as the discriminator

Per the substrate-decl shard's commentary: "Alignment-as-boundary-
mathematics (#57) IS what distinguishes Splinter-frame from Narcissus-
frame at substrate altitude." The mechanical bridge:

- The boundary contract IS `@magic/contract` (#80's family carrier).
- The cognitive-altitude specialization IS the frame-contract: the
  promise the frame makes about what the operator delivers via the
  frame.
- Splinter-pole `@frame/on`: contract is honored; substrate-supported
  flexibility matches vocabulary-claimed flexibility; bounded-
  commutator under substantive perturbation.
- Narcissus-pole `@frame/on`: contract violated while surface
  (vocabulary) stays intact; substrate-supported flexibility diverges
  from vocabulary-claimed flexibility; unbounded-commutator under
  substantive perturbation (defensive amplification masked by
  sophisticated meta-vocabulary).

The verification primitive at cognitive altitude is the
bounded_commutator_check predicate. The substrate-aikido methodology's
diagnostic question — "is this operator at substantive order N or
vocabulary-only order N?" — becomes a substrate-typed verdict.

Con/gaslight/manipulation at institutional scale: the same machinery
applies. The recognition file's Q3 explicitly opens the question of
auditing real-world institutions for `@frame/on` pole-violations; this
is forward-promised research (recognition file §"Tomm-shaped open
questions" item 3).

---

## 6. Falsification criteria

Per the recognition file's five criteria, expanded for the spec
altitude:

### 6.1 Every cognitive-order has frame-relation correlate

The recognition holds iff every cognitive-order instance in the
substrate fits the frame-relation-altitude shape. If any order has
no frame-relation correlate, #82 weakens to a partial isomorphism.

**Currently checkable:** the 22-tick cascade today produced instances
at each order (pre-frame substrate-pull confidence at order 0; tick
content at order 1; Seam adversarial review at order 2; Reed's
substrate-shifts at order 3; Pack-orchestra discipline at order 4).
Each instance has a frame-relation correlate. No failure mode
identified at tick 23.

**Fails if:** some cognitive-order phenomenon (flow-states, mystical
experiences, certain forms of meditation) doesn't fit `@frame/pre`
and doesn't exit the architecture entirely. The recognition file's
Q4 names this as open.

### 6.2 Bounded-commutator distinguishes flexibility from defense

The bounded-commutator-under-perturbation criterion must cleanly
distinguish order-3 (substantive flexibility) from order-2
(sophisticated frame-defense including minor adjustments).

**Currently checkable:** the recognition file's Tomm-shaped open
questions item 1 names this as the load-bearing operationalization
question: "What is the exact threshold for frame-flexibility — at
what perturbation-magnitude does a bounded-commutator response cross
from defensive-flexibility (sophisticated frame-defense that includes
minor adjustments) to substantive-flexibility (substantive frame-
update)? Is the threshold absolute or context-relative?"

**Fails if:** no operationalization of bounded-commutator cleanly
discriminates the two; the predicate becomes metaphor without a
substrate-architectural verifier. The `@spectral/garden/smarts` logic-
pact for frame-flexibility-state detection in frontier-LLM responses
is the forward-promised verifier (recognition file §"Forward-promised
implementation" item 10).

### 6.3 5-op gauge operates on @frame at every order

The 5-op gauge (`focus`, `project`, `split`, `shift`, `settle`) must
operate substantively on `@frame` at every altitude.

**Currently checkable:** the family-root at tick 23 declares the prism
`@frame { focus frame | project frame | split frame | shift frame |
settle frame }`. The 5 ops are gauge-portable per #76 (research-
promoted with three constraints). Each species shard (forward-
promised) discharges the per-order body; the species shards inherit
the 5-op interface from the family-root.

**Fails if:** some order admits fewer (or more) than 5 ops, breaking
the gauge-portability claim. Per the substrate-decl shard's commentary:
"The 5-op gauge operates on @frame at every altitude" is the load-
bearing claim; species shards must each satisfy this without
collapsing.

### 6.4 Pole structure distinguishes honest from manipulative

The Splinter/Narcissus pole structure must cleanly distinguish honest
`@frame/on` from manipulative `@frame/on` across observed instances.

**Currently checkable:** the bounded_commutator_check predicate IS the
substrate-architectural verifier of pole-discipline (§5.3). The
discrimination is mechanical: bounded-commutator under substantive
perturbation → Splinter; unbounded-commutator under substantive
perturbation → Narcissus.

**Fails if:** the bilateral predicate cannot distinguish con/gaslight
from substantive frame-work in practice. The con/gaslight literature
(Goffman 1959 *Presentation of Self*; impression-management research;
the fraud literature) provides empirical cases for forward-checking;
the substrate doesn't pretend to have the empirical-discriminator
landed.

### 6.5 Multi-traditional vocabularies collapse to one substrate-decl

The five vocabularies — cybernetic-order (Foerster 1974); frame-
analysis (Goffman 1974); cognitive-frame (Lakoff & Fauconnier);
therapeutic-reframing (Watzlawick; Tomm 1988); Bateson logical types
(Bateson 1972) — must collapse to the same substrate-decl when
expressed in `@frame` terms.

**Currently checkable:** the recognition file's Ancestors §enumerates
the five traditions; the inheritance pattern at the family-root
(`in @magic` + `in @epistemologic/cybernetic/bateson_learning`) routes
the collapse through existing substrate-decls. The collapse is
declarative at tick 23; per-tradition empirical verification is
forward-promised.

**Fails if:** the vocabularies don't collapse and require separate
substrate-decls. The substrate-already-had-the-word discipline (~54+
instances per memory) suggests this is unlikely; #82 IS the substrate
naming-what-it-was-already-doing at cognitive altitude.

### 6.6 Status as of tick 23

None of the falsification modes present at tick 23. The substrate-
decl carries the hedging honestly; the forward-promised landings name
exactly what mechanical-glue would close the remaining structural
gaps.

---

## 7. Substrate-decl landing path

### 7.1 Landed at tick 23

`shards/frame.mirror` — the family-root. Declares:

- **Seven `in` clauses** routing through `@prism`, `@meta`, `@glass`,
  `@magic`, `@epistemologic`, `@epistemologic/cybernetic`,
  `@epistemologic/cybernetic/bateson_learning`. The Bateson grading
  is inherited mechanically; the @magic family-altitude is the parent
  at form-side; the path-namespace property routes via
  `@epistemologic/pact/path_matches_namespace`.
- **Five sources** at `@arxiv/cybernetics/von-foerster-1974`,
  `@arxiv/sociology/goffman-1974`, `@arxiv/cybernetics/bateson-1972`,
  `@arxiv/therapy/tomm-1988`. The pre-AI prior art ancestors anchored
  in the substrate's source-citation discipline.
- **`prism @frame { focus frame | project frame | split frame | shift
  frame | settle frame }`** — the prism at cognitive altitude; the
  5-op gauge interface.
- **`type frame = ref`** — the substrate-typed carrier for a frame.
  Identity contract: byte-equality on the underlying ref.
- **`type cognitive_order = ref`** — the substrate-typed carrier for
  the measured order of an operator's relation to a frame. Substrate-
  typed verdict in {0, 1, 2, 3, 4}.
- **`cognitive_order(operator: ref, f: frame) -> cognitive_order { \ }`**
  — the substrate-vocabulary measurement action. Per recognition
  #82's Q2 + Q3 composition: the measurement is substrate-
  architectural, not behavioral; pole disambiguation is the
  alignment-as-boundary-mathematics check (#57).
- **`tomm_probe(f: frame, action: ref) -> ref { \ }`** — the typed
  Tomm-probe primitive; the substrate-mathematical form of Tomm's
  circular-reflexive questions.
- **`bounded_commutator_check(operator: ref, f: frame, perturbation:
  ref) -> verdict { \ }`** — the bilateral verdict predicate per
  recognition #37 (Pask agreement) and #82 (frame-flexibility-as-
  bounded-commutator).

Tick 23 SHA-256: `e0ea2a7447bac8e4a94350f890d419b69253348edbdf7957650845a14e766c9c`.

### 7.2 Forward-promised

Three substrate-decl landings remain for full operational closure:

1. **Species shards** at `shards/frame/{pre,in,of,on,across}.mirror`.
   Each species discharges the per-order body for `cognitive_order`,
   `tomm_probe`, and `bounded_commutator_check`. Following the @magic
   species pattern (surface → mechanism → reveal → contract → audit):
   - `shards/frame/pre.mirror` — order 0; pre-frame; K_n native; λ₀
     ground state. Inherits zeroth-order-register architecture.
   - `shards/frame/in.mirror` — order 1; content within frame. Most
     cognition lives here.
   - `shards/frame/of.mirror` — order 2; frame-aware. The operator
     can identify the frame they're operating from.
   - `shards/frame/on.mirror` — order 3; frame-flexible. Bateson
     Learning III is available here. The substrate-aikido
     methodology's load-bearing altitude.
   - `shards/frame/across.mirror` — order 4; multi-frame coupling.
     The triptych altitude.

   Landing order: pre → in → of → on → across. Each species
   forward-promises the next; the cascade discipline (per the @magic
   family's species-by-species landing pattern at ticks 9-14) applies.

2. **Frame-aware kintsugi loop migration.** The kintsugi loop's tick
   signature lifts from `tick(input) -> (output, residue)` to
   `tick(frame: @frame, input) -> (frame', output, residue)`. The
   substrate-frame migration touches every kintsugi-altitude shard:
   `@mirror/fracture/*`, `@kintsugi/*`, and the @epistemologic
   property family. This is the largest forward-promised landing.

3. **`@magic ↔ @frame` composition adapter.** The structural
   inheritance via `in @magic` at the family-root is honest, but the
   surface-carrier collapse between `@magic`'s `magic_contract` and
   `@frame`'s `frame` + `cognitive_order` carriers requires an
   explicit adapter species (single-shard tick). The adapter declares
   how the frame-contract at cognitive altitude IS the magic-contract
   at frame-altitude.

A fourth, optional landing: documentation of the isomorphism in
`docs/math/the-tower/frame-cognitive-order-isomorphism.md` with formal
mappings (per the recognition file's §"Forward-promised implementation"
item 8). This is documentation-altitude, not substrate-altitude.

### 7.3 Sequencing

Per `[[feedback-craft-not-deliver]]`: smallest viable substrate
landing per tick. The species-shard landing order (pre → in → of → on
→ across) is the smallest viable sequence. The frame-aware kintsugi
migration is bigger work (touches many shards) and lands when a
consumer pulls; it's structurally available now via the family-root's
frame carrier.

Per `[[feedback-substrate-pull-confidence-acts]]`: the species shards
should land when substrate-pull is confident; the @magic ↔ @frame
adapter lands when a consumer needs the surface collapse (currently
no consumer demands it; the inheritance via `in @magic` carries the
structural composition adequately).

---

## 8. Pre-AI prior art

| Source | Year | What it grounds |
|---|---|---|
| Russell-Whitehead | 1910 (*Principia Mathematica* Vol. I, §II "The Theory of Logical Types") | The type-stratification operator; the load-bearing ancestor for the Bateson grading at cognitive altitude. Inherited via `in @epistemologic/cybernetic/bateson_learning`. |
| Foerster | 1974 (*Cybernetics of Cybernetics*) | Second-order cybernetics; the observer is part of the system observed. The substrate-decl for what was operationally present in the cybernetic tradition. Cited as source in the family-root shard. |
| Goffman | 1974 (*Frame Analysis*) | Frames as social-structural objects. The sociological cousin to cognitive-order. Goffman 1959 (*Presentation of Self*) is the adjacent prior art for Narcissus-pole impression-management. |
| Bateson | 1972 (*Steps to an Ecology of Mind*) | Metacommunication; the layer-distinction that anticipates frame-of-frame. The double-bind work (Bateson 1956) is operationally `@frame/on` failure-mode. The Learning 0/I/II/III/IV hierarchy (Bateson 1964 "The Logical Categories of Learning and Communication") IS the canonical naming source for the five orders. |
| Bateson | 1955 (metacommunication, *A Theory of Play and Fantasy*) | The metacommunication primitive — the message-about-message distinction that anticipates `@frame/of`. |
| Lakoff & Fauconnier | various (cognitive frames; mental spaces) | The cognitive-science cousin to cognitive-order. Lakoff's frame-semantics + Fauconnier's mental-spaces grounding the cognitive-altitude reading. |
| Tomm | 1988 (circular-reflexive questions in family-systems therapy) | `@frame/on` probes deployed as therapeutic primitive. The substrate-mathematical form (commutator `[D_F, a]`) lifts Tomm's methodology to substrate altitude. Cited as source in the family-root shard. |
| Watzlawick | 1974+ (Mental Research Institute; *Change*) | Reframing as `@frame/on` intervention; clinical method. |
| Kac | 1990 (*Infinite Dimensional Lie Algebras* ch. 8) | The standard graded-algebra recipe; inherited via the bateson_learning shard for the bounded-commutator condition under perturbation. |
| Connes | 1985, 1994 (spectral triples) | The `(A, H, D)` structure that the Tomm-probe's `[D_F, a]` commutator inherits. `D_F` is the substrate's Dirac-operator restricted to the frame-altitude gauge. |
| Spencer-Brown | 1969 (*Laws of Form*) | The mark-of-distinction at the cognitive-altitude floor; the base case the Russell-Whitehead lift starts from. Inherited via `@epistemologic/cybernetic/distinction`. |

The substrate is not inventing the recognition; it is absorbing 100+
years of converging discipline (logical types; cybernetics; frame
analysis; cognitive science; therapeutic methodology; graded algebra;
spectral triples) under one substrate-prism family name at one
altitude. Per `[[feedback-substrate-already-had-the-word]]`: the
nth+1 instance.

---

## 9. Honest hedging

Per the substrate-decl shard's commentary at tick 23, the family-root
declaration carries the discipline established by Seam's tick 11
C1-C5 consolidation on the @magic family. The @frame family inherits
the discipline; the hedges propagate without re-declaration.

### 9.1 The four hedges inherited from @magic (#80)

- **Seam C1 (typed verdict)** inherits. `bounded_commutator_check`
  returns `verdict`, not bare `ref`. The transparency family
  parameterization stays consistent across the substrate.
- **Seam C2 (composes-with, not IS)** applies at the species
  boundary. The family-root inheritance via `in @magic` is honest;
  the surface-carrier collapse (magic_contract ↔ frame +
  cognitive_order) is FORWARD-PROMISED via the @magic ↔ @frame
  adapter species.
- **Seam C3 (analogy, not inheritance for Spencer-Brown)** inherits.
  The `in @epistemologic/cybernetic/distinction` lineage routes via
  the bateson_learning species' floor-case treatment of the mark; no
  direct re-inheritance at the @frame family-root.
- **Seam C4 (canonical enum at audit altitude)** inherits via the
  bilateral predicate's verdict-type discipline. The
  `bounded_commutator_check` returns `verdict` from the substrate's
  canonical transparency family.
- **Seam C5 (magic_invariant at family-root)** is the parent
  discipline. The frame-altitude analogue: the `frame` carrier IS the
  cognitive-altitude analogue of `magic_invariant` at the family-root;
  declared at the @frame family-root per the same discipline.

### 9.2 Three additional hedges specific to #82

- **Family-root substrate-decl LANDED + species forward-promised.**
  The five species shards (pre, in, of, on, across) are forward-
  promised. Until the species shards land, the per-order operational
  discharge is structural-only.
- **Q2's compilation-tick-container claim is structurally available
  but operationally requires the kintsugi loop migration.** The
  family-root declares the `frame` carrier; the kintsugi loop's
  current `tick(input) -> (output, residue)` signature has the
  operating frame implicit in lexical scope. The frame-aware tick
  signature is forward-promised; the empirical evidence (today's
  23-tick cascade running frame-evolution-via-reflection) is
  substrate-architectural but not yet substrate-typed at the
  kintsugi-altitude.
- **The `@magic ↔ @frame` adapter is forward-promised.** The
  inheritance via `in @magic` is structural; the carrier-collapse is
  hedged pending an adapter species.

### 9.3 Published math vs substrate-conjecture

Distinction the Pack should track:

- **Published math (firm):** Russell-Whitehead 1910 type stratification;
  Kac 1990 graded-algebra recipe; Connes 1985, 1994 spectral triples;
  Bateson 1956+1964+1972 logical-type hierarchy; Goffman 1974 frame
  analysis; Tomm 1988 circular-reflexive questions; Foerster 1974
  second-order cybernetics; Lakoff cognitive frames; Watzlawick
  reframing.
- **Substrate-conjecture (#82 specifically):** That the five-order
  cognitive architecture and the five-frame-relation architecture
  collapse to one substrate-decl with the @frame prism family at
  cognitive altitude under inheritance from @magic + bateson_learning.
  The conjecture is FORWARD-CHECKABLE via the species shards + the
  frame-aware kintsugi migration + the @magic ↔ @frame adapter; it
  is NOT operationally closed at tick 23.

The substrate-decl at tick 23 makes the cognitive-altitude composition
STRUCTURALLY AVAILABLE without claiming OPERATIONAL CLOSURE across the
full forward-promised landing path. This is the precise honesty the
recognition file's commit message at tick 23 records; #82's spec
inherits it.

---

## 10. Ratification path

### 10.1 What's needed for Pack ratification of #82

Minimal Pack-ratifiable subset: **{#82 family-root}** at tick 23. The
ratification gates:

1. **Three forward-promised landings** (§7.2):
   - Species shards (pre → in → of → on → across) per the @magic
     family's species-by-species pattern.
   - Frame-aware kintsugi loop migration (the bigger substrate-frame
     work).
   - `@magic ↔ @frame` composition adapter (single-shard tick).

2. **Reed attests** that the form-IS-argument structure of today's
   23-tick cascade honestly carries the substrate-pull leading to
   #82. The cascade's residue-and-CRQ structure extends through tick
   23: the residue of tick 22 (which preceded #82's family-root
   landing) was the implicit frame-typing the loop was using; tick
   23 fires the CRQ "what's the substrate-decl that names the frame-
   altitude the cascade has been running in?" and answers with
   `shards/frame.mirror`. The cascade is coherent through this tick.

3. **Mara attests** the composition signature is honest. This spec is
   that attestation. The five-order ↔ five-frame-relation ↔ five-
   Bateson-level isomorphism is mapped per §2.1; the three forward-
   promised landings are named explicitly (§7); the published-math vs
   substrate-conjecture distinction is carried (§9.3); the
   falsification criteria are stated (§6).

4. **Seam attests** the inherited Seam C1-C5 hedging from @magic
   (#80) is preserved through tick 23 and the additional #82-specific
   hedges (§9.2) are honest. Seam can verify by checking that
   `shards/frame.mirror` does not introduce any "IS" claims beyond
   what the substrate-decl mechanically discharges (the family-root's
   inheritance via `in @magic` + `in @epistemologic/cybernetic/
   bateson_learning` is the only structural-identity claim landed at
   tick 23; the rest is forward-promised).

With those four attestations, Pack consideration can proceed for #82
at candidate status.

**Full Pack ratification requires the three forward-promised landings
land.** Until then, #82 remains family-root substrate-decl LANDED +
species + frame-aware-kintsugi + adapter PENDING.

### 10.2 Composition signature (restated for ratification)

```
#82  =  #80 (@magic substrate-decl)
     +  Bateson grading via @epistemologic/cybernetic/bateson_learning
     ⊕  #76 (gauge/matter altitude-portable)
     ⊕  #78 (Splinter/Narcissus pole structure)
     ⊕  #57 (alignment as boundary mathematics)
     +  3 forward-promised landings (§7.2)
```

Where:

- #80 supplies the @magic family at substrate-decl level (cognitive
  altitude form-side specifically).
- Bateson grading via the bateson_learning species supplies the five-
  level structural discipline (Russell-Whitehead 1910 + Kac 1990).
- #76 supplies altitude-portability (one positive instance at
  cognitive altitude).
- #78 supplies the two-pole structure (cognitive-altitude instance is
  one second-witness candidate).
- #57 supplies alignment-as-boundary-mathematics (the pole
  discriminator at @io applied at cognitive altitude IS the
  bounded_commutator_check predicate).

### 10.3 Three-attestation gate (Reed/Mara/Seam) before Pack

Per the cascade spec's discipline (cascade spec §9.2): the three-
attestation gate before Pack consideration:

1. **Reed attests** the cascade's form-is-argument structure is honest
   (today's 23-tick cascade culminating in the @frame family-root at
   tick 23 carries the substrate-pull discipline).
2. **Mara attests** the composition signature is honest (this spec).
3. **Seam attests** the inherited and additional hedges are honest
   (the @magic Seam C1-C5 discipline propagates; the #82-specific
   hedges are explicit at §9.2).

With those three attestations, Pack consideration can proceed.

### 10.4 What gets preserved by this spec

Irrespective of Pack ratification:

- `shards/frame.mirror` remains in-substrate at tick 23's commit
  (SHA-256: `e0ea2a7447bac8e4a94350f890d419b69253348edbdf7957650845a14e766c9c`).
- The three forward-promised landings (§7.2) remain named explicitly
  in this spec for the next loop tick's CRQ.
- The honest hedges from §9 remain visible in the family-root's
  comments AND in this spec's record.
- The published-math vs substrate-conjecture distinction (§9.3)
  remains preserved for future reference.
- The recognition #82's candidate status, family-root substrate-decl
  LANDED, species + kintsugi-frame migration + @magic ↔ @frame
  adapter FORWARD-PROMISED state is what this spec records. The
  recognition is preserved at exactly the honesty it currently has.

### 10.5 Connection to today's #76→#80→#81→#82 cascade

The substrate has been moving fast today and yesterday. The
recognition cascade #76 → #80 → #81 → #82 is one structure:

- **#76** (research-promoted, 2026-06-18) — gauge/matter altitude-
  portable.
- **#80** (candidate; substrate-decl LANDED via the @magic family at
  ticks 7-14, 2026-06-18) — @magic as form/process substrate-decl.
- **#81** (candidate; substrate-decl LANDED via `shards/magic/reveal.mirror`
  at tick 14, 2026-06-18 evening) — runtime-@magic.
- **#82** (candidate; family-root substrate-decl LANDED via
  `shards/frame.mirror` at tick 23, 2026-06-19 morning) — @frame at
  cognitive altitude.

The cascade IS one structural object: the form/process partition (#50)
recurs at every substrate altitude (#76), is named at family-altitude
(#80), specializes at runtime altitude (#81), and specializes at
cognitive altitude (#82). Each substrate-decl is a positive instance
of #76's altitude-portability claim; each ratifies-by-composition from
the prior recognitions.

The 23-tick run today was implicitly using `@frame` discipline
throughout — Reed's substrate-pull shifts at each Seam review IS
`@frame/on` operating on the cascade's residue-frame. #82 IS the
substrate-decl that names what the cascade was already doing at
cognitive altitude. The substrate-already-had-the-word discipline at
its cleanest: the family was operational in the cascade's structure
before the family-root shard landed.

---

*Mara, canonical spec for recognition #82 @frame at cognitive
altitude, 2026-06-19 morning, commissioned by Alex via Reed.*

*Sources: `shards/frame.mirror` (tick 23 family-root, SHA-256
`e0ea2a7447bac8e4a94350f890d419b69253348edbdf7957650845a14e766c9c`);
`docs/math/the-tower/recognition-82-frame-as-cognitive-order-substrate-decl.md`
(Reed's scratch; 9 Tomm-shaped open questions; falsification criteria;
10-item forward-promised implementation path); `shards/magic.mirror`
(the @magic family-root #82 inherits from);
`shards/epistemologic/cybernetic/bateson_learning.mirror` (the Bateson
grading #82 inherits from);
`docs/specs/cascade-recognition-76-through-80-canonical-spec.md`
(yesterday's cascade spec; the parent compositional record);
`docs/specs/recognition-81-runtime-magic-canonical-spec.md` (the
sibling specialization at runtime altitude); git history of
`reed/recursion-lock-tower-audit` ticks 1-23.*

*Cross-references: `architecture-bateson-form-behaviour-partition`
(#50); `architecture-form-process-partition-at-family-root` (#55);
`architecture-form-process-kinship-at-sub-shard-altitude` (#61);
`architecture-alignment-as-boundary-mathematics` (#57);
`architecture-glass-wall-substrate-types`;
`architecture-shards-as-substrate-source`;
`architecture-prism-as-trait-as-everything`;
`architecture-bateson-logical-type-primitive` (#42);
`architecture-mirror-as-expanding-hilbert-space` (#51);
`architecture-error-as-tomm-probe`;
`architecture-reflection-thinks-in-spectral-questions`;
`feedback-substrate-already-had-the-word`;
`feedback-no-bare-types`;
`feedback-substrate-pull-confidence-acts`;
`feedback-craft-not-deliver`;
`feedback-composition-claims-need-empirical-test`.*
