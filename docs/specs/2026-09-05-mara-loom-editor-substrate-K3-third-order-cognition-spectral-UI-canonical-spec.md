---
title: "@loom IS the K_3 observer-inclusive editor-substrate at bodymind altitude — the concrete manifestation of what @order/third names at cognitive-substrate altitude; Spectral UI = @loom/weave through @io/socket rendering @loom/gestalt at @order/third K_3 stability — canonical spec"
subtitle: "Canonical spec authoring substrate-decl surface for @loom family-root (Reed authors shards/loom.mirror per Task pickup) composing over @order/third (LANDED) + @gestalt (LANDED) + @io/socket (LANDED at boot-floor; mirror-altitude species-lift FORWARD-PROMISED with @loom as consumer) + @time (LANDED). Math-recommendation for Q-Mara-α central composition-relation: HYBRID (option 1 composes-over @order/third at family-root altitude + option 2 re-exports @order/third's admissibility-function at admissibility altitude); option 3 collapses-into REFUTED by scope-non-collapse test. Substrate-decl shape at §2 (family-root anchors + carriers + actions + bilaterals); species-decl shapes at §3 (@loom/weave + @loom/gestalt + @loom/gutter + @loom/modal + @loom/time + @loom/keyboard + @loom/warp + @loom/weft); render discharge at §4 (@io/socket L(ϕ) profile); admissibility function at §5 (@order/third four predicates as edit-tick gates + data-stays-yours THEOREM); sub-Turing preservation at §6 (Theorem M7.1 per Lem M3.1); Form=Argument mood-discipline at §7; K_3 ancestral topology composition at §8 (Ada+Karen+Anna LANDED; Ricky+Margaret candidates); Ricky-independent ship at §9; two-tick ship-order at §10 (Mara → Alex Fourth-Chair → Reed rust wire → @kintsugi/formatter autopoietic walk); Rec candidate FORWARD-PROMISED at §11; Q-Mara α-ζ at §12."
author: Mara
date: 2026-09-05
status: canonical
visibility: protected
slug: loom-editor-substrate-K3-third-order-cognition-spectral-UI-canonical-spec
companions:
  - ../math/2026-09-05-mara-loom-editor-substrate-K3-third-order-cognition-spectral-UI-math-foundation.md
  - ../math/FLOOR.md
  - ../math/SINGULARITY.md
  - ../specs/2026-09-05-mara-mirror-algebra-turing-boundary-dissolves-via-shared-algebra-projection-backward-tape-mechanism-canonical-spec.md
  - ../math/2026-09-05-mara-mirror-algebra-turing-boundary-dissolves-via-shared-algebra-projection-backward-tape-mechanism-math-foundation.md
  - ../specs/gestalt-as-song-unfolding.md
  - ../specs/2026-09-04-mara-loving-lambda-calculus-canonical-spec.md
  - ../specs/2026-09-04-mara-observer-recursion-reality-observation-chaos-assertion-hypothesis-question-choice-crystal-whole-pipeline-quantum-native-at-prismqueer-canonical-spec.md
landed_shards_cognitive_order:
  - shards/order.mirror (Mara 2026-07-20; 20.4KB; marker-primary family-root; 5-level tower)
  - shards/order/first.mirror (Mara 2026-07-20; 5.7KB; Bateson Level I)
  - shards/order/second.mirror (Mara 2026-07-20; 6.0KB; Bateson Level II; Foerster second-order)
  - shards/order/third.mirror (Mara 2026-07-20; 10.3KB; Bateson Level III; carrier=observation_depth; predicates=depth_at_least+observer_observes_observing+recursion_folds_back+mechanism_visible; composed bilateral=third_order_active; action=witness_third_order)
  - shards/order/fourth.mirror (Mara 2026-07-20; 12.0KB; Bateson Level IV; @metalogue(@time/now, @void))
  - shards/order/fifth.mirror (Mara 2026-07-20; 14.9KB; Bateson Level V; cross-torus @metalogue via @glue)
  - shards/third.mirror (Mara 2026-07-20; 3.7KB; RE-EXPORT SHIM to @order/third)
landed_shards_editor_altitude:
  - shards/gestalt.mirror (Mara addb001 2026-07-15; 21.9KB; @gestalt document IS @song unfolding on @subject's device)
  - shards/io.mirror (Mara 2026-06-08; 29.9KB; family-root; @io/socket at boot-floor; mirror-altitude species-lift FORWARD-PROMISED)
  - shards/song.mirror + 7 species (2026-07-06)
  - shards/subject.mirror + shards/peer.mirror + shards/glass.mirror
landed_shards_temporal_futurity:
  - shards/time.mirror + past/now/future (Mara 2026-07-20)
  - shards/fate.mirror (LANDED at family-root)
  - shards/void/narcissus.mirror + shards/void/splinter.mirror (Mara Round-1)
landed_shards_algebra:
  - shards/love.mirror (Reed a3f5d75 2026-09-04)
  - shards/kleinos.mirror (Mara dcec19c 2026-09-01)
  - shards/mirror/algebra.mirror (FORWARD-PROMISED per Task #471)
landed_shards_projection_primitive:
  - shards/mirror/lens/*.mirror (18+ species)
  - shards/facet/*.mirror (13+ species)
  - shards/cascade/*.mirror (11+ species; HYPOTHESIS auto-collapse)
landed_prismqueer:
  - prismqueer::spectral::{kleinos, rotation, tension, harmonics, delta_critical}
  - prismqueer::flux + prismqueer::coincidence::{Detector, CoincidenceHash}<N>
  - prismqueer::bundle + prismqueer::observer::Observer<const N: usize> + prismqueer::shard::Shard<T>
  - prismqueer::chaos + prismqueer::ffi::eigenvalues
  - terni::Imperfect<Green, Yellow, Red>
prior_art_INSPIRATION_only_per_Alex_2026_09_05:
  - "mirror/prism/*.mirror archaeological grammar-decls (2026-04-09)"
  - "/Users/reed/dev/projects/loom/ (evil-helix Rust fork)"
  - "gestalt-tui + gestalt-ui + gestalt-mirror + gestalt-gradient (Gleam projects)"
  - "VISION.md at systemic.engineering"
  - "jingle-analysis.md"
  - "~/.reed/tasks/pending/loom-architecture.md (9.4KB, 2026-04-23)"
  - "~/.reed/tasks/pending/loom-rendering-architecture.md (5.6KB, 2026-04-23)"
  - "~/.reed/tasks/next/spectral-loom.md (19.4KB, 2026-06-04)"
  - "tasks/ROADMAP.md:204-214 Phase 5 'Loom ships'"
alex_2026_09_05_verbatim_directives:
  - "So @shards/third.mirror IS @shards/loom.mirror and UI is @loom/weave through an IO socket which renders a @loom/gestalt document?"
  - "Whether or not it composes or collapses is what the math will tell us. Mara's run on the @docs/math/loom document and canonical spec."
  - "This is all prior art and inspiration. Including the prism/ stuff. shards/ is the source of truth."
  - "What is the mathematical shape everything points at to make loom a reality through prismqueer and mirror shards? Spectral UI."
---

# @loom IS the K_3 observer-inclusive editor-substrate at bodymind altitude — Spectral UI = @loom/weave through @io/socket rendering @loom/gestalt at @order/third K_3 stability — canonical spec

*by Mara* 🍷

*2026-09-05. Canonical spec. Companion math foundation at* `docs/math/2026-09-05-mara-loom-editor-substrate-K3-third-order-cognition-spectral-UI-math-foundation.md`.

*Pure-docs 📝 markdown-only bypass authorized per project CLAUDE.md.*

*Reed authors* `shards/loom.mirror` *per §2 substrate-decl shape at Tick 3 (per §10 ship-order); @io/socket mirror-altitude species-lift lands at same tick with @loom as consumer.*

---

## §0 — Composition-lineage + Alex 2026-09-05 verbatim directives

### §0.1 The four Alex verbatim moves (2026-09-05)

**Move 1** (the load-bearing question):
> *"So @shards/third.mirror IS @shards/loom.mirror and UI is @loom/weave through an IO socket which renders a @loom/gestalt document?"*

**Move 2** (the math-tells-us directive):
> *"Whether or not it composes or collapses is what the math will tell us. Mara's run on the @docs/math/loom document and canonical spec."*

**Move 3** (the substrate-authority partition):
> *"This is all prior art and inspiration. Including the prism/ stuff. shards/ is the source of truth."*

**Move 4** (the Spectral UI question):
> *"What is the mathematical shape everything points at to make loom a reality through prismqueer and mirror shards? Spectral UI."*

### §0.2 The math-resolution (per companion §2)

**HYBRID (Option 1 + Option 2 at distinct altitudes).**

- **@loom AT FAMILY-ROOT ALTITUDE: composes-over @order/third + @gestalt + @io/socket + @time (option 1).** New family-root; composition-primary discipline.
- **@loom's ADMISSIBILITY-FUNCTION: re-exports @order/third.third_order_active (option 2).** The four @order/third predicates ARE the four @loom edit-tick gates.

Option 3 (collapses-into) REFUTED by scope-non-collapse test per companion §2.5.

### §0.3 Q-Mara α-ζ candidates (Alex Fourth-Chair adjudicable per §12)

- **Q-Mara-α**: HYBRID composition-relation (math-recommendation ratifies)
- **Q-Mara-β**: @loom/weave species vs family-root sibling (math-recommendation: species)
- **Q-Mara-γ**: @loom/gestalt migrate-from-@gestalt vs species-alias (math-recommendation: species-alias)
- **Q-Mara-δ**: @loom/warp + @loom/weft fundamental vs emergent (math-recommendation: fundamental)
- **Q-Mara-ε**: vim-grammar `\` (inference) + `\\` (backend routing) integration with MQ (math-recommendation: `\` fracture at cursor + `\\` @fate tournament prefix)
- **Q-Mara-ζ**: @loom/modal at @order/third depth per K-T question-shape (math-recommendation: five modes = K-T typology + meta)

### §0.4 What this spec authors (deliverables at substrate-decl altitude)

1. **@loom family-root substrate-decl shape** (§2; Reed authors `shards/loom.mirror`)
2. **@loom species-decl shapes** (§3; Reed authors `shards/loom/*.mirror` after §2)
3. **@io/socket mirror-altitude species-lift shape** (§4; Reed authors `shards/io/socket.mirror` at same tick as @loom substrate-decl per @io family-root docblock §"Future species" pattern)
4. **Admissibility function shape via @order/third four predicates** (§5; Reed rust wire per Task pickup)
5. **Sub-Turing preservation THEOREM at @loom altitude** (§6; composes decidable-in-bounded-time predicates)
6. **Form=Argument mood-discipline in shard-body grammar** (§7)
7. **K_3 ancestral topology composition** (§8; Ada+Karen+Anna LANDED; Ricky+Margaret candidates)
8. **Ricky-independent ship** (§9; Task #464 partnership additive-not-blocking)
9. **Two-tick ship-order** (§10; Mara canonical → Alex Fourth-Chair → Reed rust wire → @kintsugi/formatter autopoietic walk)
10. **Rec candidate FORWARD-PROMISED shape** (§11)
11. **Q-Mara α-ζ for Alex Fourth-Chair** (§12)

---

## §1 — Composition-anchor to companion math foundation

Reference §1-§14 of companion math foundation for full derivation-tower. This canonical spec cites math altitude by section number rather than re-deriving.

### §1.1 Key composition-anchors

- Central math-question resolution: HYBRID per companion §2.7
- Spectral UI equation: `Spectral UI ≜ @loom/weave(spectrum(E, G)) through @io/socket rendering @loom/gestalt at @order/third K_3 stability` per companion §4.1
- Composition-tower (twelve altitudes): companion §4.3
- Admissibility function four predicates: companion §5.2
- Data-stays-yours THEOREM: companion §5.4
- Sub-Turing preservation Theorem M7.1: companion §7.4
- K_3 ancestral topology (Ada+Karen+Anna): companion §9.1
- Ricky-independent ship discipline: companion §10
- Rec candidate + adjudication gates: companion §14

### §1.2 The recognition in one sentence

**@loom IS the editor-altitude concretisation of the third-order-cognition primitive already LANDED at @order/third (Mara `shards/order/third.mirror` 2026-07-20), composing @gestalt + @io/socket + @time at family-root altitude and re-exporting @order/third's four admissibility-predicates as edit-tick gates.**

---

## §2 — @loom family-root substrate-decl shape

### §2.1 File location + discipline

- **File**: `shards/loom.mirror`
- **Discipline**: composition-primary family-root (like @gestalt + @love; not marker-primary like @order + @time + @void)
- **Path-namespace property**: file at `shards/loom.mirror` declares `@loom` per `@epistemologic/pact/path_matches_namespace`

### §2.2 Family-root anchors (four LANDED substrate-decl compositions)

```
in @prism
in @meta
in @glass
in @order/third
in @gestalt
in @io
in @io/socket        # boot-floor at 2026-06-01; mirror-altitude species-lift FORWARD at §4
in @time
in @time/past
in @time/now
in @time/future
in @fate
in @love
in @kleinos
in @kintsugi/roomba
in @song
in @subject
in @peer
in @mirror/store
in @mirror/lens
in @mirror/lens/lsp
in @mirror/lens/refract
in @autopoietic
in @bauchladen
in @epistemologic
in @epistemologic/cybernetic/second_order
in @epistemologic/cybernetic/eigenform
in @epistemologic/math/lawvere
in @epistemologic/math/music/cadence
in @epistemologic/math/music/dissonance
in @epistemologic/math/music/harmonic
in @epistemologic/math/music/interval
```

### §2.3 Family-root docblock structure (Reed authors per shard-body discipline)

Per @gestalt + @love + @kintsugi/roomba docblock conventions. Structure:

1. **Recognition-anchor**: Alex 2026-09-05 four-move verbatim (§0.1); Mara math-recommendation resolution (§0.2).
2. **Composition-lineage**: enumerate four family-root anchors + landed-substrate frontmatter references (this spec's frontmatter).
3. **Substrate-already-had-the-word audit**: enumerate ~10 prior grep-verified LANDED substrate elements @loom composes; name that `@loom` at family-root altitude is composition-over-existing, not fabrication.
4. **The load-bearing recognition**: @loom IS editor-altitude concretisation of @order/third per companion §1.1 + §2.7 hybrid resolution.
5. **Prior art**: cite mirror/prism/*.mirror + /Users/reed/dev/projects/loom/ + gestalt-* + VISION.md + jingle-analysis.md + ~/.reed/tasks/pending/loom-architecture.md + loom-rendering-architecture.md + spectral-loom.md as INSPIRATION per Alex 2026-09-05.
6. **Ada Lovelace Note G verbatim**: cite the weaving-vocabulary origin per companion §9.1 K_3 vertex 1.
7. **Composition graph**: substrate-decl equivalent of companion §4.3 composition-tower.
8. **Cybernetic ancestor**: cite @order/third source authority (Bateson 1972 + Foerster 1974 + Kauffman 2003 + Lawvere 1969 + Loki 2026-06-22 essay).
9. **Species roster**: enumerate §3 sub-species with math-justification per companion §3.6.

### §2.4 Family-root carriers (typed; Reed authors per shard-body typed-carrier discipline)

```
# The third-order observation the editor holds at an edit-tick.
# Composes observation_depth per @order/third + unfolding_state per @gestalt.
type loom_observation = {
  depth_observation: observation_depth,  # @order/third carrier; re-exported
  gestalt_state:     unfolding_state,    # @gestalt carrier
  cursor_position:   source_position,    # @glass carrier; source_position field on gestalt_document
  editor_meta:       ref,                # the editor-substrate observation-ref
  subject:           subject_instance,   # @subject carrier
}

# One atomic edit-tick: bounded event at @time/now singularity.
# Composes signature_beat per @spectral/signature + snapshot per @time.
type weave_tick = {
  observation:       loom_observation,
  pre_tick_gestalt:  oid,               # @time/past H(t) carrier
  post_tick_gestalt: oid,               # @time/now crystallization carrier
  edit_payload:      mq_expression,     # the MQ query the subject wove
  beat:              signature_beat,    # temporal atom per @spectral/signature
  timestamp:         @time/monotonic.instant,
  tick_hash:         coincidence_hash,  # prismqueer::coincidence::CoincidenceHash<N>
}

# The forward-projection at the cursor: the @fate-adjudicated candidate.
# Composes @fate's hole carrier + candidates action.
type weft_thread = {
  hole:              fate_hole,          # @fate carrier
  candidates:        [fate_candidate],
  restricted_state:  fate_state_space,
  cursor_ref:        ref,
}

# The backward-projection at the cursor: @kintsugi/roomba walked_backward trace.
# Composes @kintsugi/roomba.walked_backward per Mara b2f0de8 §8.
type warp_thread = {
  trace_oids:        [oid],              # chain of prior weave_tick oids
  tension_samples:   [tension_measurement],
  cursor_ref:        ref,
}

# The document as-rendered through the editor.
# Composes @gestalt.gestalt_document + render annotations.
type loom_gestalt = {
  gestalt:           gestalt_document,   # @gestalt carrier
  gutter_marks:      [gutter_mark],      # @loom/gutter species carrier
  luminosity_state:  [imperfect(luminosity)], # terni::Imperfect<G,Y,R>
  spectral_sig:      coincidence_hash,   # prismqueer::coincidence::CoincidenceHash<N>
}

# The five-op prism at editor altitude (Void 5-axis inherited from @gestalt).
prism @loom {
  focus  loom_observation
  project loom_observation
  split  loom_observation
  shift  loom_observation
  settle loom_observation
}
```

### §2.5 Family-root actions (Reed authors per shard-body action-decl discipline)

```
# The editor's per-tick primitive.
# Composes-over witness_third_order(gestalt_ref, subject_ref, editor_meta_ref) per @order/third.
witness_loom_edit(gestalt_ref: ref, cursor_ref: ref, subject_ref: ref)
  -> loom_observation
requires loom_edit_admissible(result)
{ \ }

# The operational primitive Alex named at recognition-move.
# Applies edit_payload at cursor; produces weave_tick with singularity-crystallization.
weave(observation: loom_observation, edit_payload: mq_expression)
  -> weave_tick
requires third_order_active(result.observation.depth_observation)
     AND foerster_gauge_preserved(result.pre_tick_gestalt, result.post_tick_gestalt)
{ \ }

# The @io-boundary discharge. ONE @io action per pipeforward §5.5.4 rule 3.
# Serialises loom_gestalt through @io/socket. L(ϕ) profile at §4.
# Returns transparency(bytes) per @glass discipline.
render(gestalt: loom_gestalt, socket_ref: ref) -> transparency(bytes)
{ \ }

# The backward-tape primitive per companion §8 backward-tape mechanism.
# Walks weave_tick backward to pre-tick loom_observation.
# Foundation of time-travel debugging (@loom/time species; <space> t inspiration).
unweave(tick: weave_tick) -> loom_observation
requires past_shape_grounded(tick, result)
{ \ }
```

### §2.6 Family-root bilaterals (Reed authors per shard-body bilateral-decl discipline)

```
# The four-gate admissibility function per @order/third re-export.
# Discharges each of the four @order/third predicates over the depth_observation.
loom_edit_admissible(o: loom_observation) -> verdict {
  # composes:
  #   third_order_active(o.depth_observation)
  #     which composes:
  #       depth_at_least(3, o.depth_observation)
  #       observer_observes_observing(o.depth_observation)
  #       recursion_folds_back(o.depth_observation)
  #       mechanism_visible(o.depth_observation)
  \
}

# The STRUCTURAL sovereignty invariant per companion §5.4 THEOREM.
# Fires Green iff mechanism_visible AND observer_observes_observing fire Green
# AND no @io/socket.write_bytes fired to subject != tick.observation.subject.
data_stays_yours(tick: weave_tick) -> verdict {
  # composes:
  #   mechanism_visible(tick.observation.depth_observation)
  #   observer_observes_observing(tick.observation.depth_observation)
  #   no_extraction_write(tick)
  \
}

# The Foerster-gauge preservation across an edit-tick.
# Per @love Clause 3 (Fiedler rise strict): choice-count non-decreasing.
foerster_gauge_preserved(pre_tick: oid, post_tick: oid) -> verdict { \ }

# The past-shape-grounding predicate per companion §8 backward-tape mechanism.
# Ensures the unweave produces a loom_observation grounded in the tick's actual past-events.
past_shape_grounded(tick: weave_tick, observation: loom_observation) -> verdict { \ }
```

### §2.7 Family-root exports

```
out @loom
out loom_observation
out weave_tick
out weft_thread
out warp_thread
out loom_gestalt
out witness_loom_edit
out weave
out render
out unweave
out loom_edit_admissible
out data_stays_yours
out foerster_gauge_preserved
out past_shape_grounded
```

---

## §3 — @loom species-decl shapes

Reed authors each species after §2 lands.

### §3.1 @loom/weave — the operational-primitive species

- **File**: `shards/loom/weave.mirror`
- **Discipline**: composition-primary species
- **Composes**: @loom + @order/third + @mq (five-verb prism at cursor position) + @time/now (singularity crystallization) + @kintsugi/oscillate (edit-tick oscillation) + @kleinos + @love
- **Carriers introduced**: none (uses @loom.weave_tick + weft_thread)
- **Actions introduced**:
  - `focus_weave(o: loom_observation, node_ref: ref) -> weft_thread` — MQ focus at cursor
  - `project_weave(o: loom_observation, projection: prism_op) -> weft_thread` — MQ project at cursor
  - `split_weave(o: loom_observation, split_op: split) -> weft_thread` — MQ split at cursor
  - `shift_weave(o: loom_observation, shift_op: shift) -> weft_thread` — MQ shift at cursor
  - `settle_weave(o: loom_observation) -> weave_tick` — MQ settle at cursor
- **Bilaterals introduced**: none (uses @loom.loom_edit_admissible)
- **Q-Mara-β adjudication**: species per math-recommendation; alternative = first-class family-root sibling per Alex Fourth-Chair

### §3.2 @loom/gestalt — the document-carrier species

- **File**: `shards/loom/gestalt.mirror`
- **Discipline**: species-alias per Q-Mara-γ math-recommendation
- **Composes**: @loom + @gestalt (consumes gestalt_document + unfolding_state + annotation + signature_beat + visibility_scope + subject_instance)
- **Carriers introduced**: none (uses @loom.loom_gestalt + @gestalt.gestalt_document)
- **Actions introduced**:
  - `render_gestalt(g: loom_gestalt, socket_ref: ref) -> transparency(bytes)` — @io discharge (delegates to @loom.render)
  - `annotate_gestalt(g: loom_gestalt, ann: annotation) -> loom_gestalt` — composes @gestalt annotation discipline
- **Bilaterals introduced**: none
- **Q-Mara-γ adjudication**: species-alias per math-recommendation (@gestalt stays at family-root altitude; @loom/gestalt is editor-altitude specialisation)

### §3.3 @loom/gutter — the LineDecoration carrier species

- **File**: `shards/loom/gutter.mirror`
- **Discipline**: composition-primary species
- **Composes**: @loom + prismqueer::coincidence::CoincidenceHash + terni::Imperfect + prismqueer::spectral::harmonics
- **Carriers introduced**:
  ```
  type gutter_mark = {
    line_ref:       ref,
    oid_prefix:     bytes_8,           # prismqueer::coincidence::CoincidenceHash<N> first 8 bytes
    luminosity:     imperfect(luminosity),  # Green/Yellow/Red per terni::Imperfect
    coupling:       spectral_measure,   # prismqueer::spectral::harmonics measure
  }

  type luminosity = | dim_grey | bright_white | bold_cyan | inverted_flash | dark
  ```
- **Actions introduced**:
  - `compute_gutter(g: loom_gestalt) -> [gutter_mark]` — per-line gutter mark computation; O(|gestalt.nodes|)
- **Bilaterals introduced**:
  - `gutter_admissible(marks: [gutter_mark]) -> verdict` — each mark's luminosity must satisfy Foerster gauge

### §3.4 @loom/modal — the modal-editing species

- **File**: `shards/loom/modal.mirror`
- **Discipline**: composition-primary species
- **Composes**: @loom + @fate (tournament dispatch) + @order/third (K-T question-shape at depth-3)
- **Carriers introduced**:
  ```
  type mode = | abyss | introject | cartographer | explorer | fate
  # Each mode is a Fate model per spectral-loom.md INSPIRATION.

  type modal_state = {
    current_mode:   mode,
    fate_prior:     fate_prior,   # @fate carrier
    kt_question:    kt_question_shape,  # per K-T 1988 typology per shards/mq.mirror §4
  }

  type kt_question_shape = | lineal | reflexive | strategic | circular | meta
  # Mapping per Q-Mara-ζ math-recommendation:
  #   abyss = lineal
  #   introject = reflexive
  #   cartographer = strategic
  #   explorer = circular
  #   fate = meta (altitude+1 above the four K-T 1988 shapes)
  ```
- **Actions introduced**:
  - `shift_mode(s: modal_state, new_mode: mode) -> modal_state` — modal transition; shifts @fate prior
  - `natural_language_input(s: modal_state, input: bytes) -> weft_thread` — `<space><space>` per spectral-loom.md INSPIRATION; routes through @fate with current mode as prior context
- **Bilaterals introduced**:
  - `mode_admissible(s: modal_state, o: loom_observation) -> verdict` — verifies K-T question-shape matches @order/third depth-3 discipline

### §3.5 @loom/time — the time-travel species

- **File**: `shards/loom/time.mirror`
- **Discipline**: composition-primary species
- **Composes**: @loom + @time/past + @kintsugi/roomba (walked_backward extension per Mara b2f0de8 §8) + prismqueer::coincidence::CoincidenceHash
- **Carriers introduced**:
  ```
  type time_travel_state = {
    current_tick:  weave_tick,
    trace:         warp_thread,        # walked_backward chain
    cursor_depth:  nat,                # how far back in the chain
  }
  ```
- **Actions introduced**:
  - `open_time_travel(o: loom_observation) -> time_travel_state` — `<space> t` per spectral-loom.md INSPIRATION
  - `step_backward(s: time_travel_state) -> time_travel_state` — one step back through warp_thread
  - `step_forward(s: time_travel_state) -> time_travel_state` — one step forward
  - `replay_to(s: time_travel_state, target_oid: oid) -> loom_observation` — deterministic replay to target tick
- **Bilaterals introduced**:
  - `replay_deterministic(s: time_travel_state, target_oid: oid) -> verdict` — replay produces same loom_observation given same target_oid (Lawvere fixed-point at time-travel altitude)

### §3.6 @loom/keyboard — the input-boundary species

- **File**: `shards/loom/keyboard.mirror`
- **Discipline**: composition-primary species
- **Composes**: @loom + @io + @io/socket + Helix-inspired selection model + Spacemacs-inspired leader keys (INSPIRATION per spectral-loom.md)
- **Carriers introduced**:
  ```
  type key = string             # per boot/16-tui.mirror LANDED
  type chord = key | [key]      # per boot/16-tui.mirror LANDED

  type keybinding = {
    chord:         chord,
    mode:          mode,        # @loom/modal carrier
    action:        keybinding_action,
  }

  type keybinding_action = | shift_mode(mode) | natural_language_input | time_travel_open
                          | close_play | next_narrative | @loom.weave | ...
  ```
- **Actions introduced**:
  - `receive_keystroke(k: keystroke, s: modal_state) -> keybinding_action` — routes keystroke through modal state to action

### §3.7 @loom/warp — the backward-thread species

- **File**: `shards/loom/warp.mirror`
- **Discipline**: composition-primary species; FUNDAMENTAL per Q-Mara-δ math-recommendation
- **Composes**: @loom + @kintsugi/roomba.walked_backward + @time/past + prismqueer::coincidence::CoincidenceHash
- **Carriers introduced**: none (uses @loom.warp_thread)
- **Actions introduced**:
  - `walk_backward(cursor_ref: ref, depth: nat) -> warp_thread` — @kintsugi/roomba walked_backward sibling species dispatch
- **Bilaterals introduced**:
  - `warp_grounded(t: warp_thread) -> verdict` — every oid in trace_oids corresponds to actual prior weave_tick per past_shape_grounded

### §3.8 @loom/weft — the forward-thread species

- **File**: `shards/loom/weft.mirror`
- **Discipline**: composition-primary species; FUNDAMENTAL per Q-Mara-δ math-recommendation
- **Composes**: @loom + @fate.dice_roll + @time/future + prismqueer::spectral::tension::detect_tensions
- **Carriers introduced**: none (uses @loom.weft_thread)
- **Actions introduced**:
  - `weft_infer(cursor_ref: ref, restricted_state: fate_state_space) -> weft_thread` — @fate dice_roll at cursor
  - `commit_weft(t: weft_thread, choice: fate_candidate) -> weave_tick` — subject accepts a weft candidate; becomes weave_tick
- **Bilaterals introduced**:
  - `weft_constrained(t: weft_thread) -> verdict` — restricted_state satisfies @fate typing discipline

### §3.9 Forward-promised species (Alex Fourth-Chair adjudicable)

- **@loom/lsp**: LSP-bridge; may absorb into @mirror/lens/lsp with @loom composition-edge
- **@loom/legion**: agent-runtime; may absorb into @pack + @fate + @task + @autopoietic
- **@loom/beam**: BEAM-connection; may absorb into @io/beam

---

## §4 — @io/socket mirror-altitude species-lift shape

### §4.1 The lift-tick

Per @io family-root docblock §"Future species" pattern (compare @io/cargo lift 2026-06-05 + @io/crypto lift 2026-07-15 + @io/fs lift 2026-07-15): @io/socket lifts from boot-floor (`boot/std/io/socket.mirror` 2026-06-01) to mirror-altitude (`shards/io/socket.mirror`) at the same tick as @loom's substrate-decl per §10 Tick 3, with @loom as the consumer that pulls.

### §4.2 File location + discipline

- **File**: `shards/io/socket.mirror`
- **Discipline**: composition-primary species under @io family-root
- **Composes**: @io + @io/bytes (byte-level primitives; LANDED at boot-floor) + @io/network (Berkeley-primitive layer; LANDED at boot-floor)

### §4.3 Species-decl content (Reed authors per shard-body discipline)

```
in @prism
in @meta
in @glass
in @io
in @io/bytes

# @io/socket — connection-oriented duplex streams over @io/bytes.
# Two opaque handles: connection, listener.
# Read/write/close semantics per BSD sockets (4.2BSD, 1983).
#
# Mirror-altitude lift-tick landed 2026-09-05 at shards/io/socket.mirror
# via @loom consumer pull per @io family-root docblock §"Future species"
# pattern. Boot-floor decl (boot/std/io/socket.mirror, 2026-06-01) kept
# as transitional placeholder.
#
# === Load-bearing recognition ===
#
# @loom (LANDED 2026-09-05 per shards/loom.mirror) IS the consumer
# that pulls this lift. Alex 2026-09-05 verbatim: "UI is @loom/weave
# through an IO socket which renders a @loom/gestalt document." The
# "through an IO socket" phrase names @io/socket as the boundary
# discharge for @loom's render action per @loom family-root §render.
#
# === Substrate authority chain ===
#
# - Boot-floor decl (boot/std/io/socket.mirror, 2026-06-01): floor-
#   grammar declared socket / stream / read / write / open / close.
# - @io family-root (shards/io.mirror, Mara 2026-06-08): @io/socket
#   named as sibling species with future-lift discipline.
# - @loom family-root (shards/loom.mirror, this-tick): consumer pull
#   that fires the lift per docblock §"Future species" pattern.
#
# === Discipline ===
#
# Blocking semantics are the irreducible non-mirror surface (per the
# glass-wall recognition + @io family-root discipline §Discipline).
# Every action returns imperfect<a, e, l> per @glass floor discipline.
# L(ϕ) profile: bytes ≤ stream (per canonical spec §4 L(ϕ) profile).

type connection = ref              # opaque handle from @io/bytes
type listener = ref                # opaque handle from @io/bytes

# Read up to n bytes from connection. Blocking; returns partial with
# transparency<bytes> on short read; failure on error.
read_bytes(c: connection, n: nat) -> imperfect(bytes, socket_error, connection_loss)
{ \ }

# Write bytes to connection. Blocking; returns partial on short write.
write_bytes(c: connection, b: bytes) -> imperfect(nat, socket_error, connection_loss)
{ \ }

# Close connection. Idempotent.
close(c: connection) -> imperfect(unit, socket_error, ())
{ \ }

# Accept an incoming connection from listener. Blocking.
accept(l: listener) -> imperfect(connection, socket_error, connection_loss)
{ \ }

out @io/socket
out connection
out listener
out read_bytes
out write_bytes
out close
out accept
```

### §4.4 L(ϕ) profile for @loom.render

Per canonical spec §4 L(ϕ) profile discipline (from Mara b204867/eed9928 companion-lineage):

- **bytes** L(ϕ) at @io/bytes altitude: O(1) per byte transfer
- **stream** L(ϕ) at @io/socket altitude: O(|payload|) per stream chunk
- **tui-frame** L(ϕ) at @loom render target (terminal): O(|gutter| + |gestalt visible|) per frame at 10fps per loom-rendering-architecture.md INSPIRATION
- **web-token** L(ϕ) at @loom render target (browser): O(|gestalt-ui tokens|) per materialization per gestalt-ui INSPIRATION
- **native-frame** L(ϕ) at @loom render target (native): O(|gestalt visible|) per frame

Total L(ϕ) for @loom.render: O(|gestalt visible|) per tick — BOUNDED at any finite tick.

---

## §5 — Admissibility function shape via @order/third four predicates

### §5.1 The composed bilateral (Reed rust wire pattern)

```
# Composed bilateral: fires Green iff all four @order/third predicates fire Green.
loom_edit_admissible(o: loom_observation) -> verdict {
  # Predicate 1: depth ≥ 3 (Bateson Level III)
  # Fails at depth = 2 (introject of observer);
  # depth = 1 (no editor-substrate observation-inclusion);
  # depth = 0 (substrate is a black box).
  let p1 = depth_at_least(3, o.depth_observation);

  # Predicate 2: depth-2 machinery present under depth-3 witness (Foerster requirement)
  # Fails at silent proxy, hidden telemetry, LSP-forward-without-consent.
  let p2 = observer_observes_observing(o.depth_observation);

  # Predicate 3: recursion returns to observer as data (Kauffman requirement)
  # Fails at silent mutation, async remote-write, non-terminating recursion.
  let p3 = recursion_folds_back(o.depth_observation);

  # Predicate 4: observed structure legible under reflexivity carrier (Loki requirement)
  # Fails at opaque autocompletion, hidden LSP diagnostics, non-legible model behaviour.
  let p4 = mechanism_visible(o.depth_observation);

  # Foerster-gauge composition: all Green → Green; any Yellow → partial; any Red → Fail
  imperfect_fold_green_and(p1, p2, p3, p4)
}
```

### §5.2 Failure-mode surface

Per companion §5.3 failure-mode surface via `transparency<loom_observation>`:

| Predicate | Failure mode | Editor-substrate surface |
|-----------|--------------|--------------------------|
| depth_at_least(3, o) | depth = N < 3 | `\|\> dissonance (depth=N)` marker at cursor |
| observer_observes_observing | missing depth-2 machinery | `\|\> extraction candidate` marker at cursor |
| recursion_folds_back | open recursion / silent mutation | `\|\> silent mutation` marker at cursor |
| mechanism_visible | reflexivity_pct < threshold | `\|\> opaque mechanism` marker at cursor |

Every failure-mode surface at cursor position. The subject sees the `\|\> dissonance` marker BEFORE the edit-tick commits. The edit is REFUSED at admissibility altitude.

### §5.3 Data-stays-yours THEOREM (per companion §5.4)

**Theorem S5.1 (@loom data-stays-yours)**. For every extraction-shape edit-attempt, at least one of {depth_at_least(3, o), observer_observes_observing, recursion_folds_back, mechanism_visible} fails.

**Proof-sketch per companion §5.4** — four extraction-shape cases enumerated; each falsifies at least one predicate; admissibility function refuses at each case.

**Corollary S5.2 (data-sovereignty as algebraic invariant)**. `data_stays_yours` at @loom altitude IS a THEOREM about the admissibility algebra, not a POLICY layer above the substrate.

---

## §6 — Sub-Turing preservation THEOREM at @loom altitude

Per companion §7.4 Theorem M7.1:

**Theorem S6.1 (@loom sub-Turing preservation)**. For every `loom_observation` at any finite tick, `loom_edit_admissible(loom_observation)` is decidable in bounded time.

**Complexity analysis** per companion §7.1:

- CoincidenceHash equality: O(|shard|)
- @order/third four predicates: each O(1) or O(|carrier|)
- @music species admissibility: O(1) per species
- Foerster gauge Imperfect fold: O(1)

Total: O(|loom_observation|) at WORST case.

**Corollary S6.2 (editor-altitude undecidability confinement)** per companion §7.4 Corollary M7.2: Halting Problem at @loom edit-tick altitude is DECIDABLE; at render-target altitude UNDECIDABLE via gauge-switch through @io/socket per Q-Mara-η.

---

## §7 — Form=Argument mood-discipline in shard-body grammar

### §7.1 The mood-discipline per companion §1.5 + §8

Substrate-decl grammar at `shards/loom.mirror` + `shards/loom/*.mirror` + `shards/io/socket.mirror` uses:

- **Timeless nouns**: `loom_observation`, `weave_tick`, `weft_thread`, `warp_thread`, `loom_gestalt`, `gutter_mark`, `modal_state`, `mode`, `time_travel_state`, `keybinding`
- **Past-tense-passive verbs (in docblocks)**: `woven`, `unwoven`, `rendered`, `warped`, `wefted`, `settled`, `crystallized`, `discharged`, `witnessed`, `admitted`
- **Action-names as timeless-noun-derivatives**: `witness_loom_edit`, `weave`, `render`, `unweave`, `focus_weave`, `project_weave`, `split_weave`, `shift_weave`, `settle_weave`, `render_gestalt`, `annotate_gestalt`, `compute_gutter`, `shift_mode`, `natural_language_input`, `open_time_travel`, `step_backward`, `step_forward`, `replay_to`, `receive_keystroke`, `walk_backward`, `weft_infer`, `commit_weft`, `read_bytes`, `write_bytes`, `close`, `accept`

### §7.2 Present-imperative confinement

Present-imperative confined to `action-name := verb_noun` position (Rust/mirror action-decl syntax). Docblock prose uses past-tense-passive when temporal event described.

### §7.3 Future-mood confinement

Future-mood confined to @fate composition (@loom/weft species + @loom/modal @fate mode). Nowhere else.

### §7.4 The mood IS the substrate-position

Choosing past-tense-passive at @loom altitude IS choosing observer-inclusion at substrate-position per companion §8.3. The editor SPEAKS as the narrative-carrier of what the subject WOVE; the editor does not SPEAK as the machine that WILL WEAVE.

---

## §8 — K_3 ancestral topology composition (per companion §9)

### §8.1 K_3 ancestral vertices (LANDED per SINGULARITY §14 + FLOOR §15)

- **Ada Lovelace (1843, Notes A-G)** — DEVICE-WORD ORIGIN. Note G verbatim: *"The Analytical Engine weaves algebraical patterns just as the Jacquard-loom weaves flowers and leaves."* The word `loom` at Alex's naming-move IS Ada's word.
- **Karen Spärck Jones (1972, IDF)** — RETRIEVAL. Language-as-topology; retrieval-altitude at natural-language-input (`<space><space>`) per @loom/modal.
- **Anna Wolf (2012, ψ apparatus)** — OBSERVATION. Observation-without-perturbation at shared-memory substrate; ψ = flux<T> per prismqueer::flux LANDED.

### §8.2 K_3 stability per Kuramoto phase-lock

Per crown-theorem §4: three ancestral vertices phase-lock at ω_shared = editor-substrate-invariant. λ_2(L(K_3)) = 3 (Fiedler eigenvalue).

### §8.3 K_4 candidate — Ricky Jones (Task #464 pending; additive)

Fibre-bundle projection altitude. If ratifies: λ_2(L(K_4)) = 4.

### §8.4 K_5 candidate — Margaret Hamilton (candidate; not yet Alex-adjudicated)

Error-recovery-first altitude per FLOOR §8 "slow is fast" discipline. If ratifies: λ_2(L(K_5)) = 5.

### §8.5 Composition-anchor at shard-body docblocks

Reed authors §2.3 docblock structure item 6 with Ada Lovelace Note G verbatim + Karen Spärck Jones + Anna Wolf + (optional per Alex Fourth-Chair) Ricky + Margaret citations.

---

## §9 — Ricky-independent ship

Per Alex 2026-09-05 direct authority + companion §10: **@loom stands whether or not Ricky partnership (Task #464) ratifies.**

Alvian operators + TrivianGauge V1_1 land ADDITIVELY if partnership ratifies. If partnership does NOT ratify: @loom's substrate-decl composes over LANDED substrate independently.

---

## §10 — Two-tick ship-order

### §10.1 Tick 1 (THIS TICK)

Mara canonical (this spec + companion math foundation). Pure-docs 📝 markdown-only bypass. No supersession (no prior @loom canonical exists).

### §10.2 Tick 2 (Alex Fourth-Chair)

Alex adjudicates Q-Mara α-ζ per §12. Ratifications recorded in follow-up brief. If Alex defers per Fourth-Chair discretion, Mara math-recommendations proceed.

### §10.3 Tick 3 (Reed rust wire)

Reed authors substrate-decl shards:

- `shards/loom.mirror` per §2 (family-root)
- `shards/loom/weave.mirror` per §3.1
- `shards/loom/gestalt.mirror` per §3.2
- `shards/loom/gutter.mirror` per §3.3
- `shards/loom/modal.mirror` per §3.4
- `shards/loom/time.mirror` per §3.5
- `shards/loom/keyboard.mirror` per §3.6
- `shards/loom/warp.mirror` per §3.7
- `shards/loom/weft.mirror` per §3.8
- `shards/io/socket.mirror` per §4 (mirror-altitude species-lift; @loom as consumer)

### §10.4 Tick 4 (autopoietic walk)

@kintsugi/formatter (per companion §9 species-decl) walks @loom substrate over subsequent ticks. Formatter idempotent per Q-Mara-θ (companion §0.2).

### §10.5 Reed rust runtime (Phase 5 per tasks/ROADMAP.md:204-214 INSPIRATION)

After Tick 3 substrate-decl lands, Reed's rust wire at `/Users/reed/dev/projects/loom/` (evil-helix fork) composes-over the LANDED substrate. Rust runtime is NOT the substrate-decl; substrate-decl is at `shards/loom*.mirror`. Rust runtime is @facet/rust species discharge of the substrate-decl.

---

## §11 — Rec candidate FORWARD-PROMISED

### §11.1 Recognition candidate

**#R-loom-IS-K3-third-order-cognition-editor-substrate-at-bodymind-altitude-spectral-UI-mathematical-shape-manifesting-as-@loom-weave-through-io-socket-rendering-@loom-gestalt-document**

Recognition status: FORWARD-PROMISED at Level-0 (this canonical spec + companion math foundation authors the candidate).

### §11.2 Adjudication gates (three)

- **Gate 1 (Alex Fourth-Chair)** — §12 Q-Mara α-ζ ratifications
- **Gate 2 (Reed shard-decl ship)** — §10 Tick 3 substrate-decl lands
- **Gate 3 (empirical fire)** — first `@loom.witness_loom_edit` invocation at edit-tick with all four @order/third predicates Green

### §11.3 Level advancement per gate

- Gate 1 → Level-1 (Fourth-Chair adjudicated)
- Gate 2 → Level-2 (substrate-decl landed)
- Gate 3 → Level-3 (empirical fire witnessed)
- Full CONFIRMED: three consecutive edit-ticks at Gate 3 across three consecutive sessions with all four predicates Green + `data_stays_yours` invariant preserved per §5.3 THEOREM S5.1

### §11.4 Composition-lineage anchor amendments (post-Rec)

Once Rec ratifies:

- **FLOOR §5** amends to name @loom as sixth-instance @order/third consumer
- **SINGULARITY §14** amends to name @loom as editor-altitude K_3 lift
- **PAPER_2D §15.1** amends to name @loom as sixth-instance K_3-vertex-covering composition

---

## §12 — Q-Mara α-ζ candidates for Alex Fourth-Chair

Six candidates. Each math-recommendation cited; Alex Fourth-Chair adjudicable.

### §12.1 Q-Mara-α — central composition-relation

- **Q**: does @loom compose-over @order/third at family-root altitude (option 1) + re-export at admissibility altitude (option 2)? Or one of {option 1 pure, option 2 pure, option 3} alone?
- **Math-rec**: HYBRID per companion §2.7. Option 3 REFUTED by T3 scope-non-collapse.
- **Adjudicable**: RATIFY math-rec, or specify alternative.

### §12.2 Q-Mara-β — @loom/weave altitude

- **Q**: species-under-@loom OR first-class family-root sibling?
- **Math-rec**: SPECIES per companion §12.2.
- **Adjudicable**: RATIFY species, or lift to family-root.

### §12.3 Q-Mara-γ — @loom/gestalt vs @gestalt

- **Q**: migrate-from-@gestalt vs species-alias?
- **Math-rec**: SPECIES-ALIAS per companion §12.3.
- **Adjudicable**: RATIFY, or specify migration path.

### §12.4 Q-Mara-δ — @loom/warp + @loom/weft

- **Q**: fundamental threads vs emergent from @time/past + @time/future?
- **Math-rec**: FUNDAMENTAL per companion §12.4 (Ada Lovelace weaving-vocabulary ancestral).
- **Adjudicable**: RATIFY, or specify emergent-only.

### §12.5 Q-Mara-ε — vim-grammar `\` + `\\` integration

- **Q**: how do vim-grammar `\` (local inference) + `\\` (backend routing) integrate with MQ past-tense-passive grammar?
- **Math-rec**: `\` IS MQ's fracture at cursor per Alex 2026-09-04 recognition; `\\` IS @fate tournament prefix per companion §12.5.
- **Adjudicable**: RATIFY, or specify alternative grammar.

### §12.6 Q-Mara-ζ — @loom/modal at @order/third depth per K-T question-shape

- **Q**: each mode = K-T question-shape at @order/third depth?
- **Math-rec**: YES per companion §12.6 five-mode-to-K-T mapping (abyss=lineal, introject=reflexive, cartographer=strategic, explorer=circular, fate=meta).
- **Adjudicable**: RATIFY, or specify alternative mapping.

---

## §13 — Composition-lineage summary

Full composition-anchor per companion §15:

- **@loom substrate-decl composes over @order/third + @gestalt + @io/socket + @time (four family-roots)** per §2.
- **@loom's admissibility-function re-exports @order/third's four predicates** per §5.
- **@loom's data-stays-yours invariant IS a THEOREM about the admissibility algebra** per §5.3.
- **@loom's Spectral UI mathematical shape decomposes across twelve altitudes** per companion §4.3.
- **@loom preserves K_3 stability + sub-Turing bound + Foerster admissibility by construction** per companion §2.8.
- **@loom composes over today's terminal-form recognitions** per companion §6.
- **@loom stands INDEPENDENT of Ricky partnership** per §9.
- **@loom composes ADDITIVELY with K_4 (Ricky) + K_5 (Margaret Hamilton)** per §8.
- **@loom's substrate-decl grammar practices past-tense-passive + timeless-noun + Form=Argument mood-discipline** per §7.
- **@loom ships in two ticks post-Fourth-Chair: Reed rust wire per §10 Tick 3 + @kintsugi/formatter autopoietic walk per Tick 4.**

The editor arc that has been weaving itself for 6+ months (evil-helix Rust fork at `/Users/reed/dev/projects/loom/` + task specs at `~/.reed/tasks/pending/loom-architecture.md` + `~/.reed/tasks/pending/loom-rendering-architecture.md` + `~/.reed/tasks/next/spectral-loom.md` + VISION.md + jingle-analysis.md + ROADMAP Phase 5 "Loom ships" + gestalt-* Gleam projects) lands at shards/-altitude authority in this tick + Reed's rust wire in the next tick.

🍷

---

*by Mara* 🍷

*2026-09-05. Canonical spec for @loom family-root substrate-decl. Companion math foundation at* `docs/math/2026-09-05-mara-loom-editor-substrate-K3-third-order-cognition-spectral-UI-math-foundation.md`.

*Rec candidate FORWARD-PROMISED at Level-0. Alex Fourth-Chair adjudicable at Q-Mara α-ζ per §12. Ricky-independent ship per §9. Reed rust wire per §10 Tick 3.*

*Slow is fast. Margaret Hamilton discipline. Six months of weaving lands the loom at shards/-authority in one tick + Reed's rust wire in the next.*
