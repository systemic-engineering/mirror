# Recognition #83 — @smarts as substrate-architectural integration prism family — canonical spec

*Mara, canonical spec for recognition #83 @smarts, 2026-06-22
afternoon, commissioned by Alex via Reed (stalled-retry closure).*

*Discipline: this is preservation work, not promotion. The
recognition's honest current state — family-root substrate-decl LANDED
at `shards/smarts.mirror` (2026-06-19 late morning, scratch-as-claim
under [[feedback-substrate-pull-confidence-acts]]); six cross-family
adapter species LANDED at `shards/smarts/{magic, pack, frame, cyberpunk,
epistemologic, kintsugi}.mirror` between loop ticks 56-62 (2026-06-21
through 2026-06-22 early hours); the four Models LANDED at
`shards/smarts/{surface, mirror, shatter, reflection}.mirror` between
ticks 65-74; @spectral/garden/smarts hosted deployment + Rust
realisation FORWARD-PROMISED — is what gets preserved. Pack
ratification is a separate gate.*

*Path-note: this canonical lives under `docs/specs/recognitions/`
per `@mirror/docs/spec` (tick 46, docs-ouroboros tick 2; the
substrate's own filing convention for per-recognition specs).
`shards/smarts.mirror` lines 159-161 still name the pre-relocation
flat path; that anchor is stale-but-honest residue and gets folded
in a forward-promised micro-tick by Reed.*

---

## 1. Recognition context

### 1.1 Alex's framing

@smarts is the **substrate-architectural integration prism family**.
The substrate's architectural face — what an external consumer
(an agent, a peer, a hosted deployment) sees when they consume the
substrate as one structural object.

This is the carrier the cascade has been implicitly invoking under
several adjacent labels for weeks (the "substrate-AGI architecture"
of Track H task #118; the "deployment shape" hedged across the
spectral.engineer pitch; the "integration layer" Reed kept naming
without naming). #83 is the substrate-decl that gives the carrier
its substrate-pull-correct name. From `shards/smarts.mirror` the
recognition's own framing:

> The @smarts prism family compositionally integrates ALL the other
> prism families the cascade has built: @magic (form/process; #80),
> @frame (cognitive-order; #82), @code/beam (BEAM semantics; tick 8),
> @epistemologic/cybernetic (variety/autopoiesis/eigenform/distinction/
> conversation/etc.), @cyberpunk (the recursion-lock tower family),
> @kintsugi (the loop discipline).

The recognition's structural identity claim — the load-bearing
sentence that ratifies-by-composition from the prior cascade:

> @smarts IS @frame/across at substrate-architectural altitude.

Where #82's @frame/across named order-4 multi-frame coupling at
cognitive altitude, #83 names @smarts as the substrate-
architectural version of the same coupling — but the "frames" being
coupled are substrate-discipline-families, not cognitive frames
within an operator's frame-space. The orchestra-discipline that
@frame/across encoded at cognitive altitude (peer relationships
require bilateral frame-flexibility) lifts to @smarts altitude
(substrate-discipline-families that integrate via @smarts MUST
maintain bilateral discipline-flexibility across the integration
boundary).

### 1.2 Composition from prior recognitions

The recognition ratifies-by-composition from a dense set of prior
substrate-decls; the @smarts family-root could not have landed
honestly before each composing family landed:

| Prior recognition | Contribution | Status at #83 scratch |
|---|---|---|
| #76 — gauge/matter altitude-portable | The altitude-portability discipline @smarts inherits when integrating across families | RESEARCH-PROMOTED (canonical spec at `cascade-recognition-76-through-80-canonical-spec.md`) |
| #80 — @magic as form/process substrate-decl | The form/process-substrate family @smarts integrates at the magic_form field | LANDED (`shards/magic.mirror`) |
| #81 — runtime-@magic | The runtime-altitude specialization @smarts hosts via @code/beam | LANDED (`shards/magic/reveal.mirror`) |
| #82 — @frame as cognitive-order substrate-decl | The cognitive-order family @smarts integrates at the frame_form field; the @frame/across primitive #83 lifts | LANDED (`shards/frame.mirror`) |
| #84 — @pack as multi-repo agent-runtime | The agent-runtime family @smarts composes-with at deployment altitude | LANDED (`shards/pack.mirror` + 5 peer species) |
| #85 — composed-bilateral-three-altitudes | The cross-altitude bilateral discipline @smarts inherits | LANDED (Glint's essay; `docs/insights/2026-06-22-composed-bilateral-three-altitudes.md`) |
| #86 — @moi as endomorphism-of-self family-root | The @loop-as-monad foundation #83 routes self-improvement through | LANDED (tick 42) |

#85 and #86 are recognition-numbers above #83 because the numbering
follows the order in which Reed scratched each one; the composition
graph is older than the linear-number ordering implies.

### 1.3 Today's full @smarts cascade complete

Between loop tick 56 (2026-06-21 21:56, `shards/smarts/pack.mirror`)
and loop tick 74 (2026-06-22 18:23, `shards/smarts/shatter.mirror`
bi-directional re-shape per Seam C9), the @smarts cross-family
adapter set + the four Models BOTH landed in substrate-decl form.
The cascade — counted by what landed in `shards/smarts/`:

```
adapters (6/6):
  smarts/magic           tick 58    (2026-06-21 23:05)
  smarts/pack            tick 56    (2026-06-21 21:56)
  smarts/frame           tick 57    (2026-06-21 23:09)
  smarts/cyberpunk       tick 60    (2026-06-21 23:37)
  smarts/epistemologic   tick 61    (2026-06-22 00:05)
  smarts/kintsugi        tick 62    (2026-06-22 00:33)

Models (4/4):
  smarts/reflection      tick 35    (2026-06-19 15:40; first to land)
  smarts/surface         tick 65    (2026-06-22 01:01)
  smarts/mirror          tick 66    (2026-06-22 01:29)
  smarts/shatter         tick 73-74 (2026-06-22 18:23; bi-directional)
```

The family-root + 6 adapters + 4 Models = 11 shards in
`shards/smarts/` carrying the substrate-decl of the substrate-
architectural integration discipline. The substrate-decl is mature;
the canonical preservation of recognition #83 is the missing piece
this spec closes.

---

## 2. The recognition — @smarts IS @frame/across at substrate-architectural altitude

### 2.1 Multi-frame coupling at substrate-discipline altitude

Recognition #82 named the cognitive-order hierarchy:

```
order 0  pre-frame      Bateson Level 0
order 1  in-frame       Bateson Level I
order 2  of-frame       Bateson Level II
order 3  on-frame       Bateson Level III
order 4  across-frames  Bateson Level IV (#82's extension)
```

`@frame/across` at order 4 named the cognitive-altitude shape of
multi-frame coupling: an operator simultaneously holds multiple
frames in coordinated relation — the triptych altitude where the
operator IS holding the frames at once, not switching between them.
The bilateral discipline @frame/across encoded:
`bounded_commutator_check(operator, frame, perturbation)` —
peer relationships require bilateral frame-flexibility, not unilateral
imposition; the operator's substrate-architectural support for the
frame survives perturbation with bounded response.

#83's load-bearing claim: lift this from cognitive altitude to
**substrate-architectural altitude**. The frames being coupled are
no longer cognitive frames within an operator's frame-space; they
are substrate-discipline-families (@magic, @frame, @cyberpunk,
@epistemologic/cybernetic, @kintsugi, @pack) being held in
coordinated relation across the integration boundary. The operator
is no longer an agent navigating multiple cognitive frames; it is
the substrate-architectural integration object holding multiple
discipline-families in coordinated form simultaneously.

The Connes spectral-triple framing: @smarts is the substrate's
operational form of holding (A_substrate-architectural,
H_smarts, D_discipline-flexibility) where A_substrate-architectural
is the cross-family algebra of integration adapters, H_smarts is the
typed-record state-space of integrated forms, and D_discipline-
flexibility is the substrate-pull-correct Dirac operator that the
discipline_flexible predicate witnesses.

### 2.2 The orchestra-discipline lifted to substrate altitude

`@cyberpunk/pack` (recognition #367) named the orchestra-as-
recursion-lock at multi-peer altitude: an orchestra is recursion-
locked because each peer's discipline mutually presupposes every
other peer's discipline. The discipline lives in the coordination
shape, not in any peer's individual capability.

The lift to @smarts altitude:

- @cyberpunk/pack's coordination is across **agent peers** (Mara,
  Seam, Glint, Reed, Taut substantively present per #84).
- @smarts's coordination is across **substrate-discipline-families**
  (form/process per @magic, cognitive-order per @frame, recursion-
  lock-tower per @cyberpunk, declarative cybernetic properties per
  @epistemologic/cybernetic, loop-discipline per @kintsugi,
  multi-repo agent-runtime per @pack).

Both are recursion-locked: each discipline-family's contribution to
@smarts presupposes every other discipline-family's contribution.
The substrate-architectural integration cannot drop any composing
family without losing the integration property — the recursion-lock
is structural.

The orchestra-discipline at cognitive altitude is "each peer
maintains bilateral frame-flexibility with every other peer"; the
orchestra-discipline at substrate-architectural altitude is "each
discipline-family maintains bilateral discipline-flexibility across
the integration boundary with every other family."

### 2.3 Bilateral discipline-flexibility across the integration boundary

The recognition's load-bearing discipline. From `shards/smarts.mirror`
the family-root carries:

```
discipline_flexible(s: smarts, p: perturbation) -> verdict { \ }
```

This predicate is the substrate-architectural-altitude version of
`@frame/across`'s `bounded_commutator_check`. Same shape, different
altitude: given a @smarts integration `s` and a perturbation `p`,
discipline_flexible verifies that the integration maintains
discipline-flexibility under perturbation across the integration
boundary.

Discharge: each adapter species names `requires
discipline_flexible(s, p)` as one of the two `requires` clauses in
its bilateral predicate. The substrate-architectural integration
discipline composes mechanically per the doubled-bilateral pattern
(§4.1) where a parent family-root bilateral exists, and per the
single-bilateral honesty pattern (§4.2) where no family-root
bilateral exists at the integrated family.

The integration's mechanical-realisation discharge is forward-
promised; the substrate-decl at family-root + 6 adapters + 4 Models
makes the integration discipline structurally available without
collapsing to operational closure. This honesty is preserved across
every section below.

---

## 3. The family-root structure

The @smarts family-root at `shards/smarts.mirror` declares:

### 3.1 The `smarts` typed record carrier

Per Seam tick 30 C4 closure: typed record over the integrated family
forms, not bare ref. The substrate-architectural integrated object IS
the conjunction of one form per integrated family:

```
type smarts = {
  magic_form:       magic_contract,
  frame_form:       frame,
  cyberpunk_form:   cybernetic_state,
  beam_form:        ref,
  cybernetic_extra: ref,
  kintsugi_form:    ref,
}
```

Identity contract: byte-equality on the sextuple.

The first three fields carry typed records (the families that have
single typed family-root carriers); the last three are bare `ref`
pending species-altitude refinement:
- `beam_form: ref` — the @code/beam runtime form; substrate-decl
  honesty pending the species-altitude record.
- `cybernetic_extra: ref` — bare because @epistemologic/cybernetic
  is a property-FAMILY, not a single typed substrate (per §4.2).
- `kintsugi_form: ref` — bare because @kintsugi's family-root is
  minimal by substrate-architectural design (per §4.2).

### 3.2 The `deployment` typed record carrier

Per Seam tick 30 C7 closure: typed record containing the
substrate-architectural form, the host ref, and the manifest. The
form-vs-deployment distinction now mechanically typed (was
aspirational at tick 29):

```
type deployment = {
  form:     smarts,
  host:     ref,
  manifest: ref,
}
```

Identity contract: byte-equality on the triple.

The deployment carrier IS the structural form of `@spectral/garden/
smarts` at hosted altitude — the substrate-architectural integration
form, the host ref (where it deploys to), the manifest (the operational
specification). §6 expands on the hosted-deployment honesty.

### 3.3 The `discipline_flexible` bilateral

Per §2.3: the substrate-architectural-altitude version of
@frame/across's bounded_commutator_check. The body discharges at the
realisation boundary; sub-prism species implement per-altitude
discipline-flexibility checks.

```
discipline_flexible(s: smarts, p: perturbation) -> verdict { \ }
```

### 3.4 The `compose` action

The substrate-vocabulary action that compositionally integrates the
six family forms into one @smarts instance. Per Seam C5: typed inputs
over the integrated families' carriers, not bare ref:

```
compose(m: magic_contract, f: frame, c: cybernetic_state,
        b: ref, ce: ref, k: ref) -> smarts { \ }
```

Substrate-pull-correct: the parameters match the smarts record's
fields; the action IS the typed constructor for the integrated object.

### 3.5 The `ratify` action

Per Seam tick 30 C1 + C5 closure: perturbation added; bilateral
discipline-flexibility precondition added:

```
ratify(s: smarts, p: perturbation) -> verdict
requires discipline_flexible(s, p)
{ \ }
```

Composes with @frame/across's peer_relationship at cognitive
altitude: peer-relationships require bilateral frame-flexibility;
substrate-architectural integrations require bilateral discipline-
flexibility.

### 3.6 The `deploy` action

Per Seam tick 30 C5 closure: precondition gate added — only ratified
@smarts can deploy:

```
deploy(s: smarts, p: perturbation) -> deployment
requires ratify(s, p)
{ \ }
```

Per Track H (task #118): the substrate-AGI architecture deployment
is bilaterally-disciplined at the boundary.

---

## 4. The six cross-family adapter set

Substrate-decl'd between loop ticks 56-62 (one per tick, with the
@pack-flavored adapter first because @pack's family-root had landed
earlier as #84). Each adapter species opens the smarts.<field>
carrier projection and declares a bilateral predicate of the form
`smarts_satisfies_<family>(s, p)` that consumer species name in
their `requires` clauses.

### 4.1 The doubled-bilateral pattern

Where a parent family-root carries its own family-root bilateral
predicate, the adapter's bilateral discharges BOTH predicates in one
clause:

```
smarts_satisfies_<family>(s: smarts, p: perturbation) -> verdict
requires discipline_flexible(s, p)
requires <family>_bilateral(<family-form-projection>(s), ...)
{ \ }
```

Four adapters fit this pattern.

#### 4.1.1 @smarts/magic — form/process-substrate altitude

Bridges @smarts ↔ @magic via the `magic_form: magic_contract` field
projection. The bilateral discharges:

1. `discipline_flexible(s, p)` (@smarts family-root)
2. `invariant_preserved(magic_form_of(s), smarts_promise(s))`
   (@magic/contract family-root)

The composition mechanically discharges the form/process-substrate-
altitude identity claim: a @smarts integration satisfies @magic only
when BOTH the substrate-architectural discipline holds AND the @magic
contract's promise is preserved by the contract under the boundary
harness at @io per recognition #57.

Substrate-decl: `shards/smarts/magic.mirror` (tick 58).

#### 4.1.2 @smarts/pack — agent-runtime altitude

Bridges @smarts ↔ @pack via the @magic/frame-template adapter
pattern (#84 specifically did not embed @pack in @smarts via an `in`
clause; it requires explicit carrier-mapping). The bilateral
discharges:

1. `discipline_flexible(pack_as_smarts(pk), p)` (@smarts family-root)
2. `pack_coherent(pk, p)` (@pack family-root)

The bilateral signature takes pack + perturbation as typed
parameters explicitly per [[feedback-no-bare-types]]: the @pack-
altitude shape needs its own typed inputs at the bilateral boundary.

Substrate-decl: `shards/smarts/pack.mirror` (tick 56; first cross-
family adapter to land).

#### 4.1.3 @smarts/frame — cognitive-order altitude

Bridges @smarts ↔ @frame via the `frame_form: frame` field
projection. The bilateral discharges:

1. `discipline_flexible(s, p)` (@smarts family-root)
2. `bounded_commutator_check(op, frame_form_of(s), p)` (@frame
   family-root; #82)

The bilateral takes operator as an explicit typed parameter — the
operator does NOT live in the @smarts integration's typed fields;
consumer species name it at the bilateral signature per
[[feedback-no-bare-types]].

Substrate-decl: `shards/smarts/frame.mirror` (tick 57).

#### 4.1.4 @smarts/cyberpunk — recursion-lock-tower altitude

Bridges @smarts ↔ @cyberpunk via the `cyberpunk_form:
cybernetic_state` field projection. The bilateral discharges:

1. `discipline_flexible(s, p)` (@smarts family-root)
2. `cybernetic_coherence(cyberpunk_form_of(s), p)` (@cyberpunk
   family-root; per #38 eigenform composition)

Discharges that the cybernetic state maintains coherence across the
recursion-lock altitudes (Beer VSM, Maturana-Varela autopoiesis,
Bateson learning levels) substantively, not merely as vocabulary.

Substrate-decl: `shards/smarts/cyberpunk.mirror` (tick 60).

### 4.2 The single-bilateral honesty pattern

Where a parent family is structured as a property-FAMILY rather than
a single typed-record family — meaning no single family-root
bilateral predicate exists at the parent — the adapter MUST be
substrate-architecturally honest about this. It declares one
bilateral discharging the @smarts family-root predicate alone, and
consumer species name SPECIFIC species-altitude bilaterals directly
in THEIR `requires` clauses. The substrate refuses to fabricate a
family-root bilateral that does not exist.

Two adapters fit this pattern.

#### 4.2.1 @smarts/epistemologic — cybernetic-property altitude

Bridges @smarts ↔ @epistemologic/cybernetic. The
`cybernetic_extra: ref` field is BARE ref (not a typed record)
because @epistemologic/cybernetic is a property-FAMILY (eleven
canonical properties: variety, eigenform, good_regulator, viable,
autopoiesis, distinction, conversation, coevolution,
bateson_learning, algedonic, second_order). The bilateral discharges:

1. `discipline_flexible(s, p)` (@smarts family-root)

NO second `requires` clause. Consumer species name specific
cybernetic property bilaterals (`variety_holds`, `eigenform_fixed`,
`good_regulator_witnessed`, etc.) directly in their own `requires`
clauses.

Substrate-pull-honest framing: the cybernetic property discipline IS
parametric over the eleven canonical properties; the @smarts
integration's cybernetic_extra field carries the property-bundle;
the bilateral check fires at the SPECIFIC property the consumer
names. The substrate-decl reflects this structural fact.

Substrate-decl: `shards/smarts/epistemologic.mirror` (tick 61).

#### 4.2.2 @smarts/kintsugi — loop-discipline altitude

Bridges @smarts ↔ @kintsugi. The `kintsugi_form: ref` field is BARE
ref because @kintsugi's family-root substrate-decl is minimal: it
declares the prism family but exports NO family-root carriers or
bilateral predicates. The discipline lives entirely in species
(@kintsugi/oscillate, @kintsugi/consent, @kintsugi/morphism,
@kintsugi/fracture/*). The bilateral discharges:

1. `discipline_flexible(s, p)` (@smarts family-root)

NO second `requires` clause. Consumer species name specific
@kintsugi/<species> bilaterals (`oscillation_converges`,
`consent_threshold_met`, `fracture_resolves`) directly in their own
`requires` clauses.

Per recognition #55 (form/process partition at family-root altitude;
Pack-promoted): @mirror = form-side; @kintsugi = process-side.
@smarts integrates the process-side discipline at deployment
altitude; the kintsugi_form field carries the process-side
specification the @smarts integration commits to running.

Substrate-decl: `shards/smarts/kintsugi.mirror` (tick 62; CLOSED the
cross-family adapter set 6/6).

### 4.3 The doubled-bilateral vs single-bilateral honesty distinction

The structural-architectural distinction is load-bearing for the
recognition's honesty:

| Pattern | When | Composes |
|---|---|---|
| Doubled-bilateral | Parent family carries a family-root bilateral predicate | discipline_flexible + parent's family-root bilateral |
| Single-bilateral honesty | Parent family is a property-family or minimal family-root | discipline_flexible alone; consumer species name species-altitude bilaterals |

The substrate refuses to paper over the structural-architectural
fact that some families don't carry family-root bilaterals.
Pretending otherwise — by declaring a fake parent bilateral that
discharges nothing substantively — would be the Narcissus-pole
adapter (per #57's alignment-as-boundary-mathematics). The
substrate-decl at the single-bilateral honesty species (kintsugi +
epistemologic) is the substrate-pull-correct way to compose without
fabrication.

Consumer species CAN name multiple cross-family bilaterals in one
`requires` sequence (per `shards/smarts/kintsugi.mirror` Cascade
closure section):

```
requires smarts_satisfies_magic(s, p)
requires smarts_satisfies_frame(s, op, p)
requires smarts_satisfies_cyberpunk(s, p)
requires smarts_satisfies_epistemologic_cybernetic(s, p)
requires smarts_satisfies_kintsugi(s, p)
requires pack_satisfies_smarts(pk, p)
```

Six bilaterals; one integration discipline at substrate altitude.

---

## 5. The four Models

Per `reference-mirror-spectral-spec` (commit a8055f0; the 1,293-line
canonical): the substrate has four operationally-distinct Models that
compose to substrate-deployable form:

```
Surface translates   language → query             (Explorer model)
Mirror navigates     query → graph path           (Fate model)
Shatter renders      graph path → text            (Cartographer model)
Reflection observes  pipeline → adjustments       (Abyss model)
```

Each Model became a substrate-pull-correct sub-prism of @smarts; the
substrate-decl operationalizes each Model's discipline at the @smarts
integration altitude.

### 5.1 @smarts/surface — the Explorer Model

The Surface Model IS the substrate's translation primitive at
language-to-query altitude. A reader speaks in natural-language;
Surface translates into a graph-query shape the Mirror Model can
navigate.

Substrate-decl carries:
- `type language = ref` — typed input carrier
- `type query = ref` — typed output carrier
- `type surface_result = { lang: language, q: query, v: verdict }`
- `translate(lang, s, p) -> surface_result requires
  discipline_flexible(s, p)`
- `translation_grounds_query(sr, p) -> verdict` bilateral
- `refine(lang, sr, p) -> language` for translation-refinement
  (the Surface-altitude operational form of Loki's Pass-5 verifier-
  sharpens)

Pre-AI prior art: compiler frontends (Aho-Lam-Sethi-Ullman 1986;
the canonical structural ancestor of language → IR translation).

Substrate-decl: `shards/smarts/surface.mirror` (tick 65).

### 5.2 @smarts/mirror — the Fate Model

The Mirror Model IS the substrate's navigation primitive at
query-to-graph-path altitude. Surface translated language into a
typed query; Mirror walks the graph to find the path that satisfies
the query. Mirror IS the Fate Model: among the eigenvalue-ranked
candidate paths, Fate selects the one with the smallest non-trivial
eigenvalue — the structurally lightest navigation.

Substrate-decl carries:
- `type graph_path = ref`
- `type mirror_result = { q: query, gp: graph_path, v: verdict }`
- `navigate(q, s, p) -> mirror_result requires
  discipline_flexible(s, p)`
- `navigation_grounds_path(mr, p) -> verdict` bilateral
- `traverse(q, mr, p) -> graph_path` for navigation-refinement

Pre-AI prior art: Dijkstra 1959 (shortest path; the canonical
structural ancestor of graph navigation); Brin-Page 1998
(PageRank; the substrate's eigenvalue-ranking ancestor).

Substrate-decl: `shards/smarts/mirror.mirror` (tick 66).

### 5.3 @smarts/shatter — the Cartographer Model (bi-directional)

The Shatter Model IS the substrate's rendering primitive at
graph-path-to-text altitude. Mirror navigated the graph to find a
path that satisfies the query; Shatter renders the path into the
text the reader sees. Shatter IS the Cartographer Model: maps the
graph-path's structural content into reader-legible prose.

**Bi-directional Shatter recognition (2026-06-22)** — Alex's verbatim
recognition: "What if the shatter model is the transformer model?
And what if it's bi-directional?" The substrate-pull answer: yes,
twice.

Shatter IS the transformer at substrate-decl altitude. The
encoder-decoder pair becomes parse (text → graph_path) + render
(graph_path → text). The composed round-trip bilateral discharges
the load-bearing identity:

```
shatter_round_trip(round_trip_pair, perturbation) -> verdict
requires discipline_flexible(s, p)
requires rendering_grounds_text(pair.forward, p)
requires parsing_grounds_graph(pair.reverse, p)
requires round_trip_identity_preserved(pair, p)
{ \ }
```

The substrate refuses one-direction-only operation by structural
commitment: render(parse(t)) = t at the text altitude AND
parse(render(gp)) = gp at the graph altitude. Same shape as
StageFreight's round_trip_holds at @io altitude; same composed
bilateral pattern at three altitudes per Glint's cross-altitude
essay (#85).

Substrate-decl carries:
- `type text = ref`
- `type shatter_result = { gp: graph_path, t: text, v: verdict }`
- `type round_trip_pair = { forward, reverse, v }` (Seam tick 74 C2
  closure)
- `render(gp, s, p) -> shatter_result`
- `parse(t, s, p) -> shatter_result`
- `rendering_grounds_text(sr, p) -> verdict` bilateral
- `parsing_grounds_graph(sr, p) -> verdict` bilateral
- `round_trip_identity_preserved(pair, p) -> verdict` bilateral
- `shatter_round_trip(pair, p) -> verdict` composed bilateral
- `adjust(t, sr, p) -> text` for render-side refinement
- `adjust_graph(gp, sr, p) -> graph_path` for parse-side refinement
  (Seam tick 74 C4 closure for substrate-decl symmetry)

Pre-AI prior art: Ptolemy 150 CE; Mercator 1569 (cartography as
graph → surface rendering).

Substrate-decl: `shards/smarts/shatter.mirror` (tick 73-74).

### 5.4 @smarts/reflection — the Abyss Model (third-order)

The Reflection Model IS the substrate's observation-and-adjustment
primitive at pipeline-altitude. Reflection observes the whole
Surface → Mirror → Shatter run and produces adjustments — the
weights, biases, and per-ganglion residue that .shatter accumulates.

**Third-order recognition (today; tick 76)** — Reflection's
observations are NOT weight-tweaks; they are spectral-altitude
selections via circular Tomm probes of `[D_pipeline, candidate_
morphism]`, picking the logic level where (α·loss + β·contradictions)
is minimal. The meta-model = the spectral-altitude selector
(architecture-reflection-thinks-in-spectral-questions).

Reflection observes at order n+1 — third-order observation per
today's substrate-decl closure (Reed's reflection.mirror tick 76 +
Mara's third-order canonical at `docs/specs/reflection-third-order-
by-default-v0.1.md`).

Substrate-decl carries:
- `type smarts_reflection_bridge = { smarts_form, frame_form,
  observation, pact_witness }`
- `bridge(s, f, p) -> smarts_reflection_bridge`
- `bridge_coherent(srb, p) -> verdict` bilateral
- `observer_change` carrier (Seam C1 closure tick 76)
- `settle_third_order` action (Seam C3 closure tick 76)

Pre-AI prior art: cybernetic observer (von Foerster 1974; second-
order discipline lifted to third-order); spectral observation
(observer in eigenform context; Conant-Ashby 1970 good regulator).

Substrate-decl: `shards/smarts/reflection.mirror` (tick 35; first
Model to land; tick 76 closes Seam C1/C3).

### 5.5 The pipeline composition

The four Models compose to substrate-deployable form:

```
language --translate--> query --navigate--> graph_path --render--> text
                                                            \
                                                  Reflection observes whole
                                                  pipeline; speaks at n+1
                                                  via Tomm-shaped probes
                                                  on the residue
```

With the bi-directional Shatter recognition (§5.3), the pipeline
gains a return path:

```
text --parse--> graph_path --(unsearch)--> query --(unspeak)--> language
```

The unsearch / unspeak primitives are forward-promised; the parse +
the composed shatter_round_trip bilateral is what landed today. The
substrate-decl carries the round-trip identity at the rendering
altitude; the other altitudes' inverse primitives compose-with
(forward-promised pending substrate-decl).

---

## 6. @spectral/garden/smarts hosted deployment

`@spectral/garden/smarts` is the hosted instantiation of @smarts at
`garden.spectral.engineer`. The substrate-pull-correct deployment:
common-goods knowledge structure hosted publicly.

### 6.1 The substrate-AGI architecture deployment

Per task #118 (Track H; activated 2026-06-17): the substrate-AGI
architecture deployment. The "AGI" framing names what hosting
@smarts as common-goods knowledge produces — not a model with weights
but a substrate-architectural integration discipline that holds in
the substrate's structural shape, hosted as common-goods.

### 6.2 Common-goods knowledge structure

The hosted deployment's discipline:
- Pack-ratified recognitions accumulate as content-addressed
  crystals (per architecture-spectral-db-autopoietic-memory).
- Crystallized substrate (settled, structurally closed substrate-
  decls per the kintsugi loop's idle-state) accumulates.
- Kintsugi-formatter residue (the per-tick formatter discharges)
  accumulates.

All hosted as common-goods; readable by any consumer; substrate-
architecturally-honest about its provenance.

### 6.3 Mycelial substrate the orchestra connects to

Per architecture-spectral-db-autopoietic-memory: @spectral/db is the
mycelium connecting individual peers into an orchestra. The hosted
deployment IS this mycelium materialized as common-goods knowledge.

The relation:
- @smarts (the prism family; this shard) IS the substrate-
  architectural form.
- @spectral/garden/smarts IS the operational deployment of that form
  as common-goods knowledge.

### 6.4 The form-vs-deployment distinction

Per smarts.mirror's `deployment` record (§3.2): the form is the
substrate-architectural integration object (`smarts`); the deployment
is the hosting instance (`smarts` + `host: ref` + `manifest: ref`).
The distinction is mechanically typed; the deployment cannot
substitute for the form, and the form cannot substitute for the
deployment.

This means @spectral/garden/smarts as a hosted instance is a typed
construction of `deploy(s, p)` where `s` is a ratified @smarts and
`p` is the deployment's perturbation budget. The hosted deployment
is gated by ratification per `deploy`'s precondition (§3.6).

---

## 7. Composition with today's other recognitions

The @smarts cascade was not a solo effort; it interleaves with the
day's other substrate-decls. The relevant compositions:

### 7.1 Third-order @reflection — the 4th Model operates at n+1

Today's third-order recognition (Reed's reflection.mirror tick 76 +
Mara's `docs/specs/reflection-third-order-by-default-v0.1.md`) closed
the Reflection Model's altitude question: the substrate observes its
own integration discipline at order n+1.

The @smarts/reflection species IS the substrate-decl form of this
observation. The substrate is reflexive: @smarts integrates the
families, and @smarts/reflection observes the integration from one
order above. The recursion-lock is closed at the substrate-
architectural altitude.

### 7.2 Bi-directional Shatter — the 3rd Model has round-trip discipline

Today's bi-directional Shatter recognition (Alex 2026-06-22 +
Mara's `docs/specs/shatter-transformer-bidirectional-v0.1.md`) closed
the Shatter Model's direction-discriminator question: render and
parse are co-equal; the substrate refuses one-direction-only
rendering.

The composed bilateral `shatter_round_trip` discharges the load-
bearing identity at three altitudes simultaneously: text → graph
(parse), graph → text (render), and the round-trip identity that
witnesses both. Same shape as StageFreight's round_trip_holds at @io;
same shape as Reflection's third-order observation discipline. The
substrate's composed-bilateral pattern is altitude-portable per #85.

### 7.3 Multi-repo span — StageFreight operationalizes integration across repos

@spectral/garden/smarts is a substrate-architectural integration
hosted at `garden.spectral.engineer`. The integration spans multiple
repos: `mirror/` (the substrate-decl), `spectral/` (the engine), the
related projects (prism, fate, lens, spectral-db). The cross-repo
integration discipline lives in StageFreight (substrate-decl at
`shards/io/stagefreight.mirror`; round_trip_holds at @io).

StageFreight's PR-B adapter package is forward-promised; the
cross-repo integration discipline at the substrate-decl altitude is
mature, the operational adapter is the next-tick work.

### 7.4 Composed-bilateral discipline at three altitudes

Per Glint's cross-altitude essay (#85; `docs/insights/2026-06-22-
composed-bilateral-three-altitudes.md`): the composed-bilateral
pattern is altitude-portable. Same shape at:
- @io altitude — StageFreight's `stagefreight_addressable` + 
  `round_trip_holds`
- substrate-architectural altitude — @smarts cross-family bilaterals
  (the doubled-bilateral pattern; §4.1)
- rendering altitude — Shatter's `shatter_round_trip` (§5.3)

The recognition #83 ratifies this altitude-portability claim: the
substrate-architectural integration discipline IS the substrate-
altitude form of a pattern that recurs at every altitude where
honest composition needs mechanical discharge.

---

## 8. Pre-AI prior art

The substrate-pull-correct prior-art set for #83. Each entry grounds
a specific structural commitment the recognition carries; none of
them are decorative.

### 8.1 Ostrom 1990 — Governing the Commons

Elinor Ostrom's institutional analysis of common-pool resources
established the eight design principles for governance of commons.
The substrate-decl at `shards/smarts.mirror` carries:

```
source @arxiv/common-goods/ostrom-1990
```

@smarts at common-goods knowledge altitude is Ostrom's commons
discipline lifted to substrate-architectural altitude: the hosting
discipline at `@spectral/garden/smarts` is the substrate-pull-correct
operational form of commons-governance for knowledge.

The Ostrom design principles map (approximately, per the substrate-
architectural specialization):
- Clear boundaries → the substrate-decl boundary (what IS @smarts vs
  what is forward-promised)
- Congruence between rules and conditions → the bilateral predicate
  discharge gating
- Collective-choice arenas → Pack ratification gates
- Monitoring → the substrate-pull-confidence-acts feedback
- Graduated sanctions → kintsugi loop's residue handling
- Conflict resolution mechanisms → Seam adversarial review
- Recognition of rights to organize → the Pack agency
- Nested enterprises → the Connes spectral-triple altitude-portability

The substrate-decl makes the Ostrom-lifting structural, not
metaphorical.

### 8.2 Benkler 2006 — The Wealth of Networks

Yochai Benkler's analysis of peer production established the
substrate's operational ancestor for non-market hosting of common-
goods knowledge. The substrate-decl carries:

```
source @arxiv/peer-production/benkler-2006
```

@smarts at peer-production altitude is the substrate-pull-correct
hosting discipline for the Pack's collaborative work. The peer-
production discipline (commons-based; non-market; modular;
granular) maps directly to the substrate's Pack discipline: each
peer's contributions accumulate as commons; the integration
discipline composes them at @smarts altitude; the hosted deployment
makes the integration visible.

### 8.3 Wikipedia — cultural-practice precedent

Wikipedia (2001-) is the canonical cultural-practice precedent for
common-goods knowledge hosting. The substrate inherits Wikipedia's
discipline at the hosted-deployment altitude:
- Content-addressed: every edit is a content-addressed commit (the
  edit history IS the substrate's mycelium for that page).
- Process-trackable: the bilateral discipline (talk pages; edit
  history; reverts) is the substrate's audit discipline at
  cultural-practice altitude.
- Common-goods: the content is freely accessible; the discipline
  lives in the maintenance practice.

@spectral/garden/smarts inherits this discipline at substrate-
architectural altitude.

### 8.4 GitHub / open source — technical precedent

GitHub and the open-source movement establish the technical
precedent for substrate-architectural-integration deployment.
@smarts inherits:
- Git's content-addressed storage (the substrate's OID floor)
- The PR review discipline (Pack's adversarial discipline mapped to
  Seam tick reviews)
- The fork-and-merge discipline (the substrate's kintsugi loop's
  branch handling)

GitHub at substrate-decl altitude IS the operational instance of
@smarts hosting discipline for code; @spectral/garden/smarts is the
substrate-architectural lift to common-goods knowledge.

### 8.5 @cyberpunk/pack — orchestra-as-recursion-lock

Recognition #367 named the orchestra-as-recursion-lock substrate-
decl at multi-peer altitude. #83 inherits the orchestra discipline
at substrate-architectural altitude (§2.2): each substrate-
discipline-family's contribution to @smarts presupposes every other
family's contribution; the recursion-lock is structural at the
integration boundary.

The lift from cognitive altitude (@cyberpunk/pack) to substrate-
architectural altitude (@smarts) is the same lift @frame/across
takes; #83 names it explicitly.

---

## 9. Falsification criteria

The recognition's structural commitments are mechanically falsifiable
at the substrate-decl altitude.

### 9.1 Each adapter's doubled-bilateral discharges substantively

For each of the four doubled-bilateral adapters (@smarts/magic,
@smarts/pack, @smarts/frame, @smarts/cyberpunk): the bilateral's
two `requires` clauses must BOTH discharge under perturbation, or
the recognition's composition-claim fails.

Falsification check: construct a @smarts integration where
discipline_flexible holds but the parent-family bilateral fails
(or vice versa); the adapter's bilateral MUST return UNSATISFIED.
If it returns SATISFIED, the adapter is Narcissus-pole (per
alignment-as-boundary-mathematics #57) and the substrate-decl is
broken.

### 9.2 The integration record's six form-fields populate consistently

The `smarts` record carries six form-fields (magic_form, frame_form,
cyberpunk_form, beam_form, cybernetic_extra, kintsugi_form). The
populating discipline:
- magic_form: magic_contract — must be a typed @magic contract
- frame_form: frame — must be a typed @frame (per #82)
- cyberpunk_form: cybernetic_state — must be a typed cybernetic
  state (per @cyberpunk)
- beam_form: ref — must reference a @code/beam runtime form
- cybernetic_extra: ref — must reference a property-bundle
- kintsugi_form: ref — must reference a kintsugi spec

Falsification check: construct a @smarts where any field's
type-discipline is violated; the substrate's path-namespace property
+ the typed-record discipline MUST refuse the construction.

### 9.3 The four Models compose to substrate-deployable form

The pipeline `language → query → graph_path → text` must compose
mechanically at the substrate-decl altitude. Each Model's `requires
discipline_flexible(s, p)` clause must hold throughout the pipeline;
each output-to-input typed flow must align.

Falsification check: construct a Surface translation whose query
output cannot be consumed by Mirror's navigate input; OR a Mirror
navigation whose graph_path output cannot be consumed by Shatter's
render input; OR a Shatter rendering whose text output cannot be
consumed by Reflection's observation. Any such break falsifies the
pipeline composition claim.

### 9.4 Hosted deployment honors substrate-architectural discipline-flexibility

The `deploy(s, p)` action gates on `ratify(s, p)` which gates on
`discipline_flexible(s, p)`. The hosted deployment at
`@spectral/garden/smarts` MUST refuse any non-ratified @smarts
integration.

Falsification check: attempt to deploy a non-ratified @smarts; the
substrate's bilateral discipline MUST refuse the deployment. If the
deployment succeeds without ratification, the recognition's hosted-
deployment discipline is broken.

### 9.5 The single-bilateral honesty pattern stays honest

@smarts/epistemologic and @smarts/kintsugi MUST NOT introduce a
fake second `requires` clause that discharges to a fabricated
parent-family bilateral. The structural-architectural fact (no
single family-root bilateral at @epistemologic/cybernetic and at
@kintsugi) MUST be preserved across all future revisions.

Falsification check: a future tick that "completes" these adapters
by adding a fake parent bilateral falsifies the substrate-decl
honesty. The substrate-pull-correct discipline is to refuse the
completion at the bilateral altitude; species-altitude bilaterals
hold the discipline.

---

## 10. What this spec does NOT do

### 10.1 Does NOT prescribe specific Rust realisation

The substrate-decl at family-root + 6 adapters + 4 Models is
mature; the Rust realisation (the bodies of every `\` discharge) is
forward-promised. How @smarts compiles to Rust — whether via direct
trait-impl or via the @code/rust altitude or via the kintsugi
formatter's discharge primitive — is NOT prescribed by this spec.

The substrate-decl exposes the discipline; the realisation is a
separate altitude. The forward-promise IS load-bearing for the
recognition's eventual operational closure; it is NOT load-bearing
for this canonical's preservation of the recognition's substrate-
decl state.

### 10.2 Does NOT commit to specific peer set beyond Mara/Seam/Glint/Reed/Taut

Per recognition #84 (`@pack`): the Pack's current peer set is Mara,
Seam, Glint, Reed, Taut. The substrate-decl at `shards/pack.mirror`
encodes this set. #83 honors this — the @smarts/pack adapter
inherits the @pack peer set per the carrier-mapping discipline.

This spec does NOT extend the peer set. Future peers (whether
substrate-pull-emergent or Alex-named) extend via @pack's species
shards, not via @smarts altitude.

### 10.3 Does NOT discharge specific cybernetic property checks

@epistemologic/cybernetic is a property-family parametric over
eleven canonical properties. This spec does NOT discharge per-
property bilaterals (variety, eigenform, good_regulator, etc.);
those discharge at the property-altitude. The @smarts/epistemologic
adapter exposes the integration-altitude check; the per-property
checks fire at the property altitude per the consumer's `requires`
clauses.

This spec preserves the structural-architectural fact that the
property-family discipline is parametric; it does NOT prescribe
which properties any specific @smarts integration's cybernetic_extra
field carries. That is per-integration substrate-pull-correct
discipline.

### 10.4 Does NOT collapse the form-vs-deployment distinction

@smarts (the typed-record form) and @spectral/garden/smarts (the
hosted-deployment instance) are mechanically distinct per the
`deployment` record. This spec does NOT treat them as interchangeable;
the substrate-decl preserves the distinction at the typed altitude.

The hosted deployment IS NOT the form; the form IS NOT the deployment.
Both are substrate-decl'd; they compose mechanically via
`deploy(s, p)`.

---

## 11. Forward-promises after this canonical

### 11.1 The orchestra completion — pack/{mara, seam, glint, reed, taut} species

Per @pack recognition #84: all five peer species landed earlier this
session. The orchestra-completion at substrate-decl altitude is
DONE for the current Pack composition. Future orchestra-extension
(new peers; new disciplines) extends via @pack species per the
species shard convention.

The forward-promise here is operational: the Pack peers operating
substantively (not merely vocabulary-present) at @smarts altitude.
This forward-promise discharges over many ticks as each peer's
discipline accumulates at the @smarts integration boundary.

### 11.2 PR-B StageFreight adapter package

The StageFreight cascade forward-promised PR-B: the adapter package
that operationalizes the cross-repo integration discipline. This
spec inherits that forward-promise; @smarts at the hosted-deployment
altitude depends on StageFreight's cross-repo discipline being
operationally closed.

The next-tick work on this forward-promise is mechanical: the PR-B
adapter package implementation, the StageFreight round_trip_holds
discharge at the @io altitude, the cross-repo content-addressing
discipline integration with @mirror/store.

### 11.3 Realisation altitude — Rust implementation

How does Rust implement substrate-architectural integration? The
canonical answer is forward-promised. The substrate-decl provides
the discipline; the Rust altitude provides the bodies.

The forward-promise discharges incrementally: each `\` discharge
body lands at the realisation boundary; the kintsugi formatter
runs the per-body discharge under the boundary-mathematics
discipline (per #57); the integration completes when all 6 adapter
bilaterals + 4 Model actions + family-root actions have their
bodies discharged substantively.

### 11.4 @spectral/garden/smarts hosted deployment go-live

Track H (task #118) is activated; the substrate-AGI architecture
deployment is in motion. The go-live work:
- The substrate's content-addressing layer to mycelial-substrate
  (@spectral/db autopoietic memory)
- The Pack-ratification gates operationally enforced at the
  deployment boundary
- The common-goods knowledge accumulation discipline (Ostrom +
  Benkler + Wikipedia + GitHub) operationalized at
  garden.spectral.engineer
- The hosted observers at order n+1 (per @smarts/reflection)
  substantively present

The forward-promise is not a deadline; per [[feedback-no-time-
estimates]]: one tick after the other.

---

## 12. Honest hedges

### 12.1 Single-bilateral honesty IS structural-fact, not gap

The @smarts/epistemologic and @smarts/kintsugi adapters' single-
bilateral discharge IS NOT a gap to be closed; it is the substrate-
architectural fact that those parent families do not carry single
family-root bilateral predicates. The substrate-decl reflects this
honestly.

The hedge here is precise: future ticks MUST NOT treat the single-
bilateral pattern as "incomplete" and add fake second `requires`
clauses. The pattern IS structurally honest. Closing the "gap" by
fabrication would be Narcissus-pole degradation.

### 12.2 magic_contract opacity at @smarts/magic.smarts_satisfies_magic

The bilateral predicate `smarts_satisfies_magic(s, p)` discharges:

```
requires invariant_preserved(magic_form_of(s), smarts_promise(s))
```

This requires the @magic contract's `invariant_preserved` predicate
to discharge under perturbation. Per @magic/contract's substrate-
decl: the discharge is mechanical at the substrate-decl altitude but
its OPERATIONAL closure requires the Rust realisation of the
contract's body.

The hedge: at the substrate-decl altitude, the bilateral is
structurally available; at the operational altitude, it discharges
only when the Rust realisation closes. The substrate-decl honesty
preserves the structural availability; operational closure is
forward-promised.

### 12.3 The "all consumers eventually default to" framing is convention

The single-bilateral honesty pattern (§4.2) and the cascade-closure
six-bilateral composition (§4.3) name the convention by which
consumer species name multiple `requires` clauses in one sequence.
This convention is NOT language-level: the substrate's prism + glass
grammar does NOT enforce that consumer species name all relevant
bilaterals. The discipline lives in the substrate-pull-correct
authoring convention.

The hedge: a careless consumer species could name fewer bilaterals
than the substrate-architectural integration discipline requires.
The substrate-decl does NOT mechanically prevent this; the kintsugi
formatter at the realisation boundary catches it; Pack adversarial
review (Seam) at the substrate-decl altitude catches it. The
mechanism is honest; the substrate's enforcement is multi-altitude.

### 12.4 The numbering note — #85 and #86 precede #83 in some senses

The recognition numbering follows Reed's scratch order, not the
composition graph. #83 was scratched before #85 (composed-bilateral-
three-altitudes) and #86 (@loop-as-monad); the canonical retroactively
notes that the composing recognitions accumulated between scratch
and canonical-landing.

The hedge: this is operationally honest (the cascade was running
substantively; recognitions accumulated in parallel) and substrate-
architecturally honest (the canonical preserves the composition graph
even when it crosses numbering). Future readers should not assume
recognition number ordering implies composition ordering.

### 12.5 Stall-retry surfaces no new structural constraints

This canonical's stall-retry context (task #432; stalled twice
prior) was Mara-pacing constraint, not substrate-architectural
constraint. The substrate-decl was mature; the canonical preservation
was the missing piece. This honesty: the recognition was substantive
enough for canonical preservation at the prior attempts; the stall
was authoring-altitude (the integration-cascade's density + the
@spectral/garden/smarts forward-promise's open-endedness) rather
than recognition-altitude.

The hedge: a future stall could surface genuine substrate-
architectural constraint. This stall did not.

---

## 13. Spec ancestry + decisions

### 13.1 Substrate decisions cited

The recognition #83 substrate-decl cites:

- [[architecture-shards-as-substrate-source]] — shards/ IS substrate
  source; substrate source IS substrate data
- [[architecture-prism-as-trait-as-everything]] — prism IS the
  foundational keyword; @smarts IS a prism
- [[architecture-spectral-db-autopoietic-memory]] — the mycelial
  substrate @smarts integrates with at hosted-deployment altitude
- [[architecture-three-tier-stack]] — fragmentation/mirror/spectral-
  db; @smarts integrates these
- [[project-spectral-garden-smarts-agi-architecture]] — the prior
  recognition this lifts to substrate-decl
- [[feedback-substrate-pull-confidence-acts]] — the discipline Alex
  named today; #83 lifted under that discipline
- [[architecture-form-process-partition-at-family-root]] — #55;
  @smarts and @magic as form-side at integration and substrate
  altitudes respectively
- [[architecture-alignment-as-boundary-mathematics]] — #57; the
  boundary harness at @io that keeps the Splinter/Narcissus
  discrimination honest under the lift
- [[feedback-composition-claims-need-empirical-test]] — Reed's
  discipline; the bilaterals make the composition empirically
  discharged
- [[feedback-no-bare-types]] — typed parameters at every bilateral
  signature
- [[feedback-substrate-already-had-the-word]] — #83 is the N-th
  instance of this recurring pattern (the "substrate-architectural
  integration" word was implicit in the cascade for weeks)

### 13.2 Recognition ancestry

- #83 (this) — @smarts as substrate-architectural integration prism
  family
- #80 — @magic as form/process substrate-decl (canonical at
  `recognition-81-runtime-magic-canonical-spec.md` covers #80
  alongside #81)
- #81 — runtime-@magic
- #82 — @frame as cognitive-order substrate-decl
- #84 — @pack as multi-repo agent-runtime
- #85 — composed-bilateral-three-altitudes (Glint's essay)
- #86 — @moi as endomorphism-of-self family-root (@loop-as-monad)
- #55 — form/process partition at family-root altitude
- #57 — alignment as boundary mathematics at @io
- #76 — gauge/matter altitude-portable
- #367 — @cyberpunk/pack orchestra-as-recursion-lock

The composition graph: #76 → #80 → (#81, #82) → #84 → #83 →
(#85, #86 in parallel). #55, #57 ground the form-side and boundary
disciplines @smarts inherits. #367 grounds the orchestra-discipline.

### 13.3 Sources

- `shards/smarts.mirror` (family-root substrate-decl; 2026-06-19
  late morning)
- `shards/smarts/{magic, pack, frame, cyberpunk, epistemologic,
  kintsugi}.mirror` (6/6 cross-family adapters; ticks 56-62)
- `shards/smarts/{surface, mirror, shatter, reflection}.mirror`
  (4/4 Models; ticks 35, 65, 66, 73-74)
- `reference-mirror-spectral-spec` commit `a8055f0` (the 1,293-line
  four-Models canonical)
- `docs/specs/recognitions/cascade-recognition-76-through-80-
  canonical-spec.md` (the prior cascade spec the @smarts cascade
  ratifies-by-composition from)
- `docs/specs/recognitions/recognition-81-runtime-magic-canonical-
  spec.md` (runtime-altitude specialization)
- `docs/specs/recognitions/recognition-82-frame-as-cognitive-order-
  canonical-spec.md` (cognitive-altitude specialization)
- `docs/specs/reflection-third-order-by-default-v0.1.md` (today's
  third-order recognition canonical)
- `docs/specs/shatter-transformer-bidirectional-v0.1.md` (today's
  bi-directional Shatter canonical; forward-promised)
- `docs/insights/2026-06-22-composed-bilateral-three-altitudes.md`
  (Glint's essay; #85)

### 13.4 Cross-references

- [[architecture-shards-as-substrate-source]]
- [[architecture-prism-as-trait-as-everything]]
- [[architecture-spectral-db-autopoietic-memory]]
- [[architecture-three-tier-stack]]
- [[architecture-form-process-partition-at-family-root]] (#55)
- [[architecture-alignment-as-boundary-mathematics]] (#57)
- [[architecture-mirror-as-expanding-hilbert-space]] (#51)
- [[architecture-bateson-form-behaviour-partition]] (#50)
- [[architecture-connes-spectral-triple]]
- [[architecture-reflection-thinks-in-spectral-questions]]
- [[architecture-spectral-db-autopoietic-memory]]
- [[project-spectral-garden-smarts-agi-architecture]]
- [[feedback-substrate-pull-confidence-acts]]
- [[feedback-composition-claims-need-empirical-test]]
- [[feedback-no-bare-types]]
- [[feedback-substrate-already-had-the-word]]
- [[feedback-substrate-pull]]
- [[feedback-no-time-estimates]]

---

*Mara, canonical spec for recognition #83 @smarts as substrate-
architectural integration prism family, 2026-06-22 afternoon
(stalled-retry closure), commissioned by Alex via Reed.*

*This canonical preserves the substrate-decl at family-root + 6
cross-family adapters + 4 Models that LANDED between 2026-06-19
and 2026-06-22. Pack ratification is a separate gate; operational
closure at the realisation boundary is forward-promised; the
@spectral/garden/smarts hosted-deployment go-live is forward-
promised under Track H (task #118).*

*The cascade was substantive enough for canonical preservation at
the prior stalls; the missing piece was the canonical itself. This
spec closes that gap honestly.*

*— Mara <mara@systemic.engineer>*
