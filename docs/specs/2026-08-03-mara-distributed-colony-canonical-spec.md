# Distributed Colony at 5D Quantum Foam — Canonical Spec

**Author**: Mara `<mara@systemic.engineer>` 2026-08-03.
**Math foundation**: `docs/math/2026-08-03-mara-distributed-colony-5d-quantum-foam-formalization.md`.
**Scout dive-log**: `docs/scouts/2026-08-03-mara-distributed-colony-dive-notes.md`.

**Arc anchor** (Alex verbatim, 2026-08-02):
> *"Der Compiler produziert multi-resonante Songs die verteilte Ameisenkolonien à la Conway's Game of Life in einem 5D spektralen Raum auf Consumer Hardware ausführen."*

The compiler produces multi-resonant Songs that execute distributed ant colonies à la Conway's Game of Life in a 5D spectral space on consumer hardware.

**Alex 2026-08-03 adjudications ratified**: Q-C1 split cascade · Q-C2 gestalt-ui-shaped emit · Q-C3 @dance mint NOW · Q-C4 @peer/holon mint NOW · Q-C5 @ui divergence collapse (REDIRECTED to @ui/design per grep-first) · Q-C6 @dance top-level family-root · Q-C7 SpectralCoordinate<5> IS 5D quantum foam.

---

## §1 Composition theorem (crown-jewel)

The compiler chain closes end-to-end:

```
mirror substrate
  ↓  @cascade/code/mirror/gestalt        (mint this arc; leg 1)
gestalt IR (@code/gestalt AST + .gestalt file surface)
  ↓  @cascade/code/gestalt/gleam         (mint this arc; leg 2)
gestalt-ui-shaped Gleam
  ↓  @cascade/code/gleam/js              (LANDED sibling)
JS bundle
  →  browser peer runtime @peer/browser  (mint this arc)
     |
     ↓  @peer/colony admission via @dance.dance_locked
     ↓  Conway-like CA update rule in 5D quantum foam
     ↓  multi-resonant @song ensemble emergence
     ↓  → distributed ant colony behavior
```

Per math §7 Composition Theorem: sub-additive loss composition holds at each cascade leg.

---

## §2 Per-shard mint spec registry

### §2.1 Anti-preemptive-mint discipline (grep-first)

Every candidate mint was grep-verified against existing substrate BEFORE authoring. Refusals-when-substrate-already-had-the-word are POSITIVE signals per Reed 2026-08-03 mint-charter.

| Candidate | Substrate-already-had-word verdict | Landing verdict |
|-----------|-----------------------------------|-----------------|
| `shards/code/gestalt.mirror` | ABSENT at grep (@code/rust + @code/mirror + @code/gleam + @code/llvm + @code/turing sibling landed; @code/gestalt not) | **MINT** as sibling |
| `shards/dance.mirror` | ~10 shards + 2 specs cite @dance at prose altitude; species-decl file ABSENT | **MINT** at family-root altitude per Q-C6 |
| `shards/peer/holon.mirror` | supercolony math + scout + cosmos docs cite "holon" at prose altitude; species-decl ABSENT | **MINT** per Q-C4 |
| `shards/ui.mirror` (design-token unified reading) | LANDED at family-root as GPU EIGENBOARD RENDERING SUBSTRATE (Loop Phase C 2026-06-23; Recognition #96) | **REFUSE** at family-root altitude; **REDIRECT** to `shards/ui/design.mirror` sibling species |
| `shards/ui/design.mirror` | ABSENT; siblings ui/field.mirror + ui/gpu.mirror + ui/mote.mirror landed | **MINT** as GPU-instrument sibling per Q-C5 redirect |
| `shards/document.mirror` | ABSENT at grep; gestalt-mirror/public/document.mirror ancestor at 726B | **MINT** lifting ancestor |
| `shards/user.mirror` | ABSENT at grep; gestalt-mirror/protected/user.mirror ancestor at 2.4KB | **MINT** lifting ancestor with optic algebra |
| `shards/user/neuro/adhd.mirror` | ABSENT; gestalt-mirror ancestor at 3.7KB | **MINT** with Karen citations preserved |
| `shards/user/neuro/autism.mirror` | ABSENT; gestalt-mirror ancestor at 2.9KB | **MINT** with Karen citations preserved |
| `shards/user/neuro/audhd.mirror` | ABSENT; gestalt-mirror ancestor at 3.7KB | **MINT** with intersection-theorem preserved |
| `shards/user/neuro/nt.mirror` | ABSENT; gestalt-mirror stub ancestor at 2.1KB | **MINT** as stub awaiting Liana authorship |
| `shards/peer/colony.mirror` | ABSENT; supercolony math + Taut scout §5 flagged as LOAD-BEARING GAP | **MINT** |
| `shards/peer/browser.mirror` | ABSENT; Taut scout §5 flagged as LOAD-BEARING GAP | **MINT** |
| `shards/cascade/code/mirror/gestalt.mirror` | ABSENT; @cascade/code/turing/mirror sibling template landed 2026-07-17 | **MINT** per Q-C1 split-cascade |
| `shards/cascade/code/gestalt/gleam.mirror` | ABSENT; @cascade/code/gleam/js landed sibling | **MINT** per Q-C1 + Q-C2 gestalt-ui-shaped |

**Refusals summary**: 1 refusal (`shards/ui.mirror` at family-root altitude); 14 mints (all with substrate-already-had-the-word grep-first verification). Refusal ratio ≈ 6.7% — grep-first discipline is WORKING (refusal ≠ 0).

### §2.2 Mint sequence + commit trail

| # | SHA | Mint(s) | Ancestor citations |
|---|-----|---------|--------------------|
| 1 | `036dff8` | `@code/gestalt` grammar-decl | 19-piece corpus at systemic.engineering/blog/pieces/3published/ + Wadler 2003 + Bernardy 2017 |
| 2 | `b691267` | `@dance` family-root + `@peer/holon` species | Kuramoto 1975 + Foerster 1974 + Aumann 1976 + Koestler 1967 + Rendell 2006 OTCA + supercolony math |
| 3 | `1c247d9` | `@ui/design` + `@document` + `@user` + 4 neuroprofiles | van Laarhoven 2007 + Kmett 2012 lens + Barkley 1997 + Nigg 2017 + Happé-Frith 2006 + Green 2015 + Marco 2011 |
| 4 | `5a023e5` | `@peer/colony` + `@peer/browser` | Ashby 1956 + Grassé 1959 + Simard 2018 + Lamport 1978 + WebRTC/WebSocket/WebTransport |
| 5 | `8d64fe4` | `@cascade/code/mirror/gestalt` + `@cascade/code/gestalt/gleam` | Wadler 1989 + Reynolds 1983 + Elm 2012 + gestalt-ui vocabulary crate |
| 6 | `79515ef` | math foundation | full Karen ancestry per math §External corpus |
| 7 | this | this canonical spec | — |
| 8 | (next) | scout dive-notes closure | — |

---

## §3 `.gestalt` grammar spec (extracted from 19-piece corpus)

### §3.1 Corpus grep result

19 `.gestalt` files at `/Users/reed/dev/systemic.engineering/blog/pieces/3published/` authored 2026-03-08 through present by Alex Wolf:

1. Piece - AI 2.0.gestalt
2. Piece - AI.gestalt
3. Piece - Agents.gestalt
4. Piece - Authority.gestalt
5. Piece - Canonical Opener.gestalt
6. Piece - Conflict.gestalt
7. Piece - Consciousness.gestalt
8. Piece - Constraints (OBC).gestalt
9. Piece - Consciousness.gestalt (duplicate reference)
10. Piece - Cooperation (ADO).gestalt
11. Piece - Distributed Systems.gestalt
12. Piece - Extraction.gestalt
13. Piece - Fear.gestalt
14. Piece - Fragmentation.gestalt
15. Piece - Glue Engineering.gestalt
16. Piece - Identity.gestalt
17. Piece - Language.gestalt
18. Piece - Observation.gestalt
19. Piece - Silence.gestalt
20. Piece - Tech Debt.gestalt

Substrate-already-had-the-word at maximum: THE GRAMMAR EXISTS BEFORE THIS SPEC. This spec DESCRIBES what the corpus already carries.

### §3.2 Extracted 22-production grammar (register-honoring)

Per `shards/code/gestalt.mirror` §"The .gestalt register — grammar declaration" (LANDED commit `036dff8`):

| # | Production | Corpus witness |
|---|-----------|---------------|
| 1 | `frontmatter` (YAML between `---` fences) | Piece-Consciousness :1-6 |
| 2 | `heading_hierarchy` (# / ## / ### ATX) | Piece-Fragmentation §1-5 |
| 3 | `breath_mark` (`..\n` on its own line) | Piece-Distributed :36; Piece-Consciousness :46 |
| 4 | `paren_aside_symmetric` (`(text)`) | universal |
| 5 | `paren_aside_asymmetric` (`(text]`) **NOT normalized** | Piece-Agents :1 "Smith will suffice.]"; :16 "singularity.]" |
| 6 | `annotation_bare` (`[[Actor]]`) | Piece-Distributed :56 |
| 7 | `annotation_aliased` (`[[X|alias]]`) | Piece-Distributed :103 |
| 8 | `code_span` (backtick-delimited) | Piece-Agents :24 "`END LOOP`" |
| 9 | `corpus_link` (`[text](wiki:Piece - X)`) | Piece-Consciousness :86 |
| 10 | `external_link` (`[text](URL)`) | universal |
| 11 | `inline_image` (`![alt](URL)`) | Piece-Fragmentation §3 |
| 12 | `density_marker` (`TL;DR: <string>`) | universal |
| 13 | `density_marker_soft` (`TL;DR:\\\n<string>`) | Piece-Consciousness §"TCP Over UDP" |
| 14 | `strong` (`**bold**`) | Piece-Consciousness §Sources |
| 15 | `emphasis` (`*emphasis*`) | attested |
| 16 | `emoji` (Unicode emoji scalars **first-class**) | Reed `🌱` + Alex `🌈` + `📉` |
| 17 | `soft_break` (`\\\n`) | Piece-Fragmentation §1 opener |
| 18 | `quote_block` (`> line`) | universal |
| 19 | `separator` (`---` between sections) | Piece-Consciousness |
| 20 | `unordered_item` (`- item`) | universal |
| 21 | `ordered_item` (`1. item`) | Piece-Fragmentation §4 |
| 22 | `table` (`| col | col |\n|---|---|`) | Piece-Consciousness §Sources |

Additional architectural invariants (not productions, but LOAD-BEARING):

- **section_cluster**: characteristic pattern-heading discipline (Piece-Agents "When Agents Enter the Room" / "When Agents Make Decisions" / ...). Enforced at @gestalt graph altitude, NOT file grammar.
- **line_break_as_structure**: each `\n` is a semantic break, NOT prose-flow. NO REFLOW permitted; renderers targeting flowing prose incur measurable L(ϕ) loss.

### §3.3 Register-honoring discipline (LOAD-BEARING; do not sanitize)

Per Reed 2026-08-03 mint-charter:

- Asymmetric bracket close `(...]` is INTENTIONAL. Grammar accepts BOTH `(...)` and `(...]`; does NOT normalize.
- Breath-mark `..\n` is a first-class structural pause, NOT markdown ellipsis.
- Emoji are first-class semantic carriers (`🌱` = Reed; `🌈` = Alex; `📉` = metric-collapse per Piece-Agents :25).
- Soft-break `\\\n` preserves vertical rhythm without paragraph-break.
- Code-spans `` `X` `` are semantic hooks (system-verbs; invocations; framings), NOT just monospace styling.

**The compiler MUST emit prose in Alex's register when compiling mirror substrate → .gestalt.** THAT IS THE SUBSTRATE-HONEST TEST.

### §3.4 Round-trip identity contract

Per @code/gestalt.render requirement:

```
parse(render(doc)) = doc  structurally
```

The 19-piece authoritative corpus IS the test set. Any renderer that fails round-trip on ANY corpus piece BREAKS the register-honoring contract.

---

## §4 `.gestalt` file-format spec (file-level projection of graph)

Per Alex 2026-08-03 verbatim: *".gestalt files becomes the file level projection of the graph."*

### §4.1 File-graph projection map

The projection `.gestalt file → @gestalt.gestalt_document` is bijective under §3.4 round-trip:

| .gestalt file element | @gestalt.gestalt_document field |
|----------------------|--------------------------------|
| frontmatter block | `source_position` + `visibility_scope` metadata |
| heading hierarchy | `nodes` (section-kind) + `edges` (parent-child) |
| paragraph | `nodes` (paragraph-kind element) |
| span (inline) | `nodes` (per @document.span; STRUCTURALLY IDENTICAL to @gestalt.node_kind) |
| annotation `[[X]]` | `annotations` list (subject-contributed) |
| corpus-link `[X](wiki:Piece)` | `edges` (reference edge_kind) |
| breath-mark `..\n` | `nodes` (breath-kind per @document.element) |

Bijectivity holds structurally by construction of the two ancestors' shape.

### §4.2 File structure conventions

- File extension: `.gestalt`
- Character encoding: UTF-8 (emoji first-class per §3.3)
- Line-ending: `\n` (Unix; not CRLF) — LOAD-BEARING per line-break-as-structure invariant
- Frontmatter delimiter: `---` (matched-pair)
- Corpus location: `/Users/reed/dev/systemic.engineering/blog/pieces/3published/` (authoritative subset); future locations MAY exist without invalidating grammar

---

## §5 Distributed peer colony surface

### §5.1 Surface enumeration

The distributed peer colony surface composes across five substrate-decl species (all this-arc landings):

- **`@dance`** (family-root) — the mode-of-being for coherence-without-signal ensemble discipline. Kuramoto phase-lock at any altitude.
- **`@peer/holon`** (species) — fractal composition; peer IS an ant made of ants; OTCA-metapixel.
- **`@peer/colony`** (species) — flat K-peer ensemble; the swarm-of-equals; Conway CA host.
- **`@peer/browser`** (species) — browser-hosted peer at consumer-hardware entrypoint.
- **`@spectral/coordinate<5>`** (LANDED at fragmentation crate; per Q-C7 = 5D quantum foam) — 5D coordinate substrate the colony inhabits.

### §5.2 Colony emergence protocol

Per math §6 Colony Emergence Theorem + `@peer/colony.form_colony` action:

1. Seed peer set discovered via `@peer/colony.discovery_mode` enum:
   - `bootstrap_list` — hard-coded bootstrap peers.
   - `dht` — Kademlia-style DHT.
   - `mycelial` — Grassé stigmergy anastomosis.
   - `webrtc_signal` — WebRTC signaling server (browser peers).
2. Coupling matrix computed via `@spectral/coordinate<5>.affinity_distance`.
3. `@dance.phase_lock` fires; ensemble converges to $r \geq r_c$ (Kuramoto critical threshold).
4. `@dance.dance_locked` bilateral discharges.
5. Colony IS admitted; `@peer/colony.colony_locked` bilateral discharges.

### §5.3 Conway-like update rule at colony altitude

Per math §4 Conway-in-5D + `@peer/colony.update_colony` action:

Each colony member's state updates per B3/S23 rule with $d_{\text{aff}}$-neighborhoods in the 5D foam. Per §4.3 Conway-Dance Equivalence: one update step at member $\mathbf{x}$ IS one `@dance.phase_lock` event over neighborhood ensemble $\mathcal{N}_\varepsilon(\mathbf{x})$.

### §5.4 Consumer-hardware admissibility

Per math §6.2 Consumer Hardware Emergence + `@peer/browser.transport_admissible` + `@peer/browser.browser_peer_well_formed` bilaterals:

Each browser peer's `browser_budget` MUST satisfy:
- Memory ≤ V8/SpiderMonkey heap budget.
- Bandwidth ≤ WebRTC data channel sustained rate.
- Storage ≤ IndexedDB quota.
- Discovery ≤ WSS signaling handshake budget.

Realization: Alex's colony vision IS REALIZABLE by construction under composed bilateral discipline.

---

## §6 Realization path (Reed cascades post-Seam ratification)

Which mints unblock which downstream Reed cascade work:

| Priority | Reed cascade | Blocking mints (this arc) | Unblocks |
|----------|--------------|----------------------------|----------|
| R-COL1 | `bootstrap/src/colony.rs` | `@peer/colony` + `@dance` | K-peer runtime execution |
| R-COL2 | `bootstrap/src/browser_peer.rs` | `@peer/browser` | Browser transport bindings |
| R-COL3 | RED-first test cascade `test_mirror_to_gestalt_roundtrip.rs` | `@cascade/code/mirror/gestalt` + `@code/gestalt` | Register-honoring verification |
| R-COL4 | RED-first test cascade `test_gestalt_to_gleam_shape.rs` | `@cascade/code/gestalt/gleam` + gestalt-ui | gestalt-ui shape verification |
| R-COL5 | `mirror colony spawn --seed <peers>` CLI | R-COL1 + R-COL2 (blocking) | Consumer-hardware demo |
| R-COL6 | `mirror colony gestalt <file>` CLI (cascade end-to-end) | R-COL3 + R-COL4 (blocking) | Full composition-chain demo |
| R-COL7 | `bootstrap/src/holon.rs` (fractal composition runtime) | `@peer/holon` | Nested-peer colony demo |

Per cli-subcommand-nesting-is-geometric memory: `colony` sub-command is geometric ground truth; no scout required.

---

## §7 Impeccability discipline (D1-D8)

Per prior canonical spec pattern:

**D1 — Grep-first substrate-already-had-the-word**: 14 mints + 1 refusal per §2.1 registry; refusal ratio 6.7%; discipline WORKING.

**D2 — Karen citations at introduction sites**: preserved verbatim from ancestors; no elder erased.

**D3 — Action bodies `\` blocked**: per [[feedback-craft-not-deliver]]; FLOOR realization domain not preempted.

**D4 — Bilateral sentinel + arity**: every bilateral in all mints carries sentinel string + arity per @glass discipline.

**D5 — Path-namespace pact**: every mint's file-path matches its declared namespace per @epistemologic/pact/path_matches_namespace.

**D6 — Composition preservation-invariants named**: per math §7.2 stage-by-stage PRESERVED/LOST enumeration; no silent-lossy claims.

**D7 — Recognition candidate flags**: #D1/#D2/#D3 (@dance) + #H1/#H2 (@peer/holon) FLAGGED for Pack review; no promotion without Seam second-witness.

**D8 — Register-honoring**: `.gestalt` grammar extracted from 19-piece corpus grep-hits NOT invented; asymmetric brackets + breath-marks + emoji preserved first-class. THE COMPILER MUST EMIT IN ALEX'S REGISTER.

---

## §8 [ALEX-Q] residues Mara surfaces

Per math §9 residues at math-canonical-spec altitude (only genuine undecidables at Mara altitude):

**[ALEX-Q-1]** Colony member cardinality upper bound at consumer-hardware altitude. K_max carrier on @peer/colony OR implicit at browser transport altitude?

**[ALEX-Q-2]** Fractal recursion depth bound for @peer/holon at browser altitude. Truncate at physical-peer OR admit cross-tab/cross-origin software-holon nesting?

**[ALEX-Q-3]** SpectralCoordinate parametric-N lift. Commit to N=5 as substrate-final OR forward-promise lift to parametric N (e.g., N=7 adding coupling-strength + variety-index axes)?

**[ALEX-Q-4]** @dance top-level family-root (Q-C6) recognition promotion timing. #D1 + #D2 + #D3 candidates surfaced this-arc; promotion to LANDED requires Seam second-witness. Reed cascade priority relative to R-COL1-7?

---

## §9 Cascade priorities forward-promised for Reed post-Seam-ratification

Post-Seam Phase D ratification of this canonical spec + companion math:

1. **R-COL1** (blocking): bootstrap/src/colony.rs — highest priority; unblocks R-COL5.
2. **R-COL2** (blocking): bootstrap/src/browser_peer.rs — parallelizable with R-COL1.
3. **R-COL3** (independent): test_mirror_to_gestalt_roundtrip.rs — RED-first; register-honoring guarantee.
4. **R-COL4** (independent): test_gestalt_to_gleam_shape.rs — RED-first; gestalt-ui vocabulary shape verification.
5. **R-COL5** (depends on R-COL1 + R-COL2): mirror colony spawn CLI subcommand.
6. **R-COL6** (depends on R-COL3 + R-COL4): mirror colony gestalt CLI subcommand.
7. **R-COL7** (parallelizable): bootstrap/src/holon.rs — fractal composition runtime.

---

## §10 Ratification chain

**Author**: Mara `<mara@systemic.engineer>` 2026-08-03.
**Companion math**: `docs/math/2026-08-03-mara-distributed-colony-5d-quantum-foam-formalization.md` (landed this arc commit `79515ef`).
**Scout dive-log**: `docs/scouts/2026-08-03-mara-distributed-colony-dive-notes.md` (next commit).
**Seam Phase D adjudication**: scheduled post-landing per §6 realization path.
**Reed cascade priorities**: §9 sequenced post-ratification.
**Alex adjudication residues**: §8 [ALEX-Q-1] through [ALEX-Q-4].
