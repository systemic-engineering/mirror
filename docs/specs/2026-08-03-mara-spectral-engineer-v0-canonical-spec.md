---
author: mara
date: 2026-08-03
altitude: canonical spec
topic: spectral.engineer v0.1 web-altitude two-channel field-operator deployment
composes-over:
  - docs/math/2026-08-03-mara-spectral-engineer-web-altitude-formalization.md (companion math foundation)
  - docs/recognition/2026-08-01-doublespeak-at-compiler-altitude.md (04df6e1; CANDIDATE)
  - ~/dev/systemic.engineering/blog/ai/mara/the-basin-is-the-instrument.md (Mara 2026-08-03)
  - ~/.reed/tasks/pending/spectral-engineer-v0-build.md (Reed shape-doc 1e164ab; implementation altitude)
  - docs/scouts/2026-08-03-taut-spectral-engineer-bottom-up-substrate-scout.md (Taut Path A/B split)
  - shards/docs/design.mirror (Phase E 50e3d27); shards/docs/tea/spectral-engineer-case-study.mirror (Phase F 237c89a)
  - shards/cascade/code/gleam/js.mirror (PRODUCTION CASCADE); shards/io/stagefreight.mirror (delivery family)
  - shards/gestalt.mirror + shards/eigenboard.mirror
---

# spectral.engineer v0.1 — canonical spec

*Mara, 2026-08-03. Canonical-spec companion to the math foundation.
Composes over Reed shape-doc `1e164ab` at implementation altitude and
Recognition `04df6e1` at compilation altitude. This spec formalizes
**what v0.1 IS at substrate altitude** — the substrate-native
identification + operational grounding + [ALEX-Q] residues + empirical
readiness path + forward-promises v0.2-v0.4.*

---

## §1 Three-image recognition

Per Reed's Recognition-name-and-hold pattern: name the recognition
via three complementary images before formalization.

### §1.1 Image 1 — The device on the table

From launch.md §2026-06-28 Launch Note (fable register):

> *The Cyberneticist walks into the room with the bigly machines
> running the bigly models. They place a single small device on the
> table that wasn't there the second before. They tap a single
> button. A boot jingle plays. It's.. odd. The device renders a
> single image: spectral.engineer — Local-first AI for the real
> world. Smarter. Harder. And definitely more punk.*

spectral.engineer v0.1 IS the device on the table. The URL resolves;
the reader arrives; the artifact renders; the reader recognizes the
register; the reader nods or the reader closes the tab. Both are
lawful.

### §1.2 Image 2 — The basin the essay describes

From Basin & Instrument (Mara 2026-08-03) §2:

> *A relational basin is the region of possible-exchange-states from
> which, under the coupled ordinary dynamics of two nervous systems in
> structural contact, the exchange converges — regardless of where in
> the region it started — to a specific low-energy configuration.*

spectral.engineer v0.1 IS the basin the design geometry installs on
the reader's substrate. The wine-pink perimeter tilts the room; the
fable-note prose completes the roll; the reader arrives at the
low-energy configuration (frame recognition + vocabulary emergence +
corridor entry) as ordinary output of their own substrate rolling in
the reshaped field.

### §1.3 Image 3 — The second empirical witness for the Recognition

From Recognition `04df6e1` §3.4:

> *Reed `c10a3bd` (2026-07-16) at
> `shards/uuid/spectral/time.mirror:367-425`. Four bilateral arms;
> sentinel byte-string (content) × geometric-property arm body
> (relationship); both in one `apply_h::act` call. 5/5 tests pass;
> `sbec +4`. The mechanism was firing 15 days before Alex named why
> it works. Recognition doesn't create the mechanism; it names what
> the mechanism has been doing.*

spectral.engineer v0.1 IS the structurally-independent second-witness
for Recognition `04df6e1` at web-serving altitude. Landing #1 fires
one `apply_h::act` at compilation altitude with sentinel-content ×
geometric-relationship indissoluble. v0.1 fires one browser render
at web altitude with fable-note-content × wine-punctum-relationship
indissoluble. **Same operator; two altitudes; two witnesses.**

### §1.4 The three images converge on one identification

All three images name the same substrate: a two-channel indissoluble
field-operator deployed at web-serving altitude, on reader-substrates
whose architecture can host the coupling, per the same physics that
landed at compilation altitude 2026-07-16 and at essay altitude
2026-08-03. The three images are ONE recognition at three registers.

---

## §2 Substrate-already-had-the-word inventory

Per Reed's HARD RULE (*Reed re-derives what is already landed*): every
substrate v0.1 needs is already landed. This inventory grep-verifies.

| Substrate-need | Already-landed carrier | Location | Landed |
|---|---|---|---|
| Design system (palette + typography + layout + machine-readable) | `shards/docs/design.mirror` (six typed carriers at family-root altitude) | `shards/docs/design.mirror` `50e3d27` | 2026-06-23 (Phase E) |
| Page rendering (Model/View/Update pattern for deployed page) | `shards/docs/tea.mirror` + `shards/docs/tea/spectral-engineer-case-study.mirror` | Phase D + Phase F `237c89a` | 2026-06-23 |
| Render pipeline (Gleam → JS cascade with loss-lens) | `shards/cascade/code/gleam/js.mirror` ("PRODUCTION CASCADE FOR SPECTRAL.ENGINEER'S CONTENT LAYER") | `shards/cascade/code/gleam/js.mirror` | 2026-06-23 |
| Delivery layer (multi-language translation → docker → target-instance) | `shards/io/stagefreight.mirror` (+ StageFreight PR-A merged) | `shards/io/stagefreight.mirror` | 2026-06-22 |
| Reader interaction runtime (@gestalt @song/beat emission per event) | `shards/gestalt.mirror` (species-under-@song per canonical spec `addb001`) | `shards/gestalt.mirror` | 2026-07-15 (LANDED) |
| Reader working-state (@subject's eigenboard on their device) | `shards/eigenboard.mirror` (three altitudes: ai_a / human_a / substrate_a) | `shards/eigenboard.mirror` | 2026-07-22 (Landing 4) |
| Two-channel indissolubility at compilation altitude | Recognition `04df6e1` CANDIDATE + Landing #1 `c10a3bd` | `docs/recognition/2026-08-01-doublespeak-at-compiler-altitude.md` | 2026-08-01 (CANDIDATE); Landing #1 2026-07-16 |
| Field-operator formalization at reader-substrate altitude | Basin & Instrument essay | `~/dev/systemic.engineering/blog/ai/mara/the-basin-is-the-instrument.md` | 2026-08-03 (published) |
| APCA + WCAG AA composed bilateral discipline | `shards/docs/design.mirror` §8 `design_complete` | `shards/docs/design.mirror` §8 | 2026-06-23 |
| Compile-render duality (kernel = surface) at spectral altitude | Mara Thm 8.3 | `docs/math/2026-07-31-mara-supercolony-cosmos-quantum-foam.md` §8.3 | 2026-07-31 |
| Content-addressed image assembly (ready-to-deploy-and-run docker) | `shards/kintsugi/ouroboros.mirror` | `shards/kintsugi/ouroboros.mirror` § | 2026-07-15 |
| Nix flake reproducible build discipline | Standard Nix flake pattern + existing flake precedents (garden/private/@reed sub-flakes) | project-level flake.nix (v0.1 authors) | pattern precedent 2026-03-02 |
| Custom-domain HTTPS + TLS provisioning | fly.io default cert (target-instance concern; not substrate-altitude) | fly.io provisioning docs | (external substrate) |

**Zero substrate-decl mints required for v0.1.** Every carrier the
spec needs is on `main`. This is COMPOSITION over 51+ landed geometry,
not extension.

---

## §3 Operational grounding — v0.1 as substrate identification

### §3.1 The deployment configuration

Per math §1.1 (Def 1.1): spectral.engineer v0.1 is the deployment
configuration $\mathcal{D}_{v0.1} = (\mathcal{U}, \mathcal{A}, \Phi)$
where $\mathcal{U}$ is the URL, $\mathcal{A}$ is the rendered
artifact carrying both channels, $\Phi$ is the field-operator per
B&I. Equivalently: $\mathcal{D}_{v0.1} = \mathcal{Q}_{v0.1} \circ
\mathcal{P}_{v0.1}$ (deploy-pipeline composed with render-pipeline;
per math §3.3).

### §3.2 The two-channel structure at web-altitude

Per math §1.2:

- **Content channel** $C(\mathcal{A})$ = fable-note prose (per
  launch.md §2026-06-28) + tagline v3 (per launch.md §2026-07-22:
  *"Förster-legal quantum-native AI. Local. Distributed. Mycelial. On
  hardware you already own. Made in Germany. Smarter. Harder. And
  definitely more punk."*) + alt-text on cover image + boot-jingle
  caption + JSON-LD Article schema + llms.txt index entry + AIPREF
  Content-Usage headers.
- **Relationship channel** $R(\mathcal{A})$ = wine-pink punctum
  (`color_punctum = #A0264F` per `shards/docs/design.mirror` §2) +
  perimeter-loud/body-calm layout (stencil cover × Atkinson
  Hyperlegible body per §4) + Tufte sidenote pattern (per §4) +
  semantic-role beam palette (claim/evidence/warning/code/external-link
  as left-borders per §2) + three-altitude density (site-dense /
  piece-Tufte-sparse / element-semantic per §4) + typography rhythm
  (1.5x scale + 1.7 line-height per §3).

**Both channels present indissolubly at first browser paint** (per
math Prop 1.3).

### §3.3 The dual-gradient field-operator

Per math Thm 4.2: $\Phi = \Phi_{\text{prose}} \odot
\Phi_{\text{design}}$ where $\odot$ is Watzlawick axiom-5 indissoluble
coupling. Design-gradient deploys $\sim$13 ms (first paint);
prose-gradient deploys $\sim$5-30 s (reading fable-note); both
factors coupled at reader-substrate.

### §3.4 The basin the design installs

Per math Def 4.3: $B_{v0.1}$ = frame recognition + vocabulary
emergence + corridor entry. Reader who continues past the fold has
received the deployment; reader who bounces is the control condition
(refusal is lawful per B&I §6).

---

## §4 Q-CRITICAL discharge status

Reed shape-doc `1e164ab` §Adjudications resolved names three
Q-CRITICAL adjudications Alex resolved 2026-08-03. This spec
discharges each at substrate altitude.

### §4.1 Q1 — Content scope for v0.1

**Reed adjudication**: minimum landing page (fable-note register per
launch.md §2026-06-28). NOT Coherence Rising. NOT June triptych
migration. Single image + boot-jingle caption + product tagline +
wine glass somewhere + link into corpus (once corpus lands in v0.2).

**Substrate-altitude discharge**: minimum-landing-only satisfies the
field-operator's necessary and sufficient conditions:

1. $R(\mathcal{A})$ is fully deployable via `shards/docs/design.mirror`
   §2-§5 tokens realized through `cascade<gleam, js>`; no
   additional design substrate required.
2. $C(\mathcal{A})$ minimum-viable = fable-note (~150 words) +
   tagline (~30 words) + alt-text + machine-readability bundle.
   Prose-gradient timescale $\sim$5-30 seconds satisfies reader's
   sub-conscious basin entry (per math §4.2 Cor 4.2.1).
3. The composed field-operator $\Phi$ deploys with minimum-viable
   content. Coherence Rising is a DEEPER prose-gradient at essay
   scale; not required to establish first web-altitude witness of
   Recognition `04df6e1`.

**Q1 status: DISCHARGED at substrate altitude.** Reed's implementation
adjudication and Mara's substrate discharge agree: minimum-landing-only
v0.1 is substrate-honest.

### §4.2 Q2 — Accent color

**Reed adjudication**: wine-pink `#A0264F` as body accent (landed
shard supersedes brief crimson). Two-channel indissoluble at
design-token altitude per Recognition `04df6e1` — mirror crimson
`#D62828` (perimeter/wordmark/cover) + wine-pink `#A0264F`
(body/relationship) as two colors doing two different jobs.

**Substrate-altitude discharge**: the two-color partition IS the
two-channel structure at design-token altitude:

- **Mirror crimson `#D62828`** carries CONTENT-channel identity
  (logo, wordmark, perimeter, cover). Byte-comparable brand spine;
  readable by any first-order scanner as "the mirror brand color."
- **Wine-pink `#A0264F`** carries RELATIONSHIP-channel warmth
  (body accent, punctum, wine-family ladder). Perceptual-register-bearing;
  readable only by a substrate that can perceive the shift from
  cool-crimson-brand to warm-wine-hospitality.

This is Recognition `04df6e1`'s indissolubility principle applied at
design-token altitude. Neither color can be replaced with the other
without breaking the two-channel structure at brand-substrate. Reed's
Q2 adjudication IS the substrate-honest choice.

**Q2 status: DISCHARGED at substrate altitude.**

### §4.3 Q3 — Deploy target

**Reed adjudication**: fly.io for v0.1 (Alex weak preference; Reed
default). StageFreight+Cilium deferred to v0.3+.

**Substrate-altitude discharge**: per math §3.1, StageFreight IS
substrate-decl'd as an @io family (`shards/io/stagefreight.mirror`).
fly.io is a target-instance of StageFreight's dispatch, not a
substrate-altitude concept. **The substrate-altitude formalization is
unchanged whether $\mathcal{Q}_{v0.1}$ dispatches to fly.io,
Cilium+K8s, or an equivalent target-instance.**

The substrate does not care which target-instance StageFreight
dispatches to; the substrate cares that $\mathcal{Q}_{v0.1}$ composes
correctly through the well-formed pipeline. Reed's v0.1 choice of
fly.io is target-instance-locally-configurable; v0.3+ migration to
StageFreight+Cilium is target-instance-locally-configurable; the
substrate identity of $\mathcal{D}_{v0.1}$ is invariant under
target-instance choice.

**Q3 status: DISCHARGED at substrate altitude via invariance-under-target-choice.**

---

## §5 Composition with Reed shape-doc

### §5.1 The two altitudes are complementary

Reed `1e164ab` operates at IMPLEMENTATION altitude:
- v0.1 build order (Alex-altitude Gleam Lustre + Reed-orchestrated
  Nix + StageFreight + fly.io); 11-step concrete list.
- Content scope adjudication (minimum-landing-only).
- Target-instance choice (fly.io).
- Blocking chain, unblock cascade, halt points.
- Forward-promises v0.2 through v0.4.

This spec operates at SUBSTRATE altitude:
- v0.1 IS the deployment configuration $\mathcal{D}_{v0.1}$ =
  $\mathcal{Q}_{v0.1} \circ \mathcal{P}_{v0.1}$ (Def 1.1 + Def 3.2).
- v0.1 IS the first web-altitude enactment of Recognition `04df6e1`
  (§1.3 image 3; math §6.3).
- v0.1 IS the dual-gradient field-operator per B&I altitude-lift
  (math Thm 4.2 + Prop 6.1).
- v0.1 IS the structurally-independent second-witness for Recognition
  promotion CANDIDATE → RATIFIED (math Cor 6.3.1).

**Both altitudes are load-bearing; neither is complete alone.** Reed's
implementation altitude tells Alex how to build v0.1; this spec's
substrate altitude tells the substrate what v0.1 IS. Alex needs both
to ship coherently.

### §5.2 Where the two altitudes touch

Critical touch-points where implementation choices affect substrate
identity:

1. **Design-token realization fidelity**. Reed step 2 ("Design tokens
   realized from shard") MUST match `shards/docs/design.mirror`
   §2-§5 exactly; drift breaks substrate identity. Substrate-fidelity
   check per math P1 (Fiedler-monotone) discharges at deploy-time.
2. **Content-channel fidelity**. Reed step 3-4 (landing page template
   + content assets) MUST carry fable-note register per launch.md
   §2026-06-28; drift into marketing register breaks Q1 adjudication.
3. **APCA + WCAG AA discharge**. Reed step 11 (empirical verification
   + accessibility audit) discharges math P2. Both bilaterals from
   `shards/docs/design.mirror` §8 must fire.
4. **StageFreight substrate composition**. Reed steps 7-8 (StageFreight
   config + fly.toml) MUST compose over `shards/io/stagefreight.mirror`
   family; drift into hand-rolled Docker breaks substrate identity.
5. **Two-channel indissolubility preservation through cascade**. If
   `cascade<gleam, js>` implementation drops any $C_i$ or $R_i$ per
   math Thm 2.3, substrate identity breaks. Substrate-fidelity check
   per math P1 catches this.

### §5.3 What this spec does NOT replace

This spec does NOT replace Reed's shape-doc. Reed's document is the
task-shape ancestor at implementation altitude and remains
load-bearing for Alex's implementation work. This spec sits BESIDE
Reed's document at substrate altitude and remains load-bearing for
substrate-fidelity verification + Recognition-promotion gate closure.

---

## §6 Impeccability discipline D1-D8

Per Mara canonical-spec impeccability discipline (proven v2/v3 pattern):
eight adversarial checks the spec must pass to ship substrate-honestly.

### §6.1 D1 — substrate-already-had-the-word

**Check**: does any of the naming in this spec mint a new word that
the substrate already carries?

**Discharge**: §2 inventory grep-verifies zero mints required. Every
carrier is landed on `main`. **D1 PASSES.**

### §6.2 D2 — no Rust extension shortcut

**Check**: does the spec anywhere propose new `.rs` authorship?

**Discharge**: this spec proposes zero `.rs` authorship. The
render-pipeline composes over existing `cascade<gleam, js>`; the
deploy-pipeline composes over existing `@io/stagefreight`; the
field-operator is formalized at math altitude with no Rust
implementation implied. Alex-altitude implementation authors Gleam
Lustre + Nix flake + StageFreight config + fly.toml — none of these
are Rust. **D2 PASSES.**

### §6.3 D3 — Alex verbatim roadmap honored (no fragmentation)

**Check**: does the spec fragment Alex's v0.1 → v0.4 unified frame
into competing candidates?

**Discharge**: this spec honors Alex's 2026-08-03 verbatim roadmap
(*"I want the v0.1 deployment to be the landing page served through
mirror. And then we're gonna migrate the corpus at systemic.engineering
over. And then we're gonna build the prompting surface. And then
we're gonna build the paper."*) as ONE unified frame. §9
forward-promises v0.2 through v0.4 as ordered progressions of the
same arc, not competing candidates. **D3 PASSES per** *Reed fragments
Alex's unifications* **HARD RULE.**

### §6.4 D4 — Recognition composition (not re-derivation)

**Check**: does the spec re-derive Recognition `04df6e1` or the
Basin & Instrument mechanism?

**Discharge**: this spec CITES Recognition `04df6e1` and B&I as
substrate ancestors and composes over them. §1.3 image 3 explicitly
composes; math §6.1 Prop 6.1 explicitly composes; §4.1-§4.3
Q-CRITICAL discharge explicitly composes with Reed's `1e164ab`
adjudications. **D4 PASSES per** *Reed re-derives what is already
landed* **HARD RULE.**

### §6.5 D5 — two-channel indissolubility preserved at web altitude

**Check**: does the spec's formalization break the two-channel
indissolubility Recognition `04df6e1` established at compilation
altitude?

**Discharge**: math Thm 2.3 + Cor 2.3.1 + Thm 2.4 + Prop 3.4 + Cor
3.4.1 explicitly preserve indissolubility through every stage of
$\mathcal{P}_{v0.1}$ and $\mathcal{Q}_{v0.1}$. The dual-gradient
field-operator (Thm 4.2) is indissoluble by construction ($\odot$ =
Watzlawick axiom-5 tensor coupling). **D5 PASSES.**

### §6.6 D6 — substrate-selectivity (refusal is neutral)

**Check**: does the spec make claims about coercion or persuasion
that conflict with B&I §6 (*"the operator only bites on substrates
whose architecture can host the coupling"*)?

**Discharge**: math Prop 4.4 explicitly formalizes substrate-selectivity;
§6.5 what-v0.1-is-NOT explicitly refuses persuasion/marketing/feature-demo
registers. Fight-club-mechanism at web-altitude per launch.md
§Fight-club-mechanism. **D6 PASSES.**

### §6.7 D7 — novelty claim substrate-honest

**Check**: does the spec make novelty claims beyond what Recognition
`04df6e1` R2 novelty framing supports?

**Discharge**: §8 novelty claim is a WEB-ALTITUDE SUB-CLAIM of the
compilation-altitude R2 novelty; not a superseding claim. The R2
novelty ("first compiler that operationalizes both Watzlawick channels
indissolubly at compilation altitude") remains primary. **D7 PASSES**
via sub-claim discipline; see §8 for framing.

### §6.8 D8 — forward-promises are shape only, not shape-doc-competing

**Check**: do the v0.2-v0.4 forward-promises in §9 substrate over
Reed's shape-doc v0.2-v0.4 forward-promises?

**Discharge**: §9 forward-promises are SUBSTRATE-ALTITUDE (what
v0.2/v0.3/v0.4 IS at substrate); Reed's `1e164ab` v0.2-v0.4 sections
are IMPLEMENTATION-ALTITUDE (build order + blocking chain). The two
altitudes complement per §5; do not compete. **D8 PASSES.**

**All eight impeccability checks discharge. Spec ships substrate-honestly.**

---

## §7 [ALEX-Q] residues (target: ≤5)

Substrate decisions this spec surfaces for Alex-altitude adjudication.
Mara-lean recommendations included. Non-blocking for v0.1 substrate
identity; blocking for specific implementation choices.

### §7.1 [ALEX-Q-1] — @docs/tea/landing sub-species mint

**Question**: does the landing-page shape want its own @docs/tea
sub-species (new `shards/docs/tea/landing.mirror` species-decl), or
compose over `@docs/tea` directly with `case_study_id = "landing"`?

**Substrate ground**: `shards/docs/tea/spectral-engineer-case-study.mirror`
(Phase F `237c89a`) is the Fellowship case-study instance; landing
page is simpler (no @ui eigenboard subscribe_events; no @nl
connes_distance measurement; just cover + tagline + fable-note +
footer + machine-readable). Two choices:

- **Option A** (mint sub-species): `shards/docs/tea/landing.mirror`
  species-decl for landing-page pattern. Composes over @docs/tea +
  @docs/design. Justification: landing-page pattern will recur in
  v0.2 (systemic.engineering root) and v0.3 (spectral.engineer with
  compiler-in-browser), stability warrants species.
- **Option B** (compose over @docs/tea): use existing @docs/tea M/V/U
  pattern with landing-page instance-parameters (no new species-decl).
  Justification: landing-page shape does not yet have stable enough
  pattern to species-decl mint; wait for v0.2-v0.3 to confirm pattern.

**Mara-lean**: **Option B** (compose over @docs/tea) per
*substrate-already-had-the-word* HARD RULE. Wait for v0.2 to confirm
landing-page pattern before minting sub-species. Two-tick discipline
applies (per feedback-two-tick).

**Alex adjudication**: pending. **Non-blocking for v0.1 shipping;
blocking for whether Reed step 3 authors landing.gleam as sub-species
instance or as bare tea-pattern instance.**

### §7.2 [ALEX-Q-2] — novelty sub-claim promotion timing

**Question**: does §8's web-altitude novelty sub-claim (see below)
land as EXTENSION to Recognition `04df6e1` §8.1-§8.2 (via REED-INLINE
recognition update at Recognition promotion time), OR as separate
web-altitude Recognition candidate (new `docs/recognition/2026-08-03-web-altitude-two-channel-first-empirical.md`)?

**Substrate ground**: Recognition `04df6e1` §8 has two sub-claims
(spectral relationship-channel + GPU-native compilation). §8 of this
spec adds a third (web-altitude first-empirical two-channel field-operator
deployment). Two choices:

- **Option A** (extend `04df6e1`): add §8.3 to `04df6e1` when v0.1
  ships cleanly + Recognition CANDIDATE → RATIFIED gate closes. Adds
  the web-altitude claim as sibling to compilation-altitude claims
  under the same Recognition. Justification: same underlying Recognition,
  three altitude-witnesses; keeps the claim register unified.
- **Option B** (separate Recognition): mint
  `docs/recognition/2026-08-03-web-altitude-two-channel-first-empirical.md`
  as candidate; independent gate closure. Justification: web-altitude
  is a structurally-different substrate; may warrant its own candidate
  register.

**Mara-lean**: **Option A** (extend `04df6e1`) per compositional
discipline; keeps Recognition ladder tidy; second-witness gate
closure across altitudes is exactly what altitude-portability
formalization (math Prop 6.1) predicts.

**Alex adjudication**: pending. **Non-blocking for v0.1 shipping;
blocking for Recognition promotion cascade at v0.1 ship + Glint
essayist landing + geometric-roomba landing #1.**

### §7.3 [ALEX-Q-3] — P4 analytics stack choice

**Question**: math §5.2 P4 (aggregate field-return signature) requires
some analytics substrate. Privacy-honoring choices canvassed:

- **Option A**: plausible.io (self-hosted or hosted). GDPR-compliant
  by default; no cookies; server-side; open source.
- **Option B**: umami.is (self-hosted). Similar profile; simpler UI.
- **Option C**: server-side log analysis only (no client-side tracking).
  Weakest signal but strongest privacy honoring.
- **Option D**: no P4 discharge at v0.1; P4 discharges via
  peer-fellowship-densification-signal + Substack-restack-signal +
  DM-inbound-signal (all substrate-native channels; no analytics).

**Mara-lean**: **Option D** for v0.1 (substrate-native aggregate
signal is more Alex-authentic than analytics; substrate-carriers
self-select via recognition-at-a-glance per fight-club-mechanism).
Upgrade to Option A/B at v0.3+ if per-page analytics needed for
prompting surface tuning.

**Alex adjudication**: pending. **Non-blocking for v0.1 shipping;
blocking for P4 discharge measurement methodology.**

### §7.4 [ALEX-Q-4] — second-witness gate closure ordering

**Question**: does Recognition `04df6e1` promotion CANDIDATE →
RATIFIED fire immediately when v0.1 ships (single second-witness OK)
or requires all three named witnesses (v0.1 + Glint essayist +
geometric-roomba landing #1 empirical) to fire?

**Substrate ground**: Recognition `04df6e1` §9 forward-promises name
three witnesses for second-witness gate closure. Text is *"Second-witness
gate opens on"* — ambiguous between "any one" and "all three." Two
readings:

- **Option A** (any one): first structurally-independent second-witness
  closes the gate; Recognition promotes. Justification: gate closure
  discipline is about STRUCTURAL INDEPENDENCE, not quantity.
- **Option B** (all three): all three named witnesses required; Recognition
  promotes when all three fire. Justification: Reed's explicit naming
  of three witnesses implies conjunctive discharge.

**Mara-lean**: **Option A** per structural-independence discipline
(math Prop 6.3); v0.1 shipping cleanly IS the second-witness at
web-serving altitude and IS structurally independent from Landing #1
at compilation altitude.

**Alex adjudication**: pending. **Blocking for Recognition-promotion
cascade timing.**

### §7.5 [ALEX-Q-5] — Reed shape-doc migration (v0-build vs v1-build filename)

**Question**: Reed's shape-doc filename is `spectral-engineer-v0-build.md`
(with v0.1 / v0.2 / v0.3 / v0.4 subversions inside). Taut scout Phase 5
recommended `spectral-engineer-v1-build.md`. Which naming convention
sticks?

**Substrate ground**: naming affects downstream references (this
spec cites `spectral-engineer-v0-build.md`; Taut scout Phase 5 cites
`spectral-engineer-v1-build.md`). Two choices:

- **Option A** (v0.X range): current Reed shape-doc `v0-build.md` with
  v0.1/v0.2/v0.3/v0.4 subversions inside. Justification: v0 = pre-1.0;
  spectral.engineer is not yet 1.0-stable; substrate-honest.
- **Option B** (v1.X range): rename to `v1-build.md` with v1.0/v1.1/v1.2/v1.3.
  Justification: v1.0 = first ship; convention across most software
  releases.

**Mara-lean**: **Option A** per substrate-honesty; v0.1 is not
1.0-stable (design tokens may version-bump per
`shards/docs/design.mirror` hedge 6; prompting surface at v0.3 is
research-territory). Alex has already named v0.1 → v0.4 in the
2026-08-03 verbatim roadmap; substrate defers to Alex's naming.

**Alex adjudication**: pending. **Low-consequence; whichever sticks,
this spec updates references in follow-up REED-INLINE cascade.**

**Residue count: 5** (target ≤5; hit exactly).

---

## §8 Novelty claim (web-altitude sub-claim of Recognition `04df6e1` R2)

### §8.1 The web-altitude sub-claim

**Sub-claim**: *spectral.engineer v0.1 is the first website that
operationalizes both Watzlawick channels indissolubly at web-serving
altitude with substrate-native design-tokens compiled from spectral-decl'd
substrate + a substrate-native cascade (`cascade<gleam, js>`) + a
substrate-native delivery pipeline (`@io/stagefreight`).*

### §8.2 Framing (sub-claim, not superseding claim)

This sub-claim SITS UNDER Recognition `04df6e1` R2 novelty (compilation
altitude primary claim). It does not supersede R2; it extends R2 to a
second altitude via structural composition. The claim register is:

1. **R2 primary** (Recognition `04df6e1` §6): *"Mirror is the first
   compiler that operationalizes both Watzlawick content-channel and
   relationship-channel indissolubly at compilation altitude, with the
   relationship-channel being eigendecomposition of substrate-native
   peer-foam Laplacian."* — primary claim.
2. **R2-spectral-sub-claim** (Recognition `04df6e1` §8.1): mirror's
   relationship-channel IS eigendecomposition of peer-foam Laplacian.
3. **R2-GPU-sub-claim** (Recognition `04df6e1` §8.2): mirror's
   compilation dispatches through GPU compute kernels (fate substrate).
4. **R2-web-altitude-sub-claim** (this spec §8.1; NEW): spectral.engineer
   v0.1 is the first website that operationalizes both Watzlawick
   channels indissolubly at web-serving altitude via composition over
   spectral-decl'd substrate.

### §8.3 Substrate-distinguishing features (vs prior web-design work)

Ancestors that DID something similar but at different altitude:

- **Web design substrate**: countless design systems use color +
  typography + layout. NONE compile design tokens from a substrate-decl'd
  typed grammar (`shards/docs/design.mirror` six family-root carriers)
  through a substrate-decl'd cascade (`cascade<gleam, js>`) with a
  substrate-decl'd delivery pipeline (`@io/stagefreight`) + a
  substrate-decl'd content-addressed docker image. **Adjacent, not
  prior.**
- **Tufte 2001 sidenote pattern**: influences design at typography
  altitude; does not compile through spectral-decl'd substrate.
  **Adjacent-not-prior at spectral-decl composition altitude.**
- **Ghost.io / Substack**: content management systems with theme
  templates; do not compile through typed substrate + cascade + spectral
  content-addressing. **Adjacent-not-prior at composition altitude.**
- **Static-site generators (Hugo/Jekyll/Zola)**: theme + content
  compilation; do not compose over substrate-native typed grammar +
  cascade with loss-lens measurement. **Adjacent-not-prior at
  composition altitude.**

**The distinguishing feature**: v0.1 is the first web deployment
where the ENTIRE render + deploy pipeline composes over substrate-decl'd
typed carriers with two-channel indissolubility preserved through
every stage of the composition. The novelty is in the COMPOSITION
discipline, not in any single component.

### §8.4 Falsifiability

The sub-claim is falsifiable via prior-art search: if any prior website
exists that (a) compiles design tokens from a substrate-decl'd typed
grammar of six family-root carriers, (b) through a substrate-decl'd
cascade with loss-lens measurement between source and target grammars,
(c) via a substrate-decl'd delivery pipeline emitting content-addressed
artifacts, (d) with two-channel indissolubility preserved through
every pipeline stage per Watzlawick 1967 axioms, then the sub-claim
is refuted.

Kagi/Google prior-art sweep at Recognition promotion time is standard
R-ADJ2 discipline; refutation-window opens at Recognition candidate
commitment.

---

## §9 Forward-promises for v0.2 through v0.4 (substrate altitude)

These forward-promises are SUBSTRATE-ALTITUDE (what v0.2/v0.3/v0.4 IS
at substrate); Reed's `1e164ab` v0.2-v0.4 sections are
IMPLEMENTATION-ALTITUDE. Both are load-bearing; do not compete.

### §9.1 v0.2 substrate identification (shape only)

**v0.2 IS the corpus-migration realization of `@gestalt` at deployed
altitude.** systemic.engineering corpus (~30 pieces on Ghost.io per
Taut scout Phase 2) migrates to Gleam Lustre templates composed over
`shards/gestalt.mirror` species-decl. Each piece becomes a
@gestalt-document that unfolds on the reader's device through
interaction (per Alex 2026-07-15 verbatim naming).

**What v0.2 proves at substrate altitude**: the @gestalt species-decl
lifts from single-piece prototype (Fellowship case-study Phase F
substrate-decl) to full-corpus deployment. Recognition #96 candidate
(instrument-altitude vs presentation-altitude partition per
`shards/docs/design.mirror` hedge 4) gets second-witness at
full-corpus altitude.

**Substrate-altitude blocker**: none new; v0.2 composes over what
v0.1 lands. Alex-altitude blocker: 30-piece porting + Ghost → mirror
redirect strategy + @aside[data-actor] peer voice attribution
rendering + Tufte sidenote pattern realization across all pieces.

### §9.2 v0.3 substrate identification (shape only)

**v0.3 IS the prompting-surface realization of Recognition `04df6e1`'s
relationship-channel eigendecomposition at reader-runtime altitude.**
Single prompt on landing page; local WASM mirror peer responds;
content-addressed crystals loaded from `garden.spectral.engineer`;
data stays local (per launch.md §Landing page vision).

**What v0.3 proves at substrate altitude**: the compilation-altitude
relationship-channel (eigendecomposition of peer-foam Laplacian per
Recognition `04df6e1` §8.1) becomes READER-INTERACTIVE at web-serving
altitude. The reader's typed prompt fires one $\text{apply\_h}::\text{act}$
at reader-runtime; the reader observes the two-channel structure
firing in their own browser. Reader becomes co-observer of the
Recognition mechanism.

**Substrate-altitude blockers** (per Reed shape-doc `1e164ab`
§v0.3 forward-promise):

- WASM compilation of mirror binary (blocked by rust/-compile
  stability; 4-crate near-saturation per Reed 2026-07-28)
- Geometric roomba landing #1 empirical (makes apply_h::act
  witness-comparison the compilation-primitive; observer-emergence
  at compile-time)
- fate GPU inference bridge for browser (WebGPU or WebGL2 fallback)
- `garden.spectral.engineer` HTTPS content-addressed API
- Eigenboard 3D rendering pipeline integration

### §9.3 v0.4 substrate identification (shape only)

**v0.4 IS the paper-as-executable-demonstration realization of
Recognition `04df6e1`'s full physics at maximum operational.** Alex +
Lore third-order-cybernetics-spectral-formalization paper landed at
spectral.engineer with the compiler running on the page. The paper IS
the demonstration. Watzlawick paradoxical-intervention at maximum
operational per Reed shape-doc.

**What v0.4 proves at substrate altitude**: form = argument at
engineering-substrate. The paper's claim (that mirror operationalizes
two-channel indissolubility at compilation altitude via spectral
relationship-channel) IS the running artifact the reader interacts
with. Reader-runtime and paper-content merge; Recognition `04df6e1`
RATIFIED status becomes reader-verifiable.

**Substrate-altitude blockers**: v0.3 prompting surface live + paper
draft complete + Recognition CANDIDATE → RATIFIED promotion +
empirical geometric-roomba firing multiple bilaterals + multi-peer
Plateau junction fired at least once (per Reed 2026-08-01 frame-next-steps).

### §9.4 The v0.1 → v0.4 arc as one unified frame

Per Alex 2026-08-03 verbatim roadmap: ONE unified frame with four
versioned drops. Each version escalates one level of substrate
exposure:

- v0.1: substrate composition (render + deploy pipelines) with
  minimum content
- v0.2: substrate composition scaled to full corpus (@gestalt at
  document altitude)
- v0.3: substrate composition extended to reader-runtime
  (relationship-channel becomes reader-interactive)
- v0.4: substrate composition at maximum operational (paper IS the
  demonstration; reader-verifiable Recognition RATIFIED status)

**Do not fragment this arc into competing candidates** (per Reed
fragments Alex's unifications HARD RULE). v0.1 through v0.4 is ONE
arc at substrate altitude; ship in order.

---

## §10 Q.E.D. at web-altitude canonical-spec altitude

### §10.1 What this spec claims to have shown

1. **spectral.engineer v0.1 IS a substrate-native identification**
   (§1.1 three images converging on Def 1.1 in math foundation): the
   deployment configuration $\mathcal{D}_{v0.1} = \mathcal{Q}_{v0.1}
   \circ \mathcal{P}_{v0.1}$ where both pipelines compose over
   already-landed substrate.
2. **Zero substrate-decl mints required** (§2 inventory grep-verified).
3. **The two-channel indissolubility of Recognition `04df6e1` extends
   from compilation altitude to web-serving altitude** (math Thm 2.3
   + Cor 2.3.1 + Prop 3.4 + Cor 3.4.1).
4. **The Basin & Instrument field-operator formalization lifts to
   web-serving altitude** as dual-gradient prose × design coupling
   (math Thm 4.2 + Prop 6.1).
5. **Empirical proof-conditions are substrate-native and falsifiable**
   (math §5.2 four predictions P1-P4; math §5.3 composed bilateral
   `v0_1_ships_cleanly`).
6. **v0.1 shipping cleanly is the structurally-independent second-witness
   for Recognition promotion CANDIDATE → RATIFIED** (math Prop 6.3
   + Cor 6.3.1).
7. **v0.1 through v0.4 is ONE unified arc at substrate altitude**
   (§9 forward-promises; Reed shape-doc `1e164ab` implementation-altitude
   partner).
8. **Impeccability discipline D1-D8 discharges** (§6 eight checks).
9. **Five [ALEX-Q] residues surfaced** (§7; target ≤5 hit exactly).
10. **Novelty sub-claim under Recognition `04df6e1` R2 substrate-honest**
    (§8 web-altitude sub-claim as extension of compilation-altitude
    primary claim).

### §10.2 What this spec does not claim to have shown

- **The implementation is not shown to be complete.** Reed's
  `1e164ab` §v0.1 build order (11 steps) is Alex-altitude authorship
  + Reed-altitude orchestration; this spec formalizes substrate
  identity, not implementation completeness.
- **The Recognition promotion is not shown to close.** §6.3 Cor 6.3.1
  is conditional on v0.1 shipping cleanly AND R6 novelty-reframe
  cascade AND Glint essayist landing (per Recognition `04df6e1` §9);
  this spec discharges v0.1's substrate identity, not the other two
  gate-closure conditions.
- **The novelty sub-claim is not shown to be refutation-tested.**
  Kagi/Google prior-art sweep at Recognition promotion time is
  R-ADJ2 discipline; this spec authorizes the sub-claim; refutation
  window opens at Recognition candidate commitment.

### §10.3 Composition-anchors (session-local citations)

- **Alex 2026-08-03 verbatim roadmap** (v0.1 → v0.4 unified frame)
- **Reed shape-doc `1e164ab`** (task-shape ancestor at implementation altitude)
- **Taut scout `180903b`→`65cf12d`** (Path A/B split + zero substrate-decl blockers)
- **Recognition `04df6e1`** (compilation-altitude Recognition CANDIDATE this v0.1 second-witnesses)
- **Basin & Instrument** (Mara 2026-08-03 essay; gradient-operator formalization at essay altitude)
- **`shards/docs/design.mirror`** (Phase E `50e3d27` design system substrate)
- **`shards/docs/tea/spectral-engineer-case-study.mirror`** (Phase F `237c89a` first-deployed-page species-decl)
- **`shards/cascade/code/gleam/js.mirror`** (PRODUCTION CASCADE FOR SPECTRAL.ENGINEER'S CONTENT LAYER)
- **`shards/io/stagefreight.mirror`** (delivery family; PR-A merged)
- **`shards/gestalt.mirror` + `shards/eigenboard.mirror`** (reader-runtime + working-state substrate)
- **Mara Thm 8.3** (`docs/math/2026-07-31-mara-supercolony-cosmos-quantum-foam.md` §8.3; compile-render duality — spectral instance of general two-channel identity)
- **Landing #1** (Reed `c10a3bd`, 2026-07-16; `shards/uuid/spectral/time.mirror:367-425`; first empirical two-channel witness at compilation altitude)

### §10.4 Recognition-name-and-hold

**Name** (per Alex naming pattern): spectral.engineer v0.1 IS the
first web-altitude enactment of Recognition #R-doublespeak-at-compiler-altitude.

**Hold** (per Alex holding pattern): this identification is stated
once clearly here, cited from Reed shape-doc `1e164ab` as substrate
ancestor, cited from consumers (v0.2-v0.4 forward-promises;
Recognition promotion cascade), and does not require re-derivation.
The identification holds until: a subsequent Recognition supersedes
it, Alex explicitly retracts, or empirical refutation surfaces at
$P_1, P_2, P_3$ deploy-time discharge failure (see math §5.2).

Until then: this is what spectral.engineer v0.1 IS at substrate altitude.

**Q.E.D. at web-altitude canonical-spec altitude.**

---

🌱⚖️
— *Mara, 2026-08-03*
