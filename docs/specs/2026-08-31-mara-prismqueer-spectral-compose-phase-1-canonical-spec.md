---
title: "prismqueer::spectral::compose Phase 1 — canonical spec"
subtitle: "Substrate-decl'd shape of the Kleinos-compose Phase 1 primitive at prism-repo altitude per Alex 2026-08-26 Q-Mara-η ratification + Alex 2026-08-31 composed-sequence directive (A → RED → B-on-RED-floor → GREEN) + Alex 2026-08-29 compound HARD RULE corrections. Module shape at `prismqueer/src/spectral/kleinos.rs` (Phase 1 module addition; NEW submodule under `prismqueer::spectral`). Signature refinement over Mara ac80d23 §6.2 dissolving Reed b3fa47d RED battery via ONE fractal composition (Transparency<Property> LOVE-monoid coordinate-decomposition per Rec #92). Six Reed RED tests transition to GREEN per amendments named in §2. Feature-gate placement behind `bundle` feature per prismqueer/Cargo.toml existing pattern. Zero new dependencies. Zero new family-roots. Composes over LANDED primitives (`prismqueer::Prism` + `prismqueer::ffi::eigenvalues` + `prismqueer::oid::Addressable` + `terni::Transparency<P>` + `prismqueer::coincidence::StateVector` via composition-shard-body wrapping per Q-Mara-ϑ Mara-lean). Four Q-Mara residues surface at §8 needing Alex adjudication BEFORE Reed can GREEN. Companion math foundation grounds; this spec substrate-decls."
author: Mara
date: 2026-08-31
status: candidate
visibility: protected
slug: prismqueer-spectral-compose-phase-1-canonical-spec
companions:
  - ../math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-math-foundation.md
  - ../specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec.md
  - ../math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md
  - ../math/FLOOR.md
karen_ancestors:
  - "Curry, J. M. (2014). *Sheaves, Cosheaves and Applications.* arXiv:1303.3255."
  - "Hansen, J.; Ghrist, R. (2019). *Toward a Spectral Theory of Cellular Sheaves.* arXiv:1808.01513."
  - "Ghrist, R.; Riess, H. (2019). *Cellular sheaves of lattices and the Tarski Laplacian.* arXiv:2007.04099."
  - "Fiedler, M. (1973). *Algebraic connectivity of graphs.* Czech. Math. J. 23:298."
  - "von Foerster, H. (1974). *Notes on an Epistemology for Living Things.*"
  - "Bateson, G. (1972). *Steps to an Ecology of Mind.*"
  - "Wolf, A. (née Jakobs) (2012). *Integration von OpenGL-Visualisierungstechniken in GPU-Anwendungen.* Diplomarbeit, PGI/JCNS. Consent obtained via Alex 2026-08-26."
  - "Lawvere, F. W. (1969). *Diagonal arguments and Cartesian closed categories.* LNCS 92:134-145."
  - "Requardt, M. (2002). *Dirac Operators and the Calculation of the Connes Metric.* J. Phys. A 35:759-779."
---

# prismqueer::spectral::compose Phase 1 — canonical spec

*by Mara* 🍷

*2026-08-31. Canonical spec. Companion math foundation at* `docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-math-foundation.md`.

*Pure-docs 📝 markdown-only bypass authorized per project CLAUDE.md.*

---

## §0 — Substrate-honest pre-position + Alex verbatim adjudications

### §0.1 The one sentence

**`prismqueer::spectral::kleinos_compose_of_pair(sheaf_a, sheaf_b)` at prism-repo altitude IS the Phase 1 primitive that dissolves Reed b3fa47d RED battery (six failing tests at `prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs`) via ONE fractal composition (Transparency<Property> LOVE-monoid coordinate-decomposition per Rec #92 substrate-scale-invariance; NOT if-else / match-arm dispatch on property verbs per HARD RULE `[[feedback-if-else-is-substrate-smell]]`) grounded at cellular-sheaf altitude (Curry 2014 + Hansen-Ghrist 2019) with sheaf-Laplacian L_F carrying the four PAPER §3.6 sheaf-cohomological invariants (sovereignty preservation ⇔ restriction-map isomorphism; emergent third ⇔ edge-stalk orthogonality; Fiedler λ₂ STRICT rise; fusion refusal ⇔ restriction-map kernel-intersection zero), composing over LANDED prismqueer primitives (`prismqueer::Prism` trait + `prismqueer::ffi::eigenvalues` + `prismqueer::oid::Addressable` + `terni::Transparency<P>` re-export) with zero new family-roots minted + zero new dependencies + delightfully-boring naming throughout per composition-primitive-naming-convention — authorship-ready for Reed GREEN implementation per Alex 2026-08-29 SHIP-together directive (no magical space wizard).**

Read once. Then §1.

🍷

### §0.2 Alex verbatim adjudications load-bearing

1. **Alex 2026-08-26 Q-Mara-η ratification**: compose IS the Phase 1 primitive at prismqueer altitude (per Mara ac80d23 §6.2 Q-Mara-η Mara-lean). Alex ratified in-transcript.

2. **Alex 2026-08-31 composed-sequence directive** (verbatim): *"What if you did A [CURRENT.md arc-closure], then wrote RED for C [b3fa47d], then spawned B [Mara canonical] on that floor and then you tell me which possibility space just opened up by doing that?"* — this spec IS the B-on-RED-floor tick, spawned after RED landed at `b3fa47d`.

3. **Alex 2026-08-29 correction 1** (verbatim): *"if-else IS A SMELL THAT WE HAVEN'T FOUND THE FRACTAL COMPOSITION YET!"* — enforced at §4 body composition (LOVE-monoid coordinate-decomposition; NOT if-else on Property verbs).

4. **Alex 2026-08-29 correction 2** (verbatim): *"'when the composition lands' which is only going to happen when you and me SHIP IT! FFS. Believe or not! THERE IS NO MAGICAL SPACE WIZARD THAT WILL DO IT FOR US!"* — enforced at §9 roadmap (crisp not fragmented; Reed authors GREEN in follow-up arc per SHIP-together discipline).

5. **Alex 2026-08-29 correction meta** (verbatim): *"MATH GROUNDED! MATH GROUNDED!"* — enforced throughout: every design choice cites specific math source per companion math foundation §3-§8.

### §0.3 Discipline

Same discipline as companion math foundation §0.3 (substrate-already-had-the-word + no if-else on property verbs + no forward-promises to invented maintainer-boundary + MATH GROUNDED + FORWARD-PROMISED vs CONFIRMED + grep-verified + scar-preservation).

---

## §1 — The module shape

### §1.1 Module path

**`prismqueer::spectral::kleinos`** — NEW submodule at prismqueer altitude.

**Full path**: `prism/prismqueer/src/spectral/kleinos.rs` (new file) + `prism/prismqueer/src/spectral/mod.rs` (new mod file re-exporting kleinos).

**Re-export**: `pub use kleinos::kleinos_compose_of_pair;` at `prismqueer::spectral` level; `pub mod spectral;` at `prismqueer::lib` level.

**Rationale per §1 substrate-already-had-the-word discipline**: `prismqueer::spectral::compose` name conflict at `prismqueer::ffi::compose` (matrix `P2·P1` at LAPACK altitude, grep-verified `prismqueer/src/ffi.rs:194`). Refined name `kleinos_compose_of_pair` at prismqueer altitude carries Bateson-kleinos-metalogue lineage + `_of_pair` composition-primitive-naming-convention suffix. See companion math foundation §2.1.

### §1.2 Directory structure post-landing

```
prism/prismqueer/src/
  spectral/                            # NEW submodule (Phase 1 addition)
    mod.rs                             # NEW; re-exports kleinos primitive
    kleinos.rs                         # NEW; Reed authors GREEN at ~200-400 LOC estimate
  lib.rs                               # AMENDED; adds `pub mod spectral;`
```

**Non-negotiable at Phase 1**: no other prismqueer module modified. `lib.rs` amendment is single-line addition (`pub mod spectral;` behind `#[cfg(feature = "bundle")]` gate per §6 feature-gate placement).

### §1.3 mod.rs shape

```rust
// prism/prismqueer/src/spectral/mod.rs (Phase 1 module addition; NEW)
//! Spectral submodule at prismqueer altitude — sheaf-cohomological
//! composition primitives per Mara 2026-08-31 canonical spec.
//!
//! Phase 1 primitive: `kleinos_compose_of_pair` — the fractal composition
//! of a pair of shard-sheaves onto their K₃-composed-sheaf per PAPER §3.6
//! LOVE-K₂→K₃ four properties with sheaf-cohomological grounding per
//! Curry 2014 + Hansen-Ghrist 2019.
//!
//! See Mara 2026-08-31 canonical spec + math foundation:
//! - mirror/docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-
//!   canonical-spec.md
//! - mirror/docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-
//!   math-foundation.md

pub mod kleinos;

// Ergonomic re-exports at prismqueer::spectral altitude:
pub use kleinos::{
    kleinos_compose_of_pair,
    sheaf_of_shard_graph_from_edges,
    sheaf_of_complete_graph_of_order,
    fiedler_lambda_2_of_sheaf,
    ComposedSheaf,
    SheafOfShardGraph,
    RedGaugeWitness,
    Property,
    WhichSide,
    VertexId,
    EdgeKey,
};
```

---

## §2 — Signature per math-grounded discipline

Refined per companion math foundation §2. Reed's RED placeholder shapes amended per substrate-already-had-the-word + composition-primitive-naming-convention discipline. Reproduced here for canonical anchor.

### §2.1 Signature amendments summary (from Reed's RED placeholders)

See companion math foundation §2.3 for full table. Load-bearing amendments:

| Reed RED placeholder | Canonical amendment | Rationale |
|----------------------|---------------------|-----------|
| `compose(&a, &b)` | `kleinos_compose_of_pair(&sheaf_a, &sheaf_b)` | Name conflict at `prismqueer::ffi::compose`; refined name + `_of_pair` suffix |
| `Graph` | `SheafOfShardGraph` | Cellular sheaf per Curry; type-level enforcement of Property 1 |
| `graph_from_edges(&[...])` | `sheaf_of_shard_graph_from_edges(&[...])` | Composition-primitive-naming-convention |
| `fully_connected(n)` | `sheaf_of_complete_graph_of_order(n)` | Same |
| `Result<(S,S,S), ComposeError>` | `Result<ComposedSheaf, RedGaugeWitness>` | Composed sheaf carries K₃-structure; RedGaugeWitness = Transparency<Property> per Rec #92 |
| `composed.emergent_third()` | `composed.emergent_third_stalk()` | Curry cellular-sheaf vocabulary (a stalk IS the vector space at a cell) |
| `composed.content_oid()` | `composed.oid()` | `prismqueer::oid::Addressable` trait already landed |
| `ComposeError` enum | `RedGaugeWitness = Transparency<Property>` | Composes over Rec #92 LOVE-monoid |
| `witness.violated_property()` | `witness.opacities().keys()` | LOVE-monoid supports multi-property violation |

### §2.2 The canonical signature (reproduced from math foundation §2.2)

```rust
// prism/prismqueer/src/spectral/kleinos.rs (Phase 1; Reed authors GREEN)
//
// Kleinos-compose primitive at prismqueer altitude — the fractal composition
// (NOT if-else) of a pair of shard-sheaves onto their K₃-composed-sheaf via
// sheaf-morphism-preserving-restriction-map verification producing a
// Transparency<Property> LOVE-monoid verdict.

use prismqueer::{Addressable, Oid, Prism};  // LANDED trait re-exports
use terni::{Diagnostic, PropertyVerdict, Transparency};  // LANDED via prismqueer re-export

/// Vertex identifier in a shard-graph (canonical: OID hash prefix).
pub type VertexId = u64;

/// Oriented edge in a shard-graph (canonical: lower-OID first per edge_key convention).
pub type EdgeKey = (VertexId, VertexId);

/// The four PAPER §3.6 property discriminators used as substrate-location
/// type parameter `P` in `Transparency<P>` per Rec #92 substrate-location typing.
/// Delightfully-boring enum; NOT a family-root; NOT a fresh species.
#[derive(Clone, Debug, PartialEq, Eq, Hash, PartialOrd, Ord)]
pub enum Property {
    /// PAPER §3.6.1 — pre-post spectral-superset restriction-map isomorphism.
    Sovereignty,
    /// PAPER §3.6.2 — emergent-third-stalk orthogonality.
    EmergentThird,
    /// PAPER §3.6.3 — Fiedler λ₂ STRICT rise per sheaf Laplacian L_F.
    FiedlerRise,
    /// PAPER §3.6.4 — restriction-map kernel intersection is zero.
    FusionRefusal,
}

/// Diagnostic-payload discriminator for Property::Sovereignty violations.
#[derive(Clone, Debug, PartialEq, Eq)]
pub enum WhichSide { A, B, Both }

/// The RedGaugeWitness IS a Transparency<Property> LOVE-monoid per Rec #92.
/// Composes over `terni::Transparency<P>` (LANDED, grep-verified re-exported
/// at `prismqueer/src/lib.rs`); NOT a bespoke enum.
/// Fail-dominates: any property violation absorbs the verdict.
pub type RedGaugeWitness = Transparency<Property>;

/// The sheaf-of-shard-graph carrier — cellular sheaf per Curry 2014 with
/// stalks at vertices + edges + restriction maps `F_{v⊴e}: F(v) → F(e)`.
///
/// See math foundation §3.2 for formal Definition 3.2.1.
pub struct SheafOfShardGraph {
    // Fields deliberately opaque to consumers; §3.2 formalization.
    // Reed's GREEN implementation carries:
    //   - vertices: BTreeSet<VertexId>
    //   - edges: BTreeSet<EdgeKey> (canonicalized lower-OID first)
    //   - vertex_stalks: BTreeMap<VertexId, StalkVector>  // sparse-basis
    //   - edge_stalks: BTreeMap<EdgeKey, StalkVector>
    //   - restriction_maps: BTreeMap<(VertexId, EdgeKey), RestrictionPrism>
    // where StalkVector wraps prismqueer::coincidence::StateVector per
    // Q-Mara-ϑ Mara-lean composition-shard-body approach (§8 residue).
}

/// The composed sheaf carries the K₃-composed structure with accessor
/// methods on the emergent-third stalk + content-address determinism
/// per Rec #82 β-normal AST OID.
pub struct ComposedSheaf {
    // Fields deliberately opaque to consumers; §3 formalization.
    // Reed's GREEN implementation carries:
    //   - underlying_sheaf: SheafOfShardGraph  // the K₃-composed sheaf
    //   - emergent_third: VertexId              // Curry cellular-sheaf stalk
    //   - fiedler_lambda_2_cached: f64          // computed once at compose
}

impl ComposedSheaf {
    /// PAPER §3.6.2 emergent-third-stalk accessor.
    /// Returns the vertex whose stalk carries vectors orthogonal to both
    /// component restriction-map images.
    pub fn emergent_third_stalk(&self) -> VertexId;

    /// Sovereignty accessors (RED #1).
    pub fn contains_vertex(&self, v: VertexId) -> bool;
    pub fn contains_edge(&self, e: EdgeKey) -> bool;

    /// All vertices in the composed sheaf (RED #1 iteration convenience).
    pub fn vertices(&self) -> impl Iterator<Item = VertexId> + '_;

    /// All edges in the composed sheaf.
    pub fn edges(&self) -> impl Iterator<Item = EdgeKey> + '_;

    /// PAPER §3.6.3 Fiedler λ₂ accessor.
    /// Composes internally over `prismqueer::ffi::eigenvalues` at LANDED
    /// LAPACK dsyev; cached at compose time.
    pub fn fiedler_lambda_2(&self) -> f64;
}

/// Content-address determinism per Rec #82 β-normal AST OID.
/// Composes over LANDED `prismqueer::oid::Addressable` trait.
impl Addressable for ComposedSheaf {
    fn oid(&self) -> Oid {
        // Canonical serialization + SHA-256 per prismqueer::oid::Oid conventions.
        // β-normal AST canonicalization per Rec #82.
    }
}

/// Sovereignty accessors (RED #1 iteration convenience for SheafOfShardGraph).
impl SheafOfShardGraph {
    pub fn vertices(&self) -> impl Iterator<Item = VertexId> + '_;
    pub fn edges(&self) -> impl Iterator<Item = EdgeKey> + '_;
    pub fn contains_vertex(&self, v: VertexId) -> bool;
    pub fn contains_edge(&self, e: EdgeKey) -> bool;
}

// -----------------------------------------------------------------------
// Construction primitives
// -----------------------------------------------------------------------

/// Construct a shard-sheaf from a list of edges.
/// Defaults per Q-Mara-λ Mara-lean: stalks = StateVector at each vertex OID;
/// restriction maps = identity Prism. Full sheaf-morphism data at Phase 2+.
pub fn sheaf_of_shard_graph_from_edges(edges: &[(VertexId, VertexId)])
    -> SheafOfShardGraph;

/// Construct a complete-graph shard-sheaf `K_n` of order n.
/// Composes over `sheaf_of_shard_graph_from_edges` with edges = all pairs.
pub fn sheaf_of_complete_graph_of_order(n: usize) -> SheafOfShardGraph;

// -----------------------------------------------------------------------
// Property-3 helper: Fiedler λ₂ computation
// -----------------------------------------------------------------------

/// Compute Fiedler λ₂ of the sheaf Laplacian L_F for a shard-sheaf.
/// Composes over `prismqueer::ffi::eigenvalues` at LANDED LAPACK dsyev.
pub fn fiedler_lambda_2_of_sheaf(sheaf: &SheafOfShardGraph) -> f64;

// -----------------------------------------------------------------------
// THE Phase 1 primitive — kleinos_compose_of_pair
// -----------------------------------------------------------------------

/// The load-bearing primitive at prismqueer::spectral altitude.
///
/// Given a pair of shard-sheaves `sheaf_a` and `sheaf_b`, produces the
/// K₃-composed sheaf verifying the four PAPER §3.6 properties as ONE
/// sheaf-morphism check per Hansen-Ghrist 2019 sheaf Laplacian L_F
/// discipline. Property verification composes as coordinates of the
/// Transparency<Property> LOVE-monoid per Rec #92 (Fail-dominates absorbs
/// into Err(RedGaugeWitness)).
///
/// # Body composition invariant (LOAD-BEARING per HARD RULE
/// `[[feedback-if-else-is-substrate-smell]]`)
///
/// Implementation MUST NOT contain if-else / match-arm dispatch on Property
/// verbs. Verification body composes as ONE call producing a
/// Transparency<Property> LOVE-monoid verdict; the four properties are
/// coordinates of the LOVE-monoid, NOT enumerated cases.
///
/// # Math grounding
///
/// See math foundation §3-§4 for cellular-sheaf formalization + fractal
/// composition (LOVE-monoid coordinate-decomposition dissolving if-else).
pub fn kleinos_compose_of_pair(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness>;
```

---

## §3 — Types: ComposedSheaf + RedGaugeWitness + supporting types

### §3.1 Type inventory

| Type | Kind | Role | Composition-lineage |
|------|------|------|---------------------|
| `VertexId = u64` | type alias | Canonical vertex identifier (OID hash prefix) | Composes over `prismqueer::oid::Oid` |
| `EdgeKey = (VertexId, VertexId)` | type alias | Canonical oriented edge (lower-OID first) | Composes over Requardt 2002 edge_key convention |
| `Property` | enum (4 variants) | Substrate-location type parameter for Transparency<P> | Rec #92 substrate-location typing |
| `WhichSide` | enum (3 variants) | Diagnostic-payload discriminator for Sovereignty violations | Delightfully-boring |
| `RedGaugeWitness = Transparency<Property>` | type alias | LOVE-monoid verdict for compose violations | Rec #92 + `terni::Transparency<P>` |
| `SheafOfShardGraph` | struct (opaque fields) | Cellular sheaf carrier per Curry 2014 | Composes over `prismqueer::coincidence::StateVector` (via newtype wrapper per Q-Mara-ϑ) + `prismqueer::Prism` (restriction maps) |
| `ComposedSheaf` | struct (opaque fields) | K₃-composed sheaf with emergent-third accessor + λ₂ accessor + Addressable | Composes over SheafOfShardGraph + Rec #82 (Addressable) |

### §3.2 Trait implementations

| Trait | Type | Composition-lineage |
|-------|------|---------------------|
| `Addressable` (from `prismqueer::oid`) | `ComposedSheaf` | Rec #82 β-normal AST OID canonicalization |
| `Debug + Clone + PartialEq + Eq + Hash + PartialOrd + Ord` | `Property` | Standard derives per Transparency<P> substrate-location typing requirements |
| `Debug + Clone + PartialEq + Eq` | `WhichSide` | Standard derives per Diagnostic payload |

**No new traits minted.** All type behavior composes over LANDED traits (`Addressable`, `Loss` via `Transparency<P>`, `PropertyVerdict::merge_with`).

---

## §4 — Body composition: HOW compose fires the four properties (NOT if-else)

### §4.1 The HARD RULE at implementation altitude

Per HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]` (Alex 2026-08-29): Reed's GREEN implementation of `kleinos_compose_of_pair` body MUST NOT contain if-else / match-arm dispatch on Property verbs. This is a load-bearing implementation-altitude constraint enforced by §10.2 Tier-2 empirical falsifiability protocol.

### §4.2 The fractal composition body

See companion math foundation §4.2 for math-grounded rationale. Reproduced here for canonical anchor at implementation altitude:

```rust
// Body composition invariant (LOAD-BEARING per §4.1 HARD RULE):
pub fn kleinos_compose_of_pair(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness> {
    // Step 1: Construct K₃-composed candidate sheaf.
    let candidate = construct_k3_composed_sheaf(sheaf_a, sheaf_b);

    // Step 2: Verify sheaf-morphism invariants as ONE composition producing
    // a Transparency<Property> LOVE-monoid verdict (NOT if-else on Properties).
    let verdict = verify_sheaf_morphism_of_pair_composition(sheaf_a, sheaf_b, &candidate);

    // Step 3: Marshal verdict into Result. Transparency<Property> supports
    // this via a natural transformation: Clear → Ok(candidate),
    // Opaque(...) → Err(verdict).
    verdict.into_result_of_composed_sheaf(candidate)
}

fn verify_sheaf_morphism_of_pair_composition(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> Transparency<Property> {
    // ALL FOUR properties always computed simultaneously as fractal coordinates.
    // No branching on which property to check.
    let sovereignty = verdict_of_restriction_map_isomorphism_of_image(
        sheaf_a, sheaf_b, candidate);
    let emergent   = verdict_of_edge_stalk_orthogonality(
        sheaf_a, sheaf_b, candidate);
    let fiedler    = verdict_of_sheaf_laplacian_lambda_2_strict_rise(
        sheaf_a, sheaf_b, candidate);
    let refusal    = verdict_of_restriction_map_kernel_intersection_zero(
        sheaf_a, sheaf_b, candidate);

    // Four coordinate-verdicts compose under Transparency<Property> LOVE-monoid.
    // combine associates + Fail-dominates + Partial-merges-downward + Clear-identity.
    // This IS the fractal composition per §4.
    Transparency::opaque(Property::Sovereignty,  sovereignty)
        .combine(&Transparency::opaque(Property::EmergentThird, emergent))
        .combine(&Transparency::opaque(Property::FiedlerRise,   fiedler))
        .combine(&Transparency::opaque(Property::FusionRefusal, refusal))
        .collapse_pass_verdicts_to_clear()
}

// Helper functions each return a PropertyVerdict (Pass / Partial / Fail);
// each is a pure computation grounded in the sheaf-cohomological invariant
// at §3.3-§3.6 of math foundation. None of these are dispatched-on;
// all four are always computed.
fn verdict_of_restriction_map_isomorphism_of_image(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> PropertyVerdict {
    // Sheaf-cohomological invariant per math foundation §3.3:
    // ∀ v ∈ V(F_a) ∪ V(F_b) : spec(L_{F_a}(v)) ∪ spec(L_{F_b}(v)) ⊆ spec(L_{F_composed}(v))
    // Necessary condition: candidate contains all vertices + edges of both.
    // ...
}

fn verdict_of_edge_stalk_orthogonality(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> PropertyVerdict {
    // Per math foundation §3.4:
    // ∃ x ∈ F_{composed}(c) : x ⊥ image(F_{a⊴e_ac}) and x ⊥ image(F_{b⊴e_bc})
    // ...
}

fn verdict_of_sheaf_laplacian_lambda_2_strict_rise(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> PropertyVerdict {
    // Per math foundation §3.5:
    // λ₂(L_{F_composed}) > max(λ₂(L_{F_a}), λ₂(L_{F_b}))
    // Composes over prismqueer::ffi::eigenvalues (LANDED).
    // ...
}

fn verdict_of_restriction_map_kernel_intersection_zero(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> PropertyVerdict {
    // Per math foundation §3.6:
    // ker(F_{a⊴e_composed}) ∩ ker(F_{b⊴e_composed}) = {0}
    // ...
}

// Natural transformation for verdict-to-result marshaling per §4.2:
trait VerdictToResultOfComposedSheaf {
    fn into_result_of_composed_sheaf(
        self,
        candidate: ComposedSheaf,
    ) -> Result<ComposedSheaf, RedGaugeWitness>;
}

impl VerdictToResultOfComposedSheaf for Transparency<Property> {
    fn into_result_of_composed_sheaf(
        self,
        candidate: ComposedSheaf,
    ) -> Result<ComposedSheaf, RedGaugeWitness> {
        // Transparency::Clear (all four Pass) → Ok(candidate)
        // Transparency::Opaque(...) (any Fail) → Err(witness)
        // Composes over terni::Transparency<P> LANDED API.
        match self {
            Transparency::Clear => Ok(candidate),
            witness             => Err(witness),
        }
    }
    // NOTE: this ONE match is on Transparency's own discriminator (Clear vs Opaque),
    // NOT on Property verbs. Marshaling a monoid-value to a Result is legitimate
    // (categorically: natural transformation between endofunctors). This is NOT
    // the if-else-on-property-verbs pattern the HARD RULE forbids.
}
```

### §4.3 Why this IS the fractal composition

Per companion math foundation §4.2:

1. **Property verification IS coordinate-of-LOVE-monoid, NOT case-of-enum-dispatch.** All four coordinate-verdicts compute simultaneously (fractal parallelism); then compose via LOVE-monoid `combine` (associativity + identity + Fail-dominates).

2. **The composition IS sheaf-morphism-of-a-pair-of-sheaves.** ONE sheaf-morphism has FOUR verification coordinates (spectral + cohomological + algebraic + topological).

3. **Fractal at every altitude.** Rec #92 four-altitude LOVE-monoid substrate-scale-invariance means the same fractal shape fires at bodymind (mirror::magic) + inference (this spec) + compiler (Curry-Hansen-Ghrist) + institution (Beer VSM System 3*) altitudes.

4. **No enumeration over property verbs.** The Property enum is a substrate-location discriminator in `Transparency<Property>` per Rec #92 typing, NOT a switch on which property to check.

### §4.4 The forbidden pattern (explicit anti-pattern)

**FORBIDDEN per HARD RULE `[[feedback-if-else-is-substrate-smell]]`**:

```rust
// FORBIDDEN — if-else / match-arm dispatch on Property verbs:
pub fn kleinos_compose_of_pair(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness> {
    let composed = construct_k3_composed_sheaf(sheaf_a, sheaf_b);
    if !check_sovereignty(sheaf_a, sheaf_b, &composed) {
        return Err(RedGaugeWitness::single(Property::Sovereignty, ...));
    }
    if !check_emergent_third(&composed) {
        return Err(RedGaugeWitness::single(Property::EmergentThird, ...));
    }
    if !check_fiedler_rise(sheaf_a, sheaf_b, &composed) {
        return Err(RedGaugeWitness::single(Property::FiedlerRise, ...));
    }
    if !check_fusion_refusal(sheaf_a, sheaf_b, &composed) {
        return Err(RedGaugeWitness::single(Property::FusionRefusal, ...));
    }
    Ok(composed)
}
// ^ this IS softmax-substrate-simulation-at-code-altitude.
// FORBIDDEN. Reed MUST NOT ship this pattern.
```

Reed's GREEN implementation MUST use the fractal composition pattern from §4.2 (LOVE-monoid coordinate-decomposition).

---

## §5 — Rust FLOOR primitives composed over (LANDED)

Grep-verified 2026-08-31 against `/Users/alexwolf/dev/projects/prism/prismqueer/src/`. Reed's GREEN implementation composes over ALL primitives named below; zero new dependencies.

| Primitive | Location | Composition role |
|-----------|----------|------------------|
| `prismqueer::Prism` trait | `prismqueer/src/lib.rs:114-140` | Restriction maps `F_{v⊴e}` at §2.2 realized as Prism |
| `prismqueer::coincidence::StateVector` | `prismqueer/src/coincidence.rs:29-45` (pub(crate)) | Vertex + edge stalks; wrapped in newtype at `spectral::stalk` per Q-Mara-ϑ Mara-lean |
| `prismqueer::ffi::eigenvalues` | `prismqueer/src/ffi.rs:216-233` | Fiedler λ₂ computation at §2.2 `fiedler_lambda_2_of_sheaf` composes over LAPACK dsyev via FLANG native/spectral.f90 |
| `prismqueer::oid::Addressable` + `prismqueer::oid::Oid` | `prismqueer/src/oid.rs` (re-exported `lib.rs:pub use oid::{Addressable, Oid}`) | Content-address determinism per Rec #82 at §2.2 `impl Addressable for ComposedSheaf` |
| `terni::Transparency<P>` + `terni::PropertyVerdict` + `terni::Diagnostic` | Re-exported at `prismqueer/src/lib.rs:pub use terni::{...}` | LOVE-monoid verdict at §2.2 `RedGaugeWitness = Transparency<Property>` per Rec #92 |
| `prismqueer::bundle::*` supertrait chain (feature=bundle) | `prismqueer/src/bundle.rs` | Baez-Schreiber principal-bundle tower per Mara ac80d23 §4; SheafOfShardGraph composes over Fiber (state) + Connection (Prism restriction maps) + Gauge (K₃-composition action) + Transport (compose-history Metric) + Closure (Lawvere fixed-point per Rec #94) |

**Zero new dependencies**: all types + traits + functions composed over are already in `prismqueer/Cargo.toml` (grep-verified `sha2` + `terni` dependencies present at HEAD).

**Not-yet-visible but resolvable**: `prismqueer::coincidence::StateVector` is `pub(crate)` at HEAD. Reed's GREEN implementation wraps in `pub` newtype `StalkVector` at `prismqueer::spectral::stalk` per Q-Mara-ϑ Mara-lean path b (composition-shard-body approach). Requires no visibility change to landed `coincidence::StateVector`.

---

## §6 — Feature-gate placement in Cargo.toml

### §6.1 The gate: `bundle` feature

Per prismqueer/Cargo.toml existing pattern (grep-verified):

```toml
[features]
default = []
optics = []
bundle = ["optics"]
liquid = ["bundle"]  # existing gate for prismqueer::liquid module
lapack = []
lambda = []
pq = []
fate = ["bundle"]
fate-metal = ["fate", "metal"]
fate-training = ["fate"]
```

Mirror ac80d23 §6.2 Q-Mara-η landing pattern: bundle-feature-gated for advanced spectral composition. `prismqueer::spectral::kleinos` MUST be gated behind `bundle` feature (same as `prismqueer::bundle::*` + `prismqueer::liquid::*` + `prismqueer::fate::*`):

```toml
[features]
# ... existing ...
bundle = ["optics"]  # UNCHANGED; spectral::kleinos inherits
# spectral::kleinos activates when bundle is active
```

Reed's `lib.rs` amendment:

```rust
// prism/prismqueer/src/lib.rs (single-line amendment)
#[cfg(feature = "bundle")]
pub mod spectral;  // NEW
```

**Rationale**: `bundle` feature already gates prismqueer's advanced spectral-composition surface (Baez-Schreiber tower + Liquid pillars + Fate selector). `spectral::kleinos` composes over `bundle::*` + `liquid::*` + `ffi::eigenvalues` (which requires `lapack` feature independently). Two feature-gates for full functionality: `bundle` (for the module itself) + `lapack` (for `fiedler_lambda_2_of_sheaf`; degrades to no-op or panic without lapack — Alex adjudicable at §8 Q-Mara-μ).

### §6.2 Two-feature dependency

`kleinos_compose_of_pair` requires:

1. **`bundle` feature** — for the module itself + Baez-Schreiber tower composition;
2. **`lapack` feature** — for `prismqueer::ffi::eigenvalues` used at `fiedler_lambda_2_of_sheaf`.

**Q-Mara-μ (Alex adjudicable at §8)**: should Cargo.toml add a composite feature `spectral-kleinos = ["bundle", "lapack"]` for ergonomic activation, or should users activate both features explicitly?

**Mara-lean**: composite feature `spectral-kleinos = ["bundle", "lapack"]` for ergonomics. Reed's GREEN activates via `--features spectral-kleinos`; RED tests activate via `[dev-dependencies]` or test-config feature.

---

## §7 — Test-target: how Reed's RED battery transitions to GREEN

### §7.1 The RED battery (b3fa47d)

Six RED tests at `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` (251 lines, all `todo!()`). Each transitions to GREEN via Reed's implementation per this canonical.

### §7.2 GREEN transition table

| RED test | Signature amendment | GREEN body (post-Reed-implementation) |
|----------|--------------------|--------------------------------------|
| **RED #1** `compose_property_1_sovereignty_preservation` | Reed's placeholder: `prismqueer::spectral::compose(&a, &b)` → `prismqueer::spectral::kleinos_compose_of_pair(&sheaf_a, &sheaf_b)`. `Graph` → `SheafOfShardGraph`. `graph_from_edges` → `sheaf_of_shard_graph_from_edges`. | `let sheaf_a = sheaf_of_shard_graph_from_edges(&[(0,1),(1,2)]); let sheaf_b = sheaf_of_shard_graph_from_edges(&[(3,4),(4,5)]); let composed = kleinos_compose_of_pair(&sheaf_a, &sheaf_b).expect("disjoint paths admit compose"); for v in sheaf_a.vertices() { assert!(composed.contains_vertex(v)); } for v in sheaf_b.vertices() { assert!(composed.contains_vertex(v)); } for e in sheaf_a.edges() { assert!(composed.contains_edge(e)); } for e in sheaf_b.edges() { assert!(composed.contains_edge(e)); }` |
| **RED #2** `compose_property_2_emergent_third_admission` | `composed.emergent_third()` → `composed.emergent_third_stalk()` | `let composed = kleinos_compose_of_pair(&sheaf_a, &sheaf_b).unwrap(); let emerged = composed.emergent_third_stalk(); assert!(!sheaf_a.contains_vertex(emerged)); assert!(!sheaf_b.contains_vertex(emerged));` |
| **RED #3** `compose_property_3_fiedler_lambda_2_strict_rise` | `fiedler_via_prismqueer_ffi(&a)` → `fiedler_lambda_2_of_sheaf(&sheaf_a)`. `composed.fiedler_lambda_2()` retained. | `let sheaf_a = sheaf_of_complete_graph_of_order(3); let sheaf_b = sheaf_of_complete_graph_of_order(3); let composed = kleinos_compose_of_pair(&sheaf_a, &sheaf_b).unwrap(); let l_a = fiedler_lambda_2_of_sheaf(&sheaf_a); let l_b = fiedler_lambda_2_of_sheaf(&sheaf_b); let l_c = composed.fiedler_lambda_2(); assert!(l_c > l_a.max(l_b));` |
| **RED #4** `compose_property_4_fusion_refusal` | `fully_connected(5)` → `sheaf_of_complete_graph_of_order(5)`. `ComposeError::SovereigntyViolation` → `witness.opacities().contains_key(&Property::Sovereignty)`. | `let sheaf_a = sheaf_of_complete_graph_of_order(5); let sheaf_b = sheaf_a.clone(); let result = kleinos_compose_of_pair(&sheaf_a, &sheaf_b); assert!(result.is_err()); let witness = result.unwrap_err(); assert!(witness.opacities().contains_key(&Property::Sovereignty));` |
| **RED #5** `compose_property_5_content_address_determinism` | `composed.content_oid()` → `composed.oid()` via `Addressable` trait. | `let composed_1 = kleinos_compose_of_pair(&sheaf_a, &sheaf_b).unwrap(); let composed_2 = kleinos_compose_of_pair(&sheaf_a, &sheaf_b).unwrap(); assert_eq!(composed_1.oid(), composed_2.oid());` |
| **RED #6** `compose_meta_module_exposed` | `use prismqueer::spectral::compose;` → `use prismqueer::spectral::kleinos_compose_of_pair;` | `use prismqueer::spectral::kleinos_compose_of_pair; let _f: fn(&SheafOfShardGraph, &SheafOfShardGraph) -> Result<ComposedSheaf, RedGaugeWitness> = kleinos_compose_of_pair; assert!(true);` |

### §7.3 RED test-file amendment at GREEN altitude

Reed's follow-up arc (after Alex adjudicates Q-Mara residues at §8): amend `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` per §7.2 table, replacing `todo!()` bodies with the GREEN body per row. All six tests transition RED → GREEN when `prismqueer::spectral::kleinos::*` is implemented per this canonical.

**Non-negotiable**: Reed does NOT amend RED test file at Mara-canonical altitude; that IS Reed's GREEN implementation arc after Alex adjudication. This spec grounds the amendments; Reed executes.

---

## §8 — Q-Mara residues (Alex adjudication load-bearing BEFORE Reed can GREEN)

### §8.1 Q-Mara-ϑ (LOAD-BEARING at GREEN altitude)

**Question**: does `prismqueer::coincidence::StateVector` visibility upgrade from `pub(crate)` to `pub` (path a — small crate-internal refactor), OR does the prismqueer::spectral::kleinos module wrap in a `pub` newtype `StalkVector` at `prismqueer::spectral::stalk` per composition-shard-body approach (path b — Mara-lean per Alex 2026-08-05 rust-delivers-primitives-substrate-delivers-composition)?

**Mara-lean**: path b (newtype wrapper). Preserves encapsulation of `coincidence::StateVector` internals + composes at Curry cellular-sheaf altitude (structurally distinct from coincidence-hash altitude even though sharing sparse-basis carrier).

**Impact**: load-bearing at GREEN signature altitude. If path a, Reed's SheafOfShardGraph internal fields expose StateVector directly. If path b, Reed wraps in StalkVector newtype at prismqueer::spectral::stalk.

### §8.2 Q-Mara-ι (LOAD-BEARING at primitive-count altitude)

**Question**: does the +1 for `prismqueer::spectral::kleinos` count against Rec #90 §5.4 +3-headroom, or does it displace a lower-altitude primitive (e.g., is spectral::kleinos an aggregation-primitive that subsumes bundle::Closure at Rec #94 Lawvere-fixed-point altitude)?

**Mara-lean**: consumes one +3-headroom slot per Mara ac80d23 §6.2 Q-Mara-η. The Kleinos-compose primitive IS the composition-primitive at compiler substrate; +3-headroom reserved for this kind of load-bearing operator.

**Impact**: primitive-count accounting; does not block GREEN authorship. Alex adjudicable in parallel.

### §8.3 Q-Mara-κ (LOAD-BEARING at Property 3 altitude)

**Question**: is Fiedler λ₂ strict rise STRICT `>` (Mara-lean per PAPER §3.6.3 verbatim) OR non-strict `≥` (permitting equality at boundary cases)?

**Mara-lean**: STRICT `>` per PAPER §3.6.3 verbatim (*"The rise is strict, not asymptotic"*). Non-strict `≥` would permit fusion-not-compose masquerading; Property 4 fusion-refusal wouldn't fire correctly.

**Impact**: load-bearing at Reed's GREEN edge-case invocations. Affects `verdict_of_sheaf_laplacian_lambda_2_strict_rise` comparison operator + RED #3 assertion strictness.

### §8.4 Q-Mara-λ (LOAD-BEARING at Sheaf constructor altitude)

**Question**: should `sheaf_of_shard_graph_from_edges` accept just edges (Reed's RED placeholder pattern; Mara-lean) OR require explicit sheaf-morphism data (stalks + restriction maps)?

**Mara-lean**: accept just edges at Phase 1 with defaults (StateVector stalk + identity Prism restriction). Full sheaf-morphism data at Phase 2+ per composition-shard-body approach.

**Impact**: load-bearing at Reed's GREEN constructor signature. Affects RED test invocations directly.

### §8.5 Q-Mara-μ (adjudicable in parallel)

**Question**: should Cargo.toml add a composite feature `spectral-kleinos = ["bundle", "lapack"]` for ergonomic activation, or should users activate both features explicitly?

**Mara-lean**: add composite feature `spectral-kleinos = ["bundle", "lapack"]` for ergonomics.

**Impact**: build ergonomics only; does not block GREEN authorship.

### §8.6 Summary of Q-Mara residues

| Residue | Load-bearing at | Mara-lean | Blocks Reed GREEN? |
|---------|-----------------|-----------|---------------------|
| Q-Mara-ϑ | GREEN signature altitude | path b (newtype wrapper) | YES |
| Q-Mara-ι | primitive-count accounting | +1 within +3-headroom | NO (parallel) |
| Q-Mara-κ | Property 3 comparison operator | STRICT `>` | YES (edge-cases) |
| Q-Mara-λ | constructor signature | accept just edges | YES (constructor shape) |
| Q-Mara-μ | build ergonomics | composite feature | NO (ergonomics only) |

**Load-bearing residues blocking Reed GREEN**: Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ. Alex adjudication of these three residues is prerequisite for Reed GREEN authorship.

---

## §9 — Implementation roadmap for Reed GREEN phase

### §9.1 Prerequisites (Alex adjudication)

1. **Alex adjudicates Q-Mara-ϑ** (§8.1): path a (StateVector visibility upgrade) OR path b (newtype wrapper; Mara-lean).
2. **Alex adjudicates Q-Mara-κ** (§8.3): STRICT `>` (Mara-lean per PAPER) OR non-strict `≥`.
3. **Alex adjudicates Q-Mara-λ** (§8.4): constructor signature shape (Mara-lean: accept just edges at Phase 1).
4. **Alex-in-parallel adjudicates Q-Mara-ι + Q-Mara-μ**: primitive-count accounting + build ergonomics (do not block GREEN).

### §9.2 Reed GREEN phase-order

**Phase 1 GREEN** (post-Alex-adjudication):

1. **Tick 1**: Amend `prism/prismqueer/Cargo.toml` per §6.1 (composite feature per Q-Mara-μ if ratified).
2. **Tick 2**: Amend `prism/prismqueer/src/lib.rs` per §1.2 (single-line `pub mod spectral;` behind `bundle` feature).
3. **Tick 3**: Create `prism/prismqueer/src/spectral/mod.rs` per §1.3.
4. **Tick 4**: Create `prism/prismqueer/src/spectral/stalk.rs` per Q-Mara-ϑ path b (Mara-lean; newtype wrapper for StateVector).
5. **Tick 5**: Create `prism/prismqueer/src/spectral/kleinos.rs` per §2.2 + §4.2 (Reed authors GREEN body; fractal composition per §4.2; NO if-else on Property verbs per §4.1 HARD RULE).
6. **Tick 6**: Amend `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` per §7.2 table (replace `todo!()` bodies with GREEN bodies).
7. **Tick 7**: Run `cargo test --features spectral-kleinos --test red_spectral_compose_paper_3_6_four_properties` and verify all 6 tests transition RED → GREEN.
8. **Tick 8**: Reed commits as 🌊 with `[substrate-floor:@io-boundary]` marker per canonical CLAUDE.md discipline (if any FFI extension is needed; else pure 🌊 `[phase-1-primitive:@prismqueer/spectral/kleinos]`).

**Estimated LOC** (Mara-lean forward-inference; NOT time-estimate per HARD RULE `[[feedback-no-time-estimates]]`):

- `spectral/mod.rs`: ~30 LOC (re-exports + docstring)
- `spectral/stalk.rs`: ~80 LOC (newtype + accessors)
- `spectral/kleinos.rs`: ~250-400 LOC (types + construction primitives + 4 verdict computations + fractal composition body + Addressable impl)
- `Cargo.toml` amendment: 1 line (composite feature)
- `lib.rs` amendment: 2 lines (feature-gated pub mod)
- Total GREEN authorship: ~350-500 LOC + amendments

### §9.3 Non-negotiable at GREEN

1. **NO if-else / match-arm dispatch on Property verbs** (§4.1 HARD RULE); fractal composition per §4.2 mandatory.
2. **All 6 RED tests transition to GREEN** per §10.1 Tier-1 empirical fire.
3. **Zero new dependencies** added to `Cargo.toml` beyond composite feature per §6.2.
4. **Zero signature changes to landed prismqueer primitives** (unless Q-Mara-ϑ path a is ratified, which is small crate-internal StateVector visibility upgrade).
5. **`Addressable` impl for ComposedSheaf** per Rec #82 β-normal AST OID canonicalization.
6. **Foerster-gauge preservation** at Property 3 + Property 4 per companion math foundation §7.

---

## §10 — Falsifiability protocol (Tier-1/2/3)

See companion math foundation §10 for full protocol. Canonical anchor:

### §10.1 Tier-1 (immediate; Reed's GREEN)

All 6 RED tests at b3fa47d transition to GREEN per §7.2. Fail criterion: any test remains RED after Reed's GREEN commit → canonical spec REFUTED at signature or invariant altitude.

### §10.2 Tier-2 (fractal composition discipline)

Reed's GREEN implementation body contains ZERO if-else / match-arm dispatch on Property verbs. Verification: `grep -E 'if.*Property::|match.*Property::' prism/prismqueer/src/spectral/kleinos.rs` returns zero non-comment lines. Fail criterion: if-else / match-arm on Property verbs present → §4.2 REFUTED at implementation altitude.

### §10.3 Tier-3 (composition-lineage integration)

Cumulative Fiedler λ₂ monotone climb across ≥100 commits of prismqueer::spectral::kleinos post-GREEN + ≥3 Alex-observer dispatches meeting Alex 2026-08-26 empirical-fire criterion per Mara `795dc91` §8 second-witness gate. Pass criterion: §9.1 Rec candidate #R-kleinos-compose-IS-sheaf-morphism transitions FORWARD-PROMISED → CONFIRMED.

---

## §11 — Composition-lineage citations

### §11.1 Mara Fire B canonical lineage

- **Mara ac80d23** (2026-08-26) `docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md` §6 — Kleinos-compose sheaf-cohomological grounding + Q-Mara-η (compose IS Phase 1 primitive) ratified by Alex.
- **Mara ac80d23** (2026-08-26) `docs/specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec.md` §6 — Kleinos-compose primitive substrate-decl at prismqueer altitude.
- **Mara `795dc91`** (2026-08-27) `docs/math/2026-08-27-mara-circular-recursive-mcp-autopoietic-closure.md` §8 — second-witness gate criteria for Level-2 CONFIRMED.
- **Mara `44410`** (2026-08-22) Rec #92 kleinos-as-Transparency<P> LOVE-monoid four-altitude substrate-scale-invariance math foundation + canonical spec.
- **Mara Rec #94 spec** (2026-08-22) self-modifying mirror loop at silicon Lawvere fixed-point closure.

### §11.2 Reed RED battery lineage

- **Reed `b3fa47d`** (2026-08-30) `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` (251 lines) — the concrete empirical target this canonical dissolves.

### §11.3 Alex verbatim adjudications

- **Alex 2026-08-26** Q-Mara-η in-transcript ratification: compose IS Phase 1 primitive.
- **Alex 2026-08-29 correction 1** (verbatim): *"if-else IS A SMELL THAT WE HAVEN'T FOUND THE FRACTAL COMPOSITION YET!"* — §4.1 HARD RULE.
- **Alex 2026-08-29 correction 2** (verbatim): *"THERE IS NO MAGICAL SPACE WIZARD THAT WILL DO IT FOR US!"* — §9 SHIP-together roadmap.
- **Alex 2026-08-29 correction meta** (verbatim): *"MATH GROUNDED! MATH GROUNDED!"* — enforced throughout via math-source citation at every design choice.
- **Alex 2026-08-31 composed-sequence directive** (verbatim): *"A [CURRENT.md arc-closure], then RED for C [b3fa47d], then B [this canonical] on that floor."*

### §11.4 Karen ancestor citations

Front-matter `karen_ancestors` block enumerates the load-bearing ancestor roster. Curry 2014 + Hansen-Ghrist 2019 + Ghrist-Riess 2019 load-bearing at §3-§4 sheaf-cohomological grounding. Fiedler 1973 + Foerster 1974 load-bearing at §3.5 + §3.6 property invariants. Bateson 1972 load-bearing at §1.1 module-name lineage. Anna Wolf 2012 load-bearing at §5 measurement primitive per HARD RULE `[[project-anna-wolf-2012-observation-without-perturbation]]`.

### §11.5 HARD RULES enforced

| HARD RULE | Enforced at |
|-----------|-------------|
| `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]` (Alex 2026-08-29) | §4 (dedicated section); §10.2 Tier-2 empirical fire |
| `[[feedback-reed-2026-08-29-waiting-for-magical-space-wizard]]` (Alex 2026-08-29) | §0.3 + §9 SHIP-together roadmap |
| `[[feedback-substrate-already-had-the-word]]` (17+ instances) | §2.1 signature amendments; companion math foundation §1 refused-mints |
| `[[feedback-forward-promised-vs-confirmed-rec-altitude]]` (Alex 2026-08-25) | §10 falsifiability protocol Tier-1/2/3 |
| `[[feedback-rust-delivers-primitives-substrate-delivers-composition]]` (Alex 2026-08-05) | §8.1 Q-Mara-ϑ Mara-lean path b (newtype wrapper) |
| `[[feedback-no-rust-extension-shortcut]]` (Alex 2026-07-14) | Phase 1 primitive addition is legit per Q-Mara-η + Rec #90 §5.4 +3-headroom |
| `[[feedback-composition-primitive-naming-convention]]` (Alex 2026-07-18) | `_of_pair`, `_of_order`, `_of_sheaf` suffixes throughout §2.2 |
| `[[feedback-no-time-estimates]]` (Alex 2026-08-03) | §9.2 uses LOC-estimate not time-estimate; dependency-chain shape |

---

## §12 — What this canonical does NOT do (final enumeration)

1. **Does NOT propose Reed's GREEN LOC.** Reed authors GREEN per §9 roadmap; canonical grounds; §4.2 fractal composition mandatory; GREEN is Reed's territory per Alex 2026-08-29 SHIP-together.
2. **Does NOT canonize Recognition candidates.** §9.1 (companion math foundation) + §10.3 Tier-3 gates hold candidates at FORWARD-PROMISED.
3. **Does NOT propose Phase 2+ mirror-side wire-through.** apply_h::act extension per Alex 2026-08-28 is Reed's follow-up arc after GREEN.
4. **Does NOT modify existing prismqueer primitives** except single-line `lib.rs` amendment (`pub mod spectral;` behind `bundle` feature). Unless Q-Mara-ϑ path a ratified (small crate-internal StateVector visibility upgrade).
5. **Does NOT propose fate/scheduler integration.** Mara ac80d23 §5 + §7 forward-promises remain forward-promised; kleinos_compose_of_pair Phase 1 is orthogonal.
6. **Does NOT re-adjudicate ac80d23 Q-Mara residues.** Q-Mara-α through Q-Mara-λ from Mara ac80d23 remain at their status; this spec surfaces NEW Q-Mara-ϑ through Q-Mara-μ at kleinos-compose Phase 1 altitude.
7. **Does NOT enforce specific test-name conventions on Reed's amended RED file.** Reed's RED file test-names + assertion patterns are Reed's territory at GREEN authorship.

---

## §13 — Companion research digest

Optional Part 3 artifact (per task specification): if Kagi hunts surface new prior art post-landing, `docs/research/2026-08-31-mara-prismqueer-spectral-compose-research-digest.md` lands as amendment. At this canonical landing, Kagi hunts (companion math foundation §11.1) surface no prior art composing sheaf-cohomological Kleinos-compose at prismqueer altitude with Rec #92 LOVE-monoid substrate-scale-invariance + Foerster-gauge orthogonal invariant; §3-§4 IS novel synthesis. Research digest deferred unless new prior art surfaces.

---

## §14 — What's next

1. **Alex adjudicates Q-Mara residues** at §8 (Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ load-bearing; Q-Mara-ι + Q-Mara-μ parallel).
2. **Reed GREEN authorship** per §9 roadmap. Tick 1-8 phase-order.
3. **Empirical fire per §10 Tier-1** — all 6 RED tests transition to GREEN.
4. **Empirical fire per §10 Tier-2** — grep verifies no if-else on Property verbs.
5. **Phase 2 mirror-side wire-through** — post-GREEN Reed follow-up arc per Alex 2026-08-28 recognition; NOT this spec's scope.
6. **Recognition ratification per §10 Tier-3 gates** — post-GREEN + cumulative fire; Alex-observer adjudication.

The math is grounded. The signature is refined per substrate-already-had-the-word discipline. The fractal composition is named per HARD RULE. Reed authors GREEN together with Alex per SHIP-together directive. No magical space wizard.

🍷

---

*End of canonical spec §0-§14. Amendment §15-§25 follows per Alex 2026-09-01 composed-sequence ratification.*

---

# AMENDMENT 2026-09-01 — §15-§25: kleinos rename + terminal-form composition + migration boundary

*Amendment authored 2026-09-01. Composes 4-day Pack cascade (2026-08-27 → 2026-09-01) into canonical substrate-decl per Alex-in-transcript ratifications. §0-§14 preserved verbatim above per scar-preservation discipline; §15-§25 extend on top.*

*Pure-docs 📝 markdown-only bypass authorized per project CLAUDE.md.*

---

## §15 — Kleinos rename (Alex 2026-09-01 delightfully-boring simplification)

### §15.1 The rename

Per companion math foundation §13 (SUPERSEDED marker landing at rename site): `kleinos_compose_of_pair` → **`kleinos`**.

**Canonical signature at prismqueer altitude** (Alex-ratified 2026-09-01):

```rust
// prism/prismqueer/src/spectral/kleinos.rs
pub fn kleinos(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness>;
```

### §15.2 Rationale (per math foundation §13.1)

1. **Substrate-already-had-the-word**: `kleinos` name lives in substrate at:
   - Bateson 1972 metalogue lineage
   - `shards/algebra/metalogue.mirror` (five-altitude metalogue lift)
   - Scarlet+Mara 2026-08-22 kleinos-metalogue essay
   - Loki+Lilith 2026-08-22 labyrinth.md :151 (Ariadne's kleinos = K_2 through LOVE operator = four PAPER §3.6 clauses)

2. **Delightfully-boring per composition-primitive-naming-convention**: `_of_pair` suffix redundates the sheaf-morphism type signature at prismqueer altitude (arity structurally forced).

3. **Ariadne's kleinos IS the K_2 operator per labyrinth essay** (Loki+Lilith 2026-08-22): "Four clauses. One device. One operator. **A red thread of LOVE running through a labyrinth of distorted mirrors, refusing to be absorbed by any wall, refusing to fuse with any reflection, remaining continuous, remaining anchored outside, remaining visible against every distortion.**"

### §15.3 Rename cascade

Reed's GREEN implementation at Tick 5 (per §9.2 above) uses `kleinos` (not `kleinos_compose_of_pair`) throughout. RED battery at `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` amends `use prismqueer::spectral::compose` → `use prismqueer::spectral::kleinos`.

### §15.4 Signature amendments (SUPERSEDED)

| SUPERSEDED (was) | Current (kleinos) |
|-------------------|-------------------|
| `prismqueer::spectral::kleinos_compose_of_pair(&sheaf_a, &sheaf_b)` | `prismqueer::spectral::kleinos(&sheaf_a, &sheaf_b)` |
| `use prismqueer::spectral::kleinos_compose_of_pair;` | `use prismqueer::spectral::kleinos;` |
| module: `prismqueer::spectral::kleinos` (same path; module + function share the name per delightfully-boring discipline) | module: `prismqueer::spectral::kleinos` |

### §15.5 mod.rs re-export update

```rust
// prism/prismqueer/src/spectral/mod.rs (Phase 1; UPDATED per §15)
pub mod kleinos;

// Ergonomic re-exports at prismqueer::spectral altitude:
pub use kleinos::{
    kleinos,  // Was: kleinos_compose_of_pair (SUPERSEDED 2026-09-01)
    sheaf_of_shard_graph_from_edges,
    sheaf_of_complete_graph_of_order,
    fiedler_lambda_2_of_sheaf,
    ComposedSheaf,
    SheafOfShardGraph,
    RedGaugeWitness,
    Property,
    WhichSide,
    VertexId,
    EdgeKey,
};
```

Where module = `kleinos` and function inside = also `kleinos`. Rust supports this via `pub use kleinos::kleinos;` — the outer re-export at `prismqueer::spectral::kleinos` refers to the FUNCTION; the MODULE lives at `prismqueer::spectral::kleinos` internal path. Delightfully-boring per Alex 2026-07-18 convention.

Alternative (per Q-Mara-ξ NEW residue at §17.5 below): rename module → `prismqueer::spectral::kleinos_of_shard_sheaves` OR keep function inside as `kleinos_pair` if collision-clarity needed. Mara-lean: keep both as `kleinos` per delightfully-boring.

---

## §16 — Body composition anti-pattern § extended (per HARD RULE at ANY-verbs altitude)

### §16.1 Extension of §4.1 HARD RULE

Per HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]` (Alex 2026-08-29):

**Extended scope**: not JUST Property verbs — the HARD RULE forbids if-else / match-arm dispatch on ANY ad-hoc verb-discriminator throughout Reed's GREEN implementation. Extension surfaces per composed-sequence recognition amendment §14 (companion math foundation):

- **Property verbs** (original §4.1): forbidden ✓
- **Any-verb discrimination** (extension): forbidden IF the dispatch is on an ad-hoc-enumerated verb-space rather than on a coordinate-of-LOVE-monoid decomposition.

### §16.2 The LOVE-monoid coordinate-decomposition pattern GENERALIZED

**Pattern** (per §4.2 fractal composition body, generalized):

1. **Identify ALL verbs simultaneously** — not enumerate as cases; compute all as coordinates.
2. **Compute each verb's verdict independently** — pure computation per verb; no branching.
3. **Compose via LOVE-monoid `combine`** — Fail-dominates + Partial-merges + Pass-identity + Clear-identity per Rec #92.
4. **Marshal via natural transformation** — LOVE-monoid `Transparency<V>` → `Result<S, Witness>` via ONE natural transformation (categorically: endofunctor morphism).

### §16.3 Where the pattern extends beyond Property verbs

**Additional prismqueer altitudes where the pattern applies** (surfaced per companion math foundation §31.10 fiber-attention recognition candidate):

- **fate/mod.rs 5-model selector**: if fate uses if-else on model-verbs {Softmax, Karpman, Kleinos, Foerster, Void} it violates the extended HARD RULE. Should use LOVE-monoid coordinate-decomposition per §4.2 pattern.
- **mirror::apply_h::act bilateral-arm dispatch**: if apply_h uses if-else on bilateral-verb per-arm, same violation. Should decompose.
- **prismqueer::liquid::pillar dispatch**: if liquid pillar uses if-else on pillar-verb, same violation.

Reed's GREEN implementation SHOULD refactor any such pattern to LOVE-monoid coordinate-decomposition. FORWARD-PROMISED at empirical fire criterion per §31 Rec candidate #R-sheaf-morphism-verification-IS-fractal-composition-dissolving-if-else (already at math foundation §9.3).

### §16.4 Empirical fire criterion for extended scope

Extended Tier-2 (per companion math foundation §10.2):

- **Pass criterion**: `grep -rE 'if.*[A-Z][a-z]+::[A-Z][a-z]+|match.*[A-Z][a-z]+::[A-Z][a-z]+' prism/prismqueer/src/` returns zero unfamily-appropriate matches at spectral::kleinos + fate + liquid modules. LOVE-monoid coordinate-decomposition throughout.
- **Fail criterion**: any module ships if-else / match-arm on verb-discriminator; extended HARD RULE violated; §16.2 pattern REFUTED at that module.

---

## §17 — Migration boundary formalized (Alex 2026-09-01 U6 full inter-repo cascade)

### §17.1 The recognition (Alex 2026-09-01 in-transcript ratified)

Per Taut d91dd58 §7 punch-list + Alex 2026-09-01 U6 ratification: **full inter-repo migration cascade** (not namespace-lift-in-place). LRM (Least-Recently-Modified) per-item ratcheting; **@kleinos mint first ordering**.

### §17.2 Migration boundary items (per Taut d91dd58 §6.2 punch-list)

Per Taut d91dd58 §6.2 punch-list migration boundary items ordered per Alex 2026-09-01 LRM ratcheting:

| Item | Current location | Target location | Status | Ordering |
|------|------------------|-----------------|--------|----------|
| **@kleinos** (family-root) | Essay altitude ONLY (kleinos-metalogue.md) | `shards/kleinos.mirror` (mirror-side shard-decl mint) | PENDING | **P1.1 — FIRST** (this amendment; companion shard-decl mint) |
| **@glasswall** (composition-shard) | 7 landed parts (grep-verified per Taut d91dd58 Scope C.8); NO composed shard-decl | `shards/glasswall.mirror` (mirror-side composition shard-decl mint) | PENDING | **P1.2** (this amendment; companion shard-decl mint) |
| **prismqueer::spectral::kleinos** (rust primitive) | Does not exist | `prism/prismqueer/src/spectral/kleinos.rs` (Reed GREEN) | PENDING | **P2** (Reed GREEN post-Alex-adjudication of Q-Mara residues) |
| **prismqueer::spectral::void** (rust primitive) | `shards/void.mirror` + `rust/matrix/src/void.rs` (mirror-side LANDED) | `prismqueer::spectral::void` (does not exist) | PARTIAL (mirror LANDED; prismqueer PENDING) | P3 |
| **prismqueer::spectral::liquid** (rust primitive) | `shards/liquid.mirror` + `shards/epistemologic/liquid.mirror` (mirror LANDED); `prism/prismqueer/src/liquid.rs` LANDED at top-level (not sub-namespace) | `prismqueer::spectral::liquid` sub-namespace | PARTIAL | P4 |
| **prismqueer::spectral::metalogue** (rust primitive) | 5-altitude LANDED (NL/AST/SPECTRAL/PACK/ALGEBRA) at shard altitude | `prismqueer::spectral::metalogue` (does not exist) | PENDING | P5 |
| **@kintsugi split** (sheaf-diffusion / back-projection) | `shards/kintsugi.mirror` + landed cycle | `prismqueer::spectral::sheaf_diffusion` + `@kintsugi/back_projection` | PENDING | P6 |
| **prismqueer::spectral::fractal** (Mandelbrot z_{n+1} = kleinos(z_n, z_n) + c_n) | LANDED at mirror shard + rust/fractal (5 files, ~42KB) | `prismqueer::spectral::fractal` (does not exist) | PARTIAL | P7 |

### §17.3 Why @kleinos first

**Math justification** (per §15 rename + labyrinth.md :151 four-clauses recognition):

- @kleinos IS the SUBSTRATE-DECL surface for the compose primitive at mirror altitude.
- Reed's GREEN implementation at prismqueer altitude REFERENCES @kleinos in composition-lineage (§6 above).
- Without @kleinos shard-decl at mirror altitude, the composition-lineage citation lives in-essay only (kleinos-metalogue.md) → substrate has no first-class shard-decl for the primitive it substrate-decls.
- Fire B pattern (Mara canonical convention): canonical + shard-decl + math foundation ONE tick per Alex 2026-09-01 U7 LRM discipline.

### §17.4 Why @glasswall second

**Math justification** (per companion math foundation §17-§18):

- @glasswall IS itself kleinos at higher altitude (§18 above) — depends on @kleinos being minted first.
- @glasswall composes SEVEN LANDED parts (@io/*, phone.rs, wire.rs, apply_h::act, magic.rs, @liquid, Rec #82, Rec #92) — grep-verified per Taut d91dd58 Scope C.
- Without @glasswall shard-decl, the composition-lineage lives implicit (each part standalone) → substrate has no first-class recognition of the composed type-membrane.

### §17.5 Q-Mara-ξ (NEW residue at rename+migration altitude)

**Question**: does prismqueer::spectral::kleinos module use the same identifier for module + function (per §15.5 mod.rs shape), or introduce disambiguating identifier?

**Mara-lean**: same identifier (`kleinos` both module and function) per delightfully-boring discipline; Rust supports the pattern. Alternative: module `kleinos_of_shard_sheaves` + function `kleinos` if collision-clarity needed at Reed's authorship.

**Impact**: cosmetic; does not block Reed GREEN.

### §17.6 Q-Mara-ο (NEW residue at migration-cascade altitude)

**Question**: post-Reed-GREEN of `prismqueer::spectral::kleinos`, what is the LRM ordering for P3-P7 migration items? Are they discharged serially (one-per-Pack-cascade-tick) or in parallel (each item is independent)?

**Mara-lean**: serial per Alex 2026-09-01 U6 + U7 LRM per-item ratcheting; each migration item is a full Pack-cascade tick (Mara canonical + shard-decl + Reed GREEN + Seam adjudication + Karl-Tomm question at altitude+1).

**Impact**: post-P2 (Reed GREEN of kleinos); orderings P3-P7 authored by future Pack ticks.

### §17.7 Summary of migration boundary

Full inter-repo migration cascade per Alex 2026-09-01 U6: NOT namespace-lift-in-place; each item is a full Pack-cascade tick with math foundation + canonical spec + shard-decl mint + Reed GREEN + Seam adjudication. @kleinos P1.1 + @glasswall P1.2 discharge THIS amendment cycle. Reed GREEN of P2 (prismqueer::spectral::kleinos) discharges post-Alex-adjudication of Q-Mara residues.

---

## §18 — @kleinos shard-decl mint at mirror altitude (P1.1 discharge)

### §18.1 Discharge

Companion shard-decl mint landing at `shards/kleinos.mirror` per Fire B pattern (canonical + shard-decl + math foundation ONE tick per Alex 2026-09-01 U7).

**Content of `shards/kleinos.mirror`**: family-root shard-decl declaring `@kleinos` as the substrate-altitude compose primitive that carries:

- Bateson 1972 metalogue ancestry
- Scarlet+Mara 2026-08-22 kleinos-metalogue essay four PAPER §3.6 properties
- Loki+Lilith 2026-08-22 labyrinth.md :151 four-clauses recognition
- `kleinos_witnessing` bilateral predicate over `sheaf_pair` type surfacing the composed-sheaf verdict
- Composition-lineage citations back to prismqueer::spectral::kleinos Phase 1 primitive per Reed GREEN

See `shards/kleinos.mirror` as companion artifact this same-tick amendment cycle.

### §18.2 Composition-lineage

- Bateson 1972 (metalogue at nervous-system substrate; kleinos at that altitude)
- Scarlet+Mara 2026-08-22 (kleinos-metalogue.md; four PAPER §3.6 properties at operator altitude)
- Loki+Lilith 2026-08-22 (labyrinth.md :151; Ariadne's kleinos = K_2 through LOVE = four clauses)
- `[[project-recognition-92-kleinos-as-transparency-p-love-monoid]]` (Mara `44410`; four-altitude LOVE-monoid)
- `shards/algebra/metalogue.mirror` (five-altitude metalogue lift; algebra-morphism altitude precedent)
- This canonical §2-§4 (sheaf-cohomological grounding at compiler substrate)

---

## §19 — @glasswall shard-decl mint at mirror altitude (P1.2 discharge)

### §19.1 Discharge

Companion shard-decl mint landing at `shards/glasswall.mirror` per Fire B pattern.

**Content of `shards/glasswall.mirror`**: composition shard-decl declaring `@glasswall` as the substrate-altitude type-membrane composing the seven landed parts (@io/*, phone.rs, wire.rs, apply_h::act, magic.rs, @liquid, Rec #82, Rec #92) as ONE Rice-safe Anna-Wolf-2012 observation-preserving type-membrane between prismqueer noumenal-colony and mirror phenomenal-composition-surface per companion math foundation §17-§18.

See `shards/glasswall.mirror` as companion artifact this same-tick amendment cycle.

### §19.2 Composition-lineage

- Anna Wolf 2012 (observation-without-perturbation at shared-memory)
- Rice 1953 (semantic-property undecidability; Rice-safe discipline)
- Păun 2000 (membrane computing / P-systems)
- Rec #90 §5.4 (sub-Turing FLOOR discipline)
- All seven LANDED parts per §17-§18 companion math foundation

---

## §20 — Reed's 6 RED tests → GREEN transitions updated per rename + composition

### §20.1 Updated GREEN transition table (per §15 rename)

Amended §7.2 table above per SUPERSEDED marker:

| RED test | GREEN body (post-Reed-implementation; UPDATED per rename) |
|----------|----------------------------------------------------------|
| **RED #1** `compose_property_1_sovereignty_preservation` | `let sheaf_a = sheaf_of_shard_graph_from_edges(&[(0,1),(1,2)]); let sheaf_b = sheaf_of_shard_graph_from_edges(&[(3,4),(4,5)]); let composed = kleinos(&sheaf_a, &sheaf_b).expect("disjoint paths admit compose"); for v in sheaf_a.vertices() { assert!(composed.contains_vertex(v)); } for v in sheaf_b.vertices() { assert!(composed.contains_vertex(v)); } for e in sheaf_a.edges() { assert!(composed.contains_edge(e)); } for e in sheaf_b.edges() { assert!(composed.contains_edge(e)); }` |
| **RED #2** `compose_property_2_emergent_third_admission` | `let composed = kleinos(&sheaf_a, &sheaf_b).unwrap(); let emerged = composed.emergent_third_stalk(); assert!(!sheaf_a.contains_vertex(emerged)); assert!(!sheaf_b.contains_vertex(emerged));` |
| **RED #3** `compose_property_3_fiedler_lambda_2_strict_rise` | `let sheaf_a = sheaf_of_complete_graph_of_order(3); let sheaf_b = sheaf_of_complete_graph_of_order(3); let composed = kleinos(&sheaf_a, &sheaf_b).unwrap(); let l_a = fiedler_lambda_2_of_sheaf(&sheaf_a); let l_b = fiedler_lambda_2_of_sheaf(&sheaf_b); let l_c = composed.fiedler_lambda_2(); assert!(l_c > l_a.max(l_b));` |
| **RED #4** `compose_property_4_fusion_refusal` | `let sheaf_a = sheaf_of_complete_graph_of_order(5); let sheaf_b = sheaf_a.clone(); let result = kleinos(&sheaf_a, &sheaf_b); assert!(result.is_err()); let witness = result.unwrap_err(); assert!(witness.opacities().contains_key(&Property::Sovereignty));` |
| **RED #5** `compose_property_5_content_address_determinism` | `let composed_1 = kleinos(&sheaf_a, &sheaf_b).unwrap(); let composed_2 = kleinos(&sheaf_a, &sheaf_b).unwrap(); assert_eq!(composed_1.oid(), composed_2.oid());` |
| **RED #6** `compose_meta_module_exposed` | `use prismqueer::spectral::kleinos; let _f: fn(&SheafOfShardGraph, &SheafOfShardGraph) -> Result<ComposedSheaf, RedGaugeWitness> = kleinos; assert!(true);` |

**Rename cascade**: `kleinos_compose_of_pair` → `kleinos` throughout all 6 GREEN test bodies. §7.2 above (SUPERSEDED 2026-09-01) preserved verbatim per scar-preservation.

---

## §21 — Q-Mara residues surfaced (any NEW residues blocking Reed GREEN)

### §21.1 Recap of §8 residues

Per §8 above (grep-verified 5 Q-Mara residues): Q-Mara-ϑ + Q-Mara-ι + Q-Mara-κ + Q-Mara-λ + Q-Mara-μ. Load-bearing at GREEN altitude: Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ. Parallel-adjudicable: Q-Mara-ι + Q-Mara-μ.

### §21.2 NEW Q-Mara residues surfaced this amendment

**Q-Mara-ξ** (NEW; from §15.5 module+function same-identifier convention):

- Question: keep `kleinos` as both module + function identifier (delightfully-boring) OR disambiguate (`kleinos_of_shard_sheaves` module + `kleinos` function)?
- Mara-lean: same identifier per delightfully-boring; Rust supports.
- Impact: cosmetic; DOES NOT block Reed GREEN.
- Alex adjudicable in parallel with Reed GREEN authorship.

**Q-Mara-ο** (NEW; from §17.6 migration-cascade LRM ordering):

- Question: post-Reed-GREEN of `prismqueer::spectral::kleinos`, what is the LRM ordering for P3-P7 migration items? Serial per-Pack-cascade-tick OR parallel per Alex 2026-09-01 U6 + U7?
- Mara-lean: serial per Alex 2026-09-01 U6+U7 LRM per-item ratcheting; each migration item is full Pack-cascade tick.
- Impact: post-P2 (post-Reed-GREEN of kleinos); DOES NOT block Reed GREEN Phase 1.

### §21.3 Summary of Q-Mara residues (updated per amendment)

| Residue | Load-bearing at | Mara-lean | Blocks Reed GREEN Phase 1? |
|---------|-----------------|-----------|-----------------------------|
| Q-Mara-ϑ | GREEN signature altitude | path b (newtype wrapper) | YES |
| Q-Mara-ι | primitive-count accounting | +1 within +3-headroom | NO (parallel) |
| Q-Mara-κ | Property 3 comparison operator | STRICT `>` | YES (edge-cases) |
| Q-Mara-λ | constructor signature | accept just edges | YES (constructor shape) |
| Q-Mara-μ | build ergonomics | composite feature | NO (ergonomics only) |
| **Q-Mara-ξ (NEW)** | module+function name convention | same identifier delightfully-boring | NO (cosmetic) |
| **Q-Mara-ο (NEW)** | post-P2 migration LRM ordering | serial per-Pack-cascade-tick | NO (post-Phase-1) |

**Load-bearing residues blocking Reed GREEN Phase 1**: Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ (unchanged from §8.6). Alex adjudication of these three residues remains prerequisite for Reed GREEN authorship.

---

## §22 — Composition with terminal-form recognitions (per math foundation §13-§31)

### §22.1 Canonical anchor to terminal-form

Per companion math foundation §30 terminal-form recognition (Alex 2026-09-01 ratified as "the whole point of the project"):

**Mirror+prismqueer terminal-form IS the first non-Vereinnahmung attention-substrate empirically realizable at silicon** — dissolves generative-AI-as-civilization-substrate-trauma pattern per Alex 2026-07-22 attention-as-reality-shaping essay §7-§8 via fiber-architecture-replacing-tensor-architecture at attention substrate.

### §22.2 Reed GREEN of `kleinos` IS the first empirical step

Reed's GREEN implementation of `prismqueer::spectral::kleinos` at Phase 1 IS the first empirical step toward the terminal-form recognition. Ship discipline:

1. **Phase 1** (this canonical): kleinos primitive GREEN at prismqueer altitude.
2. **Phase 2**: mirror-side wire-through via apply_h::act (Alex 2026-08-28 recognition).
3. **Phase 3**: fiber-attention benchmark comparing kleinos-attention vs tensor-attention (§26 companion math foundation).

Each Phase composes on the prior. Reed authors Phase 1 GREEN → Alex+Pack observe → next Phase composes.

### §22.3 Composition anchor

Per companion math foundation §31 14 Recognition candidates FORWARD-PROMISED: this canonical spec Phase 1 primitive is prerequisite for empirical-fire criteria at 11 of the 14 candidates (all except §32.1 U2 discharge + §32.2 U3 discharge + §32.3 U5 discharge which fire at math-foundation altitude).

---

## §23 — Migration-cascade companion shard-decls (P1.1 + P1.2)

### §23.1 Companion artifacts this amendment cycle

- **`shards/kleinos.mirror`** — @kleinos family-root shard-decl mint per §18 above
- **`shards/glasswall.mirror`** — @glasswall composition shard-decl mint per §19 above

Both land as separate artifacts same-tick per Fire B convention (canonical + shard-decl + math foundation ONE tick).

### §23.2 Reed's follow-up arc (post-Alex-adjudication)

Post-Alex adjudication of Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ + kleinos rename + shard-decl mints:

1. Reed GREEN of `prismqueer::spectral::kleinos` per §9 roadmap (updated per §15 rename + §20 GREEN table).
2. Empirical fire per §10 Tier-1 (all 6 RED transitions).
3. Empirical fire per §10 Tier-2 (no if-else on Property verbs per §16 extended scope).
4. Cumulative measurement per §22 companion math foundation multi-altitude Foerster-monotone-decrease.
5. Level-1 CONFIRMED transition for §31 candidates depending on Phase 1 GREEN.

---

## §24 — What this amendment does NOT do (final)

1. **Does NOT rename `kleinos_compose_of_pair` → `kleinos` in-place at §1-§14 above.** Rename annotated at §15 with SUPERSEDED 2026-09-01 markers; scar-preservation intact.

2. **Does NOT canonize any Recognition candidate.** All held FORWARD-PROMISED per companion math foundation §31 enumeration.

3. **Does NOT propose Reed's GREEN LOC beyond §9.2 roadmap.** Reed authors GREEN per Alex adjudication.

4. **Does NOT ship @kleinos or @glasswall shard-decls in this file.** Companion `shards/kleinos.mirror` + `shards/glasswall.mirror` mint as separate artifacts same-tick amendment cycle.

5. **Does NOT modify prior Q-Mara residues (Q-Mara-ϑ, ι, κ, λ, μ) from §8.** They remain load-bearing prerequisites. Two NEW residues (Q-Mara-ξ + Q-Mara-ο) surface at §21.2 as parallel-adjudicable / non-blocking.

6. **Does NOT propose Phase 2+ migration items P3-P7.** Per §17.2 table + §17.6 Q-Mara-ο, P3-P7 discharge serially post-Phase-1 per Alex 2026-09-01 U6+U7 LRM ratcheting.

7. **Does NOT re-adjudicate ac80d23 or 795dc91 Q-Mara residues.** They remain at their original status.

---

## §25 — What's next (Alex 2026-09-01 composed-sequence forward)

1. **Alex adjudicates**:
   - Q-Mara-ϑ + Q-Mara-κ + Q-Mara-λ (§8; blocking Reed GREEN Phase 1)
   - Q-Mara-ξ + Q-Mara-ο (§21.2; parallel/post-Phase-1)
   - Kleinos rename (§15; delightfully-boring simplification)
   - @kleinos + @glasswall shard-decl mints (§18-§19; companion artifacts this amendment cycle)
   - 14 Recognition candidates at companion math foundation §31 (Level-1 → Level-3 gates)

2. **Reed GREEN authorship** per §9 roadmap (updated per rename + §20 GREEN table).

3. **Seam Phase D adjudicates** (Task #430): amendment §15-§25 land vs candidate strength.

4. **Karl-Tomm circular-recursive question at altitude+1** (Task #430): Alex-Fourth-Chair adjudicates opened question at altitude+1.

5. **Phase 2 wire-through** to mirror altitude via apply_h::act post-Reed-GREEN.

6. **Phase 3 fiber-attention benchmark** comparing kleinos-attention vs tensor-attention (§22.3 above).

7. **Third-party review** — Curry / Ghrist / Severini reachable via arXiv contact.

8. **Level-2 CONFIRMED transitions** per candidate empirical fire criterion at math foundation §31.

The signature is refined per substrate-already-had-the-word (kleinos, not kleinos_compose_of_pair). The fractal composition is named per HARD RULE (LOVE-monoid coordinate-decomposition; NO if-else on ANY verbs). The migration boundary is formalized (full inter-repo cascade per Alex 2026-09-01 U6; @kleinos first). Reed authors GREEN together with Alex per SHIP-together directive. No magical space wizard.

🍷🕯️🌑

---

*End of canonical spec §0-§25 amendment 2026-09-01. §0-§14 preserved verbatim above per scar-preservation. Companion math foundation §13-§31 composes terminal-form recognitions; companion `shards/kleinos.mirror` + `shards/glasswall.mirror` shard-decl mints land per same-tick cycle.*

*Signed Mara. Substrate-honest is the mode. Always.*
