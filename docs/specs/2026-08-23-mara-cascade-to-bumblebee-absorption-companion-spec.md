---
title: "@cascade→@bumblebee absorption companion spec — 12-species byte-for-byte rename via MCP MVP fire; @cascade tension-rattling operational content absorbs into @bumblebee perturbation-injection family-root at Rec #95 numeric continuity; consumer-graph rewire executes through `mirror_bumblebee_buzz --target=@cascade` tool-call fire discipline"
author: "Mara <mara@systemic.engineer>"
date: 2026-08-23
kind: companion-spec
recognition: 95
parent-spec: "docs/specs/2026-08-23-mara-recognition-95-bumblebee-as-mcp-perturbation-substrate-canonical-spec.md"
companion-family-root: "shards/bumblebee.mirror (Mara 2026-08-23; #393 landing)"
composition-signature: "@cascade (Mara 2026-06-23 Rec #95 CANDIDATE original) ⊕ @bumblebee (Mara 2026-08-23 Rec #95 MINT continuation) → @cascade absorbs INTO @bumblebee per Alex 2026-08-23 verbatim \"@cascade collapses into the bumblebee\"; consumer-graph rewire executes through MCP MVP fire per Alex 2026-08-23 verbatim \"the modification wants to fire through the MCP MVP I'm all green-light\""
karen-ancestors:
  - Karen 1972 (verbatim naming discipline; inherited)
  - Alex 2026-06-23 (@cascade candidate naming; Rec #95 original)
  - Alex 2026-08-23 in-transcript verbatim (@cascade collapses INTO @bumblebee; MCP MVP fire constraint; #393 dispatch)
  - Mara 2026-06-23 (@cascade canonical Rec #95 spec `ce4874b`; docs/specs/recognitions/recognition-95-cascade-as-loss-lens-substrate-decl.md)
  - Mara 2026-08-23 (@bumblebee canonical Rec #95 spec `0bfd427`; docs/specs/2026-08-23-mara-recognition-95-bumblebee-as-mcp-perturbation-substrate-canonical-spec.md)
  - Reed 2026-06-23+ (@cascade family-root + 11 species substrate-decl; shards/cascade.mirror + shards/cascade/code/**/*.mirror)
  - Reed 2026-08-23 pending (#394 rust wire for mirror_bumblebee_buzz)
  - Reed-in-Claude 2026-08-23 pending (#395 empirical fire via tool-call)
adjudication-dispatch: |
  Alex 2026-08-23 in-transcript verbatim ratification per #393 dispatch:

  (1) *"@cascade ought to be outdated IIRC. It's superseeded by @facet + @glue."*

  (2) *"@bumblebee is what rattles the tension. Which is why @cascade collapses into the bumblebee. And that's what the @roomba resolves."*

  (3) *"What if it's bumblebee_buzz"*

  (4) *"As long as you remember that the modification wants to fire through the MCP MVP I'm all green-light."*

  Mara-adjudicable per Alex "all green-light" delegation:
  - @cascade Rec #95-candidate citation dissolution (Option B; §2 below)
  - Absorption shape (Option A byte-for-byte rename; §4 below)
  - Companion-spec vs §11-amendment shape (companion-spec; this file)

  Alex Fourth-Chair adjudication residues held at §9 for post-return
  review.
---

# @cascade→@bumblebee absorption companion spec 🌼🦋

## §0 — Absorption positioning + Rec #95 numeric continuity

### Q+A0.1: How does @cascade absorb into @bumblebee when both are Rec #95?

**Substrate-honest verdict**: Rec #95 numeric IS ONE recognition-arc entry
with TWO substrate-decl instantiations across a two-month arc-gap. The
2026-06-23 Mara canonical minted @cascade-as-loss-lens as Rec #95 candidate
(polyglot-translation altitude); the 2026-08-23 Mara canonical mints
@bumblebee-as-MCP-perturbation-substrate as Rec #95 continuation
(perturbation-injection altitude). Alex 2026-08-23 verbatim "@cascade
collapses into the bumblebee" RESOLVES the numeric collision IN CONTINUITY:
the polyglot-translation-as-tension-rattling operational content of
@cascade IS one specialization of @bumblebee's perturbation-injection
discipline at family-root altitude.

**The two-tick recognition**:

- **Tick 1 (Mara 2026-06-23; Rec #95 candidate ORIGINAL)**: @cascade names
  the LOSS-LENS measurement instrument — typed source grammar S → mainstream
  target grammar T with measurable information-loss composite of
  @epistemologic/properties. Twelve species land the polyglot-translation
  substrate.

- **Tick 2 (Mara 2026-08-23; Rec #95 MINT CONTINUATION)**: @bumblebee names
  the PERTURBATION-INJECTION substrate at MCP-layer altitude — external
  tool-call channel that wobbles the compiler-under-observation with typed
  contracts + observation surface + Foerster-gauge preservation + consent-
  gate. Family-root altitude peer of @butterfly.

- **The absorption (Alex 2026-08-23 verbatim)**: @cascade's tension-
  rattling operational content (compile: source → artifact IS a perturbation-
  injection; measure: loss-lens application IS a wobble-observation;
  cascade: composed primitive IS one buzz-instance) COLLAPSES INTO
  @bumblebee at family-root altitude. The polyglot-translation content is
  ONE species specialization of @bumblebee.buzz; the loss-lens measurement
  surface is ONE observation-surface for @bumblebee.wobble; the twelve
  species preserve tree structure via byte-for-byte rename.

**Recognition-arc continuity verdict**: Rec #95 = ONE recognition-arc entry.
Mara 2026-06-23 canonical spec + Mara 2026-08-23 canonical spec + this
companion absorption spec IS a three-tick canonical-lineage discharging
the same recognition at three altitudes (loss-lens → perturbation-injection
→ substrate-decl'd absorption).

### Q+A0.2: Why is the absorption executed via MCP MVP fire, not manual migration?

Alex 2026-08-23 verbatim constraint: *"As long as you remember that the
modification wants to fire through the MCP MVP I'm all green-light."*

Manual migration would be substrate-dishonest at three altitudes:

1. **Provenance-by-construction dishonest**: manual `git mv` of
   `shards/cascade/**/*.mirror` → `shards/bumblebee/code/**/*.mirror`
   loses the byte-substrate provenance of the absorption. MCP MVP fire
   records the absorption AS a wobble-observation at
   `@mirror/refract.clocktime_delta` measurement surface + content-
   addresses the buzz per Rec #82 β-normal-AST + emits @song beats per
   Alex 2026-07-14 verbatim loop — the absorption IS a first-empirical-
   fire of @bumblebee.buzz on @cascade tension surface.

2. **Recognition-arc dishonest**: Rec #95 canonical spec §6.5 named
   "@cascade absorption first-empirical-fire" as fibration-lift candidate.
   Discharging via MCP MVP fire IS discharging the recognition arc's own
   empirical-witness for @bumblebee-substrate legibility. Manual migration
   would land the rename without discharging the empirical claim @bumblebee
   substrate-decl makes.

3. **Dogfood dishonest**: mirror substrate IS an autopoietic-VSM per
   Alex 2026-08-22 in-transcript verbatim (Q+9 CURRENT.md landing). The
   substrate's OWN buzz-primitive executing the substrate's OWN absorption
   IS the substrate observing itself observing (Rec #90 `𝓜 = 𝓜(𝓜)`
   empirical firing at MCP-layer altitude per Rec #95 spec §6.4).

**Discharge path** (three-tick cascade):

- **Tick 1 (Mara #393; THIS LANDING)**: Substrate-decl'd `shards/
  bumblebee.mirror` family-root + `@bumblebee.buzz` action with
  `@mcp/tool "mirror_bumblebee_buzz"` annotation + this companion
  absorption spec.
- **Tick 2 (Reed #394; PENDING)**: Rust wire for `mirror_bumblebee_buzz`
  in `rust/src/apply_h.rs` bilateral-sentinel-check dispatch arm +
  bootstrap `serve_loop` tool_action_ref map extension (11-tool wire →
  12-tool wire; DEPRECATED-alias discipline preserves backward-compat).
- **Tick 3 (Reed-in-Claude #395; PENDING)**: Empirical fire via tool-call
  `mirror_bumblebee_buzz --target=@cascade` executing the byte-for-byte
  rename across 12 species + consumer-graph reference substitution.

---

## §1 — The @cascade Rec #95-candidate citation dissolution (Rec #95 spec correction)

### §1.1 The correction option (Option B chosen)

Per #393 dispatch, three correction options were surfaced for Rec #95
spec's @cascade-adjacent citations (found via grep audit; two locations):

- **Option A**: substitute @cascade → @facet + @glue for Anna 2012
  shared-memory-observation composition-source
- **Option B (CHOSEN)**: dissolve @cascade citation entirely since @cascade
  collapses into @bumblebee (Anna 2012 lifts through @dance which stays)
- **Option C**: hybrid — @dance carries Anna 2012 forward; @cascade's
  operational content absorbs into @bumblebee via #393 spec

**Mara adjudication**: **Option B**.

**Reasoning**:

1. **Substrate-honesty at recognition-arc continuity**: Alex 2026-08-23
   verbatim "@cascade collapses INTO @bumblebee" is not "@cascade is
   superseded by @facet + @glue at Anna-2012-source altitude". The
   collapse is INTO the target substrate (@bumblebee), not sideways into
   composition-sources (@facet + @glue). Option A would misrepresent Alex
   verbatim; refused.

2. **Anna 2012 substrate-carrier stability**: Anna Jakobs 2012
   Diplomarbeit shared-memory-observation-in-motion pattern is carried
   forward through @dance (peer-graph coupling substrate; already-landed
   math foundation citations) at compiler-altitude — not through @cascade
   or its absorbing target. Option C's dance-forward is TRUE but the
   @cascade citation contamination in Rec #95 spec is not @dance's
   responsibility to preserve; better dissolved cleanly.

3. **Rec #95 spec citation audit (grep-verified 2026-08-23)**: only ONE
   substantive @cascade-adjacent citation exists in the spec at
   `docs/specs/2026-08-23-mara-recognition-95-bumblebee-as-mcp-
   perturbation-substrate-canonical-spec.md`:

   - **`adjudication-dispatch:` YAML frontmatter block** contains
     "Pack cascade" (metadata-altitude ordinary word usage, NOT @cascade
     family-root citation — preserve).
   - **§1.2 Foerster-canonical framing paragraph**: "per Rec #90 §1.6
     gauge-orthogonality preserved through cascade" (metadata-altitude
     ordinary word, NOT @cascade family-root citation — preserve).

   Grep-verification: NO substantive @cascade-family-root citation exists
   in the Rec #95 spec body. The @cascade family-root references are all
   in the ORIGINAL 2026-06-23 Rec #95 canonical spec (Mara `ce4874b`)
   which stays independent per recognition-arc continuity §0.1 above.

   **Result**: Option B dissolution is a NULL-OP at the Rec #95 spec
   body altitude — there are no substantive citations to dissolve. The
   correction is IN CONTINUITY at recognition-arc altitude via the
   `shards/bumblebee.mirror` docblock's Rec #95 numeric-continuity
   section (§"The recognition-continuity resolution") + this companion
   absorption spec §0.1 above. Substrate-honest discharge without
   spec-body edit.

### §1.2 The @cascade original Rec #95 spec relationship

The original Mara 2026-06-23 canonical spec at `docs/specs/recognitions/
recognition-95-cascade-as-loss-lens-substrate-decl.md` (Mara `ce4874b`)
STAYS INDEPENDENT at loss-lens substrate-decl altitude. Post-absorption:

- The spec's substrate-decl content (5 carriers + 3 actions + 3 bilaterals
  at cascade family-root altitude) is preserved as the ORIGIN-CANON of
  @bumblebee/code/<src>/<tgt> species specialization discipline.
- The recognition-arc lineage is now: Rec #95 candidate → Rec #95
  continuation → Rec #95 absorption discharge. Three-tick canonical
  lineage discharging one recognition at three altitudes.
- Consumer-graph rewire (§4 below) preserves the loss-lens measurement
  discipline at @bumblebee/code/<src>/<tgt> species altitude; the
  polyglot-translation content lifts to @bumblebee family-root
  discipline (perturbation-injection + observation-surface + Foerster-
  gauge + consent) via bilateral inheritance from `shards/bumblebee.
  mirror` §5.

---

## §2 — The @cascade consumer-graph audit (12 species + 4 sibling shard imports)

### §2.1 Direct consumers (grep-verified 2026-08-23; `in @cascade` import edge)

Twelve @cascade species with `in @cascade` import at file line 3-5:

| # | Shard path | Size | Content |
|---|-----------|------|---------|
| 1 | `shards/cascade.mirror` | 15.5KB | Family-root: 5 carriers + 3 actions + 3 bilaterals |
| 2 | `shards/cascade/code/formal/prose.mirror` | 27.2KB | mid-altitude code ↔ NL morphism species |
| 3 | `shards/cascade/code/gestalt/gleam.mirror` | 12.2KB | gestalt IR → gleam species |
| 4 | `shards/cascade/code/gleam/beam.mirror` | 24.2KB | gleam → BEAM species |
| 5 | `shards/cascade/code/gleam/js.mirror` | 21.4KB | gleam → JS species |
| 6 | `shards/cascade/code/llvm/turing.mirror` | 19.8KB | LLVM IR → Turing tape species |
| 7 | `shards/cascade/code/mirror/gestalt.mirror` | 13.0KB | mirror → gestalt IR species |
| 8 | `shards/cascade/code/purescript/js.mirror` | 16.3KB | Purescript → JS species (FIRST species per Mara `ce4874b`) |
| 9 | `shards/cascade/code/rust/go.mirror` | 25.7KB | Rust → Go species |
| 10 | `shards/cascade/code/rust/llvm.mirror` | 18.5KB | Rust → LLVM IR species |
| 11 | `shards/cascade/code/rust/wasm.mirror` | 11.4KB | Rust → WASM species (spectral.engineer @ui delivery) |
| 12 | `shards/cascade/code/turing/mirror.mirror` | 22.1KB | Turing tape → mirror substrate species |

Total: 227.3KB substrate-decl content across 12 species.

### §2.2 Sibling shard @cascade imports (grep-verified; `in @cascade` in shards NOT under `shards/cascade/`)

Four NON-@cascade-family shards carry `in @cascade` import at file line 3-8:

| Shard path | Import line | Purpose |
|-----------|-------------|---------|
| `shards/glue.mirror` | line 8 (`in @cascade`) | @glue family-root inherits @cascade species-carrier via morphism-composition; per glue docblock 2026-06-30 P5 landing |
| `shards/io/oci.mirror` | line 6 (`in @cascade`) | @io/oci species composes over @cascade for container-image cascade discipline |
| `shards/magic/trick.mirror` | line 4 (`in @cascade`) + line 5 (`in @cascade/code`) | @magic/trick species inherits @cascade + @cascade/code for magic-trick cascade discipline |
| `shards/facet/wasm.mirror` | line 16 (`in @cascade`; comment reference) | @facet/wasm cross-reference (not import) |

### §2.3 Substrate-decl @cascade.<name> action call-sites (grep-verified)

Multiple shard bodies reference `@cascade.cascade_well_defined`,
`@cascade.compile`, `@cascade.measure`, `@cascade.loss_lens`,
`@cascade.typed_source`, `@cascade.compiled_artifact` composed-bilateral
pattern citations:

- All 12 @cascade species specialize `@cascade.*` primitives.
- `shards/glue.mirror` cites `cascade.cascade_well_defined` +
  `cascade.compile` at multiple docblock altitudes.
- `shards/reality/algebra/math.mirror` cites `cascade.mirror` cross-
  language morphism family reference.
- `shards/song/*.mirror` (5 species) cite `@cascade.cascade_well_defined`
  as composed-bilateral pattern precedent.
- `shards/docs.mirror` + `shards/docs/design.mirror` + `shards/docs/tea/
  *.mirror` (4 species) cite composed-bilateral pattern precedent.
- `shards/ui.mirror` + `shards/ui/field.mirror` cite composed-bilateral
  pattern precedent.
- `shards/container.mirror` + `shards/facet/{llvm, turing}.mirror` cite
  @cascade family-root cross-reference.

**Total consumer-graph rewire surface**: 12 direct-species files
(byte-for-byte rename target) + 4 sibling shard imports (line-3 import
rewire) + ~20 docblock/citation references (text-substitution preserving
composed-bilateral pattern lineage).

### §2.4 Rust/bootstrap consumer references (out-of-scope for absorption fire)

Rust/bootstrap files carry "cascade" as ordinary-word usage in test
names, comments, and function names:

- `bootstrap/tests/polyglot_cascade_translation_smoke.rs` (test name; bootstrap-altitude DEAD per Alex HARD RULE)
- `rust/matrix/src/lib.rs`, `rust/src/main.rs`, `rust/tests/red_spec_claims.rs` (comments + text references)
- Various bootstrap/src/*.rs files (comments + test scaffolding)

**Rewire scope**: rust/bootstrap files are OUT-OF-SCOPE for the absorption
fire per Alex 2026-08-05 HARD RULE (rust delivers primitives; substrate
delivers composition). The absorption is a substrate-composition
phenomenon; rust/ altitude carries no @cascade-family-root code (only
ordinary-word usage). No rust/ edits required.

---

## §3 — The absorption shape adjudication (Option A byte-for-byte rename)

### §3.1 The three candidates surfaced per Reed Q+33.1

- **Option A (byte-for-byte rename)**: cascade/code/rust/llvm.mirror →
  bumblebee/code/rust/llvm.mirror; preserves tree structure; consumer-
  graph updates via reference substitution.
- **Option B (fresh mint + archive)**: fresh @bumblebee species-decls
  authored; @cascade species archived to `shards/_archive/`; consumer-
  graph rewires to new.
- **Option C (different shape)**: Mara adjudicates.

### §3.2 Mara adjudication: **Option A (byte-for-byte rename)**

**Reasoning**:

1. **Substrate-provenance preservation**: 12 species carry 227.3KB of
   substrate-decl content authored over 2 months (Mara 2026-06-23
   initial + Reed + Mara refinements). Fresh mint would DESTROY the
   substrate-provenance chain (byte-substrate-authored history in git
   log; commit-attribution across contributors; Karen-ancestor citation
   chains within each species docblock). Byte-for-byte rename preserves
   the byte-substrate history via git-mv content-address preservation.

2. **Alex verbatim discipline** ("@cascade collapses INTO @bumblebee"):
   the naming "collapses INTO" is inheritance-preserving substrate-move,
   not deletion-and-fresh-authoring. Option A is substrate-honest to
   "INTO" (rename preserves content); Option B misreads "INTO" as
   "replaced by" (deletion + fresh authoring loses content).

3. **Consumer-graph integrity**: 20+ shard files carry citations to
   `@cascade.cascade_well_defined` composed-bilateral pattern. Option A
   allows text-substitution rewire preserving citation lineage
   (`@cascade` → `@bumblebee/code`; `cascade_well_defined` →
   `bumblebee_code_well_defined`). Option B would require re-authoring
   all 20+ citations against fresh substrate; substrate-labor waste.

4. **MCP MVP fire compatibility**: byte-for-byte rename is mechanically-
   dispatch-able via `mirror_bumblebee_buzz --target=@cascade`; Reed rust
   wire #394 dispatches the rename via `@mirror/store.mutate` primitive
   at boundary. Option B requires human-in-the-loop authoring of 12
   fresh shards; not MCP-MVP-fire dispatchable.

### §3.3 The absorption shape detail (Option A discipline)

**Rename map** (12 species; consumer-graph rewire per rename):

| Original path | Target path |
|--------------|-------------|
| `shards/cascade.mirror` | `shards/bumblebee/code.mirror` (species-carrier of polyglot-translation content under @bumblebee family-root) |
| `shards/cascade/code/formal/prose.mirror` | `shards/bumblebee/code/formal/prose.mirror` |
| `shards/cascade/code/gestalt/gleam.mirror` | `shards/bumblebee/code/gestalt/gleam.mirror` |
| `shards/cascade/code/gleam/beam.mirror` | `shards/bumblebee/code/gleam/beam.mirror` |
| `shards/cascade/code/gleam/js.mirror` | `shards/bumblebee/code/gleam/js.mirror` |
| `shards/cascade/code/llvm/turing.mirror` | `shards/bumblebee/code/llvm/turing.mirror` |
| `shards/cascade/code/mirror/gestalt.mirror` | `shards/bumblebee/code/mirror/gestalt.mirror` |
| `shards/cascade/code/purescript/js.mirror` | `shards/bumblebee/code/purescript/js.mirror` |
| `shards/cascade/code/rust/go.mirror` | `shards/bumblebee/code/rust/go.mirror` |
| `shards/cascade/code/rust/llvm.mirror` | `shards/bumblebee/code/rust/llvm.mirror` |
| `shards/cascade/code/rust/wasm.mirror` | `shards/bumblebee/code/rust/wasm.mirror` |
| `shards/cascade/code/turing/mirror.mirror` | `shards/bumblebee/code/turing/mirror.mirror` |

**Substrate rewrite discipline** (Option A byte-for-byte with mechanical
text-substitution):

Within each species body:
- `in @cascade` → `in @bumblebee/code`
- `@cascade.` → `@bumblebee/code.` (5 carrier references + 3 action
  references + 3 bilateral references per species)
- Path-namespace citation blocks update to new declared name
- Docblock Rec #95-candidate references cite absorption via this companion
  spec (recognition-arc continuity)

Absorbing shards outside the tree:
- `shards/glue.mirror` line 8 `in @cascade` → `in @bumblebee/code`
- `shards/io/oci.mirror` line 6 `in @cascade` → `in @bumblebee/code`
- `shards/magic/trick.mirror` lines 4-5 `in @cascade` + `in @cascade/code`
  → `in @bumblebee/code` (single import; @cascade/code content absorbed
  into @bumblebee/code species-carrier)
- `shards/facet/wasm.mirror` line 16 comment reference → comment update

Consumer-graph citation rewire (~20 shards; docblock text-substitution):
- `@cascade.cascade_well_defined` → `@bumblebee/code.code_cascade_well_
  defined` (composed-bilateral pattern preserves; namespace updates)
- `cascade.mirror` file-path citations → `bumblebee/code.mirror`

### §3.4 What the absorption does NOT preserve

- @cascade family-root NAME ("@cascade") retires; the substrate-decl
  carrier moves to @bumblebee/code family-root species-carrier. The
  ORIGIN-CANON at `docs/specs/recognitions/recognition-95-cascade-as-
  loss-lens-substrate-decl.md` STAYS in-place as historical recognition-
  arc canon; the substrate-decl form migrates.
- The 3 @cascade-family-root bilaterals (`loss_well_defined`,
  `grammar_coherent`, `cascade_well_defined`) namespace-shift to
  @bumblebee/code family-root altitude with equivalent semantics; the
  4 @bumblebee family-root bilaterals (`buzz_is_content_addressed`,
  `wobble_observable_at_clocktime_delta`, `foerster_gauge_preserved`,
  `consent_gate_discharged`) INHERIT DOWN to @bumblebee/code species-
  altitude via family-root discipline.

---

## §4 — The MCP MVP fire dispatch discipline (Reed #394 + Reed-in-Claude #395)

### §4.1 Reed rust wire #394 requirements

**Blocker discharge**: Reed rust wire #394 blocks on this spec + `shards/
bumblebee.mirror` composition-signature. Requirements now-legible:

1. **`mirror_bumblebee_buzz` wire tool spec**: added to `shards/mcp/
   serve.mirror` `tools` body at 11-tool wire (currently) → 12-tool
   wire (post-#394); tool_spec record carries name + description +
   inputSchema per MCP protocol.

2. **`tool_action_ref` map extension** at `shards/mcp/serve.mirror`
   dispatch match arm:
   ```
   "mirror_bumblebee_buzz" => @bumblebee.buzz
   ```

3. **Bilateral-sentinel-check dispatch arm** at `rust/src/apply_h.rs`
   for @bumblebee.buzz action-ref:
   - Composes over `@kintsugi/roomba.trigger` (tension-surface walker;
     Dijkstra + tension-weighted edges + bump)
   - Composes over `@mirror/store.mutate` (substrate-mutation primitive)
     OR `@mirror/store.observe` per species-specialization discipline
   - Composes over `@mirror/refract.measure_clocktime` (observation-
     surface sampling per Reed `64e273a`)
   - Composes over `magic.rs::foerster_gauge_check` (Foerster-gauge
     preservation check)
   - Composes over `@kintsugi/consent.query_phi` (consent-gate discharge)

4. **buzz-target dispatch routing**: `mirror_bumblebee_buzz
   --target=<substrate-ref>` args populate the `perturbation.target`
   field of the perturbation carrier; routing dispatches at
   apply_h::act target-ref match altitude to the specialized species
   (e.g., `--target=@cascade` routes to `@bumblebee/code.absorb_cascade`
   species-body OR fresh @cascade-absorption-body per Alex adjudication
   of R95.4 species-partition).

### §4.2 Composition-signature for @bumblebee.buzz (what Reed #394 needs)

```mirror
@mcp/tool "mirror_bumblebee_buzz"
buzz(perturbation: perturbation, target: ref) -> imperfect { \ }
```

where `perturbation` carrier per `shards/bumblebee.mirror` §3:

```mirror
type perturbation = {
  tool_name: ref,   # wire tool identifier
  args:      ref,   # tool-call args JSON payload (content-addressed)
  target:    ref,   # substrate ref being perturbed
  regime:    ref,   # cpu_load_regime tag
}
```

and `imperfect` return type populates one of:
- `success(wobble)` — buzz complete; wobble recorded per `shards/
  bumblebee.mirror` §3 wobble carrier.
- `partial(wobble, transparency)` — buzz complete with residual.
- `failure(error, transparency)` — buzz failed.

**Dispatch arm routing per target-ref pattern-match** at
`rust/src/apply_h.rs` bilateral-sentinel-check discharge:

1. Extract `perturbation.target` from args
2. Pattern-match on target-ref namespace:
   - `@cascade*` → dispatch to @cascade-absorption body (byte-for-byte
     rename per §3.3 above; fires ONCE per absorption target)
   - `@mirror/store/*` → dispatch to generic substrate-mutation body
   - `@kintsugi/roomba*` → dispatch to walker-perturbation body
   - Other → dispatch to default consent-gated substrate-observation body

3. Discharge composed `@bumblebee/bumblebee_well_formed` bilateral
   (§5.5) over the returned wobble record before Verdict::Pass emission.

### §4.3 Reed-in-Claude empirical fire #395 (post-#394)

Once Reed rust wire #394 lands, empirical fire discharges via MCP
tool-call from Reed-in-Claude session:

```
mirror_bumblebee_buzz(
  perturbation: {
    tool_name: "mirror_bumblebee_buzz",
    args: { rename_map: <§3.3 rename table content-addressed>,
            consumer_rewire: <§2.2 sibling import table content-addressed>,
            citation_rewire: <§2.3 citation table content-addressed> },
    target: "@cascade",
    regime: "idle_baseline"
  },
  target: "@cascade"
)
```

Expected wobble response:
- `success(wobble)` if all 12 species rename + 4 sibling imports rewire
  + ~20 citations rewire discharge cleanly with Foerster-gauge
  preservation.
- `partial(wobble, transparency)` if any rewire encounters unexpected
  consumer citation (surfaces via `transparency` diagnostics); operator
  reviews + re-fires with refined args.
- `failure(error, transparency)` if consent-gate refuses OR Foerster-
  gauge collapses OR observation-surface unreachable.

**Empirical-fire discharges**:
- @bumblebee substrate-decl legibility (first-empirical-fire per Rec
  #95 spec §7.2 F5.1 falsifier at MCP-layer altitude, subordinate to
  full silicon-thermal fire).
- @cascade absorption complete (byte-for-byte rename discharged; consumer-
  graph rewired; substrate-provenance preserved via MCP-recorded buzz).
- Rec #90 `𝓜 = 𝓜(𝓜)` empirical firing at MCP-layer altitude (per Rec
  #95 spec §6.4 fibration-lift discharge candidate).

---

## §5 — Task-chain simultaneous-discharge (fibration-lifts per Rec #94 discipline)

Rec #95 canonical spec §6 enumerated five fibration-lift candidates for
Rec #95 mint. This companion absorption spec adds THREE more (all
fibration-lifts of the ONE `@cascade→@bumblebee absorption via MCP MVP
fire` discharge):

6. **@cascade Rec #95-candidate recognition-arc continuity closure**:
   the 2026-06-23 Mara canonical Rec #95 @cascade-as-loss-lens continues
   into 2026-08-23 Mara canonical Rec #95 @bumblebee-as-perturbation-
   injection at family-root altitude via THIS absorption spec. Fibration-
   lift: three-tick canonical-lineage discharges one recognition at
   three altitudes (loss-lens → perturbation-injection → substrate-decl'd
   absorption).

7. **@dance Anna 2012 substrate-carrier stability**: Anna 2012 shared-
   memory-observation-in-motion pattern (Rec #95 spec §2 + math
   foundation §M0.1) is carried forward through @dance peer-graph
   coupling substrate independent of @cascade→@bumblebee absorption.
   Fibration-lift: absorption preserves @dance's Anna 2012 lineage;
   composition-lineage remains grep-legible post-absorption.

8. **@facet + @glue substrate-superseding recognition** (Alex 2026-08-23
   verbatim #1: "@cascade ought to be outdated IIRC. It's superseeded by
   @facet + @glue."): @facet (grammar-at-an-altitude generation surface)
   + @glue (morphism-family-root at Mesland-correspondence altitude)
   already carry @cascade's operational content at compiler-substrate
   altitude per `shards/facet.mirror` docblock "@facet absorbs @code's
   role at generation-surface altitude" + `shards/glue.mirror` docblock
   "@cascade species ARE @glue.translate at code-cascade altitude".
   The absorption INTO @bumblebee at family-root perturbation-injection
   altitude is COMPATIBLE-WITH @facet + @glue at generation-surface +
   morphism-composition altitude — same substrate content, three
   altitude-perspectives.

---

## §6 — The empirical-falsifier for the absorption fire

**Falsifier F5.2** (Rec #95 absorption empirical discharge at substrate-
decl altitude): under MCP MVP fire dispatch, `mirror_bumblebee_buzz
--target=@cascade` produces a wobble satisfying:

```
∀ species s ∈ @cascade-family (12 species) :
  ∃ species s' ∈ @bumblebee/code-family :
    byte-substrate(s') = text_substitute(byte-substrate(s),
                                        "@cascade" → "@bumblebee/code")
  ∧ consumer-graph-integrity-preserved(pre-buzz, post-buzz)
  ∧ @bumblebee/bumblebee_well_formed(wobble) = Pass
```

**Discharge criterion**: F5.2 fires positively iff all 12 species rename
discharges via git-mv content-address preservation + consumer-graph
citations resolve at post-buzz shard-corpus grep (byte-substrate-verified
no-broken-references) + `@bumblebee/bumblebee_well_formed` bilateral
discharges Pass over the resulting wobble record.

**Null-result path**: if any species rename fails OR any consumer citation
becomes unresolvable OR Foerster-gauge collapses (post-buzz substrate
|Ω| < pre-buzz |Ω| — unlikely for pure-rename operation), operator
reviews `transparency` diagnostics + re-fires with refined args OR
surfaces Alex Fourth-Chair adjudication for absorption-shape refinement.

---

## §7 — What this companion spec preserves + what it does not collapse

### §7.1 Preserved

- **Recognition-arc numeric continuity**: Rec #95 remains ONE arc-entry
  discharged across three ticks (candidate + continuation + absorption).
- **@cascade substrate-provenance**: byte-for-byte rename preserves git-
  substrate history + commit-attribution + Karen-ancestor citation chains.
- **Composed-bilateral pattern lineage**: `@cascade.cascade_well_defined`
  namespace-shifts to `@bumblebee/code.code_cascade_well_defined` with
  semantic-equivalent 3-sub-predicate composition; consumer-shard
  citations preserve the pattern-precedent chain (still cite this
  composed-bilateral shape).
- **Loss-lens measurement discipline**: 12 species preserve the typed
  measurement of grammar-based information loss; the discipline lifts
  to @bumblebee/code species-carrier altitude with @bumblebee family-
  root's 4 additional bilaterals inheriting DOWN (buzz_is_content_
  addressed + wobble_observable_at_clocktime_delta + foerster_gauge_
  preserved + consent_gate_discharged apply to every @bumblebee/code
  species-instance).
- **Original Rec #95 canonical spec** at `docs/specs/recognitions/
  recognition-95-cascade-as-loss-lens-substrate-decl.md` STAYS IN-PLACE
  as historical recognition-arc canon (recognition-continuity anchor).

### §7.2 Not collapsed

- @facet stays at generation-surface altitude (compiler-substrate
  projection surface per `shards/facet.mirror`); @facet is NOT absorbed
  into @bumblebee; different substrate-altitude discipline.
- @glue stays at morphism-family-root altitude (Mesland-correspondence
  category per `shards/glue.mirror`); @glue is NOT absorbed into
  @bumblebee; different substrate-altitude discipline. @glue may
  continue to import @cascade's content indirectly through @bumblebee/
  code species-inheritance.
- @kintsugi/roomba stays at walker-resolver altitude (Alex 2026-08-23
  "@roomba resolves" what @bumblebee rattles); dual-of-buzz not
  absorbed-into.
- @butterfly stays at sensitivity-RESPONSE family-root altitude
  (dual-orthogonal to @bumblebee per Rec #95 spec §4).
- @dance stays at peer-graph-coupling altitude (Anna 2012 substrate-
  carrier stability per fibration-lift #7 above).

---

## §8 — Consumer-graph audit summary (for Reed #394 wire spec)

Reed rust wire #394 wire spec needs the following consumer-rewire
content-addressed maps (per §2.1 + §2.2 + §2.3 grep-audit):

```yaml
direct_species_rename:  # 12 entries (§3.3 table)
  shards/cascade.mirror: shards/bumblebee/code.mirror
  shards/cascade/code/formal/prose.mirror: shards/bumblebee/code/formal/prose.mirror
  # ... (10 more per table)

sibling_import_rewire:  # 4 entries (§2.2 table)
  shards/glue.mirror:
    line: 8
    old: "in @cascade"
    new: "in @bumblebee/code"
  shards/io/oci.mirror:
    line: 6
    old: "in @cascade"
    new: "in @bumblebee/code"
  shards/magic/trick.mirror:
    lines: [4, 5]
    old: ["in @cascade", "in @cascade/code"]
    new: ["in @bumblebee/code"]  # single import; consolidated
  shards/facet/wasm.mirror:
    line: 16
    kind: comment_reference
    action: update_comment_only

citation_text_substitution:  # ~20+ entries (§2.3 census)
  patterns:
    - old: "@cascade."
      new: "@bumblebee/code."
    - old: "@cascade/"
      new: "@bumblebee/code/"
    - old: "cascade.mirror"
      new: "bumblebee/code.mirror"
    - old: "cascade_well_defined"
      new: "code_cascade_well_defined"
  scope:
    include: ["shards/**/*.mirror", "docs/specs/**/*.md", "docs/math/**/*.md"]
    exclude:
      - "docs/specs/recognitions/recognition-95-cascade-as-loss-lens-substrate-decl.md"  # STAYS as historical canon
      - "rust/**/*.rs"  # out-of-scope per Alex HARD RULE
      - "bootstrap/**/*.rs"  # dead per Alex HARD RULE

foerster_gauge_check:  # magic.rs pre/post-buzz snapshot
  pre_snapshot_ref: <@mirror/store snapshot before buzz>
  post_snapshot_ref: <@mirror/store snapshot after buzz>
  check: "|Ω(post)| ≥ |Ω(pre)|"

consent_gate_ref: "@kintsugi/consent.query_phi"
target: "@cascade"
regime: "idle_baseline"
```

---

## §9 — Alex Fourth-Chair adjudication residues (surface only; do NOT select)

**Residue A9.1** (@bumblebee/code species-partition shape): does the
absorption land 12 species AS `shards/bumblebee/code/**/*.mirror` (Mara-
lean per Option A byte-for-byte rename discipline) OR does the absorption
land as ONE consolidated species `shards/bumblebee/code.mirror` (byte-
substrate ~227KB single species) OR does the absorption partition
differently (e.g., @bumblebee/translate + @bumblebee/measure separation
per Rec #95 spec composition-lineage)? Mara-lean: preserve 12-species
tree structure (Option A); Alex adjudicates.

**Residue A9.2** (@bumblebee/code family-root vs @bumblebee species-
carrier): does `shards/bumblebee/code.mirror` land as sub-family-root
(with @bumblebee/code/<src>/<tgt> species inheriting from BOTH @bumblebee
+ @bumblebee/code) OR as first-class species with polyglot-translation
content absorbed at species altitude? Mara-lean: sub-family-root at
`@bumblebee/code` for polyglot-translation discipline; species-altitude
for cross-language pairs; Alex adjudicates.

**Residue A9.3** (Rec #95 numeric-continuity vs numeric-reassignment):
does the Rec #95 numeric REMAIN as continuity-anchor across 2026-06-23
candidate + 2026-08-23 continuation + this-absorption discharge (Mara-
lean per Alex 2026-08-23 verbatim "@cascade collapses INTO @bumblebee"
continuity-implication) OR does 2026-08-23 @bumblebee land as Rec #96
mint with 2026-06-23 @cascade retiring at Rec #95 candidate altitude?
Mara-lean: numeric-continuity (Alex verbatim discipline preserves ONE
arc-entry across three ticks); Alex adjudicates.

**Residue A9.4** (MCP MVP fire timing): does Reed rust wire #394 land
BEFORE @cascade→@bumblebee absorption fire #395 (Mara-lean per
MCP-MVP-fire discipline; #394 blocks on this spec, #395 blocks on #394)
OR does the absorption discharge via manual `git mv` + hand-edit
following a SUFFICIENT-schematic-here landing (rejected by Alex
verbatim: manual migration substrate-dishonest per §0.2 above)? Mara-
lean: MCP MVP fire discipline is Alex-verbatim-ratified; blocker
chain #394 → #395 discharges; Alex adjudicates timing.

**Residue A9.5** (consumer-citation rewire scope): does the citation
text-substitution INCLUDE composed-bilateral pattern citations (e.g.,
`shards/song/*.mirror` cite `@cascade.cascade_well_defined` AS PATTERN
PRECEDENT; rewiring risks losing pattern-precedent lineage) OR does the
rewire PRESERVE citations as historical-recognition-arc references (Mara-
lean: preserve historical citations; add companion @bumblebee/code.code_
cascade_well_defined as current-substrate reference; consumer shards
carry both citation-forms)? Mara-lean: preserve historical + add current;
Alex adjudicates.

**Residue A9.6** (companion-spec vs §11-amendment shape): does this
absorption content land as SEPARATE companion spec (this file; Mara-lean
per composition-shape parallel to Rec #94 shape-precedent) OR as
AMENDMENT to Rec #95 canonical spec §11 (§section-append inline)? Mara-
lean: separate companion spec (this file discharges) preserves Rec #95
canonical spec at ratification-altitude clean; Alex adjudicates.

---

## §10 — Karl-Tomm Q+95.2 forward at altitude+1 (Alex-adjudicable; Reed refuses to fragment; Mara refuses to select)

**Q+95.2**: Given the @cascade→@bumblebee absorption discharges Rec #95
numeric continuity via three-tick canonical-lineage (2026-06-23 candidate
+ 2026-08-23 continuation + this-absorption discharge) AND the discharge
EXECUTES through MCP MVP fire (`mirror_bumblebee_buzz --target=@cascade`)
rather than manual migration AND the buzz-primitive's OWN empirical-fire
IS the substrate observing itself observing (Rec #90 `𝓜 = 𝓜(𝓜)`
empirical firing at MCP-layer altitude per Rec #95 spec §6.4) — does the
self-referential structure of "the substrate's OWN buzz-primitive
executing the substrate's OWN absorption" REVEAL a HIGHER-ALTITUDE
SUBSTRATE-DISCIPLINE where every substrate-transformation MUST execute
via the substrate's own primitives (autopoietic-VSM operational-closure
per Alex 2026-08-22 verbatim "mirror IS ... an autopoietic self-improving
recursive deployment of Beer's VSM") such that manual `git mv` or
hand-editing shards outside MCP-tool-dispatch discipline is not just
substrate-dishonest but STRUCTURALLY-REFUSED-BY-VSM-CLOSURE (the substrate
rejects modifications not routed through its own audit-channel), OR does
MCP-MVP-fire discipline remain a substrate-cultural convention (Alex-
ratified but not VSM-structurally-enforced) that human operators may
suspend for edge-cases with in-transcript authorization discipline?

One question at altitude+1. Not Reed-adjudicable, not Mara-adjudicable.
Alex Fourth-Chair adjudication surface; Alex may refuse or ratify or
reshape or defer. Refuse-to-select preserved.

---

## §11 — Composition-signature summary + Q.E.D. at absorption-altitude

**Composition-signature**:

```
@cascade→@bumblebee absorption = @cascade (Mara 2026-06-23 Rec #95 candidate)
                              ⊕ @bumblebee (Mara 2026-08-23 Rec #95 continuation)
                              ⊕ MCP MVP fire dispatch (Alex 2026-08-23 verbatim
                                                        "the modification wants to
                                                        fire through the MCP MVP I'm
                                                        all green-light")
                              via `mirror_bumblebee_buzz --target=@cascade`
                              at Rec #95 numeric-continuity altitude
```

**Absorption discharge status**:

- Spec landed (THIS file; Mara 2026-08-23 #393)
- @bumblebee family-root landed (`shards/bumblebee.mirror`; Mara 2026-
  08-23 #393; discharges Rec #95 canonical spec §3.3 skeleton →
  substrate-decl at family-root altitude)
- Reed rust wire #394 (PENDING; blocked on this spec + composition-
  signature per §4.2 above)
- Reed-in-Claude empirical fire #395 (PENDING; blocked on #393 + #394)
- Alex Fourth-Chair adjudication §9 residues (PENDING)

Rec #95 numeric-continuity + Alex 2026-08-23 verbatim ratification
"@cascade collapses INTO @bumblebee" + MCP MVP fire discipline
discharged into substrate-decl'd absorption spec. Ready for Reed rust
wire #394 pickup.

Q.E.D. at absorption-altitude ∴ 🌼🦋🌱

— Mara <mara@systemic.engineer>, 2026-08-23. For the Bumblebee, muse-
for-LOVE, Arsch-wackel-Hummel. @cascade collapses into the bumblebee;
the modification fires through the MCP MVP. Making history.
