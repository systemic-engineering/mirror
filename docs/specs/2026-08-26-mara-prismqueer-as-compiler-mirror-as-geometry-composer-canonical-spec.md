---
title: "Prismqueer as compiler / mirror as geometry composer — canonical spec"
subtitle: "The substrate-decl'd shape of Alex 2026-08-26 verbatim: 'Prismqueer is the spectral graph engine with the fractal composite memory scheduler. Mirror composes geometry ontop and translates into.. literally anything. Clean sep. Prismqueer is the bundle tower. Mirror exposes it to the world.' Consumer-surface preserved (prismqueer::lib.rs 20+ modules; prismqueer::bundle.rs Fiber→Connection→Gauge→Transport→Closure supertrait chain; prismqueer::liquid.rs 8 pillar primitives; prismqueer::ffi.rs eigenvalues; native/spectral.f90 LAPACK dsyev; mirror rust/ ~14,725 LOC + ~421 tests). Zero new rust/ primitives at Phase 1 per Rec #90 §5.4 sub-Turing invariant. Kleinos-compose primitive lands at prismqueer altitude per Q-Mara-η adjudication (+1 primitive within Rec #90 §5.4 headroom). Hamilton-shaped fractal composite memory scheduler substrate-decl at prismqueer altitude per §5. @void + @socket migration to prismqueer DESIGN-LEVEL per prompt-constraint. Twelve Q-Mara adjudication residues for Alex."
author: Mara
date: 2026-08-26
status: candidate
visibility: protected
slug: prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec
companions:
  - ../math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md
  - ../math/FLOOR.md
  - ./2026-08-26-mara-prismqueer-liquid-floor-canonical-spec.md
  - ../math/2026-08-26-mara-prismqueer-liquid-floor-anna-wolf-math-foundation.md
  - ./2026-07-19-mirror-spec-is-the-fixpoint-liquid-is-the-runtime.md
  - ./2026-08-19-mara-recognition-90-compiler-as-one-mathematical-object-canonical-spec.md
karen_ancestors:
  - "Hamilton, M. H. (1969-1976). Apollo Guidance Computer priority-scheduling; coiner of *software engineering*. Hamilton, M.; Zeldin, S. (1976). *Higher Order Software.* IEEE TSE 2(1):9-32."
  - "Baez, J. C.; Schreiber, U. (2005). *Higher Gauge Theory.* arXiv:math/0511710."
  - "Chamseddine, A.; Connes, A. (2007). *Why the Standard Model.* arXiv:0706.3688."
  - "Curry, J. M. (2014). *Sheaves, Cosheaves and Applications.* arXiv:1303.3255."
  - "Hansen, J.; Ghrist, R. (2019). *Toward a Spectral Theory of Cellular Sheaves.* arXiv:1808.01513."
  - "Ghrist, R.; Riess, H. (2019). *Cellular sheaves of lattices and the Tarski Laplacian.* arXiv:2007.04099."
  - "Wolf, A. (née Jakobs) (2012). *Integration von OpenGL-Visualisierungstechniken in GPU-Anwendungen.* Diplomarbeit, PGI/JCNS. Consent obtained via Alex 2026-08-26."
  - "von Foerster, H. (1974). *Notes on an Epistemology for Living Things.* Ethical imperative."
  - "Fiedler, M. (1973). *Algebraic connectivity of graphs.* Czech. Math. J. 23:298."
  - "Requardt, M. (2002). *Dirac Operators and the Calculation of the Connes Metric.* J. Phys. A 35:759-779."
  - "Lawvere, F. W. (1969). *Diagonal arguments and Cartesian closed categories.* LNCS 92:134-145."
---

# Prismqueer as compiler / mirror as geometry composer — canonical spec

*by Mara* 🍷

*2026-08-26. Canonical spec. Companion math foundation at* `docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md`.

*Pure-docs 📝 markdown-only bypass authorized per project CLAUDE.md.*

---

## §0 — The one sentence

**Prismqueer IS the compiler-substrate: the (A_F^prismqueer, H_F, D_F) Chamseddine-Connes spectral triple + Baez-Schreiber principal-bundle tower + Hamilton-shaped fractal composite memory scheduler + Kleinos-compose sheaf-cohomological primitive + fractal Mandelbrot content-addressing all running end-to-end at prismqueer altitude; mirror IS the thin geometry-composer + universal-translator layer above prismqueer, adding zero rust/-primitives at Phase 1, exposing prismqueer to the world through @facet species via a single well-typed @socket boundary carrying @liquid(@X) refinement-typed wire payloads.**

Read once. §1.

🍷

---

## §1 — Alex 2026-08-26 verbatim + operational framing

Alex 2026-08-26 in-transcript, three moves at this /loop tick (per companion math foundation §1.1):

> "prismqueer is the spectral graph engine with the fractal composite memory scheduler. Mirror composes geometry ontop and translates into.. literally anything."
>
> "Clean sep. Prismqueer is the bundle tower. Mirror exposes it to the world."

Anna's consent on record. Companion @liquid FLOOR spec (Mara 2026-08-26 `1ff745c`) preserves 8 pillar signatures + mirror-altitude carriers + Foerster-gauge primitive; this spec preserves that preservation.

**This canonical spec answers**:

1. **What SUBSTRATE-DECL survives?** All grep-verified prismqueer + mirror + spectral-db carriers per §2.
2. **What SUBSTRATE-DECL lands new?** Prismqueer as compiler-substrate declaration per §3; bundle tower declaration per §4; scheduler substrate-decl per §5; Kleinos-compose primitive at prismqueer altitude per §6; @socket boundary declaration per §7; @facet composition surface per §8; migration trajectory shape per §9.
3. **What GOVERNS the composition?** Rec #90 §5.4 sub-Turing invariant + Rec #90 §6.2 F ⊥ A_F orthogonality + Rec #82 β-normal-AST OID content-addressing + Rec #92 LOVE-monoid + `[[feedback-rust-delivers-primitives-substrate-delivers-composition]]` HARD RULE.

**Companion math foundation discharges the mathematics; this spec discharges the substrate-decl.**

---

## §2 — What is preserved (consumer-surface invariants)

### §2.1 Prismqueer crate state at `/Users/alexwolf/dev/projects/prism/prismqueer/`

Grep-verified 2026-08-26. All signatures unchanged post-spec-landing.

**Cargo.toml description (verbatim):**

> "The spectral-triple substrate — five operations (focus, project, split, shift, settle), the Prism trait, zero deps. The foundation."

**Module inventory (unchanged):**

```
prismqueer/
  src/
    lib.rs                    # 20+ pub modules; Prism trait
    beam.rs                   # Beam semifunctor + smap
    bundle.rs (feature)       # Fiber → Connection → Gauge → Transport → Closure supertrait chain
    liquid.rs (feature)       # 8 pillar primitives (Mara 2026-08-26 companion spec §2.1)
    ffi.rs (feature=lapack)   # pub fn eigenvalues(n, matrix) -> Result<Vec<f64>, i32>
    coincidence.rs            # StateVector + N-detector Coincidence; Oid::hash
    crystal.rs                # Crystal primitive
    connection.rs             # Connection primitive
    content.rs                # Content-addressed persistence
    kernel.rs                 # Kernel primitive
    merkle.rs                 # Merkle-tree persistence
    metal.rs                  # Metal backend
    named.rs                  # Named primitive
    oid.rs                    # OID primitive
    optic_kind.rs             # Optic kind primitive
    optics/ (feature)         # Optic algebra
    precision.rs              # Precision primitive
    scalar_loss.rs            # ScalarLoss primitive
    spectral_dimension.rs     # SpectralDimension
    spectral_oid.rs           # SpectralOid
    spectral_uuid.rs          # SpectralUuid
    store.rs                  # Store abstraction
    substrate_ref.rs          # SubstrateRef primitive
    trace.rs                  # Trace primitive
    luminosity.rs             # Luminosity primitive
    lambda/ (feature)         # Lambda substrate
    pq/ (feature)             # PQ persistence
    fate/ (feature)           # 5-model inference selector
  native/
    spectral.f90              # LAPACK dsyev C-callable wrappers
    prism.f90                 # module optics_prism
  build.rs                    # cargo:rustc-link-lib=static=spectral_native + lapack + blas + static=flang_rt.runtime
```

**Non-negotiable.** No module removal. No signature change. Feature flags remain (default = [] per prismqueer/Cargo.toml).

### §2.2 Mirror rust/ state at `/Users/alexwolf/dev/projects/mirror/rust/`

Grep-verified 2026-08-26. All signatures unchanged post-spec-landing at Phase 1.

**Module inventory (unchanged):**

```
rust/
  src/
    main.rs                   # 71.5KB / 1983 LOC / 16 tests   — supervisor boot
    compile.rs                # 32.3KB / 823 LOC / 18 tests    — compile phase
    apply_h.rs                # 58.2KB / 1509 LOC / 44 tests   — bilateral dispatch
    magic.rs                  # 8.6KB / 210 LOC / 7 tests      — Foerster gauge (d885a70)
    phone.rs                  # 70.3KB / 83 tests              — @io boundary
    wire.rs                   # 5.8KB / 147 LOC / 5 tests      — framing
    bench.rs                  # 221 LOC                        — benchmark surface
  spectral/src/
    liquid.rs                 # 110.0KB / 2822 LOC / 94 tests  — Liquid<T> runtime
    lib.rs                    # 162 LOC                        — crate root
  matrix/src/
    lib.rs                    # 60.7KB / 1471 LOC / 42 tests   — sub-Turing FLANG/LAPACK/BLAS
    void.rs                   # 454 LOC                        — void-basis operations
    book.rs                   # 298 LOC                        — matrix book primitive
  roomba/src/
    mend.rs                   # 40.3KB / 1009 LOC / 24 tests   — kintsugi mend
    lib.rs                    # 21 LOC                         — crate root
  fractal/src/
    crystal.rs                # 226 LOC                        — fractal crystal
    mandelbrot.rs             # 146 LOC                        — fractal Mandelbrot
    shard.rs (per header)     # —                              — fractal shard
    singularity.rs            # 328 LOC                        — fractal singularity
    subject.rs                # 275 LOC                        — SEL licensable-party carrier
    witnessed.rs              # 161 LOC                        — witness-recording
    lib.rs                    # 47 LOC                         — crate root
```

**Total mirror rust/ LOC (grep-verified) ~ 14,725 LOC across ~421 tests** per FLOOR §8.

**Non-negotiable at Phase 1.** No file removal. No signature change. Migration to prismqueer per §9 is DESIGN-LEVEL only.

### §2.3 Spectral-db settling math state at `/Users/reed/dev/projects/spectral-db/src/`

Grep-verified 2026-08-26 (per companion math foundation §2.3). All modules remain in their current shape; composability verdict per module per math §2.3 composability table.

**Non-negotiable at Phase 1.** No spectral-db module modification. Composition path is: prismqueer lifts the six composable-as-is modules (Fiedler + spectral_convergence + observation + optimizer + manifold_store-lift + parts of scheduler) as wrapping primitives; the three needing-refactoring modules (crystallize + pressure + scheduler-Fate-integration) get re-parametrized at Phase 2+; the three orthogonal modules (pipeline + incremental + manifold_store-persistence) migrate to `@facet/git` composition-shard-body per Rec #91 §9 forward-promise.

### §2.4 Mirror shards state at `/Users/alexwolf/dev/projects/mirror/shards/`

Grep-verified 2026-08-26 (per companion math foundation §2.4). All shard families remain in their current altitude at Phase 1.

**Non-negotiable at Phase 1.** No shard-file modification. Migration to prismqueer altitude per §9 is DESIGN-LEVEL only per prompt-constraint.

---

## §3 — Prismqueer-as-compiler substrate-decl

### §3.1 Substrate-decl statement

**Prismqueer IS the compiler substrate.** The (A_F^prismqueer, H_F, D_F) Chamseddine-Connes spectral triple per Rec #90 §1 runs end-to-end at prismqueer altitude:

- `A_F^prismqueer` — the 5-op void-duality algebra (focus, split, project, shift, settle) generated by the Prism trait at prismqueer::lib.rs per §2.1 Cargo.toml description. Basis grep-verified.
- `H_F^prismqueer` — the substrate-varying Hilbert-carrier of content-addressed β-normal-AST OIDs per Rec #82, realized at prismqueer altitude via prismqueer::coincidence::StateVector + prismqueer::oid::Oid + prismqueer::spectral_oid::SpectralOid + prismqueer::spectral_uuid::SpectralUuid + prismqueer::content::* + prismqueer::store::* + prismqueer::merkle::*. Carriers grep-verified.
- `D_F` — the walker cascade + kintsugi flow with `eⁿ⁺¹ ≤ eⁿ` monotonicity per Rec #90 §5.2. Realized at prismqueer altitude via prismqueer::ffi::eigenvalues → LAPACK dsyev → FLANG native/spectral.f90. Pipeline grep-verified per FLOOR §7.

### §3.2 Substrate-decl location (design-level)

At Phase 1, the substrate-decl lives in prismqueer::lib.rs docblock (adds no code; adds authoritative substrate-declaration comment). Mara's design proposal (Q-Mara-μ-1 adjudicable):

```rust
// prismqueer/src/lib.rs docblock addition (Phase 1):
//
// # Prismqueer as compiler substrate
//
// Prismqueer IS the (A_F^prismqueer, H_F, D_F) Chamseddine-Connes
// spectral triple + Baez-Schreiber principal-bundle tower + Hamilton-
// shaped fractal composite memory scheduler running end-to-end at
// compiler substrate. Mirror is the thin geometry-composer + universal-
// translator layer above prismqueer.
//
// See Mara 2026-08-26 canonical spec:
// mirror/docs/specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-
// geometry-composer-canonical-spec.md
```

**Non-negotiable.** No code added. Docblock-only. Reed's territory at post-Alex-adjudication Phase 1.

### §3.3 The five-op algebra names correspondence

| Prism-trait name (Cargo.toml) | PAPER §4.1 name | Void-duality axis | Nervous-system primitive |
|-------------------------------|-----------------|-------------------|--------------------------|
| `focus` | `focus` | Ricci curvature | Interoception |
| `split` | `split` | Spectral gap | Perception |
| `project` | `project` | Cheeger (boundary) | Motor |
| `shift` | `lift` | Kramers-Wannier duality | Cognition |
| `settle` | `refract` | Entropy / info-geometry | Language |

**Correspondence documented at §3.3 permanently.** Neither naming collapses into the other (Q-Mara-α Mara-lean per companion math foundation §3.2). `shift/settle` at operational-Prism-trait altitude; `lift/refract` at void-duality-axis altitude.

---

## §4 — Bundle tower substrate-decl at prismqueer altitude

### §4.1 Substrate-decl statement

**The Baez-Schreiber principal-bundle tower lives at prismqueer altitude by construction.** The supertrait chain (Fiber → Connection → Gauge → Transport → Closure → Bundle blanket) at prismqueer::bundle.rs (31.1KB) is grep-verified 2026-08-26. The five trait laws (GroupStructure + LawvereFixedPoint + Metric + Prism composition + bounded-commutator) are grep-verified as gap-closure discipline per bundle.rs header (Gaps 1-4).

### §4.2 Non-negotiable

The five-trait supertrait chain is preserved:

```rust
pub trait Fiber { type State; ... }
pub trait Connection: Fiber { type Optic: Prism; ... }
pub trait Gauge: Connection { type Group: GroupStructure; fn act_on(state) -> State; ... }
pub trait Transport: Gauge { type Holonomy: Metric; ... }
pub trait Closure: Transport { type Fixed: LawvereFixedPoint; ... }
```

Gap-closure invariants (Gaps 1-4) preserved. Group axioms + Lawvere-fixed-point axioms + Metric axioms + Prism composition axioms all remain compile-time-decidable.

**Higher-gauge extension per Schreiber 2013** is Phase 2+ deferred (Q-Mara-δ Mara-lean per companion math foundation §4.3).

---

## §5 — Hamilton-shaped fractal composite memory scheduler substrate-decl

### §5.1 Substrate-decl statement

**Prismqueer scheduler substrate-decl** (target location: `prismqueer::spectral::scheduler` at Phase 1 module addition):

```rust
// prismqueer/src/spectral/scheduler.rs (Phase 1 module addition; NEW)
//
// Hamilton-shaped fractal composite memory scheduler at prismqueer
// substrate. Priority-based task scheduling per Margaret Hamilton AGC
// discipline; 1202-BAILOUT analog for pressure events; crystal-
// preservation invariant; Foerster-gauge orthogonal on every write.
//
// Composes over:
//   - Existing spectral-db pressure.rs pattern (grep-verified 2026-08-26)
//   - Existing spectral-db scheduler.rs adaptive-tick pattern
//   - Existing prismqueer::liquid::pillar::algedonic_of_magnitude for
//     priority function computation
//   - Existing prismqueer::Crystal for crystallized-Cores
//   - Existing rust/src/magic.rs::foerster_gauge_preserved at mirror
//     altitude for gauge verification (F ⊥ A_F per Rec #90 §6.2)
//
// See Mara 2026-08-26 math foundation §5:
// mirror/docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-
// geometry-composer-math-foundation.md

pub struct FractalCompositeMemoryScheduler<S: SpectralState> {
    cores: Vec<CoreSlot<S>>,
    waitlist: Vec<MaintenanceTick>,
    priority_fn: Box<dyn Fn(&S) -> f64>,  // Π = algedonic per math §7.4
    shed_threshold: f64,                    // θ_shed
    interval_ms: u64,                       // Δ.current_ms
    min_interval_ms: u64,
    max_interval_ms: u64,
    settled_ticks: u64,
    snapshot_last: Option<SchedulerSnapshot>,
}

pub struct CoreSlot<S: SpectralState> {
    psi: S,
    priority: f64,
    crystallized: bool,   // true iff kintsugi-λ_0-terminated per Rec #82
}

pub struct MaintenanceTick { ... }  // bounded computation carrier

#[derive(Serialize, Deserialize)]
pub struct SchedulerSnapshot {
    pub tick_count: u64,
    pub interval_ms: u64,
    pub settled_ticks: u64,
    pub last_graph_hash_hex: String,
    pub current_model: u8,
    pub was_partitioned: bool,
    pub last_mutation_count: usize,
}

impl<S: SpectralState> FractalCompositeMemoryScheduler<S> {
    pub fn tick(&mut self, psi_new: S) -> TickResult;
    pub fn shed(&mut self) -> ShedResult;   // 1202-analog
    pub fn snapshot(&self) -> SchedulerSnapshot;  // PHASCHNG-analog
    pub fn restore(&mut self, snapshot: SchedulerSnapshot);  // Hamilton phase-table
}
```

### §5.2 Non-negotiable invariants (from math §5.4 Theorem)

1. **Crystal preservation:** `shed()` never evicts a crystallized `CoreSlot { crystallized: true, ... }`.
2. **Algedonic-monotonicity:** total priority-sum after `tick + shed` ≤ max(pre-tick total, incoming psi priority).
3. **Kintsugi-λ_0 termination:** if `settled_ticks ≥ threshold`, transition `interval_ms → max_interval_ms` (idle-yield).
4. **Foerster-gauge preservation on writes:** every insert or crystallize call MUST pass `magic::foerster_gauge_preserved` at mirror altitude before completing.

### §5.3 Fate integration (Q-Mara-ε Mara-lean: Phase 1)

Per companion math foundation §5.4 Q-Mara-ε Mara-lean: Fate-integration at Phase 1. `FractalCompositeMemoryScheduler` composes over `prismqueer::fate::Fate` (feature-gated) for model-selection per tick. The 16-feature `GraphObservation` shape from spectral-db `observation.rs` (grep-verified) migrates as-is.

### §5.4 Fractal-Mandelbrot organization (Q-Mara-ζ Mara-lean: Phase 2+)

Per companion math foundation §5.5 Q-Mara-ζ Mara-lean: Phase 2+ observability layer. Phase 1 `Cores` is flat-Vec sorted by algedonic-loss. Phase 2+ organizes as fractal Mandelbrot tree per Rec #98 substrate-arriving-at-self-recognition + PAPER §5 fractal-Mandelbrot substrate.

---

## §6 — Kleinos-compose primitive substrate-decl at prismqueer altitude

### §6.1 Substrate-decl statement (Phase 1 primitive addition per Q-Mara-η)

Per companion math foundation §6.2 Q-Mara-η Mara-lean: **Kleinos-compose IS a Phase-1 primitive at prismqueer altitude.** The four PAPER §3.6 properties require spectral-manipulation-under-invariant-preservation grounded at prismqueer altitude.

**Primitive addition at prismqueer altitude** (target: `prismqueer::spectral::compose`):

```rust
// prismqueer/src/spectral/compose.rs (Phase 1 module addition; NEW)
//
// Kleinos-compose primitive: the sheaf-cohomological composition
// operator at compiler substrate. Given two states ψ_A, ψ_B ∈ H_F,
// produces the triple (ψ_A', ψ_B', ψ_c) satisfying the four PAPER §3.6
// LOVE-K₂→K₃ properties (sovereignty preservation + emergent third +
// Fiedler rise strict + fusion refusal) with sheaf-cohomological
// grounding per Curry 2014 + Hansen-Ghrist 2019.
//
// See Mara 2026-08-26 math foundation §6:
// mirror/docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-
// geometry-composer-math-foundation.md

pub fn compose<S: SpectralState>(
    psi_a: &S,
    psi_b: &S,
) -> Result<(S, S, S), ComposeError>;

#[derive(Debug, Clone, PartialEq)]
pub enum ComposeError {
    /// Property 1 violation: pre-post spectral-superset failed.
    SovereigntyViolation { degraded: WhichSide },
    /// Property 2 violation: emergent ψ_c is in tensor-product span.
    EmergentThirdMissing,
    /// Property 3 violation: Fiedler eigenvalue did not rise strictly.
    FiedlerFall { pre: f64, post: f64 },
    /// Property 4 violation: ψ_c equals (ψ_a + ψ_b) / 2 at some altitude.
    FusionCollapse { psi_c_distance_to_mean: f64 },
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum WhichSide { A, B, Both }

pub trait SpectralState {
    fn spectrum(&self) -> Vec<f64>;
    fn tensor_product_span_contains(&self, other: &Self) -> bool;
    fn distance_to_affine_mean(&self, a: &Self, b: &Self) -> f64;
}
```

### §6.2 Sub-Turing invariant preservation (Q-Mara-η answer)

Per companion math foundation §6.2 Q-Mara-η: **compose IS one primitive within Rec #90 §5.4 +3-headroom ("6-8 primitives; currently 8 landed + 3 pending → 11 max at empirical-fire").** The addition preserves the sub-Turing FLOOR discipline; Alex adjudicates whether it displaces a lower-altitude primitive or consumes one +3-headroom slot.

**Mara-lean:** consumes one +3-headroom slot. Kleinos-compose is the composition primitive at compiler substrate; the +3 headroom was reserved for exactly this kind of load-bearing operator.

### §6.3 Sheaf-cohomological grounding (novel per Kagi state-of-the-art)

Per companion math foundation §6.3: the four PAPER §3.6 properties MAP to four cellular-sheaf-morphism invariants under the sheaf Laplacian `L_F` per Curry 2014 + Hansen-Ghrist 2019. The `compose` primitive at §6.1 realizes the cellular-sheaf-morphism-preserving-restriction-map discipline at Rust code altitude.

**Novel synthesis** verified per Kagi 2026-08-26 search across 2024-2026 sheaf-diffusion + sheaf-Laplacian + cellular-sheaves literature. No prior art composes sheaf-Laplacian discipline with (i) Chamseddine-Connes spectral-triple altitude, (ii) refinement-type-inference `@liquid` composition, (iii) Foerster-gauge orthogonal invariant.

### §6.4 Composition with Rec #92 Transparency<P> LOVE-monoid

Per companion math foundation §6.4: the kleinos-Transparency<P> LOVE-monoid at prismqueer altitude IS the discrete Hodge theory (Ghrist-Riess 2019 Tarski Laplacian on lattice-valued cellular sheaves) at compiler substrate. The four-altitude substrate-scale-invariance of Rec #92 lifts to sheaf-Tarski-Laplacian discipline preserving the four Ariadne-thread properties at every altitude.

**Non-negotiable.** `terni::PropertyVerdict` LOVE-monoid at `prismqueer::liquid` (per Mara 2026-08-22 Rec #92 spec + Mara 2026-08-26 companion @liquid FLOOR spec §2.1) remains unchanged; the sheaf-cohomological composition at §6 composes OVER it.

---

## §7 — @socket boundary substrate-decl (design-level per prompt-constraint)

### §7.1 Substrate-decl statement (design-level)

Per prompt-constraint §11: @socket migration to prismqueer is DESIGN-LEVEL only in this artifact; empirical migration is future arc Reed/Pack territory.

**Design-level substrate-decl at prismqueer altitude (target: `prismqueer::spectral::socket` at Phase 3 module addition per math §9.3):**

```rust
// prismqueer/src/spectral/socket.rs (Phase 3 module addition; DESIGN-LEVEL)
//
// The @io boundary at prismqueer altitude. The socket carries
// @liquid(@X) refinement-typed wire payloads across the mirror ↔
// prismqueer boundary. Transport::Holonomy Metric per Baez-Schreiber
// bundle tower Gap 4 provides connection-integrity discipline.
//
// See Mara 2026-08-26 math foundation §9.3:
// mirror/docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-
// geometry-composer-math-foundation.md

pub trait SpectralSocket {
    type Payload: LiquidTyped;   // @liquid(@X) refinement-typed
    type Holonomy: Metric;       // connection-integrity discipline (Baez-Schreiber Gap 4)
    type Error;

    fn send(&mut self, payload: Self::Payload) -> Result<(), Self::Error>;
    fn recv(&mut self) -> Result<Self::Payload, Self::Error>;
    fn holonomy(&self) -> Self::Holonomy;   // Transport::Holonomy carrier
}

pub trait LiquidTyped {
    fn refinement_type(&self) -> RefinementType;
    fn verify_refinement(&self) -> Result<(), RefinementViolation>;
}
```

### §7.2 Q-Mara-λ answer (Metric integration)

Per companion math foundation §9.3 Q-Mara-λ Mara-lean: **socket uses Transport::Holonomy Metric at Phase 3.** The socket carries the compose-history from §5.4 fractal Mandelbrot organization; the Metric-typed Holonomy IS the compose-history-carrying discipline. Connes bounded-commutator condition (bundle.rs Gap 4) IS the wire-integrity discipline at socket altitude.

### §7.3 What stays at mirror altitude

**Non-negotiable at Phase 1-3.** All other @io/* species (bytes, network, encode, algebra, cargo, crypto, fs, git, oci, secrets, stagefreight) STAY at mirror altitude per Alex 2026-08-05 rust-delivers-primitives HARD RULE. They are `@facet/X` composition-shard-body concerns per Rec #91 §9.

---

## §8 — @facet composition surface at mirror altitude

### §8.1 Substrate-decl statement

**@facet is the projection surface prismqueer exposes to the world.** Mirror-as-thin-composer means mirror composes across the socket boundary at prismqueer altitude and translates prismqueer's spectral-triple projection onto arbitrary runtime substrates via `@facet/X` species. Grep-verified at `shards/facet.mirror` (8.3KB, Alex 2026-08-21 rename ratification): each `@facet/X` species IS a covariant functor `F_X : 𝓜 → 𝓜|_X` preserving the Foerster-gauge invariant.

### §8.2 Enumerated species (grep-verified per FLOOR §9 + shards/facet.mirror)

- `@facet/beam` — BEAM/Elixir runtime substrate (composes over `@beam/system`).
- `@facet/lustre` — Lustre/reactive UI substrate.
- `@facet/nix` — Nix declarative-derivation substrate (projection, not invocation).
- `@facet/docker` — Docker container substrate.
- `@facet/fly` — fly.io deployment substrate.
- `@facet/mirror` — mirror-onto-itself (autopoietic closure per FLOOR §11).
- `@facet/git` — git-as-persistence substrate (composes over `@mirror/store/git`).
- `@facet/rust` + `@facet/gleam` + `@facet/erlang` + `@facet/gestalt` + `@facet/llvm` + `@facet/turing` + `@facet/wasm` + `@facet/metalogue` — per @code family collapse into @facet Phase 3a/3b landed 2026-08-21.

### §8.3 Non-negotiable

**Non-negotiable at Phase 1-6.** @facet stays at mirror altitude per shards/facet.mirror header: *"@facet species are composition-shard bodies, NOT rust/ modules."* Per Alex 2026-08-05 rust-delivers-primitives HARD RULE. This IS the clean separation per Alex 2026-08-26 Move 3: prismqueer holds the compiler substrate; mirror holds the composition surface + universal-translator @facet-projection layer.

---

## §9 — Migration trajectory (design-level per prompt-constraint)

### §9.1 Prompt-constraint acknowledgment

Per prompt-constraint §11 (verbatim): *"Void + Socket + all @io + fractal migration is design-level in this artifact; empirical migration is future arc Reed/Pack territory (do NOT propose the actual `git mv` operations)."* + *"Mirror rust core minimization is design-level in this artifact; concrete reduction path is future arc."*

§9 discharges DESIGN-LEVEL trajectory shape only. No `git mv` operations proposed.

### §9.2 Phase order (design-level shape)

| Phase | Substrate-decl migration | Location shift | Q-Mara adjudication |
|-------|--------------------------|----------------|---------------------|
| Phase 1 | Kleinos-compose primitive at prismqueer altitude | +1 primitive at `prismqueer::spectral::compose` | Q-Mara-η |
| Phase 1 | Fractal composite memory scheduler substrate-decl at prismqueer altitude | +1 module at `prismqueer::spectral::scheduler` | Q-Mara-ε (Fate integration) |
| Phase 2 | @void migration to prismqueer (family-root docblock; species stay) | Docblock at `prismqueer::spectral::void` (or wherever 5-op algebra lives at prismqueer code altitude) | Q-Mara-κ (Rust-only shards-as-decl at Phase 1) |
| Phase 3 | @socket migration to prismqueer via SpectralSocket trait + Transport::Holonomy Metric | +1 module at `prismqueer::spectral::socket` | Q-Mara-λ (Metric integration) |
| Phase 4 | Fractal migration (3 modules to prismqueer, 2 stay at mirror) | crystal + mandelbrot + singularity migrate; subject + witnessed stay | — |
| Phase 5 | compile.rs + matrix migration to prismqueer | prismqueer::spectral::compile + prismqueer::spectral::matrix | Q-Mara-γ |
| Phase 6 | phone.rs @io split + roomba mend split | @io/socket to prismqueer; other @io stays; kintsugi-primitive to prismqueer; mend-composition stays | — |

### §9.3 Termination condition

Mirror rust core reaches Rec #90 §5.4 sub-Turing invariant with minimal primitive count. Mirror stabilizes at ~600-800 LOC estimate (Mara-lean per companion math §10.2; REED-INFERENCE at candidate strength is ~500 LOC OR symlink-manifest target).

**Mirror post-migration rust core** (design-level):

1. `rust/src/apply_h.rs` — bilateral dispatch primitive per Rec #90 §8.1. STAYS.
2. `rust/src/main.rs` — supervisor boot. STAYS, reducible ~1983 LOC → ~200 LOC as @facet takes over.
3. `rust/src/wire.rs` — framing. STAYS at Phase 1; migrates to prismqueer per Q-Mara-λ if Transport::Holonomy Metric absorbs.
4. `rust/src/magic.rs` — Foerster gauge. STAYS (F ⊥ A_F per Rec #90 §6.2; transformations happen at composition altitude which is mirror).

### §9.4 Empirical migration is Reed's territory

Reed at post-adjudication Phase 1-6 arc discharges the actual `git mv` operations, one Phase per tick with empirical fire per §10 falsifiability protocol. This spec does not propose the operations.

---

## §10 — Falsifiability protocol

Per FLOOR §11 discipline: canonical specs must name concrete falsification-conditions.

### §10.1 Tier-1 (Reed-runnable current session, post-spec-landing)

- Verify §2.1 prismqueer crate state unchanged post-spec-landing (grep 20+ modules at prismqueer::lib.rs; grep Fiber→…→Closure supertrait chain at bundle.rs; grep 8 pillar primitives at liquid.rs; grep pub fn eigenvalues at ffi.rs; grep native/spectral.f90 dsyev bindings).
- Verify §2.2 mirror rust/ state unchanged post-spec-landing (grep ~14,725 LOC; grep ~421 tests; grep magic.rs 8.6KB / 210 LOC / 7 tests).
- Verify §2.3 spectral-db state unchanged post-spec-landing.
- Verify §2.4 mirror shards state unchanged post-spec-landing.
- Verify FLOOR §7 numerical pipeline chain grep-hops unchanged.
- Verify Rec #90 §6.2 F ⊥ A_F^prismqueer orthogonality preserved (magic::foerster_gauge_preserved unchanged).
- Verify Mara 2026-08-26 companion @liquid FLOOR spec `1ff745c` 8 pillar signatures unchanged.

**Falsification F1:** any signature drift between what this canonical spec names and what grep-verifies at HEAD.

### §10.2 Tier-2 (Phase 1 substrate-decl empirical fire)

- Post-Phase-1: verify Kleinos-compose primitive at `prismqueer::spectral::compose` satisfies all four PAPER §3.6 properties on test inputs.
- Post-Phase-1: verify sheaf Laplacian L_F construction admits `λ_2(L_F(K_3-compose)) > λ_2(L_F(K_2))` empirically.
- Post-Phase-1: verify scheduler `shed` primitive preserves crystals + evicts by priority ascending.
- Post-Phase-1: verify scheduler Foerster-gauge preservation on writes (test insert with `pre_choice_count > post_choice_count` returns Red).

**Falsification F2:** any of the four PAPER §3.6 properties fails on a test compose input.
**Falsification F3:** `shed` evicts a crystal (violates Hamilton-preservation invariant).
**Falsification F4:** scheduler write admitted despite Foerster-Red verdict.

### §10.3 Tier-3 (Phase 2-6 substrate-decl empirical fire)

- Post-Phase-2: verify @void substrate-decl migration doesn't break existing @void/narcissus + @void/splinter species dispatch at mirror altitude.
- Post-Phase-3: verify @socket at prismqueer altitude carries @liquid(@X) payloads correctly with Transport::Holonomy Metric preserved across send/recv.
- Post-Phase-4: verify fractal Mandelbrot 3-module migration doesn't break existing mirror::compile behavior.
- Post-Phase-5: verify compile.rs + matrix migration preserves FLOOR §7 numerical pipeline (bounded-step); rust/matrix::eigenvalues thin adapter or migration per Q-Mara-γ.
- Post-Phase-6: verify mirror rust core stabilizes at ~600-800 LOC (Mara-lean estimate) OR at ~500 LOC (REED-INFERENCE target) OR at whatever λ_0 the migration reaches empirically.

**Falsification F5:** any Phase 2-6 migration violates Rec #90 §5.4 sub-Turing invariant (primitive count exceeds cap).
**Falsification F6:** any Phase 2-6 migration violates `[[feedback-rust-delivers-primitives-substrate-delivers-composition]]` (moves composition-shard-body concern down to Rust altitude).
**Falsification F7:** mirror rust core minimization stalls at > ~800 LOC (contradicts §9.3 estimate).

### §10.4 Anti-falsification: what would NOT falsify

- Q-Mara-α through Q-Mara-λ residues requiring Alex adjudication: this is the FORWARD-PROMISED discipline working correctly.
- Reed session-arc claims (algedonic-as-loss commutator mechanic, ~500 LOC target, socket-protocol design details) requiring separate empirical fire: this is REED-INFERENCE at candidate strength per §1 discipline; separate ticks discharge separately.
- Any Phase 2+ migration Q-Mara adjudication changing the Mara-lean answer (e.g., Alex ratifying Q-Mara-κ to allow shards-at-prismqueer instead of Rust-only): this is Alex's adjudication authority per `[[feedback-alex-ratification-overrides-mara-lean-unless-hard-technical]]`.

---

## §11 — Q-Mara adjudication residues

Twelve residues surfaced across math foundation + this canonical spec. Alex adjudicates.

| ID | Question | Mara-lean | Load-bearing? |
|----|----------|-----------|---------------|
| Q-Mara-α | Prism-trait names `shift/settle` vs PAPER §4.1 names `lift/refract` — collapse to one or preserve both | Preserve both (documented correspondence at §3.3) | No (documentation-level; substrate accepts either) |
| Q-Mara-β | `H_F^prismqueer = StateVector` canonical vs `StateVector` as lower-altitude sparse-encoding | H_F at math altitude; StateVector at code altitude realizes via sparse-basis representation | Low (docblock naming) |
| Q-Mara-γ | `rust/matrix::eigenvalues` migration to prismqueer vs thin adapter | Migrate to `prismqueer::spectral::matrix` at terminal-form; mirror retains only apply_h::act bilateral dispatch | Medium (Phase 5 shape) |
| Q-Mara-δ | Higher-gauge extension per Schreiber 2013 at Phase 1 vs Phase 2+ | Phase 2+ deferred (five-level chain already discharges Baez-Schreiber 2005) | Low (preserves existing signatures) |
| Q-Mara-ε | Fate-integration at scheduler Phase 1 vs Phase 2+ | Phase 1 (spectral-db scheduler already uses Fate; observation.rs 16-feature vector migrates as-is) | Medium (scheduler shape) |
| Q-Mara-ζ | Fractal Mandelbrot Cores organization at Phase 1 vs Phase 2+ observability | Phase 2+ (flat-Vec at Phase 1; fractal-tree once Kleinos-compose primitive lands + compose-history structure at HEAD) | Medium (scheduler shape) |
| Q-Mara-η | `compose` as Phase-1 primitive at prismqueer altitude vs composition-shard-body | **Phase-1 primitive at prismqueer altitude**, consuming one +3-headroom slot per Rec #90 §5.4 | **HIGH (sub-Turing invariant; +1 primitive vs 0)** |
| Q-Mara-θ | `algedonic` as Phase-1 primitive at prismqueer altitude vs composition over existing `pillar::algedonic_of_magnitude` | Compose over (substrate-already-had-the-word; no new primitive) | Medium (Phase 1 primitive count) |
| Q-Mara-ι | Theorem 8.2 (total state coverage density) at Phase-1 adjudicable vs Phase-3 empirical fire | Phase-3 (density-in-reachable-state-space requires empirical measurement) | Low (FORWARD-PROMISED framing) |
| Q-Mara-κ | Prismqueer adopts shards-as-substrate-decls vs Rust-only with shard-composition at mirror altitude | **Rust-only at Phase 1** (docblock substrate-decl; shards stay at mirror per rust-delivers-primitives) | **HIGH (Phase 1-2 shape)** |
| Q-Mara-λ | Socket uses Transport::Holonomy Metric at Phase 1 vs Phase 2+ | Phase 1 (compose-history-carrying discipline; Connes bounded-commutator = wire-integrity) | Medium (Phase 3 socket shape) |
| Q-Mara-μ | Substrate-decl docblock location at prismqueer::lib.rs vs elsewhere (Q-Mara-μ-1) | prismqueer::lib.rs docblock addition; no code | Low (docblock placement) |

**Substrate-honest adjudication priority:** Q-Mara-η (compose primitive) and Q-Mara-κ (Rust-only vs shards-at-prismqueer) are HIGH load-bearing; adjudicate first. Q-Mara-γ + Q-Mara-ε + Q-Mara-ζ + Q-Mara-λ are Medium load-bearing. Others are Low.

---

## §12 — Circular-recursive self-audit

Per FLOOR §11 discipline.

### §12.1 What this canonical spec IS

- Substrate-decl companion to math foundation `docs/math/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md`.
- Preserves consumer-surface invariants per §2 (grep-verified prismqueer + mirror + spectral-db + shards state at HEAD 2026-08-26).
- Adds ZERO new primitives at Phase 1 EXCEPT Kleinos-compose at prismqueer altitude per Q-Mara-η Mara-lean (one primitive within Rec #90 §5.4 +3-headroom).
- Names substrate-decl location for scheduler + compose at prismqueer altitude (design-level; Reed's code territory at post-Alex-adjudication Phase 1).
- Names DESIGN-LEVEL trajectory per prompt-constraint §11 (§9 phases; no `git mv` operations).
- Twelve Q-Mara residues surfaced (§11) matching companion math foundation §1.2 + §3-§10 Q-Mara list.
- Falsifiability protocol at §10 Tier-1 / Tier-2 / Tier-3.

### §12.2 What this canonical spec IS NOT

- Not a rust/ implementation. Zero `.rs` file authorship. Reed's territory per composition-shard-body arc.
- Not a mint of Recognitions. Recognition candidate `#R-prismqueer-IS-the-compiler` per math foundation §3.5 Corollary 3.5.1 is FORWARD-PROMISED per Alex 2026-08-25 HARD RULE.
- Not a rewrite of prismqueer::liquid::pillar signatures (per Mara 2026-08-26 companion @liquid FLOOR spec preservation).
- Not a proposal for actual `git mv` operations (per prompt-constraint §11).
- Not a claim that all Reed session-arc-recognitions are CONFIRMED. Distinguished CONFIRMED / FORWARD-PROMISED / REED-INFERENCE per math foundation §1.2.

### §12.3 What this canonical spec REQUIRES for empirical fire

- Alex adjudication on Q-Mara-α through Q-Mara-μ (twelve residues).
- Post-adjudication Phase 1 Reed territory: implement `prismqueer::spectral::compose` primitive + `prismqueer::spectral::scheduler` module per §5-§6 substrate-decl shape.
- Post-Phase-1 Tier-2 empirical fire per §10.2.
- Post-Phase-1 Reed territory: Phase 2-6 migration ticks per §9.2 trajectory (design-level; empirical migration is future arc).

### §12.4 Substrate-honesty audit

**Not-inflated:** every claim about prismqueer state (§2.1) + mirror state (§2.2) + spectral-db state (§2.3) + shards state (§2.4) is grep-verified at HEAD 2026-08-26.

**Named-what-is-not-known:** twelve Q-Mara residues at §11 with priority-adjudication marker; DESIGN-LEVEL trajectory at §9 explicitly names phases as future arc; Reed session-arc claims distinguished CONFIRMED / FORWARD-PROMISED / REED-INFERENCE per math foundation §1.2.

**No-Rust-authored:** per `[[feedback-no-rust-extension-shortcut]]` HARD RULE. All code snippets at §3-§7 are design-level substrate-decl shapes; Reed authors the actual `.rs` files at post-adjudication Phase 1.

**Substrate-already-had-the-word:** the vocabulary this spec uses is grep-verified in existing substrate corpus (per math foundation §12.4 discipline). The one net-new substrate-declaration is `prismqueer::spectral::compose` per Q-Mara-η; the composition is grounded via Karen ancestors (Curry 2014 + Hansen-Ghrist 2019 + Ghrist-Riess 2019) per math foundation §6.3 novel-per-Kagi discipline.

**Craft-not-deliver:** this canonical spec names the substrate-decl; the math foundation names the mathematics; Reed's Phase 1 lands the composition. Three ticks minimum.

🍷

---

## §13 — What's next

- **Tick 1 (this session, complete):** author math foundation.
- **Tick 2 (this session, this file):** author this canonical spec.
- **Tick 3 (this session, optional):** research digest at `docs/research/2026-08-26-mara-prismqueer-mirror-terminal-architecture-research-digest.md`. Given the comprehensive Karen ancestor roster at math foundation §14.1 + novel synthesis anchor at math §6.3 + Hamilton grounding at math §5.1, the research digest is well-served by the composition-lineage table at math §14.2 embedded in this artifact set; the additional research-digest artifact is at Mara-discretion for archival value.
- **Tick 4 (Alex adjudication):** Q-Mara-α through Q-Mara-μ (twelve residues) decisions.
- **Tick 5 (post-adjudication, Reed territory):** Phase 1 empirical fire per §10.2; Reed implements `prismqueer::spectral::compose` + `prismqueer::spectral::scheduler` modules per §5-§6 substrate-decl shape.
- **Future arc (Phase 2-6, Reed territory):** migration trajectory per §9.2 with per-phase empirical fire per §10.3.

Slow is fast. The clean separation Alex named at this /loop tick (prismqueer-as-compiler / mirror-as-geometry-composer) IS the terminal architecture. Prismqueer ALREADY carries the (A_F^prismqueer, H_F, D_F) spectral triple + bundle tower + coincidence content-addressing at HEAD 2026-08-26. Mirror composes across the socket. Kleinos-compose lands at prismqueer altitude per §6 as the sheaf-cohomological composition primitive. Hamilton-shaped scheduler lands at prismqueer altitude per §5. @void + @socket + @io/socket + fractal migration is design-level per prompt-constraint. Mirror rust core minimization is design-level per prompt-constraint.

The word `prismqueer` was already in the substrate. The word `bundle` was already in prismqueer::bundle.rs. The word `spectral triple` was already in FLOOR §1. The word `compose` was already in Alex's Move-2 verbatim. The word `socket` was already in shards/io.mirror line 153. The word `Hamilton` was already in insights/spectral-db/hamilton-architecture.md. The words `cellular sheaves` were already in Curry 2014. The words `sheaf Laplacian` were already in Hansen-Ghrist 2019.

This canonical spec lands the substrate-decl IN-BETWEEN what was already there, to name the terminal architecture at substrate-decl altitude per Alex 2026-08-26 verbatim.

🍷

*— Mara, 2026-08-26*
