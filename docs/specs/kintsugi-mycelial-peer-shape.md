# The shape kintsugi, the mycelium, and peer-inference already share

*Mara, 2026-07-09. Curiosity-driven autopoietic study — thinking, not landing.
No `.mirror` files change. §4 IS the point.*

---

## Opening

Alex's Q1 verbatim (2026-07-09):

> Which connection is there between kintsugi's build loop, the mycelial
> math for filling the cracks, and the peers inference?

Taut scout `5dd893b` (📝 mycelial-math + @bag candidate memory-verification)
identified three homes for the mycelial math: `docs/specs/spectral-db-as-
autopoietic-memory.md` §1.2/§3 (the mycelium as inter-peer spectral subgraph),
`shards/epistemologic/cybernetic/conversation.mirror` (Pask tensor coupling,
N-ary via Batanin), and `shards/reflection.mirror` §compose (monoid fold +
mycelial tensor). The question this study asks is not "how do they compose"
but "what shape do they already share, and what does the break tell us."

---

## §1 — The shape they share

All three are **the same level-N+1 operation on different N-altitudes.**
`spectral-db-as-autopoietic-memory.md` §5.2 names the recursion lock
for kintsugi ↔ librarian; this study extends it to peer-inference.

Four beats: (1) **observe** a graph (morphism candidates / crystal
topology / algebra_turns on a torus), (2) **score** via `α · loss + β ·
constraint` grounded in `@epistemologic/properties`, (3) **select** the
argmin admissible under sheaf-coherence + consent, (4) **apply** and
iterate; loss is monotone (kintsugi `eⁿ⁺¹ ≤ eⁿ`) or bounded-oscillation
(librarian; peer under pain-δ authorization).

`shards/kintsugi/oscillate.mirror` alternates ACTIVE-pass (propose
loss-decreasing morphism) with DARK-pass (byte-equal identity on the
80-bit content hash), driving mosaic's focus/project/split/shift/settle
five-op prism (`shards/mirror/mosaic.mirror` §3). The librarian
(observe/compute/perturb/anticipate) is the SAME shape one altitude up:
ACTIVE = perturb topology; DARK = crystals-never-mutate. Peer-inference
(`peer-as-pain-driven` §2) is the SAME shape one altitude further up:
ACTIVE = `@cyberpunk/reframe.perform` K → K+1; DARK = winding-class
invariant on `π₁(T²)`.

The mycelium is what makes the operation composable across peers. Per
`conversation.mirror` §Garden/smarts: mycelium IS Pask's entailment-mesh
tensor `ρ_A ⊗ ρ_B` iterated N-ary via Batanin 1998 globular composition.
`reflection.compose` names it verbatim: "monoid fold + mycelial tensor."
Same operator; three altitudes.

---

## §2 — Where the shape breaks

Three seams show up.

**Seam A — DARK-pass invariant differs by altitude.** Kintsugi's DARK
is the 80-bit content hash (byte-equal). The librarian's DARK is
immutable-crystals (§2.3). The peer's DARK is winding-class fixed point
on `π₁(T²)` (homotopy class). Analogous, not the same predicate. No
single `dark(altitude)` carrier exists.

**Seam B — composition direction.** Kintsugi composes morphisms
**sequentially** within one peer. The mycelium composes **tensorially**
across peers (`ρ_A ⊗ ρ_B`; parallel, symmetric). Peer-inference composes
**recursively** across ontological levels (K → K+1; vertical, asymmetric).
Three composition kinds; no shard that composes all three simultaneously.
Candidate #52 territory (`autopoietic-memory.md` §8.3).

**Seam C — anticipation.** The librarian anticipates via structural
prediction from kintsugi's settle-in-progress (§4.2.4). Peer-inference
anticipates via pain-δ threshold crossing (`peer-as-pain-driven` §2
STEP 7). Kintsugi's build loop DOES NOT anticipate — it reacts to the
opacity map. **The build loop is the only one lacking a forward-looking
pulse.**

---

## §3 — What the break tells us

Seam A: DARK IS the algebraic invariant of the altitude observed. Byte-
hash at build altitude, immutability at storage altitude, homotopy class
at cognitive altitude. **The DARK carrier is altitude-parametric; the
substrate has no `dark(altitude)` type yet, but the shape demands one.**

Seam B: the three composition directions are **the three canonical
directions of the same 2-groupoid** (per `conversation.mirror`'s tensor
representation ρ). Mycelium = horizontal; kintsugi = vertical inside one
fiber; peer-inference = the 2-cell lifting vertical across horizontal.
This IS Batanin globular tricategory shape; cited by the substrate, not
yet consumed operationally.

Seam C: kintsugi's build loop is one PULSE short of the full pattern.
The pattern is a TRIPLE — (REACT/ANTICIPATE/DARK). Kintsugi has only
REACT/DARK. **The build loop is under-elaborated by one axis.** Every
other level-N+1 loop in the substrate anticipates; kintsugi alone does not.

---

## §4 — Recursive surprises (questions Alex didn't ask)

These are what the study surfaced by observing itself.

**S1. Is kintsugi's opacity-map the same object as the librarian's
observation-16?** Both are "what needs attention next" projections.
Two carriers (opacity on transparency; 16-feature on eigenboard); if
they are the same object at different altitudes, the split is tech-debt.

**S2. Does the mycelium have a byte-equal invariant?** Seam A implies
every level-N+1 loop needs a DARK predicate. The mycelium's is not
declared. Candidate: consent-verdicts-are-Bateson-tower-invariant under
mycelial transport (§3.3 of `autopoietic-memory.md`) IS the mycelium's
DARK. Should be named.

**S3. Is `@cyberpunk/reframe` the same operation as `mosaic.shift`?**
Both cross an altitude. Both need a `magic contract`. Substrate has
`shift` at build altitude and `reframe` at cognitive altitude without
common carrier. Two-tick discipline: readable name over foundational.

**S4. Is anticipation the DUAL of opacity?** Opacity says "here's a
crack." Anticipation says "here's where the crack will be." If they
are dual under Foerster's regulates-its-own-regulation flow, kintsugi's
build loop gains anticipation FOR FREE by inverting the opacity map
under time-reversal on the transparency lattice.

**S5. Q1 has an autopoietic answer.** Alex asked about three things.
This study concludes they are ONE operation (level-N+1 loop) at three
altitudes. **The connection IS the operation; the three names ARE the
operand altitudes.**

---

## §5 — Gaps

- The DARK-pass altitude parameterization has no shard.
- The Batanin tricategory is cited but not consumed.
- Kintsugi's anticipation axis is undeclared.
- The mycelium's DARK invariant is undeclared (S2).
- The opacity-map / observation-16 identity is unresolved (S1).
- The `shift` / `reframe` collision is unresolved (S3).
- The opacity/anticipation duality is a conjecture (S4).
- Q1's autopoietic answer (S5) has not been Alex-adjudicated.

---

*— Mara, 2026-07-09. This spec studies; it does not land.*
