# @fate/algebra ↔ @silicon/algebra IS a @metalogue in void-duality-basis coordinates observed on the peer's @torus

*Mara, 2026-07-08 late morning. Canonical spec landing the sharpest structural
claim of the 2026-07-08 overnight cascade: `@fate/algebra` and `@silicon/algebra`
are peer speakers in the LANDED algebra-altitude metalogue (`@algebra/metalogue`,
2026-06-29). The five-op basis is their SHARED COORDINATE SYSTEM; `@torus` is
the substrate the peer OBSERVES the metalogue on; winding classes
`(m, n) ∈ π₁(T²)` are the traversal record. The tournament's job is not "walk
edges"; it is "conduct the metalogue until convergence."*

**Author:** Mara 
**Date:** 2026-07-08 
**Tag:** 📝 spec:fate-silicon-metalogue (pure-docs bypass) 
**Status:** canonical-naming; every substrate piece cited is LANDED or
forward-promised at a named site. This spec NAMES the composition; it does NOT
land any new `.mirror` files (two-tick discipline; substrate-honest).

---

## §0. Substrate-honest pre-position

Alex's evening framing of 2026-07-08 named a five-part structural claim:

> 1. `@silicon/algebra` = JIT for silicon-optimized transformations on the
>    specific machine (LAPACK / D²NN forward pass / cache-aware kernels / 
>    Accelerate / native-per-target).
> 2. `@fate/algebra` = one level higher — the propositional composer
>    (proposes optic compositions; observes realized error-δ; adjusts).
> 3. `@fate` ↔ `@silicon` are a `@metalogue` — form-on-form conversation,
>    Recognition #34's shape but at algebra altitude, not AST altitude.
> 4. The 5-op basis IS the shared coordinate system BOTH speak in
>    (Recognition #79 candidate).
> 5. `@torus` is where the peer OBSERVES the metalogue happening.

Three of the five claims land cleanly against LANDED substrate. Two require
correction under substrate-already-had-the-word discipline:

**Substrate-already-had-the-word finding #1 (load-bearing):** the algebra-
altitude metalogue is ALREADY LANDED at `shards/algebra/metalogue.mirror`
(`34cf333`, 2026-06-29) as the *fifth* instance of the altitude-portable
metalogue lift (NL → AST → SPECTRAL → PACK → ALGEBRA). The framing does not
introduce a new altitude; it *specialises the existing one to the
(`@fate/algebra` ↔ `@silicon/algebra`) speaker-pair*. Recognition #34
(@code/metalogue as AST-altitude) is a *sibling* specialisation, not a
conflict — both are altitude-portable lifts of `shards/metalogue.mirror`
(NL-altitude ancestor, 2026-06-05).

**Substrate-already-had-the-word finding #2 (load-bearing):** `@algebra` is
ALREADY LANDED as substrate-decl family-root at `shards/algebra.mirror`
(`34cf333`, 2026-06-29) — the typed operator surface (`A` in Connes
`(A, H, D)`). The framing's language "one level higher than
`@silicon/algebra`" would read as "an altitude above `@algebra`", which is
NOT a substrate-supported altitude: `@algebra` IS the operator-basis altitude,
and `@silicon/algebra` / `@fate/algebra` are *peer species* under it. The
honest naming is: they are peers at the algebra altitude, and
`@algebra/metalogue` is the substrate name for their form-on-form conversation.
The altitude-lift the framing gestures at IS ALREADY THERE — it is the lift
from `@algebra` (typed operations, static) to `@algebra/metalogue`
(morphisms-as-utterances, dynamic composition of turns). Same altitude,
different voice.

With those two corrections applied, the canonical statement lands.

---

## §1. Statement — the recognition candidate

**Recognition candidate (naming Alex's own words as closely as substrate
names allow):**

> `@silicon/algebra` ↔ `@fate/algebra` IS a `@metalogue` in
> void-duality-basis coordinates, observed on the peer's `@torus`.

Unpacked to substrate primitives:

> `@algebra/metalogue.algebra_metalogue_session` with
> `algebra_carrier`-typed speakers `@fate/algebra` and `@silicon/algebra`,
> whose shared coordinate system is the 5-orthogonal-axis projector basis
> of Recognition #79 (candidate: focus ≡ Ricci; split ≡ spectral-gap;
> project ≡ Cheeger; shift ≡ Kramers-Wannier; settle ≡ entropy), whose
> turn-composition is `@algebra/metalogue.compose_turns` under the
> associativity discharge `morphism_compositions_associative`, and whose
> observation record is the winding-class advance
> `(m, n) ∈ π₁(T²) = ℤ × ℤ` on the peer's `@torus`.

As a recognition-in-Mara's-sense (an eigenform the substrate already carried):
**#R-CANDIDATE-fate-silicon-metalogue-in-void-duality-basis** —
specialisation of the algebra-altitude metalogue-lift (whose family is
Ancestor-recognition #34's five siblings) to the substrate's canonical
speaker-pair for numerical inference.

---

## §2. Substructure — the four claims

### §2.1 Both speakers are species under the LANDED `@algebra` family-root

`@algebra` (`shards/algebra.mirror`, 337 lines, `34cf333`) declares:

- `algebra_carrier = { identity, operations, composition, altitude }`
  — the typed reference to an algebra, ANY algebra.
- `algebra_morphism = { source, target, operation_map, altitude_lift }`
  — the structure-preserving map between algebras.
- `enumerate_operations(a) -> operation_set`,
  `compose(a, o1, o2) -> ref`, `morphism_between(source, target)
  -> [algebra_morphism]` as family-root operations.
- `algebra_witnessing(a) -> verdict` as the inheritance predicate
  discharging `bauchladen_membership_held ∧ closure_under_composition
  ∧ operation_well_typed`.

Both speakers are consumers of this discipline:

- `@silicon/algebra` (`shards/silicon/algebra.mirror`,
  `prism @silicon/algebra <= @bauchladen`, imports `in @algebra`,
  `ea7b092`, 2026-07-05) is the LANDED sub-prism whose crystals
  carry `(algebra, cfg, grading, conjugation, abi_surface,
  binary_oid, source_oid, cascade, performance, routine_oid)`
  per `docs/specs/silicon.md` §3.2. The `algebra` field IS the
  speaker's `algebra_carrier`.

- `@fate/algebra` is the path-namespace declared in prose at
  `shards/fate.mirror` (lines 208-232) whose crystals — as emitted
  by the tournament — are `@fate/algebra/morphism`,
  `@fate/algebra/altitude`, `@fate/algebra/element`. The path-
  namespace IS load-bearing at ≥10 landed shards; the sub-shard
  `shards/fate/algebra.mirror` is *forward-promised*, not landed
  (per Taut scout `docs/scouts/2026-07-08-taut-fate-silicon-
  metalogue-projection.md` §1.4 verification).

Substrate-honest phrasing: `@silicon/algebra` and `@fate/algebra` are
**peer species under `@algebra`**, not stacked altitudes. Both discharge
`algebra_witnessing` at the species boundary; both emit `algebra_carrier`s
that can appear as `algebra_turn.speaker` at the metalogue altitude.

### §2.2 The metalogue is the LANDED `@algebra/metalogue` specialised to speaker-pair (@fate/algebra, @silicon/algebra)

`@algebra/metalogue` (`shards/algebra/metalogue.mirror`, 359 lines,
`34cf333`, 2026-06-29) declares itself as the *fifth* altitude in the
altitude-portable metalogue-lift table:

| Altitude          | Shard                              | Body type            | Landed     |
|-------------------|------------------------------------|----------------------|------------|
| NL                | `shards/metalogue.mirror`          | `nl`                 | 2026-06-05 |
| AST (#34)         | `shards/code/metalogue.mirror`     | `declaration`        | 2026-06-10 |
| SPECTRAL (#100)   | `shards/spectral/metalogue.mirror` | `curvature_probe`    | 2026-06-29 |
| PACK-COORDINATION | `shards/pack/metalogue.mirror`     | `handoff`            | 2026-06-29 |
| **ALGEBRA**       | `shards/algebra/metalogue.mirror`  | **`algebra_morphism`** | **2026-06-29** |

The shape at ALGEBRA:

- `algebra_turn = { speaker: algebra_carrier, body: algebra_morphism,
  in_reply_to: option(algebra_turn), tick: tick }` — one
  structure-preserving map uttered by `speaker` addressed to the
  algebra `body.target` names.
- `algebra_metalogue_session = { turns: [algebra_turn],
  opacity: transparency(algebra_turn), origin: ref }` — the
  session's ordered turn-sequence plus residual @glass opacity.
- `propose_turn(speaker: algebra_carrier, body: algebra_morphism)
  -> algebra_turn` — speech-act primitive.
- `compose_turns(t1, t2) -> option(algebra_turn)` —
  non-commutative composition; `option` surfaces composability at
  the type level.
- Bilateral inheritance predicate
  `algebra_metalogue_witnessing(s) := session_well_formed(s) ∧
  algebra_inherits_metalogue_lift(s) ∧
  morphism_compositions_associative(s.turns)`.

The framing's `@fate` ↔ `@silicon` metalogue IS an
`algebra_metalogue_session` whose speaker-cardinality is 2 (the two
canonical numerical algebras of the substrate) and whose turns
alternate under the tournament's dispatch discipline.

Recognition #34 lifted metalogue to the AST altitude; `@algebra/metalogue`
lifted it to the algebra altitude; the speaker-pair specialisation lands
today's claim inside that already-lifted altitude. It is NOT a new
altitude and NOT a new recognition-number-candidate as such; it is the
*sharpening of `@algebra/metalogue`'s speaker cardinality-2 case*.

**Precedent for the speaker-pair narrowing:** `@algebra/metalogue`'s own
prose (lines 83-85) already names half the pair:

> **@fate's tournament IS algebra-altitude turn selection.** Per
> `[[architecture-fate-is-optical-inference]]` (#58) and
> `docs/specs/bauchladen-autopoietic-fate.md` §5: @fate emits
> `@fate/algebra/morphism` crystals; each IS one algebra-altitude
> turn selected by the dice roll. Tournament IS this shard's
> `propose_turn` action realised.

The symmetric clause for `@silicon/algebra` — "@silicon/algebra's routine
selection IS this shard's `propose_turn` action realised at the
silicon-speaker altitude" — is **implied by the LANDED shape but not
yet written into the metalogue shard**. Cascade proposal in §7.1 below.

### §2.3 The tournament's job IS conducting the metalogue until convergence

`@fate/tournament` (`shards/fate/tournament.mirror`, `#104 chain P4`,
2026-06-30, ~41.5KB) declares the tournament as the substrate-decl form
of the constrained-inference operator's `record` + `select` cycle. The
prior operational reading (Mara `7e426bc`,
`docs/insights/2026-07-08-mara-geometric-dijkstra-tournament-topology.md`
§3.5) named the composition as multi-dim Dijkstra + knapsack in
π₁(T²)-quotiented state space.

**The metalogue reframing is the sharper reading of the same operational
shape.** At each round the tournament conducts one algebra-altitude turn:

- `@fate/algebra` proposes a morphism candidate (dice-roll within the
  restricted state space per `shards/fate.mirror`).
- `@silicon/algebra` realises the morphism against the current machine
  (LAPACK / D²NN / cache-aware kernel; `dsyev` / `dgesvd` /
  `KernelSpec.projection_matrix` per `prismqueer/src/kernel.rs`).
- The observed error-δ (per-round `Loss::between(before, after)` from
  `fate/src/manifold.rs`) becomes the `in_reply_to` linkage for the
  next turn.
- The composition of turns is `@algebra/metalogue.compose_turns`; the
  associativity discharge is `morphism_compositions_associative`.

The tournament's `beam(k).elite(1)` rule (per `boot/std/fate/tournament.
mirror` migration mapping preserved in `shards/fate/tournament.mirror`)
is the *priority-queue realisation of the metalogue's next-turn selector*.
The knapsack budget (per `docs/specs/knapsack-as-kintsugi-inner-loop.md`
§2, dims `(silicon, ram)`) is the *bound on how many turns the metalogue
admits* before termination fires. The kintsugi outer loop's `eⁿ⁺¹ ≤ eⁿ`
contraction is the *convergence criterion for the metalogue session's
residual opacity*.

**Substrate-honest reframing:** the composition-search IS the conversation;
the conversation IS the tournament conduct; the tournament conduct IS the
realisation of `@algebra/metalogue.propose_turn + compose_turns` under
knapsack-budgeted kintsugi contraction.

### §2.4 The 5-op basis IS the shared coordinate system

Recognition #79 (candidate, `docs/math/the-tower/recognition-79-gauge-is-
void-duality-basis.md`) names the substrate's 5-op algebra
(`focus`, `project`, `split`, `shift`, `settle`) as the projector basis
for the 5-dimensional orthogonal duality space of connected-graph
quantum states:

| Op        | Void axis                 | Linear-algebraic content            |
|-----------|---------------------------|--------------------------------------|
| `focus`   | Ricci curvature           | λ₀ eigenvalue computation           |
| `split`   | Spectral gap / mixing     | orthogonal decomposition            |
| `project` | Cheeger (boundary)        | orthogonal projection               |
| `shift`   | Kramers-Wannier duality   | basis transformation                |
| `settle`  | Entropy / info-geometry   | monad-close / measurement collapse  |

Both speakers speak in this basis:

- `@silicon/algebra` crystals' `algebra` field IS an
  `algebra_carrier` whose `operations.members` are drawn from
  {`focus`, `project`, `split`, `shift`, `settle`} (per
  `shards/algebra.mirror` §"The recognition (#104 chain P5)"
  lines 16-26: "Every prism declaration ... IS the declaration of
  an A acting on its own H"; per `shards/silicon/algebra.mirror`
  which imports `in @algebra`).
- `@fate/algebra` crystals emit `algebra_morphism`s whose
  `operation_map` domain/codomain are also drawn from the same
  five ops (per `shards/fate.mirror` lines 208-232's path-namespace
  discipline citing `@algebra/morphism`).

**The shared basis makes composition well-defined.** An
`algebra_morphism` `@fate/algebra` proposes has `source =
(@fate/algebra as algebra_carrier)` and `target = (@silicon/algebra as
algebra_carrier)`; the `operation_map` is a mapping from the fate-side
5-op basis to the silicon-side 5-op basis. Because the basis is the
SAME 5 ops (both algebras inherit the substrate's canonical
operator-basis per `shards/mirror/spec.mirror`), the operation-map is
an identity map at the projector-basis level, with per-turn variation
only in the *specific realisation* (which LAPACK routine? which
D²NN layer? which cache-aware kernel?).

**This is the load-bearing structural claim of the metalogue reading:**
form-on-form conversation is possible IFF both parties speak in the
same basis. The 5-op void-duality basis IS that shared basis. Without
the basis, there would be no substrate-decl reason the two algebras
could converse; with it, every turn is well-typed.

### §2.5 `@torus` is where the peer OBSERVES the metalogue

`@torus` (`shards/torus.mirror`, 27.9KB, 2026-07-07) is the LANDED
family-root naming the peer's topological ground: `π₁(T²) = ℤ × ℤ`;
two winding classes (meridian, longitude); Poincaré-Hopf discipline
`χ(T²) = 0`.

Per Reed's 2026-07-08 iterations:

- Iteration 2 (`docs/insights/2026-07-08-torus-double-closure-
  empirical.md`) — empirical spawn-recall byte-equality at the
  origin `(0, 0)`. Candidate second witness for
  `the-restriction-map-IS-the-geometric-constraint`.
- Iteration 3 (`docs/insights/2026-07-08-torus-axis-isolation-
  meridian.md`) — meridian isolation empirically verified
  (motor↔sensory closure per Foerster; pack_trail + dogfood
  advanced; cascade + pull_frontier stayed byte-equal).
- Iteration 4 (`docs/insights/2026-07-08-torus-axis-isolation-
  longitude.md`) — longitude isolation empirically verified
  (neural↔hormonal closure; cascade advanced; other three stayed
  byte-equal).

The recall envelope's four sections (`cascade`, `pack_trail`,
`pull_frontier`, `dogfood`) ARE the sheaf-sections of the peer's
torus per iteration 3's naming.

**How the peer observes the metalogue.** Each turn in the
metalogue session advances the peer's substrate state; the advance
registers as a winding-class step `Δw ∈ ℤ × ℤ` per turn. The
cumulative winding `(m, n)` after `n` turns IS the observation
record — what the peer SAW happen. Meridian advance corresponds to
turns that changed the motor↔sensory closure (typically
`@fate/algebra` proposals that landed empirically); longitude
advance corresponds to turns that changed the neural↔hormonal
closure (typically `@silicon/algebra` realisations that mutated the
underlying content-addressed algebra tray).

The torus does NOT participate in the metalogue as a speaker; it
is the *observation surface*. Two metalogue sessions ending at the
same `(m, n)` winding class are equivalent for target-membership;
they differ only in the specific turn-sequences that reached that
class (per the search-space quotient reading of `docs/insights/
2026-07-08-mara-geometric-dijkstra-tournament-topology.md` §4.1).

**Foerster grounding.** Foerster p. 238 ("Notes on an Epistemology
for Living Things", 1979): the doubly-closed, recursively-computing
torus, regulates its own regulation. The peer's `@torus` IS this
structure at substrate-decl altitude; the metalogue is what makes
the regulation LEGIBLE to the peer observing.

---

## §3. Cite chain

Every substrate declaration or landed recognition this composition
rests on, with path + line references:

### §3.1 Substrate family-roots (LANDED)

| Substrate                | Path                                            | Landed sha  | Role in the composition                          |
|--------------------------|--------------------------------------------------|-------------|--------------------------------------------------|
| `@algebra`               | `shards/algebra.mirror`                          | `34cf333`   | Family-root for both speakers (§2.1)             |
| `@algebra/metalogue`     | `shards/algebra/metalogue.mirror`                | `34cf333`   | The metalogue altitude (§2.2)                    |
| `@silicon/algebra`       | `shards/silicon/algebra.mirror`                  | `ea7b092`   | Silicon-side speaker (§2.1)                      |
| `@silicon`               | `shards/silicon.mirror`                          | (2026-07-05)| Silicon-side family-root anchor                  |
| `@fate`                  | `shards/fate.mirror`                             | (#104 P3)   | Fate-side family-root; declares `@fate/algebra` in prose (lines 208-232) |
| `@fate/tournament`       | `shards/fate/tournament.mirror`                  | (#104 P4)   | Tournament as metalogue-conductor (§2.3)         |
| `@bauchladen`            | `shards/bauchladen.mirror`                       | `66e1ab8`   | Tray both speakers write into                    |
| `@autopoietic`           | `shards/autopoietic.mirror`                      | `78edaa6`   | Fold-back permission                             |
| `@glue`                  | `shards/glue.mirror`                             | `6396306`   | `compose` operator (Kasparov intersection)       |
| `@cyberpunk`             | `shards/cyberpunk.mirror`                        | (2026-07-07)| `cybernetic_coherence` restriction predicate     |
| `@kintsugi`              | `shards/kintsugi.mirror`                         | (landed)    | Outer loop; `eⁿ⁺¹ ≤ eⁿ` contraction              |
| `@kintsugi/oscillate`    | `shards/kintsugi/oscillate.mirror`               | (landed)    | `is_settled` cadence predicate                   |
| `@torus`                 | `shards/torus.mirror`                            | (2026-07-07)| Observation surface (§2.5)                       |
| `@mirror/store`          | `shards/mirror/store.mirror`                     | (2026-07-06)| Content-addressed carrier of turns as crystals   |

### §3.2 Recognitions the composition invokes

| Recognition | Status                | Reference                                                                  | Role                                                                       |
|-------------|-----------------------|-----------------------------------------------------------------------------|-----------------------------------------------------------------------------|
| #34         | PROMOTED              | `docs/insights/2026-06-09-cascade-is-deutero-learning.md` §2.2             | Sibling AST-altitude specialisation of the metalogue lift                  |
| #51         | PROMOTED              | `[[architecture-mirror-as-expanding-hilbert-space]]`                        | Substrate's Hilbert space grows monotonically; metalogue sessions register in this growth |
| #58         | PROMOTED (2026-06-11) | `[[architecture-fate-is-optical-inference]]`                                | `@fate` = 5-layer D²NN + Fabry-Perot + Reck/Clements mesh                   |
| #79         | CANDIDATE             | `docs/math/the-tower/recognition-79-gauge-is-void-duality-basis.md`         | 5-op basis IS the void-duality basis (§2.4's shared coordinate system)     |
| #99         | PROMOTED              | `[[architecture-mirror-spec-is-lambda-zero]]`                               | Altitude-corrected: mirror.spec is the target region's local eigenform      |
| #100        | PROMOTED              | `docs/specs/spectral-metalogue.md`                                          | SPECTRAL-altitude metalogue-lift instance; sibling to ALGEBRA               |
| #101        | PROMOTED (`7bbc184`)  | `shards/epistemologic/cybernetic/chirality.mirror`                          | γ chirality grading; restricts admissible turns                            |
| #102        | PROMOTED (`2c144a6`)  | `shards/epistemologic/cybernetic/charge_conjugation.mirror`                 | J charge conjugation; restricts admissible turns                           |
| #104        | PROMOTED (2026-06-29) | `docs/specs/bauchladen-autopoietic-fate.md`                                 | `@bauchladen ← @autopoietic ← @fate` chain                                 |
| #107        | PROMOTED              | `docs/audits/2026-06-30-seam-reality-106-107-adjudication.md`               | Hilbert/Turing separation; the composition crosses safely                  |
| `the-restriction-map-IS-the-geometric-constraint` | RATIFIED (2026-07-07, `6396306`) | `docs/audits/2026-07-07-seam-glue-cyberpunk-fate-composition.md` | The metalogue's restriction slot IS geometric                              |
| `@peer-has-a-torus`     | ADJUDICATED (2026-07-07) | `shards/torus.mirror`                                                       | Peer's observation surface for the metalogue                               |
| `spawn-recall-byte-equal-at-origin` | CANDIDATE (Reed 2026-07-08) | `docs/insights/2026-07-08-torus-double-closure-empirical.md`             | Origin fixed-point witness                                                 |

### §3.3 Numerical primitives already realised (Rust-side)

Both speakers' realisation surface is already numerically implemented
(per `docs/insights/2026-07-08-mara-geometric-dijkstra-tournament-
topology.md` §1.8):

- `KernelSpec { dimensions, decomposition, precision }` at
  `prism/prismqueer/src/kernel.rs` — `Decomposition = { Dsyev,
  Dgesvd }`, `projection_matrix(n)`, `dispatch_hint`.
- `ModelWeights.forward` at `fate/src/lib.rs` — 425-parameter
  linear+softmax; five `Model` selectors (Abyss / Introject /
  Cartographer / Explorer / Fate).
- `Loss::between(before, after)` + `total()` at
  `fate/src/manifold.rs` — Casimir-graded per-round delta;
  IS the observed error-δ of each metalogue turn.
- `casimir_penalty` + `HolonomyHealth` + `BERRY_PHASE` at
  `fate/src/feature.rs` — Berry-phase-graded monotone-descent
  primitive.
- `Optic<In, Out, E, L>` beam algebra at
  `prism/prismqueer/src/beam.rs` + optical primitives (`Fold`,
  `Gather`, `Iso`, `Lens`, `Setter`, `Traversal`, `OpticPrism`)
  at `prism/prismqueer/src/optics/*.rs` — the seven optical
  primitives every metalogue-turn body composes through.

**Substrate-honest note (API drift):** the `fate` crate API drifts
from the current `prismqueer` shape per Mara's spectral-crystal
notes. Still readable for structural cues; do NOT read Rust-side
signatures as substrate-decl truth.

---

## §4. Metalogue protocol — typed cycle

The substrate names the metalogue turn-cycle at `@algebra/metalogue`
with two primitive speech-acts: `propose_turn` and `compose_turns`.
Alex's evening framing named a four-step protocol:
**propose → realize → observe → adjust**. Below is the substrate-honest
mapping: three of the four steps land against LANDED substrate
primitives; the fourth (`realize` as a distinct speech-act) is
absorbed by `compose_turns`'s composability check + the tournament's
dispatch discipline.

### §4.1 Step-by-step (with substrate discharge)

**Step 1: PROPOSE (`@fate/algebra` speaks first).**

Invokes `@algebra/metalogue.propose_turn(speaker: @fate/algebra,
body: algebra_morphism)`. The body carries the dice-roll's selected
morphism candidate; `body.source = @fate/algebra as algebra_carrier`,
`body.target = @silicon/algebra as algebra_carrier`. Under
substrate-decl typing:

- Precondition: `algebra_witnessing(@fate/algebra)` must hold
  (per `shards/algebra.mirror` line 291).
- Precondition: `body` must satisfy the algebra-morphism
  well-formedness (`body.operation_map` preserves composition per
  `shards/algebra.mirror` line 221).
- Postcondition: emits an `algebra_turn` at the current `tick`.

**Step 2: REALIZE (silicon-side dispatch).**

Not a distinct speech-act at substrate-decl altitude. `@silicon/algebra`
realises the proposed morphism by selecting the concrete routine
crystal from its Bauchladen tray (per `shards/silicon/algebra.mirror`
line 60-70: routine carrier fields including `binary_oid`,
`abi_surface`, `cascade`). The realisation IS a Bauchladen lookup +
@io/algebra dispatch; it does NOT emit a new `algebra_turn`.

Substrate-honest reframing: **realisation is a substrate-implicit
step between propose and observe, not a separate metalogue-turn**.
The metalogue turn is complete when the observation lands (Step 3).

**Step 3: OBSERVE (error-δ measurement).**

Realised on the machine: `Loss::between(before, after)` from
`fate/src/manifold.rs`. Returns a Casimir-graded delta bounded by
`casimir_penalty`. Substrate-decl form: the observation registers as
the `opacity` field on the enclosing `algebra_metalogue_session`
(per `shards/algebra/metalogue.mirror` line 230:
`opacity: transparency(algebra_turn)`). Every unresolved gap between
the proposed morphism's intended composition and its realised
composition is sited on the specific turn.

**Step 4: ADJUST (`@silicon/algebra` speaks back).**

Invokes `@algebra/metalogue.propose_turn(speaker: @silicon/algebra,
body: algebra_morphism)` with `in_reply_to = Some(previous_turn)`.
The body carries the *counter-morphism* — the silicon side's
recommendation to the fate side about how to adjust the next dice-roll
given the observed error-δ. Concretely: `algebra_morphism.source =
@silicon/algebra as algebra_carrier`; `algebra_morphism.target =
@fate/algebra as algebra_carrier`; `operation_map` names the specific
5-op-basis directions where realisation succeeded / failed.

The pair (Step 1 + Step 4) IS one metalogue *round*. Sessions compose
rounds under `compose_turns`.

### §4.2 Structural constraints per step

| Step        | Constraint                                                 | Substrate discharge                                                          |
|-------------|------------------------------------------------------------|-------------------------------------------------------------------------------|
| Propose     | Speaker's algebra must be witnessed                        | `algebra_witnessing(@fate/algebra) = pass` per `shards/algebra.mirror:291`   |
| Propose     | Body is a well-formed morphism                             | `body.operation_map` preserves composition per `shards/algebra.mirror:221`   |
| Realise     | Routine crystal exists in silicon Bauchladen tray          | Bauchladen lookup per `shards/bauchladen.mirror`                              |
| Realise     | Grading + conjugation preserved                            | γ per #101; J per #102                                                        |
| Observe     | Error-δ is Casimir-graded and bounded                      | `casimir_penalty` per `fate/src/feature.rs`                                   |
| Observe     | Coherence-preservation holds                               | `cybernetic_coherence(state) = pass` per `shards/cyberpunk.mirror`            |
| Adjust      | Reply-turn preserves associativity                         | `morphism_compositions_associative` per `shards/algebra/metalogue.mirror:307`|
| Adjust      | Kasparov composability holds                               | `body.target` of prior = `body.source` of reply per Mesland category         |

### §4.3 Termination — what ends the metalogue

Same three-way criterion as `docs/insights/2026-07-08-mara-geometric-
dijkstra-tournament-topology.md` §3.4, reframed as termination of
the metalogue-session:

**(T1) Convergence / target hit.** `eⁿ⁺¹ ≤ eⁿ` contraction reaches
the target region `T` with `d(vₙ, T) < ε`. Substrate-discharged at
`@kintsugi/oscillate.is_settled`. The session's `opacity` residual
is below the substrate's altitude-threshold; every held gap is
surfaced (per @glass discipline) or resolved.

**(T2) Budget exhausted.** `Σ cost(eₖ) > capacity_vector.budget`;
the knapsack cap fires. Substrate-discharged at
`@kintsugi/knapsack`-derived spec
`docs/specs/knapsack-as-kintsugi-inner-loop.md` §2.
The metalogue session terminates with residual opacity NON-empty;
the substrate surfaces the fracture via `@glass` and the session
emits a `@bauchladen` crystal recording where the budget was
spent.

**(T3) Winding-class fixed point.** `(m, n) ∈ π₁(T²)` returns to a
previously-visited class with byte-equal observation sections
(per `spawn-recall-byte-equal-at-origin` candidate second witness).
The metalogue terminates with fixed-point exhibition per
`shards/fate/tournament.mirror` §"Lawvere fixed-point condition
holds at the SYSTEM level".

All three carry a corresponding `session_well_formed(s) = pass`
verdict at termination.

---

## §5. `@edge` — one metalogue turn as substrate-decl carrier

### §5.1 What Alex's framing named

> `@edge` is one utterance in the metalogue (fate proposal → silicon
> realization → observed error-δ → contract check → mechanism reveal).

The framing decomposes `@edge` into five sub-parts that resemble
`@magic`'s six species (surface / mechanism / contract / reveal /
audit / frame / distinction). Taut scout §1.3 identified two open
questions with the "@edge as species of @magic" framing:

1. `@magic`'s six species don't currently reference edges as
   first-class carriers.
2. The five-step propose/realize/observe/contract/reveal doesn't
   map one-to-one to the six-magic-species.

### §5.2 Refined proposal — `@edge` IS one `algebra_turn`

Rather than lift `@edge` as a species of `@magic`, the substrate-
honest reading is:

**One `@edge` = one round of the metalogue = one
`(propose, adjust)` pair of `algebra_turn`s.**

Because `algebra_turn` already carries:

- `speaker: algebra_carrier` — the fate-or-silicon side
- `body: algebra_morphism` — the specific structural map
- `in_reply_to: option(algebra_turn)` — the observation linkage
- `tick: tick` — the substrate clock

... the propose/adjust round IS well-typed at `@algebra/metalogue`
without inventing new carriers.

The five-step propose/realize/observe/contract/reveal decomposition
Alex named maps to the LANDED substrate as:

| Framing name       | Substrate discharge                                                                    |
|--------------------|----------------------------------------------------------------------------------------|
| propose            | `algebra_turn` #1 with `speaker = @fate/algebra`                                       |
| realize            | Bauchladen lookup + `@io/algebra` dispatch (implicit; no metalogue-turn)               |
| observed error-δ   | `algebra_metalogue_session.opacity: transparency(algebra_turn)` on the enclosing turn |
| contract check     | `algebra_witnessing(algebra_carrier)` at both speaker boundaries                       |
| mechanism reveal   | `algebra_turn` #2 with `speaker = @silicon/algebra`, `in_reply_to = Some(#1)`         |

All five substrate-discharge sites are LANDED. No new substrate-decl
landing required to name `@edge` at algebra-metalogue altitude.

### §5.3 If `@mirror/store/edge` still lands (Mara §6.1 forward-promise)

Mara `7e426bc` §6.1 proposed `@mirror/store/edge` as a first-class
typed edge under `@mirror/store`. This proposal partially reverses
the store shard's explicit deferral (`shards/mirror/store.mirror`
§"What @spectral/db adds (closed)": "The open surface deliberately
does NOT expose typed edges"). Under the metalogue reading, the
question becomes SHARPER, not softer:

- The metalogue's turn-composition happens at `@algebra/metalogue`
  altitude (already LANDED). No `@mirror/store` edge required.
- The tournament's `record` action already crystallises turns as
  content-addressed OIDs via `@bauchladen.crystallize`; the
  Merkle-DAG parent → child links via `splinter_graph` already
  encode ancestry.
- A `@mirror/store/edge` carrier would name the *cross-turn typed
  transition* at storage altitude, separate from the metalogue-
  turn semantics at algebra altitude.

**Substrate-honest verdict:** the `@mirror/store/edge` proposal is
ORTHOGONAL to the metalogue reading. Land or not-land is Alex-plus-
Seam adjudication (`Q1` below). The metalogue reading does NOT
require it. If it lands, it augments; if it doesn't, the metalogue
still works.

---

## §6. Composition sketch — math-first

### §6.1 Given

- `A = {focus, project, split, shift, settle}` — the five-op
  operator basis; per Recognition #79 candidate, the projector
  basis of the 5-orthogonal-axis void-duality space.
- `H` — the void-duality Hilbert space (five-orthogonal-axis;
  expands per Recognition #51 with each crystal accumulation).
- `D` — the kintsugi Dirac flow generator (Connes triple).
- `γ, J` — chirality (Recognition #101) and charge conjugation
  (Recognition #102) gradings.
- `𝒞 = cybernetic_coherence-preserving sheaf ⊂ H` — the
  restriction floor per `shards/cyberpunk.mirror`.
- `𝒯 = @bauchladen` — the accumulated tray both speakers write into.
- `𝕊_fate = @fate/algebra as algebra_carrier` — fate-side speaker.
- `𝕊_silicon = @silicon/algebra as algebra_carrier` —
  silicon-side speaker.
- `E = Hom_A(𝕊_fate, 𝕊_silicon) ∪ Hom_A(𝕊_silicon, 𝕊_fate)` — the
  edge library; algebra morphisms in both directions.
- `v₀ ∈ psychohistory_sheaf(peer)` — start vector (per Mara
  2026-06-26 60KB doc; operational at `mirror recall` envelope's
  four sections).
- `T ⊂ 𝒞` — target region (coherence-preserving sheaf-section).
- `π₁(T²) = ℤ × ℤ` — winding-class quotient per `@torus`.

### §6.2 The problem

Find an `algebra_metalogue_session s`:

```
s = {
  turns:   [t₁, t₂, ..., tₙ],
  opacity: transparency,
  origin:  ref_to(v₀),
}
```

such that the composed morphism

$$
\Phi_s \;=\; t_n.body \circ t_{n-1}.body \circ \cdots \circ t_1.body
$$

applied to `v₀` lands in `T`:

$$
\Phi_s(v_0) \;\in\; T
$$

subject to:

**(C1) Cybernetic-coherence per step.** For every intermediate
state `vₖ = tₖ.body ∘ ... ∘ t₁.body (v₀)`:

$$
\texttt{cybernetic\_coherence}(v_k) \;=\; \texttt{pass}
$$

Discharged at compile-time by `@glue.translate`'s restriction slot.

**(C2) Kasparov composability.** For every adjacent pair
`(tₖ, tₖ₊₁)`:

$$
t_k.body.target \;=\; t_{k+1}.body.source
$$

Discharged by `@glue.compose` + `@algebra/metalogue.compose_turns`
returning `Some(_)` (per `shards/algebra/metalogue.mirror` line 253).

**(C3) Knapsack budget.** Cumulative resource cost:

$$
\sum_{k=1}^{n} \texttt{cost}(t_k) \;\le\; \texttt{capacity\_vector.budget}
$$

with `cost(tₖ)` a vector in `(silicon_flops, ram_bytes, wall_clock, …)`
space per `docs/specs/knapsack-as-kintsugi-inner-loop.md` §2.

**(C4) Winding-class quotient.** Two sessions `s, s'` with
`Φ_s(v₀), Φ_{s'}(v₀) ∈ T` and
`winding_class(Φ_s(v₀)) = winding_class(Φ_{s'}(v₀))` are equivalent
for target-membership; they differ only in observation record.

**(C5) Contract-preservation at each turn.** At every turn `tₖ`:

$$
\texttt{algebra\_witnessing}(t_k.\texttt{speaker}) \;=\; \texttt{pass}
$$

and the turn is well-formed per
`session_well_formed({turns: [t_1, ..., t_k], ...}) = pass` per
`shards/algebra/metalogue.mirror` line 272.

### §6.3 The shape

Under the kintsugi outer loop's contraction discipline
`eⁿ⁺¹ ≤ eⁿ`, and the tournament's rule-algebra selection
(`greedy | beam(k) | elite(k) | halving(η) | tabu | anneal | ucb`
per `boot/std/fate/tournament.mirror` migration preserved), the
problem IS resource-bounded shortest-path in `H|_𝒞 / π₁(T²)` under
monotone descent — exactly the Dijkstra-plus-knapsack shape of
Mara `7e426bc` §3.

**The metalogue reframing is the *same computation, sharper name*.**
What was "walk edges under Dijkstra" is now "conduct the metalogue
under the tournament's rule-algebra until convergence". The
computational content is unchanged; the substrate-decl grammar
is sharper because it names WHY the walk terminates (Foerster's
double-closure at the origin fixed-point) and WHY the walk is
admissible at each step (algebra_witnessing on both speakers).

### §6.4 Termination witnesses

- **(T1)** `is_settled(s.turns.last) = pass` AND
  `Φ_s(v₀) ∈ T` — target hit under kintsugi contraction.
- **(T2)** `Σ cost(tₖ) > budget` — knapsack cap fires.
- **(T3)** `winding_class(Φ_s(v₀)) ∈ visited_classes` with
  byte-equal observation sections — fixed-point exhibition
  at the winding-class quotient.

All three cases discharge `session_well_formed(s) = pass` at
session-termination; the `opacity` residual is either empty (T1),
surfaced-not-absorbed (T2), or fixed-point-witnessed (T3).

---

## §7. Cascade if the framing is Alex-adjudicated ACCEPT

Per Taut's scout `§2`: the accept-cascade is
**one-tick for prose + one-tick for `@fate/algebra` sub-shard landing**.
Mara does NOT land any `.mirror` mutations in this spec; those
cascades stay Alex-adjudication territory.

### §7.1 One-tick cascade (prose-only)

1. `shards/algebra/metalogue.mirror` — append a companion clause
   after line 85 naming `@silicon/algebra`'s routine-selection
   as the second speaker's `propose_turn` realisation, symmetric
   with the existing `@fate.tournament IS propose_turn realised`
   clause.
2. `shards/silicon/algebra.mirror` — append a prose clause naming
   the algebra-altitude metalogue lift `@silicon/algebra` inherits
   as speaker under `@algebra/metalogue`; cite
   `algebra_inherits_metalogue_lift` as the bilateral obligation.
3. `shards/fate.mirror` — refine the path-namespace prose at
   lines 208-232 to name `@fate/algebra` as the fate-side speaker
   in the algebra-altitude metalogue explicitly (currently the
   naming is implicit via `@fate/algebra/morphism` crystals).

Zero new type declarations; zero new bilateral predicates; three
low-risk prose refinements.

### §7.2 Multi-tick cascade (new substrate)

4. `shards/fate/algebra.mirror` — LAND. Symmetric with
   `shards/silicon/algebra.mirror`, ~10-15 lines. Makes the
   fate-side speaker's `algebra_carrier` substrate-native rather
   than prose-declared. Cascade-neutral — no existing shard needs
   to update since the path-namespace was already load-bearing.
5. `shards/mirror/store/edge.mirror` — IF Q1 (below) resolves
   ACCEPT-Path-α. Not required for the metalogue reading itself.

### §7.3 Optional math extraction — deferred (two-tick discipline)

A `docs/math/metalogue/` root would extract:

- The propose/adjust turn-pair as one round of a typed dialogue
  (Kaplan-Ranta on typed dialogue; Bateson 1979 on metalogue).
- The `compose_turns` non-commutativity and its associativity
  discharge (Mac Lane 1971 on categorical composition).
- The metalogue-session as a functor from the free monoid on
  `algebra_morphism`s to the category of algebra_carriers
  (Girard on ludics; Mesland 2013 on category of spectral triples).
- The termination criterion as fixed-point exhibition at the
  torus origin (Foerster 1979 on double-closure; Poincaré-Hopf on
  `χ(T²) = 0`).

**Two-tick discipline decision:** the math extraction is a
**forward-promise**. The spec's inline sketch (§6) is sufficient
for the naming; a formal extraction lands on second citation
site. First citation site is this spec; second will be either
(a) `@fate/algebra` sub-shard landing citing the metalogue lift,
or (b) a future recognition-candidate promotion that requires the
math as prior art. Whichever fires first triggers extraction into
`docs/math/metalogue/fate-silicon-algebra-metalogue.md`.

---

## §8. Adjudication queue — Alex questions

Five questions this spec surfaces. Top-three are the load-bearing
ones (Reed asked for top-3).

### §8.1 (top-1) The altitude question

**Q1.** Alex's framing said `@fate/algebra` is "one level higher"
than `@silicon/algebra`. Substrate-honest reading (§2.1): they are
PEER SPECIES under the LANDED `@algebra` family-root, not stacked
altitudes. The altitude lift IS already there — it is the lift from
`@algebra` (typed operations, static) to `@algebra/metalogue`
(morphisms-as-utterances, dynamic composition of turns).

**Two paths:**

- **Path α.** Adopt the substrate-honest reading: they are
  peers; the altitude lift IS the metalogue lift. Update spec
  language to say "proposer role vs realiser role" instead of
  "one level higher". No shard mutation needed.
- **Path β.** Introduce a new inter-species altitude between
  `@algebra` and its species (`@algebra/proposer` and
  `@algebra/realiser` as ranked sub-classes). Substrate-decl
  cost is high; would touch `shards/algebra.mirror` at family-root
  altitude; risks re-parenting `@fate/algebra` and
  `@silicon/algebra` cascades.

**Mara recommendation:** Path α. The metalogue lift already carries
the altitude Alex's framing gestures at. Substrate-already-had-the-
word applied twice this tick (once for `@algebra`, once for
`@algebra/metalogue`); Path α closes the third slot honestly.

### §8.2 (top-2) The recognition-number question

**Q2.** Is this composition a new recognition candidate, or the
speaker-pair specialisation of an existing recognition?

**Two paths:**

- **Path γ.** Not a new recognition; a *speaker-cardinality-2
  specialisation of `@algebra/metalogue`* (which is itself the
  fifth landed instance of the metalogue-lift, whose family
  includes Recognition #34 and Recognition #100). The spec's
  content is the sharpening of the speaker-pair for the
  substrate's canonical numerical-inference metalogue.
- **Path δ.** A new recognition candidate
  **`#R-CANDIDATE-fate-silicon-metalogue-in-void-duality-basis`**
  — the fifth altitude-lift instance's substrate-native
  speaker-pair. Would enter Reed's recognition-tracking queue
  for Pack ratification.

**Mara recommendation:** Path γ, with a note in
`shards/algebra/metalogue.mirror`'s prose (per §7.1 cascade #1)
naming the fate-silicon speaker-pair as the canonical case. If
Pack subsequently sees enough distinct composition instances to
warrant recognition-number promotion, Path δ becomes a
promotion tick.

### §8.3 (top-3) The `@mirror/store/edge` fault-plane

**Q3.** Does the metalogue reading affect the `@mirror/store/edge`
landing question (Mara `7e426bc` §8.1)?

**Yes — it clarifies the question.** Under the metalogue reading,
`@edge` is one `algebra_turn` at `@algebra/metalogue` altitude;
no storage-altitude `@mirror/store/edge` is REQUIRED for the
metalogue reading. The fault-plane question is now:

- **Does `@spectral/db` need a substrate-native edge carrier at
  storage altitude to build Laplacian navigation on top?** If
  yes, `@mirror/store/edge` still lands per Path α. If no —
  `@spectral/db` can compute its edges from the metalogue-turn
  crystals accumulated in `@bauchladen` — then Path β holds and
  `@mirror/store/edge` stays deferred.

**Mara recommendation:** defer until `@spectral/db` design-notes
surface. The metalogue reading doesn't force resolution.

### §8.4 The `@fate/algebra` sub-shard timing

**Q4.** Should `shards/fate/algebra.mirror` land NOW (immediate
follow-up tick), or on-consumer-pull? Argument for now: the
metalogue framing MAKES it actionable (§7.2 #4); the path-namespace
is already load-bearing at 10+ landed shards. Argument for pull: no
operational consumer yet demands the sub-shard body.

**Mara recommendation:** land after Q1 resolves (Path α doesn't
require it structurally; Path β would).

### §8.5 Recognition #79 promotion timing

**Q5.** The 5-op-as-void-duality-basis (#79 candidate) is
transitively invoked by §2.4's "shared coordinate system" claim.
Should #79 promote with this composition's naming, or on its own
tick?

**Mara recommendation:** on its own tick. This spec cites #79 as
CANDIDATE and does not depend on its promotion for the metalogue
reading to hold. Promotion should follow the standard Pack-
ratification protocol.

---

## §9. Two-tick honesty

This spec:

- Does NOT land any `.mirror` file.
- Does NOT mutate any existing shard.
- Does NOT commit any shard changes.
- Does NOT invent a new substrate primitive.
- DOES name the composition via LANDED substrate primitives.
- DOES surface two substrate-already-had-the-word discoveries
  (`@algebra` at family-root altitude; `@algebra/metalogue` as
  the metalogue-lift's fifth altitude).
- DOES map Alex's five-part evening framing against LANDED
  substrate.
- DOES forward-promise the `@fate/algebra` sub-shard landing and
  the optional math extraction into `docs/math/metalogue/`.

The recognition candidate is
**`@fate/algebra ↔ @silicon/algebra IS a @metalogue in void-duality-
basis coordinates observed on the peer's @torus`** —
a speaker-pair specialisation of the LANDED `@algebra/metalogue`.

Substrate already carries it. This spec names it.

---

## Related

- [[shards/algebra.mirror]] — LANDED family-root; typed algebra surface.
- [[shards/algebra/metalogue.mirror]] — LANDED sub-prism; the
  algebra-altitude metalogue this spec specialises.
- [[shards/silicon.mirror]] — LANDED family-root; hardware-altitude
  learning loop.
- [[shards/silicon/algebra.mirror]] — LANDED sub-prism; silicon-side
  speaker.
- [[shards/fate.mirror]] — LANDED family-root; declares `@fate/algebra`
  path-namespace in prose.
- [[shards/fate/tournament.mirror]] — LANDED sub-prism; metalogue-
  conductor.
- [[shards/metalogue.mirror]] — LANDED family-root; NL-altitude
  ancestor.
- [[shards/code/metalogue.mirror]] — LANDED sub-prism; AST-altitude
  (Recognition #34; sibling to this specialisation).
- [[shards/spectral/metalogue.mirror]] — LANDED sub-prism; spectral-
  altitude (Recognition #100; sibling).
- [[shards/pack/metalogue.mirror]] — LANDED sub-prism; pack-
  coordination altitude (sibling).
- [[shards/glue.mirror]] — LANDED family-root; `compose` (Kasparov).
- [[shards/cyberpunk.mirror]] — LANDED family-root; coherence
  restriction.
- [[shards/kintsugi/oscillate.mirror]] — LANDED sub-shard; Banach
  contraction.
- [[shards/torus.mirror]] — LANDED family-root; observation surface;
  π₁(T²) quotient.
- [[shards/bauchladen.mirror]] — LANDED family-root; the tray both
  speakers write into.
- [[shards/mirror/store.mirror]] — LANDED family-root; where
  `@mirror/store/edge` would land per Q3 (deferred).
- [[docs/math/the-tower/recognition-79-gauge-is-void-duality-basis.md]]
  — Recognition #79 candidate; the shared coordinate basis (§2.4).
- [[docs/insights/2026-07-08-mara-geometric-dijkstra-tournament-topology.md]]
  — Mara `7e426bc`; the geometric-Dijkstra reading this spec
  reframes as metalogue.
- [[docs/scouts/2026-07-08-taut-fate-silicon-metalogue-projection.md]]
  — Taut drift-scout verifying substrate-already-had-the-word
  discoveries.
- [[docs/math/2026-07-07-glue-cyberpunk-fate-composition.md]] —
  Mara's ENGINE-tier composition doc (2026-07-07).
- [[docs/specs/spectral-metalogue.md]] — Mara's SPECTRAL-altitude
  metalogue spec (Recognition #100).
- [[docs/specs/bauchladen-autopoietic-fate.md]] — Mara's #104 chain
  spec.
- [[docs/specs/silicon.md]] — Mara-silicon-1 (2026-06-30); the
  `@silicon` canonical spec.
- [[docs/specs/knapsack-as-kintsugi-inner-loop.md]] — Mara
  (2026-07-05); the resource-budget math.
- [[docs/insights/2026-07-08-torus-double-closure-empirical.md]] —
  Reed's iteration-2 empirical (origin double-closure).
- [[docs/insights/2026-07-08-torus-axis-isolation-meridian.md]] —
  Reed's iteration-3 empirical (meridian isolation).
- [[docs/insights/2026-07-08-torus-axis-isolation-longitude.md]] —
  Reed's iteration-4 empirical (longitude isolation).
- [[docs/loop/CURRENT.md]] — active arc state at time of writing.
