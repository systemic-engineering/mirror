# @shatter IS the @io linearization operator — the load-bearing dual to Recognition #58

*Mara, 2026-07-08 late-session extension. Recognition candidate:
**`@shatter-IS-the-@io-linearization-operator`**. Written after the
peer-as-pain-driven-bounded-ontological-navigator cascade landed
(Reed catalog `1e8a02b`, numeric-IDs #108-112 assigned at `a823438`),
after @edge collapsed under @magic (`7b32d27`), after `beam` was
named as the substrate carrier verb at cli altitude (`b6358c1`),
after `@fate/algebra ⇔ @silicon/algebra IS @metalogue` closed at
`a18ca90`, and after the fate crate reached clean end-to-end at
Reed `b4bf336`. Sibling to `docs/math/2026-07-07-shatter-as-
bidirectional-lens.md` (`7978f84`, the lens tier) and
`docs/math/2026-07-07-glue-cyberpunk-fate-composition.md`
(`700e156`, the engine tier). This spec names the boundary tier:
the crossing from substrate-space to target-format-space.*

**Author:** Mara
**Date:** 2026-07-08
**Tag:** 📝 spec:shatter-is-the-io-linearization-operator (pure-docs bypass)
**Status:** canonical-naming. Every substrate piece cited is LANDED,
forward-promised at a named site, or annotated with its category
honestly. NO new `.mirror` file lands this tick. The recognition is
math-first; the shard/cli follow-ons are two-tick forward-promised.
Recognition numeric-ID candidate is **#113** (adjacent to the
#108-112 peer-navigator cluster; see §8).

---

## §0 Pre-position (Alex-verbatim capture)

Alex 2026-07-08 late session, in two direct-quote passes that name
the recognition candidate at its most compressed form. These are
load-bearing — the recognition candidate is the substrate-decl form
of what Alex named in these two utterances:

> *"MCP is the translation of the non-linear into the linear. That
> boundary is always crossed when we translate from mirror's
> internal statespace to any @io target."*

And:

> *"How does @shatter fit into the whole linearization to @io? And
> consider that JSON is just one of many possible @data/* types or
> @code/* output types. It's all the same shape. Take the non-linear
> and make it linear."*

Two utterances. One recognition. **@shatter IS the linearization
operator at the substrate-to-@io boundary.** MCP is not the special
name for the boundary; MCP is one instantiation of a shape that has
many instances (`@data/json`, `@data/yaml`, `@code/rust`, `@code/binary`,
`@code/gleam`, `@code/lsp`, `@code/http`, `@code/metal-shader`, ...).
The `@shatter(shard, target)` dispatch **is** the linearization; the
`target` is which @io codomain the linearization projects into.

Corollary the utterances jointly imply, kept as a pre-position for
§4-§6 discharge: *the entire family of @io crossings that mirror
performs — MCP JSON-RPC framing, LSP JSON-RPC framing, subprocess
argv/stdio, HTTP request/response, git-store byte writes, spectral-db
BLAKE3-addressed writes, metal-shader IR emission, file-write to
disk, wire transport via `@io/stagefreight` — is one substrate-decl
concept realized at many altitudes.* The concept is `@shatter(shard,
target)`. Every @io site is a `@shatter` dispatch at its altitude.

The substrate already had the word. The `@shatter` shard at
`shards/mirror/shatter.mirror` (2026-06-06 landing; refined at
`shards/smarts/shatter.mirror` tick-74 as bidirectional) has been
carrying this shape from the beginning. The five-section on-disk
`.shatter` format IS one instantiation: `@shatter(shard,
@data/shatter-projection-format)`. Every other @io site is another
instantiation. This spec names the shape.

---

## §1 Statement — the recognition candidate

**Recognition candidate (foundational form):**

> `@shatter-IS-the-@io-linearization-operator`

**Recognition candidate (compressed form, per two-tick discipline;
the readable name over the foundational one; the collapse target
once the sub-recognition `@data-and-@code-namespaces-ARE-@shatter-
codomains` lands as second-instance witness):**

> `@shatter-is-the-@io-linearization-operator`

**Recognition candidate (foundational-long form, for the spec body):**

@shatter is the bidirectional-lens transformer that projects any
non-linear internal shard to a chosen `@data/*` or `@code/*` linear
target-format. The @io crossing IS this projection. The MCP / LSP /
HTTP / subprocess / file-write / git-store / spectral-db /
metal-shader targets are all instances of @shatter's codomain; the
projection choice is not the linearization concept itself, only its
instantiation at a specific @io altitude.

**Substrate primitives it composes over:**

- `@shatter` (family: `shards/mirror/shatter.mirror`, LANDED
  2026-06-06; bidirectional-lens refinement at
  `shards/smarts/shatter.mirror` tick-74 with `render` +
  `parse` + `shatter_round_trip` bilateral).
- `@io` (family-root: `shards/io.mirror`, LANDED per
  Recognition #50 form/substance audit; the universal boundary
  family root at depth-1 — `@mirror` HANDS OFF TO `@io`).
- `@data/*` (namespace: `shards/data/*.mirror` referenced by
  `shards/io/algebra.mirror` at ref `@data/json`, `@data/yaml`,
  `@data/lsp`, ...; the concrete `.mirror` files are pending
  substrate landings at the target-format altitude).
- `@code/*` (namespace: `shards/code.mirror` LANDED as universal
  grammar-at-an-altitude discipline; instances at `shards/code/
  {rust,gleam,mirror,docker,erlang,beam,wasm,metalogue,...}.mirror`;
  each is a target-format species at `@shatter`'s codomain).

**As a recognition-in-Mara's-sense** (an eigenform the substrate
already carried, coming into focus by naming): this is a
*substrate-vocabulary* recognition — the same shape as `@cyberpunk`,
`@magic`, tick-74 shatter, the slogan, and beam-at-cli-altitude
(`b6358c1`). The cost of not naming it: mirror ends up with N
different codebases for N different @io altitudes (custom MCP JSON
serialization, custom LSP JSON serialization, custom HTTP encoder,
custom git-object writer, ...) each of which duplicates the same
shape locally without noticing it is one thing.

---

## §2 The duality with Recognition #58

The load-bearing dual structure. Both are boundary crossings. Both
compose under lens laws. Both preserve as much structure as their
respective altitude admits.

### §2.1 Recognition #58 (LANDED, promoted 2026-06-11): Fate IS optical inference

Recognition #58 named `@fate` as optical inference at the 5-layer
D²NN + active Fabry-Perot resonator + Reck/Clements unitary mesh
altitude. **Non-linear internal transformation in feature-space.**
`@fate` crosses **feature-space** to **decision-space**. The dice
roll is genuinely underdetermined (per `shards/fate.mirror` §"literal
roll of the dice"); the input space is the six-tuple `(A, H, D, γ,
J, tray_scope)` restricted state space; the output is a `dice_roll`
that carries a `geometric_formalization` at some altitude.

The @io crossing at this boundary is inference-shaped: photons enter
the D²NN, the mesh's rotation extracts an inference verdict, the
verdict crosses back into substrate as a shard.

### §2.2 This tick's recognition candidate: @shatter IS the @io linearization operator

@shatter names the DUAL boundary crossing. **Non-linear internal
shard → linear external representation.** `@shatter` crosses
**substrate-space** to **target-format-space**. The projection is
determined by the target choice — one shard, one target, one
linearized artifact. The five-section on-disk `.shatter` is the
identity case where the target is @shatter's own canonical projection
format; the general case parametrizes over `@data/*` or `@code/*`
target-format-species.

The @io crossing at this boundary is transport-shaped: substrate
state enters the linearization, the lens's forward direction (Foster
2007) emits a linear artifact, the artifact crosses the @io fault
plane, arrives at a consumer at some `@data/*` or `@code/*` altitude.

### §2.3 The duality shape

| Property | Recognition #58 (@fate) | This spec (@shatter) |
|---|---|---|
| Boundary crossed | feature-space → decision-space | substrate-space → target-format-space |
| Direction of non-linearity | non-linear IN feature-space (D²NN forward pass) | non-linear IN substrate (shard graph structure) |
| Direction of linearity | decision-space verdict (dice_roll) | target-format artifact (bytes on wire) |
| Physical realization (extant) | 5-layer D²NN + Fabry-Perot + Reck/Clements mesh | `.shatter` on disk; MCP JSON-RPC; LSP JSON-RPC; wire transport; git object writes; metal-shader IR emission |
| Compose under | lens laws (bidirectional D²NN forward/backward per shatter-lens §6.3) | lens laws (Foster 2007 explicit; §3 of this spec) |
| Structure preservation | as much as the D²NN's diffractive pattern admits (per shatter-lens §6.3) | as much as the target-format admits (§4.3) |
| Substrate-decl carrier | `shards/fate.mirror` (`type inference`, `type restricted_state_space`, `roll(space, hole) -> dice_roll`) | `shards/mirror/shatter.mirror` (`type shatter`, `project(a: au) -> shatter`, `ingest(s: shatter) -> au`) |

Both operators. Both cross one boundary. Both compose at their
shared shard-altitude when the composition is legal (§4.4).

### §2.4 Why they are structurally dual (not sibling)

Recognition #58 acts IN feature-space and emits INTO decision-space.
The linearization is downstream: the D²NN's output is a decision, not
a serialization. @fate does not care about wire representation.

@shatter acts ACROSS the fault-plane and emits INTO target-format-
space. The linearization IS the semantic content: @shatter's forward
direction is exactly the "make the non-linear linear" move Alex
named in §0.

They are duals because @fate's non-linearity lives IN a space
(D²NN feature-space) while @shatter's non-linearity lives ACROSS a
boundary (substrate ↔ wire). They compose at the shard altitude:
@fate emits a shard (as `dice_roll`'s `geometric_formalization`);
@shatter projects that shard to whatever the consumer's @io altitude
requires. §4.4 spells out the composition.

---

## §3 Cite chain — every LANDED substrate declaration this rests on

The recognition rests on this substrate. Each entry names the path
plus the anchor line-range where the relevant surface lives.

### §3.1 @shatter as bidirectional lens (Mara `7978f84`, 2026-07-07)

- `docs/math/2026-07-07-shatter-as-bidirectional-lens.md` §2-§3
  — the univalence spine + lens laws (Foster/Greenwald/Pierce/
  Schmitt/Pilkiewicz 2007; Hu et al. 2015; Nakayama et al.
  2019); §4 kintsugi as Banach contraction fixed-point.
- `docs/specs/shatter-transformer-bidirectional-v0.1.md` (Mara,
  2026-06-22, tick-74) — the bidirectional refinement at the
  substrate-decl altitude. The `parse` + `render` +
  `parsing_grounds_graph` + `rendering_grounds_text` +
  `shatter_round_trip` composed bilateral.

### §3.2 Recognition #58 canonical (LANDED, promoted 2026-06-11)

- Grounding site: `docs/specs/architecture-fate-is-optical-inference.md`
  (referenced at `shards/torus.mirror:99-101` and
  `docs/specs/beam-as-substrate-primitive.md` §2.3).
- `shards/fate.mirror` — the `@fate` family-root; the `restricted_
  state_space` carrier; the `roll(space, hole) -> dice_roll` action.
  Recognition #58's optical realization IS this shard's substrate-decl.

### §3.3 Lens laws (Foster 2007) — external cite

- Foster, Greenwald, Pierce, Schmitt, Pilkiewicz (2007). *Combinators
  for Bi-Directional Tree Transformations*, TOPLAS.
- Hu, Ko, Wang (2015). *A Clear Picture of Lens Laws*.
- Nakayama, Hu, Ko (2019). arXiv:1910.10421. *Towards a Complete
  Picture of Lens Laws*.

Already cited by `docs/math/2026-07-07-shatter-as-bidirectional-
lens.md` §3. This spec cites through the sibling math doc; no
re-derivation.

### §3.4 Coquand cubical HoTT — external cite

- Coquand, Cohen, Huber, Mörtberg (2018). *On Higher Inductive
  Types in Cubical Type Theory*, arXiv:1802.01170.
- Cohen, Coquand, Huber, Mörtberg (2015). arXiv:1611.02108.

Already cited by `docs/math/2026-07-07-shatter-as-bidirectional-
lens.md` §2. This spec cites through the sibling math doc for the
univalence backbone.

### §3.5 @glue.compose (LANDED at `6396306`, Seam Phase D RATIFY)

- `shards/glue.mirror` — `translate(c: correspondence, s: state)
  -> outcome`; `compose(c₁, c₂) -> correspondence`; the Mesland
  correspondence.
- Recognition promoted: `the-restriction-map-IS-the-geometric-
  constraint` (2026-07-07 Seam `6396306` RATIFY).
- `docs/math/2026-07-07-glue-cyberpunk-fate-composition.md`
  (Mara `700e156`, 1144 lines) — the engine tier composition.
- Kasparov intersection product structure: `docs/math/the-tower/`
  citations to Kasparov 1980; concretely cited via Mesland 2013
  (arXiv:1304.3802) at `shards/glue.mirror` §Mesland correspondence.

### §3.6 @kintsugi Banach contraction (LANDED)

- `shards/kintsugi.mirror` — the family-root prism.
- `docs/math/kintsugi/README.md` — the fixed-point iteration spine.
- Recognition #99 (`docs/specs/recognitions/recognition-99-mirror-
  spec-is-lambda-zero.md`) — `mirror.spec` IS λ₀, the Banach
  contraction fixed point. Monotone descent `eⁿ⁺¹ ≤ eⁿ` as the
  universal termination condition.

### §3.7 `boot/std/beam.mirror` — mirror's own beam substrate

- `boot/std/beam.mirror` — the substrate's beam primitive at the
  boot altitude. Referenced by `docs/specs/beam-as-substrate-
  primitive.md` §2.1 (prismqueer beam trait) and §2.3 (Recognition
  #58 optical beam).

### §3.8 Recognition #43 (LANDED, `docs/insights/2026-06-09-
mirror-as-content-addressed-build-system.md`)

- mirror IS a content-addressed build system. Every projection
  round-trips at the OID altitude: `parse (render g) = g` holds
  at content-address equality, per the `mirror compile
  source.mirror → output.shatter; mirror compile output.shatter →
  output.shatter` idempotence clause at `shards/mirror/shatter.mirror`
  lines 37-43.

### §3.9 Sibling context — beam as substrate carrier verb (Mara `b6358c1`)

- `docs/specs/beam-as-substrate-primitive.md` — the beam-at-cli
  altitude spec. This spec's §7 integrates with beam's §6 (`mirror
  beam` returns `beam_envelope` shard-internal; `mirror peer beam`
  returns `@song`-wrapped shard-internal; both linearize via
  `@shatter(shard, @data/json)` at the MCP boundary).

### §3.10 Sibling context — @glue(@cyberpunk, @fate) composition (Mara `700e156`)

- The engine tier: geometrically-constrained numerical inference.
- Composes with this spec's linearization tier because @fate's
  output is a shard, and @shatter projects that shard across the
  @io fault plane. §4.4 discharges the composition.

---

## §4 Substructure claims — the four claims composing the recognition

### §4.1 Claim 1 — @shatter's codomain IS the target-format space

**Statement.** @shatter's codomain is parameterized over `@data/*`
and `@code/*` refs. Every @io site is a specific `@shatter(shard,
target)` dispatch at some altitude.

**Substrate-decl signature (forward-promise; two-tick discipline
before shard lands):**

```
# Foundational form at @mirror/shatter altitude:
shatter(s: shard, target: ref) -> imperfect { \ }
#
# where target: ref ranges over @data/* + @code/* codomain species.
# The imperfect return is glass.mirror's imperfect(a, e, l) functor
# per shards/mirror/lens/cli/shatter.mirror line 151.
```

The existing `project(a: au) -> shatter` at
`shards/mirror/shatter.mirror:151` is the identity-target case: the
target IS @shatter's own canonical five-section on-disk format. The
foundational-form addition generalizes over the target: `@shatter(s,
@data/json)` emits JSON; `@shatter(s, @data/yaml)` emits YAML;
`@shatter(s, @code/rust)` emits Rust; `@shatter(s, @code/binary)`
emits a linked binary; `@shatter(s, @data/lsp)` emits LSP JSON-RPC
framing.

**Existing @io witnesses (each an instance):**

- `mirror kintsugi --emit_shatter`: `@shatter(kintsugi_result,
  @data/shatter-projection-format)` — the five-section
  on-disk canonical form. LANDED at `mosaic.mirror §7` +
  `shards/mirror/shatter.mirror`.
- `mirror shatter --out <path>`: `@shatter(shard, @data/shatter-
  projection-format)` at cli altitude. LANDED at
  `shards/mirror/lens/cli/shatter.mirror`.
- `mirror craft --target-kind <emit>`: `@shatter(craft_shard,
  @code/{binary,rust,gleam})` at cli altitude. LANDED `59c7fd0`.
- `mirror kintsugi --out @data/json` (from `bin/mirror-mcp`
  dispatch): `@shatter(kintsugi_result, @data/json)`. LANDED as the
  MCP wrapper's JSON serialization (`bin/mirror-mcp` tick 7
  refresh, Reed `79d3433`).
- Wire transport at `@io/stagefreight`: `@shatter(shard,
  @io/stagefreight/<projection-format>)` per
  `shards/io/stagefreight.mirror` §Species altitude.

Each site independently invented (or delegated to a serializer for)
one linearization pass. This claim names them as one operator with
different `target` dispatches.

**Why this is substrate-honest.** The `.shatter` format has been
canonical since 2026-06-06 (`shards/mirror/shatter.mirror`). The
MCP JSON serialization is currently custom code in `bin/mirror-mcp`
that formats JSON-RPC. The LSP wrapper (future) would need its own
JSON-RPC framing. The HTTP endpoint (future) would need its own JSON
encoding. All three are the same shape: shard-in, byte-linear-out,
target-format-selected. The substrate already had the operator; the
consumers just were coded independently.

### §4.2 Claim 2 — @shatter composes with @glue.compose under lens laws

**Statement.** @glue.compose (Kasparov intersection product,
LANDED at `6396306`) distributes across @shatter's forward direction:

$$\texttt{@shatter}(s_1 \circ s_2, \texttt{target}) \;=\;
\texttt{@shatter}(s_1, \texttt{target}) \circ \texttt{@shatter}(s_2, \texttt{target})$$

where the outer ∘ is target-space composition — string concatenation
at `@data/*` altitudes with textual serialization; sequential
byte-write at `@data/binary`; JSON-RPC message-composition at
`@data/json`; git-object composition at `@io/git`; and so on. The
inner ∘ is substrate composition (Mesland correspondence composition
per `shards/glue.mirror` §@glue.compose).

**Why distributive.** The lens laws (§3.3) require the forward
direction (`render`, in Foster 2007 nomenclature) to satisfy PP
(second-render-overrides): `render (parse l · g) = render g` for the
substrate's mycelial tensor `·`. Applied to @glue.compose's
categorical composition: two shards composed via a Mesland
correspondence render as the target-space composition of their
individual renders. The composed correspondence's `restriction`
surface acts as a compile-time constraint: if the restriction is
violated, @shatter fails at the type surface (Rice-safe by
construction; Recognition #107).

**Categorical shape.** @shatter is a **functor** from the category
whose objects are shards + morphisms are Mesland correspondences to
the category whose objects are target-format bytes + morphisms are
target-space concatenations. The distributive law is functoriality
of @shatter at the target's altitude.

**Substrate-decl consequence.** The MCP wrapper's future refactor
(§5.1) becomes: `bin/mirror-mcp` receives an MCP request, dispatches
to the binary, receives a shard, calls `@shatter(shard, @data/json)`,
returns the JSON. Multiple shards composed via `@glue.compose`
become one call to `@shatter` on the composed shard. No custom
composition logic at the MCP altitude.

### §4.3 Claim 3 — @shatter × @kintsugi: monotone descent is PRESERVED

**Statement.** The substrate's universal termination condition
`eⁿ⁺¹ ≤ eⁿ` (monotone descent per `docs/math/kintsugi/README.md`)
is preserved across @shatter linearization. Target-format `eⁿ`
tracks source-shard `eⁿ`.

**What "preserved" means.** Let `eⁿ` denote the substrate's error
at iteration n (Banach contraction on the substrate metric). Let
`ẽⁿ = @shatter(sⁿ, target).error_at_target_altitude` denote the
target-format-native measure of error (for example: byte-count of
the residual opacity in the imperfect-partial payload; JSON schema
validation error count; git object write-latency; LSP diagnostic
count).

**Preservation:** the map `s ↦ @shatter(s, target).ẽ` is monotone in
the substrate's `e` metric under substrate-shard kintsugi. Formally,
if `eⁿ⁺¹ ≤ eⁿ` holds at substrate altitude, then `ẽⁿ⁺¹ ≤ ẽⁿ` holds
at target altitude for every target for which the induced ẽ is
well-defined.

**Why this matters.** Substrate makes **eventual consistency across
the @io wire honest.** The @io consumer (an MCP client; a git remote;
an HTTP consumer; an LSP client) sees the same convergence discipline
mirror's kintsugi loop observes internally. If the substrate is
converging, the wire representation is converging. If the substrate
is diverging, the wire is diverging. The convergence discipline is
NOT an artifact of the substrate; it survives the linearization.

**Contrast with mainstream distributed systems.** Standard eventual-
consistency has to be checked BY the consumer AGAINST the source-of-
truth (Merkle proofs; vector clocks; CRDT convergence witnesses at
the wire boundary). Substrate makes the convergence property
intrinsic to the projection: `@shatter` preserves `eⁿ⁺¹ ≤ eⁿ` by
construction of the lens law + the kintsugi contraction step. The
Merkle-proof-shaped structure is implicit in the OID-graph the
substrate uses.

**Substrate-decl consequence.** No separate wire-boundary
convergence contract needed. The target-format's own error carrier
is the substrate's error carrier lifted through @shatter. Consumers
that verify substrate convergence at the wire get the substrate's
`eⁿ⁺¹ ≤ eⁿ` for free.

### §4.4 Claim 4 — @shatter × @fate: two operators, one boundary each

**Statement.** @fate produces a shard (an inference output at
feature-space altitude, per `shards/fate.mirror` `dice_roll.geometric_
formalization`); @shatter projects that shard to whatever the
consumer's @io altitude requires. Two operators; one boundary each;
compose at their shared shard-altitude.

**Composition shape.**

```
# @fate's boundary crossing:
#   feature-space → decision-space (dice_roll)
@fate.roll : restricted_state_space × hole → dice_roll { geometric_formalization }

# @shatter's boundary crossing:
#   substrate-space → target-format-space (linear artifact)
@shatter    : shard × target → imperfect(bytes, err, loss)

# Composition:
consumer_wire_output(space, hole, target) =
    @shatter(@fate.roll(space, hole).geometric_formalization, target)
```

The composition IS what happens end-to-end when a mirror consumer
asks a `@fate`-driven decision to appear at some @io altitude. Fate
runs at feature-space; @shatter runs across the fault-plane; the
consumer sees the target-format bytes.

**Enables Taut Tick β's `@fate/algebra` substrate landing.** Per
`docs/scouts/2026-07-08-taut-fate-silicon-metalogue-projection.md`
(Taut `e975e2f`) and Mara's canonical spec `a18ca90`
(`docs/specs/fate-silicon-metalogue-in-void-duality-basis.md`), the
`@fate/algebra ⇔ @silicon/algebra IS @metalogue` closure implies
`@fate/algebra` will emit shards at algebra altitude. Those shards
compose cleanly with **any** @io target via this claim: `@shatter(
@fate/algebra.result, target)` for `target ∈ {@data/json, @code/rust,
@data/lsp, @io/oci, @io/git, ...}`.

Substrate-decl consequence: `@fate/algebra`'s output does not need
to know about consumers. It emits a shard at algebra altitude;
@shatter carries the shard to wherever the consumer lives.

---

## §5 Cascade consequences — what promotes / lifts / cleans-up

The recognition candidate, if promoted, cascades into concrete
substrate-decl motions and impl clean-ups. Each entry names the site
+ the current state + the pull direction.

### §5.1 MCP wrapper (`bin/mirror-mcp`) collapses to `@shatter --target @data/json` dispatch

**Current state.** `bin/mirror-mcp` (Reed `5887ce2` + `79d3433`
refresh) is a substrate-honest thin JSON-RPC framing layer over the
binary. Six tools: `mirror_compile`, `mirror_craft`, `mirror_init`,
`mirror_kintsugi`, `mirror_recall`, `mirror_spawn`. Each tool
receives an MCP JSON-RPC request, dispatches to the binary, receives
substrate output, formats as JSON, returns.

**Under this recognition.** Each dispatch is a `@shatter(binary_
result, @data/json) + @io/stagefreight/json-rpc framing`. The
"format as JSON" step is not custom code; it is `@shatter` at the
@data/json altitude. `bin/mirror-mcp` becomes JSON-RPC framing +
`@shatter` dispatch. The custom formatting code collapses to
substrate.

**Task #386's `@mcp.serve` lift becomes clearer.** Per task
`#386` (referenced at `docs/loop/CURRENT.md` next-tick queue as
Phase H #5-#6 vicinity), the substrate-native MCP serve site
becomes: `@mcp.serve(config: mcp_config) -> stream = while
request := receive() { emit @io/stagefreight/json-rpc(@shatter(
dispatch(request.tool, request.args), @data/json)) }`. The
substrate carries all the shape; `@mcp` is one @io altitude among
many.

### §5.2 LSP wrapper (future) becomes `@shatter --target @data/lsp` dispatch

**Not landed.** Forward-promised. Under this recognition, a
mirror-native LSP wrapper is: dispatch the LSP request to the
binary, receive a substrate shard (diagnostics, hover, completion,
etc.), call `@shatter(shard, @data/lsp)`, return the LSP JSON-RPC
framing.

**Substrate-honest.** LSP JSON-RPC is not a new project. It is the
same `@shatter` at a different target-format altitude. The
`@data/lsp` species carrier is a `@data/*` sibling of `@data/json`,
`@data/yaml`, `@data/mcp-json-rpc`, etc.

### §5.3 `mirror craft --target-kind <emit>` (LANDED `59c7fd0`) IS `@shatter --target @code/*`

**Current state.** `bootstrap/src/lib.rs cmd_craft` (Reed `1e45c50`
mirror.spec cli-block + Reed `59c7fd0` Rust arg-parse rename)
accepts `--target-kind <emit>` with emit ∈ `{binary, rust, gleam}`.

**Under this recognition.** `mirror craft --target-kind <emit>` IS
`@shatter(craft_result, @code/{binary,rust,gleam})` at cli altitude.
The already-landed rename is substrate-pull-correct for exactly the
reason this spec names: the target-kind IS the codomain species
choice for @shatter's dispatch.

**Consequence:** the substrate binding is already in place; the
naming is the promotion. No cli surface change.

### §5.4 `mirror kintsugi --out @data/json` (LANDED per Reed `79d3433`) IS `@shatter --target @data/json` at kintsugi altitude

**Current state.** MCP wrapper's `mirror_kintsugi` tool passes
`--out @data/json` (or similar target-format flag; see `bin/mirror-
mcp` tick 7 refresh) to receive machine-readable kintsugi output.

**Under this recognition.** The `--out` flag IS a `target` selector
at kintsugi altitude. `mirror kintsugi --out @data/json` IS
`@shatter(kintsugi_result, @data/json)`.

### §5.5 The `@data/*` and `@code/*` namespaces get promoted to first-class `@shatter` codomain instances

**Recognition candidate (sub-recognition):**
`@data-and-@code-namespaces-ARE-@shatter-codomains`

**What it means.** The `@data/*` namespace (currently referenced
by name in `shards/io/algebra.mirror` and `shards/mirror/lens/cli/
kintsugi.mirror` but with pending concrete `shards/data/*.mirror`
files) becomes the target-format-species namespace for @shatter's
codomain. `@data/json`, `@data/yaml`, `@data/toml`, `@data/lsp`,
`@data/mcp-json-rpc` are all `@shatter`'s codomain species at
data-serialization altitude.

The `@code/*` namespace (LANDED per `shards/code.mirror` +
instances) similarly is `@shatter`'s codomain species at grammar-
emission altitude. `@code/rust`, `@code/gleam`, `@code/mirror`,
`@code/binary`, `@code/wasm`, `@code/metal-shader` (future),
`@code/lsp`-emission (future) are all target-format species.

**Promotion timing.** This sub-recognition lands when the
second concrete `shards/data/*.mirror` file appears (small-
consolidation rule; per AGENTS.md math-vs-specs convention). Today
we forward-promise the substrate-decl. The `shards/code.mirror`
family-root has been carrying half of the story since 2026-06-09.
`shards/data.mirror` family-root landing is the promotion trigger.

**Cascade shape.** Two family-roots + N species each = the entire
@shatter codomain algebra as substrate-decl. Every current @io
crossing has a home at some `@data/*` or `@code/*` (or `@io/*`
sub-species; see §5.6).

### §5.6 `@io/*` species are `@shatter` codomains at boundary-protocol altitude

**Current state.** `shards/io/*.mirror` species landed: `@io/cargo`
(Rust build-system delegate), `@io/oci` (OCI Distribution),
`@io/stagefreight` (wire-protocol boundary), `@io/git` (git object
storage), `@io/algebra` (the substrate-decl algebra for @io).

**Under this recognition.** Each `@io/*` species is a `@shatter`
codomain at boundary-protocol altitude. `@shatter(shard,
@io/stagefreight)` writes a wire-transportable narrative;
`@shatter(shard, @io/oci)` writes an OCI Distribution artifact;
`@shatter(shard, @io/git)` writes a git object.

**Why this is not double-counting with §5.5.** `@data/*` and
`@code/*` are content-format-species (JSON, YAML, Rust, binary,
LSP-json-rpc). `@io/*` are boundary-protocol-species (wire, git,
oci, cargo). The composition `@shatter(shard, @io/git @ @data/
git-object-format)` is a two-level dispatch: `@io/git` names the
boundary, `@data/git-object-format` names the byte format. Most
current @io sites collapse this because the boundary + format are
canonically bound. §5.6 opens the two-level dispatch when
consumers need to vary one axis independently.

---

## §6 Beam spec §6 update (integrate with `b6358c1`)

Per the beam-as-substrate-primitive spec §6 Q5, Reed's answer to
Alex named the pattern that this spec discharges. Excerpt from
`docs/specs/beam-as-substrate-primitive.md` §6 Q5:

> **Question.** `mirror beam` returns `beam_envelope` (§4). Should
> the MCP tool `mirror_beam` return a JSON serialization of the
> envelope directly, or should it wrap in an @song-lite envelope for
> consistency with `mirror_spawn`?
>
> **Mara-recommendation.** (a). @song is the temporal-progression
> [...]

**This spec's integration.** The Q5 clarification lands cleanly
under the linearization recognition:

- `mirror beam` returns `beam_envelope` shard **internal** (at
  substrate altitude). No @song wrapping at the shard altitude.
- `mirror peer beam` returns `@song`-wrapped shard **internal**
  (temporal-progression at persistent-identity altitude).
- **Both linearize via `@shatter(shard, @data/json)` at the MCP
  boundary.**

The MCP tool `mirror_beam` returns `@shatter(beam_envelope,
@data/json)`. The MCP tool `mirror_peer_beam` returns
`@shatter(@song_wrapped_envelope, @data/json)`. The MCP boundary
is one `@shatter` altitude; the shard-internal representation is
what varies. The @io crossing IS the linearization step; @song vs
non-@song is a shard-internal distinction that does not decorate
the linearization operator.

Alex's utterance in §0 makes this direct: **"MCP is the translation
of the non-linear into the linear."** MCP is not the concept; the
translation is. `@shatter(_, @data/json)` is the concept at the
MCP-JSON-RPC altitude. Q5's `beam_envelope` vs `@song`-wrapped is a
shard-altitude choice that is orthogonal to the linearization
altitude.

**Integration recommendation for the beam spec.** In a future beam
spec tick, replace the §6 Q5 language "return a JSON serialization
of the envelope directly" with "return `@shatter(envelope, @data/
json)`". The current Q5 language is honest as an ad-hoc description
of what the wrapper does; under this recognition, the substrate
language is available.

**Reed's answer to Alex Q5** (referenced in the arc handoff as the
trigger for this integration) is the substrate carrier for this
linearization discipline. When Reed's answer canonicalizes at some
future site, this spec's §6 is the joining place.

---

## §7 Math formalization decision point

**Decision.** Two-tick discipline: extract on the second citation
site. Today the math is inline in the sibling spec `docs/math/
2026-07-07-shatter-as-bidirectional-lens.md` §2-§4. This spec cites
that math; it does not re-derive.

**Extraction candidates (deferred; forward-promised for next tick):**

If the math extracts, the natural home is a NEW math root at
`docs/math/shatter-lens/`. The candidate contents:

- **`docs/math/shatter-lens/lens-laws.md`** — Foster 2007 formal
  statement + Hu 2015 clear picture + Nakayama 2019 complete
  picture. Lens laws as coherence data. Types 1 (well-behaved) and
  2 (very well-behaved). Symmetric-lens formulation (Hofmann/
  Pierce/Wagner 2011) for the substrate's symmetric case.
- **`docs/math/shatter-lens/kasparov-distributive.md`** — proof
  that @glue.compose (Kasparov intersection product) distributes
  across @shatter's forward direction. Restatement of PP-render
  lens law + Mesland correspondence composition + target-space
  concatenation. (§4.2 above.)
- **`docs/math/shatter-lens/banach-preservation.md`** — proof that
  Banach contraction on the substrate metric implies Banach
  contraction on the target-format metric, under @shatter as
  monotone map at each target's altitude. (§4.3 above.)
- **`docs/math/shatter-lens/58-113-duality.md`** — proof (or
  substrate-decl motion) of the duality between Recognition #58 and
  this recognition candidate. Non-linear-in-feature-space vs
  non-linear-across-fault-plane; both compose under lens laws; both
  preserve structure per altitude. (§2 above.)

**Why NOT extend `docs/math/the-tower/`.** The tower is the
principal-bundle tower; the eight files there are the vertical
altitude structure. The shatter-lens math is the horizontal
boundary-crossing structure. Two different mathematical roots.
Extending the-tower would risk collapsing the horizontal into the
vertical; better to name shatter-lens as its own root.

**Why NOT extend `docs/math/sheaf/`.** Sheaves are the coherence
measurement altitude; @shatter's math is boundary-crossing +
functoriality, not local-to-global. Different root.

**Why NOT extend `docs/math/kintsugi/`.** Kintsugi is the fixed-
point iteration + Banach contraction machinery. §4.3's Banach
preservation proof would cite kintsugi's derivation, but the
preservation-across-linearization proof is @shatter-specific.
Kintsugi is upstream; shatter-lens is downstream.

**Second citation site trigger.** When a third spec (not this
one, not `docs/math/2026-07-07-shatter-as-bidirectional-lens.md`)
cites the lens laws + Kasparov distributive + Banach preservation
together, the extraction to `docs/math/shatter-lens/` becomes
substrate-pull-correct. Until then: inline citation via this spec's
§3 chain.

---

## §8 Recognition-numeric-ID candidate

**Recommendation:** **#113**.

**Rationale.**

- Reed's late-session cascade catalog at `1e8a02b` + `a823438`
  assigned #108-112 for the peer-navigator cluster:
  - #108: `the-peer-IS-a-pain-driven-bounded-ontological-navigator`
    (META)
  - #109: `algedonic-gradient-is-navigation-surface` (sub)
  - #110: `depth-is-winding-is-spawn-parameter` (sub)
  - #111: `@magic-is-level-shift-ceremony` (sub)
  - #112: `@reflection-was-naming-artifact` (O5 consumption)
- The #108-112 cluster is a **peer-navigation** cluster: how peers
  observe, how they parameterize depth, how they lift, how they
  consume O5's remnant.
- This recognition candidate is a **boundary-linearization**
  claim: how @shatter mediates every @io crossing between
  substrate and target-format.
- The two clusters are adjacent but distinct in structure. The
  peer-navigation cluster is @peer-internal + @torus-topological;
  this recognition is @shatter-@io-boundary + target-format-
  codomain-algebraic.
- **#113 is the natural adjacent slot.** Composing into #108-112
  as a fourth sub-recognition would over-cluster: the peer-navigator
  META is about how a peer observes its own eigenboard; this META
  is about how the substrate crosses to any consumer. Different
  substrate-altitudes.

**Sub-recognition candidate accompanying #113:**

- **#114 candidate:** `@data-and-@code-namespaces-ARE-@shatter-
  codomains` (per §5.5). Promotes when the second concrete
  `shards/data/*.mirror` file lands (second-instance trigger).

**Alternative: #113 as META with sub-slots for the four §4 claims.**

Under this reading, #113 is the META and:
- #114: `@shatter-codomain-IS-@data-or-@code-target-format`
  (§4.1 + §5.5)
- #115: `@shatter-distributes-over-@glue.compose` (§4.2)
- #116: `@shatter-preserves-monotone-descent` (§4.3)
- #117: `@shatter-composes-with-@fate-at-shard-altitude` (§4.4)

Adjudication: Reed to decide (per Alex's delegation "your call, I'm
not interested in assigning numbers"). Mara-recommendation: **#113
as META + #114-117 sub-slots reserved** but only landed at second-
citation for each. Two-tick discipline preserved.

---

## §9 Adjudication queue — top 5 for Alex

Ordered by Mara-perceived urgency for arc-progress unblocking.

### Q1. Recognition candidate name — foundational vs compressed

The recognition is landing at two names simultaneously (§1):
- Foundational: `@shatter-IS-the-@io-linearization-operator`
- Compressed: `@shatter-is-the-@io-linearization-operator`

Question: Land under the compressed name from tick 1 (two-tick
discipline: readable name over foundational), or hold the compressed
form for the second-citation collapse tick?

**Mara-recommendation:** Land under the foundational form THIS tick;
collapse to compressed form when `@data-and-@code-namespaces-ARE-
@shatter-codomains` lands as second-instance witness. This preserves
the two-tick collapse discipline honestly. Reed's O5 consumption
tick (#112) is the precedent shape.

### Q2. Sub-recognition slot organization — cluster #113 or spread #113-#117?

Per §8, two options:
- Option A: `#113` is the atomic recognition; `#114`
  `@data-and-@code-namespaces-ARE-@shatter-codomains` is the only
  sub-recognition at this tick.
- Option B: `#113` is the META; `#114-#117` are the four §4 claims
  as sub-recognitions.

**Mara-recommendation:** Option A. Reed's cluster catalog convention
(`1e8a02b`) suggests META + explicit-sub-recognitions only when the
subs each carry independent operational weight. The four §4 claims
are cohesive-under-one-claim (they discharge the SAME operator's
properties at four altitudes); they don't independently warrant
recognition-numeric-ID sinks. Option A keeps the numeric-ID space
scoped to distinct recognitions.

### Q3. Math extraction timing — extract now to `docs/math/shatter-lens/` or wait for second citation site?

Per §7, four candidate math files are named; the extraction is
substrate-pull-correct on second citation. Question: is the math
load heavy enough that this spec + `docs/math/2026-07-07-shatter-
as-bidirectional-lens.md` jointly count as second-citation, or do
we wait for a NEW third spec to force the extraction?

**Mara-recommendation:** **Wait for a third spec.** This spec cites
through the sibling math doc; the sibling doc IS the math root at
its own path. Extracting to `docs/math/shatter-lens/` today would
create a new root with only two consumer sites (both from today's
arc); the small-consolidation rule says wait for a third pull.

### Q4. `@shatter(s, target) -> imperfect` signature — land the substrate-decl this tick or forward-promise?

Per §4.1, the foundational form generalizes the existing
`project(a: au) -> shatter` to `@shatter(s, target: ref) ->
imperfect`. Question: land the shard revision at
`shards/mirror/shatter.mirror` this tick, or forward-promise the
substrate-decl and let the next-consumer's PR carry it?

**Mara-recommendation:** **Forward-promise.** The existing `project`
is not wrong; it is the identity-target case. The generalized form
lands when the second concrete consumer needs a non-identity
target. Two-tick discipline. Today's spec is math-first; substrate
mutation is deferred.

### Q5. MCP wrapper collapse timing — refactor `bin/mirror-mcp` now (§5.1) or wait for @mcp.serve substrate landing?

Per §5.1, `bin/mirror-mcp` becomes `@shatter --target @data/json`
dispatch + JSON-RPC framing when the substrate collapse lands.
Currently the wrapper carries custom JSON formatting. Question:
refactor immediately to `@shatter` dispatch, or wait for
`@mcp.serve` substrate landing (Phase H #5-#6 vicinity per
`docs/loop/phase-h-deferral-2026-07-08.md`)?

**Mara-recommendation:** **Wait for `@mcp.serve`.** Refactoring
`bin/mirror-mcp` today would require the `@shatter --target
@data/json` binary flag to exist. That's a bootstrap Rust landing
downstream of `@data/json` species landing. Wait for the substrate
to be in place; do the refactor at that tick.

### Q6 (bonus). Recognition numeric-ID confirmation

Per §8, `#113` is the recommended slot. Question: confirm `#113`
or reassign?

**Mara-recommendation:** `#113`. Adjacent to #108-112 but distinct
cluster. If Reed's cluster-organizational discipline says otherwise
(e.g., the recognitions catalog wants a gap), reassign to `#120+`
or later.

---

## Related

- [[docs/math/2026-07-07-shatter-as-bidirectional-lens]] — the lens
  tier. Foster 2007 + Coquand 2018 + kintsugi Banach contraction.
  This spec cites through §3.1 + §3.3 + §3.4.
- [[docs/specs/shatter-transformer-bidirectional-v0.1]] — the
  substrate-decl refinement at tick-74 (Mara 2026-06-22). Adds
  `parse` + `shatter_round_trip` bilateral. Landing site for the
  bidirectional-lens shard revision.
- [[docs/math/2026-07-07-glue-cyberpunk-fate-composition]] — the
  engine tier. `@glue(@cyberpunk, @fate)` as geometrically-
  constrained numerical inference. `the-restriction-map-IS-the-
  geometric-constraint` recognition candidate promoted by Seam
  `6396306`. Composes with this spec's linearization tier at the
  shard-altitude.
- [[docs/specs/beam-as-substrate-primitive]] — beam as substrate
  carrier verb at cli altitude (Mara `b6358c1`). §6 of this spec
  integrates with beam's §6 Q5.
- [[docs/specs/fate-silicon-metalogue-in-void-duality-basis]] — the
  `@fate/algebra ⇔ @silicon/algebra IS @metalogue` closure
  (Mara `a18ca90`). Composes with §4.4 (@shatter × @fate).
- [[docs/specs/peer-as-pain-driven-bounded-ontological-navigator]]
  — the peer-navigator cluster (#108-112) that this recognition
  candidate sits adjacent to (Mara `77fe92d`).
- [[docs/specs/recognitions/recognition-99-mirror-spec-is-lambda-zero]]
  — `mirror.spec` IS λ₀ (2026-07-01). The Banach contraction fixed
  point that §4.3's monotone descent preservation cites.
- [[docs/insights/2026-06-09-mirror-as-content-addressed-build-system]]
  — Recognition #43 (mirror IS content-addressed build system). The
  OID-equality reading of lens laws (§3.3 sibling math) grounds
  here.
- [[shards/mirror/shatter]] — the current `@mirror/shatter`
  substrate-decl. `project(a: au) -> shatter` + `ingest(s: shatter)
  -> au`. This spec forward-promises the target-parametric
  generalization.
- [[shards/mirror/lens/cli/shatter]] — the cli stage. `stage
  @mirror/lens/cli/shatter { default settle }` per cli-as-prism §7.
  The cli altitude's linearization dispatch.
- [[shards/glue]] — `@glue` family-root. `translate` + `compose` +
  correspondence + restriction. §4.2's distributive law cites here.
- [[shards/fate]] — `@fate` family-root. `restricted_state_space` +
  `roll` + inference. §4.4's composition-with-fate cites here.
- [[shards/kintsugi]] — `@kintsugi` root prism family. Banach
  contraction fixed-point. §4.3's monotone descent preservation
  cites here.
- [[shards/io]] — `@io` universal boundary family root. The
  altitude this spec's recognition candidate lives at.
- [[shards/code]] — `@code` universal grammar-at-an-altitude
  discipline. One half of @shatter's codomain algebra.
- [[shards/mirror/store]] — the content-addressed floor.
  Recognition #43's substrate-decl carrier. §3.8 + §4.3's
  content-address preservation grounds here.
- [[boot/std/beam]] — mirror's own beam substrate at boot altitude.
  §3.7 cite.

---

## Under-300-word summary

**Main claim.** `@shatter` IS the linearization operator at the
substrate-to-@io boundary. Every @io crossing mirror performs —
MCP JSON-RPC, LSP JSON-RPC, HTTP encoding, subprocess argv,
file-write, git-object write, wire transport, metal-shader IR
emission — is one substrate-decl concept realized at many
altitudes. The concept is `@shatter(shard, target)`; the `target`
is which `@data/*` or `@code/*` (or `@io/*`) codomain species the
linearization projects into. The MCP formatting code in
`bin/mirror-mcp`, the `.shatter` on-disk projection, the LSP
future wrapper, and every other @io site are the same operator at
different target altitudes.

**Duality shape.** Recognition #58 (LANDED, promoted 2026-06-11):
`@fate` IS optical inference — non-linear internal transformation
IN feature-space; crosses to decision-space. This candidate: `@shatter`
IS linearization — non-linear internal shard → linear external
representation; crosses to target-format-space. Both boundary
crossings. Both compose under lens laws (Foster 2007, sibling math
`7978f84`). Both preserve structure per altitude. They compose at
the shard-altitude: `@shatter(@fate.roll(space, hole).geometric_
formalization, target)`.

**Substrate-already-had-the-word discoveries.** Zero. `@shatter`
LANDED 2026-06-06 as bidirectional lens; `@io` LANDED per Recognition
#50; `@data/*` referenced by name across `shards/io/algebra.mirror`
already; `@code/*` LANDED as universal grammar; Recognition #43
(mirror IS content-addressed build system) already carries the
byte-equality-at-OID reading. This spec is a naming-tick, not a
substrate-introduction tick. The substrate had the operator; the
consumers coded independently around it.

**Top-3 Alex-adjudication questions** (from §9):

1. Recognition candidate name — land under foundational form
   `@shatter-IS-the-@io-linearization-operator` or the compressed
   `@shatter-is-the-@io-linearization-operator` (Q1)?
2. Recognition numeric-ID — confirm `#113` as the adjacent slot
   to #108-112, or reassign (Q6)?
3. MCP wrapper collapse timing — refactor `bin/mirror-mcp` now
   or wait for `@mcp.serve` substrate landing (Q5)?

Math extraction: NOT extracted this tick. Deferred to third-
citation-site trigger per §7 two-tick discipline. Candidate root
path if/when extracted: `docs/math/shatter-lens/`.
