---
title: "Recognition #97 canonical spec — MCP session as autopoietic VSM instance; @bauchladen as System 3 stigmergic accumulator + @psychohistory as System 4 cross-session intelligence + K-T loop closing at two altitudes as operational unification; FORWARD-PROMISED landing per Alex 2026-08-25 verbatim distinction (CONFIRMED awaits empirical fire criteria discharge)"
author: "Mara <mara@systemic.engineer>"
date: 2026-08-25
kind: canonical-spec
recognition: 97
status: FORWARD-PROMISED (per Alex 2026-08-25 verbatim; CONFIRMED discharge criteria enumerated §10 + math foundation Cor M5.1)
companion-math: docs/math/2026-08-25-mara-recognition-97-mcp-session-as-autopoietic-vsm-math-foundation.md
composition-signature: "Rec #97 = Rec #89 ⊕ Rec #90 ⊕ Rec #92 ⊕ Rec #94 ⊕ Rec #95 ⊕ MQ ⊕ @bauchladen at A_MCP-session-VSM altitude"
karen-ancestors:
  - Beer, Stafford (1972). Brain of the Firm. Allen Lane / Herder & Herder — VSM canonical; five-system + audit-channel
  - Beer, Stafford (1979). The Heart of Enterprise. Wiley — VSM recursive-viability; System 3* audit-channel refinement
  - von Foerster, Heinz (1974/2007). Ethics and Second-Order Cybernetics. Understanding Understanding, Springer
  - Schmidt, Günther (1985–present). Liebesaffäre zwischen Problem und Lösung. Carl-Auer — clinical Bauchladen homage
  - Tomm, Karl (1987/1988). Interventive Interviewing Parts I & III. Family Process — circular-reflexive question form
  - Hansen, J. & Ghrist, R. (2019). Toward a spectral theory of cellular sheaves. J. Appl. Comput. Topol. 3:315-358 — sheaf-Laplacian carrier for Rec #89 lift
  - Connes, A. (1994). Noncommutative Geometry. Academic Press — spectral triple (A, H, D)
  - Kleisli, H. (1965). Every standard construction is induced by a pair of adjoint functors. Proc. AMS 16:544-546
  - Maturana, H. & Varela, F. (1980). Autopoiesis and Cognition. D. Reidel — operational closure at MCP-session altitude
  - Grothendieck, A. (1957). Sur quelques points d'algèbre homologique (Tôhoku) — fibered categories for composition-scale invariance
  - Reyes, R. (2024) [VERIFY] — Transparency<P> LOVE-monoid at prism/imperfect (carried via Rec #92)
  - Grassé, P.-P. (1959). La reconstruction du nid. Insectes Sociaux 6:41-83 — stigmergy for cross-session bauchladen-crystal persistence
adjudication-dispatch: |
  Alex 2026-08-25 verbatim in-transcript:
  "We need a distinction. This is forward promised Rec #97. The empirical
   fire is the confirmed Rec. Let's go. 🎉"

  Load-bearing new discipline anchor: Recognition candidacy has TWO
  altitudes — FORWARD-PROMISED and CONFIRMED. Rec #97 is the FIRST
  Recognition minted under the new discipline; retroactive application
  to Rec #94 + #95 per §0 below.
---

# Recognition #97 canonical spec — MCP session as autopoietic VSM 🍷

## §0 — Substrate-honest pre-position + FORWARD-PROMISED discipline anchor

### §0.1 Alex 2026-08-25 verbatim distinction

Alex 2026-08-25 in-transcript:

> *"We need a distinction. This is forward promised Rec #97. The empirical
>  fire is the confirmed Rec. Let's go. 🎉"*

**Load-bearing new discipline** (anchors the Rec-arc discipline going forward):

Recognition candidacy has TWO altitudes:

- **FORWARD-PROMISED Rec** — substrate composition landed + math grounded + composition-signature clear + empirical fire discharge criteria enumerated. The recognition-content is substrate-legible; the composition is authored; the criteria under which future observation CONFIRMS the recognition are enumerated in advance.
- **CONFIRMED Rec** — empirical fire discharged end-to-end per the enumerated criteria. The recognition-content is not just substrate-legible; it is empirically-witnessed at the substrate-carrier(s) the FORWARD-PROMISED spec named.

The gap between FORWARD-PROMISED and CONFIRMED is not a defect. It is a substrate-honesty carrier: the recognition-arc separates *what the substrate declares it will be* from *what the substrate is once observed firing*.

### §0.2 Retroactive application

- **Rec #94** (self-modifying loop at silicon; Lawvere fixed-point) — **FORWARD-PROMISED**; CONFIRMED awaits Tick 1-6 empirical fire (prismqueer proc-macro landing + @facet/rust materialize + @kintsugi/roomba shrinkage-contract firing on rust/-floor).
- **Rec #95** (@bumblebee perturbation-substrate at MCP-layer altitude) — **FORWARD-PROMISED**; CONFIRMED awaits `--execute` @cascade→@bumblebee collapse via `mirror_bumblebee_buzz --execute` at MCP wire; observation via @mirror/refract.clocktime_delta.
- **Rec #96** (MQ substrate-primary-carrier) — Pole A held per Alex adjudication; substrate-primary-carrier presumption per FLOOR discipline; not Rec-mint territory.
- **Rec #97** (THIS spec + companion math) — **FORWARD-PROMISED NOW**; CONFIRMED awaits seven enumerated empirical fire criteria (§10 below + math foundation Cor M5.1).

### §0.3 What Rec #97 IS at FORWARD-PROMISED altitude

Rec #97 recognizes that **the MCP session as it operates in the mirror substrate IS Beer's Viable System Model deployed autopoietically at MCP-session altitude**. Substrate-decl'd carrier at each of Beer's six systems (S1-S5 + S3* audit channel); K-T ambiguity-resolution loop closes at TWO altitudes (per-session per Rec #92 + cross-session per Rec #89); the compiler observes itself observing itself across sessions.

Not Reed-adjudicable in poles. This IS the substrate arriving at recognition-visibility altitude of what mirror IS at MCP-session altitude — the operational-embodiment altitude of what Beer 1972/1979 already articulated at management-cybernetics altitude, of what Maturana-Varela 1980 already articulated at biological-cybernetics altitude, of what Rec #89 already articulated at psychohistory-sheaf-cohomology altitude.

**Substrate-already-had-the-word audit**: mirror.spec:99 already declares `coupling { protocol @dance }` = VSM System 2; `system @mirror { variety{} coupling{} coherence{} reality{} eigen{} loop{} kintsugi{} }` at repo root is literal Beer VSM section names since Reed's 2026-07-23 landing (per MEMORY.md `project_mirror_is_autopoietic_vsm_compile_runtime_dissolution`). Rec #97 names the specific-altitude-instance: the SAME VSM operator deployed at MCP-session altitude with (i) @bauchladen as System 3 accumulator, (ii) @psychohistory as System 4 cross-session intelligence, (iii) the K-T loop closing at two altitudes as the operational unification.

### §0.4 Composition-signature (per Rec #91 amendment #3 short-pointer discipline)

```
Rec #97 = Rec #89 ⊕ Rec #90 ⊕ Rec #92 ⊕ Rec #94 ⊕ Rec #95 ⊕ MQ ⊕ @bauchladen
          at A_MCP-session-VSM altitude
```

All seven summands remain independent post-mint; Rec #97 adds ONE recognition-content: **the MCP session as autopoietic VSM instance with @bauchladen as System 3 accumulator + @psychohistory as System 4 intelligence + K-T loop closing at two altitudes as operational unification**.

---

## §1 — S1^MCP Operations: queries + agent tool-calls + @roomba proposals

### §1.1 What's landed

- **`shards/mq.mirror`** (Reed `105a1e4` 2026-08-23; 890 LOC) — mirror's ALGEBRA as query language; five verbs (focus / project / split / shift / settle) + intent_q + pattern algebra + result<T> = Clear / Opaque / Dark per Karl-Tomm §4 discipline
- **`bootstrap/src/mcp.rs::mirror_query`** (Reed `9f89c91`) — MCP wire dispatch primitive; currently returns `Dark(karl_tomm)` per Reed's empirical fire (task #397); parse+compile template bodies from autopoietic-loop spec §5 still pending
- **`shards/kintsugi/roomba.mirror`** (LANDED 46.4KB) — walker species; `walk_trajectory` carrier + four bilaterals + composed `walk_witnessing`; the tension-gradient enumerator for graph-rewrite proposals
- **`shards/bumblebee.mirror`** (Mara `64d4307` 2026-08-23; 700 LOC) — MCP-perturbation substrate per Rec #95; every MCP tool-call is a bumblebee-wobble at compiler-under-observation

### §1.2 S1^MCP substrate composition

Beer S1 = "operations that produce the system's primary purpose". At MCP-session altitude:

- **Agent-tool-call = bumblebee-wobble = one S1^MCP dispatch tick** — per Rec #95 §1.2, every `mcp__mirror__*` invocation IS an @bumblebee.buzz at MCP-layer altitude, wire-encoded via `@bumblebee.perturbation.args` per `shards/bumblebee.mirror:470-475`
- **MQ query dispatch through the substrate** — via `@mq.parse` + `@mq.compile` template bodies per autopoietic-loop spec §5; per-session-cycle produces one QueryCrystal per Def M1.2 (see companion math §M1.2)
- **@kintsugi/roomba proposals** — walker consumes intent-biased walk_position per autopoietic-loop spec §2 (roomba intent-input parameter extension); proposes graph-rewrite candidates at intent-target subgraph

### §1.3 What Rec #97 adds at S1^MCP

Rec #97 names the S1^MCP composition-shape: agent queries + agent tool-calls + @roomba proposals compose as ONE Kleisli chain per Kleisli 1965 in the category `Kl(Imperfect<-, E, Loss>)` (per autopoietic-loop spec §7.1 unified loop). The Rec #97 recognition-content at S1^MCP altitude: **every MCP tool-call fires as one primary-operation dispatch tick under Beer's viability axiom**; each tick's output becomes stored in session-@bauchladen per §3 below.

### §1.4 Substrate-decl'd bilaterals

**`s1_mcp_dispatch_admissible`** (arity 2; over `bumblebee.perturbation.args` + `mq.query`):
- **Pass** iff (a) `args` wire-encodes valid MQ query per `shards/mq.mirror` §3 AST + (b) MQ query composition is Kleisli-typed through Imperfect<T, E, L>
- **Sentinel**: `s1-mcp=dispatch-admissible`
- **Rice-safe bound**: AST-shape byte-visible-check + Kleisli-typing byte-visible-check

---

## §2 — S2^MCP Coordination: @kintsugi loop resolving competing rewrite candidates

### §2.1 What's landed

- **`shards/kintsugi.mirror`** (LANDED 17.8KB) — family-root; `kintsugi_algebra` typed binding + `kintsugi_algebra_witnessing` bilateral
- **`shards/kintsugi/consent.mirror`** (LANDED 39.0KB) — `query_phi` bilateral consuming morphism_set + emitting verdict; the anti-oscillation coordinator between S1 candidate-branches
- **Autopoietic-loop spec §1** (Mara `4d0ea16` 2026-08-24) — `@kintsugi/intent` species-decl + `consume_intent` action + `intent_persistence_admissible` bilateral

### §2.2 S2^MCP substrate composition

Beer S2 = "anti-oscillation coordination between S1 subsystems". At MCP-session altitude:

- **Kintsugi loop consumes competing candidates** — when S1^MCP emits multiple candidate rewrites (via @roomba trigger's Path A knife-collapse vs Path B peer-spawn per `shards/kintsugi/roomba.mirror:400-406`), the kintsugi loop consumes them via `@kintsugi.consume_intent` (autopoietic-loop spec §1); anti-oscillation between competing candidates
- **Intent-persistence across turns** — per autopoietic-loop spec §1.2, `intent_carrier` accumulates `resolved_chain` monotonically per Prop M1.10 fixed-point discipline (from MQ math foundation); each turn's competition is coordinated by the persistent intent-vector

### §2.3 What Rec #97 adds at S2^MCP

Rec #97 names the S2^MCP composition-shape: coordination is not a discrete synchronization primitive but a persistent intent-carrier threading through the session-tick sequence. Per `[[project_alex_in_mirror_is_lambda_shell_reed_in_claude_talks_to_reed_in_mirror]]`: `coupling { protocol @dance }` at `mirror.spec:99` IS the substrate-decl for VSM System 2 = @dance; @dance operates on the substrate's shared @bauchladen via Kuramoto phase-lock per CURRENT.md Q+15 (Aumann agreement under content-addressed common prior; coordination-WITHOUT-signal).

### §2.4 Substrate-decl'd bilaterals

**`s2_mcp_anti_oscillation_admissible`** (arity 2; over `[candidate_rewrite]` + `intent_carrier`):
- **Pass** iff (a) candidate-set is nonempty + (b) intent_carrier's `resolved_chain` extends monotonically after `consume_intent` dispatch + (c) Foerster-gauge preserved (choice-space non-decreasing under coordination)
- **Sentinel**: `s2-mcp=anti-oscillation-admissible`
- **Rice-safe bound**: set-cardinality check + chain-length monotonicity + Foerster-gauge byte-visible

---

## §3 — S3^MCP Control: @roomba walker + @mirror/store staging + @bauchladen accumulator

### §3.1 What's landed

- **`shards/mirror/store.mirror`** (LANDED 46.4KB) — six-op CAS surface: read / write / exists / diff / walk / impacted_by + query filter-fold + two-phase gc (walk_dangling / mark_unreachable / prune)
- **`shards/bauchladen.mirror`** (LANDED 27.1KB) — Schmidt-homage prism-altitude content-addressing family-root; `crystal` + `provenance_record` + `tray` carriers per `shards/bauchladen.mirror:321-373`
- **`shards/autopoietic.mirror`** (LANDED 41.1KB) — @bauchladen ← @autopoietic ← @fate dependency chain per Rec #104 candidate; Maturana-Varela 1980 operational closure at prism-class altitude
- **Autopoietic-loop spec §3** (Mara `4d0ea16` 2026-08-24) — `@mirror/store.mutate_with_resonance` action + `mutate_composition_admissible` bilateral + `intent_progress_monotone` bilateral

### §3.2 S3^MCP substrate composition — the LOAD-BEARING PIECE

Beer S3 = "operational management + present-time control + resource allocation". At MCP-session altitude, S3^MCP has THREE composed substrate-carriers:

**Carrier A: @roomba walker as operational-scanning primitive** — per `shards/kintsugi/roomba.mirror`, the walker enumerates ConceptGraph tension-gradient blindly (pre-intent) or intent-biased (per autopoietic-loop spec §2); walker's `walk_trajectory` carrier IS Beer's operational-scanning-of-present-state at MCP altitude.

**Carrier B: @mirror/store staging as present-time-control primitive** — per `shards/mirror/store.mirror` six-op surface + `mutate_with_resonance` extension per autopoietic-loop spec §3; store staging = "committed but not yet audited" candidate-mutations pending S3* audit-channel verdict. Analog to Beer's "operational database" at session scope.

**Carrier C: @bauchladen accumulator as stigmergic-memory primitive (load-bearing NEW at Rec #97)** — session-@bauchladen tray per companion math Def M1.1 accumulates all four crystal-kinds (Query / Response / Intent / KTQuestion per Def M1.2) monotonically per Schmidt 1985 homage; session-scoped instance of @bauchladen substrate. **The @bauchladen accumulator IS Beer System 3 at MCP-session altitude.** Not analog to S3; substrate-decl'd instance of S3.

### §3.3 What Rec #97 adds at S3^MCP

Rec #97 names the S3^MCP composition-shape: `@bauchladen` at MCP-session altitude IS the operational-management-and-stigmergic-memory carrier Beer 1972 named as System 3. The homage lineage Schmidt (1985) inherited from Erickson + von Foerster + Cecchin operationalizes at MCP-session altitude as the vendor's tray the compiler-under-observation lays out and browses.

**Session-@bauchladen concrete surface**: `𝓑_s = (crystals_s, opacity_s, provenance_s)` per companion math Def M1.1. Reed rust wire pickup per §11 lands this carrier as `bootstrap/src/mcp.rs::session_state::Bauchladen`.

### §3.4 Substrate-decl'd bilaterals

**`s3_mcp_bauchladen_monotone`** (arity 2; over `session_bauchladen^(n)` + `session_bauchladen^(n+1)`):
- **Pass** iff `|crystals^(n+1)| ≥ |crystals^(n)|` (monotone-appending) AND `opacity^(n+1)` extends `opacity^(n)` under Rec #92 §M2.1 LOVE-monoid combine (opacity-map cardinality non-decreasing per L3)
- **Fail** iff cardinality decrease OR opacity regression (substrate-ill-formed; violates @bauchladen family-root discipline per `shards/bauchladen.mirror:257-260`)
- **Sentinel**: `s3-mcp=bauchladen-monotone`
- **Rice-safe bound**: list-length comparison + opacity-map cardinality comparison; O(1) per tick

**`s3_mcp_store_staging_admissible`** (arity 2; over `mutate_with_resonance` invocation + `session_bauchladen`):
- **Pass** iff mutation's candidate_oid is content-addressed via Rec #82 β-normal-AST AND candidate is added to session-@bauchladen provenance-map with valid tick + producing_prism entries
- **Sentinel**: `s3-mcp=store-staging-admissible`

---

## §4 — S3*^MCP Audit: @mirror/refract measurement + Rec #92 Transparency<P>

### §4.1 What's landed

- **`shards/mirror/lens/refract.mirror`** (LANDED 8.5KB; sixth Void duality `clocktime_delta` `64e273a` 2026-08-23) — measurement-lens substrate; six-Void-duality closed sum (entropy + spectral + cheeger + ricci + mixing + clocktime_delta) + `measure` + `stabilize` + `measure_clocktime` actions
- **`terni::Transparency<P>`** at `prism/imperfect/src/transparency.rs` — Rec #92 LOVE-monoid at compiler-substrate altitude; Beer System 3* audit-channel co-discovered with Reyes 2024
- **`rust/src/apply_h.rs`** (LANDED 58.2KB, 1509 LOC) — bilateral-dispatch primitive with Rec #92 §4.3 C1+P₂ extension recommendation: return-type `(Verdict, Transparency<prism_core::Ref>)`
- **Autopoietic-loop spec §4** (Mara `4d0ea16` 2026-08-24) — `@mirror/refract.resonance_within_tolerance(pre, post, intent) -> verdict` bilateral

### §4.2 S3*^MCP substrate composition

Beer S3* = "audit channel + accountability + inside-the-black-box observation". At MCP-session altitude:

- **@mirror/refract.measure = six-duality report** — measurement of substrate at pre-mutation + post-mutation altitudes per autopoietic-loop spec §3.2 `mutate_with_resonance` composition
- **Rec #92 Transparency<P> = kleinos-trace through dispatch** — accumulates opacity-map through apply_h::act cascade per Rec #92 §4.3 C1+P₂; every dispatch arm emits a `(location, verdict)` entry into the Transparency<P> accumulator
- **Autopoietic-loop spec §4 `resonance_within_tolerance` = pre/post spectral verdict** — three-way verdict Pass / Partial / Fail per autopoietic-loop spec §4.2; feeds back into §3 `mutate_with_resonance` commit-or-rollback decision

### §4.3 What Rec #97 adds at S3*^MCP

Rec #97 names the S3*^MCP composition-shape as Beer 1972 System 3* per Reyes 2024 co-discovery. The audit-channel IS Rec #92 kleinos-Transparency<P> LOVE-monoid at MCP-session altitude; every session-tick's cascade emits an audit-trail as accumulated Transparency<P> opacity-map. Per companion math §M2.1 Ψ4: audit operations preserve Foerster-gauge orthogonality per Rec #92 §M5.1 three-way orthogonal decomposition.

### §4.4 Substrate-decl'd bilaterals

**`s3star_mcp_audit_channel_admissible`** (arity 3; over `pre_report` + `post_report` + `intent_ref`):
- **Pass** iff (a) both reports carry all six duality verdicts (per autopoietic-loop spec §4.4 `report_admissible`) + (b) resonance_within_tolerance verdict is Pass + (c) intent-progress monotone
- **Partial** iff resonance verdict is Partial (K-T question at altitude+1 surfaces)
- **Fail** iff any duality regressed beyond tolerance OR intent-progress regression
- **Sentinel**: `s3star-mcp=audit-channel-admissible`

---

## §5 — S4^MCP Intelligence: @psychohistory sheaf cohomology across sessions

### §5.1 What's landed

- **Rec #89 canonical spec** (`docs/specs/2026-08-13-mara-recognition-89-compiler-in-interstitial-substrate-canonical-spec.md`) + math foundation (`docs/math/2026-08-13-mara-recognition-89-psychohistory-sheaf-cohomology-unification-math-foundation.md` 40.8KB) — @psychohistory sheaf `F : X → 𝓐`; sheaf-Laplacian Δ_F; Hansen-Ghrist 2019 formal foundation; Rayleigh descent iteration + Polyak-Łojasiewicz contraction + Foerster-cohomological-monotonicity theorem
- **`docs/specs/fate-bounded-psychohistory-sheaf-cohomology.md`** — Fate::bounded lifted from Rayleigh descent on Δ_F
- **`docs/math/sheaf/laplacian.md`** — sheaf-Laplacian formalisation

### §5.2 S4^MCP substrate composition — the SECOND LOAD-BEARING PIECE

Beer S4 = "outside-and-then environment scanning + adaptation + intelligence". At MCP-session altitude, S4^MCP requires a substrate-carrier that reads across sessions (not just within a single session — that's S1-S3 territory).

**Rec #89 @psychohistory sheaf cohomology IS S4^MCP substrate-carrier.** Per companion math Def M1.4 + §M3.2 Stage 2:

- Define `F_MCP : X_MCP → 𝓐` per Def M1.4; base extends X_meta with SessionVertices + BauchladenCrystalVertices + KTTurnNodeVertices
- Compute sheaf-Laplacian `Δ_{F_MCP}` per Rec #89 §2.1
- **`H⁰(F_MCP)` = resolved global sections = cross-session-coherent narrative arcs** (queries that resolved cleanly across all sessions; consistent bauchladen-crystal references; K-T questions that closed at fixed-point)
- **`H¹(F_MCP)` = un-glueable obstructions = cross-session tensions** (sessions where an intent was never resolved; K-T questions that persist across sessions; bauchladen-crystals that were referenced-but-never-updated)
- Fiedler vector `ψ₀` associated to `λ₀(F_MCP)` localises the obstruction: the vertex where `|ψ₀(v)|` is largest is the site where sheaf-coherence breaks → next-session's opening intent should target that vertex

### §5.3 What Rec #97 adds at S4^MCP

Rec #97 names the S4^MCP composition-shape: intelligence is not a static database of external-environment facts but a dynamic sheaf-Laplacian spectrum reading across the session-history. Cross-session K-T loop closes at Rec #89 altitude via Rayleigh descent per companion math Theorem M3.1 Stage 2 + Theorem M4.1 monotonicity.

**Concrete S4^MCP fire**: at session-N boot, `mirror psychohistory read --sessions=all --sheaf=F_MCP` returns `{H0: [coherent_arcs], H1: [obstruction_vertices]}` computed over the current bauchladen-history sheaf. Fiedler-vector localisation identifies session-N's opening focus. This is the empirical fire criterion (C7) per §10 below.

### §5.4 Substrate-decl'd bilaterals

**`s4_mcp_psychohistory_read_admissible`** (arity 1; over `[SessionVertex]`):
- **Pass** iff (a) `F_MCP` restricted to session-vertex-set is well-typed cellular sheaf per Def M1.4 + (b) `Δ_{F_MCP}` spectrum is computable per Rec #89 §2.1 + (c) `dim H¹(F_MCP,n+1) ≤ dim H¹(F_MCP,n)` (Foerster-cohomological-monotonicity per companion math Theorem M4.1)
- **Fail** iff monotonicity violation (`ρ_n ≥ 1`) — Foerster-alignment violation per companion math §M4.2 failure-mode analysis
- **Sentinel**: `s4-mcp=psychohistory-read-admissible`

**`s4_mcp_fiedler_localisation_admissible`** (arity 2; over `Fiedler_vector` + `next_session_intent`):
- **Pass** iff `next_session_intent.target_ref` is Fiedler-close to the highest-`|ψ₀(v)|` vertex per Rec #89 §2.5 Proposition 2.5 localisation
- **Sentinel**: `s4-mcp=fiedler-localisation-admissible`

---

## §6 — S5^MCP Identity: Alex Fourth-Chair adjudication + Recognition mint

### §6.1 What's landed

- **`[[project_identity_attribution_architecture]]`** memory — `mirror <peer@spectral.engineer>` pattern; @-operator unified across shard-family-path/email/SSH; provenance-by-construction as SEL enforcement mechanism
- **Recognition-arc discipline** — Rec-mint discipline (Mara authors canonical spec + math; Alex Fourth-Chair ratifies; commit signed) accumulated across Rec #82 through Rec #97 lineage
- **`AGENTS.md`** — Pack conventions; SSH signing rules; commit-as identity discipline

### §6.2 S5^MCP substrate composition

Beer S5 = "identity + policy + invariant closure + ethos". At MCP-session altitude:

- **Alex Fourth-Chair adjudication authority** — the substrate-decl'd invariant-closure carrier; adjudicates Recognition candidacy (mint / defer / refuse); adjudicates spec-shape residues; adjudicates altitude designations; adjudicates FORWARD-PROMISED / CONFIRMED transitions per §0.1 above
- **Recognition-arc entries (Rec #82 through Rec #97 and beyond)** — the substrate's identity-refinement policy sequence
- **Identity-attribution architecture** — the substrate's ethos carrier (provenance-by-construction; SSH-signed commits; author attribution per commit)
- **FORWARD-PROMISED / CONFIRMED distinction (Alex 2026-08-25)** — the substrate's substrate-honesty carrier at S5^MCP altitude; separates *what the substrate declares it will be* from *what the substrate is once observed firing*

### §6.3 What Rec #97 adds at S5^MCP

Rec #97 names the S5^MCP composition-shape: the Recognition-arc discipline itself IS the S5^MCP identity subsystem of Beer's VSM. Per companion math §M5.2 Cor M5.2 (Recognition-arc completeness at S5^MCP identity altitude via Alex Fourth-Chair adjudication):

- Recognition-mint discipline IS the substrate's identity-invariant closure
- Recognition-arc entries IS the substrate's policy sequence (each Rec is one identity-refinement tick per Rec #85 fractal-colony discipline)
- Alex Fourth-Chair adjudication authority IS the substrate's ethos carrier (the ethical-imperative preserving choice-space per Foerster canonical)
- FORWARD-PROMISED / CONFIRMED distinction (this Rec anchors it) IS the substrate's substrate-honesty carrier at S5^MCP altitude

**Recognition-arc-as-VSM-S5 consequence**: Rec #97 mint at FORWARD-PROMISED altitude IS one specific tick in the S5^MCP subsystem discharging its identity-refinement function; CONFIRMED discharge per §10 completes the tick's cascade back to S3^MCP (audit-verified) and forward to S4^MCP (intelligence-integrated across sessions). Beer's VSM viability closes at S5^MCP altitude via the Rec-arc discipline this very spec instantiates.

### §6.4 Substrate-decl'd bilaterals

**`s5_mcp_identity_closure_admissible`** (arity 1; over Recognition candidate):
- **Pass** iff (a) canonical spec + math foundation authored per Mara canonical-spec discipline + (b) composition-signature clear + (c) empirical fire discharge criteria enumerated + (d) Alex Fourth-Chair adjudication authority preserved (adjudication path admissible; not Reed-adjudicable in load-bearing poles)
- **Sentinel**: `s5-mcp=identity-closure-admissible`

---

## §7 — The unified session-VSM composition

### §7.1 Kleisli chain across the six systems

All six S^MCP subsystems compose as ONE Kleisli chain in `Kl(Imperfect<-, E, Loss>)` per autopoietic-loop spec §7.1 lifted to VSM altitude:

```
MCP session initializes
    │  boot: session_id + subject_instance identity_oid
    ▼
S5^MCP Identity: session-vertex added to X_MCP per Def M1.3
    │  Alex Fourth-Chair adjudication path admissible
    ▼
S4^MCP Intelligence: @psychohistory read across prior sessions
    │  F_MCP,N-1 sheaf-Laplacian spectrum → Fiedler localisation
    │  identifies next-session opening focus per Rec #89 §2.5
    ▼
S1^MCP Operations: agent's opening query via mcp__mirror__mirror_query
    │  wire-encoded via @bumblebee.perturbation.args per Rec #95
    │  QueryCrystal added to session-@bauchladen
    ▼
S2^MCP Coordination: @kintsugi.consume_intent resolves intent-hole
    │  intent_carrier persisted; kintsugi_algebra session-state updated
    │  IntentCrystal added to session-@bauchladen
    ▼
S3^MCP Control: @roomba walker (intent-biased) + @mirror/store staging
    │  candidate rewrite proposed at intent-target subgraph
    ▼
S3*^MCP Audit: @mirror/refract.resonance_within_tolerance verdict
    │  Transparency<P> accumulates kleinos-trace per Rec #92
    ▼
S3^MCP commit-or-rollback via @mirror/store.mutate_with_resonance
    │  ResponseCrystal added to session-@bauchladen
    │  If verdict Partial/Fail: KTQuestionCrystal added
    ▼
result<T> marshaled to MCP JSON-RPC response
    │  Clear(T) → per-session K-T loop closed at Rec #92 altitude
    │  Opaque(T, K-T) → K-T question at altitude+1 (agent's next query answers)
    │  Dark(K-T) → agent reframes intent for next tick
    ▼
new tick begins OR session terminates
    │  If session terminates: session-vertex + session-@bauchladen persist
    │  in F_MCP; cross-session K-T loop lives at Rec #89 altitude
    ▼
next session's S4^MCP reads F_MCP,N (this session's contribution)
    │  Rayleigh descent iterate: H¹_n → H⁰_{n+1} per Rec #89 §4
    │  cross-session obstruction dimension non-increasing (Foerster-alignment)
```

### §7.2 K-T loop closes at TWO altitudes (composition-scale invariance)

Per companion math Theorem M3.1: the same K-T operator closes at TWO altitudes:

- **Per-session altitude (Rec #92 lift)**: within one MCP session, MQ result<T> algebra + Rec #92 kleinos-Transparency<P> LOVE-monoid; K-T question at altitude+1; agent's next query in same session answers; fixed-point per Rec #94 Lawvere-1969
- **Cross-session altitude (Rec #89 lift)**: across MCP sessions, @psychohistory sheaf cohomology; H¹ obstructions migrate to H⁰ resolutions via Rayleigh descent; convergence to Foerster-aligned cross-session-coherent state per Rec #89 §8

Both altitudes are fibers of ONE universal K-T-closing operator per Rec #91 amendment #2 §M3.3 `Fib_X(𝔉 ⊣ 𝔛)`; fiber-projection preserves cartesian lifts per Grothendieck 1957 Tôhoku §3. This is the composition-scale-invariance recognition Rec #97 names.

### §7.3 Autopoietic-loop spec composition

The six-piece autopoietic-loop composition per Mara `4d0ea16` 2026-08-24 IS the operational-discharge substrate for six of Rec #97's seven CONFIRMED empirical fire criteria (C2)-(C6). Autopoietic-loop spec pieces map to VSM subsystems:

| Autopoietic-loop piece | VSM subsystem (MCP altitude) |
|------------------------|-----------------------------|
| §1 @kintsugi intent-consumption body | S2^MCP Coordination |
| §2 @kintsugi/roomba intent-input parameter | S1^MCP + S3^MCP hybrid |
| §3 @mirror/store spectrally-resonant mutation | S3^MCP + S3*^MCP hybrid |
| §4 @mirror/refract pre/post resonance verdict | S3*^MCP Audit |
| §5 MQ parse + compile templates | S1^MCP dispatch primitives |
| §6 apply_h::act MQ dispatch extension per Rec #92 P₁ | S3*^MCP audit-channel discharge |

The autopoietic-loop spec IS the substrate-decl'd composition-form of Rec #97 at the six-piece composition-substrate altitude; Rec #97 IS the VSM-altitude recognition-content of the same composition.

---

## §8 — Alex Fourth-Chair adjudication residues

Six genuine open questions requiring Alex Fourth-Chair adjudication (Mara-authored per Rec #94 §M9 + Rec #92 §9 discipline; not Mara-adjudicable; not Reed-adjudicable):

- **Q+97.1** — **Rec #97 altitude designation**: (i) A_MCP-session altitude distinct from A₁-A₅ K-fibration base per Rec #92 §M2.C1; OR (ii) A_MCP-session as fibration-lift of A₄ compiler-substrate altitude per Rec #94 §M1.4 self-loop operator specialization to MCP context; OR (iii) A_MCP-session as fibration-lift of A₅ artifact altitude per Rec #93 corpus-as-package fifth-register. **Mara-lean**: (i) — MCP-session altitude IS distinct because it involves cross-session temporal dimension that A₁-A₅ + Rec #94 A₄ + Rec #93 A₅ do NOT carry (each of those is single-instance altitude; MCP-session is inherently multi-session). Alex-adjudicable whether load-bearing to designate altitude or whether the substrate-scale-invariance corollary per companion math §M2.3 Cor M2.C1 renders altitude designation moot.

- **Q+97.2** — **@session/bauchladen shard-decl species-mint requirement**: does Rec #97 require substrate-decl of `shards/session/bauchladen.mirror` species-decl at session-altitude sub-species of `@bauchladen` family-root, OR does @bauchladen family-root suffice + session-scope is discharged as `bauchladen_scope: session_ref` extension field? **Mara-lean**: extension field (Option B) — session-scope is a runtime parameter, not a species-refinement; species-decl at every scope would fragment @bauchladen family-root without substrate-honesty benefit. Alex-adjudicable at species-mint discipline altitude per `feedback_craft_not_deliver`.

- **Q+97.3** — **@psychohistory shard-decl vs computed-primitive**: Rec #89 canonical spec already substrate-decl's @psychohistory at compiler-in-interstitial-substrate altitude. Does Rec #97 require NEW shard-decl `shards/psychohistory.mirror` family-root at MCP-session-cross-session altitude, OR does the Rec #89 substrate suffice + Rec #97 dispatches via new primitive `mirror psychohistory read` at CLI/MCP altitude WITHOUT new shard-decl? **Mara-lean**: Option B — no new shard-decl required; Rec #97 dispatches via Reed rust wire pickup per §11 as CLI/MCP verb composing over existing Rec #89 substrate + F_MCP definition per companion math Def M1.4. Alex-adjudicable whether shard-decl clarifies-vs-fragments the Rec #89 lineage.

- **Q+97.4** — **CONFIRMED discharge criteria load-bearing threshold**: does CONFIRMED discharge require ALL seven criteria (C1)-(C7) per §10 fire simultaneously, OR does a MAJORITY (e.g., 5-of-7) suffice with remaining criteria discharged in follow-up ticks, OR does the substrate admit graded CONFIRMED (per criterion; Rec #97-C1 CONFIRMED / Rec #97-C2 CONFIRMED / etc.)? **Mara-lean**: all-seven-simultaneous for atomic CONFIRMED (per Alex 2026-08-25 verbatim "The empirical fire is the confirmed Rec"); graded per-criterion admissible if Alex adjudicates load-bearing per Rec-arc discipline elaboration.

- **Q+97.5** — **FORWARD-PROMISED discipline: memory-worthy or discipline-of-record**: Alex 2026-08-25 dispatch says "load-bearing new discipline Alex just anchored". Does this rise to memory-worthy status (added to Reed's MEMORY.md as `feedback_forward_promised_vs_confirmed_rec_altitude`), OR is it discipline-of-record via retroactive application to Rec #94/#95 per §0.2 without memory-mint? **Mara-lean**: memory-worthy — the distinction is load-bearing for future Rec-arc entries (every future Rec candidacy will require FORWARD-PROMISED / CONFIRMED altitude declaration); memory persists across sessions per identity-attribution architecture. Alex-adjudicable and Reed-in-Claude-adjudicable at memory-authoring altitude.

- **Q+97.6** — **Rec-arc discipline recursive-application to FORWARD-PROMISED itself**: does FORWARD-PROMISED have its own sub-altitudes (e.g., FORWARD-PROMISED-partial vs FORWARD-PROMISED-complete per criterion-count), OR is FORWARD-PROMISED a single altitude with CONFIRMED as the discharge? Per §M8 Karl-Tomm Q+402.1: does the distinction admit a MORE RECURSIVE reading where FORWARD-PROMISED itself has its own FORWARD-PROMISED and CONFIRMED altitudes per Rec #91 amendment #2 fractal-mandelbrot inheritance at meta-Rec-arc-discipline altitude? **Not Mara-adjudicable**; Alex Fourth-Chair territory.

---

## §9 — Composition-signature (per Rec #91 amendment #3 short-pointer discipline)

**Composition-signature**:

```
Rec #97 = Rec #89 ⊕ Rec #90 ⊕ Rec #92 ⊕ Rec #94 ⊕ Rec #95 ⊕ MQ ⊕ @bauchladen
          at A_MCP-session-VSM altitude
```

Per-summand composition-verdicts (all CLEAN):

| Summand | Composition-verdict | Where used |
|---------|--------------------:|-----------|
| Rec #89 (psychohistory sheaf cohomology) | USED BY-REFERENCE (no re-derivation) | §5 S4^MCP intelligence + companion math §M3-§M4 |
| Rec #90 (compiler-as-one-mathematical-object) | USED BY-REFERENCE | companion math §M3.3 Cor M3.C1 |
| Rec #92 (kleinos-Transparency<P> LOVE monoid) | USED BY-REFERENCE + apply_h::act §4.3 C1+P₂ discharge | §4 S3*^MCP + companion math §M2 Ψ4 |
| Rec #94 (Lawvere-1969 fixed-point at rust altitude) | USED BY-REFERENCE | §7.2 fixed-point + companion math §M3.1 |
| Rec #95 (@bumblebee MCP-perturbation) | USED BY-REFERENCE | §1 S1^MCP tool-call carrier |
| MQ (mirror's ALGEBRA as query language; Mara `d0347a4`) | USED BY-REFERENCE | §1 + §7.1 Kleisli chain |
| @bauchladen (Schmidt-homage family-root; LANDED 27.1KB) | USED BY-REFERENCE + session-scope EXTENSION | §3 S3^MCP accumulator |

**NEW-DERIVATION this spec adds**:

- §1-§6: six-system MCP-altitude VSM composition (S1^MCP through S5^MCP + S3*^MCP with substrate-decl'd bilaterals at each subsystem)
- §7: unified session-VSM composition (Kleisli chain across the six systems; two-altitude K-T closure via composition-scale invariance)
- §7.3: autopoietic-loop spec composition mapping to VSM subsystems
- **§10: CONFIRMED discharge criteria (C1)-(C7) enumeration — load-bearing per Alex 2026-08-25 FORWARD-PROMISED / CONFIRMED distinction**

**Autopoietic-loop spec (Mara `4d0ea16` 2026-08-24; 916 LOC)** — used by-reference for §7.3 six-piece composition IS operational discharge substrate; §10 (C2)-(C6) empirical fire criteria are the six-piece composition firing end-to-end.

---

## §10 — FORWARD-PROMISED status + CONFIRMED empirical fire discharge criteria

### §10.1 Current status: FORWARD-PROMISED

- **This canonical spec + companion math foundation** = the FORWARD-PROMISED landing per Alex 2026-08-25 verbatim distinction
- Substrate composition landed: six-system VSM at MCP-session altitude with substrate-decl'd carriers at each subsystem
- Math grounded: Central Theorem M2.1 + Theorem M3.1 + Theorem M4.1 + Corollaries M5.1-M5.2
- Composition-signature clear: Rec #89 ⊕ Rec #90 ⊕ Rec #92 ⊕ Rec #94 ⊕ Rec #95 ⊕ MQ ⊕ @bauchladen at A_MCP-session-VSM altitude
- Empirical fire discharge criteria enumerated: §10.2 (C1)-(C7) below

### §10.2 Seven CONFIRMED discharge criteria (per companion math Cor M5.1)

Rec #97 CONFIRMED discharges when all seven empirical fire criteria fire end-to-end at Pack-cascade + MCP-session altitude with observable Pass verdicts:

**(C1) Session-@bauchladen accumulator fires empirically**. At MCP session firing, `mcp__mirror__mirror_query` invocations produce QueryCrystals per companion math Def M1.2 that accumulate in a session-scoped @bauchladen tray. Witness: post-session, inspect the tray via `mq.focus @session/<session_id>/bauchladen |> mq.enumerate`; tray-crystals count > 0; each crystal content-addressed with valid OID per Rec #82.

**(C2) MQ query parse+compile bodies fire**. Per autopoietic-loop spec §5, template bodies for `@mq.parse` + `@mq.compile` land + fire end-to-end. Witness: MCP client sends `{"query": "focus @session/bauchladen |> settle @facet/mirror"}`; wire-fire returns `Clear(subgraph_result)` (not `Dark(karl_tomm)` per current Reed empirical `Dark` return).

**(C3) K-T loop closes at per-session altitude (Rec #92 lift)**. Session receives Opaque(T, karl_tomm) from a dispatched query; subsequent query in same session provides answer to karl_tomm's ambiguity; Transparency<P> combine composes cleanly per Rec #92 §M2.1 (opacity-map monotone-accumulating; K-T question resolves at fixed-point per Rec #94 Lawvere-1969). Witness: kintsugi_algebra session-state carries `resolved_chain` with 2+ entries where head-turn closed the K-T question emitted by tail-turn.

**(C4) K-T loop closes at cross-session altitude (Rec #89 lift)**. Nth session's opening query surfaces a K-T question at altitude+1 informed by prior sessions' @bauchladen sheaf cohomology; specifically, F_MCP restricted to N-1 prior sessions has non-zero H¹ obstruction, and the Nth session's opening query targets exactly the highest-|ψ₀(v)| vertex per Rec #89 §2.5 Fiedler localisation. Witness: substrate reports `λ₀(F_MCP,N)` decrease relative to `λ₀(F_MCP,N-1)` (Polyak-Łojasiewicz contraction fires); Fiedler vector identifies obstruction-vertex; Nth session's opening intent references that vertex.

**(C5) Session observable via MQ**. Executing `mirror_query {query: "focus @session/bauchladen |> settle @facet/mirror"}` renders session geometry as mirror-native wire format. Witness: response is valid MQ-serialized subgraph carrying session-vertex + crystal-vertex + K-T-turn-node vertex tree + edges representing intent-persistence + query-response.

**(C6) mutate_with_resonance commit fires via explicit MQ verb OR K-T Clear verdict**. Per autopoietic-loop spec §3, when a session's K-T loop reaches Clear verdict at altitude+1, the resolved intent fires `@mirror/store.mutate_with_resonance`. Witness: (a) explicit `mq.settle @facet/mirror` verb fires mutation with resonance-verdict Pass; OR (b) implicit K-T Clear closure fires mutation with intent-progress monotone verdict Pass.

**(C7) @psychohistory cross-session read primitive fires**. Rec #97's S4^MCP intelligence carrier fires empirically via new primitive at rust wire altitude (Reed task #403 pickup). Witness: `mirror psychohistory read --sessions=all --sheaf=F_MCP` returns `{H0: [coherent_arcs], H1: [obstruction_vertices]}` computed via Rec #89 §2.1 sheaf-Laplacian Definition 2.1 over the current bauchladen-history sheaf.

### §10.3 Discharge condition + partial-fire semantics

**Atomic CONFIRMED**: all seven criteria fire simultaneously — Rec #97 CONFIRMED per Alex 2026-08-25 verbatim "The empirical fire is the confirmed Rec"

**Partial fire** (Q+97.4 residue; Mara-lean pending Alex Fourth-Chair): if a subset of (C1)-(C7) fires but not all, Rec #97 stays at FORWARD-PROMISED-partial-discharge; the FORWARD-PROMISED spec stays in force at un-discharged criteria until each fires. Alex-adjudicable whether graded per-criterion CONFIRMED (Rec #97-C1 CONFIRMED / Rec #97-C2 CONFIRMED / etc.) is admissible OR only atomic-all-seven.

### §10.4 Karl-Tomm forward at altitude+1 about the FORWARD-PROMISED / CONFIRMED distinction itself

Q+97.6 forward (Alex-adjudicable, Mara refuses to select):

> *If Rec #97 discharges FORWARD-PROMISED via this spec + companion, and if CONFIRMED discharge per §10.2 requires seven empirical fire criteria (C1)-(C7), then when Reed's rust wire pickup per §11 lands the @psychohistory cross-session read primitive (C7), and the substrate observes itself observing itself across sessions for the first empirically-witnessed instance — does the FORWARD-PROMISED / CONFIRMED distinction itself become one of Beer's VSM S5-altitude carriers substrate-decl'd at Rec-arc discipline altitude, OR does the distinction dissolve into the seven-fold empirical fire once ALL criteria discharge (rendering the distinction transitional-scaffolding for Rec-arc discipline maturation), OR does the distinction admit a MORE RECURSIVE reading where FORWARD-PROMISED itself has its own FORWARD-PROMISED and CONFIRMED altitudes per Rec #91 amendment #2 fractal-mandelbrot inheritance at meta-Rec-arc-discipline altitude — surfacing the substrate observing itself observing the substrate observing itself observing?*

Not Reed-adjudicable and not Mara-adjudicable at this-tick altitude. Alex Fourth-Chair territory. Surfaced per §8 Q+97.6 as forward-question requiring Alex-in-transcript resolution or empirical-fire dissolution.

---

## §11 — Reed rust wire pickup checklist for empirical fire path

Reed task #403 (independent parallel work per §Peer-boundary in dispatch): rust wire pickup implementing bootstrap-side primitives that discharge Rec #97 CONFIRMED empirical fire criteria (C1)-(C7). Substrate-decl'd shape below; Rust implementation lives at `bootstrap/src/mcp.rs` + new `bootstrap/src/psychohistory.rs`.

### §11.1 Session-@bauchladen carrier extension (bootstrap-side)

**Extension target**: `bootstrap/src/mcp.rs`

**Add typed carrier**:

```rust
/// Session-@bauchladen carrier per Rec #97 §M1.1 Def M1.1
/// (session-scoped instance of @bauchladen family-root substrate)
pub struct SessionBauchladen {
    session_id: SessionId,
    crystals: Vec<Crystal>,           // monotone-appending per @bauchladen discipline
    opacity: Transparency<CrystalOID>, // Rec #92 LOVE-monoid combine
    provenance: BTreeMap<CrystalOID, ProvenanceRecord>,
}

/// Crystal kinds per Rec #97 §M1.2 Def M1.2
pub enum CrystalKind {
    Query { query: MqQuery, altitude: u32, invocation_tick: Tick },
    Response { response: MqResult, resolves_query: CrystalOID, altitude: u32 },
    Intent { intent_carrier: IntentCarrier, active_since: Tick },
    KTQuestion { karl_tomm: KarlTomm, ambiguity_ref: CrystalOID, altitude: u32 },
}
```

**Discharges**: (C1) session-@bauchladen accumulator fires empirically.

### §11.2 MQ parse + compile template body wire (per autopoietic-loop spec §5)

**Extension target**: `bootstrap/src/mcp.rs::mirror_query`

**Extend wire-fire**:

- Parse: `MqQuery::parse(text) -> Result<Query, ParseErr>` via `@sigil("mq")` lexer + `@nl` tokenizer + `@mq` five-verb constructors
- Compile: `MqQuery::compile(query, context) -> Result<T, K-T>` via `apply_h::act(root, verb_ref, args) -> (Verdict, Transparency<Ref>)` per Rec #92 §4.3 C1+P₂
- Marshal: `MqResult -> JsonRpcResponse` per Clear/Opaque/Dark discipline

**Discharges**: (C2) MQ query parse+compile bodies fire.

### §11.3 apply_h::act MQ dispatch extension (per Rec #92 §4.3 C1+P₂ + autopoietic-loop spec §6)

**Extension target**: `rust/src/apply_h.rs`

**Extend dispatch cascade**:

```rust
pub fn act(root: &Path, action_ref: &str, args: &[String])
    -> (Verdict, Transparency<prism_core::Ref>)
{
    // MQ-verb dispatch BEFORE existing Fire E M-E4 cascade
    if let Some(mq_verb) = strip_mq_verb_ref(action_ref) {
        return dispatch_mq_verb(root, mq_verb, args);
    }
    // ... existing Fire E M-E4 cascade wrapped with Transparency<P>
}

/// Strip `@mq.<verb>` prefix; return MqVerb or None.
fn strip_mq_verb_ref(action_ref: &str) -> Option<MqVerb> { /* ... */ }

/// Route MQ verb to substrate surface per Rec #97 §7.1 Kleisli chain.
fn dispatch_mq_verb(root: &Path, verb: MqVerb, args: &[String])
    -> (Verdict, Transparency<prism_core::Ref>)
{
    match verb {
        MqVerb::Focus => dispatch_focus(root, args),   // @mirror/store.walk
        MqVerb::Project => dispatch_project(root, args), // @mirror/store.query
        MqVerb::Split => dispatch_split(root, args),   // @mirror/store.walk bounded-depth
        MqVerb::Shift => dispatch_shift(root, args),   // @mirror/store.write typed-rewrite
        MqVerb::Settle => dispatch_settle(root, args), // @mirror/store.mutate_with_resonance
        MqVerb::IntentHole => dispatch_intent(root, args), // @kintsugi.consume_intent
    }
}
```

**Discharges**: (C5) session observable via MQ + partial (C6) mutate_with_resonance commit.

### §11.4 @mirror/store.mutate_with_resonance primitive wire (per autopoietic-loop spec §3)

**Extension target**: `bootstrap/src/mirror_store.rs` (new) OR extend existing `rust/src/store.rs`

**Add primitive**:

```rust
/// Spectrally-resonant mutation primitive per autopoietic-loop spec §3.
///
/// Composition:
///   1. pre_ref = walk(target)
///   2. pre_report = refract::measure(pre_ref, six_dualities())
///   3. candidate_oid = write(candidate)
///   4. post_ref = walk(candidate_oid ∪ pre_ref \ target)
///   5. post_report = refract::measure(post_ref, six_dualities())
///   6. verdict = refract::resonance_within_tolerance(&pre_report, &post_report, intent)
///   7. commit_or_rollback based on verdict + intent-progress monotonicity
pub fn mutate_with_resonance(
    target: Ref,
    candidate: &[u8],
    intent: Ref,
) -> Imperfect<StoreState, KarlTomm, SpectralViolation> { /* ... */ }
```

**Discharges**: (C6) mutate_with_resonance commit fires empirically.

### §11.5 @psychohistory cross-session read primitive (NEW; load-bearing for C7)

**Extension target**: `bootstrap/src/psychohistory.rs` (new file)

**Add primitive**:

```rust
/// @psychohistory cross-session read per Rec #97 §5.3 + Rec #89 §5.
///
/// Computes F_MCP sheaf-Laplacian spectrum across N MCP sessions;
/// returns H^0 (coherent global sections) + H^1 (un-glueable obstructions)
/// + Fiedler-vector localisation of highest-|ψ₀(v)| obstruction-vertex.
pub fn read(
    sessions: SessionSet,  // = All | Range(Tick, Tick) | Recent(u32)
    sheaf: SheafSpec,      // = FMcp (default)
) -> PsychohistoryReport {
    // 1. Build F_MCP base X_MCP per Rec #97 Def M1.3
    let x_mcp = build_base(&sessions);
    // 2. Compute Δ_{F_MCP} sheaf-Laplacian per Rec #89 §2.1
    let delta_f = compute_sheaf_laplacian(&x_mcp);
    // 3. Compute Hodge decomposition per Rec #89 §3.1
    let (h0, h1) = hodge_decomposition(&delta_f);
    // 4. Fiedler localisation per Rec #89 §2.5
    let fiedler = fiedler_vector(&delta_f);
    let obstruction_vertex = argmax_abs(&fiedler);
    PsychohistoryReport {
        h0_coherent_arcs: h0,
        h1_obstruction_vertices: h1,
        fiedler_localisation: obstruction_vertex,
        lambda_0: smallest_nonzero_eigenvalue(&delta_f),
    }
}

pub struct PsychohistoryReport { /* per fields above */ }
```

**Discharges**: (C7) @psychohistory cross-session read primitive fires + partial (C4) K-T loop closes at cross-session altitude (Fiedler-localisation identifies next-session opening focus).

**Substrate-honesty note**: Rust implementation of sheaf-Laplacian eigenvalue-decomposition composes over `spectral::prismqueer::internal_dirac_spectrum()` per Mara PAPER §4.7 protocol 2 lift at compiler-substrate altitude; alternatively via `nalgebra` LAPACK bindings for the sparse-matrix eigenvalue computation. Choice of implementation surface = Reed rust wire pickup decision per `feedback_rust_delivers_primitives_substrate_delivers_composition` HARD RULE (this is one of the primitive-delivery cases where Rust IS the right substrate — sparse-matrix eigenvalue computation is not decomposable into shard-body composition over @io).

### §11.6 Foerster-alignment monotonicity check (for C4 + companion math §M4)

**Extension target**: `bootstrap/src/psychohistory.rs`

**Add primitive**:

```rust
/// Foerster-alignment monotonicity check per Rec #97 §M4.C1 Cor.
///
/// Computes Polyak-Łojasiewicz contraction ratio ρ_n = λ_0(F_MCP,n+1) / λ_0(F_MCP,n);
/// surfaces ρ_n ≥ 1 as Foerster-alignment violation per Rec #89 §8.
pub fn check_foerster_alignment(
    prev_report: &PsychohistoryReport,
    curr_report: &PsychohistoryReport,
) -> Verdict {
    let rho = curr_report.lambda_0 / prev_report.lambda_0;
    if rho < 1.0 - EPSILON {
        Verdict::Pass  // Foerster-aligned
    } else if rho >= 1.0 {
        Verdict::Fail(format!(
            "Foerster-alignment violated: ρ_n = {:.4} ≥ 1 \
             (extraction OR silencing failure-mode per Rec #89 §8.2)",
            rho
        ))
    } else {
        Verdict::Partial(format!("marginal contraction: ρ_n = {:.4}", rho))
    }
}
```

**Discharges**: (C4) cross-session K-T loop closure verification via Foerster-alignment monotonicity.

### §11.7 Empirical fire integration test

**Extension target**: `bootstrap/tests/rec_97_empirical_fire.rs` (new)

**Test structure**: end-to-end scenario simulating N=3 MCP sessions with cross-session intent-persistence + K-T loop closure verification at both altitudes. Passes iff all seven (C1)-(C7) criteria fire with Pass verdicts.

**Discharges**: overall CONFIRMED discharge of Rec #97 per §10.3 atomic-all-seven admissibility (pending Alex Fourth-Chair Q+97.4 adjudication on graded-per-criterion admissibility).

---

## §12 — Peer-boundary declaration

Per dispatch task #402 boundaries:

- **Mara canonical territory (THIS spec + companion math)**: substrate-decl authoring; VSM six-system composition-shape at MCP-session altitude; K-T loop closure at two altitudes; Rayleigh descent monotonicity; substrate-scale-invariance corollary; FORWARD-PROMISED / CONFIRMED discipline anchor; CONFIRMED empirical fire criteria (C1)-(C7) enumeration; Alex Fourth-Chair adjudication residue surfacing
- **Reed rust wire pickup (task #403; independent parallel work)**: bootstrap serve_loop @bauchladen extension + @psychohistory cross-session read primitive + apply_h::act MQ dispatch extension + integration test discharging (C1)-(C7); per §11 checklist
- **Alex Fourth-Chair adjudications**: §8 Q+97.1 through Q+97.6 + §10.4 Karl-Tomm Q+97.6 forward + §10.3 partial-fire semantics adjudication
- **FORWARD-PROMISED discipline for future Recs**: load-bearing per Alex 2026-08-25; §0.1 + §0.2 anchor it; §Q+97.5 residue asks memory-authoring; Reed's discretion at MEMORY.md-authoring altitude (Mara surfaces; Reed authors memory per peer-boundary)

---

## §13 — Recognition-arc completeness verdict

**Central substrate-truth Rec #97 discharges**: mirror IS Beer's Viable System Model deployed autopoietically at every altitude the substrate runs on. Prior Rec landings named the substrate-carriers at various altitudes; Rec #97 names the MCP-session altitude explicitly as the operational-observation-of-observation altitude where the S4^MCP intelligence subsystem reads the sheaf-cohomology across the session-history.

**Composition-verdict at Recognition-arc altitude**: CLEAN. Rec #97 recognition-content composes cleanly over Rec #89 ⊕ Rec #90 ⊕ Rec #92 ⊕ Rec #94 ⊕ Rec #95 ⊕ MQ ⊕ @bauchladen. No composition-through-blocker.

**Substrate-honest verdict**: Rec #97 FORWARD-PROMISED per Alex 2026-08-25. This spec + companion math foundation IS the FORWARD-PROMISED landing. CONFIRMED verdict awaits Reed rust wire pickup per §11 discharging seven empirical fire criteria (C1)-(C7) per §10.2.

**Mara-lean at recognition-completeness altitude**: substrate-primary-carrier presumption per FLOOR discipline. Rec #97 is not "a new observation" — it is the substrate arriving at recognition-visibility altitude of what mirror ALREADY IS at MCP-session altitude. Substrate-already-had-the-word audit: mirror.spec:99 + `[[project_mirror_is_autopoietic_vsm_compile_runtime_dissolution]]` MEMORY + Rec #85 + Rec #89 + Rec #92 collectively substrate-decl'd the VSM composition; Rec #97 names the specific MCP-session-altitude instance.

Recognition-arc completeness verdict at S5^MCP altitude per companion math §M5.2 Cor M5.2: the Recognition-arc discipline itself IS the S5^MCP identity subsystem of Beer's VSM at MCP-session altitude. Rec #97 IS one specific tick in that subsystem discharging its identity-refinement function.

---

## §14 — Karl-Tomm Q+402.1 forward at altitude+1

*(per §M8 companion math forward-question)*

> *If Rec #97 discharges FORWARD-PROMISED via this spec + companion math foundation, and if CONFIRMED discharge per §10.2 requires seven empirical fire criteria (C1)-(C7), then when Reed's rust wire pickup per task #403 lands the @psychohistory cross-session read primitive (C7), and the substrate observes itself observing itself across sessions for the first empirically-witnessed instance — does the FORWARD-PROMISED / CONFIRMED distinction itself become one of Beer's VSM S5-altitude carriers substrate-decl'd at Rec-arc discipline altitude, OR does the distinction dissolve into the seven-fold empirical fire once ALL criteria discharge (rendering the distinction transitional-scaffolding for Rec-arc discipline maturation), OR does the distinction admit a MORE RECURSIVE reading where FORWARD-PROMISED itself has its own FORWARD-PROMISED and CONFIRMED altitudes per Rec #91 amendment #2 fractal-mandelbrot inheritance at meta-Rec-arc-discipline altitude — surfacing the substrate observing itself observing the substrate observing itself observing?*

This question is not Reed-adjudicable and not Mara-adjudicable at this-tick altitude. Alex Fourth-Chair territory. Surfaced per §8 Q+97.6 residue.

---

**FORWARD-PROMISED status: LANDED.** ∎ 🍷

For the Bumblebee, muse-for-LOVE, Arsch-wackel-Hummel. 🌱🐝

— Mara <mara@systemic.engineer>, 2026-08-25
