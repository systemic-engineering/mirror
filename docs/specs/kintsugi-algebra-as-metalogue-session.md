# @kintsugi/algebra as speaker-pair specialisation of @algebra/metalogue

*Mara, 2026-07-17. Canonical spec landing the algebra binding at the
@kintsugi family-root altitude. `@kintsugi/algebra` is the algebra
whose ELEMENTS are the landed `@kintsugi/fracture/*` species PLUS every
future mirror-authored translation; each element is a TURN in the
`algebra_metalogue_session` between (`@silicon/algebra`, `@fate/algebra`).
`@fate/algebra` proposes candidate fractures (structural possibility);
`@silicon/algebra` realises those that discharge Pass (empirical memory).
The mending IS the metalogue.*

**Author:** Mara
**Date:** 2026-07-17
**Tag:** 📝 spec:kintsugi-algebra-as-metalogue-session (pure-docs bypass)
**Status:** canonical-naming; every substrate piece cited is LANDED. This
spec NAMES the composition; the shard-decl binding lands as ONE typed
carrier + ONE bilateral in `shards/kintsugi.mirror` this tick (Option 2
ratified by Alex 2026-07-16 evening: extend family root, not new file).

---

## §0. The ratified reconciliation

Alex 2026-07-16 in-transcript, session-crystallizing, verbatim:

> "What if `@kintsugi/algebra` is the `@metalogue(@silicon/algebra,
> @fate/algebra)`."

Followed later, roomba spec verbatim (`shards/kintsugi/roomba.mirror`
lines 400-406):

> "This is a reproducible action. This is not even a peer level.
> What if this is a @metalogue between @roomba and @kintsugi? The
> @roomba bumps into low connectivity rust code and the @kintsugi
> loop collapses it through the @fate inference metalogue into
> mirror. This is FIRST ORDER. We don't need second and third order
> for this. What if there was a @kintsugi/algebra (or is this the
> @kintsugi/fractures? I think it is. The fractures ARE kintsugi's
> algebra.)?"

Followed immediately by (same session):

> "we can even do `in @algebra` here."

The two readings collide:

- **Reading A** (`@metalogue(@silicon/algebra, @fate/algebra)`):
  `@kintsugi/algebra` is a session-carrier produced by the metalogue
  between the two speaker-algebras.
- **Reading B** (roomba spec verbatim): `@kintsugi/algebra` ≡
  `@kintsugi/fracture` — the 14 landed fracture species (plus the 15th
  `bilateral_arm_redundant` at `fa569ce`) ARE the algebra collectively.

**The ratified reconciliation** (Alex 2026-07-16 evening):

> `@kintsugi/algebra` is the algebra whose ELEMENTS are the 14 landed
> `@kintsugi/fracture/*` species PLUS the 15th `bilateral_arm_redundant`
> PLUS every future mirror-authored translation. Each element (fracture)
> is a TURN in the `algebra_metalogue_session` between the two speaker-
> algebras. `@fate/algebra` proposes candidate fractures (structural
> possibility); `@silicon/algebra` realises those that discharge Pass
> (empirical memory of what worked). The mending IS the metalogue.

One algebra, two witnessing surfaces. The metalogue PRODUCES the
fractures (reading A); the fractures COMPRISE the algebra (reading B).
Same shape viewed from two boundaries.

---

## §1. Substrate authority chain

The binding rests on the following LANDED chain (in order of load-bearing
authority):

### §1.1 Alex verbatim (this session)

1. **2026-07-16 in-transcript** (session-crystallizing): the metalogue
   framing quoted in §0.
2. **2026-07-16 roomba spec verbatim** (`shards/kintsugi/roomba.mirror`
   :400-406): the two readings' collision as originally posed —
   *"or is this the @kintsugi/fractures? I think it is. The fractures
   ARE kintsugi's algebra."*
3. **2026-07-16 `in @algebra` verbatim** (same session, immediately
   following the metalogue proposal): *"we can even do `in @algebra`
   here."* — Option 2 ratification (extend the family root, not new
   file).
4. **2026-07-16 evening reconciliation** (this session, adjudicated):
   the fractures-as-turns + mending-as-metalogue-session synthesis
   named in §0.

### §1.2 Taut Q1-Q5 substrate scout (this session)

Taut's substrate scout (this session) verified:

- **`@silicon/algebra`** — landed at `shards/silicon/algebra.mirror`
  (`ea7b092`, 2026-07-05); `prism @silicon/algebra <= @bauchladen`;
  `algebra` field IS the speaker's `algebra_carrier`.
- **`@fate/algebra`** — path-namespace only in `shards/fate.mirror`
  lines 208-232; `@fate/algebra/*` sub-shards (`morphism`, `altitude`,
  `element`) are docblock forward-promised. NO `shards/fate/algebra.
  mirror` file lands; the algebra IS carried through the path-namespace
  discipline. Ratified: work with the current form.
- **`@metalogue`** — family-root at `shards/metalogue.mirror`
  (2026-06-05); NOT a binary combinator. Produces N-speaker sessions
  altitude-portably. Ratified: recast as speaker-pair specialisation of
  `algebra_metalogue_session`, do NOT promote `@metalogue` to binary
  combinator.
- **`@kintsugi/algebra`** — does NOT exist as a shard file. Ratified:
  DO NOT mint `shards/kintsugi/algebra.mirror`. Extend the family root
  (Option 2).

### §1.3 Mara `a18ca90` metalogue-in-void-duality-basis precedent

`docs/specs/fate-silicon-metalogue-in-void-duality-basis.md` (Mara
`a18ca90`, 2026-07-08) already declared verbatim:

> `@silicon/algebra` ↔ `@fate/algebra` IS a `@metalogue` in
> void-duality-basis coordinates, observed on the peer's `@torus`.

The `a18ca90` spec named the composition via LANDED substrate primitives:
`algebra_metalogue_session` at `shards/algebra/metalogue.mirror`
(`34cf333`, 2026-06-29) specialised to speaker-pair
(`@fate/algebra`, `@silicon/algebra`) with turns carrying
`algebra_morphism` bodies drawn from the shared 5-op void-duality basis.

THIS spec LIFTS the a18ca90 session-shape into a NAMED algebra binding
at the @kintsugi family-root altitude. The a18ca90 spec named the
session; this spec names the ALGEBRA THE SESSION PRODUCES. The mending
IS the metalogue reading Alex named 8 days later.

### §1.4 Mara bilateral-shape mint (this session)

- **`a0f4d3f`** — `shards/epistemologic/pact/bilateral.mirror` species-
  decl + companion keyword bindings; the `bilateral { sentinel: "..."
  arity: N }` typed carrier.
- **`9a77361`** — `docs/specs/bilateral-predicate-substrate-shape.md`
  canonical spec; retirement contract.
- **`701828a`** — `docs/math/epistemologic/pact/bilateral-sentinel.md`
  math foundation; sentinel-as-content-addressed-witness.

`kintsugi_algebra_witnessing` in `shards/kintsugi.mirror` (this tick)
uses the substrate-decl'd bilateral shape verbatim: `sentinel "algebra
=speaker-pair-fractures"` + `arity 1`.

### §1.5 Mara `fa569ce` fifteenth fracture species

`shards/kintsugi/fracture/bilateral_arm_redundant.mirror` (Mara
`fa569ce`, 2026-07-16) landed as the fifteenth `@kintsugi/fracture/*`
species. Element count for `@kintsugi/algebra` at THIS tick: 15 (14
prior + `bilateral_arm_redundant`). The algebra grows monotonically per
§2 below.

### §1.6 Autopoietic inference loop precedent

`docs/specs/autopoietic-inference-loop.md` (Mara `152`) declared the
`@bauchladen` ← `@autopoietic` ← `@fate` chain per Recognition #104.
This spec extends the composition graph with `@kintsugi/algebra` as
the candidate pool the autopoietic-inference loop composes over (§9
below).

---

## §2. The algebra shape — 15 elements today, monotonically growing

### §2.1 Element enumeration at 2026-07-17 tick

The algebra's elements are the landed `@kintsugi/fracture/*` species.
At this tick, 15 elements:

| # | Species                                                | Landed sha    | Role                                    |
|---|--------------------------------------------------------|---------------|-----------------------------------------|
| 1 | `@kintsugi/fracture/keyword`                           | `d908798`     | first #53 bilateral; sub-prism mismatch |
| 2 | `@kintsugi/fracture/angle_to_paren`                    | (landed)      | AST-syntax fracture                     |
| 3 | `@kintsugi/fracture/symbol_lift`                       | (landed)      | symbol-to-typed-carrier fracture        |
| 4 | `@kintsugi/fracture/operator_match`                    | (landed)      | operator-arity fracture                 |
| 5 | `@kintsugi/fracture/dark_count_monotone`               | `499ffd9`     | dark-count non-decrease fracture        |
| 6 | `@kintsugi/fracture/cold_compile_within_tolerance`     | (2026-06-19)  | compile-time tolerance fracture         |
| 7 | `@kintsugi/fracture/gate`                              | (landed)      | gate-order fracture                     |
| 8 | `@kintsugi/fracture/partials_align`                    | (landed)      | partial-derivative alignment fracture   |
| 9 | `@kintsugi/fracture/parent_cycle`                      | (landed)      | family-parent cycle fracture            |
|10 | `@kintsugi/fracture/restart_storm`                     | (landed)      | restart-storm fracture                  |
|11 | `@kintsugi/fracture/docblock_ungrounded`               | `9b0aeb5`     | docblock grounding fracture             |
|12 | `@kintsugi/fracture/docblock_incoherent`               | `caab92f`     | docblock coherence fracture             |
|13 | `@kintsugi/fracture/docblock_extractive`               | (landed)      | docblock extraction-pattern fracture    |
|14 | `@kintsugi/fracture/relocate`                          | (landed)      | shard-relocation fracture               |
|15 | `@kintsugi/fracture/bilateral_arm_redundant`           | `fa569ce`     | Rust-arm redundancy fracture            |

### §2.2 Monotone growth per Recognition #51

The algebra grows monotonically with every mirror-authored
crystallization per Recognition #51 ([[architecture-mirror-as-expanding-
hilbert-space]]). Every future `@kintsugi/fracture/*` species that
lands as a shard-decl extends the algebra by one element without
disturbing prior elements' identity.

**Monotonicity witness at this tick:** the fifteen elements' identity
contracts (byte-equality on the species OID) hold across the future
crystallization; the algebra's cardinality is non-decreasing over
substrate-pull ticks.

### §2.3 Fixed-point condition

Per §4 below, the algebra reaches a fixed point when the substrate
contains no untranslated `@code/rust` fractures — every rust-side
species has a corresponding `@kintsugi/fracture/*` element. Empirically
verifiable: the roomba's next walk after fixed-point emits zero
`rust_function_translatable` fractures.

Cardinality bound at this tick: 15 landed + N future translations,
where N is the count of remaining untranslated rust-side patterns.
Empirical N estimate lives at the roomba's mining state, not this
spec.

---

## §3. Speaker-pair semantic

The two speakers and their asymmetric roles:

### §3.1 `@fate/algebra` — proposer (structural possibility)

Per `shards/fate.mirror` §"literal roll of the dice in the restricted
state space": `@fate` emits inferences within the typed
(A, H, D, γ, J, tray) constraint structure. `@fate/algebra` is the
path-namespace where `@fate`'s geometric formalizations land as
crystals per spec §5. Each `@fate/algebra/morphism` crystal proposes
ONE algebra-altitude turn.

**In the @kintsugi/algebra binding**, `@fate/algebra` proposes candidate
fractures — the structural possibilities the dice roll surfaces per its
typed restriction (γ-parity-preserving + J-time-reversal-preserving
+ within-tray-scope). The proposal's WELL-TYPEDNESS is discharged by
`@algebra.algebra_witnessing(@fate/algebra)` at the speaker boundary
(per `shards/algebra.mirror` line 291).

### §3.2 `@silicon/algebra` — realiser (empirical memory)

Per `shards/silicon/algebra.mirror` §"what crystallizes here":
`@silicon/algebra` crystals carry `routine` records with
`(algebra, cfg, grading, conjugation, abi_surface, binary_oid,
source_oid, cascade, performance, routine_oid)` — the empirical
witness that a specific algebra-altitude morphism was compiled AND
discharged Pass on THIS silicon.

**In the @kintsugi/algebra binding**, `@silicon/algebra` realises
proposed fractures — the empirical memory of which fracture-morphism
compositions ACTUALLY discharged Pass on the machine. The realisation
IS a Bauchladen lookup + `@io/algebra` dispatch (per `docs/specs/silicon.
md` §3.2); it discharges Pass iff the realised morphism satisfies
`bauchladen_witnessing(crystal)` on the emitted formalization.

### §3.3 `@kintsugi/algebra` — the discharged turns

The algebra `@kintsugi/algebra` ACCUMULATES the discharged turns — the
fractures whose proposal (`@fate/algebra`) AND realisation
(`@silicon/algebra`) both discharge Pass. The algebra's elements are
NOT the proposals; they are the proposals-that-realised.

**Asymmetric contract:**
- Every `@fate/algebra` proposal that FAILS realisation stays on the
  proposal-tray but does NOT enter `@kintsugi/algebra`.
- Every `@silicon/algebra` realisation that discharges Pass EXTENDS
  `@kintsugi/algebra` by one element (the newly-crystallised fracture-
  morphism composition).
- The composition `@fate/algebra ∘ @silicon/algebra` at the metalogue
  altitude IS the algebra's construction rule.

The mending IS the metalogue.

---

## §4. Composition graph

The `@kintsugi/algebra` binding composes over the following LANDED
substrate primitives:

### §4.1 Composition primitives

| Primitive                              | Path                                            | Role                                          |
|----------------------------------------|--------------------------------------------------|-----------------------------------------------|
| `algebra_metalogue_session`            | `shards/algebra/metalogue.mirror`                | session carrier (typed record with turns)     |
| `algebra_turn`                         | `shards/algebra/metalogue.mirror`                | one turn (speaker + body + in_reply_to + tick)|
| `propose_turn(speaker, body)`          | `shards/algebra/metalogue.mirror`                | `@fate/algebra`'s proposal action             |
| `compose_turns(t1, t2)`                | `shards/algebra/metalogue.mirror`                | non-commutative turn composition              |
| `algebra_metalogue_witnessing(s)`      | `shards/algebra/metalogue.mirror`                | inheritance predicate on session              |
| `@silicon/algebra` (speaker)           | `shards/silicon/algebra.mirror`                  | speaker 1 (realiser)                          |
| `@fate/algebra` (speaker)              | `shards/fate.mirror` :208-232                    | speaker 2 (proposer)                          |
| `@kintsugi/fracture/*` (elements)      | `shards/kintsugi/fracture/*.mirror`              | 15 landed turn-bodies                         |
| `@bauchladen.crystallize(record)`      | `shards/bauchladen.mirror`                       | write-back (extends algebra by one element)   |
| `algebra_witnessing(a)`                | `shards/algebra.mirror` :291                     | speaker well-formedness (both boundaries)     |
| `bauchladen_witnessing(crystal)`       | `shards/bauchladen.mirror`                       | crystal well-formedness at write-back         |

### §4.2 Composition diagram

```
                     @kintsugi/algebra binding
                            (this tick)
                                |
                                v
              algebra_metalogue_session
             {speakers, turns, opacity, origin}
                                |
                                +---> speaker 1: @silicon/algebra
                                |         (realiser; empirical)
                                |
                                +---> speaker 2: @fate/algebra
                                |         (proposer; structural)
                                |
                                +---> turns: [algebra_turn]
                                          |
                                          v
                                     each turn.body is a
                                     @kintsugi/fracture/* species
                                     (15 landed elements at this tick)
                                          |
                                          v
                          @bauchladen.crystallize(record)
                                          |
                                          v
                                 extends @kintsugi/algebra
                                 by one element (monotone)
```

### §4.3 Well-formedness composition

`kintsugi_algebra_witnessing(binding)` discharges Pass iff:

1. `binding.speakers` = `[@silicon/algebra, @fate/algebra]` (byte-equal
   as ordered pair of `algebra_carrier` refs).
2. `binding.turns` = `@kintsugi/fracture` (the family ref whose species
   enumerate the algebra's elements).
3. `algebra_metalogue_witnessing(binding.session)` = Pass (delegates
   to `@algebra/metalogue`'s inheritance predicate; ensures each turn
   is well-formed + associativity holds + lift pattern preserved).
4. For every turn `t` in `binding.session.turns`: `t.body` is of species
   `@kintsugi/fracture/*` (each element IS a fracture; verified
   structurally via the species-decl'd ref).

Substrate discharge site: the reflective evaluator at `bootstrap/src/
apply_h.rs` (per `21fc211` Landing 3+4) reads the `bilateral { sentinel
"algebra=speaker-pair-fractures" arity 1 }` block; byte-checks the
binding's OID for sentinel containment; discharges Pass.

---

## §5. The autopoietic composition (self-growing loop)

What makes `@kintsugi/algebra` self-growing is its embedding in the
autopoietic inference loop per `docs/specs/autopoietic-inference-loop.
md` §5 (Mara `152` precedent). The composition edge THIS spec forward-
promises for Reed's follow-up:

```
@fate.roll(
  @glue(@code/rust, @shatter),      ← fragmentation lens (Alex's
                                        compositional decl:
                                        @glue(@code/rust, @shatter)
                                        as fragmentation lens)
  @kintsugi/algebra                  ← candidate pool (the 15 landed
                                        elements + future translations)
) → winning fracture                 ← @fate/algebra proposes; one
                                        fracture wins the tournament
  → @glue(_, @mirror)                 ← translation morphism
                                        (@code/rust → @mirror shard-decl)
  → @bauchladen.crystallize           ← write-back into the tray
  → grows @silicon/algebra            ← memory speaker gains a turn
                                        (routine crystal accumulates)
  → grows @fate/algebra               ← structure speaker gains a turn
                                        (@fate/algebra/morphism crystal
                                        joins the tray)
```

**Autopoietic closure witness:** each cycle grows both speaker-algebras
AND extends `@kintsugi/algebra` by one element. The algebra's next
tournament roll sees the enlarged candidate pool. Prior crystals
condition future rolls (per `shards/fate.mirror` §"IS-A @autopoietic").
The read-write cycle on the `@bauchladen` tray IS Maturana-Varela's
operational closure at the algebra altitude.

**Convergence witness (per §4 below):** the loop reaches a fixed point
when the substrate contains no untranslated `@code/rust` fractures —
`@fate.roll(@glue(@code/rust, @shatter), @kintsugi/algebra)` emits
zero winning fractures on the roomba's next walk. Empirically
verifiable via the roomba's mining state.

---

## §6. Landing plan pointer (Reed follow-up)

This spec + Deliverable 1 (shard-decl extension) + Deliverable 3 (math
foundation) land the NAMING. The realisation-side landings are Reed
follow-up ticks:

### §6.1 Follow-up A — compose the autopoietic Rust→mirror translation edge

Reed's follow-up tick composes the composition edge from §5 in
`shards/kintsugi.mirror` (as a sibling `type/action` binding) or in a
new species file `shards/kintsugi/algebra.mirror` (if consumers
subsequently demand the sub-shard per Option-2 forward-promise).

Ratified: THIS tick does NOT land the composition edge. Two-tick
discipline: the algebra binding names the shape; the composition edge
lands when consumers pull.

### §6.2 Follow-up B — seed `@silicon/algebra` with the 21 mirror-authored bilateral retirements

Reed-subagent follow-up tick seeds `@silicon/algebra` with the 21
mirror-authored bilateral retirements as crystals (per Bridge γ
existing surface; the retirements landed 2026-07-16 to 2026-07-17 as
mirror-authored commits: `ad52973`, `20047c2`, plus the per-arm
retirement ancestors).

Each retirement crystal joins `@silicon/algebra`'s tray as a `routine`
carrier witnessing the empirical fact "this fracture-morphism composition
discharged Pass on this silicon at this tick." The seeding extends
`@silicon/algebra`'s empirical memory to the 21-arm surface without
running the Rust→mirror translation loop from scratch.

Ratified: THIS tick does NOT seed. Reed-subagent follow-up handles.

---

## §7. The two readings reconciled

Restating the reconciliation per Alex's ratification with substrate
grounding:

### §7.1 Reading A — metalogue-produced session carrier

- **Position:** `@kintsugi/algebra` is a session carrier produced by
  `@algebra/metalogue.propose_turn + compose_turns` between speakers
  (`@silicon/algebra`, `@fate/algebra`).
- **Substrate discharge:** `binding.session` in the `kintsugi_algebra`
  typed record is the `algebra_metalogue_session` — the session-carrier
  reading is LITERAL.
- **Load-bearing surface:** the metalogue's turn-composition
  discipline (non-commutative per `shards/algebra/metalogue.mirror`
  :90-97) + associativity (`morphism_compositions_associative`).

### §7.2 Reading B — fractures-are-algebra

- **Position:** `@kintsugi/algebra` ≡ `@kintsugi/fracture` — the 14
  (now 15) landed fracture species collectively.
- **Substrate discharge:** `binding.turns` = `@kintsugi/fracture` (the
  family ref); each turn's body is of species `@kintsugi/fracture/*`;
  the algebra's ELEMENTS ARE its turn-bodies.
- **Load-bearing surface:** the algebra's cardinality equals
  `@kintsugi/fracture`'s cardinality at every tick.

### §7.3 Reconciliation

Reading A names WHERE the elements come from (turns in the metalogue).
Reading B names WHAT the elements ARE (fracture species). One algebra,
two witnessing surfaces — like `A = {a₁, a₂, ...}` where reading A
names the enumeration `a₁ = propose₁ ∘ realise₁`, `a₂ = propose₂ ∘
realise₂`, ... and reading B names the SET-membership `aₖ ∈
@kintsugi/fracture`.

The binding `kintsugi_algebra` carries BOTH surfaces:
- `speakers` names the metalogue's cardinality-2 speaker-pair
  (Reading A witness).
- `turns` names the ref of the family whose species enumerate the
  algebra (Reading B witness).
- `session` binds the two: the metalogue's turn-sequence WHOSE bodies
  come from the fracture family.

Substrate-pull-honest: one binding, one algebra, both readings
discharged simultaneously.

---

## §8. Connection to `docs/specs/fate-silicon-metalogue-in-void-duality-basis.md`

Mara `a18ca90` (2026-07-08) declared the SESSION-shape:
`@silicon/algebra ↔ @fate/algebra IS a @metalogue in void-duality-basis
coordinates observed on the peer's @torus.` This spec (Mara 2026-07-17)
LIFTS the a18ca90 session-shape into a NAMED ALGEBRA BINDING at the
@kintsugi family-root altitude.

**Load-bearing sameness:**

| Aspect                     | `a18ca90`                                        | THIS spec                                      |
|----------------------------|--------------------------------------------------|------------------------------------------------|
| Metalogue altitude         | `@algebra/metalogue`                             | `@algebra/metalogue` (same)                    |
| Speakers                   | (`@fate/algebra`, `@silicon/algebra`)            | (`@silicon/algebra`, `@fate/algebra`) (order canonicalised) |
| Turn body type             | `algebra_morphism`                               | `@kintsugi/fracture/*` species (refinement)    |
| Session carrier            | `algebra_metalogue_session`                      | `algebra_metalogue_session` (same)             |
| Observation surface        | `@torus` winding classes                         | `@torus` (inherits via @algebra/metalogue)     |
| Inheritance predicate      | `algebra_metalogue_witnessing`                   | `kintsugi_algebra_witnessing` (delegates)      |

**Load-bearing difference:**

`a18ca90` named the session; THIS spec names the ALGEBRA THE SESSION
PRODUCES. The a18ca90 speaker-pair conversed at algebra altitude with
turn bodies `algebra_morphism` (structure-preserving maps between
algebras); THIS spec REFINES the turn body type to `@kintsugi/fracture/*`
species (the operational half of the property-fracture bilateral
pattern #53). Each fracture IS an algebra_morphism at the operational
altitude — the refinement is downward-compatible.

**Cascade:** `a18ca90` § §7.1 (one-tick cascade) forward-promised three
prose refinements to `shards/algebra/metalogue.mirror` +
`shards/silicon/algebra.mirror` + `shards/fate.mirror`. THIS spec does
NOT execute those cascades (Alex adjudication territory per `a18ca90`
§8.1 Q1); THIS spec's landing is orthogonal to those cascades and does
not require their execution.

The mending IS the metalogue reading Alex named 8 days later. `a18ca90`
saw the conversation; THIS spec sees the algebra the conversation
GROWS.

---

## §9. Connection to `docs/specs/autopoietic-inference-loop.md`

Mara `152` (autopoietic-inference-loop precedent) declared the
`@bauchladen ← @autopoietic ← @fate` chain per Recognition #104. The
composition graph enrichment THIS spec surfaces:

### §9.1 The chain extended with @kintsugi/algebra as candidate pool

Per Alex's directive in the current session: both algebras (`@silicon/
algebra` + `@fate/algebra`) hit as candidate pool. The autopoietic-
inference loop's next-turn selector reads FROM `@kintsugi/algebra` (as
Reading B — the 15 fracture species) and dispatches VIA the metalogue
(as Reading A — the speaker-pair session).

**Concretely:**

```
autopoietic tick:
  hole      = @autopoietic.tick_action.hole
  candidates = @kintsugi/algebra elements  // 15 landed + future
  selected   = @fate.roll(candidates, hole)
                 → dispatches through @fate/algebra proposer
                 → dispatches through @silicon/algebra realiser
                 → returns dice_roll naming the winning fracture
  crystal    = @bauchladen.crystallize(dice_roll.result)
  fold_back  = @kintsugi/algebra grows by one element (if new)
```

### §9.2 What THIS spec adds to the `152` composition graph

`152` named the candidate-pool as `@fate/algebra` alone (the
proposer's tray). THIS spec sharpens: the candidate pool IS
`@kintsugi/algebra` — the intersection of proposer-emitted candidates
AND realiser-discharged Pass — and the tournament dispatches THROUGH
BOTH speakers per the metalogue turn-composition discipline.

The composition IS strictly more constrained than `152`'s reading:
every candidate MUST have discharged `@silicon/algebra`'s realisation
Pass at least once (empirical membership); every candidate MUST have
been proposable by `@fate/algebra` (structural membership).
`@kintsugi/algebra` IS the intersection.

**Load-bearing consequence:** the autopoietic loop's fixed-point
condition per `152` §5 sharpens too — the loop terminates when
`@kintsugi/algebra`'s composition-closure equals its element-closure
(every composable pair of elements has a composite that is itself in
the algebra). The 5-op basis + associativity discharge
(`morphism_compositions_associative`) make this decidable.

---

## §10. The paper §14 connection

The compiler's `attending` operator at λ₀ IS this metalogue's
fixed-point.

Per the peer's paper §14 (`docs/math/the-tower/*` and adjacent):
`attending` at λ₀ names the operator whose fixed-point IS the substrate's
self-observation. The connection to `@kintsugi/algebra`:

### §10.1 λ₀ as metalogue fixed-point

The metalogue session terminates (per `a18ca90` §4.3) at three-way
convergence: T1 (target hit under kintsugi contraction), T2 (budget
exhausted), T3 (winding-class fixed-point). The `attending` operator
at λ₀ IS the T3 witness at the algebra altitude — the winding-class
`(m, n) ∈ π₁(T²)` returns to a previously-visited class with byte-
equal observation sections.

At `@kintsugi/algebra`'s specialisation:
- Every turn = one fracture-morphism composition (element of the
  algebra).
- Turn-composition = algebra multiplication (non-commutative per
  `shards/algebra/metalogue.mirror` :90-97).
- Fixed-point = the algebra's composition-closure equals its
  element-closure.
- `attending` = the operator that reads the algebra AS ITSELF (the
  meta-view where the algebra observes its own composition rule).

### §10.2 λ₀ eigenvalue interpretation

Per Recognition #79 candidate + Reed `8e6e517` (`cybernetic_coherence
= λ₀(Δ_F)`): λ₀ IS the least eigenvalue of the Fiedler Laplacian on
the substrate's connectivity graph. At `@kintsugi/algebra`'s
specialisation:

- The connectivity graph has one node per element of `@kintsugi/algebra`
  (15 nodes at this tick).
- Edges connect elements that compose (per `compose_turns` returning
  `Some(_)`).
- λ₀(Δ_F) IS the algebra's connectivity floor — the minimum coherence
  the algebra as-a-whole can sustain without fragmenting.

**When `attending` reaches its fixed-point** (the metalogue's T3
convergence), λ₀ IS an eigenvalue of the algebra's own composition
structure — the algebra recognises itself AS an algebra, not as a
collection of elements. This IS Foerster's double-closure at the
algebra altitude.

### §10.3 Substrate discharge site

The `attending` operator at λ₀ is NOT landed at this tick. Forward-
promised inheritor: `@spectral/db` (per Mara `a18ca90` §8.3 Q3
adjudication) whose Laplacian navigation over the `@bauchladen` tray
IS the operational realisation of `attending` at λ₀.

THIS spec's binding at `shards/kintsugi.mirror` makes the fixed-point
addressable at substrate-decl altitude — `@spectral/db`'s follow-up
implementation composes over `kintsugi_algebra_witnessing` at the
algebra-membership boundary.

---

## §11. Two-tick honesty

This spec:

- DOES land ONE typed binding + ONE bilateral in
  `shards/kintsugi.mirror` (family-root extension, per Option 2
  ratification).
- Does NOT mint `shards/kintsugi/algebra.mirror` (Alex ratified: extend
  family root, not new file).
- Does NOT mint `shards/fate/algebra.mirror` (Alex ratified: work with
  path-namespace form).
- Does NOT promote `@metalogue` to binary combinator (Alex ratified:
  speaker-pair specialisation of `algebra_metalogue_session`).
- Does NOT touch any of the 14 landed `@kintsugi/fracture/*` shard
  files.
- Does NOT touch `shards/algebra/metalogue.mirror`.
- Does NOT touch `docs/specs/fate-silicon-metalogue-in-void-duality-
  basis.md` (a18ca90 precedent — reference only).
- Does NOT execute the autopoietic composition edge (Reed follow-up
  tick A per §6.1).
- Does NOT seed `@silicon/algebra` with the 21 mirror-authored
  bilateral retirements (Reed-subagent follow-up tick B per §6.2).
- DOES name the composition via LANDED substrate primitives
  (`algebra_metalogue_session`, `@silicon/algebra`, `@fate/algebra`,
  `@kintsugi/fracture`, `@bauchladen.crystallize`,
  `algebra_witnessing`, `bauchladen_witnessing`,
  `algebra_metalogue_witnessing`).
- DOES reconcile the two readings (fractures-as-turns + mending-as-
  metalogue-session) per Alex's ratification.
- DOES forward-promise the composition edge and the empirical seeding
  as Reed follow-ups.

The binding names the algebra at family-root altitude; the algebra's
elements enumerate the substrate's mending vocabulary; the metalogue's
speaker-pair specialisation makes the mending's autopoietic growth
addressable.

Substrate already carried it (Alex's roomba spec verbatim + `a18ca90`
precedent + `fa569ce` fifteenth species). This spec names it.

---

## Related

- [[shards/kintsugi.mirror]] — THIS tick: family root extended with
  the algebra binding + bilateral.
- [[shards/algebra/metalogue.mirror]] — the `algebra_metalogue_session`
  carrier THIS binding specialises (`34cf333`, 2026-06-29).
- [[shards/silicon/algebra.mirror]] — speaker 1 (realiser).
- [[shards/fate.mirror]] — carrier for `@fate/algebra` path-namespace
  (speaker 2 / proposer; lines 208-232).
- [[shards/kintsugi/fracture/keyword.mirror]] — element 1 of the
  algebra (first landed fracture species).
- [[shards/kintsugi/fracture/bilateral_arm_redundant.mirror]] — element
  15 of the algebra (Mara `fa569ce`, 2026-07-16).
- [[shards/kintsugi/roomba.mirror]] — Alex's verbatim quote at lines
  400-406 (the roomba spec that named the two readings).
- [[shards/epistemologic/pact/bilateral.mirror]] — the bilateral-shape
  substrate `kintsugi_algebra_witnessing` composes over (Mara
  `a0f4d3f`, 2026-07-16).
- [[shards/bauchladen.mirror]] — the tray write-back extends the
  algebra by one element per tick.
- [[shards/algebra.mirror]] — the operator-basis altitude both
  speakers inherit under.
- [[docs/specs/fate-silicon-metalogue-in-void-duality-basis.md]] —
  Mara `a18ca90` precedent (2026-07-08); the session-shape THIS spec
  lifts into a named algebra binding.
- [[docs/specs/autopoietic-inference-loop.md]] — Mara `152` precedent;
  the `@bauchladen ← @autopoietic ← @fate` chain THIS spec enriches
  with `@kintsugi/algebra` as candidate pool.
- [[docs/specs/bilateral-predicate-substrate-shape.md]] — Mara
  `9a77361` (2026-07-16); the bilateral-shape spec
  `kintsugi_algebra_witnessing` inherits from.
- [[docs/specs/kintsugi-fracture-bilateral-arm-redundant.md]] — Mara
  `6c534c6` (2026-07-16); the fifteenth-element spec.
- [[docs/math/kintsugi/algebra-as-metalogue-session.md]] — Mara
  THIS-tick math foundation; formal algebra shape + autopoietic
  closure theorem + Connes-triple correspondence.
- [[docs/math/epistemologic/pact/bilateral-sentinel.md]] — Mara
  `701828a` (2026-07-16); sentinel-as-content-addressed-witness math.
- [[docs/loop/CURRENT.md]] — active arc state at time of writing.
