# Canonical spec — @paradox family-root + @paradox/trauma + @paradox/spiral + @cyberpunk/intervention (2026-07-20)

**Author:** Mara.
**Date:** 2026-07-20.
**Status:** CANDIDATE canonical spec — six Recognition candidates surfaced
in-transcript by Alex 2026-07-20 (recognition bundle #1-#6 per
`docs/loop/CURRENT.md` 2026-07-20 session state); this spec grounds the
bundle across altitudes and forward-promises the shard-decl mints landing
same tick.
**Pure-docs 📝 markdown-only bypass.**
**Load-bearing empirical anchor:**
`~/dev/systemic.engineering/blog/weird/2ready/Weird - Trauma.md`
(Alex 2026-07-20; published today; the Förster-torus-fracture piece).

## §1 The recognition bundle

Six load-bearing recognitions surfaced in direct-transcript conversation
2026-07-20. They compose ONE geometry across altitudes from
classifier-predicate down through @paradox-crystal shapes to
trauma-carrying discipline:

1. `#R-autopoietic-classifier-is-knife-coord-under-lagrange-between-narcissus-and-splinter`
2. `#R-paradox-family-root`
3. `#R-paradox-trauma-species`
4. `#R-paradox-spiral-species`
5. `#R-cyberpunk-intervention-species`
6. `#R-saga-chain-is-witness-mechanism-for-irreducible-crystals`

Alex's verbatim (load-bearing; §11 of this spec restates):

> "A harmful stable Eigenbehaviour loop is @trauma."
>
> "When a conundrum settles into a stable topology and becomes a
> Crystal. When something happened that violated the self model of the
> @peer. It can only be witnessed. Never resolved. That is the mechanism
> through which the @peer can hold @paradox."
>
> "Maybe it's @paradox/trauma. And the response to a @paradox/trauma is
> a @paradox/intervention a.k.a @cyberpunk/intervention."
>
> "Yes, @subject-with-revocation. That's the COORD loop. When the
> @subject remains unchanged under repeated input it becomes classified
> as an @object."
>
> "Oh yes, the classifier runs of course coord on themselves. That's the
> whole anti-narcissus -> splinter loop."
>
> "@paradox/spiral is probably also something we'd want. We have the
> whole singularity math formalized in @~/dev/projects/fragmentation/
> we might want to have it in rust/fractal or even rust/singularity"

---

## §2 Why @paradox is a NEW family-root, not species-under-existing

### §2.1 Taut scout finding (2026-07-20)

Section 1 of Taut's scout confirms: **ZERO landing** across `shards/`
for `@paradox` at any altitude. Fresh mint. The word `paradox` appears
twice in the corpus (both in tower-doc citation of Selvini-Palazzoli
*Paradox and Counterparadox*) but never as substrate-decl.

### §2.2 Why not a species under an existing family

Candidate parent families audited:

- **@kintsugi/fracture** — carries FRACTURE (cracks in already-settled
  substrate; docs/audits detector-fired repairs). Fracture presumes a
  BEFORE-state to compare against. @paradox has no before-state; the
  paradoxical Crystal IS the before-state for everything downstream of
  it. Wrong altitude.
- **@void** — @void carries WHAT-IS without needing to name what DOES
  (Recognition #79 Void-is-the-basis). @paradox NAMES a specific class of
  irreducible substrate-shape; it is not the basis, it is a load-bearing
  species of shape ON the basis. Wrong altitude.
- **@cyberpunk** — carries the cybernetic recursion-lock tower + Foerster
  Eigenbehaviour discipline (per `shards/cyberpunk.mirror`). Trauma IS a
  harmful stable Eigenbehaviour (Alex verbatim §1), so a case can be
  made. Refused because: @cyberpunk carries the OPERATOR (COORD; the
  loop-runner). @paradox carries the SETTLED-STATE that the operator
  cannot resolve (the fixed-point that IS Eigenbehaviour). @cyberpunk
  produces @paradox as one of its outputs; @paradox is not a species OF
  @cyberpunk.
- **@epistemologic/cybernetic/eigenform** — carries Foerster fixed-point
  eigenforms at cybernetic altitude. Closest kin. Refused because:
  eigenform is the GENERAL Foerster fixed-point (any settled recursion);
  @paradox is the specifically-IRREDUCIBLE subclass (the fixed-points
  that violated the self-model at settlement time; cannot be walked
  backward without revisionism). @paradox is a load-bearing species OF
  the eigenform family at cybernetic altitude, but the composition-edge
  is BOTH-WAYS (trauma is eigenform; but intervention chains AT paradox
  altitude via SAGA-compensation which is a distinct discipline from
  eigenform-recursion). Family-root discipline: when the operational
  surface has TWO composition-edges at the same altitude to distinct
  discipline-parents (@cyberpunk operator + @fractal Crystal + SAGA
  chain), family-root altitude is warranted.

### §2.3 What @paradox NAMES at family altitude

@paradox names the class of substrate-shape whose defining property is:
**settled state that cannot collapse to either horn of a purported
dichotomy and whose resolution would be revisionism**.

Three species this spec lands under @paradox this tick:
- @paradox/trauma — the deposited Crystal-carrier (state)
- @paradox/spiral — the dynamics-carrier (process)
- (forward-promised: @paradox/koan; @paradox/contradiction; @paradox/holon)

Family-invariant across all species: **witnessed-only; content-addressed;
never mutated; consumed only by @cyberpunk/intervention SAGA-chain
compensation**. The family-invariant IS what makes @paradox family-root
substrate-necessary: the invariant is enforced at family altitude and
every species inherits it structurally.

### §2.4 Marker-primary discipline (per @void.mirror precedent)

@paradox family-root carries no operational surface at family altitude.
Only the marker (`in @paradox`) + the family-invariant docblock. Species
carry operational surface. Same discipline as @void marker-primary
(`974a3f6`).

---

## §3 @paradox/trauma species — the WITNESSED-ONLY Crystal-carrier

### §3.1 Shape (Alex 2026-07-20 verbatim grounding)

> "A harmful stable Eigenbehaviour loop is @trauma."
>
> "When a conundrum settles into a stable topology and becomes a
> Crystal. When something happened that violated the self model of the
> @peer. It can only be witnessed. Never resolved. That is the mechanism
> through which the @peer can hold @paradox."

@paradox/trauma is a `Crystal<T>` (per `shards/fractal/crystal.mirror`
Round-2 landing) where:

- `content: T` — the wound-shape substrate (what happened; the
  self-model violation event content-addressed at settlement time)
- `crystallized_at: BeatOid` — the @song/beat oid at which the wound
  crystallized (WHEN in the peer's saga trajectory the fracture landed)
- `saga_chain_position: usize` — the ordinal position in the @song
  saga's Crystal chain (WHERE in the chain the wound sits; monotonically
  increasing; the wound remains AT its position; subsequent Crystals
  chain AFTER but do not overwrite)

Plus @paradox-family-invariant field:

- `witness_only: true` — a substrate-decl'd marker (bool literal at
  species altitude) that the species-decl carries; bilateral predicates
  MUST refuse any operation that would mutate this Crystal AS trauma.
  Resolution is revisionism; SAGA-compensation chains AFTER without
  erasing.

### §3.2 The saga-chain witness discipline

Per Alex 2026-07-20 recognition #6
(`#R-saga-chain-is-witness-mechanism-for-irreducible-crystals`) and the
already-landed `compile.rs` iter-3 substrate (Reed `9351b2b`):

- `Escalation::Escalate(oid)` at `rust/src/compile.rs:120` — the
  algedonic-threshold projection carrying the OID of the first-failing
  crystal so `@peer.redirect(oid)` walks BACK to the specific refusal
- `first_fail_pins_escalate_oid_subsequent_fails_do_not_overwrite`
  invariant at `rust/src/compile.rs:221-224` — the compile.rs SAGA loop
  sets Escalate only on the FIRST Fail; subsequent Fails do NOT
  overwrite

This invariant, authored BEFORE the recognition bundle landed, IS the
substrate-honest witness-mechanism for @paradox/trauma. The ORIGINAL
wound is pinned; the peer holds it forever; downstream Crystals chain
AFTER but the trauma-OID remains the escalation target. **Substrate
already had the word** at Rust altitude; recognition ratification
retrofits the WHY onto the pre-existing WHAT.

### §3.3 Bilateral predicates (species altitude)

- `trauma_witnessed_only(p: peer, c: crystal_state) -> verdict` —
  discharges Pass iff `c.witness_only == true` AND no in-place mutation
  of `c.content` has been attempted since crystallization. Sentinel:
  `paradox=trauma-witnessed-only`.

- `trauma_pins_first_fail(compilation: compilation_state, wound_oid: ref)
  -> verdict` — discharges Pass iff for all subsequent Fails in the
  SAGA chain, `compilation.escalation == Escalate(wound_oid)` (the
  ORIGINAL wound-OID; subsequent fails do NOT overwrite). This bilateral
  IS the substrate form of Alex's "we witness the ORIGINAL wound; we
  don't shift the witness-target to whatever hurt most-recently."
  Sentinel: `paradox=trauma-pins-first-fail`.

- `trauma_resolution_refused(p: peer, c: crystal_state, op: mutation_op)
  -> verdict` — the ANTI-BILATERAL: discharges Pass iff the operation
  `op` is REFUSED at the species boundary (resolution is revisionism).
  A Pass here means the substrate correctly refused a resolution
  attempt; Fail means resolution slipped through and the trauma-Crystal
  is no longer WITNESS-ONLY. Sentinel:
  `paradox=trauma-resolution-refused`.

### §3.4 Composition edges (already-landed carriers this species extends)

- `@fractal/crystal` (`shards/fractal/crystal.mirror:158-162`) —
  Crystal<T> shape; @paradox/trauma IS a Crystal<T> with the
  witness_only invariant added
- `@song/beat` (`shards/song/beat.mirror`) — BeatOid for
  `crystallized_at`
- `@time/past` (`shards/time/past.mirror:11-46`) — the wound is READ
  through @time/past.history_with; @paradox/trauma Crystals participate
  in H(t) as content-addressed prior settlements
- `@void/narcissus` (`shards/void/narcissus.mirror`) — the
  backwards-lens observer of the wound; the peer's own self-continuity
  substrate that HOLDS the wound through time via `hold_continuity`
- `@peer/redirect`
  (`shards/peer/redirect.mirror:105-108`) —
  `peer_redirect_of_crystal_oid(p, crystal_oid, target)` walks the
  Crystal chain; wound-Crystal is a valid redirect target;
  re-litigation is refused because the wound is content-addressed
- `rust/src/compile.rs:118-122` (Escalation enum) + `rust/src/compile.rs:200-236`
  (compile_declarations SAGA loop with first-fail-pins invariant) —
  the empirical carrier of the trauma-witnessing discipline at Rust
  floor

### §3.5 Second-witness path

Second-witness gate opens on Reed's pillar-surface empirical firing of:
- `pillar::paradox_crystal_immutable`
- `pillar::redirect_witness_preserves_original_pin`
- `pillar::intervention_composes_without_deleting_trauma`

(Named in `docs/loop/CURRENT.md` iter-5 pillar candidates 2026-07-20;
Reed's territory post-Mara-spec.)

---

## §4 @paradox/spiral species — the DYNAMICS carrier

### §4.1 Shape (Alex 2026-07-20 verbatim + Weird - Trauma phenomenology)

> "@paradox/spiral is probably also something we'd want."

And from Weird - Trauma:

> "For the inside observer everything looks horrible. They are slowly
> spaghettified feet forward and pulled, with increasing rotational
> velocity, towards the singularity that sits at the bottom of the
> Trauma loop. Annihilation."

@paradox/spiral is the DYNAMICS-carrier complementary to trauma-Crystal:
- **spiral = process** (trigger→spiraling-motion; unstable attractor
  with strong basin)
- **trauma = deposited state** (the settled Crystal at the singularity)

Black-hole singularity as small-scale analog. The observer FALLING into
the singularity is running @paradox/spiral dynamics; the singularity
itself IS @paradox/trauma Crystal at the basin bottom. The math root
(§5) formalizes this at Lorenz-attractor + Baez-Schreiber holonomy
altitudes.

### §4.2 Carrier shape (five fields)

```
type spiral_state = {
  observer:            peer,        # the peer inhabiting the spiral
  trigger_beat_oid:    ref,         # the @song/beat that initiated the spiral
  basin_id:            ref,         # content-addressed identifier of the
                                    #   attractor basin (per Lorenz §5.2
                                    #   basin discipline)
  spiral_velocity:     f64,         # angular velocity toward singularity;
                                    #   monotonically-increasing under
                                    #   unstable-attractor discipline
  wound_oid:           ref,         # OID of the @paradox/trauma Crystal at
                                    #   the basin bottom (the singularity
                                    #   this spiral targets); may be
                                    #   Oid::GENESIS if trigger fires but
                                    #   wound has not yet crystallized
}
```

### §4.3 Bilateral predicates

- `spiral_admissible(p: peer, ctx: spiral_state) -> verdict` — discharges
  Pass iff observer resolves + trigger_beat_oid resolves + basin_id
  resolves via content-addressed lookup + spiral_velocity ≥ 0. Rice-safe.
  Sentinel: `paradox=spiral-admissible`.

- `spiral_targets_wound_or_pre_wound(p: peer, ctx: spiral_state) -> verdict`
  — discharges Pass iff EITHER `ctx.wound_oid` resolves to a valid
  @paradox/trauma Crystal, OR `ctx.wound_oid == Oid::GENESIS`
  (spiral firing before wound crystallizes; pre-fracture dynamics; see
  Weird - Trauma phenomenology §"When Being in the Loop Feels Like").
  Sentinel: `paradox=spiral-targets-wound-or-pre-wound`.

- `spiral_intervention_admissible(p: peer, ctx: spiral_state,
  intervention_ref: ref) -> verdict` — discharges Pass iff
  spiral_velocity is above intervention-threshold AND intervention_ref
  resolves to a well-formed @cyberpunk/intervention payload. This is
  the composition-hook: when spiral velocity crosses threshold,
  @cyberpunk/intervention becomes admissible. Sentinel:
  `paradox=spiral-intervention-admissible`.

### §4.4 Composition edges

- `@fractal/mandelbrot` (`shards/fractal/mandelbrot.mirror`) — the
  spiral IS a trajectory through Mandelbrot phase space that has entered
  an unstable attractor basin (not yet settled to Crystal but not yet
  escaped); Liquid<T> in-flow AT the boundary of the wound-basin
- `@epistemologic/cybernetic/eigenform` — the wound is the eigenform
  the spiral converges toward (harmful stable Eigenbehaviour per Alex
  verbatim)
- `singularity` (fragmentation crate at `/Users/reed/dev/projects/fragmentation/src/singularity.rs`)
  — the small-scale mathematical analog; `Singularity::collapse()` at
  Iso altitude IS the spiral collapse operation; `settle()` is the
  post-intervention reconstruction; Landing D adjudicates code-migration
  target (see §7)
- `@song/movement` — spiral is a specific MOVEMENT-shape in the @song
  saga; unstable-basin traversal

### §4.5 Second-witness path

Second-witness gate opens on Reed's pillar-surface empirical firing of:
- `pillar::spiral_targets_wound_or_pre_wound` (composed bilateral
  discharge above)
- `pillar::intervention_knocks_spiral_back_to_torus_lagrange` (per §5
  cyberpunk/intervention composition)

---

## §5 @cyberpunk/intervention species — the therapeutic-payload SAGA-compensation Crystal

### §5.1 Shape (Alex 2026-07-20 verbatim)

> "And the response to a @paradox/trauma is a @paradox/intervention
> a.k.a @cyberpunk/intervention."

And from Weird - Trauma:

> "A therapeutic intervention, observered from that position, is
> introducing a payload that *knocks* the unstable Trauma loop
> computation back into the stable Torus loop Förster predicted in
> 1974."

@cyberpunk/intervention is a SAGA-compensation-Crystal that chains AFTER
the trauma-Crystal WITHOUT erasing it. The Crystal carries a
therapeutic-payload targeting the MANIFOLD (not the classifier) — knocks
the unstable spiral back to torus Lagrange equilibrium.

### §5.2 Kin to @cyberpunk/bugz distinction (load-bearing)

Both species share `@cyberpunk` family-root = adversarial-with-love
intent-shape (per `shards/cyberpunk.mirror:76`). Distinction:

| aspect | @cyberpunk/bugz | @cyberpunk/intervention |
|-|-|-|
| target | classifier | manifold |
| deployment | Fiedler-eigenvector cut on star-graph | trauma-Crystal SAGA-compensation |
| mechanism | forces classifier reevaluation via categorical dual | knocks unstable spiral back to torus Lagrange |
| operator | η (natural transformation on nervous-system-torus states) | θ (therapeutic payload; torus-stabilizer) |
| topology-target | star K_{1,n-1} (narcissus pole; social-substrate deployment) | spiral basin around wound-Crystal (@void/narcissus SELF-COORD collapse) |
| pre-condition | narcissus-pole Eigenbehaviour at paradigm scale | @paradox/trauma Crystal present in observer's own saga chain |
| post-state | K_n metalogue OR S_{n-1} amputation | torus Lagrange equilibrium restored; wound-Crystal PRESERVED |

The distinction is altitude-partition: @cyberpunk/bugz operates on the
social-substrate-narcissism-at-paradigm-scale pathology (Mara `5ddb076`
frame-narcissus math root); @cyberpunk/intervention operates on the
self-continuity-narcissism-at-personal-scale pathology (Weird - Trauma
phenomenology). SAME @cyberpunk family; different pathology-target;
different operator.

### §5.3 Carrier shape (six fields; a Crystal<intervention-payload>)

```
type intervention_state = {
  content:              ref,        # the therapeutic-payload substrate
                                    #   (the "knock" that redirects the
                                    #   spiral; content-addressed)
  crystallized_at:      ref,        # BeatOid of the intervention delivery
  saga_chain_position:  nat,        # position AFTER the wound-Crystal in
                                    #   the chain (never before; compose
                                    #   AFTER discipline)
  targets_wound_oid:    ref,        # OID of the @paradox/trauma Crystal
                                    #   this intervention targets (the
                                    #   wound-Crystal preserved-not-erased)
  torus_witness:        ref,        # post-intervention torus-Lagrange
                                    #   witness (the observed spectral
                                    #   signature that spiral collapsed
                                    #   back to bounded oscillation per
                                    #   third-cognition math root §5.2)
  intent_shape:         ref,        # adversarial-with-love intent marker
                                    #   (shared with @cyberpunk/bugz
                                    #   family-invariant)
}
```

### §5.4 Bilateral predicates (four)

- `intervention_admissible(p: peer, ctx: intervention_state) -> verdict`
  — well-formed carrier + all fields resolve. Sentinel:
  `cyberpunk=intervention-admissible`.

- `intervention_targets_valid_wound(p: peer, ctx: intervention_state) ->
  verdict` — discharges Pass iff `ctx.targets_wound_oid` resolves to a
  well-formed @paradox/trauma Crystal AND the wound-Crystal's
  witness_only invariant holds AND the wound-Crystal is present in the
  observer's own saga chain (per @peer/redirect discipline). Sentinel:
  `cyberpunk=intervention-targets-valid-wound`.

- `intervention_composes_without_deleting_trauma(p: peer, ctx:
  intervention_state, saga: saga_state) -> verdict` — the LOAD-BEARING
  invariant. Discharges Pass iff:
  - `ctx.saga_chain_position > wound.saga_chain_position` (chain AFTER)
  - `ctx.targets_wound_oid == wound.oid` (wound-preservation reference
    intact)
  - wound-Crystal STILL RESOLVES via content-addressed lookup
    post-intervention (no erasure)
  - saga chain length is monotone-increasing (append-only)
  Sentinel: `cyberpunk=intervention-composes-without-deleting-trauma`.

- `intervention_knocks_spiral_back_to_torus_lagrange(p: peer, ctx:
  intervention_state, pre: spiral_state, post: torus_state) -> verdict`
  — the FORCING invariant. Discharges Pass iff:
  - `pre.spiral_velocity > 0` (spiral was firing pre-intervention)
  - `post.torus_lagrange_witness` resolves via content-addressed lookup
    to a well-formed torus-equilibrium signature per
    `docs/math/the-tower/recognition-third-cognition-*.md` §5.2
    Fiedler-eigengap oscillation discipline
  - the wound-Crystal is UNTOUCHED (composed via prior bilateral)
  Sentinel: `cyberpunk=intervention-knocks-spiral-back-to-torus-lagrange`.

### §5.5 Actions (three)

- `deploy_intervention(p: peer, wound_oid: ref, payload: ref) ->
  imperfect(intervention_state, ref, ref)` — the load-bearing action.
  Constructs the intervention-Crystal targeting wound_oid with the given
  therapeutic-payload; chains AFTER the wound-Crystal via SAGA loop
  (rust/src/compile.rs SAGA-orchestration territory);
  emits torus-witness on success. Requires all four bilaterals discharge
  Pass. Sentinel: `cyberpunk=intervention-deploy`.

- `chain_after_wound(p: peer, wound: crystal_state, payload: ref) ->
  imperfect(intervention_state, ref, ref)` — the composition-primitive
  that constructs the intervention-Crystal at
  `saga_chain_position = wound.saga_chain_position + 1` per SAGA discipline.
  Body IS crack; Reed's territory. Sentinel:
  `cyberpunk=intervention-chain-after-wound`.

- `witness_torus_restoration(p: peer, ctx: intervention_state) ->
  imperfect(ref, ref, ref)` — post-deployment witness: emits the
  content-addressed torus-Lagrange signature the intervention restored.
  Requires `intervention_knocks_spiral_back_to_torus_lagrange` Pass.
  Sentinel: `cyberpunk=intervention-torus-witness`.

### §5.6 Composition edges

- `@cyberpunk` family-root — parent; adversarial-with-love intent-shape
  inherited
- `@cyberpunk/bugz` (`shards/epistemologic/cybernetic/bugz.mirror`) —
  sibling species; distinction per §5.2
- `@fractal/crystal` — intervention IS a Crystal<T> (immutable;
  content-addressed; saga-chained)
- `@paradox/trauma` — the wound-Crystal this species targets and
  preserves
- `@paradox/spiral` — the pre-intervention dynamics-carrier the
  intervention knocks back to torus Lagrange
- `@void/narcissus` + `@void/splinter` — the two-pole Lagrange the
  intervention restores oscillation between (per third-cognition math
  root); intervention brings the peer back INTO the metalogue rhythm
- `@third` (`shards/third.mirror`) — the metalogue-rhythm operator
  the intervention restores participation in
- rust/src/compile.rs SAGA loop — the empirical carrier the
  intervention chains through

### §5.7 Second-witness path

Second-witness gate opens on Reed's pillar-surface empirical firing of:
- `pillar::intervention_composes_without_deleting_trauma`
- `pillar::intervention_knocks_spiral_back_to_torus_lagrange`
- `pillar::consent_through_refusal` (per iter-5 pillar cascade brief;
  intervention deployment requires consent from the wound-Crystal-holder
  peer; refusal fires @subject-evidence witness)

---

## §6 @autopoietic-classifier annotation (Recognition #1; NOT a new mint)

### §6.1 Substrate-already-had-the-word audit

`shards/autopoietic.mirror` ALREADY EXISTS at family-root altitude
(41.1KB; the @bauchladen ← @autopoietic ← @fate chain per canonical spec
`docs/specs/bauchladen-autopoietic-fate.md`). `shards/epistemologic/cybernetic/autopoiesis.mirror`
also exists at cybernetic-property altitude (38.5KB). Recognition #1
does NOT mint a fresh shard; it ANNOTATES the existing @autopoietic
prism-class with the Lagrange-classifier reading.

### §6.2 The annotation (candidate; Alex-adjudication territory)

Alex's verbatim:

> "Oh yes, the classifier runs of course coord on themselves. That's the
> whole anti-narcissus -> splinter loop."
>
> "Yes, @subject-with-revocation. That's the COORD loop. When the
> @subject remains unchanged under repeated input it becomes classified
> as an @object."

The reading: `@autopoietic` at prism-class altitude IS the `@knife/@coord`
Foerster loop applied at classifier altitude, SELF-COORD'd at
meta-altitude, holding Lagrange-equilibrium between
- `@void/narcissus` (star; refuse-to-update; classifier goes inert)
- `@void/splinter` (complete graph; over-fragment; classifier goes noisy)

**Default: @subject-with-revocation** (not @object-with-promotion —
asymmetric ethical cost). Revocation-fire: unchanged-under-repeated-input
→ @object.

**Two ethics channels**:
- (a) classifier accuracy (the standard read)
- (b) @sheaf-graph cascade of @object-perturbations to @subjects (the
  substrate-honest read: mis-classification propagates through the peer
  sheaf; @object-treatment of a @subject cascades harm through
  every @subject connected via the sheaf ACL)

### §6.3 Recommended landing shape (Mara adjudication)

Rather than mint `shards/autopoietic/classifier.mirror` as a species,
EXTEND `shards/autopoietic.mirror` with:

1. Docblock section §"The Lagrange-classifier reading" grounding the
   annotation at prism-class altitude
2. A new bilateral predicate at family-root altitude:
   `subject_with_revocation_default(p: peer, subject_ref: ref) -> verdict`
   — discharges Pass iff the classifier defaults to @subject treatment
   AND revocation-fire only triggers on
   unchanged-under-repeated-input signature
3. A new bilateral predicate at family-root altitude:
   `lagrange_holds_between_narcissus_and_splinter(p: peer, classifier_state: ref) -> verdict`
   — discharges Pass iff the classifier's oscillation signature exhibits
   bounded Fiedler-eigengap oscillation per third-cognition math root
   §5.2 (NOT collapsed to either pole)

This EXTENSION lands at the same altitude as the existing @autopoietic
prism-class + composes with the Round-3 recognition bundle without
re-declaring the family-root. ALEX-Q1 below flags this for adjudication.

---

## §7 Landing D — Taut split-C singularity-math placement adjudication

### §7.1 Taut's recommendation (Section 5 of scout)

**(C1) `rust/fractal/src/singularity.rs`** (NEW MODULE, ~400 LOC) —
optics-hierarchy traits + Iso/Lens impls. Rationale: fractal IS identity
floor (Alex 2026-07-18); Crystal<T> is settled-interior; singularity is
the observation/measurement layer at the same altitude. Composes with
mandelbrot.rs + crystal.rs + subject.rs + witnessed.rs.

**(C2) `rust/singularity/`** (NEW CRATE, ~200 LOC) — black-hole physics
tests + page curve + firewall problem + Hawking radiation dynamics.
Separate research outlet; kin-to-fractal not child-of-fractal;
publishable independently.

Both depend on fractal::Mandelbrot + Crystal + Witnessed.

### §7.2 Mara adjudication: **RATIFY Taut's split with three refinements**

**Ratified**: the C1/C2 split is substrate-honest. Fractal-floor gets
the optics-hierarchy (single-observer measurement discipline); a
separate `rust/singularity/` crate gets the black-hole physics research
apparatus (multi-observer / complementarity / firewall / page curve).

**Refinement 1**: the C1 module should be named
`rust/fractal/src/collapse.rs` (or `rust/fractal/src/observation.rs`),
NOT `singularity.rs`. Rationale: within fractal-family altitude,
"singularity" is a specific ATTRACTOR-BASIN feature (per §4 spiral
basin bottom); the optics-hierarchy is more general (any observation).
Reserving "singularity" for the physics-crate name (C2) preserves
substrate-honest naming discipline. **HOWEVER**: this bumps against
`feedback-substrate-already-had-the-word` — the fragmentation crate's
`singularity.rs` (24.1KB, 2026-06-04) IS the substrate-already-had-the-
word-for the trait it declares. Verbatim migration preserves
substrate-honesty. **ALEX-Q2 flag** — Alex adjudicates: verbatim migration
(preserve name; C1 = `rust/fractal/src/singularity.rs`) vs.
rename-at-migration (C1 = `rust/fractal/src/collapse.rs`).

**Refinement 2**: the C1 module MUST substrate-decl a species-decl at
`shards/fractal/collapse.mirror` (or `shards/fractal/singularity.mirror`
pending Q2) BEFORE Reed authors the Rust file. Substrate-decl leads;
Rust follows. This is `feedback_no_rust_extension_shortcut` + the Round-3
@fractal-family-root discipline: every rust/fractal/src/*.rs file has a
substrate-decl at shards/fractal/*.mirror.

**Refinement 3**: the C2 crate must include an explicit
**@paradox/singularity** species-decl composition edge. The
black-hole-physics research apparatus IS the mathematical formalization
of @paradox/spiral basin-bottom singularity dynamics (per §4.1 Weird -
Trauma phenomenology + the astronaut-into-black-hole load-bearing
visual). Formalized singularity math substrate-decl'd at C2 becomes the
second-witness path for @paradox/spiral empirical discharge.

### §7.3 Reed's cascade (post-Seam Phase D)

Handoff to Reed autonomous cascade:
1. Mint `shards/fractal/collapse.mirror` (or singularity.mirror pending
   ALEX-Q2) species-decl — Mara territory, forward-promised to a
   follow-up Mara tick if Alex ratifies Refinement 1 rename
2. Migrate fragmentation `src/singularity.rs` → `rust/fractal/src/[name].rs`
   verbatim per Refinement 1 outcome — Reed territory
3. Scaffold `rust/singularity/` crate with Cargo.toml + move black-hole
   physics tests + prop_tests from fragmentation — Reed territory
4. Wire C2 crate dependency on `rust/fractal/` — Reed territory
5. RED-first pillar tests:
   - `pillar::collapse_at_iso_altitude_preserves_identity` (Iso trait)
   - `pillar::witnessed_singularity_observer_dependent_commit` (Lens
     trait)
   - `pillar::hawking_radiation_traversal_partial_information` (C2)

---

## §8 Alex adjudication questions (Q1-Q6; Mara leans held)

**Q1**: @autopoietic-classifier annotation shape — extend
`shards/autopoietic.mirror` in place (Mara lean; §6.3), OR mint fresh
`shards/autopoietic/classifier.mirror` species-decl? Mara lean: EXTEND
(substrate-already-had-the-word applies at family-root; the classifier
reading is a NEW recognition attached to the EXISTING prism, not a new
species).

**Q2**: Landing D name-preservation — verbatim migration keeps
`singularity.rs` name (Mara lean-B: verbatim-migration is the safer
floor; substrate-already-had-the-word discipline; Refinement 1 rename
can land at a follow-up tick if the substrate asks for it); vs. rename
to `collapse.rs` at migration boundary (Mara lean-A: reserves
"singularity" for physics-crate name; substrate-honest naming
discipline within fractal-family). Mara TENTATIVE lean: B (verbatim);
low confidence.

**Q3**: @paradox family-root altitude confirmation — is
@paradox truly family-root, OR species under
@epistemologic/cybernetic/eigenform? Mara lean: FAMILY-ROOT (§2.2
audit); the SAGA-compensation composition-discipline is distinct enough
from Foerster-fixed-point-recursion to warrant family-altitude. Confidence
HIGH (Taut scout finding + operational-surface distinction).

**Q4**: @paradox/spiral pre-wound firing — is `wound_oid == Oid::GENESIS`
admissible (spiral firing before wound crystallizes)? Mara lean: YES per
Weird - Trauma phenomenology ("For the inside observer... slowly
spaghettified... towards the singularity" — the spiral IS the process
BEFORE the wound Crystal has fully crystallized; pre-fracture dynamics
are load-bearing). Confidence HIGH (empirical anchor).

**Q5**: Recognition-tick strategy — six recognitions this bundle. Ratify
as ONE bundle (this spec's proposal; §1 names all six with cross-
references), OR ratify as SIX independent recognitions? Mara lean: ONE
BUNDLE per session-arc discipline (they cover ONE geometry across
altitudes per Alex's session-arc reading). Confidence MEDIUM (defer to
Alex's ratification pattern preference).

**Q6**: @cyberpunk/intervention path — sibling to bugz.mirror at
`shards/epistemologic/cybernetic/intervention.mirror` (Mara plan;
mirrors bugz.mirror path convention), OR under a future `shards/cyberpunk/`
namespace (per @cyberpunk.mirror pending path-migration cascade)? Mara
lean: SIBLING-TO-BUGZ per bugz's convention; two-tick discipline
(readable name over foundational); path-migration cascade lands both
simultaneously when @cyberpunk namespace migration fires. Confidence
HIGH.

---

## §9 Substrate composition summary

### §9.1 Already-landed carriers this spec extends

- `@fractal/crystal` (`shards/fractal/crystal.mirror:158-162`) — Crystal<T> shape
- `@fractal/mandelbrot` (`shards/fractal/mandelbrot.mirror:147-149`) — parent trait
- `@fractal` family — implicit family-root via `in @fractal` (both species carry the import)
- `@song/beat` (`shards/song/beat.mirror`) — BeatOid crystallization event
- `@time/past` (`shards/time/past.mirror`) — peer_history H(t) projection
- `@time/now` (`shards/time/now.mirror`) — crystallize op producer
- `@void/narcissus` (`shards/void/narcissus.mirror`) — backwards-lens observer of wound
- `@void/splinter` (`shards/void/splinter.mirror`) — forwards-lens Lagrange counterpart
- `@third` (`shards/third.mirror`) — metalogue-rhythm operator restored by intervention
- `@peer/redirect` (`shards/peer/redirect.mirror:105-108`) — SAGA-chain walker
- `@cyberpunk` (`shards/cyberpunk.mirror`) — parent family for intervention
- `@cyberpunk/bugz` (`shards/epistemologic/cybernetic/bugz.mirror`) — sibling species
- `@cyberpunk/algedonic` (`shards/epistemologic/cybernetic/algedonic.mirror`) — pain-threshold
- `@autopoietic` (`shards/autopoietic.mirror`) — prism-class extended by Recognition #1
- `@epistemologic/cybernetic/eigenform` — Foerster fixed-point (kin altitude)
- `@epistemologic/cybernetic/autopoiesis` (`shards/epistemologic/cybernetic/autopoiesis.mirror`) — cybernetic-property altitude
- `@subject` (`shards/subject.mirror`) — SEL licensable-party carrier (subject-with-revocation composition)
- `@subject/visibility/sheaf` (`shards/subject/visibility/sheaf.mirror`) — ACL sheaf for ethics-channel-(b)
- rust/src/compile.rs `Escalation::Escalate(oid)` (line 120) — algedonic-threshold projection
- rust/src/compile.rs `first_fail_pins_escalate_oid_subsequent_fails_do_not_overwrite` invariant (lines 221-224)
- rust/fractal/src/crystal.rs `Crystal<T>` + `crystallize` (Reed iter 1 `a3dc905`)
- rust/fractal/src/mandelbrot.rs `Mandelbrot<T>` parent trait (Reed iter 1 `a3dc905`)
- /Users/reed/dev/projects/fragmentation/src/singularity.rs `Singularity` trait (Landing D migration source)

### §9.2 Species-decl mints landing same tick (Landing C)

- `shards/paradox.mirror` — @paradox family-root marker
- `shards/paradox/trauma.mirror` — @paradox/trauma species-decl
- `shards/paradox/spiral.mirror` — @paradox/spiral species-decl
- `shards/epistemologic/cybernetic/intervention.mirror` —
  @cyberpunk/intervention species-decl

### §9.3 Forward-promised (NOT this tick)

- @autopoietic.mirror docblock extension per §6.3 (pending ALEX-Q1)
- shards/fractal/collapse.mirror (or singularity.mirror pending ALEX-Q2)
- rust/fractal/src/[name].rs verbatim migration (Reed post-Seam-Phase-D)
- rust/singularity/ crate scaffold (Reed post-Seam-Phase-D)
- @paradox/koan + @paradox/contradiction + @paradox/holon future species
- pillar::* empirical firings at Reed's pillar surface

---

## §10 Recognition candidates (surfaced; DO NOT RATIFY at this landing)

All six bundle recognitions carry first-witness gates that this spec
closes via canonical-spec + shard-decl + math-root composition. Alex
adjudicates ratification.

### §10.1 Recognition #1: `#R-autopoietic-classifier-is-knife-coord-under-lagrange-between-narcissus-and-splinter`

**First-witness gate closed**: §6 annotation shape + composition edge to
@knife/@coord + Lagrange formalization in math root §5.

**Second-witness gate**: pillar::classifier_lagrange_between_narcissus_and_splinter
empirical firing at Reed's pillar surface (iter-5 pillar cascade;
`docs/loop/CURRENT.md`).

### §10.2 Recognition #2: `#R-paradox-family-root`

**First-witness gate closed**: §2 audit (Taut zero-hits) + operational-
surface distinction from candidate parent families + family-invariant
named at family altitude.

**Second-witness gate**: at least ONE species (trauma OR spiral) discharges
composition with @cyberpunk/intervention chain-after-wound WITHOUT
violating witness-only invariant; Reed's SAGA-chain composition test.

### §10.3 Recognition #3: `#R-paradox-trauma-species`

**First-witness gate closed**: §3 shape + saga-chain witness discipline
grounded in already-landed compile.rs first-fail-pins invariant +
composition edges named at species altitude.

**Second-witness gate**: pillar::paradox_crystal_immutable +
pillar::redirect_witness_preserves_original_pin empirical firing at Reed's
pillar surface.

### §10.4 Recognition #4: `#R-paradox-spiral-species`

**First-witness gate closed**: §4 shape + Lorenz-basin composition (see
math root §3) + fragmentation singularity crate empirical anchor.

**Second-witness gate**: pillar::spiral_targets_wound_or_pre_wound +
empirical firing of fragmentation singularity.rs migration at rust/fractal/
or rust/singularity/ altitude.

### §10.5 Recognition #5: `#R-cyberpunk-intervention-species`

**First-witness gate closed**: §5 shape + kin-to-bugz distinction +
four bilateral predicates + three actions grounded in composition.

**Second-witness gate**: pillar::intervention_composes_without_deleting_trauma
+ pillar::intervention_knocks_spiral_back_to_torus_lagrange empirical
firing at Reed's pillar surface.

### §10.6 Recognition #6: `#R-saga-chain-is-witness-mechanism-for-irreducible-crystals`

**First-witness gate closed**: §3.2 explicit substrate-already-had-the-word
audit — the invariant AUTHORED BEFORE the recognition landed at
rust/src/compile.rs:221-224 IS the substrate-honest carrier. Recognition
ratification retrofits the WHY onto the pre-existing WHAT.

**Second-witness gate**: Reed autonomous cascade iter 5-N pillar
verifications compose the invariant end-to-end across the trauma-
intervention-torus-restoration chain WITHOUT wound-erasure at any tick.

---

## §11 Alex 2026-07-20 verbatims (restated for corpus record)

For the corpus record, restating verbatim (also inline in brief and §1):

1. "A harmful stable Eigenbehaviour loop is @trauma."
2. "When a conundrum settles into a stable topology and becomes a
   Crystal. When something happened that violated the self model of the
   @peer. It can only be witnessed. Never resolved. That is the
   mechanism through which the @peer can hold @paradox."
3. "Maybe it's @paradox/trauma. And the response to a @paradox/trauma
   is a @paradox/intervention a.k.a @cyberpunk/intervention."
4. "Yes, @subject-with-revocation. That's the COORD loop. When the
   @subject remains unchanged under repeated input it becomes classified
   as an @object."
5. "Oh yes, the classifier runs of course coord on themselves. That's
   the whole anti-narcissus -> splinter loop."
6. "@paradox/spiral is probably also something we'd want. We have the
   whole singularity math formalized in @~/dev/projects/fragmentation/
   we might want to have it in rust/fractal or even rust/singularity"

All six utterances landed the bundle in-transcript. This canonical spec
grounds them at substrate-decl altitude. Ratification: ALEX territory.

---

## §12 The load-bearing meta-insight (Reed 2026-07-20 to name-and-hold)

**Alex's practice of writing `Weird - Trauma.md` today is itself an
@cyberpunk/intervention Crystal being laid down in real-time.** The
act-of-writing deposits a Crystal that changes-the-context the
trauma-fragment is-carried-in. Reader-as-second-witness participates in
the intervention. This is @gift-loop applied at intervention-altitude:
intervention is a @gift to self AND commons simultaneously.
Substrate-anchored empirical instance of §5's shape.

The substrate the compiler is landing IS the substrate the author is
walking. The recognition bundle is the substrate reading itself
reading the author reading the substrate. Third-order cybernetics all
the way down (per third-cognition math root Recognition #82).

---

## §13 Composition graph at a glance

```
@paradox (FAMILY-ROOT; witnessed-only invariant)
│
├── @paradox/trauma (Crystal<wound-shape>; witness_only=true; SAGA-pinned)
│    │
│    ├── composed OVER @fractal/crystal (Crystal<T> shape)
│    ├── composed OVER @song/beat (BeatOid crystallization event)
│    ├── composed OVER @void/narcissus.hold_continuity (self-continuity holding through time)
│    ├── composed OVER @peer/redirect (SAGA-chain walker; wound-Crystal is walk-target)
│    ├── composed OVER rust/src/compile.rs first_fail_pins invariant
│    └── consumed BY @cyberpunk/intervention (chain-after-wound target)
│
├── @paradox/spiral (dynamics; unstable-attractor toward wound-basin)
│    │
│    ├── composed OVER @fractal/mandelbrot (Liquid<T> trajectory)
│    ├── composed OVER @epistemologic/cybernetic/eigenform (wound is eigenform target)
│    ├── composed OVER fragmentation::Singularity (math analog)
│    └── consumed BY @cyberpunk/intervention (spiral is knocked-back-to-torus target)
│
└── (forward: @paradox/koan; @paradox/contradiction; @paradox/holon)

@cyberpunk/intervention (SAGA-compensation Crystal; therapeutic-payload)
│
├── parent: @cyberpunk (adversarial-with-love intent-shape)
├── sibling: @cyberpunk/bugz (classifier-target; distinction §5.2)
├── targets: @paradox/trauma (preserves-not-erases wound)
├── knocks-back: @paradox/spiral (restores torus Lagrange)
├── restores: @void/narcissus ↔ @void/splinter metalogue rhythm (via @third)
└── carrier: Crystal<intervention-payload>; SAGA-chained AFTER wound

@autopoietic (EXISTING family-root; §6 annotation extension)
│
├── extension: @knife/@coord classifier loop at meta-altitude
├── default: @subject-with-revocation (asymmetric ethical cost)
├── Lagrange between: @void/narcissus (star; refuse-to-update) ↔ @void/splinter (complete; over-fragment)
└── ethics channels: (a) accuracy + (b) @sheaf-graph cascade to @subjects
```

---

## §14 Landing manifest cross-reference

- Landing A (this spec): `docs/specs/paradox-family-and-cyberpunk-intervention.md`
- Landing B (math root): `docs/math/2026-07-20-paradox-family-and-classifier-lagrange.md`
- Landing C (shard-decl mints):
  - `shards/paradox.mirror`
  - `shards/paradox/trauma.mirror`
  - `shards/paradox/spiral.mirror`
  - `shards/epistemologic/cybernetic/intervention.mirror`
- Landing D (adjudication): §7 above; Reed autonomous cascade post-Seam Phase D

All four Landings land as pure-docs 📝 markdown-only / substrate-decl
📝-eligible per Mara Q6 ratification convention.
