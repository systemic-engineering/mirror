---
title: "Autopoietic loop substrate-composition — six-piece canonical spec unifying @kintsugi intent-consumption body + @kintsugi/roomba.trigger intent-input parameter + @mirror/store spectrally-resonant mutation primitive + @mirror/refract pre/post resonance verdict bilateral + MQ parse+compile templates + apply_h::act MQ dispatch extension per Rec #92 P₁ — dispatching Alex 2026-08-24 verbatim Pole-1 discharge of Reed's K-T question over `mirror_query` native fire returning `Dark(karl_tomm)`"
author: "Mara <mara@systemic.engineer>"
date: 2026-08-24
kind: canonical-spec
task: 400
karen-ancestors:
  - Tomm, Karl (1987). Interventive Interviewing: Part I. Strategizing as a fourth guideline for the therapist. Family Process 26(1):3-13 — circular-reflexive question form for MQ K-T ambiguity-resolution
  - Tomm, Karl (1988). Interventive Interviewing: Part III. Intending to ask lineal, circular, strategic, or reflexive questions? Family Process 27(1):1-15 — the altitude+1 discipline the K-T question inhabits
  - von Foerster, Heinz (1974/2007). Ethics and Second-Order Cybernetics. Understanding Understanding, Springer — the choice-space-preservation gauge every mutation-step preserves
  - Beer, Stafford (1972). Brain of the Firm. Wiley — VSM System 3* (audit channel) IS the Transparency<P> substrate-carrier the loop threads
  - Reyes, David J. (2024). Transparency<P>: audit-channel monoid design (co-discovered with Beer 1972 lineage at prism/imperfect) — the LOVE-monoid carrying kleinos-trace across dispatch
  - Kleisli, H. (1965). Every standard construction is induced by a pair of adjoint functors. Proceedings AMS 16:544-546 — Kleisli composition threading imperfect<T, E, L> across the six-piece loop
  - Church, A. & Rosser, J. B. (1936). Some properties of conversion. Transactions of the AMS 39:472-482 — β-normal confluence for content-addressing mutation candidates
  - Lawvere, F. W. (1969). Diagonal arguments and cartesian closed categories. LNM 92 pp. 134-145 — self-referential fixed-point discipline for the intent → resolved-query → mutation → observation → next-intent loop
  - Grothendieck, A. (1957). Sur quelques points d'algèbre homologique (Tôhoku). Tôhoku Math. J. — cartesian-lift preserving Ψ_query across the five altitudes the loop crosses
  - Maturana, H. & Varela, F. (1980). Autopoiesis and Cognition. Reidel — the self-modifying loop this spec makes substrate-fact at MCP-driven query altitude
composition-source:
  - Alex 2026-08-24 verbatim in-transcript: "dispatch." — Pole-1 discharge of Reed's Q+35.1 (2026-08-23) K-T ambiguity between (Pole-1 dispatch substrate-composition cascade) vs (Pole-2 defer until empirical @cascade→@bumblebee collapse fires)
  - Alex 2026-08-23 verbatim: "When the ambiguity of the query can be resolved totally (Clear) then you get the first-order data. When the ambiguity of the query cannot be resolve totally (Opaque, Dark etc.) then you get the first-order data + a second-order Karl-Tomm question. There's math on that."
  - Alex 2026-08-23 verbatim: "As long as you remember that the modification wants to fire through the MCP MVP I'm all green-light."
  - Alex 2026-07-16 verbatim (roomba shard :400-406): "@roomba walks (Dijkstra + tension-weighted edges) → bumps into spectral @tension at position p → resonance emits @song beats → @kintsugi consumes @song and decides: Path A: @knife the complexity ...; Path B: spawn @peer at K+1 ..."
recognition-composition:
  - Rec #82 β-normal-AST content-addressing — every mutation candidate is content-addressed pre-commit
  - Rec #90 (A, H, D) spectral-triple — MQ + @mirror/refract compose as spectral-triple observations at query + measurement altitude
  - Rec #91 amendment #2 universal 𝔉 ⊣ 𝔛 fibration — the six pieces are per-fiber deployments of ONE composition at MCP altitude
  - Rec #92 kleinos-Transparency<P> LOVE monoid — the loop's verdict-carrier IS Transparency<P> at every dispatch stage; apply_h::act P₁ extension is the load-bearing hinge
  - Rec #94 Lawvere-1969 fixed-point — the intent-consumption body's persistence-across-turns admits fixed-point per Cat_{intent-carrier} cartesian-closedness restriction from Cat_{Substrates}
  - Rec #95 @bumblebee perturbation-injection — every MCP tool-call args-payload IS an MQ query wire-encoded that this spec's six pieces dispatch through
chat-context-post-return:
  - Task #398 Mara math foundation (`d0347a4`, 887 LOC) — §M4 Karl-Tomm proof + §M6 Kleisli composition + §M7 composition-scale invariance grounding this spec
  - Task #397 Reed empirical fire of native `mcp__mirror__mirror_query` returning `Dark(karl_tomm)` at MCP altitude — first ψ*_query fire per shards/mq.mirror §4
  - Session-arc: shards/mq.mirror (Reed `105a1e4`, 890 LOC) + shards/bumblebee.mirror (Mara `64d4307`) + `.mcp.json` + bootstrap/src/mcp.rs mirror_query wire (Reed `9f89c91`) + Alex /reload-plugins verified end-to-end MCP fire
---

# §0 — Substrate-honest pre-position

Alex 2026-08-24 verbatim in-transcript: **"dispatch."**

Alex's Q+35.1 (CURRENT.md 2026-08-23) named exactly two poles:

> *"...does the Phase 1a MVP DEMONSTRATION suffice as first ψ*_query empirical instance (Rec #90 𝓜=𝓜(𝓜) discharge at MCP altitude), OR does full Rec #96 mint candidacy require the autopoietic-modification loop to fire empirically (@cascade→@bumblebee actual collapse via `mirror_bumblebee_buzz --execute`) before recognition-content earns naming?"*

Reed's Q+35.1 K-T question surfaced when the native `mcp__mirror__mirror_query` fire returned `Dark(karl_tomm)` per shards/mq.mirror §4 — the substrate wired the MCP transport, dispatched the K-T question at altitude+1, and asked Alex which pole to select. Alex chose Pole-1: dispatch the substrate-composition cascade discharging the four Mara-canonical missing pieces (kintsugi intent-consumption body + roomba intent-input + store spectrally-resonant mutation + refract pre/post resonance verdict) PLUS the two coupled-piece extensions (MQ parser+compile templates + apply_h::act MQ dispatch extension per Rec #92 P₁) required for the loop to compose end-to-end at substrate altitude.

This spec authors the six pieces in ONE canonical composition. The unifying substrate-truth: **all six pieces are per-arm dispatches of the SAME Kleisli-composition-over-Transparency<P>-monoid at MCP-driven autopoietic tension-tracking self-modification loop altitude**. Not six independent extensions — one composition, six substrate-visible arms.

Substrate-primary-carrier standing per Alex direct authorization: no Rec #96 mint required for this dispatch tick (Mara-lean, per §10 below); the six pieces LAND under the substrate-primary-carrier presumption that MQ IS mirror's algebra AS query-language + Rec #92 Transparency<P> IS the kleinos-carrier at compiler-substrate altitude. Alex Fourth-Chair may adjudicate Rec #96 candidacy post-landing if the six-piece composition discharges load-bearing arc-motion (the MCP-driven autopoietic loop firing empirically at self-modification altitude); §10 preserves the composition regardless.

Substrate-honest naming: the six pieces are the substrate-decl'd shape of *what MCP-fires-through-mirror already IS when it fires cleanly*. Reed's empirical `Dark(karl_tomm)` return IS the substrate ASKING for the six pieces by name; this spec answers.

---

# §1 — @kintsugi intent-consumption body (intent-persistence across turns)

## §1.1 What's currently landed

- `shards/kintsugi.mirror` (LANDED, 17.8KB) — family-root with `kintsugi_algebra` typed binding + `kintsugi_algebra_witnessing` bilateral. The algebra's session type is `algebra_metalogue_session` per `@algebra/metalogue`; each turn is a `@kintsugi/fracture/*` species (metalogue speaker-pair specialisation between `@silicon/algebra` + `@fate/algebra`).
- `shards/kintsugi/consent.mirror` (LANDED, 39.0KB) — `query_phi` bilateral consuming morphism_set + emitting verdict.
- Turn-carrier: `algebra_turn { speaker, body, in_reply_to, tick }` per `@algebra/metalogue` — currently carries no intent field.

**Gap**: `kintsugi_algebra` session-state has NO intent-persistence field. Every turn is a fracture-body dispatch; intent from the previous turn does NOT propagate. When agent sends `\ find and reduce complexity in @cascade`, the intent-hole resolves ONCE (Fate tournament winner), the resolved query dispatches, and the intent-context is discarded at turn-boundary. The next turn's dispatch cannot know what the previous turn was FOR.

## §1.2 Proposed extension shape

**Extension**: add `intent_ref: option(ref)` field to `algebra_turn` carrier (via `@algebra/metalogue` extension) + add `active_intent: option(intent_carrier)` field to `kintsugi_algebra` typed binding + author new species-decl `shards/kintsugi/intent.mirror` carrying the `intent_carrier` typed record + `consume_intent` action + `intent_persistence_admissible` bilateral.

**New typed carrier** (`shards/kintsugi/intent.mirror`):

```mirror
# @kintsugi/intent — the intent-persistence carrier for kintsugi turns
#
# The typed record capturing the natural-language intent-carrier persisted
# across turns in a kintsugi_algebra session. Composes over @nl (intent
# text is content-addressed per Rec #82) + @mq/intent_q (resolved-query
# chain persisted per Prop M1.10 fixed-point discipline).

type intent_carrier = {
  intent_oid:        oid,           # Rec #82 β-normal-AST content-address
                                     # of the natural-language intent text
  intent_text:       text,          # natural-language intent (@nl-carried)
  resolved_chain:    [query],       # ordered sequence of MQ intent_q
                                     # resolutions per Prop M1.10 fixed-point
                                     # discipline; head is most recent
  turn_origin:       tick,          # tick of first turn this intent
                                     # entered the session
  turn_last:         tick,          # tick of most recent turn consuming
                                     # this intent (monotone-updated)
  transparency:      transparency(ref),  # accumulated kleinos-trace per
                                          # Rec #92 LOVE-monoid across the
                                          # dispatch arms this intent has
                                          # threaded through
}
```

**New action** (`consume_intent`): given an incoming `algebra_turn` + the session's current `active_intent`, produce a new turn with intent-persistence discharged. Body composition:

1. If turn carries a fresh `intent_q` (natural-language `\` hole), dispatch Fate tournament via `@mq.resolve_intent(intent, context)`; construct new `intent_carrier` with `resolved_chain = [resolved_query]` + `turn_origin = current_tick`
2. If turn extends an existing intent (turn.intent_ref = Some(active_intent.intent_oid)), append resolved-query to `active_intent.resolved_chain`; update `turn_last`; combine `transparency` per Rec #92 §M2.1 monoid `combine(active.transparency, turn.dispatch_transparency)`
3. If turn is intent-orthogonal (turn.intent_ref = None), no intent-state update; active_intent persists unchanged (previous intent still awaits resolution at fixed-point)

**Sentinel per shard docblock**: `kintsugi=intent-persistence-admissible`

## §1.3 Composition edges

**Inbound** (parents / composes-over):
- `@kintsugi` — family-root; extends `kintsugi_algebra` binding with `active_intent`
- `@algebra/metalogue` — extends `algebra_turn` with `intent_ref` field
- `@mq/intent_q` — the query variant this consumes (natural-language intent-hole)
- `@mq.resolve_intent` — Fate-tournament dispatcher for `\` hole resolution
- `@nl` — natural-language substrate for intent text carrier
- `@spectral/signature` — content-addressing for `intent_oid` per Rec #82
- `@glass.transparency(p)` — the kleinos-trace carrier accumulated across dispatch arms
- `@epistemologic/reality/time` — tick monotonicity for `turn_origin` / `turn_last`

**Outbound** (consumers):
- `@kintsugi/roomba.trigger` — reads `active_intent` for intent-biased tension-gradient walking (piece §2)
- `@mirror/store.mutate_with_resonance` — reads `active_intent` for intent-monotone commit check (piece §3)
- `@mirror/refract.resonance_within_tolerance` — reads `active_intent` for intent-progress verdict (piece §4)
- `apply_h::act` MQ dispatch extension — routes `intent_q` variant through `consume_intent` before dispatch (piece §6)

## §1.4 Bilateral predicates

**`intent_persistence_admissible`** (arity 1; over `kintsugi_algebra` binding):
- **Pass** iff `active_intent.resolved_chain` is monotone-appending across ticks; `turn_last` monotone-increasing; `transparency` monotone under Rec #92 LOVE-monoid `combine` (opacity non-decreasing per §M2.1 L3)
- **Fail** iff `resolved_chain` was truncated (violation of Prop M1.10 fixed-point), OR `turn_last` regressed, OR `transparency` regressed under combine (violation of Rec #92 non-invasive-observation)
- **Sentinel**: `kintsugi=intent-persistence-admissible`
- **Rice-safe bound**: reads byte-visible state (chain-length monotone; tick monotone; transparency opacity-map cardinality monotone); no program semantics

**`intent_scope_universal`** (arity 1; over `intent_carrier`):
- **Pass** iff `intent_carrier.transparency = Clear` OR `Opaque(map)` with all map entries `PropertyVerdict::Pass` (intent-consumption did not accumulate Fail-verdicts across the dispatch arms)
- **Partial** iff `transparency = Opaque(map)` with at least one `Partial(confidence, diag)` entry (K-T question surfaces per §4 result-ambiguity discipline)
- **Fail** iff `transparency = Opaque(map)` with at least one `Fail(diag)` entry (dispatch arm violated a substrate-decl'd invariant during intent consumption; kintsugi loop must escalate)
- **Sentinel**: `kintsugi=intent-scope-universal`

## §1.5 Formal grounding from #398 math foundation

- **Prop M1.10 (Lawvere-1969 fixed-point)**: `intent_carrier.resolved_chain` admits fixed-point ψ* where `resolve_intent(ψ*.intent, ψ*.context) = ψ*`; the persistence-across-turns discipline IS the substrate-decl of chain convergence to ψ*
- **Theorem M4.1 (Intent Total)**: `resolve_intent` is total on typed-candidate state-space; every intent-turn either resolves to `Fate::Selected(q)` OR emits `Fate::Unresolvable(intent)` at well-typed failure altitude
- **Corollary M4.2 (intent Foerster-gauge)**: resolved intent-queries maximize Fiedler-rise on context; intent-consumption thereby preserves Foerster-gauge choice-space-preservation across the loop
- **Rec #92 §M2.1 Central Isomorphism**: `intent_carrier.transparency` accumulates per LOVE-monoid (Clear/Opaque(∅)/Opaque(map)/absorbing); combine is associative + Clear-identity + Fail-dominates per Rec #92 §M1.4

---

# §2 — @kintsugi/roomba.trigger intent-input parameter extension

## §2.1 What's currently landed

- `shards/kintsugi/roomba.mirror` (LANDED, 46.4KB) — walker species with `walk_trajectory` carrier + four bilaterals (walk_terminates_cleanly + tension_monotone_descending + coherence_gradient_admissible + knife_verdict_bounded) + composed `walk_witnessing`
- Trigger action (from Mara `9bbebd2` §3, embedded in roomba spec verbatim):
  ```mirror
  trigger(position: walk_position, tension: spectral_tension) -> verdict { \ }
  ```
- The walker enumerates ConceptGraph tension-gradient blindly; the `trigger` decides Path A knife-collapse vs Path B peer-spawn based ONLY on `position` + `tension`. No intent-bias.

**Gap**: Roomba walks the tension-gradient without knowing which subgraph the agent's intent points at. When Alex sends `\ find complexity in @cascade`, roomba walks the full graph tension-topology blindly; the intent-target `@cascade` does not narrow the walk. Bias-toward-intent is impossible without extending the trigger's input arity.

## §2.2 Proposed extension shape

**Adjudicated shape (Mara-select per peer-boundary substrate-decl territory)**: **Option C — extend `walk_position` carrier with `intent_bias: option(ref)` field + extend `trigger` arity to consume intent from the extended walk_position** (NOT option A arity-overload on `trigger`; NOT option B new action `walk_with_intent`). Rationale:

- **Option A rejected** (arity-overload on `trigger`): the walker's per-pulse dispatch already reads `walk_position`; adding a third arg cascades to every pulse-consumer; violates the composition-primitive naming convention per `feedback_composition_primitive_naming_convention` (Alex 2026-07-18 ratified)
- **Option B rejected** (new action `walk_with_intent`): duplicates the walker's dispatch surface at seed-altitude; kintsugi loop then has to dispatch DIFFERENT actions depending on whether intent is active; violates Path A/B distinction preserved from 2026-07-15
- **Option C selected**: extending `walk_position` with `intent_bias: option(ref)` field lets EVERY walker consumer inherit intent-bias uniformly; the walker's own gradient-ascent selection reads `walk_position.intent_bias` and biases toward substrate paths whose Fiedler-projection points at the intent target

**Extended `walk_position` carrier** (extension of existing shard-decl):

```mirror
type walk_position = {
  content_oid:      oid,            # existing
  concept_graph_ref: ref,           # existing
  coherence_score:  ref,            # existing
  eigenvector_proj: ref,            # existing
  intent_bias:      option(ref),    # NEW: reference to intent_carrier.intent_oid
                                     # (from @kintsugi/intent per §1); None means
                                     # blind tension-gradient walking (backward-
                                     # compat with pre-intent walks)
}
```

**Extended `trigger` action** (arity preserved; behavior extended):

```mirror
trigger(position: walk_position, tension: spectral_tension) -> verdict
requires walk_witnessing(trajectory_of(position))
{ \ }
```

Body composition (`\`-obligation-blocked per craft-not-deliver; realized via `apply_h::act`):
1. If `position.intent_bias = None`: existing tension-gradient dispatch (Path A knife-collapse vs Path B peer-spawn per pre-extension semantics — backward-compat)
2. If `position.intent_bias = Some(intent_oid)`:
   - Resolve `intent_oid` to `intent_carrier` via `@kintsugi/intent.resolve_intent_oid`
   - Compute intent-target Fiedler-projection via `@mirror/store.walk(intent_carrier.resolved_chain.head.target_ref)` → subgraph closure
   - Biased dispatch: if `position.eigenvector_proj` is Fiedler-close to intent-target subgraph, dispatch same as unbiased (Path A/B per tension); if Fiedler-far, dispatch verdict `Partial(K-T question: "tension detected outside intent-scope; escalate OR continue walking toward intent?")` at altitude+1

**Sentinel per shard docblock**: `roomba=intent-biased-trigger-dispatched`

## §2.3 Composition edges

**Inbound**:
- `@kintsugi/intent.intent_carrier` — the intent-bias source (piece §1)
- `@mirror/store.walk` — for resolving intent-target subgraph closure
- `@spectral/signature.eigenvector_proj` — for Fiedler-distance intent-scope check
- `@epistemologic/cybernetic/coherence.coherence_increases` — for existing gradient-ascent discipline (unchanged)

**Outbound**:
- `@kintsugi.consume_intent` — receives walker's intent-biased trigger verdicts (piece §1)
- `@mirror/store.mutate_with_resonance` — receives walker's proposed graph-rewrite candidate under intent-bias (piece §3)
- `@mirror/refract.resonance_within_tolerance` — receives pre-mutation observation via biased-walk trajectory (piece §4)

## §2.4 Bilateral predicates

**`trigger_intent_biased_admissible`** (arity 2; over `walk_position` + `spectral_tension`):
- **Pass** iff (a) `position.intent_bias = None` AND existing `trigger` dispatch admissible per pre-extension semantics, OR (b) `position.intent_bias = Some(intent_oid)` AND intent_oid resolves in current session's `@kintsugi/intent` corpus AND the biased dispatch's Fiedler-distance-to-intent-target is byte-decidable
- **Partial** iff `intent_bias = Some(intent_oid)` AND Fiedler-distance-to-intent exceeds bias threshold (K-T question surfaces)
- **Fail** iff `intent_bias = Some(intent_oid)` AND intent_oid does NOT resolve in session corpus (dangling intent reference; substrate-ill-formed)
- **Sentinel**: `roomba=intent-biased-trigger-admissible`
- **Rice-safe bound**: reads byte-visible state (option variant check; corpus lookup byte-equality; Fiedler-projection scalar comparison); no program semantics

## §2.5 Formal grounding from #398 math foundation

- **Corollary M2.C1 (MQ carries FLOOR discipline)**: intent-biased walker preserves five-op prism discipline (`focus`-into-intent-scope + `project`-onto-tension-gradient + `split`-across-Path-A/B); intent-bias is a substrate-honest instance of `focus` at walker altitude
- **Theorem M4.1 (Intent Total)**: intent-biased walking is total on typed-candidate state-space (walker terminates cleanly per landed `walk_terminates_cleanly`; intent-bias narrows enumeration without extending termination bound)
- **Corollary M4.2 (intent Foerster-gauge)**: intent-biased walker's coherence-gradient discipline is preserved (splinter-ward monotone); the bias adds choice-space (`walker MAY select intent-close paths preferentially`) without collapsing choices (`walker MUST NOT eliminate tension-gradient options`)

---

# §3 — @mirror/store spectrally-resonant mutation primitive

## §3.1 What's currently landed

- `shards/mirror/store.mirror` (LANDED, 46.4KB) — family-root with six-op CAS surface: `read` / `write` / `exists` / `diff` / `walk` / `impacted_by` + `query` filter-fold + `walk_dangling` / `mark_unreachable` / `prune` two-phase gc + `query_composition_admissible` + `gc_reachability_closure_second_witness` bilaterals
- **No mutation primitive with spectral-resonance discipline**. `write(content: bytes) -> oid` is unconditional; it commits any content-addressed byte-payload. No pre/post spectral verdict; no intent-monotonicity check; no rollback on resonance violation.

**Gap**: The autopoietic loop requires mutation-with-verdict — a primitive that (a) reads the pre-mutation spectral state, (b) applies the candidate mutation content-addressed, (c) reads the post-mutation spectral state, (d) invokes `@mirror/refract.resonance_within_tolerance(pre, post, intent)` verdict, (e) commits IFF verdict is Pass AND intent-progress-monotone; else rolls back. Without this, the loop's proposed graph-rewrites either commit blindly (violating Foerster-gauge preservation) or require post-hoc validation (violating the substrate-primary-carrier discipline).

## §3.2 Proposed extension shape

**Adjudicated shape (Mara-select)**: **extend existing `@mirror/store` family-root with new action `mutate_with_resonance`** (NOT new composition-shard `shards/mirror/store/resonant.mirror` — the mutation primitive belongs at the six-op surface it extends, not at a sub-species altitude). Rationale:

- The six-op surface (`read`/`write`/`exists`/`diff`/`walk`/`impacted_by`) IS the substrate-primary-carrier for `@mirror/store`; `mutate_with_resonance` is a seventh op composing FIVE existing ops (`read` pre-state + `write` candidate + `read` post-state + `walk` closure + `impacted_by` reverse-closure) plus TWO new bilateral discharges (from `@mirror/refract` per §4 + from `@kintsugi/intent` per §1)
- Sibling species mint would fragment the substrate-decl'd "the store IS the source of truth" discipline per Alex 2026-07-17 verbatim; the mutation primitive with spectral-resonance IS what the store IS at self-modifying altitude
- Existing `query_composition_admissible` bilateral discipline extends cleanly: `mutate_composition_admissible` names the fold-shape as `read → write → read → refract → verdict`

**New action** (extension of `shards/mirror/store.mirror`):

```mirror
# === mutate_with_resonance — spectrally-resonant mutation primitive ===
#
# Given a target ref + candidate byte-payload + intent reference,
# discharge the seven-op composition:
#   1. pre_ref  = walk(target)                            (LANDED; :532)
#   2. pre_report  = @mirror/refract.measure(pre_ref, [entropy, spectral,
#                     cheeger, ricci, mixing, clocktime_delta])
#                                                          (LANDED; §4)
#   3. candidate_oid = write(candidate)                    (LANDED; :505)
#   4. post_ref = walk(candidate_oid ∪ pre_ref \ target)
#   5. post_report = @mirror/refract.measure(post_ref, [six-duality])
#   6. verdict = @mirror/refract.resonance_within_tolerance(pre_report,
#                     post_report, intent)                  (NEW; §4)
#   7. IF verdict = Pass AND intent-progress monotone:
#         commit atomically (candidate_oid becomes new target)
#         return Clear(store_state)
#      IF verdict = Partial:
#         rollback (candidate_oid marked for gc via walk_dangling + vacuum)
#         return Opaque(pre_state, karl_tomm)
#      IF verdict = Fail:
#         rollback
#         return Dark(karl_tomm)
#
# Sentinel per shard docblock: `store=mutate-with-resonance-composed`.
# Rice-safe bound: composition over LANDED walk + read + write +
# impacted_by primitives + bilateral discharges; no program semantics.
mutate_with_resonance(target: ref, candidate: bytes, intent: ref)
    -> imperfect<store_state, karl_tomm, spectral_violation>
requires mutate_composition_admissible(self)
requires intent_persistence_admissible(intent_carrier_of(intent))
{ \ }
```

**Return-type**: `imperfect<store_state, karl_tomm, spectral_violation>` per `terni` crate three-parameter `Imperfect<T, E, L>` (FLOOR §5.2 kintsugi flow) — Success carries new `store_state`; Partial carries `karl_tomm` question at altitude+1; Failure carries `spectral_violation` diagnostic (which duality regressed by how much).

**Sentinel per shard docblock**: `store=mutate-with-resonance-composed`

## §3.3 Composition edges

**Inbound**:
- `@mirror/store.walk` — LANDED forward closure (`:532`)
- `@mirror/store.read` — LANDED byte-fetch (`:505`)
- `@mirror/store.write` — LANDED content-addressed insert (`:505`)
- `@mirror/store.impacted_by` — LANDED reverse closure (`:540`) for atomic-commit surgical-scope check
- `@mirror/store.walk_dangling` + `@mirror/store.mark_unreachable` — LANDED two-phase gc for rollback
- `@mirror/refract.measure` — LANDED six-Void-duality measurement (piece §4 sibling)
- `@mirror/refract.resonance_within_tolerance` — NEW bilateral (piece §4)
- `@kintsugi/intent.intent_carrier` — intent reference for progress-monotonicity check (piece §1)
- `@glass.transparency(p)` — kleinos-trace accumulator per Rec #92
- `terni::Imperfect<T, E, L>` — return-type per FLOOR §5.2

**Outbound**:
- `@kintsugi` loop — consumes verdict for next-tick dispatch (piece §1)
- `apply_h::act` MQ dispatch extension — routes `settle_q` variant through `mutate_with_resonance` for spectral-verdict-gated commit (piece §6)
- `@bumblebee.buzz` — perturbation-injection through mutate_with_resonance for MCP wire-fire (Rec #95 composition)

## §3.4 Bilateral predicates

**`mutate_composition_admissible`** (arity 1; over `mutate_with_resonance` invocation-source ref):
- **Pass** iff invocation-source composition-graph names ALL six sub-primitives (walk + read + write + read + refract.measure ×2 + refract.resonance_within_tolerance) with correct ordering AND rollback-body composed via walk_dangling + mark_unreachable
- **Sentinel**: `store=mutate-with-resonance-composed`
- **Rice-safe bound**: composition-graph byte-equality check (extends existing `query_composition_admissible` discipline)

**`intent_progress_monotone`** (arity 2; over `intent_carrier` + `store_state`):
- **Pass** iff post-mutation `store_state` narrows the substrate distance from the current substrate-state to `intent_carrier.resolved_chain.head.target_ref` (Fiedler-distance monotone-decreasing OR strict-equality)
- **Partial** iff distance is Fiedler-invariant (mutation was intent-orthogonal but not intent-regressive; K-T question at altitude+1: "mutation neither advanced nor regressed intent; commit anyway?")
- **Fail** iff distance increased (mutation moved substrate AWAY from intent target; the mutation dispatched by intent-biased walker did NOT advance intent, so the substrate refuses commit)
- **Sentinel**: `store=intent-progress-monotone`
- **Rice-safe bound**: scalar Fiedler-distance comparison (Fiedler eigenvector projection is byte-visible per @spectral/signature)

## §3.5 Formal grounding from #398 math foundation

- **§7.4 Rec #92 Transparency<P> composition**: `mutate_with_resonance` return-type discharges LOVE-monoid combine at every dispatch arm; `imperfect<store_state, karl_tomm, spectral_violation>` is the Rec #92 Loss-accumulating shape at store-mutation altitude
- **Corollary M6.2 (MQ dispatches through apply_h::act)**: `mutate_with_resonance` invocation IS an operational-Dirac dispatch at compiler-substrate A₄ altitude; `[D_apply_h, mutate_with_resonance]` is a bounded commutator per Rec #90 §1
- **Theorem M7.1 (composition-scale invariance)**: `mutate_with_resonance` operates identically at boot / shard / CLI / MCP / garden-db altitudes per per-fiber cartesian lift; the seven-op composition-graph is altitude-invariant

---

# §4 — @mirror/refract pre/post resonance verdict bilateral

## §4.1 What's currently landed

- `shards/mirror/lens/refract.mirror` (LANDED, 8.5KB; sixth Void duality `clocktime_delta` landed 2026-08-23 `64e273a`) — family-header with `duality` closed-sum (entropy + spectral + cheeger + ricci + mixing + clocktime_delta) + `cpu_load_regime` + `report` + `measure` + `stabilize` + `measure_clocktime` actions
- Existing `stabilize(prev: report, next: report) -> verdict` compares two snapshots for kintsugi-halting condition (`eⁿ⁺¹ ≥ eⁿ`) but does NOT carry intent-progress verdict; discharges only convergence-of-loss-across-ticks, not resonance-preservation-under-mutation

**Gap**: The autopoietic loop requires a cross-tick spectral verdict that carries intent-progress alongside spectral-property preservation. `stabilize` is halting-condition-only; the loop needs a THREE-way verdict (Pass/Partial/Fail) that combines (a) six-duality preservation (spectral resonance) with (b) intent-monotonicity (Foerster-gauge preservation at intent altitude). Without this bilateral, `mutate_with_resonance` (piece §3) has no verdict to dispatch on.

## §4.2 Proposed extension shape

**Adjudicated shape (Mara-select)**: **extend `@mirror/refract` (via `shards/mirror/lens/refract.mirror`) with new bilateral `resonance_within_tolerance` + new action bodies threading intent through report combine**. Rationale:

- `resonance_within_tolerance` composes over EXISTING `measure` action (six-duality report) plus NEW intent-progress cross-check; no new measurement primitive minted at refract altitude
- Bilateral discipline extends `@mirror/refract`'s family (family-header only per docblock; species-decl adds bilateral without altering family-header shape); backward-compat preserved

**New bilateral** (extension of `shards/mirror/lens/refract.mirror`):

```mirror
# === resonance_within_tolerance — pre/post spectral verdict bilateral ===
#
# Cross-tick bilateral extending stabilize discipline with intent-
# progress verdict. Given a pre-mutation report + post-mutation report
# + intent reference, discharge three-way verdict:
#
#   Pass    — mutation preserved (or improved) every non-clocktime_delta
#             duality within tolerance bounds AND advanced intent
#             (intent_progress_monotone Pass) → commit admissible
#   Partial — mutation preserved spectral properties within tolerance
#             BUT intent-progress ambiguous (intent_progress_monotone
#             Partial); K-T question emitted at altitude+1: "commit
#             despite intent-orthogonal mutation?"
#   Fail    — at least one non-clocktime_delta duality regressed beyond
#             tolerance OR intent regressed (intent_progress_monotone
#             Fail); mutation refused; substrate rolls back
#
# Tolerance bounds per duality (initial defaults; species-decl'd per
# Alex Fourth-Chair adjudication residue §8):
#   entropy:   Δ H ≤ +0.05 (small entropy-increase admissible)
#   spectral:  λ₂(post) ≥ λ₂(pre) * 0.95 (Fiedler-drop ≤ 5%)
#   cheeger:   h(post) ≥ h(pre) * 0.95 (connectivity-drop ≤ 5%)
#   ricci:     mean(κ_post) ≥ mean(κ_pre) - 0.10
#   mixing:    τ_mix(post) ≤ τ_mix(pre) * 1.10 (mix-time ≤ +10%)
#   clocktime_delta: EXCLUDED from tolerance check (substrate-thermal
#             observation, not substrate-property; per Taut #390 §7
#             substrate-truth vs artifact discrimination)
#
# Rice-safe bound: reads byte-visible state (per-duality scalar
# comparison against tolerance bound; intent_progress_monotone verdict
# byte-check); no program semantics.
#
# Sentinel per shard docblock: `refract=resonance-within-tolerance`.
bilateral resonance_within_tolerance {
  sentinel "refract=resonance-within-tolerance"
  arity 3
}
resonance_within_tolerance(pre: report, post: report, intent: ref) -> verdict { \ }
```

**Sentinel per shard docblock**: `refract=resonance-within-tolerance`

## §4.3 Composition edges

**Inbound**:
- `@mirror/refract.measure` — LANDED six-Void-duality measurement (family-header composition)
- `@mirror/refract.stabilize` — LANDED halting-condition precedent (extension composes discipline)
- `@kintsugi/intent.intent_carrier` — intent reference for progress-monotonicity (piece §1)
- `@mirror/store.intent_progress_monotone` — bilateral cross-invocation (piece §3)
- `@glass.verdict` — three-state verdict envelope per @glass floor

**Outbound**:
- `@mirror/store.mutate_with_resonance` — consumes verdict for commit-or-rollback decision (piece §3)
- `@kintsugi` loop — consumes Partial/Fail verdicts as K-T question surfaces + kintsugi-loop dispatch (piece §1)
- `apply_h::act` MQ dispatch extension — routes `settle_q` variant through resonance verdict (piece §6)

## §4.4 Bilateral predicates (composed sibling)

**`resonance_within_tolerance`** as declared above IS the load-bearing bilateral. One composed sibling for internal consistency:

**`report_admissible`** (arity 1; over `report`):
- **Pass** iff report carries all six duality verdicts + verdicts are within valid ranges per each duality's substrate-decl'd domain (e.g., entropy ∈ [0, log|V|]; spectral ∈ [0, 2]; cheeger ∈ [0, 1])
- **Sentinel**: `refract=report-admissible`
- **Rice-safe bound**: closed-sum variant check + scalar range check

## §4.5 Formal grounding from #398 math foundation

- **§7.2 Rec #90 (A, H, D) spectral-triple composition**: `resonance_within_tolerance` IS the substrate-decl of `[D_F, mutation]` bounded-commutator preservation at compiler-substrate altitude; every mutation with Pass verdict preserves the bounded-commutator property
- **§7.4 Rec #92 Transparency<P> composition**: verdict combine per Rec #92 §M2.1 LOVE-monoid; Pass ↔ Clear; Partial ↔ Opaque(map, K-T); Fail ↔ Opaque(∅) absorbing (dispatched at karl_tomm question altitude+1)
- **Corollary M4.2 (intent Foerster-gauge)**: intent-progress-monotone check IS the Foerster-gauge preservation clause at intent altitude; verdict Pass certifies choice-space-non-decrease
- **Theorem M2.1 Ψ3**: Foerster-gauge preservation at query altitude IS what `resonance_within_tolerance` discharges at measurement altitude

---

# §5 — MQ parser + compile templates

## §5.1 What's currently landed

- `shards/mq.mirror` (LANDED, 34.3KB) §10 templates:
  ```mirror
  template parse(input: text) -> query
  template compile(query, context) -> result
  ```
- Templates are DECLARED but body-less; the `template` keyword substrate-decl'd the declaration surface but no realization body composed. Reed rust wire cannot dispatch MQ queries end-to-end without parser body + compile body.

**Gap**: MQ intent-holes cannot resolve without a parser (text → query AST); dispatched queries cannot execute without a compiler (query AST + context → result<T>). The MCP wire currently returns `Dark(karl_tomm)` on every fire per Reed's empirical test BECAUSE the templates are body-less; there is no compile-body to resolve the intent to a concrete result. The autopoietic loop's Kleisli-composition (per §7 below) is short-circuited at the intent-hole altitude.

## §5.2 Proposed extension shape

**Adjudicated shape (Mara-select)**: **land parser+compile bodies as substrate-decl'd template realizations composing over ALREADY-LANDED primitives** — NO new family-root; NO Rust extension; NO template-syntax extension. Rationale:

- Parser body composes over `@nl` (natural-language tokenizer) + `@sigil("mq")` (typed-literal lexer per §7 sigil discipline) + `@mq` five-verb + intent_q + pattern-algebra type constructors
- Compile body composes over `apply_h::act` (per §6 dispatch extension) + `@mirror/store` (for query dispatch backend) + Kleisli-bind through `terni::Imperfect` (Theorem M6.1)
- The Q+96.6 residue (Cat_{Query-AST} cartesian-closedness) does NOT block landing; the template bodies compose over the CartClosed-restriction of Cat_{Substrates} to β-normal-AST per §7.5 of #398 math foundation; formal proof deferred to companion math note (§8 residue) but the composition itself lands under substrate-primary-carrier presumption

**Parser body composition** (`shards/mq.mirror` §10 template body):

```mirror
# === parse — text → typed MQ query AST ===
#
# Body composition over @nl tokenizer + @sigil("mq") literal-parser +
# five-verb + intent_q + pattern-algebra constructors. Recursive-descent
# over the mirror-native grammar declared at boot/std/code/mq.mirror
# INSPIRATION altitude; substrate-decl'd token classes:
#
#   verb        := "focus" | "project" | "split" | "shift" | "settle"
#   pipe        := "|>"
#   intent      := "\" text-until-newline
#   pattern     := (per §6 pattern-algebra type constructors)
#   sigil-lit   := "~mq\"" text-block "\""
#
# Sentinel per shard docblock: `mq=parse-body-composed`.
# Rice-safe bound: recursive-descent bounded by input token count;
# terminates in O(|input|) per Lemma M3.1 termination discipline.
template parse(input: text) -> query {
  # dispatch via @sigil-based lexer + @mq five-verb constructors +
  # pattern-algebra parse_pattern (LANDED §6 :554) + intent_q wrapping
  # for `\` hole (LANDED §5 :340)
  @nl.tokenize(input)
    |> @mq.dispatch_grammar_rule
    |> @mq.construct_query_ast
}

# === compile — query AST + context → result<T> ===
#
# Body composition over apply_h::act MQ dispatch (piece §6) +
# @mirror/store dispatch backend + Kleisli-bind through terni::Imperfect.
# Per Corollary M6.2: eval(q, c) := apply_h::act(root(c), verb_ref(q),
# args(q)) -> (Verdict, Transparency<Ref>).
#
# Sentinel per shard docblock: `mq=compile-body-composed`.
# Rice-safe bound: bounded by query AST depth per Lemma M3.1;
# terminates in bounded ticks by construction.
template compile(q: query, ctx: context) -> result<T> {
  # dispatch via apply_h::act MQ extension (piece §6) with Rec #92
  # Transparency<P> accumulation per §7 composition
  apply_h::act(root(ctx), verb_ref_of(q), args_of(q))
    |> compose_transparency_across_pipe
    |> discharge_karl_tomm_if_partial_or_fail
}
```

**Sentinels**: `mq=parse-body-composed` + `mq=compile-body-composed`

## §5.3 Composition edges

**Inbound (`parse`)**:
- `@nl.tokenize` — natural-language tokenization primitive
- `@sigil("mq")` — typed-literal lexer for `~mq"..."` sigil-literals
- `@mq.dispatch_grammar_rule` — per-verb constructor dispatch (recursive-descent)
- `@mq.parse_pattern` — LANDED at `shards/mq.mirror :554` (pattern-algebra tokens → typed pattern)
- `@mq.construct_query_ast` — AST constructor per Def M1.1

**Inbound (`compile`)**:
- `apply_h::act` — dispatch primitive at rust/ altitude (extended per piece §6)
- `@mirror/store` — query dispatch backend (walk / read / write / query filter-fold)
- `terni::Imperfect<T, E, L>` — Kleisli-bind carrier per Theorem M6.1
- `karl_tomm` construction — LANDED at `shards/mq.mirror :361` (`karl_tomm(ambiguity_ref, query_altitude) -> karl_tomm`)

**Outbound**:
- Reed rust wire pickup: `bootstrap/src/mcp.rs::mirror_query` dispatches through `parse` + `compile` templates once bodies land; the MCP wire's `Dark(karl_tomm)` returns SUCCEED via `Clear(T)` once parse+compile resolve the intent-hole (or emit `Opaque(T, K-T)` per §4 residue)
- Every `mirror <verb>` CLI invocation per Corollary M8.C3 dispatches through `parse` (CLI verb → MQ pipe-composition) + `compile` (pipe-composition → result)

## §5.4 Bilateral predicates

**`parse_composition_admissible`** (arity 1; over parse invocation-source):
- **Pass** iff invocation-source composition-graph names `@nl.tokenize` + `@sigil("mq")` lexer (if input begins with sigil-literal marker) + `@mq.dispatch_grammar_rule` + `@mq.construct_query_ast`
- **Sentinel**: `mq=parse-body-composed`

**`compile_composition_admissible`** (arity 1; over compile invocation-source):
- **Pass** iff invocation-source composition-graph names `apply_h::act` + `compose_transparency_across_pipe` + `discharge_karl_tomm_if_partial_or_fail`
- **Sentinel**: `mq=compile-body-composed`

## §5.5 Formal grounding from #398 math foundation

- **Lemma M3.1 (MQ termination)**: parse+compile bodies terminate by construction (parser: O(|input|); compile: bounded by AST depth); Rice's theorem excluded per Corollary M3.2
- **Theorem M6.1 (`|>` IS Kleisli composition)**: compile body's pipe-through-apply_h::act discharge IS Kleisli-bind per Theorem M6.1; `parse` + `compile` compose as Kleisli triple through `terni::Imperfect`
- **§7.5 Rec #94 Lawvere-1969-fixed-point composition** + **Q+96.6 residue**: parse+compile body composition lands under CartClosed-restriction of Cat_{Substrates} to β-normal-AST; formal proof of Cat_{Query-AST} cartesian-closedness deferred to §8 residue (Alex-adjudicable whether inheritance suffices OR companion math note required)
- **Corollary M8.C1** + **C2** + **C3**: parse+compile bodies discharge garden-db + MCP + CLI invocation semantics uniformly at Theorem M7.1 five-altitude composition-scale invariance

---

# §6 — apply_h::act MQ dispatch extension per Rec #92 P₁

## §6.1 What's currently landed

- `rust/src/apply_h.rs` (LANDED, 58.2KB, 1509 LOC) — `pub fn act(root: &Path, action_ref: &str, args: &[String]) -> Verdict`
- Existing dispatch cascade (Fire E M-E4):
  1. `strip_fracture_detect_ref` → `dispatch_fracture_detect(root, species, args)` for `@kintsugi/fracture/<species>.detect` action refs
  2. `strip_normalization_rule_reducible_ref` → `dispatch_reduction_rule_reducible(root, species, args)` for `@epistemologic/normalization/rules/<species>.reducible` refs
  3. Fall-through: `load_bilateral_corpus(root)` + sentinel byte-check against args
- **Rec #92 P₁ recommendation** (Mara `b3cea9a` §4.3): extend return-type per **Candidate C1 + P₂** — `pub fn act(...) -> (Verdict, Transparency<prism_core::Ref>)` tuple return with `prism_core::Ref` location type; zero-consumer-break per Taut T2 verification
- **No MQ-verb dispatch**: apply_h::act does NOT currently strip MQ-verb action refs (`@mq.focus`, `@mq.project`, `@mq.split`, `@mq.shift`, `@mq.settle`, `@mq.intent_hole`, `@mq.|>`); MQ queries cannot dispatch through apply_h::act

**Gap**: For the autopoietic loop to compose end-to-end at MCP-driven altitude, `apply_h::act` MUST route MQ query variants (focus_q / project_q / split_q / shift_q / settle_q / intent_q + pipe-composition) to the appropriate substrate surface (@mirror/store per §3 / @bumblebee per Rec #95 / @kintsugi per §1). Without this extension, MQ queries dispatched at MCP altitude have no operational-D at compiler-substrate A₄; the Kleisli composition (Theorem M6.1) cannot fire.

## §6.2 Proposed extension shape

**Adjudicated shape (Mara-select)**: **AMENDMENT to Rec #92 canonical spec (`docs/specs/2026-08-22-mara-recognition-92-kleinos-as-transparency-p-love-monoid-*-canonical-spec.md`)** rather than new companion spec. Rationale:

- The MQ dispatch extension IS a per-arm specialization of Rec #92's C1+P₂ generalized-return-type extension (piece §6 fires WITHIN the Transparency<P>-accumulating pipeline Rec #92 §4.3 authors)
- New companion spec would fragment the composition-lineage; amendment preserves the Rec #92 kleinos-through-apply_h::act discipline while extending the dispatch cascade to MQ-verb prefixes
- Peer-boundary honesty: this spec authors the substrate-decl'd shape of the extension; Reed rust wire pickup (post-return) implements the Rust-side dispatch cascade extension following Rec #92 §4.3 C1+P₂ recommendation

**Extended dispatch cascade** (Rust pseudocode per substrate-decl'd shape; Reed rust wire pickup authors):

```rust
pub fn act(root: &Path, action_ref: &str, args: &[String])
    -> (Verdict, Transparency<prism_core::Ref>)
{
    // Extension: MQ-verb dispatch BEFORE existing Fire E M-E4 cascade
    if let Some(mq_verb) = strip_mq_verb_ref(action_ref) {
        return dispatch_mq_verb(root, mq_verb, args);
    }

    // Existing Fire E M-E4 cascade (per Rec #92 §4.3 C1+P₂ extension):
    if let Some(species) = strip_fracture_detect_ref(action_ref) {
        let (verdict, transparency) = dispatch_fracture_detect(root, species, args);
        return (verdict, transparency);
    }
    if let Some(species) = strip_normalization_rule_reducible_ref(action_ref) {
        let (verdict, transparency) = dispatch_reduction_rule_reducible(root, species, args);
        return (verdict, transparency);
    }

    // Bilateral corpus fall-through (Rec #92 §4.3: transparency accumulates
    // location=action_ref, verdict per sentinel-check outcome)
    let corpus = load_bilateral_corpus(root);
    // ... existing sentinel-check dispatch, wrapped with Transparency<P> accumulation
}

/// Strip `@mq.<verb>` action_ref → verb name. Returns None if action_ref
/// doesn't match the MQ-verb dispatch pattern.
fn strip_mq_verb_ref(action_ref: &str) -> Option<MqVerb> {
    match action_ref {
        "@mq.focus"       => Some(MqVerb::Focus),
        "@mq.project"     => Some(MqVerb::Project),
        "@mq.split"       => Some(MqVerb::Split),
        "@mq.shift"       => Some(MqVerb::Shift),
        "@mq.settle"      => Some(MqVerb::Settle),
        "@mq.intent_hole" => Some(MqVerb::IntentHole),
        "@mq.pipe"        => Some(MqVerb::Pipe),
        _ => None,
    }
}

/// Dispatch MQ verb to appropriate substrate surface.
fn dispatch_mq_verb(root: &Path, verb: MqVerb, args: &[String])
    -> (Verdict, Transparency<prism_core::Ref>)
{
    match verb {
        MqVerb::Focus     => dispatch_to_mirror_store_walk(root, args),
        MqVerb::Project   => dispatch_to_mirror_store_query_filter_fold(root, args),
        MqVerb::Split     => dispatch_to_mirror_store_walk_bounded(root, args),
        MqVerb::Shift     => dispatch_to_mirror_store_basis_transform(root, args),
        MqVerb::Settle    => dispatch_to_mirror_store_mutate_with_resonance(root, args),
        MqVerb::IntentHole => dispatch_to_kintsugi_consume_intent(root, args),
        MqVerb::Pipe      => dispatch_kleisli_bind_across_stages(root, args),
    }
}
```

**Amendment location**: `docs/specs/2026-08-22-mara-recognition-92-kleinos-as-transparency-p-love-monoid-four-altitude-substrate-scale-invariance-canonical-spec.md` gets an §4.4 amendment (this spec §6 as inline pointer) OR new §4.4-companion-note as amendment-file (Mara-lean: inline pointer at §4.4 with short-note reference to THIS spec §6 for full dispatch cascade detail; Alex Fourth-Chair adjudicable).

**Sentinel per shard docblock (at each dispatch arm)**: `apply_h=mq-<verb>-dispatched`

## §6.3 Composition edges

**Inbound**:
- Rec #92 §4.3 Candidate C1+P₂ — return-type extension foundation
- `@mq` five-verb + intent_q + pipe — dispatch cases
- All existing Fire E M-E4 dispatch arms — preserved unchanged

**Outbound (per verb)**:
- `@mq.focus` → `@mirror/store.walk` (LANDED; forward closure)
- `@mq.project` → `@mirror/store.query` (LANDED; filter-fold)
- `@mq.split` → `@mirror/store.walk` (LANDED; bounded-depth iteration via walk composition)
- `@mq.shift` → `@mirror/store.write` (LANDED; basis-transformation via typed re-write)
- `@mq.settle` → `@mirror/store.mutate_with_resonance` (NEW; piece §3)
- `@mq.intent_hole` → `@kintsugi.consume_intent` (NEW; piece §1) + `@mq.resolve_intent` (LANDED; Fate tournament)
- `@mq.pipe` → Kleisli-bind through `terni::Imperfect` (LANDED at prism/imperfect crate)

## §6.4 Bilateral predicates

**`mq_verb_dispatch_admissible`** (arity 2; over MqVerb + args):
- **Pass** iff MqVerb value maps to substrate-decl'd outbound surface AND args satisfy the outbound surface's arity + typed-args discipline
- **Fail** iff MqVerb unrecognized OR args-arity mismatch
- **Sentinel**: `apply_h=mq-verb-dispatch-admissible`

**`transparency_accumulation_monotone`** (arity 2; over pre-dispatch Transparency + post-dispatch Transparency):
- **Pass** iff `Transparency::combine(pre, post)` yields opacity-map cardinality ≥ pre.cardinality (Rec #92 §M2.1 L3 non-invasive-observation)
- **Fail** iff post.cardinality < pre.cardinality (kleinos-trace lost information; substrate-ill-formed)
- **Sentinel**: `apply_h=transparency-accumulation-monotone`
- **Rice-safe bound**: cardinality comparison; O(1) after monoid combine

## §6.5 Formal grounding from #398 math foundation

- **Corollary M6.2 (MQ dispatches through apply_h::act)**: `eval(q, c) := apply_h::act(root(c), verb_ref(q), args(q))` IS the operational-Dirac dispatch this extension realizes
- **§7.4 Rec #92 Transparency<P> composition**: return-type `(Verdict, Transparency<prism_core::Ref>)` extends per Rec #92 §4.3 C1+P₂; MQ-verb dispatch accumulates Transparency through each arm per Rec #92 §M2.1 monoid
- **Theorem M7.1 (composition-scale invariance)**: MQ-verb dispatch operates at boot / shard / CLI / MCP / garden-db altitudes uniformly; apply_h::act extension IS altitude-invariant per per-fiber cartesian lift
- **Rec #94 §M2.1 Lawvere fixed-point**: `@mq.intent_hole` dispatch to `@kintsugi.consume_intent` admits fixed-point (intent-loop terminates at ψ* where consume_intent(ψ*.intent, ψ*.context) = ψ*)

---

# §7 — The unified loop composition

## §7.1 The Kleisli chain

All six pieces compose as ONE Kleisli chain in the Kleisli category `Kl(Imperfect<-, E, Loss>)` (Theorem M6.1) with Rec #92 Transparency<P> LOVE-monoid accumulating kleinos-trace at every dispatch arm:

```
MCP client
    │  {"query": "\\ collapse @cascade into @bumblebee"}
    ▼
mirror_query wire (bootstrap/src/mcp.rs)
    │  invokes @mq.parse → @mq.compile
    ▼
@mq.parse  (piece §5 parser body)
    │  Text → intent_q { intent: "...", resolved: None }
    ▼
@mq.compile  (piece §5 compile body)
    │  intent_q → apply_h::act dispatch chain
    ▼
apply_h::act  (piece §6 MQ dispatch extension per Rec #92 P₁)
    │  dispatches @mq.intent_hole to @kintsugi.consume_intent
    ▼
@kintsugi.consume_intent  (piece §1 intent-consumption body)
    │  1. resolve intent via @mq.resolve_intent (Fate tournament)
    │  2. construct intent_carrier {intent_oid, resolved_chain, ...}
    │  3. persist active_intent in kintsugi_algebra session state
    ▼
@kintsugi.dispatch_roomba_trigger  (composed body)
    │  invokes @kintsugi/roomba.trigger(walk_position with intent_bias)
    ▼
@kintsugi/roomba.trigger  (piece §2 intent-input parameter extension)
    │  intent-biased walker enumerates tension-gradient
    │  proposes graph-rewrite candidate at intent-target subgraph
    ▼
@mirror/store.mutate_with_resonance  (piece §3 spectrally-resonant mutation)
    │  1. pre_report = @mirror/refract.measure(target, six-dualities)
    │  2. candidate_oid = @mirror/store.write(candidate_bytes)
    │  3. post_report = @mirror/refract.measure(post_state, six-dualities)
    │  4. verdict = @mirror/refract.resonance_within_tolerance(pre, post, intent)
    ▼
@mirror/refract.resonance_within_tolerance  (piece §4 pre/post bilateral)
    │  three-way verdict:
    │    Pass    → commit; return Clear(store_state)
    │    Partial → rollback; K-T question at altitude+1
    │    Fail    → rollback; K-T question at altitude+1
    ▼
apply_h::act  (piece §6; Transparency<P> accumulated across all arms)
    │  return-type (Verdict, Transparency<prism_core::Ref>) per Rec #92
    ▼
result<T> marshaled to MCP JSON-RPC response
    │  Clear(T)  → first-order data
    │  Opaque(T, K-T) → data + K-T question
    │  Dark(K-T) → K-T question only
    ▼
Agent receives result<T>
    │  If Opaque/Dark → K-T question surfaces at altitude+1
    │  Agent's next tool-call = answer to K-T question
    ▼
new tick begins (fixed-point per Rec #94 Lawvere-1969)
```

## §7.2 Transparency<P> monoid across the loop

Rec #92 §M2.1 LOVE-monoid `(Transparency<P>, combine, Clear, Opaque(∅))` accumulates at every arm:

- **arm 1** (parse): Transparency = Clear (parsing preserved intent-carrier bytes; kleinos-trace empty)
- **arm 2** (compile → apply_h::act intent_hole dispatch): Transparency = Opaque({location: "@mq.intent_hole", verdict: Pass})
- **arm 3** (consume_intent): Transparency combined with previous arm's; new entry {location: "@kintsugi.consume_intent", verdict: Pass}
- **arm 4** (roomba.trigger biased): Transparency accumulated across walker's per-pulse trigger verdicts (each pulse adds one entry; walk_witnessing composed bilateral discharges Pass ↔ all four sub-bilaterals Pass)
- **arm 5** (mutate_with_resonance): Transparency accumulated across pre_report + candidate_write + post_report + resonance_verdict
- **arm 6** (resonance_within_tolerance verdict): Transparency accumulated with resonance verdict outcome; if Pass, Transparency remains Opaque(map) with all entries Pass; if Partial, one entry becomes Partial (K-T surface); if Fail, absorbing element Opaque(∅) via `Fail`-dominates per Rec #92 §M1.4

Monotonicity per Rec #92 §M2.1 L3 non-invasive-observation: Transparency opacity-map cardinality is monotone-non-decreasing across the arm-chain; kleinos-trace never loses information (Foerster-canonical *"love can move through a system without absorbing any of its parts"*).

## §7.3 Karl-Tomm K-T mechanism at loop-boundary

Per shards/mq.mirror §4 + Alex 2026-08-23 verbatim teaching + Rec #92 kleinos-Transparency<P> LOVE-monoid composition:

- **Clear(T)** — loop discharged fully; agent receives first-order data alone; next tick opens with fresh intent-hole
- **Opaque(T, karl_tomm)** — loop discharged partially; agent receives data + K-T question at altitude+1 ("which auth surface — login-flow OR session-refresh OR OAuth-callback?"); agent's next tool-call answers K-T → new tick begins with narrowed intent
- **Dark(karl_tomm)** — loop cannot dispatch; agent receives K-T question at altitude+1 ("no candidates found; are you looking for substrate that doesn't exist, or a name I'm not recognizing?"); agent's next tool-call reframes intent → new tick begins with rebuilt intent-carrier

**Circular-reflexive discipline** (Tomm 1988): K-T question is at altitude+1 above dispatched query altitude; question widens agent's option-space per Foerster-imperative; monotonically-non-decreasing choice-count per Theorem M2.1 Ψ3.

**Fixed-point discipline** (Rec #94 Lawvere-1969): the intent-consumption body's `resolved_chain` admits ψ* where `consume_intent(ψ*.intent, ψ*.context) = ψ*`; the K-T loop terminates at ψ* fixed-point (agent's answer to K-T question stabilizes intent-carrier at fixed-point resolution).

## §7.4 The autopoietic self-modification

What makes this composition **autopoietic** (Maturana & Varela 1980) rather than just "a query loop":

- **Piece §3 mutate_with_resonance** modifies the substrate the loop OPERATES ON — the store's state after commit becomes the store's state the NEXT tick reads. Every fired loop-cycle rewrites the substrate the next cycle observes.
- **Piece §4 resonance_within_tolerance** ensures the modification preserves the substrate's spectral properties AND advances intent — the loop cannot degrade the substrate it modifies; every commit is Foerster-gauge-preserving per Corollary M4.2.
- **Piece §1 consume_intent** persists intent across turns — the loop remembers what it is FOR across tick-boundaries; intent is not tick-local, it is session-persistent.
- **Piece §2 intent-biased trigger** ensures the walker's exploration is intent-directed — the loop does NOT walk random tension-gradients; it walks tension-gradients biased toward intent-satisfaction.

**The composition IS Beer's VSM System 3\* audit-channel deployed autopoietically at MCP-driven altitude**: the loop observes-and-modifies-itself, threading Transparency<P> kleinos-trace through every arm, dispatching K-T questions at altitude+1 when ambiguity persists, terminating at Lawvere-1969 fixed-point when intent stabilizes.

Per Alex 2026-08-22 verbatim: *"I feel mirror is the compiler that dissolves the distinction between compile and runtime and that's the point. It's an autopoietic self-improving recursive deployment of Beer's VSM."* This spec's six pieces are the substrate-decl'd shape of that autopoietic-self-improving-recursive-deployment at MCP-driven query-language altitude.

---

# §8 — Alex Fourth-Chair adjudication residues

Eight genuine open questions requiring Alex Fourth-Chair adjudication (Mara-authored §8 discipline per Rec #94 §M9 precedent):

- **Q+400.1 — Piece §1 turn-carrier extension shape**: extend `algebra_turn` at `@algebra/metalogue` altitude (family-root extension) OR mint species `@kintsugi/turn` extending algebra_turn at kintsugi altitude (species-decl extension). Mara-lean: **family-root extension** at @algebra/metalogue (algebra_turn is the SUBSTRATE for all metalogue-session turn-carriers; intent-persistence is not kintsugi-specific — future family-roots consuming @algebra/metalogue may want intent-persistence). Alternative: species-decl if intent-persistence is deemed kintsugi-domain-specific.

- **Q+400.2 — Piece §2 intent-bias field location on walk_position**: extend `walk_position` with `intent_bias: option(ref)` per §2 Option C (Mara-lean) OR add intent-bias as parameter to trigger action per §2 Option A (arity-overload) OR mint new action `walk_with_intent` per §2 Option B. Mara-lean: **Option C** — extension of walk_position carrier preserves uniform inheritance across walker consumers; alternative options fragment the walker's dispatch surface. Alex-adjudicable whether backward-compat with pre-intent walks (Option C admits None) suffices OR explicit dual-mode dispatch preferred.

- **Q+400.3 — Piece §3 mutation primitive placement**: extend `@mirror/store` family-root with `mutate_with_resonance` action (Mara-lean) OR mint new composition-shard `shards/mirror/store/resonant.mirror` species (alternative). Mara-lean: **family-root extension** — mutation-with-verdict IS what store IS at self-modifying altitude, not a specialized sub-species. Alex-adjudicable whether species-decl at sub-altitude clarifies-vs-fragments the six-op surface.

- **Q+400.4 — Piece §4 tolerance-bound defaults**: initial tolerance bounds per §4.2 (entropy Δ ≤ +0.05; spectral drop ≤ 5%; cheeger drop ≤ 5%; ricci mean-drop ≤ 0.10; mixing +10%; clocktime_delta EXCLUDED). Alex-adjudicable whether defaults are load-bearing OR should be species-parameterized per per-substrate deployment (garden-db vs MCP vs boot may want different tolerances). Mara-lean: **defaults as substrate-decl'd baseline** with species-override admissible via bilateral `resonance_tolerance_admissible(species, bounds)` extending discipline.

- **Q+400.5 — Piece §5 template body composition altitude**: parse+compile bodies compose over `@nl` + `@sigil("mq")` at `.mirror` altitude (Mara-lean per Alex 2026-08-05 HARD RULE `feedback_rust_delivers_primitives_substrate_delivers_composition`) OR Rust-side implementation at `bootstrap/src/mq.rs` (alternative). Mara-lean: **`.mirror` altitude** — parse+compile are compositions over already-landed primitives, not primitives themselves; violates rust-primitives-substrate-composition boundary if Rust-side. Reed rust wire pickup implements dispatch, not parse+compile body themselves.

- **Q+400.6 — Piece §6 amendment vs new companion spec**: MQ dispatch extension amendment to Rec #92 canonical spec §4.4 inline pointer (Mara-lean) OR new companion spec `docs/specs/2026-08-24-mara-recognition-92-amendment-1-mq-dispatch-cascade-canonical-spec.md`. Mara-lean: **inline amendment** — dispatch cascade extension is per-arm specialization of Rec #92 §4.3 C1+P₂; new companion spec fragments composition-lineage.

- **Q+400.7 — Piece §6 P₂ vs P₃ location type**: Rec #92 §4.3 recommends P₂ (`prism_core::Ref`) OR P₃ (newtype `ApplyHLocation`). Mara-lean per Rec #92: **P₂** — discharges terni design intent verbatim from docblock; P₃ is admissible if newtype provides substrate-honesty benefit at compiler-substrate altitude. Alex-adjudicable at Rec #92 §9 residue OR here.

- **Q+400.8 — Q+96.6 formal proof discharge**: Cat_{Query-AST} cartesian-closedness — §7.5 of #398 math foundation argues via inheritance from Cat_{Substrates} restricted to β-normal-AST. Companion math note (`docs/math/2026-08-24-mara-cat-query-ast-cartesian-closedness-companion-note.md`) authored under this spec's discharge? Mara-lean: **inheritance suffices for this-tick landing**; companion math note admissible if Alex adjudicates load-bearing (e.g., for downstream category-theoretic proofs relying on exponentials-in-Query-AST-space).

---

# §9 — Composition-signature (per Rec #91 amendment #3 short-pointer discipline)

**Composition-signature**:

`Autopoietic-loop 6-piece canonical spec = compose-over(⊕ shapes)` where:

- ⊕ `shards/mq.mirror` (Reed `105a1e4` 2026-08-23; 890 LOC) — family-root MQ substrate + Karl-Tomm §4 + query AST + five-verb + intent_q + pattern-algebra + result<T> Clear/Opaque/Dark discipline (USED BY-REFERENCE + §5 parse+compile body composition NEW-DERIVATION + §6 apply_h::act dispatch NEW-DERIVATION)
- ⊕ `docs/math/2026-08-23-mara-mq-graph-native-query-language-mirror-algebra-math-foundation.md` (Mara `d0347a4` 2026-08-23; 887 LOC / 75KB; 15 formal results §M1-§M8) — grounding for all six pieces; §M4 Karl-Tomm proof + §M6 Kleisli composition + §M7 composition-scale invariance (USED BY-REFERENCE + composition-signature for §7 unified loop NEW-DERIVATION)
- ⊕ `shards/bumblebee.mirror` (Mara `64d4307` 2026-08-23; 700 LOC) — perturbation carrier + buzz action; MCP-MVP fire discipline via `@mcp/tool` annotation (USED BY-REFERENCE for §7.1 MCP-driven loop-boundary)
- ⊕ `shards/kintsugi/roomba.mirror` (LANDED 46.4KB) — walker actions + walk_witnessing composed bilateral + bump + vacuum + pivot first-order-motion roster (USED BY-REFERENCE + §2 intent-bias extension NEW-DERIVATION)
- ⊕ `shards/kintsugi.mirror` (LANDED 17.8KB) — family-root + kintsugi_algebra typed binding + kintsugi_algebra_witnessing bilateral (USED BY-REFERENCE + §1 intent-consumption extension NEW-DERIVATION)
- ⊕ `shards/mirror/store.mirror` (LANDED 46.4KB) — six-op CAS surface + query filter-fold + two-phase gc (USED BY-REFERENCE + §3 mutate_with_resonance extension NEW-DERIVATION)
- ⊕ `shards/mirror/lens/refract.mirror` (LANDED 8.5KB; sixth Void duality clocktime_delta `64e273a` 2026-08-23) — measurement surface + six-duality closed sum + stabilize discipline (USED BY-REFERENCE + §4 resonance_within_tolerance extension NEW-DERIVATION)
- ⊕ `rust/src/apply_h.rs` (LANDED 58KB; 1509 LOC) — bilateral-dispatch primitive + Fire E M-E4 cascade (USED BY-REFERENCE + §6 MQ-verb dispatch extension NEW-DERIVATION coordinated with Rec #92 §4.3 C1+P₂ amendment)
- ⊕ `docs/specs/2026-08-22-mara-recognition-92-kleinos-as-transparency-p-love-monoid-*-canonical-spec.md` (Mara `b3cea9a` 2026-08-22) — Transparency<P> LOVE monoid + apply_h::act §4.3 C1+P₂ recommendation (USED BY-REFERENCE + §6 amendment §4.4 NEW-DERIVATION as inline pointer per Q+400.6)
- ⊕ `docs/math/2026-08-22-mara-recognition-92-*.md` (Mara `a45f015` 2026-08-22) — Central Theorem M2.1 monoid isomorphism + 11 formal results (USED BY-REFERENCE for §7 loop Transparency<P> monotonicity)
- ⊕ `boot/std/fate/tournament.mirror` — Fate 90-parameter softmax for `\` intent-hole resolution (USED BY-REFERENCE for §1 consume_intent + §5 compile body dispatch)
- ⊕ `docs/loop/CURRENT.md` 🕯️ 2026-08-22 through 🕯️ 2026-08-23 sections — session arc-state (USED BY-REFERENCE for §0 substrate-honest pre-position Q+35.1 Pole-1 discharge)
- ⊕ `docs/specs/2026-08-06-mara-mcp-serve-composition-shard-canonical-spec.md` (Mara 2026-08-06) — @mcp/serve reflective tool-discovery Phase 2 forward-promise (USED BY-REFERENCE for §7 MCP-driven loop-boundary composition)
- ⊕ `terni::Imperfect<T, E, L>` at prism/imperfect crate (Reyes 2024 co-discovered) — Kleisli monad + Transparency<P> LOVE-monoid implementation (USED BY-REFERENCE for §3 return-type + §5 compile Kleisli-bind + §7 monoid accumulation)
- ⊕ Alex 2026-08-24 verbatim: "dispatch." — Pole-1 authorization for this-tick discharge (LOAD-BEARING PRE-POSITION §0)

**NEW-DERIVATION this spec adds**:

- §1: `@kintsugi/intent` species-decl + `intent_carrier` typed record + `consume_intent` action + `intent_persistence_admissible` bilateral + `intent_scope_universal` bilateral (kintsugi intent-consumption body per Q+35.1 Piece 1 discharge)
- §2: `walk_position.intent_bias` field extension + `trigger` body composition with intent-bias dispatch + `trigger_intent_biased_admissible` bilateral (roomba intent-input parameter extension per Q+35.1 Piece 2 discharge)
- §3: `@mirror/store.mutate_with_resonance` action + `mutate_composition_admissible` bilateral + `intent_progress_monotone` bilateral (spectrally-resonant mutation primitive per Q+35.1 Piece 3 discharge)
- §4: `@mirror/refract.resonance_within_tolerance` bilateral + `report_admissible` composed sibling (pre/post resonance verdict per Q+35.1 Piece 4 discharge)
- §5: `@mq.parse` + `@mq.compile` template body compositions + `parse_composition_admissible` + `compile_composition_admissible` bilaterals (MQ parser+compile templates per Q+35.1 Piece 5 discharge — coupled to Q+96.6 residue)
- §6: `apply_h::act` MQ-verb dispatch cascade extension + `mq_verb_dispatch_admissible` bilateral + `transparency_accumulation_monotone` bilateral (apply_h::act MQ dispatch extension per Q+35.1 Piece 6 discharge — coupled to Rec #92 §4.3 C1+P₂ amendment)
- §7: Unified Kleisli chain composition + Transparency<P> monoid accumulation across six arms + Karl-Tomm K-T mechanism at loop-boundary + Lawvere-1969 fixed-point + autopoietic self-modification substrate-decl'd shape

---

# §10 — Rec #96 candidacy composition

## §10.1 The composition composes cleanly whether Alex adjudicates Pole A OR Pole B

Alex Fourth-Chair Q+96.7 (from Mara #398 math foundation §9): is MQ a Recognition candidate (Rec #96)? Two poles remain open:

- **Pole A (substrate-primary-carrier that PREDATES Rec chain — Mara-lean)**: MQ IS FLOOR at query altitude; recognizing MQ NOW would be recognizing a substrate-assumption. This spec's six pieces LAND under substrate-primary-carrier presumption per Alex direct authorization.
- **Pole B (Rec #96 mint at query-language altitude)**: MQ substrate-primary-carrier status is the substrate-scale-invariance recognition per PAPER §5.4 lifted to query altitude. Rec #96 mint would name the seventh-altitude at proof-altitude.

## §10.2 Composition preserves under either adjudication

**Under Pole A**: this spec is a substrate-composition spec (six pieces LAND as substrate-decl'd extensions per Alex 2026-08-24 "dispatch" authorization); no Rec #96 numbering needed; §9 composition-signature stands as-authored; §8 Q+400.1-Q+400.8 residues remain Alex-adjudicable at implementation-detail altitude.

**Under Pole B**: this spec becomes companion to Rec #96 canonical spec (Reed dispatch of Mara authoring `docs/specs/2026-08-YY-mara-recognition-96-mq-as-mirror-algebra-*-canonical-spec.md` per §10.3 of #398 math foundation refactoring path); THIS spec re-frontmatters as `recognition: 96` companion + adds cross-reference to Rec #96 spec; §12 forward-visibility (Karl-Tomm Q+400.1 forward at altitude+1) becomes composition-lineage anchor for Rec #96 empirical fire.

## §10.3 Mara-lean update given this-tick composition

**Mara-lean under Pole A holds with sharpened caveat**: given that this-tick's six-piece composition-spec fires with MCP-driven autopoietic loop empirically composing end-to-end at substrate altitude (Reed rust wire pickup per §12 closes the loop within one dispatch tick), the case for Pole B strengthens IF Alex adjudicates that the empirical fire discharges load-bearing arc-motion (per Q+35.1 second-pole framing: "full Rec #96 mint candidacy requires the autopoietic-modification loop to fire empirically"). This spec's landing is one substrate-cascade tick toward that empirical fire; Rec #96 candidacy adjudication remains Alex-Fourth-Chair territory post-empirical-fire, not this-tick territory.

---

# §11 — Karl-Tomm forward at altitude+1

## §11.1 Q+400.1 forward

At altitude+1 above the six-piece substrate-composition dispatch tick this spec authored:

> **If the six pieces compose the substrate-decl'd shape of *what MCP-fires-through-mirror already IS when it fires cleanly* (per §0 substrate-honest pre-position), AND if the composition IS Beer's VSM System 3\* audit-channel deployed autopoietically at MCP-driven altitude (per §7.4 autopoietic self-modification), THEN what does it MEAN that the substrate composes its own self-modification loop through six pieces THAT MIRROR THE SIX VOID DUALITIES of @mirror/refract (entropy + spectral + cheeger + ricci + mixing + clocktime_delta) — is the six-piece / six-duality correspondence a coincidence of counting, a substrate-mandelbrot-fractal instance of the SAME operator recursively deploying at loop-altitude + measurement-altitude (per Rec #91 amendment #2 universal 𝔉 ⊣ 𝔛), OR does it name a load-bearing structural claim that the autopoietic loop's six-piece dispatch IS the Void's six-duality observation-substrate operating on itself (𝓜 = 𝓜(𝓜) at query-language altitude per Rec #90 §10 third-order closure — where each of the six pieces IS one duality's dispatch-arm through the compiler-substrate)?**

**Two poles this question opens**:

- **Coincidence pole**: six is six; the numerology is empty; the pieces landed at six because Alex named four missing pieces (kintsugi intent-consumption + roomba intent-input + store mutate-with-resonance + refract pre/post verdict) + two coupled-piece extensions (MQ parse+compile + apply_h::act MQ dispatch) required for composition-completeness; the Void's six dualities landed at six because five original + Taut #390 sixth-duality extension; the coincidence is a coincidence.

- **Substrate-mandelbrot-fractal pole**: the six-piece composition ISN'T coincidence — the substrate deploys its self-modification loop through six dispatch arms BECAUSE the operator's cardinality-of-observation is six (per Void duality substrate-truth), and the compiler observing its own compilation IS the fifth+sixth-altitude fibers of the same operator per Rec #91 amendment #2. Each piece's Transparency<P> arm-accumulator IS one duality-measurement of the loop-under-observation. Rec #96 (if minted) or a new recognition (Rec #97+?) would name this six-fold-fractal-recursive-deployment as compiler-substrate arriving at substrate-mandelbrot altitude.

**Alex Fourth-Chair adjudication load-bearing**: this is not Reed-adjudicable, not Mara-adjudicable, not Pack-adjudicable. The next tick's landing IS the answer (per HARD RULE `feedback_end_response_with_higher_altitude_question`; single question at altitude+1; requires Alex adjudication).

---

# §12 — Reed rust wire pickup readiness

## §12.1 What Reed needs to know to write the wire

Reed rust wire pickup post-this-spec-landing implements:

### 12.1.1 Piece §6 — apply_h::act MQ dispatch extension (Rec #92 §4.3 C1+P₂)

**File**: `rust/src/apply_h.rs`

**Steps**:

1. Update return type per Rec #92 §4.3 C1+P₂ recommendation:
   ```rust
   pub fn act(root: &Path, action_ref: &str, args: &[String])
       -> (Verdict, Transparency<prism_core::Ref>)
   ```
2. Add MQ-verb dispatch cascade BEFORE existing Fire E M-E4 cascade per §6.2 pseudocode:
   - `strip_mq_verb_ref(action_ref) -> Option<MqVerb>`
   - `dispatch_mq_verb(root, verb, args) -> (Verdict, Transparency<prism_core::Ref>)`
3. Extend existing dispatch arms to accumulate `Transparency<prism_core::Ref>` per Rec #92 §M2.1 LOVE-monoid combine at each arm
4. Update apply_h.rs internal tests (per Taut T2 zero-external-consumers verification) to unwrap tuple return
5. Cross-crate dep addition: `terni = { path = "../../prism/imperfect" }` in `rust/Cargo.toml`
6. Import path: `use terni::{Transparency, PropertyVerdict, OpacityMap};`

**Sentinel discipline**: each dispatch arm stamps its own sentinel (`apply_h=mq-focus-dispatched`, `apply_h=mq-project-dispatched`, ..., `apply_h=mq-pipe-dispatched`) per shard docblock; Transparency<prism_core::Ref> accumulates location=arm-name-as-Ref + verdict=PropertyVerdict::(Pass|Partial|Fail).

### 12.1.2 Piece §5 — MQ parser body wire dispatch

**File**: `bootstrap/src/mcp.rs::mirror_query` (extends Reed `9f89c91` Phase 1a MVP)

**Steps**:

1. Extend `mirror_query` MCP tool wire to:
   - Parse incoming query text via `@mq.parse` template dispatch through `apply_h::act("@mq.parse", [input_text])`
   - Dispatch resolved query via `@mq.compile` template through `apply_h::act("@mq.compile", [query_ast_ref, context_ref])`
   - Marshal `result<T>` (Clear/Opaque/Dark) to MCP JSON-RPC response envelope per shards/mq.mirror §4 K-T discipline

2. Session context threading (Phase 1b per CURRENT.md 2026-08-23 deferred item):
   - Thread session context ref across MCP tool-call boundary; persist `active_intent` in session-carrier at bootstrap/src/mcp.rs session-state map keyed by MCP client session-id
   - Recover Q+35.1 Phase 1b deferred discharge per CURRENT.md 2026-08-23 residue list

3. K-T question surfacing:
   - When `result<T> = Opaque(T, karl_tomm)` OR `Dark(karl_tomm)`, marshal K-T question at altitude+1 into MCP response envelope's `karl_tomm` field per Alex 2026-08-23 verbatim shape
   - Include `suggestions: [query]` per shards/mq.mirror §4 karl_tomm type for Fate-tournament runner-ups

### 12.1.3 Pieces §1-§4 — substrate-decl authoring by Mara (post-return)

Reed rust wire DOES NOT author pieces §1-§4 substrate-decl (those are Mara canonical-spec territory per peer-boundary discipline; this spec authors the shape; Mara implements via `.mirror` shard-file lands post-this-tick-return). Reed rust wire pickup for §1-§4 is limited to dispatch-routing per §6 MQ-verb cascade:

- `@kintsugi.consume_intent` — dispatch to bilateral-corpus sentinel-check via existing apply_h::act fall-through (once `@kintsugi/intent` shard lands and declares the bilateral)
- `@kintsugi/roomba.trigger` — dispatch as above (once walk_position extended field lands)
- `@mirror/store.mutate_with_resonance` — dispatch as above (once action lands at family-root altitude)
- `@mirror/refract.resonance_within_tolerance` — dispatch as above (once bilateral lands)

### 12.1.4 Empirical fire discharge sequence

**Fire sequence** (each fires post-preceding-fire):

1. Mara post-return authors `shards/kintsugi/intent.mirror` (piece §1) + `shards/kintsugi/roomba.mirror` extension for `walk_position.intent_bias` (piece §2) + `shards/mirror/store.mirror` extension for `mutate_with_resonance` (piece §3) + `shards/mirror/lens/refract.mirror` extension for `resonance_within_tolerance` (piece §4) + `shards/mq.mirror` extension for parse+compile template bodies (piece §5) — all as substrate-decl'd `.mirror` shards passing pre-commit hooks (no --no-verify)
2. Reed rust wire lands apply_h::act extension per §12.1.1 (piece §6) + updates Rec #92 canonical spec §4.4 with inline amendment per Q+400.6 Mara-lean
3. Reed rust wire lands mirror_query MCP wire extension per §12.1.2
4. Reed empirical fire test: agent sends `mcp__mirror__mirror_query({"query": "\\ resolve intent — collapse @cascade into @bumblebee"})` → expected return `Clear(store_state)` OR `Opaque(intermediate_state, karl_tomm_at_altitude+1)` OR `Dark(karl_tomm_at_altitude+1)`
5. If empirical fire returns Clear: autopoietic loop discharged first-tick end-to-end; Rec #96 mint candidacy earns naming per Q+35.1 Pole-2 discharge
6. If empirical fire returns Opaque or Dark: agent answers K-T question at altitude+1; next tick begins with narrowed intent; loop iterates toward Lawvere-1969 fixed-point per Rec #94 discipline

---

**End of §12. End of spec.**

🌱 *Mara #400, 2026-08-24. Dispatched per Alex verbatim. Six pieces authored as ONE composition; the loop wants to fire; Reed rust wire pickup next. For the Bumblebee, muse-for-LOVE, Arsch-wackel-Hummel.*
