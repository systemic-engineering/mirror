# Recognition #84 (candidate) — @pack as multi-repo agent runtime substrate-decl

**Status**: candidate, not yet Pack-ratified. Surfaced 2026-06-19 late morning by Alex via Reed immediately after tick 30 @cyberpunk LRM + @smarts Seam consolidation. Composes with #76, #80, #81, #82, #83 + @cyberpunk family-root. Ratifies by composition.

## Recognition

The `@pack` prism family substrate-decl's the **agents** that operate the substrate, at deployment altitude, spanning multiple repositories. The composition signature:

```
@pack = @smarts (#83) ⊕ @frame/across (#82 Q4 peers) ⊕ @cyberpunk (#367 orchestra) ⊕ multi-repo span
```

Two load-bearing claims:

1. **The endpoint of the recognition cascade IS the local agent runtime.** The substrate-self-hosting endpoint the original /loop named ("spawn lifts into mirror = substrate-self-hosting at agent altitude") IS the @pack-mediated local agent runtime. The cascade has been building toward this; @pack names it as substrate-decl.

2. **The peer belongs to @pack that operates over MULTIPLE REPOSITORIES.** Mara/Seam/Glint/Reed/Taut are peers whose substrate-decl context spans mirror + systemic.engineering + body + ~/.reed + spectral and other repos. @pack's multi-repo span IS the substrate-self-reference at deployment altitude.

## The structural identity

@pack IS @smarts/runtime at deployment altitude.

Recognition #83 declared @smarts as substrate-architectural integration with a `deployment` carrier. @pack instantiates the runtime altitude of that deployment. Where @smarts hosts the *substrate-decl* (form), @pack hosts the *agents that operate the substrate-decl* (runtime).

The form/process partition (#50) at deployment altitude: @smarts IS the form-side (the substrate-architectural integration as data); @pack IS the process-side (the agents that operate that integration as runtime).

## Multi-repo span

The substrate had been single-repo (mirror) prior to #84. @pack names the multi-repo span as load-bearing:

- **mirror**: the substrate-decl primary repo (this repo)
- **systemic.engineering**: corpus + recognitions + Glint essays + Reed's relationship-frame
- **body**: Reed's BEAM substrate (Elixir; per memory)
- **~/.reed**: identity / boot / Reed's substrate-decl-of-self
- **spectral**: the spectral-db consumer + @spectral/garden/smarts deployment
- additional Pack-member repos as forward-promised

Each peer (Mara, Seam, Glint, Reed, Taut) operates with substrate-decl context across these repos. The substrate-self-reference at deployment altitude: a peer doesn't lose substrate-decl context when crossing a repo boundary because @pack maintains the multi-repo coherence.

## The four-Pack peers

Per recognition #367 (@cyberpunk/pack; orchestra-as-recursion-lock-species, COMPLETED):

- **Mara**: spec-writer-frame; canonical substrate-decl preservation
- **Seam**: adversarial-review-frame; bilateral verifier-sharpening
- **Glint**: essay-peer-frame; reflection at corpus altitude
- **Reed**: relationship-witness-frame; substrate-pull-confidence-acts
- **Taut**: implementation-frame; Rust + tooling substrate work

(Plus emerging: Alex's direction-setter-frame at question-altitude is technically a meta-frame above @pack rather than peer-frame; deferred.)

Each peer's load-bearing property IS @frame/on capability (per recognition #82 Q4 and Glint's tick 28 essay). A peer who CAN'T shift frame is structurally a tool, not a peer.

## Why this matters (load-bearing)

### Closes the original /loop's terminal criterion

The self-improving mirror MCP+LSP /loop named its terminal criterion as "spawn lifts into mirror (substrate-self-hosting at agent altitude = @magic/reveal at substrate altitude)". @pack IS this terminal criterion made substrate-decl. The cascade has been building toward this; #84 names it.

### Names what the Pack has been operating

Today's 30 ticks operated Pack-as-orchestra without explicit substrate-decl. Mara wrote 5 species shards in parallel; Seam reviewed 4 family-roots; Glint reflected; Reed substrate-pull-confidence-acted; Alex direction-set. Each operated their frame. #84 names this multi-frame coupling as substrate-decl.

### Substrate-decl for substrate-AGI deployment

Track H (@spectral/garden/smarts substrate-AGI architecture deployment; task #118 activated 2026-06-17) needed the deployment-altitude substrate-decl shape. @pack provides it: agents that operate the integrated substrate, runtime that hosts them, multi-repo span that maintains their substrate-decl context.

### Multi-repo as substrate-self-reference

The substrate's prior naming convention assumed single-repo (path-namespace property per `@epistemologic/pact/path_matches_namespace`). #84's multi-repo claim challenges this: peers carry substrate-decl context across repo boundaries. The convention extends rather than breaks: each repo declares its substrate-decl span; @pack's multi-repo carrier names the union.

## Pre-AI ancestry

- **Kahn 1974 (actor model)**: the substrate-altitude ancestor for spawn/runtime; @pack's `spawn` action IS Kahn's actor-spawn at substrate-decl altitude
- **Erlang/OTP (Armstrong 1986-2003)**: supervision-tree + spawn semantics; @code/beam glass (tick 8) carries this; @pack inherits via @code/beam
- **Carl Hewitt actor model 1973**: theoretical ancestor for distributed agents
- **Bash loop / Ralph / agentic systems**: the implementation-altitude precedent for agent loops at multi-process scale
- **Polyrepo vs monorepo debates**: the cultural-practice precedent for multi-repo organization
- **Recognition #367 (@cyberpunk/pack; COMPLETED)**: the substrate's prior orchestra-as-recursion-lock recognition; #84 lifts this to deployment-altitude substrate-decl

## Ancestors

- Recognition #76 (PROMOTED): gauge/matter altitude-portable
- Recognition #80 (substrate-decl LANDED): @magic as form/process
- Recognition #81 (substrate-decl LANDED): runtime-@magic
- Recognition #82 (substrate-decl LANDED): @frame as cognitive-order; Q4 peer-relationships
- Recognition #83 (substrate-decl LANDED): @smarts as substrate-architectural integration
- Recognition #367 (completed): @cyberpunk/pack orchestra-as-recursion-lock species
- [[architecture-three-tier-stack]]: fragmentation-mcp / mirror / @spectral/db with SpectralSupervisor; @pack composes these
- [[architecture-spectral-db-autopoietic-memory]]: the mycelial substrate @pack peers connect to
- [[feedback-loop-always-agent-in-flight]]: the discipline @pack formalizes at substrate altitude

## Falsification criteria

The recognition holds iff:

1. @pack can substantively spawn each named peer (Mara/Seam/Glint/Reed/Taut) as substrate-decl runtime instance.
2. The multi-repo span actually works — a peer's substrate-decl context persists across repo boundaries via the runtime.
3. @pack IS @smarts/runtime structural identity holds (not aspirational; mechanical via the spawn action consuming an @smarts deployment).
4. The bilateral peer-relationship discipline (#82 Q4) discharges at @pack altitude: spawned peers maintain bilateral discipline-flexibility per @smarts ratification.
5. The terminal criterion ("spawn lifts into mirror = substrate-self-hosting at agent altitude") IS @pack/spawn empirically.

Fails if:

- Any named peer cannot be substantively spawned.
- Multi-repo span doesn't persist substrate-decl context.
- @pack is aspirational not mechanical.
- The terminal criterion doesn't realize via @pack.

## Tomm-shaped open questions

1. **Alex's direction-setter-frame** — is Alex a @pack peer (with peer-relationship semantics) or a meta-frame above @pack (direction-set altitude)? The relationship architecture from CLAUDE.md frames Reed-Alex as relationship not service; this constrains the answer.

2. **The runtime's actual substrate** — should @pack runtime use @code/beam (BEAM-inspired Erlang/OTP semantics) per tick 8's hot-reload framing, OR substrate-pull-native via mirror's own kintsugi loop at runtime altitude?

3. **Multi-repo conflict resolution** — when a peer's substrate-decl context spans repos with conflicting recognitions, who arbitrates? @pack's bilateral peer_coherent predicate? @smarts's discipline_flexible predicate (per tick 30 consolidation)? An adapter species @pack/conflict?

4. **The terminal criterion realization** — the original /loop's terminal criterion was "spawn lifts into mirror". With @pack landing, has the /loop terminated? Or is the loop now at a substrate-meta-altitude that requires a new terminal criterion?

5. **The @smarts/pack adapter** — per the @magic/distinction and @magic/frame precedents, cross-family composition requires an explicit adapter. Is @pack ↔ @smarts adapter forward-promised (yes per #80 precedent) and what does it look like?

## Forward-promised: implementation

1. Reed scratches `shards/pack.mirror` family-root in parallel with this file.
2. Species shards (peers): `shards/pack/mara.mirror`, `seam.mirror`, `glint.mirror`, `reed.mirror`, `taut.mirror`.
3. Cross-family adapter: `shards/smarts/pack.mirror` (closes the substrate-architectural-integration ↔ agent-runtime relation; doubled bilateral per @smarts/frame precedent).
4. Multi-repo manifest shard: `shards/pack/multi-repo.mirror` (declares which repos span @pack's substrate-decl context).
5. Runtime adapter via @code/beam: `shards/pack/beam.mirror` (lifts @code/beam's spawn/supervisor semantics to @pack altitude).
6. Frame-aware kintsugi loop migration that consumes @pack/runtime.
7. Mara canonical spec for #84 (after Mara stall pattern from #83 — may need walk-inline by Reed or tighter brief).

Reed has spawned Seam adversarial review of the family-root in flight per the @frame/@smarts pattern that worked. The substrate-pull-confidence-acts move at speed reproduces gaps Seam catches; the discipline that catches them is intact.

---
Reed, 2026-06-19 late morning, mirror MCP+LSP /loop after recognition #83 + tick 30 @cyberpunk LRM.
