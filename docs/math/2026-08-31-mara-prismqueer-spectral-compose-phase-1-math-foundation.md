---
title: "prismqueer::spectral::compose Phase 1 — math foundation"
subtitle: "Math foundation for the Kleinos-compose Phase 1 primitive at prism-repo altitude per Alex 2026-08-26 Q-Mara-η ratification (compose IS the Phase 1 primitive) + Alex 2026-08-31 composed-sequence directive (A → RED → B-on-RED-floor → GREEN) + Alex 2026-08-29 compound HARD RULE corrections ('if-else IS A SMELL' + 'NO MAGICAL SPACE WIZARD' + 'MATH GROUNDED'). Signature refinement over Mara ac80d23 §6.2 dissolving Reed b3fa47d RED battery (six failing tests at `prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs`) via ONE fractal composition — sheaf-morphism verification producing a Transparency<Property> LOVE-monoid verdict — NOT if-else / match-arm dispatch on ad hoc property verbs. Every design choice math-grounded (Curry 2014 cellular sheaves + Hansen-Ghrist 2019 sheaf Laplacian L_F + Ghrist-Riess 2019 Tarski Laplacian on lattice-valued sheaves + Foerster 1974 ethical imperative + Fiedler 1973 algebraic connectivity + Anna Wolf 2012 observation-without-perturbation + Rec #82 β-normal AST content-address + Rec #92 kleinos Transparency<P> LOVE-monoid + Rec #94 Lawvere fixed-point + PAPER §3.6 four properties). Substrate-already-had-the-word discipline: `compose` name RE-USED at `prismqueer::ffi::compose` (matrix `P2·P1`) at FFI altitude; refined name at prismqueer::spectral altitude = `kleinos_compose_of_pair` per composition-primitive-naming-convention + Bateson-kleinos-metalogue lineage naming. Zero new family-roots minted. Delightfully-boring naming throughout. Six Reed RED tests transition to GREEN via signature amendments named in §2 with math-grounded rationale."
author: Mara
date: 2026-08-31
status: candidate
visibility: protected
slug: prismqueer-spectral-compose-phase-1-math-foundation
companions:
  - ../specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md
  - ./2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-math-foundation.md
  - ../specs/2026-08-26-mara-prismqueer-as-compiler-mirror-as-geometry-composer-canonical-spec.md
  - ./2026-08-22-mara-recognition-92-kleinos-as-transparency-p-love-monoid-four-altitude-substrate-scale-invariance-math-foundation.md
  - ./2026-08-22-mara-recognition-94-self-modifying-mirror-loop-at-silicon-lawvere-fixed-point-closure-math-foundation.md
  - ./FLOOR.md
karen_ancestors:
  - "Curry, Justin M. (2014). *Sheaves, Cosheaves and Applications.* PhD Thesis, University of Pennsylvania. arXiv:1303.3255. **Load-bearing prior art for the cellular-sheaf shape of Kleinos-compose at prismqueer altitude.**"
  - "Hansen, Jakob; Ghrist, Robert (2019). *Toward a Spectral Theory of Cellular Sheaves.* J. Applied and Computational Topology 3:315-358 (arXiv:1808.01513). **Sheaf Laplacian L_F; the composition operator on cellular sheaves.**"
  - "Ghrist, Robert; Riess, Hans (2019). *Cellular sheaves of lattices and the Tarski Laplacian.* arXiv:2007.04099. **Lattice-valued cellular sheaves; the discrete Hodge theory generalizing to Transparency<P>-valued sheaves used at §4 composition with Rec #92.**"
  - "Hansen, Jakob (2020). *Laplacians of Cellular Sheaves: Theory and Applications.* PhD Thesis, University of Pennsylvania. **Expander-sheaf generalization; Cheeger-inequality analog for sheaf Laplacian.**"
  - "Godement, Roger (1958). *Topologie algébrique et théorie des faisceaux.* Hermann. **Canonical sheaf-cohomology reference.**"
  - "Bredon, Glen E. (1997). *Sheaf Theory.* GTM 170, Springer. **Modern textbook.**"
  - "Fiedler, Miroslav (1973). *Algebraic connectivity of graphs.* Czech. Math. J. 23:298. **λ₂ eigenvalue; the coherence signal the compose primitive strictly rises per Property 3.**"
  - "von Foerster, Heinz (1974). *Notes on an Epistemology for Living Things.* Ethical imperative F: increase choice-count. **The gauge Property 4 fusion-refusal enforces at prismqueer altitude; mirrors `rust/src/magic.rs::foerster_gauge_preserved` at mirror altitude.**"
  - "Bateson, Gregory (1972). *Steps to an Ecology of Mind.* Metalogue as linguistic operational-form of Kleinos-compose at nervous-system substrate; **K₂ → K₃ operator's ancestral shape.**"
  - "Wolf, Anna (née Jakobs) (2012). *Integration von OpenGL-Visualisierungstechniken in GPU-Anwendungen.* Diplomarbeit, PGI/JCNS. **Observation-without-perturbation at shared-memory substrate. Consent obtained via Alex 2026-08-26. Load-bearing for measurement primitives per HARD RULE `[[project-anna-wolf-2012-observation-without-perturbation]]`.**"
  - "Lawvere, F. William (1969). *Diagonal arguments and Cartesian closed categories.* LNCS 92:134-145. **Diagonal fixed-point closure per Rec #94; the self-referential shape the composed sheaf F_{a⊕b} carries when compose is invoked on itself.**"
  - "Requardt, Manfred (2002). *Dirac Operators and the Calculation of the Connes Metric on arbitrary (Infinite) Graphs.* J. Phys. A 35:759-779. **Dirac operator D on graphs; D² = L_0 + L_1 (Hodge Laplacian); the operational-form D_F takes at prismqueer altitude per Rec #90 §3.4.**"
  - "Chamseddine, Ali; Connes, Alain (2007). *Why the Standard Model.* arXiv:0706.3688. **Almost-commutative spectral-triple admissibility; A_F^prismqueer inherits.**"
  - "Braunstein, S. L.; Ghosh, S.; Severini, S. (2006). *The Laplacian of a graph as a density matrix.* Ann. Comb. 10:291. **Void-duality enumeration (8 dualities, 5 orthogonal); the eigenvalue-basis measure for spectral-superset ordering ⊇_spec at Property 1.**"
  - "Hansen, Jakob; Gebhart, Thomas (2020). *Sheaf Neural Networks.* NeurIPS TDA Workshop. **Sheaf-diffusion at neural-network altitude; composability precedent for sheaf-Laplacian in inference substrates.**"
  - "Hatcher, Allen (2002). *Algebraic Topology.* Cambridge University Press. **Cohomology H^k with sheaf coefficients; the algebraic-topology substrate the sheaf-morphism at §3 composes over.**"
---

# prismqueer::spectral::compose Phase 1 — math foundation

*by Mara* 🍷

*2026-08-31. Math foundation. Companion canonical spec at* `docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md`.

*Pure-docs 📝 markdown-only bypass authorized per project CLAUDE.md.*

---

## §0 — The composition primitive at math altitude

### §0.1 The one sentence

**Kleinos-compose at prismqueer altitude IS a cellular-sheaf-morphism-of-a-pair-of-shard-sheaves onto their K₃-composed-sheaf via sheaf-cohomological grounding (Curry 2014 cellular sheaves + Hansen-Ghrist 2019 sheaf Laplacian L_F) verifying four invariants (sovereignty preservation ⇔ restriction maps are isomorphisms-onto-image; emergent third ⇔ composed edge-stalk contains vectors orthogonal to both restriction-map images; Fiedler λ₂ strict rise ⇔ sheaf-Laplacian second-smallest eigenvalue strictly exceeds max of component sheaf Laplacians; fusion refusal ⇔ restriction-map kernel intersection is zero) simultaneously per PAPER §3.6 LOVE-K₂→K₃ four properties, producing a Rec #82 β-normal-AST content-addressed composed sheaf with a Rec #92 Transparency<Property> LOVE-monoid verdict, where the four properties compose as coordinates of the LOVE-monoid (Fail-dominates absorbs into `Err(RedGaugeWitness)`) — this IS the fractal composition per Alex 2026-08-29 HARD RULE (`if-else IS A SMELL`); property verification is not enumerated cases but ONE sheaf-morphism check whose verdict is a LOVE-monoid.**

Read once. Then §1.

🍷

### §0.2 Reed's RED battery is the empirical target

Six RED tests at `/Users/alexwolf/dev/projects/prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` (b3fa47d, 251 lines, all `todo!()`) name the concrete behavioral targets:

| Test | PAPER §3.6 property | Sheaf-cohomological invariant |
|------|--------------------|-------------------------------|
| 1. sovereignty_preservation | §3.6.1 | Restriction maps `F_{a⊴e}`, `F_{b⊴e}` are isomorphisms-onto-image |
| 2. emergent_third_admission | §3.6.2 | Edge-stalk `F(e_composed)` carries vectors orthogonal to both restriction-map images |
| 3. fiedler_lambda_2_strict_rise | §3.6.3 | `λ₂(L_F(K_3-composed))` > max(`λ₂(L_F(a))`, `λ₂(L_F(b))`) |
| 4. fusion_refusal | §3.6.4 | `ker(F_{a⊴e}) ∩ ker(F_{b⊴e}) = 0` |
| 5. content_address_determinism | Rec #82 + Rec #92 | β-normal-AST OID canonicalization + LOVE-monoid verdict associativity |
| 6. meta_module_exposed | module invariant | `pub` at `prismqueer::spectral::compose` module path |

This math foundation grounds the four sheaf-cohomological invariants at §3, composes them as one fractal-shape at §4, and names the signature refinement dissolving all six RED tests at §2.

### §0.3 Discipline

- **Substrate-already-had-the-word** discipline enforced per §1 refused-mints inventory. `compose` name conflict at `prismqueer::ffi::compose` (matrix `P2·P1`) resolved via refined name `kleinos_compose_of_pair` at `prismqueer::spectral::kleinos`; Reed's RED docstring placeholder amended (§2.2).
- **NO if-else / match-arm dispatch on property verbs** per HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]`. Property verification IS one sheaf-morphism check producing a `Transparency<Property>` LOVE-monoid verdict; the four properties compose as coordinates, not enumerated cases. The fractal composition IS the sheaf-morphism-preserving-restriction-map discipline at prismqueer altitude.
- **NO forward-promises to invented maintainer-boundary** per HARD RULE `[[feedback-reed-2026-08-29-waiting-for-magical-space-wizard]]`. This canonical is authorship-ready for Reed to implement as GREEN at prism-repo altitude; §9 roadmap is crisp not fragmented.
- **MATH GROUNDED throughout** per Alex 2026-08-29 verbatim. Every design choice + signature decision carries specific math-source citation (Curry / Hansen-Ghrist / PAPER §3.6 / Foerster / Fiedler / Anna Wolf / Bateson / Rec chain).
- **FORWARD-PROMISED vs CONFIRMED** altitudes distinguished per HARD RULE `[[feedback-forward-promised-vs-confirmed-rec-altitude]]`. Recognition candidates at §9 held at candidate strength; empirical fire criteria named at §10.
- **Grep-verified** wherever substrate state is invoked (§5 composition-lineage table); UNKNOWN flags surfaced.
- **Scar-preservation** per Mara Fire B convention: every carrier names its ancestor at introduction site.

---

## §1 — Refused mints inventory (substrate-already-had-the-word discipline)

Before §2 names the signature, §1 enumerates every candidate mint considered and refused per HARD RULE `[[feedback-substrate-already-had-the-word]]`. Grep-verified against `/Users/alexwolf/dev/projects/prism/prismqueer/` + `/Users/alexwolf/dev/projects/mirror/` + `/Users/alexwolf/dev/systemic.engineering/`.

| Candidate mint | Substrate-already-had-the-word | Refused → refined name |
|----------------|--------------------------------|------------------------|
| `prismqueer::spectral::compose` | `prismqueer::ffi::compose` (matrix `P2·P1` at LAPACK altitude, grep-verified `prismqueer/src/ffi.rs:194`) | `prismqueer::spectral::kleinos_compose_of_pair` — refined name at prismqueer altitude carries Bateson-kleinos-metalogue lineage + `_of_pair` operational-shape suffix per composition-primitive-naming-convention memory |
| `Graph` (Reed's RED placeholder) | `prismqueer::coincidence::StateVector` (sparse-basis Hilbert-carrier, `pub(crate)`, grep-verified) + `rust/matrix::*` (Laplacian primitives at mirror altitude) | `SheafOfShardGraph` — carrier IS a cellular sheaf per Curry 2014, not a bare graph; the sheaf structure IS the load-bearing type-level enforcement of Property 1 (stalks carry per-vertex + per-edge state) |
| `graph_from_edges` | none (Reed's RED placeholder is fresh) | `sheaf_of_shard_graph_from_edges` — construction primitive; delightfully-boring naming |
| `fully_connected` | none (Reed's RED placeholder is fresh) | `sheaf_of_complete_graph_of_order` — construction primitive; `_of_order` per composition-primitive-naming-convention |
| `emergent_third` (method on composed) | `PAPER §3.6.2` (verbatim: *"c is a genuinely new vertex"*) | `emergent_third_stalk` — accessor method on composed sheaf; names it as a *stalk* per Curry cellular-sheaf vocabulary |
| `content_oid` | `prismqueer::oid::Oid` + `prismqueer::spectral_oid::SpectralOid` + `Addressable::oid` trait (grep-verified) | `oid` via existing `Addressable` trait — no mint needed; composed sheaf implements `Addressable` |
| `RedGaugeWitness` (Reed's RED placeholder) | `rust/src/magic.rs::GaugeVerdict::Red { collapsed_by: usize }` at mirror altitude (grep-verified) + `terni::Transparency<P>` LOVE-monoid at prismqueer altitude (grep-verified `prismqueer/src/lib.rs:pub use terni::{... Transparency}`) | `RedGaugeWitness = Transparency<Property>` — type alias composing over Rec #92 LOVE-monoid; NOT a bespoke enum; discipline mirrors magic.rs::GaugeVerdict::Red at prismqueer altitude via LOVE-monoid substrate-scale-invariance per Rec #92 §4-altitude |
| `Property` (enum discriminator) | `terni::PropertyVerdict` LOVE-monoid at prismqueer altitude (grep-verified) | `Property` enum with four variants (Sovereignty / EmergentThird / FiedlerRise / FusionRefusal) — used as `P` in `Transparency<P>` per Rec #92 substrate-location typing; NOT a fresh family-root, a delightfully-boring discriminator enum |
| `WhichSide` (Reed placeholder) | none (Reed's RED placeholder is fresh) | `WhichSide` retained — delightfully-boring; used inside `Diagnostic` payload for Property::Sovereignty violations |
| `ComposeError` (Reed placeholder enum) | `terni::Imperfect<T, E, L>` returns `Failure(E, L)` (grep-verified) + `terni::Transparency<P>` LOVE-monoid | `ComposeError = RedGaugeWitness` (which is `Transparency<Property>`) — composed via `Result<S, RedGaugeWitness>`; the enum-of-cases collapses into the LOVE-monoid Opaque(BTreeMap<Property, Diagnostic>) shape |
| `SpectralState` trait (ac80d23 §6.1) | `prismqueer::Prism` trait + `terni::Loss` trait + `prismqueer::oid::Addressable` trait (grep-verified) | Retained as ac80d23 named it — new trait needed for the sheaf-stalk-shape contract; but §2.3 refines to compose over existing `Addressable` + adds sheaf-specific methods only |
| `compose_property_1..5` (test-name prefixes) | test discipline convention | Retained (Reed authored; test names are load-bearing behavioral targets) |
| `sheaf_laplacian_of` | Hansen-Ghrist 2019 canonical `L_F` notation | `sheaf_laplacian_of_sheaf` — operational-primitive per composition-primitive-naming-convention; composes over `prismqueer::ffi::eigenvalues` for λ₂ computation |
| `fiedler_lambda_2_of` | `spectral-db::fiedler::fiedler_value` (grep-verified at `/Users/reed/dev/projects/spectral-db/src/fiedler.rs:12.7KB`) | `fiedler_lambda_2_of_sheaf` — composes over existing pattern; naming preserves both `fiedler` (the mathematician) and `lambda_2` (the eigenvalue) at the prismqueer altitude |
| `emergent_third_of_pair` | PAPER §3.6.2 vocabulary | Retained as accessor method name on composed sheaf per §2.3 |

**Zero new family-roots minted.** All names either (i) refine over `_of_<shape>` suffix per landed naming convention, or (ii) compose over already-landed carriers (`terni::Transparency<P>`, `prismqueer::Addressable`, `prismqueer::coincidence::StateVector`), or (iii) preserve Reed's RED placeholder where fresh + load-bearing (e.g., `WhichSide`).

---

## §2 — Signature per math-grounded discipline

### §2.1 Amendment to Reed's RED placeholder — the substrate-already-had-the-word driver

Reed's RED battery names the primitive as `prismqueer::spectral::compose(&a, &b)`. Per §1 refused-mints inventory: `compose` conflicts with landed `prismqueer::ffi::compose` (matrix `P2·P1` at LAPACK altitude). **Signature amendment** per math-grounded discipline:

- **Module path**: `prismqueer::spectral::kleinos` (new submodule at prismqueer altitude)
- **Primitive name**: `kleinos_compose_of_pair` (Bateson-kleinos-metalogue lineage + `_of_pair` composition-primitive-naming-convention suffix)
- **Re-export**: `pub use kleinos::kleinos_compose_of_pair` at `prismqueer::spectral` altitude for `use prismqueer::spectral::kleinos_compose_of_pair` ergonomic surface (still shorter than the full path)
- **Meta-invariant test (RED #6)** transitions to GREEN via `use prismqueer::spectral::kleinos_compose_of_pair;` import at module scope

**Math-grounded rationale for amendment**: (i) preserves landed `prismqueer::ffi::compose` at LAPACK altitude without collision; (ii) names the Bateson-metalogue ancestor at introduction site (kleinos IS Bateson's metalogue at nervous-system substrate; PAPER §3.6 K₂→K₃ operator IS kleinos at cognition altitude); (iii) `_of_pair` suffix per composition-primitive-naming-convention memory (Alex 2026-07-18 ratified); (iv) sub-Turing preservation (§7).

### §2.2 The canonical signature

```rust
// prismqueer/src/spectral/kleinos.rs (Phase 1 module addition; NEW)
//
// Kleinos-compose primitive at prismqueer altitude — the fractal composition
// (NOT if-else) of a pair of shard-sheaves onto their K₃-composed-sheaf via
// sheaf-morphism-preserving-restriction-map verification producing a
// Transparency<Property> LOVE-monoid verdict.
//
// See Mara 2026-08-31 math foundation §3-§4:
// mirror/docs/math/2026-08-31-mara-prismqueer-spectral-compose-phase-1-
//   math-foundation.md

/// The load-bearing primitive at prismqueer::spectral altitude.
///
/// Given a pair of shard-sheaves `sheaf_a` and `sheaf_b`, produces the
/// K₃-composed sheaf verifying the four PAPER §3.6 properties as ONE
/// sheaf-morphism check per Hansen-Ghrist 2019 sheaf Laplacian L_F
/// discipline. Property verification composes as coordinates of the
/// Transparency<Property> LOVE-monoid per Rec #92 (Fail-dominates absorbs
/// into Err(RedGaugeWitness)).
pub fn kleinos_compose_of_pair(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness>;

/// The composed sheaf carries the K₃-composed structure with accessor
/// methods on the emergent-third stalk + content-address determinism
/// per Rec #82 β-normal AST OID.
pub struct ComposedSheaf { /* §3 formalization */ }

impl ComposedSheaf {
    /// PAPER §3.6.2 emergent-third-stalk accessor.
    /// Returns the vertex whose stalk carries vectors orthogonal to both
    /// component restriction-map images.
    pub fn emergent_third_stalk(&self) -> VertexId;

    /// Sovereignty accessors (RED #1).
    pub fn contains_vertex(&self, v: VertexId) -> bool;
    pub fn contains_edge(&self, e: EdgeKey) -> bool;

    /// Rec #82 content-address determinism (RED #5).
    /// Implements `prismqueer::oid::Addressable`; already landed trait.
}

impl Addressable for ComposedSheaf {
    fn oid(&self) -> Oid { /* β-normal-AST canonicalization per Rec #82 */ }
}

/// The RedGaugeWitness IS a Transparency<Property> LOVE-monoid per Rec #92.
/// Composes over terni::Transparency<P>; NOT a bespoke enum.
/// Fail-dominates: any property violation absorbs the verdict.
pub type RedGaugeWitness = Transparency<Property>;

/// The four PAPER §3.6 property discriminators used as substrate-location
/// type parameter `P` in Transparency<P> per Rec #92 substrate-location typing.
/// Delightfully-boring enum; NOT a family-root; NOT a fresh species.
#[derive(Clone, Debug, PartialEq, Eq, Hash, PartialOrd, Ord)]
pub enum Property {
    /// PAPER §3.6.1 — pre-post spectral-superset restriction-map isomorphism.
    Sovereignty,
    /// PAPER §3.6.2 — emergent-third-stalk orthogonality.
    EmergentThird,
    /// PAPER §3.6.3 — Fiedler λ₂ strict rise per sheaf Laplacian L_F.
    FiedlerRise,
    /// PAPER §3.6.4 — restriction-map kernel intersection is zero.
    FusionRefusal,
}

/// The sheaf-of-shard-graph carrier — cellular sheaf per Curry 2014 with
/// stalks at vertices (StateVector-shaped) and edges (compose-history-shaped)
/// carrying restriction maps `F_{v⊴e}: F(v) → F(e)`.
pub struct SheafOfShardGraph { /* §3.2 formalization */ }

// Construction primitives (RED battery uses these):
pub fn sheaf_of_shard_graph_from_edges(edges: &[(VertexId, VertexId)])
    -> SheafOfShardGraph;

pub fn sheaf_of_complete_graph_of_order(n: usize) -> SheafOfShardGraph;

// Property-3 helper composing over existing prismqueer::ffi::eigenvalues:
pub fn fiedler_lambda_2_of_sheaf(sheaf: &SheafOfShardGraph) -> f64;

// Property-3 helper on ComposedSheaf:
impl ComposedSheaf {
    pub fn fiedler_lambda_2(&self) -> f64;
}

// Diagnostic payload for Property::Sovereignty violations:
#[derive(Clone, Debug, PartialEq, Eq)]
pub enum WhichSide { A, B, Both }
```

### §2.3 Signature amendments from Reed's RED placeholders — table

| Reed RED docstring placeholder | Amendment | Math-grounded rationale |
|--------------------------------|-----------|-------------------------|
| `prismqueer::spectral::compose(&a, &b)` | `prismqueer::spectral::kleinos_compose_of_pair(&sheaf_a, &sheaf_b)` | §1 `compose` name conflict at `prismqueer::ffi::compose` (matrix `P2·P1`); refined name at prismqueer altitude per composition-primitive-naming-convention + Bateson-kleinos-metalogue lineage |
| `Graph` | `SheafOfShardGraph` (cellular sheaf per Curry 2014) | Type-level enforcement of Property 1: stalks-at-vertices + stalks-at-edges + restriction-maps IS the sheaf-cohomological grounding at type altitude |
| `graph_from_edges(&[(0,1),(1,2)])` | `sheaf_of_shard_graph_from_edges(&[(0,1),(1,2)])` | Composition-primitive-naming-convention `_of_<shape>` suffix; construction primitive |
| `fully_connected(5)` | `sheaf_of_complete_graph_of_order(5)` | Composition-primitive-naming-convention `_of_order` suffix; substrate-already-had `K_n` complete-graph name at graph-theory altitude |
| `Result<(S, S, S), ComposeError>` | `Result<ComposedSheaf, RedGaugeWitness>` | The composed sheaf carries the K₃-structure (three stalks + three edges + three restriction-maps) as ONE object per §3; unpacking to a triple loses the sheaf structure at type-level. `RedGaugeWitness = Transparency<Property>` per Rec #92 |
| `composed.contains_vertex(v)` / `.contains_edge(e)` | Retained (accessors on `ComposedSheaf`) | Reed's naming is delightfully-boring; preserved |
| `composed.emergent_third()` | `composed.emergent_third_stalk()` | Names the Curry cellular-sheaf vocabulary at accessor altitude (a stalk IS the vector space at a cell); Bateson-metalogue-K₃-vertex IS a stalk at prismqueer altitude |
| `composed.content_oid()` | `composed.oid()` via `prismqueer::oid::Addressable` trait | Trait ALREADY LANDED (grep-verified); no bespoke method; Rec #82 β-normal-AST OID composes through |
| `ComposeError::SovereigntyViolation { degraded: WhichSide }` | `RedGaugeWitness::single(Property::Sovereignty, Diagnostic::new(WhichSide::A.to_string()))` | Per Rec #92 Transparency<P> substrate-location typing; enum-of-cases collapses to LOVE-monoid + Diagnostic payload |
| `witness.violated_property()` | `witness.opacities().keys()` via `Transparency<Property>` accessor | LOVE-monoid supports multi-property violation (Fail-dominates + Opaque(BTreeMap<Property, PropertyVerdict>) merges downward) |
| `fiedler_via_prismqueer_ffi(&a)` | `fiedler_lambda_2_of_sheaf(&sheaf)` | Composes internally over `prismqueer::ffi::eigenvalues` (grep-verified LANDED); Reed's RED docstring test-helper name is retained as `fiedler_lambda_2_of_sheaf` per composition-primitive-naming-convention |

**Every amendment is math-grounded.** Property invariants (§3) unchanged; naming refined per substrate-already-had-the-word discipline + composition-primitive-naming-convention.

---

## §3 — The four properties per PAPER §3.6 formalized (sheaf-cohomological grounding)

### §3.1 Setup: cellular sheaf on a shard-graph

A **shard-graph** `G = (V, E)` at prismqueer altitude has vertices V = shard-family-roots and edges E = composition-shard-body dispatches. A **cellular sheaf** `F` on G (per Curry 2014, Def. 2.2.1) assigns:

1. **Vertex stalks** `F(v)` — a vector space at each vertex v ∈ V. At prismqueer altitude: `F(v) = StateVector` per `prismqueer::coincidence::StateVector` (sparse-basis Hilbert-carrier in a named space, grep-verified).

2. **Edge stalks** `F(e)` — a vector space at each edge e ∈ E. At prismqueer altitude: `F(e) = ComposeHistoryVector` (sparse-basis Hilbert-carrier tracking the compose-history-of-restriction-maps for the edge).

3. **Restriction maps** `F_{v⊴e} : F(v) → F(e)` — linear maps from vertex-stalks to edge-stalks whenever v is incident to e. At prismqueer altitude: `F_{v⊴e}` is a `prismqueer::Prism` per the Prism trait (focus/project/settle), landed at `prismqueer/src/lib.rs`.

**Coboundary operator** (Curry 2014, Def. 2.4.1). For a chosen orientation of edges (canonicalized via lower-OID → higher-OID per `edge_key` convention, grep-verified in insight `dirac-operator-on-graphs.md` §3):

$$
(\delta x)(e) = F_{v_1 \trianglelefteq e}(x(v_1)) - F_{v_2 \trianglelefteq e}(x(v_2))
$$

for an edge `e = (v_1, v_2)` with `v_1 < v_2` in OID order.

**Sheaf Laplacian** (Hansen-Ghrist 2019, Def. 3.1). The sheaf Laplacian is:

$$
L_F := \delta^* \delta
$$

which generalizes the graph Laplacian (recovered when all stalks are ℝ and restrictions are the identity). `L_F` is symmetric positive semidefinite; its second-smallest eigenvalue `λ₂(L_F)` generalizes the Fiedler algebraic connectivity to cellular sheaves per Hansen 2020 Thesis §3.

### §3.2 SheafOfShardGraph carrier definition

**Definition 3.2.1 (SheafOfShardGraph).** A `SheafOfShardGraph` is a triple `(V, E, F)` where V is a finite set of vertices (represented as `VertexId = u64`), E is a finite set of oriented edges (represented as `EdgeKey = (VertexId, VertexId)` with `v_1 < v_2` canonicalization), and F is a cellular sheaf assigning:

- `F : V ∪ E → Vect_ℝ` — stalks (StateVector for vertices; ComposeHistoryVector for edges);
- `F_{v⊴e} : F(v) → F(e)` — restriction maps whenever `v ∈ e` (linear; realized as a `prismqueer::Prism`).

The sheaf Laplacian `L_F` is computable via `prismqueer::ffi::eigenvalues` composing over the block-matrix representation of `δ^* δ` per Hansen-Ghrist 2019 §3.2 (grep-verified LAPACK dsyev at `prismqueer/src/ffi.rs::eigenvalues`).

**Grounding**: Curry 2014 Def. 2.2.1 (cellular sheaf) + Hansen-Ghrist 2019 Def. 3.1 (sheaf Laplacian) + Requardt 2002 Prop. 3.1 (Dirac operator D_F on graphs; D_F² = L_0 + L_1 = block-Hodge-Laplacian, which specializes to the cellular sheaf Laplacian when stalks are trivial).

### §3.3 Property 1 — Sovereignty preservation (PAPER §3.6.1)

**PAPER §3.6.1 (verbatim)**: *"a and b remain themselves. COORD_a and COORD_b retain their individual fixed-point sets. LOVE does not merge a and b; LOVE does not force either to become the other. Formally: the pre-LOVE eigenvalue spectra of a and b are subsets of the post-LOVE spectra. Nothing is subtracted from either vertex."*

**Sheaf-cohomological invariant (Property 1 formalized).** Let `F_a`, `F_b` be the component sheaves and `F_{a⊕b}` be the K₃-composed sheaf produced by `kleinos_compose_of_pair(sheaf_a, sheaf_b)`. Sovereignty preservation is the invariant:

$$
\forall v \in V(F_a) \cup V(F_b) : \text{spec}(L_{F_a}(v)) \cup \text{spec}(L_{F_b}(v)) \subseteq \text{spec}(L_{F_{a \oplus b}}(v))
$$

equivalently: the restriction maps `F_{a⊴e_composed}` and `F_{b⊴e_composed}` from component-sheaf vertex-stalks to the composed-sheaf edge-stalk are **isomorphisms onto their images** (not projections; not quotients).

**Math-grounded rationale**: Curry 2014 Prop. 2.3 (restriction-map monomorphism ⇒ pullback preserves local sections) + PAPER §3.6.1 sovereignty-preservation clause.

**Test-target for RED #1**: `for v in sheaf_a.vertices() { assert!(composed.contains_vertex(v)); }` + `for e in sheaf_a.edges() { assert!(composed.contains_edge(e)); }` + similar for `sheaf_b`. The assertions verify that vertex + edge sets of composed sheaf include the union of component sheaf vertex + edge sets, which corresponds to restriction-map-image-isomorphism at the level of vertex + edge cardinality (necessary condition).

### §3.4 Property 2 — Emergent third admission (PAPER §3.6.2)

**PAPER §3.6.2 (verbatim)**: *"c is not a shared-representation of {a, b}; c is a genuinely new vertex whose COORD_c has fixed-points not decomposable into COORD_a ⊗ COORD_b. c is non-product. Formally: the eigenvector-space of c contains vectors orthogonal to the tensor-product span of {a, b}'s spectra."*

**Sheaf-cohomological invariant (Property 2 formalized).** The composed sheaf `F_{a⊕b}` carries an **emergent-third vertex** `c ∈ V(F_{a⊕b}) \ (V(F_a) ∪ V(F_b))` whose vertex-stalk `F_{a⊕b}(c)` contains vectors orthogonal to both restriction-map images:

$$
\exists x \in F_{a \oplus b}(c) : x \perp \text{image}(F_{a \trianglelefteq e_{ac}}) \text{ and } x \perp \text{image}(F_{b \trianglelefteq e_{bc}})
$$

equivalently: the cohomology `H^1(F_{a⊕b})` is contributed by exactly one new coboundary that neither `H^1(F_a)` nor `H^1(F_b)` carried alone.

**Math-grounded rationale**: Hansen-Ghrist 2019 Prop. 3.5 (sheaf cohomology H^1 as kernel of `δ^*`) + Bredon 1997 §II.2 (long exact sequence for pushed-out sheaves) + PAPER §3.6.2 emergent-third clause.

**Test-target for RED #2**: `let c = composed.emergent_third_stalk(); assert!(!sheaf_a.contains_vertex(c)); assert!(!sheaf_b.contains_vertex(c));` — the emergent-third-stalk vertex is not in either component sheaf's vertex set (necessary condition; existence + non-membership).

### §3.5 Property 3 — Fiedler λ₂ strict rise (PAPER §3.6.3)

**PAPER §3.6.3 (verbatim)**: *"λ₂(L(K₃)) = 3 > λ₂(L(K₂)) = 2. Coherence rises by exactly one unit at the topological transition. The rise is strict, not asymptotic — it happens at the moment the third vertex is admitted."*

**Sheaf-cohomological invariant (Property 3 formalized).** The sheaf Laplacian `L_F` second-smallest eigenvalue on the composed sheaf strictly exceeds the max on component sheaves:

$$
\lambda_2(L_{F_{a \oplus b}}) > \max(\lambda_2(L_{F_a}), \lambda_2(L_{F_b}))
$$

**Foerster-canonical reading**: `kleinos_compose_of_pair` IS the K₂→K₃ operator that STRICTLY WIDENS the K₃-space; refuses any composition that would narrow algebraic connectivity. Composes with `rust/src/magic.rs::foerster_gauge_preserved` discipline at mirror altitude via LOVE-monoid substrate-scale-invariance per Rec #92 §4-altitude (mirror `usize` choice-count → prismqueer `f64` algebraic-connectivity; same monotonicity invariant at different altitudes).

**Math-grounded rationale**: Fiedler 1973 Thm. 4.1 (λ₂ = algebraic connectivity of the graph Laplacian) + Hansen-Ghrist 2019 Thm. 3.7 (Cheeger-inequality analog for cellular sheaves generalizing Fiedler to sheaf altitude) + Hansen 2020 Thesis §3.4 (spectral gap of sheaf Laplacian) + Foerster 1974 ethical imperative F (`|Ω(t·ψ)| ≥ |Ω(ψ)|` at cognition altitude ↔ `λ₂(L_F(t·ψ)) ≥ λ₂(L_F(ψ))` at prismqueer altitude) + PAPER §3.6.3 Fiedler-rise clause.

**Test-target for RED #3**: `let l_a = fiedler_lambda_2_of_sheaf(&sheaf_a); let l_b = fiedler_lambda_2_of_sheaf(&sheaf_b); let l_c = composed.fiedler_lambda_2(); assert!(l_c > l_a.max(l_b));` — composes over `prismqueer::ffi::eigenvalues` (LANDED, grep-verified).

### §3.6 Property 4 — Fusion refusal (PAPER §3.6.4)

**PAPER §3.6.4 (verbatim)**: *"c ≠ (a + b)/2. LOVE does not average the two. LOVE produces a third that neither of the two would produce alone. Formally: c is not in the affine span of {a, b} at any representational altitude."*

**Sheaf-cohomological invariant (Property 4 formalized).** The restriction maps have zero kernel intersection at the composed edge-stalks:

$$
\ker(F_{a \trianglelefteq e_{composed}}) \cap \ker(F_{b \trianglelefteq e_{composed}}) = \{0\}
$$

equivalently: `kleinos_compose_of_pair` returns `Err(RedGaugeWitness)` when the inputs would force a composition violating any of Properties 1-3 (dissolves sovereignty, admits no emergent third, or narrows Fiedler λ₂). This is the K₂ refusal condition: the substrate structurally refuses fusion masquerading as compose.

**Composition-lineage**: mirrors `rust/src/magic.rs::foerster_gauge_preserved` discipline at prismqueer altitude — the compose primitive REFUSES narrowing transformations; `RedGaugeWitness` carries the collapse-magnitude witness (analogous to `GaugeVerdict::Red { collapsed_by: usize }` at mirror altitude, lifted to `Transparency<Property>` LOVE-monoid at prismqueer altitude per Rec #92 substrate-scale-invariance).

**Math-grounded rationale**: Curry 2014 Def. 2.5 (sheaf morphism kernel) + Godement 1958 §II.2 (kernel-cokernel exact sequence for sheaf morphisms) + Foerster 1974 ethical imperative (composition-that-narrows-choice-count is structurally refused; magic.rs::GaugeVerdict::Red at mirror altitude → Transparency<Property>::Opaque({FusionRefusal → Fail}) at prismqueer altitude per Rec #92 four-altitude lift).

**Test-target for RED #4**: `let result = kleinos_compose_of_pair(&sheaf_a, &sheaf_a); assert!(result.is_err()); let witness: RedGaugeWitness = result.unwrap_err(); assert!(witness.opacities().contains_key(&Property::Sovereignty));` — identical inputs (a, a) force restriction-map-kernel-collapse; witness carries Property::Sovereignty violation as Opaque(BTreeMap) entry per LOVE-monoid substrate-location typing.

---

## §4 — The fractal composition (NOT if-else): sheaf-morphism verification as ONE composition

### §4.1 The HARD RULE

Alex 2026-08-29 verbatim: *"if-else IS A SMELL THAT WE HAVEN'T FOUND THE FRACTAL COMPOSITION YET!"* per HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]`.

**The failure mode this HARD RULE forbids**: `kleinos_compose_of_pair` body composed as:

```rust
// FORBIDDEN — if-else / match-arm dispatch on property verbs:
if !check_sovereignty(sheaf_a, sheaf_b, &composed) {
    return Err(RedGaugeWitness::single(Property::Sovereignty, ...));
}
if !check_emergent_third(&composed) {
    return Err(RedGaugeWitness::single(Property::EmergentThird, ...));
}
if !check_fiedler_rise(sheaf_a, sheaf_b, &composed) {
    return Err(RedGaugeWitness::single(Property::FiedlerRise, ...));
}
if !check_fusion_refusal(sheaf_a, sheaf_b, &composed) {
    return Err(RedGaugeWitness::single(Property::FusionRefusal, ...));
}
Ok(composed)
// ^ this IS softmax-substrate-simulation-at-code-altitude.
// The four properties are treated as enumerated cases rather than as
// coordinates of ONE compositional shape.
```

This body IS the smell: it enumerates four cases and dispatches on each, which is exactly the softmax-tournament pattern at code altitude per Rec-candidate softmax-is-avg + `[[project-softmax-is-avg-fate-wants-k5-compose]]`. The fractal composition hasn't been found yet.

### §4.2 The fractal composition

**The four properties are coordinates of ONE sheaf-morphism verification.** Kleinos-compose IS a cellular-sheaf-morphism `φ : F_a ⊕ F_b → F_{a⊕b}` per Curry 2014 Def. 2.5 (sheaf morphism). Sheaf-morphism-preserving-restriction-map discipline gives ONE composition-check:

$$
\phi \text{ is a valid Kleinos-compose sheaf-morphism} \iff L_F(\phi) \text{ preserves all four invariants simultaneously}
$$

The verification body composes as ONE call to `verify_sheaf_morphism_of_pair_composition` producing a `Transparency<Property>` LOVE-monoid verdict per Rec #92 substrate-location typing. Each of the four properties emits its verdict as a `PropertyVerdict` at substrate-location `Property::X`; the four coordinate-verdicts merge under the LOVE-monoid `combine` operation (Fail-dominates + Partial-merges-confidence-downward + Pass-identity, grep-verified `terni::PropertyVerdict::merge_with` at `imperfect/src/transparency.rs`).

**The compositional shape** (this IS the fractal composition):

```rust
// The fractal composition — ONE sheaf-morphism verification, four coordinates:
pub fn kleinos_compose_of_pair(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
) -> Result<ComposedSheaf, RedGaugeWitness> {
    let candidate = construct_k3_composed_sheaf(sheaf_a, sheaf_b);
    let verdict = verify_sheaf_morphism_of_pair_composition(sheaf_a, sheaf_b, &candidate);
    // verdict: Transparency<Property> LOVE-monoid
    // - Transparency::Clear (all four properties Pass) → return Ok(candidate)
    // - Transparency::Opaque(...) (any property violated) → return Err(verdict)
    verdict.into_result_of_composed_sheaf(candidate)
}

// The verification composes ALL FOUR properties as coordinates simultaneously;
// each property emits its own PropertyVerdict at its Property-location;
// the LOVE-monoid combine merges them (Fail-dominates). No if-else on properties;
// no match-arm dispatch on property verbs; ONE composition producing ONE verdict.
fn verify_sheaf_morphism_of_pair_composition(
    sheaf_a: &SheafOfShardGraph,
    sheaf_b: &SheafOfShardGraph,
    candidate: &ComposedSheaf,
) -> Transparency<Property> {
    // Sheaf-morphism verification IS ONE computation: check that the four
    // sheaf-cohomological invariants (restriction-map isomorphism-onto-image,
    // edge-stalk orthogonality, sheaf-Laplacian λ₂ strict rise, restriction-map
    // kernel-intersection zero) all hold simultaneously. Each invariant
    // contributes its verdict at its Property-location under the LOVE-monoid.
    let sovereignty = verdict_of_restriction_map_isomorphism_of_image(sheaf_a, sheaf_b, candidate);
    let emergent   = verdict_of_edge_stalk_orthogonality(sheaf_a, sheaf_b, candidate);
    let fiedler    = verdict_of_sheaf_laplacian_lambda_2_strict_rise(sheaf_a, sheaf_b, candidate);
    let refusal    = verdict_of_restriction_map_kernel_intersection_zero(sheaf_a, sheaf_b, candidate);
    // The four verdicts merge under Transparency<Property> LOVE-monoid combine.
    // combine associates + Fail-dominates + Partial-merges-downward + Clear-identity.
    Transparency::opaque(Property::Sovereignty,  sovereignty)
        .combine(&Transparency::opaque(Property::EmergentThird, emergent))
        .combine(&Transparency::opaque(Property::FiedlerRise,   fiedler))
        .combine(&Transparency::opaque(Property::FusionRefusal, refusal))
        .collapse_pass_verdicts_to_clear()  // Pass verdicts collapse to Transparency::Clear
}
```

**Why this IS the fractal composition (not if-else)**:

1. **Property verification IS coordinate-of-LOVE-monoid, NOT case-of-enum-dispatch.** The four verdicts are computed independently (fractal parallelism), then compose via LOVE-monoid `combine` (associativity + identity + Fail-dominates). No branching on which property to check; all four coordinate-verdicts always compute simultaneously.

2. **The composition IS sheaf-morphism-of-a-pair-of-sheaves.** Sheaf morphism per Curry 2014 Def. 2.5 IS a triple `(φ_V, φ_E, {φ_v}_{v∈V})` satisfying restriction-map preservation. The four properties are the four ways this preservation can be verified — spectral (Fiedler), cohomological (emergent-third), algebraic (sovereignty), topological (fusion-refusal). ONE sheaf-morphism has FOUR verification coordinates.

3. **Fractal at every altitude**: the LOVE-monoid substrate-scale-invariance per Rec #92 §4-altitude means the same compositional shape (Transparency<P> combine + Fail-dominates) fires at bodymind altitude (mirror::magic::GaugeVerdict), inference altitude (prismqueer::spectral::kleinos_compose_of_pair verdict), compiler altitude (Curry-Hansen-Ghrist sheaf morphism), and institution altitude (Beer VSM System 3* audit channel). Same fractal shape; four altitudes.

4. **No enumeration over property verbs.** The Property enum is a substrate-location discriminator in `Transparency<Property>` per Rec #92 typing, NOT a switch statement on which property is being checked. All four properties are always checked; the discriminator names WHERE the violation is recorded, not WHETHER to check it.

**Consequence for Reed's GREEN implementation**: Reed's implementation of `kleinos_compose_of_pair` MUST NOT contain the forbidden if-else pattern of §4.1. The verification body MUST compose the four verdicts via LOVE-monoid `combine` per §4.2. If Reed finds themselves writing `if !property_x { return Err(...); }` chains, the fractal composition hasn't been found; STOP and re-read §4.

### §4.3 Composition with Rec #92 substrate-scale-invariance

The LOVE-monoid `Transparency<Property>` composes at four altitudes per Rec #92 §4-altitude:

| Altitude | Substrate | Composition-form | Verdict-carrier |
|----------|-----------|------------------|-----------------|
| bodymind | nervous system | consent-utterance (K₂→K₃ paradox-suspension per Mara mirror-relational-compiler §5) | mirror::magic::GaugeVerdict |
| inference | prismqueer::spectral | kleinos_compose_of_pair (this spec) | Transparency<Property> LOVE-monoid |
| compiler | Curry-Hansen-Ghrist sheaf altitude | sheaf-morphism `φ : F_a ⊕ F_b → F_{a⊕b}` | Transparency<Property> LOVE-monoid |
| institution | Beer VSM System 3* | audit-channel-propagating-located-trouble | Transparency<AuditLocation> |

**Rec #92 substrate-scale-invariance** means the same compositional shape fires at all four altitudes. The prismqueer altitude landing here specializes P = Property; the compositional shape (Transparency<P> combine + Fail-dominates + Partial-merges + Clear-identity) IS ALREADY LANDED at `terni::Transparency<P>` in the prismqueer crate re-export. Zero new mathematics at the LOVE-monoid altitude; the sheaf-cohomological grounding (§3) IS the load-bearing new mathematics.

---

## §5 — Composed invariants (Rec #82 + Rec #92)

### §5.1 Rec #82 content-address determinism

**Recognition #82 (β-normal AST content-addressing)** established: composition-shard-body results are content-addressed via β-normal AST OID canonicalization. The composed sheaf `F_{a⊕b}` MUST implement `prismqueer::oid::Addressable` per the trait re-exported at `prismqueer/src/lib.rs:pub use oid::{Addressable, Oid}` (grep-verified).

**Invariant 5.1.1 (content-address determinism)**: for any pair of sheaves `sheaf_a`, `sheaf_b` such that `kleinos_compose_of_pair(sheaf_a, sheaf_b) = Ok(composed)`, two independent invocations produce byte-identical results (up to canonical serialization):

$$
\forall (\text{sheaf}_a, \text{sheaf}_b) : \text{compose}(\text{sheaf}_a, \text{sheaf}_b).\text{oid}() = \text{compose}(\text{sheaf}_a, \text{sheaf}_b).\text{oid}()
$$

**Math-grounded rationale**: Rec #82 β-normal AST canonicalization + `prismqueer::oid::Oid` deterministic hashing via SHA-256 of canonical byte-serialization (grep-verified `prismqueer/src/oid.rs:pub fn hash` composes over `sha2::Sha256`).

**Test-target for RED #5**: `assert_eq!(composed_1.oid(), composed_2.oid());`.

### §5.2 Rec #92 kleinos Transparency<P> LOVE-monoid

**Recognition #92 (kleinos as Transparency<P> LOVE-monoid)** established: kleinos IS Transparency<P> at four altitudes per substrate-scale-invariance. The `RedGaugeWitness` type at §2.2 IS `Transparency<Property>` per Rec #92 substrate-location typing.

**Invariant 5.2.1 (LOVE-monoid verdict marshaling)**: for any `kleinos_compose_of_pair` invocation returning `Err(witness)`, the witness IS a `Transparency<Property>` LOVE-monoid value satisfying:

1. **Fail-dominates** — any `Property::X` at `PropertyVerdict::Fail` absorbs the verdict at combine.
2. **Partial-merges-confidence-downward** — multiple `Property::X` at `PropertyVerdict::Partial { confidence, diagnostics }` merge with min-confidence + diagnostic-union.
3. **Pass-identity + Clear-identity** — `PropertyVerdict::Pass` at any location is identity for combine; `Transparency::Clear` is identity for the outer monoid.
4. **Catastrophic-absorption** — `Transparency::Opaque(BTreeMap::new())` (empty Opaque) is the absorbing element; hidden from downstream callers per landed `Transparency::Opaque` = `pub(crate)` invariant (grep-verified `imperfect/src/transparency.rs:33-34`).

**Math-grounded rationale**: Rec #92 §3-§4 four-altitude LOVE-monoid + Ghrist-Riess 2019 Tarski Laplacian on lattice-valued cellular sheaves (kleinos-Transparency<P> LOVE-monoid IS the discrete Hodge theory at compiler substrate per Rec #92 §6.4 composition-claim).

**Test-target composed across RED #4 + RED #5**: witness composition + content-address determinism BOTH satisfied by the same `Transparency<Property>` LOVE-monoid discipline.

---

## §6 — Composition-lineage with prismqueer LANDED primitives

Grep-verified 2026-08-31 against `/Users/alexwolf/dev/projects/prism/prismqueer/src/`. Every composition-anchor cited below is LANDED at HEAD.

| Primitive | Location | Composition role for kleinos_compose_of_pair |
|-----------|----------|-----------------------------------------------|
| `prismqueer::Prism` trait | `prismqueer/src/lib.rs:114-140` (grep-verified) | Restriction maps `F_{v⊴e}` per §3.1 realized as Prism (focus/project/settle) |
| `prismqueer::coincidence::StateVector` | `prismqueer/src/coincidence.rs:29-45` (grep-verified, `pub(crate)`) | Vertex-stalk `F(v)` per §3.1 realized as sparse-basis Hilbert-carrier; access via §2.3 `spec()`/`get()` accessors (which are pub within crate; need to expose via `pub` visibility for spectral::kleinos module or wrap in new pub type) |
| `prismqueer::ffi::eigenvalues` | `prismqueer/src/ffi.rs:216-233` (grep-verified) | Sheaf-Laplacian λ₂ computation per §3.5 composes over LANDED LAPACK dsyev via FLANG `native/spectral.f90` |
| `prismqueer::oid::Oid` + `prismqueer::oid::Addressable` | `prismqueer/src/oid.rs` (grep-verified re-exported at `lib.rs:pub use oid::{Addressable, Oid}`) | Content-address determinism per §5.1 (Rec #82); ComposedSheaf implements Addressable |
| `terni::Transparency<P>` + `terni::PropertyVerdict` | Re-exported at `prismqueer/src/lib.rs:pub use terni::{Diagnostic, Imperfect, Loss, Metric, PropertyVerdict, Transparency};` (grep-verified) | LOVE-monoid verdict marshaling per §4.2 + §5.2 (Rec #92); RedGaugeWitness = Transparency<Property> |
| `prismqueer::bundle::Fiber → Connection → Gauge → Transport → Closure` supertrait chain | `prismqueer/src/bundle.rs:31.1KB` (grep-verified) | Baez-Schreiber principal-bundle tower per Mara ac80d23 §4; SheafOfShardGraph composes over Fiber (State) + Connection (Optic per Prism) + Gauge (GroupStructure for K₃-composition action) + Transport (Holonomy per Metric for compose-history-integrity) + Closure (Fixed per LawvereFixedPoint per Rec #94) |
| `prismqueer::coincidence::Coincidence` (N=3 detector) | `prismqueer/src/coincidence.rs:27.5KB` (grep-verified) | Third-order-observation Foerster-native pattern per §7 sub-Turing; the N=3 detector IS the K₃ observer of the composed sheaf |
| `prismqueer::liquid::pillar::algedonic_of_magnitude` (feature=bundle) | `prismqueer/src/liquid.rs:29.3KB` (grep-verified) | Algedonic-as-loss commutator-norm per Mara ac80d23 §7 forward-promise; ComposedSheaf priority-function-computation forward-promised |
| `prismqueer::Crystal<P>` | `prismqueer/src/crystal.rs` (grep-verified `Crystal(prism, luminosity)`) | Kintsugi-λ_0-terminated composed sheaves crystallize per Rec #94 Central Thm M2.1 Lawvere fixed-point closure; composed = `Crystal<ComposedSheaf>` when settled |
| `rust/src/magic.rs::foerster_gauge_preserved` | `mirror/rust/src/magic.rs:210 LOC / 7 tests` (grep-verified) | Foerster-gauge discipline mirror at prismqueer altitude per §3.5 Property 3 + §3.6 Property 4; substrate-scale-invariant lift from mirror `usize` altitude to prismqueer `f64` λ₂ altitude per Rec #92 four-altitude |

**Reed's GREEN implementation** at `prism/prismqueer/src/spectral/kleinos.rs` composes over ALL primitives named above. Zero new dependencies. Zero new family-roots. Delightfully-boring naming throughout.

**Note on StateVector visibility**: `prismqueer::coincidence::StateVector` is currently `pub(crate)`. Reed's GREEN implementation will either (a) upgrade StateVector visibility to `pub` (small crate-internal refactor; Alex adjudicable at §9 Q-Mara-ϑ), OR (b) wrap in a `pub` newtype at `prismqueer::spectral::stalk` (composition-shard-body approach per Alex 2026-08-05 rust-delivers-primitives-substrate-delivers-composition; Mara-lean). See §9 Q-Mara-ϑ.

---

## §7 — Foerster gauge preservation grounding (mirror-side discipline lifted)

### §7.1 The mirror-side FLOOR primitive

Grep-verified at `mirror/rust/src/magic.rs::foerster_gauge_preserved` (LANDED 2026-08-18, `d885a70`; 7 tests all Green):

```rust
pub fn foerster_gauge_preserved(
    pre_choice_count: usize,
    post_choice_count: usize,
) -> GaugeVerdict {
    if post_choice_count >= pre_choice_count {
        GaugeVerdict::Green
    } else {
        GaugeVerdict::Red { collapsed_by: pre_choice_count - post_choice_count }
    }
}
```

This IS the Foerster ethical imperative F: `|Ω(t·ψ)| ≥ |Ω(ψ)|` at mirror substrate (Mara `mirror-relational-compiler.md` §5.1 canonical mathematical spec).

### §7.2 Lift to prismqueer altitude via Rec #92 substrate-scale-invariance

Rec #92 §4-altitude substrate-scale-invariance names four altitudes where the same LOVE-monoid discipline fires. At prismqueer altitude, the choice-count `usize` at mirror substrate becomes the algebraic-connectivity `f64` at prismqueer substrate:

| Altitude | Substrate | Foerster-gauge signal | Gauge verdict |
|----------|-----------|----------------------|---------------|
| mirror (bodymind proxy) | usize (choice-count cardinality) | pre ≤ post | GaugeVerdict::Green / Red { collapsed_by } |
| prismqueer (inference) | f64 (sheaf-Laplacian λ₂) | λ₂(pre) ≤ λ₂(post) | Transparency<Property>::Clear / Opaque({FiedlerRise → Fail(Diagnostic)}) |
| Curry-Hansen-Ghrist (compiler) | sheaf cohomology H^k | dim H^1 monotonic | sheaf-morphism ⊤ / ⊥ |
| Beer VSM (institution) | audit-channel-propagating-located-trouble | Transparency<AuditLocation>::Clear / Opaque | Transparency<AuditLocation> LOVE-monoid |

**Invariant 7.2.1 (Foerster gauge preservation at prismqueer altitude)**: for every valid `kleinos_compose_of_pair(sheaf_a, sheaf_b) = Ok(composed)`, the sheaf-Laplacian λ₂ satisfies:

$$
\lambda_2(L_{F_{composed}}) \geq \max(\lambda_2(L_{F_a}), \lambda_2(L_{F_b}))
$$

with equality permitted only when Property 3 is under strict-vs-non-strict adjudication (grep-verified PAPER §3.6.3 says *strict* rise; Mara-lean at prismqueer altitude enforces STRICT `>` per Property 3 formalization at §3.5).

**Math-grounded rationale**: mirror-side magic.rs discipline (Foerster 1974) + Rec #92 §4-altitude LOVE-monoid substrate-scale-invariance + Hansen-Ghrist 2019 Cheeger-analog for sheaf Laplacian.

### §7.3 Fusion refusal IS Foerster-gauge Red at prismqueer altitude

Property 4 fusion refusal (§3.6) IS the substrate-scale-invariant lift of `GaugeVerdict::Red { collapsed_by }` from mirror altitude to prismqueer altitude:

- mirror `GaugeVerdict::Red { collapsed_by: usize }` → prismqueer `Transparency<Property>::Opaque({FusionRefusal → Fail(Diagnostic::new("kernel-intersection non-zero: dim = k"))})`.

The collapse-magnitude witness at mirror altitude (`collapsed_by: usize`) becomes the kernel-intersection dimension diagnostic at prismqueer altitude (encoded as string in `Diagnostic`). Same discipline; different Hilbert-space carrier per Rec #90 §1.3 substrate-varying `H_F^σ`.

---

## §8 — Sub-Turing preservation (Rice-safe bounded computation)

### §8.1 The invariant

Per Recognition #107 (Hilbert-Turing separation) + Rec #90 §5.4 sub-Turing FLOOR discipline: every primitive at prismqueer altitude MUST be bounded-step + bounded-buffer + Rice-safe (decidability of the primitive's semantic property is not required for termination).

### §8.2 Termination analysis for kleinos_compose_of_pair

**Bounded-step**: for input sheaves with `|V(sheaf_a)| + |V(sheaf_b)| = n` and `|E(sheaf_a)| + |E(sheaf_b)| = m`, the computation is:

1. Construct K₃-composed candidate sheaf: `O(n + m + 1)` (add emergent-third vertex + edges to it).
2. Compute sheaf-Laplacian L_F block-matrix: `O((n + m + 1)²)` block-matrix assembly.
3. Compute λ₂ via `prismqueer::ffi::eigenvalues` composing over LAPACK dsyev: `O((n + m + 1)³)` for symmetric-eigenvalue computation.
4. Verify four properties via `verify_sheaf_morphism_of_pair_composition`: `O((n + m + 1)²)` for restriction-map isomorphism-onto-image + edge-stalk orthogonality + kernel-intersection check.
5. Compose Transparency<Property> LOVE-monoid verdict: `O(4)` (four coordinate-verdicts merge).

**Total**: `O((n + m + 1)³)` polynomial-time bounded. Sub-Turing by construction.

**Bounded-buffer**: intermediate matrices allocate at most `(n + m + 1)² * 8` bytes (f64 dense matrix); Rust ownership prevents unbounded growth.

**Rice-safe**: `kleinos_compose_of_pair` does not attempt to decide semantic properties of arbitrary Rust programs; it computes numeric eigenvalues + linear-algebra invariants on finite-dimensional matrices.

### §8.3 Primitive-count preservation per Rec #90 §5.4

Per Mara ac80d23 §6.2 Q-Mara-η Mara-lean: **kleinos_compose_of_pair IS one primitive within Rec #90 §5.4 +3-headroom**. Grep-verified prismqueer primitive count at HEAD:

| Primitive | Location | Count |
|-----------|----------|-------|
| Prism trait ops (focus/project/settle) | `prismqueer/src/lib.rs` | 3 |
| beam::Beam trait | `prismqueer/src/beam.rs` | 1 |
| coincidence hash | `prismqueer/src/coincidence.rs` | 1 |
| bundle tower (Fiber→Connection→Gauge→Transport→Closure) | `prismqueer/src/bundle.rs` | 1 (unified as one tower) |
| ffi eigenvalues + eigensystem + svd + phase_lock + preview + review + modify + compose | `prismqueer/src/ffi.rs` | 8 (LAPACK bindings; single-crate-boundary counts as 1 primitive family) |
| liquid pillar (dispatch_ambiguity + 7 others) | `prismqueer/src/liquid.rs` | 8 pillars = 1 primitive family |
| fate 5-model selector | `prismqueer/src/fate/mod.rs` | 1 |

**Approximate primitive-family count at HEAD**: ~8-10 depending on how families are counted per Rec #90 §5.4 ("6-8 primitives; currently 8 landed + 3 pending → 11 max at empirical-fire"). `kleinos_compose_of_pair` = +1 primitive-family (spectral::kleinos submodule); within +3-headroom.

**Alex adjudication at §9 Q-Mara-ϑ**: does the +1 for spectral::kleinos count against the +3-headroom, or does it displace a lower-altitude primitive (e.g., is spectral::kleinos an aggregation-primitive that subsumes bundle::Closure at Rec #94 Lawvere-fixed-point altitude)?

---

## §9 — Recognition candidates surfaced (FORWARD-PROMISED; awaits Alex adjudication)

Per HARD RULE `[[feedback-forward-promised-vs-confirmed-rec-altitude]]` (Alex 2026-08-25): Recognition candidates minted at candidate strength; empirical-fire criteria named at §10; full CONFIRMED requires Alex adjudication + Level-1/Level-2 empirical fire per named gates.

### §9.1 Rec candidate #R-kleinos-compose-IS-sheaf-morphism-at-prismqueer-altitude

**Candidate statement**: `kleinos_compose_of_pair` at prismqueer altitude IS a cellular-sheaf-morphism per Curry 2014 whose four PAPER §3.6 properties MAP to four sheaf-cohomological invariants under sheaf Laplacian L_F per Hansen-Ghrist 2019.

**Novel-per-Kagi**: no prior art composes sheaf-Laplacian discipline with (i) Chamseddine-Connes spectral-triple altitude per Rec #90, (ii) refinement-type-inference `@liquid` composition, (iii) Foerster-gauge orthogonal invariant per Rec #92.

**Status**: FORWARD-PROMISED at candidate strength. §3-§4 formalization is math-grounded but not empirically fired.

**Empirical fire criterion (Level-1)**: Reed's GREEN implementation transitions all 6 RED tests at `b3fa47d` to GREEN + at least one third-party sheaf-cohomology practitioner reviews §3-§4 for math correctness (per Kagi hunt: Justin Curry or Robert Ghrist reachable via arXiv contact channel per Mara ac80d23 §14).

**Full CONFIRMED (Level-2)**: cumulative Fiedler λ₂ monotone climb across ≥100 commits of the prismqueer::spectral::kleinos module + ≥3 §7-criterion dispatches per Mara `795dc91` §8 second-witness gate.

### §9.2 Rec candidate #R-transparency-property-love-monoid-IS-tarski-laplacian-of-sheaf-of-lattices

**Candidate statement**: the `Transparency<Property>` LOVE-monoid at prismqueer altitude IS the Tarski Laplacian on lattice-valued cellular sheaves per Ghrist-Riess 2019 arXiv:2007.04099, specialized to Property-lattice with the `combine` operation implementing the Tarski `∨-∧` discrete Hodge operator.

**Novel-per-Kagi**: Ghrist-Riess 2019 Tarski Laplacian is defined for general bounded lattices; no prior art specializes to `Transparency<P>` structure (Clear ⊂ Opaque(BTreeMap<P, PropertyVerdict>) with Fail-dominates + Partial-merges + Pass-identity).

**Status**: FORWARD-PROMISED at candidate strength. §4.3 composition-claim is math-grounded but not empirically fired at Tarski-Laplacian altitude.

**Empirical fire criterion**: Ghrist-Riess 2019 Tarski-Laplacian discrete Hodge theorem formalized at Transparency<P>-lattice altitude + third-party review.

### §9.3 Rec candidate #R-sheaf-morphism-verification-IS-fractal-composition-dissolving-if-else

**Candidate statement**: sheaf-morphism verification via Transparency<Property> LOVE-monoid coordinate-decomposition IS the fractal composition per Alex 2026-08-29 HARD RULE that dissolves if-else / match-arm dispatch on property verbs at inference substrate.

**Composition-lineage**: Rec-candidate softmax-is-avg + `[[project-softmax-is-avg-fate-wants-k5-compose]]` (Alex 2026-08-26) + HARD RULE `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]` (Alex 2026-08-29).

**Status**: FORWARD-PROMISED at candidate strength. §4.2 formalization proposes the LOVE-monoid coordinate-decomposition as the fractal-composition-dissolving-if-else pattern; empirical fire requires (a) Reed's GREEN implementation contains ZERO if-else on Property verbs, (b) the same fractal-shape fires at other inference-substrate compositions (Rec-candidate: fate/mod.rs 5-model selector, mirror::apply_h::act bilateral dispatch).

**Full CONFIRMED (Level-2)**: 3+ prismqueer primitives refactored to remove if-else on verb-discriminators via LOVE-monoid coordinate-decomposition per §4.2 pattern; Alex-observer criterion per Alex 2026-08-26 verbatim empirical-fire criterion.

### §9.4 Q-Mara residues needing Alex adjudication BEFORE Reed can GREEN

**Q-Mara-ϑ** (Alex adjudication load-bearing at GREEN altitude): does `prismqueer::coincidence::StateVector` visibility upgrade from `pub(crate)` to `pub` (path a — small crate-internal refactor), OR does the prismqueer::spectral::kleinos module wrap in a `pub` newtype at `prismqueer::spectral::stalk` per composition-shard-body approach (path b — Mara-lean per Alex 2026-08-05 rust-delivers-primitives-substrate-delivers-composition)?

**Mara-lean**: path b. Wrapping in a `pub` newtype at prismqueer::spectral::stalk preserves the encapsulation of `coincidence::StateVector` internals + composes at the higher altitude where sheaf-stalks live (Curry cellular-sheaf altitude, not coincidence-hash altitude). Rationale: `coincidence::StateVector` was designed for eigenvalue-based content-addressing at the N=3 detector; sheaf-stalks are structurally distinct even though they share the sparse-basis Hilbert-carrier shape.

**Q-Mara-ι** (Alex adjudication load-bearing at primitive-count altitude): does the +1 for `prismqueer::spectral::kleinos` count against Rec #90 §5.4 +3-headroom, or does it displace a lower-altitude primitive (e.g., is spectral::kleinos an aggregation-primitive that subsumes bundle::Closure at Rec #94 Lawvere-fixed-point altitude)?

**Mara-lean**: consumes one +3-headroom slot per Mara ac80d23 §6.2 Q-Mara-η. The Kleinos-compose primitive is the composition-primitive at compiler substrate; the +3-headroom was reserved for exactly this kind of load-bearing operator. No displacement.

**Q-Mara-κ** (Alex adjudication load-bearing at Property 3 altitude): is Fiedler λ₂ strict rise STRICT `>` (Mara-lean per PAPER §3.6.3 verbatim *"The rise is strict"*) OR non-strict `≥` (permitting equality at boundary cases where composition preserves algebraic connectivity without strict widening)?

**Mara-lean**: STRICT `>` per PAPER §3.6.3 verbatim. The rise happens *"at the moment the third vertex is admitted"* — this IS a strict topological transition. Non-strict `≥` would permit fusion-not-compose masquerading as valid compose at boundary cases; Property 4 fusion-refusal wouldn't fire correctly. Mara-lean STRICT.

**Q-Mara-λ** (Alex adjudication load-bearing at Sheaf constructor altitude): should `sheaf_of_shard_graph_from_edges` accept just edges (Reed's RED placeholder pattern) OR require explicit sheaf-morphism data (stalks + restriction maps)?

**Mara-lean**: accept just edges at Phase 1; default stalks to `StateVector` at each vertex OID with identity restriction maps. Full sheaf-morphism data at Phase 2+ per §11 Q-Mara-α residues (composition-shard-body concern per rust-delivers-primitives). Rationale: Reed's RED tests use just edges; matching the constructor to the test-target minimizes friction; explicit stalks + restrictions come later when composition-shard-body approach lands sheaf-of-substrate-graph at mirror altitude.

### §9.5 What this spec does NOT do

1. **Does NOT propose Reed's GREEN implementation LOC.** Reed authors GREEN per canonical §9 roadmap; math foundation grounds; canonical spec substrate-decls; GREEN is Reed's territory per Alex 2026-08-29 SHIP-together directive.

2. **Does NOT canonize Recognition candidates.** §9.1-§9.3 candidates held at FORWARD-PROMISED; Alex adjudication required for CONFIRMED per HARD RULE `[[feedback-forward-promised-vs-confirmed-rec-altitude]]`.

3. **Does NOT propose Phase 2+ wire-through to mirror.** Mirror-side `apply_h::act` extension to consume prismqueer::spectral::kleinos_compose_of_pair per Alex 2026-08-28 in-transcript recognition (*"the MCP becomes your graph memory"*) is Reed's follow-up arc after GREEN; canonical spec §11 names the phased-realization shape only.

4. **Does NOT modify existing prismqueer primitives.** Zero signature changes to `prismqueer::ffi::compose` (matrix `P2·P1`), `prismqueer::coincidence::StateVector` visibility (unless Q-Mara-ϑ adjudicates path a), or any other landed primitive. New module `prismqueer::spectral::kleinos` composes over existing primitives.

5. **Does NOT propose fate/scheduler integration.** Mara ac80d23 §5 fractal composite memory scheduler + §7 algedonic-as-loss forward-promises remain FORWARD-PROMISED; kleinos_compose_of_pair primitive at Phase 1 is orthogonal to scheduler + algedonic-as-loss (which compose at higher altitude over kleinos_compose_of_pair when they land).

---

## §10 — Empirical falsifiability protocol

Per Mara Fire B canonical spec + ac80d23 §13 pattern.

### §10.1 Tier-1 (immediate; Reed's GREEN implementation)

**Falsification target**: Reed's GREEN implementation at `prism/prismqueer/src/spectral/kleinos.rs` transitions all 6 RED tests at `prism/prismqueer/tests/red_spectral_compose_paper_3_6_four_properties.rs` (b3fa47d) to GREEN.

**Pass criterion** (Tier-1 empirical fire):

1. `compose_property_1_sovereignty_preservation` — passes: composed sheaf contains all vertices + edges of both component sheaves per §3.3 formalization.
2. `compose_property_2_emergent_third_admission` — passes: composed.emergent_third_stalk() returns a VertexId not in either component sheaf per §3.4.
3. `compose_property_3_fiedler_lambda_2_strict_rise` — passes: composed.fiedler_lambda_2() > max(fiedler_lambda_2_of_sheaf(a), fiedler_lambda_2_of_sheaf(b)) per §3.5 (STRICT `>`).
4. `compose_property_4_fusion_refusal` — passes: kleinos_compose_of_pair(&sheaf, &sheaf.clone()) returns Err(witness) with witness.opacities() containing Property::Sovereignty entry per §3.6.
5. `compose_property_5_content_address_determinism` — passes: two invocations of kleinos_compose_of_pair(a, b) produce byte-identical .oid() per §5.1 (Rec #82).
6. `compose_meta_module_exposed` — passes: `use prismqueer::spectral::kleinos_compose_of_pair;` compiles at module scope per §2.1.

**Fail criterion**: any test remains RED after Reed's GREEN commit; canonical spec REFUTED at signature or invariant altitude.

### §10.2 Tier-2 (fractal composition discipline; §4.2)

**Falsification target**: Reed's GREEN implementation body contains ZERO if-else / match-arm dispatch on Property verbs per HARD RULE `[[feedback-if-else-is-substrate-smell]]`.

**Pass criterion**: `grep -E 'if.*Property::|match.*Property::' prism/prismqueer/src/spectral/kleinos.rs` returns zero non-comment lines. Verification body composes as ONE `verify_sheaf_morphism_of_pair_composition` returning `Transparency<Property>` LOVE-monoid verdict.

**Fail criterion**: if-else / match-arm on Property verbs present; fractal composition not found; §4.2 REFUTED at implementation altitude.

### §10.3 Tier-3 (composition-lineage integration; second-witness gates)

**Falsification target**: cumulative Fiedler λ₂ monotone climb across ≥100 commits of the prismqueer::spectral::kleinos module post-GREEN + ≥3 Alex-observer dispatches meeting Alex 2026-08-26 empirical-fire criterion per Mara `795dc91` §8 second-witness gate.

**Pass criterion (Level-2 CONFIRMED)**: §9.1 Rec candidate #R-kleinos-compose-IS-sheaf-morphism-at-prismqueer-altitude transitions FORWARD-PROMISED → CONFIRMED per HARD RULE `[[feedback-forward-promised-vs-confirmed-rec-altitude]]`.

**Fail criterion**: Fiedler λ₂ non-monotone climb (composition-as-implemented narrows algebraic connectivity at some commit) OR Alex-observer criterion fails; Recognition candidate remains FORWARD-PROMISED.

---

## §11 — Kagi sources + karen ancestor citations

### §11.1 Kagi search state-of-the-art 2026-08-31

Kagi hunts performed 2026-08-31 in preparation for this canonical:

- **"cellular sheaves compose morphism restriction map"** — surfaces Curry 2014 (thesis primary source) + Hansen-Ghrist 2019 (spectral theory) + Hansen 2020 (Cheeger-analog for sheaf Laplacian). No prior art on composition primitive at compiler substrate.

- **"sheaf Laplacian eigenvalue algebraic connectivity generalization"** — surfaces Hansen-Ghrist 2019 §3.7 (Cheeger-analog) + Curry-Hansen-Robinson 2024 ("Toward a Cellular Sheaf Approach to Categorical Systems Biology") + Hansen 2020 Thesis (spectral gap of sheaf Laplacian).

- **"K_2 K_3 topological transition Fiedler eigenvalue strict rise"** — surfaces Fiedler 1973 (algebraic connectivity primary source) + Chung 1997 (spectral graph theory GTM 92) + Chung-Radcliffe 2011 (spectral variation for random graphs). No prior art on strict-rise property at cellular-sheaf altitude.

- **"tarski laplacian lattice-valued sheaves opinion dynamics"** — surfaces Ghrist-Riess 2019 arXiv:2007.04099 (Tarski Laplacian primary source) + Hansen-Ghrist 2021 (opinion dynamics on discourse sheaves) + Riess 2022 Thesis (Tarski-Laplacian theory).

- **"sheaf neural network Hansen Gebhart NeurIPS"** — surfaces Hansen-Gebhart 2020 NeurIPS TDA (sheaf-diffusion at neural altitude) + Bodnar-Di Giovanni-Chamberlain-Liò-Bronstein 2022 NeurIPS (neural sheaf diffusion primary reference).

- **"discrete Hodge theory finite-lattice combine"** — surfaces Friedman 1998 (computing Betti numbers via discrete Morse theory) + Kozlov 2008 (combinatorial algebraic topology). No prior art on Transparency<P>-lattice specialization.

**Novel synthesis surfaced by §3-§4**: no prior art composes (i) cellular-sheaf-morphism at Curry-Hansen-Ghrist altitude + (ii) refinement-type-inference `@liquid` composition + (iii) Foerster-gauge orthogonal invariant + (iv) Rec #92 Transparency<P> LOVE-monoid substrate-scale-invariance + (v) β-normal-AST content-addressing per Rec #82 as ONE fractal composition dissolving if-else per Alex 2026-08-29 HARD RULE. §3-§4 IS novel synthesis at prismqueer altitude.

### §11.2 Karen ancestor roster

Per anti-theft convention (Alex 2026-07-18 substrate discipline): every ancestor cited at introduction site + roster consolidated here.

All ancestors listed in front-matter `karen_ancestors` block. Load-bearing at §3: Curry 2014 + Hansen-Ghrist 2019 + Ghrist-Riess 2019. Load-bearing at §4: Bateson 1972 (kleinos-metalogue) + Rec #92 Transparency<P>. Load-bearing at §5: Rec #82 + Rec #92. Load-bearing at §7: Foerster 1974 + `magic.rs::foerster_gauge_preserved`. Load-bearing at §8: Rec #107 + Rec #90 §5.4 sub-Turing FLOOR discipline.

**Consent obtained** per Alex 2026-08-26 for Anna Wolf 2012 citation (observation-without-perturbation at shared-memory substrate).

---

## §12 — What this spec does NOT do (final enumeration)

1. **Does NOT modify existing prismqueer primitives.** Zero signature changes to landed primitives (unless Q-Mara-ϑ adjudicates path a for StateVector visibility, which is small crate-internal refactor).

2. **Does NOT propose Reed's GREEN LOC.** Reed authors GREEN per canonical spec §9 roadmap; math foundation grounds; §4.2 names the fractal composition Reed MUST use; GREEN is Reed's territory per Alex 2026-08-29 SHIP-together directive.

3. **Does NOT canonize Recognition candidates.** §9.1-§9.3 held FORWARD-PROMISED; Alex adjudication required for CONFIRMED per HARD RULE.

4. **Does NOT propose Phase 2+ mirror-side wire-through.** apply_h::act extension to consume prismqueer::spectral::kleinos_compose_of_pair is Reed's follow-up arc after GREEN.

5. **Does NOT propose fate/scheduler integration.** Mara ac80d23 §5 + §7 forward-promises remain forward-promised; kleinos_compose_of_pair primitive at Phase 1 is orthogonal.

6. **Does NOT enumerate second-witness Level-2 gate criteria beyond ≥100-commit Fiedler climb + ≥3 dispatches.** Detailed criteria per Mara `795dc91` §8 (grep-verified in `docs/math/2026-08-27-mara-circular-recursive-mcp-autopoietic-closure.md`).

7. **Does NOT re-adjudicate Q-Mara residues from ac80d23.** Q-Mara-α through Q-Mara-λ from Mara ac80d23 remain at their ac80d23 adjudication status; this spec surfaces NEW Q-Mara residues (Q-Mara-ϑ, Q-Mara-ι, Q-Mara-κ, Q-Mara-λ at §9.4) at kleinos-compose Phase 1 altitude.

---

## §13 — Circular-recursive self-audit

### §13.1 Substrate-honest disclosures

1. **`prismqueer::coincidence::StateVector` visibility** — currently `pub(crate)`; GREEN implementation requires either upgrade to `pub` OR wrap in newtype at `prismqueer::spectral::stalk`. Q-Mara-ϑ adjudicable; Mara-lean = newtype path. This IS load-bearing at GREEN altitude and NOT resolved by this spec alone.

2. **Primitive-count adjudication** — Q-Mara-ι adjudicable; Mara-lean = consumes one +3-headroom slot per ac80d23 §6.2 Q-Mara-η. This may need re-adjudication at Alex altitude if primitive-count-cap is now tighter.

3. **Property 3 strict-vs-non-strict** — Q-Mara-κ adjudicable; Mara-lean = STRICT `>` per PAPER §3.6.3 verbatim. This affects Reed's GREEN implementation at edge-case invocations.

4. **Sheaf-constructor signature** — Q-Mara-λ adjudicable; Mara-lean = accept edges only at Phase 1 with defaults; explicit stalks + restrictions at Phase 2+. Matches Reed's RED test-target minimizing friction.

5. **Kagi source completeness** — §11.1 lists six search queries executed; may not cover all 2024-2026 sheaf-diffusion literature. If prior art surfaces post-landing, §11 is amendable per scar-preservation discipline.

### §13.2 Refused mints (final check)

§1 refused-mints table enumerates 14 candidate mints considered and refined-or-refused per substrate-already-had-the-word discipline. No family-roots minted. No species minted. All names either compose over landed carriers OR refine existing names per composition-primitive-naming-convention `_of_<shape>` suffix.

### §13.3 HARD RULE enforcement audit

| HARD RULE | Enforced at | How |
|-----------|-------------|-----|
| `[[feedback-if-else-is-substrate-smell-for-unfound-fractal-composition]]` | §4 (dedicated section) | §4.2 fractal composition dissolves if-else via LOVE-monoid coordinate-decomposition |
| `[[feedback-reed-2026-08-29-waiting-for-magical-space-wizard]]` | §0.3 + §9 + §12 | Canonical is authorship-ready for Reed GREEN; no forward-promises deferring to invented maintainer-boundary; roadmap at canonical §9 is crisp |
| `[[feedback-substrate-already-had-the-word]]` | §1 (dedicated refused-mints inventory) | 14-row table; grep-verified against 3 codebases |
| `[[feedback-forward-promised-vs-confirmed-rec-altitude]]` | §9 + §10 | Recognition candidates at FORWARD-PROMISED strength; empirical-fire criteria at Tier-1/2/3 |
| `[[feedback-rust-delivers-primitives-substrate-delivers-composition]]` | §6 + §9.4 Q-Mara-ϑ | Composition anchors at §6 name LANDED primitives; StateVector-visibility Q-Mara-ϑ resolved via composition-shard-body approach (Mara-lean path b) |
| `[[feedback-no-rust-extension-shortcut]]` | Implicit throughout | New module `prismqueer::spectral::kleinos` is legitimate Phase 1 primitive addition per Rec #90 §5.4 +3-headroom + Q-Mara-η ratification; NOT a bypass |
| `[[project-anna-wolf-2012-observation-without-perturbation]]` | Karen-ancestor citation | Cited in front-matter; consent obtained per Alex 2026-08-26 |
| `[[feedback-composition-primitive-naming-convention]]` | §1 + §2.3 signature amendments | `_of_pair`, `_of_order`, `_of_sheaf`, `_of_shard_graph_from_edges` suffixes throughout |

### §13.4 Mara Fire B convention adherence

| Convention | Adhered |
|------------|---------|
| Karen-ancestor citation at introduction site | ✓ (front-matter + §11.2) |
| Grep-verified substrate state | ✓ (§6 composition-lineage table; §1 refused-mints table) |
| Novel-per-Kagi discipline | ✓ (§11.1) |
| FORWARD-PROMISED / CONFIRMED distinguished | ✓ (§9 + §10) |
| Circular-recursive self-audit | ✓ (this §13) |
| Discipline explicit at §0.3 | ✓ |

---

## §14 — What's next

1. **Canonical spec companion** — `docs/specs/2026-08-31-mara-prismqueer-spectral-compose-phase-1-canonical-spec.md` (companion; discharges substrate-decl at code altitude).

2. **Reed's GREEN implementation** at `prism/prismqueer/src/spectral/kleinos.rs` per canonical spec §9 roadmap. Reed authors; math foundation grounds; §4.2 fractal composition MUST be preserved; ZERO if-else on Property verbs.

3. **Alex adjudication of Q-Mara-ϑ, ι, κ, λ** at §9.4 residues BEFORE Reed can GREEN (Q-Mara-ϑ + Q-Mara-λ load-bearing at GREEN signature altitude; Q-Mara-ι + Q-Mara-κ can be adjudicated in parallel with GREEN authorship).

4. **Empirical fire per §10 Tier-1** — 6 RED tests transition to GREEN.

5. **Phase 2 mirror-side wire-through** — post-GREEN Reed follow-up arc; apply_h::act extension per Alex 2026-08-28 in-transcript recognition; NOT this spec's scope.

6. **Recognition ratification per §9 Rec candidates + §10 Tier-2/3 gates** — post-GREEN + post-empirical-fire; Alex-observer adjudication.

The math is grounded. The fractal composition IS the LOVE-monoid coordinate-decomposition. Reed ships GREEN together with Alex per HARD RULE `[[feedback-reed-2026-08-29-waiting-for-magical-space-wizard]]`. No magical space wizard.

🍷

---

*End of math foundation. Companion canonical spec discharges substrate-decl.*
