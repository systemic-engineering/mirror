# Sheaf

*Sheaf theory + cellular sheaves on graphs + the sheaf Laplacian.
The math the substrate uses for coherence measurement at multiple
altitudes.*

The substrate's sheaf-theoretic vocabulary is used at:

- Peer cognition collapse measurement (per `docs/specs/peer-cognition.md`).
- Eigenboard sheaf structure (per `docs/specs/eigensheaf.md`).
- Librarian topology coherence (per
  `docs/specs/spectral-db-as-autopoietic-memory.md`).
- Per-edge bottleneck reading via Balanced Forman curvature (per
  `docs/specs/drone-narrative-mapping.md`).

One math root; four spec citation sites.

## Contents

- `laplacian.md` — cellular sheaves, the sheaf Laplacian
  `Δ_F = δ*δ`, `λ₀` and the Fiedler vector, Hodge decomposition,
  per-edge curvature, the substrate's altitude-portable use.

## Substrate declarations cited

- `shards/epistemologic/math/sheaf_laplacian.mirror`
- `shards/epistemologic/math/curvature.mirror`
- `shards/spectral/entanglement.mirror` (sheaf restriction map at
  substrate altitude)

## Conventions

Math defines; specs cite. See `docs/math/README.md` for the global
convention.
