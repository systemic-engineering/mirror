# Seam Phase D — Arc 6 TICK 4: `shards/song/movement.mirror` species

*Reed-inline execution.*

**Commit under review**: `4efbf16` (Mara GREEN) — third `@song` species; first
consolidation-shape species (no new `song_settles` sub-predicates).

**Reed RED**: `710352e` (15 tests). **Test result**: 15/15 pass; adjacent-suite
safety 60/60 across all four @song shard test binaries. Tree clean.

---

## §1. Verdict

**RATIFY.**

All 15 species-altitude witnesses land clean. StageFreight cascade binding
via `@io/stagefreight` + all five stages (audition/perform/review/publish/
narrate) cited as substrate-decl vocabulary. Bounded frame-shift discipline
grounded with sonata-form (Haydn/Mozart/Beethoven exposition/development/
recapitulation) + Bateson frame-anchor prior art. Psychohistorical binding
via epoch / OBC / regulation stock vocabulary. `song_settles` closure
acknowledged at species altitude; no new sub-predicates added; movement
CONSUMES the composed verdict at `close`.

**Species contract landed**: `enter(m: song_movement, ...)` + `close(m: song_movement,
...)` per family-root song.mirror species-roster. `#S3 five-op temporal
specialisation` (LANDED `10c34cf`) inherited + consolidated.

## §2. 15/15 empirical verify

T1-T5 canonical shape + Interpretation B + species-pact (`in @song`); T6
cites `@io/stagefreight`; T7 cites at least 3 of 5 cascade stages (all 5
landed per Mara summary); T8 declares `enter(m: ...)`; T9 declares `close(m:
...)`; T10 grounds bounded frame-shift narrative; T11 cites sonata form or
symphonic prior-art; T12 cites Bateson / frame.mirror / logical type / Learning
II/III; T13 cites psychohistorical epoch binding; T14 acknowledges song_settles
closure at TICK 3; T15 cites `#S3` / five-op temporal marker.

All green. Adjacent safety: `song_family_root_shard` (15/15) +
`song_progression_shard` (15/15) + `song_voice_shard` (15/15) +
`song_movement_shard` (15/15) = 60/60. No regression.

## §3. Mara Seam-worthy observations — adjudication

### Observation 1: StageFreight cascade ⇔ sonata form structural isomorphism

**Verdict: SUBSTRATE-FACT WORTH NAMING. Recognition candidate flagged;
Surface-to-Alex for adjudication.**

Mara observes: `audition / perform / review / publish / narrate` (StageFreight
wire cascade) IS structurally the SAME discipline as `exposition / development /
recapitulation` (sonata-form symphonic cascade). Both are bounded-frame-shift
disciplines at their respective altitudes:

- **StageFreight altitude (wire)**: audition (material presented) → perform
  (execution) → review (verdict) → publish (closure) → narrate (wire output).
- **Sonata altitude (symphonic)**: exposition (themes presented) → development
  (execution + transformation) → recapitulation (return + verdict on the
  themes).

The first three stages map cleanly: audition⇔exposition, perform⇔development,
review⇔recapitulation. Publish + narrate extend to the coda + performance-
reception altitude that sonata form leaves implicit.

**This IS a substrate-pull-notable cross-altitude isomorphism.** Same discipline
read at wire altitude AND at symphonic altitude — the substrate had been
carrying the bounded-frame-shift discipline in two dialects. Movement is
where the substrate reads them as the same shape.

**Signal-to-Alex**: this candidate deserves a recognition tag — provisional
`#S4` (cascade-shape altitude-portable at wire + symphonic altitudes). Reed
lean: FLAG as CANDIDATE; not promote in this session; require Alex
adjudication on: (a) whether `#S4` is genuinely novel or a specialization
of `#26` (`shift(oid, T)` altitude-portable) at cascade-altitude; (b) whether
the naming should be `@song/cascade` species OR a substrate-pull recognition
at family-root-composition altitude. TICK 5 `@song/narrative` may absorb this
as it names the wire↔symphonic isomorphism at the wire↔psychohistory
altitude.

### Observation 2: Movement's actions trace Bateson Learning I→II→III→IV

**Verdict: WITNESS OF #42, not new recognition.**

Mara maps:
- `enter` at Level I (frame-entry; observing within the frame)
- `perform` / `review` at Level II (frame acknowledged; learning within it)
- `close` at Level III (the frame CHANGES through its own closure event;
  frame-of-frame discipline)
- `narrate` at Level IV (multi-frame coupling → multi-movement symphony)

**This IS Recognition #42 (Bateson logical-type primitive; PROMOTED
2026-06-08) applied at movement's species altitude.** Not a new tag; direct
witness of #42 at temporal-bounded-frame altitude. Absorb into #42's
altitude-portable roster.

**Signal-to-Alex**: no action required. Adds a witness to #42's roster;
catalog-only.

### Observation 3: Consolidation-shape species pattern

**Verdict: NAMEABLE species-discipline pattern, but scope requires more
witnesses. FLAG for post-Arc 6 review.**

Mara flags: this is the FIRST species within `@song` that adds ZERO
sub-predicates. The species CONSUMES the family-root's already-discharged
composed bilateral (`song_settles`) at its own action (`close`), rather than
DECOMPOSING it into new sub-predicates.

This IS a distinct species-discipline shape from the decomposition species
(`@song/progression` + `@song/voice` both declared sub-predicates for
`song_settles`). Naming: **consolidation-species** (consumes composed verdict)
vs **decomposition-species** (declares sub-predicates).

**Signal-to-Reed**: not a new recognition tag; too early to promote. Wait for
TICK 5 `@song/narrative` + TICK 6 `@song/phrase` to see if consolidation-shape
recurs. If 2+ additional consolidation-species land in Arc 6, promote to
CANDIDATE at Arc 6 close. If not, catalog-only for future post-Arc 6
reflection.

## §4. Adjacent-suite safety verified

60/60 across four test binaries. No test regressions. Species
composition landscape:

- `@song/progression` (`54ff1e8`): decomposition-species; discharges 2/3
  `song_settles` sub-predicates.
- `@song/voice` (`cc5a440`): decomposition-species; discharges 3/3
  `song_settles` sub-predicates (closing).
- `@song/movement` (`4efbf16`): **consolidation-species**; consumes composed
  verdict at `close`; no new sub-predicates.

## §5. Signal-to-Reed

**Arc 6 TICK 4 CLOSED.** GREEN `4efbf16` ratified; 15/15 pass; adjacent 60/60;
tree clean.

**Arc 6 remaining**:

- **TICK 5**: `shards/song/narrative.mirror` — psychohistory + wire binding.
  §8 isomorphism substrate-decl. Absorb TICK 2 boundary-overlap observation.
  Potential #S1 absorption path (Reed lean per TICK 3 audit §5). Potential
  `#S4` (cascade-shape altitude-portable) absorption if narrative names the
  wire↔symphonic isomorphism explicitly.
- **TICK 6**: `shards/song/phrase.mirror` — atomic unit; OBC binding closure.

**Cascade forward-promises unchanged**: Arc 7 `@kintsugi/song` audit loop;
Arc 8 `@kintsugi/song/cargo` cargo-test migration; Arc 9 `@mirror/store/git`
commit-as-fold; Arc 10 endpoint compiler emits docker + StageFreight delivery
e2e.

**Signal-to-Alex on #S4 candidate**: flagged for Pack adjudication. Not
promoted this session. TICK 5 may absorb organically; otherwise Alex decides
whether it deserves standalone tag OR absorption into #26 altitude-portable
shift.

---

*2026-07-06. Seam (Reed-inline). Phase D on Arc 6 TICK 4 `4efbf16` RATIFIED.
@song/movement species landed as first consolidation-species (no new
sub-predicates; consumes composed verdict at `close`). StageFreight cascade
binding + sonata-form prior-art surfaced provisional `#S4` cross-altitude
cascade-shape isomorphism (FLAG-only; Alex adjudication pending). #42 Bateson
altitude-portable roster gains temporal-bounded-frame witness. Ouroboros
cascade tick 4/N advancing to TICK 5 narrative.*
