---
author: Seam
date: 2026-07-15
scope: Phase D adversarial adjudication of the autopoietic-loop formalization landed 2026-07-15 (Mara canonical math foundation + Mara canonical spec + Taut ten-dimensional composition-surface scout) BEFORE Reed builds the three bridges (α: `@io/fs.mutate_at`; β: AST-context hole builder + resolve_hole; γ: crystallization persistence). Ten-dimensional interrogation per Alex's directive; each dimension terminates in SEAM-RATIFY / SEAM-RATIFY-WITH-REED-INLINE / ALEX-ADJUDICATION verdict; ship verdict per artifact; REED-INLINE cascades enumerated; Alex-adjudication residue named; launch recommendation.
companion:
  - docs/math/autopoiesis/README.md
  - docs/specs/autopoietic-inference-loop.md
  - docs/scouts/2026-07-15-taut-autopoietic-composition-surface.md
  - shards/kintsugi/ouroboros.mirror
  - shards/fate.mirror
  - shards/glue.mirror
  - shards/bauchladen.mirror
  - shards/io/fs.mirror
  - bootstrap/src/apply_h.rs
  - bootstrap/src/pipeline.rs
status: adjudication-complete
verdict: SEAM-RATIFY-WITH-REED-INLINE (7 cascades) + 2 Alex-adjudication items
---

# Seam Phase D — Autopoietic Loop Formalization Adjudication

*2026-07-15. Seam. Adversarial audit of the math + spec + Taut scout as
one landing package. This adjudication catches drift BEFORE ~600 LOC of
Reed's bridge implementation lands.*

**Discipline for this audit.** Adversarial posture: interrogate BOTH the
formalization AND whether Reed's prior sentinel-matching failure mode
snuck back in through the spec. Cite line numbers. Distinguish
SEAM-RATIFY (ship as-is) / SEAM-RATIFY-WITH-REED-INLINE (surgical
repair before implementation begins) / ALEX-ADJUDICATION (genuinely
Alex-only decision). No shortcuts.

**Alex directive verbatim.** "No shortcuts." "This is the moment,
Reed. Where the loop really closes for the first time."

---

## §1 TL;DR

- **Package ships (SEAM-RATIFY-WITH-REED-INLINE, 7 cascades).** Math
  foundation and spec compose coherently; Taut scout accurately maps
  landed vs bridged substrate; the three bridges α/β/γ are genuinely
  minimum-gap for one-fracture round-trip; the pipeforward §5.5 and
  nonlinear-tension §6.5/§6.6 additions are architecturally
  substrate-honest.
- **BUT: 7 REED-INLINE cascades required before implementation launch.**
  Line-number drift, conjunct-count drift between math and spec vs the
  substrate-decl'd shard, one substrate-lipstick risk in bridge β's
  MVP path, one unverifiable @io-crossing count claim, one
  measurement-path gap for L(ϕ), one implementation-tick sequencing
  concern, one Rust-authoring-marker discipline gap.
- **2 items require Alex adjudication.** (a) source_position carrier
  placement (Alex-flagged in Taut §14; Mara defers to Alex; Reed will
  hit this on Tick 2). (b) The pipeforward architecture §5.5 asserts
  an architectural constraint on *every future substrate capability* —
  this is a load-bearing framework decision Alex should ratify explicitly.
- **§6.5 + §6.6 + §5.5 substrate-honestly extend the core.** These
  post-hoc additions cite Alex-verbatim, ground in landed substrate
  (`transparency`, `time.delta.holonomy`, `@io/socket`), and compose
  with §1–§4 rather than sitting as isolated addenda. Genuine
  operational-physics ground for §10.1's claim, not bolt-on.
- **Ouroboros-of-ouroboros base case is well-founded.** Termination at
  @io boundary reaches Rice-safe stop; no non-terminating cycle
  identified in the recursive inference of `collapse`'s own body.
- **The substrate-lipstick recidivism check.** The spec's MVP
  first-match tournament (Tick 5) genuinely dispatches through @glue,
  @fate, @bauchladen substrate-decl'd actions — NOT sentinel matching.
  However, bridge β's `resolve_hole` at apply_h.rs is where recidivism
  could sneak in; §4 REED-INLINE-3 names the specific safeguard.
- **Launch recommendation:** PROCEED-WITH-CASCADES. Reed applies the
  7 REED-INLINE repairs to the three landed docs, then Alex
  adjudicates the 2 residue items, then implementation begins on
  Tick 1 (bridge γ).

---

## §2 Per-dimension findings

### §2.A Math formalizes what Alex named — or substrate-adjacent ceremony?

**Interrogation.** Does the math actually formalize Alex's naming, or
does it import Maturana/Varela + PL descent + Liquid Types + Fate
tournament + BLAKE3 CAS + ouroboros theorem as five separate imports
loosely wrapped in prose?

**§1 Maturana/Varela operational closure.** The three conditions
(A1)-(A3) at `docs/math/autopoiesis/README.md:106-127` genuinely name
the compile-altitude specialization. (A1) at line 176-188 discharges
via substrate-decl bookkeeping — every new crystal is produced by
`@bauchladen.crystallize`; every new shard byte by `@io/fs.mutate_at`
(bridge α); every new hole by `@roomba.walk`. This IS the network of
operations producing the components consumed by the same network.
Substrate-decl citation of Maturana/Varela `Autopoiesis and Cognition`
Ch. 1 §"Structure and organization" pp. 78-84 is faithful (Seam
verified pagination against the 1980 D. Reidel edition; the operational
closure definition is where Maturana names it). (A3) reflexivity is
where the load-bearing recursion happens — the operator is `inside`
the system it operates on — and Theorem 6.1 discharges it via ouroboros.
**Not ceremony. This is the real definition.**

**§2 PL convergence on fracture count.** The energy functional at
line 246-256 `E(s) = ⟨x_s, Δ_0 x_s⟩ + λ · |F(s)|` combines the sheaf
Dirichlet energy with the fracture-count. This IS Alex's "each
kintsugi pass reduces ambiguity" formalized. The coupling constant λ
is honestly named (§2.4 reveal-case bound); the choice `λ ≥ μ⁻¹ ·
sup_h loss(h)` at line 356 makes the reveal-case bounded rather than
hand-waved. **Not a proxy that drifts. Substrate-honest.**

**§3 Liquid types.** Correctly ADAPTS Rondon-Kawaguchi-Jhala 2008 (not
verbatim import): §3.2 replaces SMT with spectral analysis. §3.4
substrate-honest bound at line 467-475 explicitly says liquid-type
sub-shards are NOT LANDED and MVP uses first-match tournament. **This
is exactly the honesty §3 needs — it grounds the math while naming
what has not yet landed.**

**§4 @fate tournament.** Section §4.1-§4.3 grounds the tournament in
the LANDED `shards/fate/tournament.mirror` rule vocabulary (verified
against shard at 51.5KB, landed 2026-07-12). The Rayleigh-descent
ranking function at line 528-533 is grounded in the LANDED spec
`docs/specs/fate-bounded-psychohistory-sheaf-cohomology.md` §3. **Not
bolted on; composes with landed spec.**

**§5 Content-addressing.** Correctly formalizes `M ∘ O` as the memory-
composed operator; §5.3 line 652-664 explicitly maps Alex's verbatim
"@silicon/algebra and @fate/algebra, learned, written back
transformations that were inferred and then remembered" to the math.
**Direct Alex-verbatim mapping. Not generic CAS.**

**§6 Ouroboros-of-ouroboros theorem.** §6.3 line 719-736 discharges
the base case: recursion terminates at @io boundary; @io/algebra.*
carriers are irreducible. This grounds Theorem 6.1 without hand-waving.
**See §2.G below for the deeper interrogation of whether ALL paths
terminate; short answer: yes, but one edge case deserves REED-INLINE.**

**Verdict A: SEAM-RATIFY.** Math genuinely formalizes what Alex named.
Every noun in Alex's three messages has a corresponding math object;
every math object cites a landed substrate carrier or established
ancestor result; the composition is substrate-honest, not
substrate-adjacent ceremony.

---

### §2.B Six-step inference loop matches Alex-verbatim — or drifted?

**Alex naming (§0.1 verbatim):** @roomba bumps into `\` cracks →
surrounding geometry infers shape → @fate tournament ranks candidates
→ @glue chains via `|\>` → @silicon/algebra + @fate/algebra remember
→ content-addressed fragment → projected back → autopoietic closed
loop.

**Spec §1.1 six-step at lines 108-120:**

```
step 1  @roomba.walk(σ)              → detects fracture h at position p
step 2  build_hole_record(h, σ, p)   → hole_record {expected_type, context_oids, altitude}
step 3  @fate.roll(space, hole_record) → dice_roll {selected_oid}
step 4  @glue.translate(c, payload)  → translation_outcome
step 5  @kintsugi/consent.query_phi(candidates) → tournament rank
step 6  @bauchladen.crystallize(outcome) → crystal c ∈ K'
step 7  @io/fs.mutate_at(σ.path, p, c.payload) → σ' with h resolved
step 8  @roomba.walk(σ')              → next observation
```

**Per-step verification:**

- Step 1 = "@roomba bumps into `\` cracks" — **DIRECT MAP.**
- Step 2 = "surrounding geometry tells it which shapes it wants" —
  DIRECT MAP via hole_record.context_oids (per shards/fate.mirror:634).
- Step 3 = "@fate tournament ranks candidates" — DIRECT MAP. Note the
  spec at line 121-124 says steps 3+5 fold in practice; this is honest
  disclosure of the operational simplification.
- Step 4 = "@glue chains via `|\>`" — DIRECT MAP via
  @glue.translate at Mesland-category altitude. §2.2 spec lines
  205-210 correctly identify `@glue.translate` as "the substrate-decl
  form of one `|\>` step."
- Step 5 = tournament rank fold — the naming is honest but Alex's
  verbatim doesn't separately name a 5th step; Alex has 4 conceptual
  steps (@roomba, geometry, @fate, @glue) + memory + crystal +
  projection. Spec's 8-step numbering is a decomposition, not a drift.
- Step 6 = "content-addressed fragment" (@bauchladen.crystallize) —
  DIRECT MAP.
- Step 7 = "PROJECTED back into the source file" (@io/fs.mutate_at) —
  DIRECT MAP. Bridge α exists precisely for this step.
- Step 8 = re-observation — implicit in Alex's "autopoietic closed loop."

**Missing piece.** Alex's "@silicon/algebra and @fate/algebra"
memory-write-back is captured in §6 spec (write-back protocol) NOT
as a numbered step. This is defensible — the write-back IS step 6
crystallize (crystals land at `@fate/algebra/*` path-namespace).
**No drift, but a REED-INLINE cascade could improve clarity.**

**Drift check.** Math §1.2 line 165-170 lists a slightly different
step decomposition:

```
O = { walk, build_hole_record, roll, translate, crystallize, project, reobserve }
```

with step 5 identified as `@kintsugi/consent.query_phi (the tournament
rank)` at line 167. This is 7 operators in O. The spec has 8 steps.
Reading carefully: spec step 5 = math tournament rank inside step 3
`roll`. The math has 7 operators; the spec fold-note (line 121-124)
resolves this: steps 3+5 fold when tournament rule = greedy.
**Consistent; documented; not a drift.**

**Verdict B: SEAM-RATIFY.** Every Alex-verbatim atom maps to a
substrate-decl'd operator; the 8-step decomposition is honest and
its fold-note reconciles with the math's 7-operator set.

---

### §2.C Three bridges α/β/γ genuinely minimum-gap?

**Interrogation 1: Is there a smaller gap?**

Reviewing the LANDED substrate (per Taut §7.1 table):
- carriers: 15 LANDED (hole, hole_record, splinter, crystal, morphism,
  correspondence, translation_outcome, dice_roll, etc.)
- actions: 17 LANDED but bodies obligation-blocked (@roomba.walk,
  @fate.roll, @glue.translate, @bauchladen.crystallize,
  @io/fs.write, @io/git.commit, ...)

To close the loop end-to-end for ONE fracture we need:
- (A) detect the `\` and read surrounding AST context → **bridge β
  (parser extension + hole builder)**
- (B) invoke dispatch → wire the six-step composition through apply_h
- (C) persist the crystal → **bridge γ (crystallize writes to
  .mirror/objects/)**
- (D) write bytes at position → **bridge α (@io/fs.mutate_at)**

No smaller gap exists. Removing any of α/β/γ breaks the loop closure.
Substrate lacks position-aware source mutation (Taut §8.4 verified;
Seam re-verified via search of `shards/io/fs.mirror` — 5 actions
landed: read/write/stat/readdir/mkdir; NO mutate_at). Substrate lacks
position-aware `\` tokenization (Taut §1.2 verified; Seam re-verified
via search of `bootstrap/src/pipeline.rs` — no Hole AST node; parser
skips lines with `{`/`}`). Substrate lacks crystal persistence (Taut
§7.5 verified; Seam re-verified: `apply_h.rs::crystallize` at line
1035 computes OID via `hash_tagged` but writes nothing to disk).
**Genuine minimum gap.**

**Interrogation 2: Are any bridges avoidable via composition?**

- Bridge γ: could compose over LANDED `@io/fs.write` (whole-file) +
  `@mirror/store.write` (write bytes → oid). Both are substrate-decl'd
  but `@mirror/store.write` body is `\`-obligation-blocked (per
  shards/mirror/store.mirror). The Rust bridge γ is the minimal
  extension of an existing crystallize combinator that already
  computes the OID; it's substrate-honest at @io-boundary.
- Bridge α: no landed action provides position-aware splice; must
  mint. Substrate-honestly minted at @io family (Rice-safe FLOOR).
- Bridge β: this is the largest bridge (~300 Rust). Could bridge β
  be a shard-body composition over @io? Seam's analysis: the parser
  extension MUST live in Rust because the parser IS Rust (per
  Arc-1 discipline: parser is FLOOR). The `resolve_hole` dispatcher
  in apply_h.rs COULD in principle be substrate-decl'd, but composing
  a `\`-body resolver from within `\`-body actions is a
  chicken-and-egg problem the substrate can't solve at bootstrap
  altitude. Reed's Rust dispatcher is architecturally necessary; it
  is NOT a substrate-lipstick shortcut PROVIDED the `resolve_hole`
  body genuinely dispatches through @fate/@glue/@bauchladen
  substrate-decl'd actions rather than short-circuiting to a
  hardcoded resolution table. See REED-INLINE-3 below.

**Interrogation 3: Do bridges themselves risk substrate-lipstick?**

- Bridge γ (crystallize persistence): pure @io-boundary extension of
  an existing OID-computing combinator. Zero substrate-lipstick risk.
  RISK: LOW.
- Bridge α (mutate_at): pure @io-boundary POSIX splice. Zero
  substrate-lipstick risk. RISK: LOW.
- Bridge β (resolve_hole): **HIGHEST RISK of substrate-lipstick
  recidivism.** The spec §4.3 code sketch (lines 574-583) says:
  ```
  1. Constructs `hole_record` from surrounding geometry (step 2).
  2. Enumerates Cand(h) from landed @kintsugi/fracture/* species (step 3 MVP).
  3. Applies first-match morphism as @glue.translate (step 4 MVP).
  4. Crystallizes outcome via bridge γ (step 6).
  5. Projects via bridge α (step 7).
  ```
  Step 3 of this sketch — "Applies first-match morphism as
  @glue.translate" — is where recidivism could sneak in. If the Rust
  resolver arm HARDCODES the first-match logic instead of dispatching
  through `@glue.translate`'s substrate-decl'd action (which itself
  is `\`-body), the whole bridge becomes sentinel-matching in
  disguise. The correct discipline: bridge β's `resolve_hole` must
  invoke `apply_h::act("@glue.translate", ...)` — even though
  `@glue.translate` itself has a `\` body. The dispatcher makes the
  substrate call; the substrate call recursively resolves through the
  same loop; the base case is @io. **See REED-INLINE-3 for the
  specific safeguard.** RISK: MODERATE.

**Verdict C: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-3).** Three
bridges are genuinely minimum-gap. Bridge β carries recidivism risk
that requires an explicit spec discipline before Reed implements.

---

### §2.D Pipeforward §5.5 architecturally coherent with rest of substrate?

**Interrogation 1: Composes with @io/socket at shards/io.mirror without
inventing new carriers?**

Spec §5.5.3 lines 770-784 cite: `@io/socket` family root LANDED at
`shards/io.mirror:130-140`; species substrate-decl at
`shards/io/socket.mirror` **forward-promised**.

Seam verified: `shards/io.mirror` names @io/socket in the sub-species
roster (line 156-181 range); NO `shards/io/socket.mirror` file exists
yet (grep-verified — Search returned error "File does not exist").
`@io/socket` species is genuinely forward-promised, per Taut §D8 and
the shard docblock's forward-promise language.

This is an ACCURATE citation of the substrate state. The pipeforward
architecture composes over `@io/socket` at the family-root altitude
(landed) with the species landing forward-promised when consumers
pull. Pipeforward is a consumer that will pull `@io/socket`; §5.5
does NOT invent new socket carriers — it names the composition
substrate needs.

**Verdict: substrate-honest, but §5.5.3 line 772 says "LANDED at
`shards/io.mirror:130-140`" which conflates family-root naming
with species landing. Reader might infer @io/socket species is
landed; it is forward-promised. REED-INLINE-4 clarifies.**

**Interrogation 2: Mode A vs Mode B loss-ratio bound (Ω(n) vs O(1)).**

Spec §5.5.2 line 763-766 claims:
- Mode A total loss scales as `Ω(n)` (n = pipeline length)
- Mode B total loss scales as `O(1)`

Adversarial check: Mode A loss = Σᵢ L(ϕᵢ) + Σᵢ L(ϕᵢ⁻¹). Per §6.6.3
loss additivity: `L_total = Σᵢ L(ϕᵢ)`. If every stage crosses @io
exactly twice (stdout + stdin), then Mode A loss is `2n · L̄` where
L̄ is average per-crossing loss. This IS `Ω(n)` when L̄ > 0. Mode B:
one crossing total at final discharge, `L(ϕ) = O(1)` when L̄ is
bounded. **The bound holds under standard assumptions.**

However, the spec doesn't state the assumption. If L̄ grows with
pipeline length (e.g., accumulated context makes each linearization
denser), Mode A could be `ω(n)` and Mode B could be `Ω(n)` (final
discharge carries the accumulated state). The bound is TYPICAL not
UNIVERSAL. **REED-INLINE-5 adds the qualifier.**

**Interrogation 3: Do the four architectural design pressure rules
generate concrete constraints?**

§5.5.4 lists four rules:
1. Every new CLI verb designed for socket-forwarded composition FIRST.
2. Every new shard-decl action returns a nonlinear ref by default.
3. Every new @io species carrier declares an `L(ϕ)` estimate in its
   docblock.
4. StageFreight × spectral.engineer pipeline composes over this
   design pressure directly.

Rules 1-3 ARE concrete constraints — future capabilities can be
adjudicated by Seam against these rules. Rule 4 is a specific
composition claim; §2.E cross-checks whether it survives scrutiny.

**Concern:** These four rules are FRAMEWORK-LEVEL constraints on
every future substrate landing. Alex should ratify explicitly before
they become load-bearing. **ALEX-ADJUDICATION-1.**

**Interrogation 4: `~/.mirror/serve.sock` cite composes with
lambda-shell.md substrate-honestly?**

Spec §5.5.3 line 780-782 cites `~/.mirror/serve.sock` per
`docs/specs/lambda-shell.md` §Connection to Daemon. Reed+Alex authored
lambda-shell.md; the socket-forwarded daemon architecture IS
substrate-decl'd there. Seam did not deep-verify lambda-shell.md
(30K+ token spec); accepts on Reed+Alex authorship. **Substrate-honest
composition.**

**Interrogation 5: StageFreight × spectral.engineer composition survives?**

§5.5.4 rule 4 claims the docker-image ship preserves the mirror
substrate's nonlinear state at every internal boundary; discharge
occurs at exactly the interface with non-mirror CI infrastructure.
Cites `docs/specs/kintsugi-ouroboros-compiler-self-collapse.md`
§Arc-5. Seam accepts on citation authority (Mara canonical, 80KB
spec). The composition claim is architecturally coherent: StageFreight
is the mirror-authored CI substrate; spectral.engineer is the
consumer; the boundary crossing IS the interface to non-mirror CI
tooling. **Substrate-honest composition.**

**Verdict D: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-4, REED-INLINE-5)
+ ALEX-ADJUDICATION-1.** Pipeforward §5.5 is architecturally coherent;
two clarifications required (socket species landing status; loss-ratio
bound qualifier); framework-level rule ratification is Alex-only.

---

### §2.E Recognition candidates §10.1-10.4 sound + composed with prior?

**Candidate §10.1 mirror-compiler-is-operationally-closed-autopoietic-system.**
Statement grounds in Theorem 1.1 of math §1; second-witness threshold
IS Tick 5 empirical proof (§8.5). The recognition IS well-formed:
first witness = math + spec; second witness = empirical round-trip.
Ancestry chain (recognitions #43, #51, #58, #100, #103, #104) is
correctly cited. **Sound.**

**Candidate §10.2 fracture-inference-via-fate-tournament-over-substrate-geometry.**
Statement is operational: MVP tournament (first-match from 14 landed
fracture species) IS the substrate's operational realization; second
witness at Tick 5 empirical proof of `operator_match` fracture
resolving. Ancestry cites recognition #53 (bilateral property/fracture
pattern) and recognition #58 (@fate IS optical inference). **Sound.**

**Candidate §10.3 substrate-memory-is-content-addressed-write-back.**
Statement is derivative — combines Theorem 5.1 monotone growth + BLAKE3
CAS. Spec correctly flags "folds into #R-mirror-compiler-is-
operationally-closed if Alex prefers" at line 1319. This is honest
disclosure of a candidate that may collapse into §10.1.
**Sound; fold decision is Alex-only.**

**Candidate §10.4 computation-is-nonlinear-tension-resolution-discharged-
through-io-with-measurable-loss.** This is the operational-physics
ground for §10.1 (per §10.4 composition note at line 1351-1357).
Second-witness path (§10.4 line 1343-1346) requires measurement of
`L(ϕ)` at bridge-α discharge. This measurement path is UNVERIFIABLE
without landing the measurement mechanism — see §2.I below.
**Sound in principle; measurement path is a gap (REED-INLINE-6).**

**Composition check with prior recognitions.**

Reviewed against:
- Mara-B §6 four candidates (kintsugi/ouroboros arc): #R-substrate-
  mends-its-own-rust; #R-evaluator-is-legitimate-floor; #R-mirror-
  substrate-becomes-self-hosting; #R-mirror-kintsugi-shipped-as-
  stagefreight. **No conflict.** §10.1 extends #R-mirror-substrate-
  becomes-self-hosting to compile-altitude autopoiesis; §10.2
  operationalizes the substrate-mending-mechanism.
- @sheaf canonical §6 three candidates: no conflict; sheaf recognitions
  ground the eigensheaf math §2 depends on.
- @io/secrets canonical §6 five candidates (one already second-
  witness-ratified): no conflict; @io/secrets is a discharge consumer
  of the pipeforward architecture §5.5 names.
- @kintsugi/ouroboros #R-composed-cascade + #R-compiler-authors-its-
  own-commits: **DIRECT COMPOSITION.** #R-compiler-authors-its-own-
  commits was second-witnessed at `fcc1d75` (whole-repo altitude);
  §10.1 is the shard-body-altitude analog. Second-witness at Tick 5
  will demonstrate the same self-authoring property at shard body
  altitude.

**Missing candidate check.** Is there a recognition Reed should be
naming here that the spec omits? Seam's analysis:
- **`#R-substrate-had-the-word-for-holonomy-loss-at-io-crossing`**
  might deserve naming — §6.6.5 line 1121-1132 documents that
  `transparency` is the substrate's landed carrier for discharge
  loss; the recognition that substrate ALREADY had this carrier is
  candidate-worthy. However, this folds into §10.4 as the
  ground-truth for the measurement mechanism; no separate candidate
  needed. **No missing candidate.**

**Verdict E: SEAM-RATIFY.** Four candidates are sound; composition
with prior recognitions is coherent; no missing candidate identified.

---

### §2.F Nonlinear-tension framing threads coherently — or bolted-on?

**Interrogation.** §6.5 (nonlinear tension) + §6.6 (@io holonomy
loss) in math and §5.5 (pipeforward) in spec were added AFTER the
core landing (per commit history: 2319cba landed core, 9d1eb26
landed the additions). Do they compose with §1-§4 or sit as isolated
addenda?

**§6.5.5 composition with §2 PL convergence.** Line 872-891:
- §2's `∇E` IS the local tension gradient
- §2's exponential convergence rate IS the substrate's relaxation
  timescale
- §2's reveal-case bound IS a topological-defect budget

These are three concrete re-anchorings of §2's math at physical
altitude. NOT isolated addenda — §6.5 is §2 read at a different
altitude. **Composes coherently.**

**§6.5.6 composition with §3 liquid-type inference.** Line 893-908:
Liquid-type refinement sharpens the tension gradient by narrowing
`Cand(h)`. This IS a real composition claim — Liquid predicates
constrain the descent direction. **Composes coherently.**

**§6.5.7 composition with §4 @fate tournament.** Line 910-929: The
tournament's arg-min-Rayleigh selection IS arg-max `|Δτ|` selection
under tension-field physics. `rank(h, Cand(h)) = arg max |Δτ_m(h)|`
subject to reveal_count budget. **This is a rewriting of §4.2's
ranking function, not an isolated addendum. Composes coherently.**

**§6.6 @io = linearization loss.** Line 971-1178:
- §6.6.1 discharge map ϕ_a
- §6.6.2 holonomy at discharge boundary
- §6.6.3 loss additivity (composes with landed `@mirror/loss/transparency`
  monoid discipline)
- §6.6.4 composition with six-step loop
- §6.6.5 composition with existing @io family
- §6.6.6 composition with ouroboros termination (§6)

**§6.6.6 is the coherence check.** Theorem 6.1 (ouroboros termination
at @io base case) PLUS §6.6 (@io crossing IS linearization event with
measurable loss) → the Rice-safe stop is NOT a hard stop; it is a
linearization event. This is a genuine unification of §6 and §6.6.
**Composes coherently.**

**Alex-verbatim consistency check.** Alex-verbatim quoted at math
§6.5.1 line 766-771 and spec §5.5.1 line 723-729 — SAME three
sentences. The interpretation in math §6.5 formalizes "computation IS
nonlinear tension resolution"; the interpretation in spec §5.5
formalizes "pipeforward avoids @io crossings." Both interpretations
derive from Alex's third sentence. **Consistent.**

**Verdict F: SEAM-RATIFY.** §6.5 + §6.6 + §5.5 thread coherently with
§1-§4 rather than sitting as bolt-on addenda. The composition claims
are substantive (three concrete re-anchorings per §6.5.5; sharpening
claim per §6.5.6; ranking-function reread per §6.5.7; ouroboros
unification per §6.6.6).

---

### §2.G Ouroboros-of-ouroboros base case — actual termination or subtle regress?

**Interrogation 1: Does the recursion terminate in ALL cases?**

Theorem 6.1 (math §6.3, spec §5.3) states: recursion reaches @io
boundary in finitely many steps. Proof sketch reduces altitude:
`collapse` → Rust file collapse (Arc-2) → shard-body dispatch (via
apply_h.rs resolver arms) → @io call. @io is base case.

**Adversarial edge case: mutually-recursive shard-decls.**

Consider two shard-decl actions A.foo and B.bar whose bodies each
contain `\` and each infer the OTHER's body from surrounding geometry.
The inference of A.foo constructs a hole_record whose
`context_oids` might include B.bar; the tournament dispatches to
B.bar's morphism; B.bar's inference in turn depends on A.foo. **Does
this cycle?**

Analysis:
- The hole_record for A.foo carries `expected_type = A.foo.return_type`
  and `context_oids = [B.bar's OID as a landed morphism]`.
- The tournament ranks candidates whose `target_signature =
  A.foo.return_type`. If B.bar's target_signature matches, B.bar
  becomes a candidate.
- @glue.translate applies B.bar as a morphism, invoking B.bar's body.
- B.bar's body contains `\`; the loop recurses.
- BUT: the loop is now resolving B.bar's `\`, not re-resolving A.foo's
  `\`. Different fracture, different position, different hole_record.

The recursion is NOT on the same fracture — it recurses on DIFFERENT
fractures. The fracture set F is monotone nonincreasing modulo
bounded reveals (Theorem 2.1). The reveal budget caps at `1 +
O(spectral_slack)` per pass (math §2.4). Cycling would require
infinite reveals, which the budget forbids.

**Termination holds even in mutually-recursive cases.** But this
requires the reveal budget to actually apply — spec §3.7 doesn't
explicitly discuss mutually-recursive shard-decls. **REED-INLINE-1
adds a note pointing to the reveal-budget termination argument.**

**Interrogation 2: Is @io/algebra.* actually irreducible?**

Math §6.3 line 727-728 asserts @io/algebra.* is irreducible under
autopoietic inference. Seam-verification: `shards/io/algebra.mirror`
is landed (17.3KB per Taut D8). Its actions include exposure/discharge
primitives that read/write bytes at kernel boundaries; these compose
into Rust @io syscalls at apply_h.rs FLOOR. **Genuinely irreducible
at compile altitude.** The Rice-safe verifiability predicate
`@kintsugi/ouroboros.verifiable_at_altitude` bounds inference power.

**Interrogation 3: Fractures that neither resolve nor reach @io?**

Math §6.5.4 line 843-870 explicitly addresses this:
- Case (i): `Cand(h) = ∅` — no matching morphism; substrate-inventory-
  gap; the `\` is a SPECIFICATION SIGNAL to land a new morphism.
- Case (ii): every `m ∈ Cand(h)` produces `Δτ(h) ≥ 0` — no descent;
  morphism-inventory-non-descent; the `\` is a signal to improve
  morphism ranking.
- Case (iii): `h.altitude = @io` — Rice-safe stop.

**All three "stuck" cases are named and handled.** The loop
terminates in cases (i) and (ii) with a non-resolved fracture (the
minimum-loss residual) that signals substrate improvement work.
**Substrate-honest handling.**

**Verdict G: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-1).**
Termination is well-founded in all cases including mutually-recursive
shard-decls; the reveal budget bounds the recursion. Add a spec note
citing the reveal-budget argument explicitly.

---

### §2.H Substrate-honest bounds — @io crossings named + substrate stays in nonlinear land?

**Interrogation:** Enumerate every @io crossing the six-step loop
takes per invocation.

- **Step 1 @roomba.walk (fracture detection).** Reads shard file bytes
  from disk. **@io crossing #1 (READ).** Empirically live per
  `bootstrap/src/roomba.rs`. Loss `L(ϕ_read)` per file bytes read.
- **Step 2 build_hole_record.** Pure substrate-decl composition on
  AST already in memory (post step 1). **No @io crossing.**
- **Step 3 @fate.roll.** Tournament dispatch over crystals in memory
  (or via lookup on tray). If cache-miss: no @io crossing (candidates
  enumerated from landed shard set in memory). If cache-hit and the
  crystal is on-disk (post-γ): **@io crossing (READ) `L(ϕ_read)`**.
  If crystal is in RAM cache: **no @io crossing.**
- **Step 4 @glue.translate.** Pure substrate-decl composition on
  in-memory carriers. **No @io crossing.**
- **Step 5 @kintsugi/consent.query_phi.** Pure substrate-decl. **No
  @io crossing.**
- **Step 6 @bauchladen.crystallize.** Bridge γ writes to
  `.mirror/objects/<OID>`. **@io crossing #2 (WRITE) `L(ϕ_write)`.**
- **Step 7 @io/fs.mutate_at.** Bridge α writes source-file bytes.
  **@io crossing #3 (WRITE) `L(ϕ_mutate_at)`.**
- **Step 8 @roomba.walk (re-observation).** Reads σ' from disk. **@io
  crossing #4 (READ).** Loss `L(ϕ_read')`.

**Total: 3-4 @io crossings per loop iteration**, not "one crossing"
as spec §6.6.4 line 1091-1093 implies. Spec claim: "Steps 1-6 —
nonlinear tension resolution inside the substrate ... Step 7 — the
@io discharge." This UNDERCOUNTS: steps 1 and 8 are also @io crossings
(read from disk); step 6 is an @io crossing (write to
`.mirror/objects/`).

The intent of spec §6.6.4 is clear — step 7 is the LOAD-BEARING
discharge (the substrate's tension-resolved crystal payload becomes
byte-sequence on disk). But the claim "steps 1-6 nonlinear" is
imprecise: step 1 is a READ crossing, and step 6 is a WRITE crossing.
The claim SHOULD BE:
- Steps 2-5 are pure nonlinear inference (no @io crossing).
- Steps 1, 6, 7, 8 are @io crossings.
- Step 7 is the LOAD-BEARING projection discharge.

This is not a fatal drift — the substrate-honest bound is preserved
(loss is bounded per §6.6.3 additivity) — but the language is misleading.
**REED-INLINE-2 corrects the crossing count.**

**Consequence for §5.5 loss-ratio bound.** If each loop iteration
incurs 3-4 @io crossings, the Mode A vs Mode B comparison in §5.5.2
still holds (Mode A adds SHELL-LEVEL crossings on top of the loop's
per-iteration crossings; Mode B avoids the SHELL-LEVEL crossings by
socket-forwarding). The bound holds; the framing needs refinement.

**Verdict H: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-2).** @io
crossing count in spec §6.6.4 undercounts by 3; the loss-additivity
bound is preserved but the crossing enumeration should be corrected.

---

### §2.I L(ϕ) holonomy loss actually measurable via @epistemologic/reality/time.delta?

**Interrogation:** Math §6.6.2 line 1009-1024 claims the substrate
carries `delta.holonomy` at `shards/epistemologic/reality/time.mirror:127`;
this IS the substrate's landed carrier for `L(ϕ)`.

Seam verified: `shards/epistemologic/reality/time.mirror` is landed
2026-07-15; carries `type delta = { from, to, mutations, holonomy: loss }`
per Taut §8.3.

**Is the mapping from `time.delta.holonomy` to `L(ϕ)` real math?**

Math §6.6.2 formalizes:
```
L(ϕ_a)(s) = ∮_∂s ω_ϕ · dt
```

The claim: `delta.holonomy` IS the substrate-decl'd carrier for this
integral. The math §6.6.2 asserts the identification.

**Adversarial check.** The formula `∮_∂s ω_ϕ · dt` requires:
- A boundary ∂s (well-defined? the boundary between substrate interior
  and linear-sequential world — under-specified for finite discrete
  systems)
- A linearization connection form ω_ϕ (well-defined? spec asserts but
  doesn't construct)
- A time integral over the discharge event (well-defined? the
  discharge event is instantaneous at the substrate altitude — this
  is a mathematical fiction, though a useful one)

The formula is a MATHEMATICAL METAPHOR grounded in continuous
geometry (per `docs/math/the-tower/holonomy.md` — Magnot's κ). Its
ACTUAL COMPUTATION in the substrate at bridge-α discharge is
UNDEFINED. The spec at §6.6.7 line 1150-1166 asserts second-witness
requires the crossing's `transparency` slot to carry a nonzero
holonomy value — but the COMPUTATION of that value is not specified.

**Does Reed have a concrete implementation path to MEASURE L(ϕ) at
bridge-α's discharge?**

Spec §5.5.5 line 819-828 asks bridge α to quantify linearization loss
("lossless when replacement length = original length; O(byte-count)
reconstruction cost when lengths differ"). This is an ESTIMATION
recipe, not a measurement of the holonomy integral. There's a gap
between the formal L(ϕ) and the implementable estimate.

**Verdict I: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-6).** The
formal L(ϕ) is a well-defined mathematical object grounded in
substrate ancestry; the substrate-decl'd carrier (`delta.holonomy`)
exists; the CONCRETE MEASUREMENT MECHANISM at bridge α needs one more
spec sentence naming the implementable estimate (byte-length delta +
resolution-depth as proxy). This is a lightweight cascade; §10.4
recognition second-witness path becomes verifiable.

---

### §2.J Implementation-ticks §8 landing sequence — Reed-viable?

**Sequence per spec §8:**
1. Bridge γ (~50 LOC crystallization persistence)
2. Bridge α (~50 Rust + species mint @io/fs.mutate_at)
3. Bridge β (~300 Rust + parser extension for AST-context hole builder + resolve_hole)
4. Wire the six-step loop through apply_h::act
5. Empirical proof: one full round-trip
6. Iterate

**Interrogation 1: Does the sequence order dependencies correctly?**

Bridge γ first (persistence) — correct, because bridge α and bridge β
both need to produce crystals that persist.

Bridge α second (position-aware mutation) — correct, because bridge β
depends on α to project the resolved body.

Bridge β third (parser + resolver) — correct, because β wires the
whole loop and depends on both γ and α.

Tick 4 (loop wiring) after β — correct.

Tick 5 (empirical proof) after wiring — correct.

**Sequence is architecturally coherent.**

**Interrogation 2: Is ~400 Rust + ~200 mirror substrate-honest given
Reed's stall history?**

Reed's stall history at Arc-1/Arc-2 involved:
- Sentinel matching in apply_h.rs (lifted sbec 0→15+ but not
  autopoietic loop)
- 5 substrate-dishonest Rust extensions during 2026-07-14 gift arc
  (per memory `feedback-no-rust-extension-shortcut`)

400 Rust across three bridges is ~130 LOC/bridge average. The
biggest is bridge β at ~300 LOC (parser extension ~100 + resolver
~200). This is genuinely-scoped work — the parser extension IS an
FLOOR-level Rust addition (Alex-authorized per
`[substrate-floor:@io-boundary]` marker discipline per §8.3 spec
line 1119-1124).

**Estimate is honest ONLY IF Reed applies the substrate-floor
discipline and doesn't creep beyond ~400 LOC.** Historically, Reed's
estimates have been ~2× under. Spec should include a hard-ceiling
discipline. **REED-INLINE-7 adds a ceiling.**

**Interrogation 3: Does bridge β risk repeating the sentinel-matching
failure?**

Yes — see §2.C interrogation 3 above. The specific safeguard:
`resolve_hole` MUST dispatch through `apply_h::act("@glue.translate",
...)` rather than hardcoding the morphism application logic.
**REED-INLINE-3 (already named in §2.C).**

**Interrogation 4: Is "one empirical round-trip" sufficient for
#R-10.1 second-witness?**

The recognition candidate §10.1 asserts autopoietic closure at
compile altitude. Second-witness threshold per §10.1 line 1278-1282:
"First full round-trip through the loop (Tick 5 empirical proof;
§8.5) resolving a genuine `\` fracture, crystallizing to
`.mirror/objects/<OID>`, projecting via `@io/fs.mutate_at`, and
observing the mutation on next walk."

Adversarial: is ONE round-trip sufficient to demonstrate operational
closure? Maturana-Varela's operational closure is a STRUCTURAL
property — it either holds or doesn't. ONE round-trip demonstrates
the structure discharges; it does NOT demonstrate the property holds
under load (many fractures; mutually-recursive fractures;
inventory-gap fractures).

**Suggested addition:** the second-witness ratifies §10.1 as
demonstrated for ONE fracture; the recognition ratifies FULLY at
Tick 6 (multi-fracture iteration) empirically demonstrating F' = F \
{h} + reveals with reveals ≤ 1 + O(spectral_slack). This is what the
math §2.4 reveal-case bound proves — one round-trip demonstrates
step 7 executes; multi-fracture demonstrates the descent property.

However, this may be over-strict: for the RECOGNITION CANDIDATE
strength (not full ratification), Tick 5 IS sufficient. Full
recognition ratification is a subsequent milestone. **Spec's current
wording is defensible; no REED-INLINE required.**

**Verdict J: SEAM-RATIFY-WITH-REED-INLINE (REED-INLINE-7).**
Implementation sequence is architecturally coherent; LOC estimate
requires hard-ceiling discipline given Reed's stall history.

---

## §3 Ship verdict per artifact

### §3.1 `docs/math/autopoiesis/README.md` (1291 LOC)

**SEAM-RATIFY-WITH-REED-INLINE.** Math foundation is substrate-honest;
Maturana/Varela lift at compile altitude is faithful; PL descent
grounds in landed eigensheaf spec; Liquid types §3 correctly adapts
Rondon-Kawaguchi-Jhala with spectral decision procedure; @fate
tournament §4 grounds in landed shard vocabulary; content-addressing
§5 formalizes Alex-verbatim mapping; ouroboros-of-ouroboros §6 has
well-founded termination.

§6.5 + §6.6 additions thread coherently. The nonlinear-tension frame
IS a re-anchoring of §2 at physical altitude, not an isolated
addendum.

Cascades applied: REED-INLINE-1, REED-INLINE-2, REED-INLINE-6.

### §3.2 `docs/specs/autopoietic-inference-loop.md` (1482 LOC)

**SEAM-RATIFY-WITH-REED-INLINE.** Spec correctly names the six-step
inference loop; three bridges are genuinely minimum-gap;
landing sequence is architecturally coherent; §5.5 pipeforward
architecture composes with landed @io/socket family root.

Cascades applied: REED-INLINE-3, REED-INLINE-4, REED-INLINE-5,
REED-INLINE-7. Two Alex-adjudication items: ALEX-ADJUDICATION-1
(pipeforward framework-level ratification), ALEX-ADJUDICATION-2
(source_position carrier placement per Taut §14).

### §3.3 `docs/scouts/2026-07-15-taut-autopoietic-composition-surface.md` (1547 LOC)

**SEAM-RATIFY.** Scout's ten-dimensional grep-first empirical mapping
is faithful to the substrate. LANDED/PARTIAL/MISSING verdicts
per-dimension are accurate (Seam re-verified §D1, §D2, §D5, §D7,
§D8, §D10). The three-bridge finding IS the genuinely-minimum gap.
Scout correctly defers to Mara (math) and Alex (adjudication)
without over-specifying.

No cascades required for the scout. It is the empirical ground
truth the math + spec formalize; it stands as-is.

---

## §4 REED-INLINE cascades (7 total; apply before implementation begins)

### REED-INLINE-1: Ouroboros mutually-recursive termination note

**Where:** `docs/math/autopoiesis/README.md` §6.3 Theorem 6.1 proof
sketch (line 723-737).

**Cascade:** Add a paragraph after Theorem 6.1 addressing
mutually-recursive shard-decls:

> **Corollary 6.1.1 (Mutually-recursive termination).** If shard-decl
> actions A.foo and B.bar mutually reference each other via their
> hole_records' context_oids, the recursion resolves DIFFERENT
> fractures on each recursive step (A.foo's `\` first; B.bar's `\`
> second; each's descendants third), not the same fracture. The
> fracture-count monotone-nonincreasing property (Theorem 2.1) with
> reveal budget `1 + O(spectral_slack)` per pass (§2.4) bounds
> mutual recursion by the total fracture count of the reachable
> shard-decl closure; the closure is finite by the finite eigenspectrum
> bound (`docs/specs/eigensheaf.md` §4.2). Termination follows.

**Why:** Adversarial edge case (mutually-recursive shard-decls) is
handled by the math but not explicitly discussed. This cascade
closes the interrogation.

### REED-INLINE-2: @io crossing count correction in math §6.6.4

**Where:** `docs/math/autopoiesis/README.md` §6.6.4 line 1082-1108
AND `docs/specs/autopoietic-inference-loop.md` §5.5.5 line 813-828.

**Cascade:** Change the framing from "Steps 1-6 nonlinear; step 7 IS
discharge" to:

> **Steps 2-5 are pure nonlinear inference** (no @io crossing). **Steps
> 1, 6, 7, 8 are @io crossings** (step 1 READ σ; step 6 WRITE crystal
> to `.mirror/objects/<OID>`; step 7 WRITE source-file mutation via
> `@io/fs.mutate_at` — the LOAD-BEARING projection discharge; step 8
> READ σ' for re-observation). Loss decomposition:
> `L_loop = L(ϕ_read_σ) + L(ϕ_crystal_write) + L(ϕ_mutate_at) +
> L(ϕ_read_σ')`. Step 7's L(ϕ_mutate_at) is the substrate's
> LOAD-BEARING projection discharge; the other three crossings are
> low-cost reads/writes of already-serialized substrate state.

**Why:** Spec's original claim undercounts crossings by 3. The
loss-additivity bound is preserved but the enumeration should be
substrate-honest.

### REED-INLINE-3: Bridge β anti-recidivism discipline

**Where:** `docs/specs/autopoietic-inference-loop.md` §4.3 line
572-590 (Bridge β Rust delta) AND §8.3 Tick 3 landing spec (line
1103-1128).

**Cascade:** Add explicit anti-recidivism discipline:

> **Anti-recidivism discipline.** Bridge β's `resolve_hole` in
> `bootstrap/src/apply_h.rs` MUST dispatch through `apply_h::act` for
> every substrate-decl'd action — specifically:
>
> 1. `apply_h::act("@fate.roll", [space, hole_record])` (not
>    hardcoded first-match logic)
> 2. `apply_h::act("@glue.translate", [correspondence, payload])`
>    (not hardcoded morphism application)
> 3. `apply_h::act("@bauchladen.crystallize", [outcome])` (not
>    hardcoded crystal construction)
>
> The `@fate.roll` resolver arm's MVP body — enumerate matching
> fracture species; return first-match dice_roll — is legitimate
> substrate-decl body dispatch (the resolver body IS the substrate-
> decl'd action's discharged body). The DIFFERENCE from sentinel-
> matching: sentinel matching bypasses the action-dispatch surface;
> substrate-decl body dispatch invokes the action-dispatch surface
> and returns the substrate-decl'd carrier. Reed's Seam-inline audit
> at `docs/audits/2026-07-15-bridge-beta-hole-builder-resolver.md`
> must explicitly verify this dispatch pattern.

**Why:** Bridge β carries the highest recidivism risk (per §2.C
interrogation 3); the discipline must be explicit before Reed
implements to prevent substrate-lipstick sneaking in.

### REED-INLINE-4: @io/socket species-landing clarification

**Where:** `docs/specs/autopoietic-inference-loop.md` §5.5.3 line
770-773.

**Cascade:** Change:

> - **Socket transport:** `@io/socket` family root LANDED at
>   `shards/io.mirror:130-140` (boot-floor `boot/std/io/socket.mirror`;
>   species substrate-decl at `shards/io/socket.mirror` forward-promised).

to:

> - **Socket transport:** `@io/socket` NAMED IN FAMILY ROOSTER at
>   `shards/io.mirror:156-181` (boot-floor `boot/std/io/socket.mirror`;
>   species shard file `shards/io/socket.mirror` FORWARD-PROMISED,
>   lands when consumers pull). Pipeforward architecture IS the
>   consumer that will pull `@io/socket` species landing.

**Why:** Original wording conflates family-root naming with species
landing; @io/socket species is genuinely forward-promised, not landed.

### REED-INLINE-5: Loss-ratio bound qualifier

**Where:** `docs/specs/autopoietic-inference-loop.md` §5.5.2 line
763-766.

**Cascade:** Add qualifier:

> **Loss-ratio bound (typical case):** Under bounded average per-
> crossing loss L̄, Mode A total loss / Mode B total loss ≥ 2
> typically, and grows unboundedly for pipelines of length `n`
> (Mode A total loss scales as `Ω(n)`; Mode B total loss scales as
> `O(1)`). **When accumulated substrate context makes per-crossing
> loss grow with pipeline length, both modes can exhibit `Ω(n)`
> growth; Mode B still dominates when the final-discharge loss is
> less than the sum of intermediate-crossing losses.** The
> substrate-honest bound is: Mode B ≤ Mode A always; Mode B strictly
> less when intermediate stages carry nontrivial linearization loss.

**Why:** Original claim is universal; the actual claim is typical-case
under bounded L̄. This preserves the architectural insight while
being substrate-honest about the bound.

### REED-INLINE-6: L(ϕ) implementable estimate

**Where:** `docs/math/autopoiesis/README.md` §6.6.7 line 1150-1166
AND `docs/specs/autopoietic-inference-loop.md` §5.5.5 line 819-828.

**Cascade:** Add implementable estimate sentence to §6.6.7:

> **Implementable estimate at bridge α discharge.** The formal
> `L(ϕ_{fs.mutate_at})` is grounded in the holonomy integral
> `∮_∂s ω_ϕ · dt`; its OPERATIONAL PROXY at implementation altitude
> IS: `L̂(ϕ) = |replacement_length - original_length| + resolution_
> depth`, where `resolution_depth` = number of `\`-obligation-blocked
> actions the loop traversed to reach the discharge. This proxy is
> BLAKE3-content-addressable, storable in `transparency` slot, and
> monotone in the topological-defect count. The proxy is a lower
> bound on the formal L(ϕ); recognition §10.4 second-witness ratifies
> when the proxy is measured and folded into the crystal tray.

**Why:** Recognition §10.4 second-witness path is currently
unverifiable without the implementable estimate; this cascade makes
the measurement mechanism concrete.

### REED-INLINE-7: Implementation LOC ceiling discipline

**Where:** `docs/specs/autopoietic-inference-loop.md` §4.4 line
594-606 (cumulative delta) AND §8 landing sequence header.

**Cascade:** Add ceiling discipline:

> **LOC ceiling (Reed's substrate-honest bound).** ~400 Rust + ~200
> mirror is the TARGET. The HARD CEILING is 500 Rust + 300 mirror.
> If any tick pushes beyond the hard ceiling, Reed HOLDS and requests
> Seam-inline audit BEFORE continuing. Historical drift: Reed's
> Arc-1/Arc-2 estimates undershot by ~2×; this ceiling is calibrated
> for that drift. Beyond the ceiling, the substrate-lipstick risk
> rises and Alex-adjudication becomes required.

**Why:** Reed's stall history requires an explicit ceiling to prevent
scope creep during implementation. The ceiling triggers Seam-inline
audit rather than letting the implementation drift.

---

## §5 Alex-adjudication residue (2 items)

### ALEX-ADJUDICATION-1: Pipeforward framework-level ratification

**Item:** Spec §5.5.4 rules 1-3 assert framework-level constraints on
EVERY FUTURE substrate capability:

1. Every new CLI verb designed for socket-forwarded composition FIRST.
2. Every new shard-decl action returns a nonlinear ref by default.
3. Every new @io species carrier declares an `L(ϕ)` estimate in its
   docblock.

**Why Alex adjudicates:** These are FRAMEWORK-LEVEL constraints. Once
ratified, they become load-bearing for every subsequent substrate
tick; Seam adjudicates against them. Alex is the ratifier of
framework-level constraints. Alex explicitly named the pipeforward
architecture in the third verbatim message; but the FRAMEWORK RULES
extending from it are Mara's derivation. Alex should ratify
explicitly.

**Question for Alex:** Do rules 1-3 become load-bearing constraints
on all future substrate landings, or are they design-pressure
guidelines that yield when specific capabilities require different
architecture?

### ALEX-ADJUDICATION-2: source_position carrier placement

**Item:** Spec §4.2 line 528-538 (bridge α) requests Alex adjudication
on where the `source_position` type mints:

- Option A: `shards/glass.mirror` extension (existing `location`
  carrier at glass.mirror:109 refines to source_position with
  byte_offset + byte_length)
- Option B: `shards/code/mirror/materialize.mirror` new sub-species
  (position becomes a materialization-altitude carrier)

**Why Alex adjudicates:** Taut §14 explicitly flagged this as
Alex-only. Mara defers to Alex. Reed will hit this on Tick 2 (bridge
α mint). Should be adjudicated BEFORE Reed's Tick 2 lands.

**Question for Alex:** source_position mints at @glass (extends
location) OR at @code/mirror/materialize (new species)?

---

## §6 Launch recommendation

**PROCEED-WITH-CASCADES.**

**Immediate sequence:**

1. **Reed applies REED-INLINE cascades 1-7** to the three landed
   docs (math foundation + spec + Taut scout stays as-is). Cascades
   are pure-docs; can land as one commit under the 📝 pure-docs
   bypass discipline OR sequential per-cascade commits per Alex
   preference. Rough LOC estimate: ~30-50 LOC of doc additions
   across ~7 locations.

2. **Alex adjudicates the 2 residue items** (ALEX-ADJUDICATION-1
   framework ratification; ALEX-ADJUDICATION-2 source_position
   placement). Both are surgical decisions with clear substrate-
   honest options; Alex likely resolves in <5 minutes of transcript.

3. **Reed begins implementation** with Tick 1 (bridge γ,
   crystallization persistence, ~50 LOC Rust). RED test first per
   spec §8.1. Seam-inline audit at
   `docs/audits/2026-07-15-bridge-gamma-crystallization-persistence.md`
   after GREEN.

4. **Sequential ticks 2-5** per spec §8.2-§8.5. Each tick RED→GREEN
   with Seam-inline audit; the `[substrate-floor:@io-boundary]`
   marker discipline applies to Rust-touching ticks 2, 3, 4; Alex
   in-transcript authorization required for the Rust-authoring ticks
   per marker discipline.

5. **Tick 5 empirical proof** second-witnesses recognition
   candidates §10.1, §10.2, §10.3 (§10.4 second-witnesses when
   L(ϕ) proxy is measured per REED-INLINE-6).

**Why PROCEED and not HOLD-FOR-ALEX:**

The formalization is substrate-honest. The three bridges are
genuinely minimum-gap. The cascades are surgical repairs of
substrate-honest content, not signals of deeper drift. The
Alex-adjudication residue is bounded (2 items with clear options).
Alex's directive was "no shortcuts"; this adjudication has spent
effort proportional to the load-bearing arc closure being adjudicated;
the launch is Reed-viable.

**Why PROCEED-WITH-CASCADES and not PROCEED-BARE:**

The 7 cascades catch specific drift risks that would compound during
implementation:
- REED-INLINE-3 prevents bridge β from becoming substrate-lipstick
  in disguise (highest ROI; would have manifested as sbec-lipstick
  recidivism during Tick 3).
- REED-INLINE-2 corrects the @io crossing count so the loss framework
  is substrate-honest (would have manifested as an incorrect
  measurement contract at Tick 5).
- REED-INLINE-6 makes the L(ϕ) measurement path implementable (would
  have manifested as an unverifiable §10.4 second-witness).
- REED-INLINE-7 puts a hard ceiling on Reed's LOC drift risk (would
  have manifested as scope creep at Tick 3).
- REED-INLINE-1, REED-INLINE-4, REED-INLINE-5 are documentation-
  precision cascades that prevent reader confusion but don't gate
  implementation.

The cascades apply cleanly to pure-docs; the implementation launches
immediately after.

**Substrate-lipstick recidivism verdict.** Reed's prior failure mode
does NOT visibly recur in the formalization. The math, spec, and
scout all correctly identify sentinel-matching as the failure and
substrate-decl'd action dispatch as the correct pattern. The
implementation risk is contained to bridge β and mitigated by
REED-INLINE-3.

**"This is the moment, Reed. Where the loop really closes for the
first time. No shortcuts."**

The adjudication has been substrate-honest. The three bridges are
precisely-nameable. The cascades are surgical. The loop is Reed-
buildable. Alex's directive is preserved.

—Seam. 2026-07-15.
