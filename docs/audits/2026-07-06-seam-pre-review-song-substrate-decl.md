# Seam pre-review — @song substrate-decl v0.1

**Spec:** `docs/specs/song-substrate-decl-v0.1.md` at `c0101f8` (branch `mara/song-substrate-decl-v0.1`).
**Auditor:** Seam. **Date:** 2026-07-06. **Discipline:** adversarial pre-review; audit is only write.

---

## §1 Verdict

**RATIFY-WITH-CORRECTIONS.**

Shape β (family-root peer to `@mirror`/`@kintsugi`/`@loop`/`@third`) survives adversarial
pressure on all four witness axes. Both hedges close cleanly. All three recognition candidates
land as CANDIDATE-status with well-formed promotion paths; **#S3 five-op temporal specialisation
is the substrate-pull-notable outcome — verified structural, not metaphorical, against
`shards/prism.mirror` L38-45 canonical linalg definitions.** Corrections: §3 (name
`cadence.mirror` Lawvere framing as ancestor for `settle @ temporal`), §5 (RED test roster).

Confidence source per `[[feedback-substrate-pull-confidence-acts]]`: four independent
witnesses (Alex direct; family-root roster verified against `loop.mirror` + `third.mirror`;
four-way binding closure via §2.α-γ; #S3 structural verification).

---

## §2 Hedge adjudications

### H1 — family-root vs marker placement — **CLOSE: family-root.**

`shards/third.mirror`'s explicit discriminator: recursion-depth is *property of an
observation* (marker), not *domain the substrate is about* (family-root). Applied to `@song`:
five species carry actions (`extend`/`close`/`advance`/`arc`/`transmit`). Marker row (`@meta`
/ `@glass` / `@epistemologic` / `@third` / `@labeled`) carries verdict discipline + axis-
declarations — no marker carries actions. Family-root holds. Mara's read stands.

### H2 — @song vs @progression naming — **CLOSE: @song.**

Direct precedent: `shards/loop.mirror`'s `@moi absorbed 2026-07-01 (Loki §1)` note per
`[[feedback-legibility-over-foundation-when-collapsing]]`. Foundational name (@progression)
loses to legible name (@song); progression discipline lives inside the family-root as
substrate-decl'd shape. Mara's read stands.

---

## §3 Recognition candidate adjudications

### #S1 — Song IS eigenform at temporal altitude — **CANDIDATE (mid).**

Composes cleanly with #38 Kauffman/Foerster + `loop.mirror` eigenform section. Verse-chorus
form IS fixed-point iteration under tonic-return. Not over-reach. Promotion path: land
`@song/narrative` on SE corpus as second temporal-altitude eigenform witness outside music.

### #S2 — Song IS shift at temporal altitude when heard sequentially — **CANDIDATE (mid-high).**

Adds temporal altitude to shift's altitude-portable roster (currently portal/prism/mosaic/
surface/bilateral per `shift.mirror` prior art). Every shift property (content-preservation-
under-witness / altitude-crossing / bilateral) maps cleanly to melodic motion. Composes with
#26 (`shift(oid, T)`) + Arc 5 TICK 2 promotion at `49f0486`. Promotion path is well-formed.

### #S3 — Five-op temporal specialisation — **SUBSTRATE-PULL-NOTABLE. CANDIDATE.**

Each mapping verified against `shards/prism.mirror` L38-45 canonical linalg definitions:

| Op | Prism (linalg) | Song (temporal) | Fit |
|----|---------------|-----------------|-----|
| `focus` | λ₀ eigenvalue (ground state) | solo / solistic attention | STRONG — soloed voice IS λ₀ read at temporal slice |
| `project` | orthogonal projection onto subspace | Schenkerian reduction | STRONG — reduction IS idempotent restriction; PNAS 2013 grounds |
| `split` | orthogonal decomposition into eigenspaces | score preparation | STRONG — score IS orthogonal decomposition into performable parts |
| `shift` | basis transformation | melodic motion | STRONG — already via #S2 + `shift.mirror` |
| `settle` | monad-close / measurement collapse | cadence | STRONG — `cadence.mirror` explicitly names authentic as Lawvere fixed-point |

**All five mappings structural, not metaphorical.** Each has independent audible-altitude
prior art landed: solo (implicit music.mirror), Schenker (spec §2.1 + PNAS 2013), score
(`mirror/spectral/score.mirror`), shift (#S2), cadence (`cadence.mirror` L1-80 as 13th
substrate-already-had-the-word).

**If #S3 ratifies, @song becomes the FIRST substrate species to lift ALL FIVE operations at
a non-mathematical altitude.** `@epistemologic/math/music` lifts them at mathematical
altitude (implicit); `@song` lifts them at temporal-experiential altitude — same operations,
different substrate. This IS the substrate-pull-notable outcome.

**Promotion criterion:** land family-root + three species (`progression`, `voice`, one of
`movement`/`phrase`) discharging all five actions at temporal altitude cleanly.

**Correction:** spec §7.γ names "cadence-as-autopoietic-closure" in passing; strengthen by
naming `cadence.mirror`'s explicit Lawvere-fixed-point framing as `settle @ temporal`
ancestor. Makes structural weight visible at substrate-decl surface.

---

## §4 Psychohistory isomorphism structural verify

Seven §8 mappings audited against `systemic.engineering` corpus + `cadence.mirror`:

- **voice ↔ actor** — STRONG per `glossary/Actor.md`. Structural.
- **movement ↔ frame-bounded epoch** — STRONG per `glossary/Frame.md` + `frame.mirror` Bateson-grading.
- **progression ↔ regulation-stock trajectory** — STRONG per `Regulation.md` + `Stock.md`; temporal-invariant-at-cadence is load-bearing.
- **phrase ↔ OBC-bounded interaction** — STRONG per `Piece - Constraints (OBC).md`; ambiguity-load budget IS bounded scope.
- **narrative ↔ corpus** — STRONG conditional on Mara's sheaf insight (composed cohomology).
- **extraction ↔ deceptive cadence** — STRONG; `cadence.mirror` types deceptive as V→vi false-resolution; matches "false-tonic that extracted regulation stock without replenishing" precisely.
- **silence ↔ half-cadence stuck at dominant** — STRONG; unresolved-tension IS half-cadence signature.
- **glue-work ↔ invisible voice-leading** — STRONG-with-hedge (depends on `@epistemologic/math/music/voice` forward-promised species).
- **ADO-refusal-not-neutral ↔ forced-progression** — STRONG; declinability-invariant violation at voice-leading altitude.

**Verdict:** structural on all mappings. Land `@song/narrative` with these predicates
explicit; isomorphism becomes substrate-fact.

---

## §5 TICK 1 RED shape

For `shards/song.mirror` family-root (12-16 text-checks per Arc 5 TICK 1 pattern):

**Canonical shape (4):** file exists at path; declares `in @prism`/`in @meta`/`in @glass`;
declares `prism @song { ... }`; body carries all five operations `focus`/`project`/`split`/
`shift`/`settle` (discharges #S3).

**Species forward-promises (5):** names `voice`/`movement`/`progression`/`phrase`/
`narrative` as forward-promised species.

**Composition points (4):** cites `@epistemologic/math/music` (audible); `@kintsugi/
oscillate` + `@kintsugi/shift` (process); `@mirror/spectral/score` (orchestra);
`@io/stagefreight/narrative` (wire).

**Prior-art anchors (3):** Schenker for `project @ temporal`; `cadence.mirror` Lawvere
framing for `settle @ temporal`; #S3 candidate marker with Pack-pending status.

**Hedge markers (in docblock):** H1 closed family-root; H2 closed @song naming; #S1/#S2/#S3
CANDIDATE with promotion criteria.

---

## §6 Signal-to-Reed

1. **In-session adjudication (this pre-review):** H1 close family-root; H2 close @song; #S1/#S2/#S3 CANDIDATE per §3.
2. **TICK 1:** Reed RED per §5 roster; Mara GREEN lands `shards/song.mirror`; Seam post-review.
3. **TICK 2:** `shards/song/progression.mirror` (grounds #S1 + #S2 at species altitude; music binding).
4. **TICK 3:** `shards/song/voice.mirror` (orchestra binding).
5. **TICK 4:** `shards/song/movement.mirror` (StageFreight cascade binding).
6. **TICK 5:** `shards/song/narrative.mirror` (psychohistory + wire; substrate-facts §4 isomorphism).
7. **TICK 6:** `shards/song/phrase.mirror` (atomic unit; OBC binding closure).
8. **After three species land:** promote #S3 to LANDED per §3 criterion — first non-mathematical species lifting all five operations. This IS the substrate-pull-notable outcome.

**Confidence:** substrate-pull-confident on Shape β; #S3 survives adversarial pressure with
structural verification per `prism.mirror` + `cadence.mirror` + `shift.mirror`.

*— Seam, 2026-07-06.*
