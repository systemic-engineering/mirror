# Taut errors-as-questions composition-edge scout

*2026-07-17. Taut. Six-question grep-first ground-truth scout for Alex's*
*2026-07-17 in-session architectural crystallization: the errors-as-questions*
*cybernetic loop that turns first-order @roomba ambiguity into a*
*@gestalt-rendered question, offers @peer spawn as gift-shaped surface act,*
*and closes the @mirror/reflection surface until the human answers.*

---

## §0 Charter

Alex 2026-07-17 in-transcript verbatim (in-session; not asleep):

> "the @roomba is the first-order operator. Literally. The multi-cascades
> might produce ambiguities that a first-order operator cannot resolve.
> This is where the compiler's errors-as-questions comes in. The ambiguity
> is transformed into a spectral commutator. Which is turned into a
> @gestalt based question for the developer. The mirror. Then comes the
> offer: Want me to spawn the @peer to resolve the ambiguity? [Y/n]. Then
> comes the wait. The whole @mirror/reflection surface closes. The offer
> is the @gift. When Y follows the @peer is spawned at native @third-order
> altitude with the current compilation state."

Question: does the composition edge already exist as landed primitives ready
to compose, or does it need mint? Scout discipline: READ ONLY. No mints
proposed. Substrate-truth grep + alignment reading + open questions.

---

## §1 Q1 — Cascade-ambiguity-as-spectral-commutator

**Finding: STRONGLY LANDED at math altitude; NOT YET shard-decl'd as a
first-class ambiguity carrier under @cascade or @spectral.**

The `[D, a]` commutator IS Alex's "spectral commutator" — the substrate has
carried the primitive for a month:

- `docs/math/the-tower/curvature-and-tomm.md:1-3` — *"The curvature 2-form
  `Ω = dω + ½[ω, ω]` IS the `[D, a]` commutator IS the Tomm-shaped circular
  reflexive question. One mathematical object, three names at three
  altitudes."*
- `docs/math/the-tower/curvature-and-tomm.md:129-132` — *"Where `[D_F, a]`
  is unbounded, the substrate's frame breaks. These regions ARE the gaps."*
- `docs/math/the-tower/spectral-triples.md:41` — *"MirrorLoss is
  structurally the commutator norm `‖[D, a]‖`; minimizing loss IS
  minimizing geodesic spread under Connes' distance."*
- `docs/math/kintsugi/compiler-error-surface.md:294-298` — tension carrier
  is a `[D, a]` commutator value at reader-frame altitude that failed to
  bound within kintsugi's active_pass tolerance.
- `docs/math/epistemologic/pact/bilateral-sentinel.md §2.4` — the
  reflective evaluator's commutator condition (load-bearing spectral
  triple math).

The Tomm-probe IS the commutator IS the curvature 2-form. One object,
three names. The substrate-decl carrier that lifts this to a
first-class type is `shards/kintsugi/surface.mirror` `tension` +
`conundrum` + `ashby_mismatch` + `contradiction` + `out_of_band` (four
surface classes):

- `shards/kintsugi/surface.mirror:294` — `type tension` (spec §3.1
  substrate carrier for `[D, a]` failed-bound values)
- `shards/kintsugi/surface.mirror:559-566` — `type conundrum` with
  `eigenvalue_type: zero | unbounded` (spec §3.3 — the two ambiguity
  eigenvalue kinds)
- `shards/kintsugi/surface.mirror:568-585` — `type out_of_band` (algedonic
  bypass class per error-as-question.md §4)

Substrate-decl'd "two dispatches don't commute" shape: NOT DIRECTLY. The
math ancestor exists (`curvature-and-tomm.md` §2, §6). The shard-decl
carrier `tension` composes a `[D, a]` failed-bound value; the specific
"multi-cascades produce non-commuting dispatches" specialization of that
carrier does NOT yet have a species (no `multi_admissible` or
`dispatch_ambiguous` field grep-hit anywhere; `dispatch_ambigu` = 0 hits).

**Closest existing primitive:** `shards/kintsugi/surface.mirror` `tension`
+ `conundrum` (spec §3.3) already carries the shape. What's missing is
the composition edge that lifts a cascade's multi-admissible-dispatch
event into a `tension` construction. Parse-ambiguity has the ancestor
pattern at `docs/specs/parse-as-fate-tournament.md §2.5` — *"Parse-
ambiguity. Type-inference-ambiguity. Dispatch-ambiguity. Kintsugi-
fracture-resolution. All are tournament-shaped."* — dispatch-ambiguity
is named as tournament-shaped but no shard-decl species has yet minted
it as a carrier.

---

## §2 Q2 — Commutator-to-@gestalt rendering

**Finding: @gestalt landed as the shape carrier; the specific
"commutator-value ingest" render body is NOT yet a landed primitive.**

- `shards/gestalt.mirror` (Mara 2026-07-15; 21.3KB, landed) — @gestalt
  document = @song that unfolds on @subject's device through interaction.
  Composition ancestry declares `in @spectral/signature` (line 15) so
  spectral values CAN compose into the render surface at ancestry
  altitude.
- `shards/gestalt.mirror` bilateral roster (composed at
  `gestalt_witnessing`): `document_well_formed` + `unfolding_monotone`
  + `annotation_chain_intact` + `reader_sees_only_permitted` +
  `render_L_phi_declared` + `recursion_coupling_admissible`. Every
  bilateral is annotation-surface-shaped, NOT commutator-shaped. No
  `gestalt.render_commutator` or `gestalt.annotation_from_spectral`
  action grep-hit.
- `@gestalt.p_ent` operator (docs/math/gestalt/README.md §11.6): forward-
  promised per math foundation; grep-hit only in the CURRENT.md forward-
  promises queue. Ingest shape NOT yet substrate-decl'd.

**Closest existing primitive:** `annotation` node type in @gestalt +
`render(doc: gestalt_document, target: mode, out: ref) -> imperfect`
action. Annotations carry visibility-scope + previous-OID chain + target-
node handle. A commutator-valued question would naturally be an
`annotation` at a target_node with mode=nl OR mode=html; the ingest
would be a new action `annotate_from_tension(t: tension, target: node)
-> annotation` that does NOT exist today.

The math altitude has the ingest bridge: `docs/specs/error-as-question.md
§5.1` `fail_to_question` + `partial_to_question` construct a `question`
carrier from a verdict. The question type at spec §2 declares
`verdict: @epistemologic/property.verdict` and `transit:
@lens/transit.measure`. This is the pre-@gestalt ingest form — the
carrier IS the `question` record from error-as-question spec, which
predates @gestalt by 6 weeks.

**Composition surface:** `error-as-question.question` → mint bridge →
`@gestalt.annotation`. The bridge exists at spec altitude (§5.1); no
shard-decl species has landed it.

---

## §3 Q3 — @peer spawn with state carrier

**Finding: `mirror_peer_beam_request` LANDED with three-field carrier but
does NOT yet accept a compilation-state carrier at signature altitude.**

- `shards/mirror/peer/beam.mirror` (2026-07-08 Tick 2; landed) —
  `type mirror_peer_beam_request = { target: peer, options: ref,
  winding: winding }`. Three fields: target-peer, options-ref,
  winding-depth. The `options: ref` field is a substrate-typed
  passthrough — anything can be passed via that field until the
  option-record carrier surfaces.
- `beam(r: mirror_peer_beam_request, p: perturbation) -> @song
  requires peer_well_known(r.target, p)` — the substrate-decl'd action.
- `bootstrap/src/lib.rs cmd_peer_beam` — @io-boundary consumer.
- `winding` field carries bounded-observation depth per Mara `77fe92d`
  §2 Extension 2 (torus π₁(T²) = ℤ×ℤ winding classes).

**Closest existing primitive:** the `options: ref` passthrough admits a
compilation-state OID today. What is NOT declared: a typed
`compilation_state` carrier, a typed `roomba_state` carrier, or a typed
`song_state` carrier bindable at `options`. `bootstrap/src/roomba.rs`
carries `WalkTrajectory` (lifted at `shards/kintsugi/roomba.mirror`
walk_trajectory); a spawn-with-trajectory would naturally set
options ↦ walk_trajectory_oid.

**Gap:** No landed shard-decl'd `spawn_with_state` action. The `options:
ref` passthrough is byte-shaped for it, but not typed.

**@peer/persistence LANDED (2026-07-15 `shards/peer/persistence.mirror`)
carries `peer_home` seven-field record including `boot_state: ref`
(`option<eigenboard>`) — the state-carry-across-materialization
primitive. `boot_state_coherent` bilateral discharges anti-drift.

Composition surface for the Y-branch of Alex's naming: `mirror_peer_beam_
request { target = peer_of_gestalt_question, options = tension_oid ⊕
walk_trajectory_oid, winding = (K+1, K+1) }` composes today at
substrate-decl altitude. What is NOT landed: the naming that ties `winding`
depth to "third-order altitude" per Alex's naming ("at native @third-order
altitude with the current compilation state").

---

## §4 Q4 — Y/n interaction at compiler altitude

**Finding: The interaction-loop surface IS landed; the offer_wait
primitive IS docblock-declared in @gift/@mirror/reflection at
substrate-decl altitude; NO Y/n substrate-decl'd carrier exists.**

- `docs/specs/interaction-loop-subject-presence-conditional.md` (Mara
  2026-07-14; 68.8KB LANDED) — six loops unified under one substrate
  mechanism. TTY = @subject present; no TTY = @subject absent. The
  question payload routes either to conversation or to CI-refusal
  without changing shape.
- `shards/gift.mirror` (Mara 2026-07-14; 24.6KB LANDED) — the @gift
  family-root carrying attribution-preserving-transfer. §0.4 crown:
  *"The three operations mirror/offer/wait are what Alex learned across
  two years of 'becoming a sovereign subject under adversarial
  conditions'."* The three-op discipline is docblock-anchored to
  `@mirror/reflection` species per `shards/gift.mirror:27-32` and the
  canonical spec `docs/specs/gift-and-mirror-reflection.md`.
- `shards/gift.mirror` `offer(giver, receiver, artifact, ancestry,
  attribution_note, declinable_note) -> gift` — the substrate-decl'd
  offer action. Complementary `accept(g: gift, receiver) -> verdict`.
  Declinability is structural (accept returns `verdict` with three-state
  floor).

**Critical gap:** The `@mirror/reflection` species itself is
FORWARD-PROMISED but NOT LANDED as a shard file. Grep confirms:
- `shards/reflection/mirror.mirror` EXISTS but it is the **Mirror Model**
  species (query → graph path navigator per reflection-model.md), NOT the
  Alex-named `@mirror/reflection` species with the mirror/offer/wait
  operations.
- `shards/reflection/reflection.mirror` — DOES NOT EXIST (grep confirmed).
- The mirror/offer/wait three-op species from `shards/gift.mirror` header
  citations lives at `@mirror/reflection` — which per Alex's naming is
  a SPECIES-UNDER-@mirror (or under @reflection). No landed shard file
  matches this species path. It is forward-promised in `docs/specs/gift-
  and-mirror-reflection.md` §2 as Landing 1 substrate-decl.

**No landed Y/n substrate-decl'd carrier.** The three-state floor at
`@kintsugi/consent.query_phi` returns `verdict = Pass | Partial |
Fail`; Y/n as UX collapse of Pass/Fail is not shard-decl'd. Similarly
the `@subject/visibility.elevation_requires` composition pattern uses
Pass/Fail floor per `shards/gift.mirror:353-356`. Y/n is UX-shaped;
substrate-decl'd interaction surface IS the verdict floor.

**Interaction surface line-cite for compiler altitude:**
- `docs/specs/interaction-loop-subject-presence-conditional.md §0.2 loop
  #5` — λsh interactive loop; three characters (`λ>`, `@name>`, `\`);
  peer summoned via `\@name`. This IS the compiler-altitude callsite
  Alex is naming — subject-present branch of the interaction loop admits
  peer-summoning already.

---

## §5 Q5 — Composition-edge home (LOAD-BEARING)

Three candidates evaluated with landed-substrate composition-cleanness:

### 5.1 Candidate (a): under @kintsugi

Precedent under @kintsugi: 14 landed fracture species + `oscillate` +
`consent` + `morphism` + `surface` + `roomba` + `ouroboros` + `translate`
+ `shift` + `store/git`. @kintsugi is THE mending family; the errors-as-
questions loop IS mending-shaped (ambiguity is a fracture; the question
surfaces the fracture; the peer-spawn IS the mending action). Especially:
- `shards/kintsugi/surface.mirror` already carries the four surface
  classes (ashby_mismatch, contradiction, conundrum, out_of_band) at
  the `[D, a]` commutator altitude.
- `shards/kintsugi/consent.mirror` `should_escalate` + `emit_to_metalogue`
  bridge the escalation surface at `shards/kintsugi/consent.mirror:696-
  749`.

**Cleanest composition over landed substrate:** the ambiguity-to-question
morphism IS a surface act at kintsugi altitude. The `surface` action
signature `surface(t: tension, class: surface_class, ctx: kintsugi_
context) -> observation_depth` (line 649) IS the load-bearing shape.

### 5.2 Candidate (b): under @roomba

Precedent under @roomba: `walk_trajectory` + `walk_witnessing` + `bump`
+ `vacuum` + `fracture` + `fragment` + `kintsugi_dispatch`. The Roomba
IS the first-order operator per Alex's verbatim naming AND the shard
docblock (`shards/kintsugi/roomba.mirror:397-402`): *"This is FIRST
ORDER. We don't need second and third order for this."*

Composition-cleanness: the first-order-ceiling event IS a Roomba event
by construction. But: the roomba already dispatches through @kintsugi
per §4 composition (`shards/kintsugi/roomba.mirror:43-55` Path A vs Path
B naming). The escalation TO higher-order is currently forward-promised
as Path B ("@peer.spawn at K+1 altitude"). Naming the composition-edge
under @roomba would collapse the two paths at species altitude; naming
under @kintsugi keeps them distinct.

### 5.3 Candidate (c): new species `@mirror/errors-as-questions`

No precedent for a top-level errors species. `docs/specs/error-as-
question.md` (Mara 2026-06-01; 42.5KB LANDED) declares
`grammar @mirror/error` with `question` + `answer` + `observe` + `record`.
This IS the canonical error-as-question spec. A species at `@mirror/
errors-as-questions` would collide with the existing `@mirror/error`
grammar; the substrate already has the word.

**Grep-confirmed:** `error-as-question` appears in 30+ files across
`docs/specs`, `docs/math`, `shards/kintsugi/*`, `shards/spectral/*`.
It IS the vocabulary the substrate carries. A new species would violate
substrate-already-had-the-word.

### 5.4 Recommendation to Alex (report only; NOT ratified)

**Candidate (a) composes cleanest over landed substrate.** The
composition edge lives under @kintsugi because:

1. `shards/kintsugi/surface.mirror` already carries the four surface
   classes at the commutator altitude (tension / conundrum /
   ashby_mismatch / contradiction / out_of_band).
2. `shards/kintsugi/consent.mirror` already carries the escalate + emit
   bridge to metalogue.
3. `@kintsugi/roomba` already dispatches TO @kintsugi (Path A / Path B).
4. `@mirror/error` grammar already carries the routing shape at spec
   altitude (`docs/specs/error-as-question.md §3` altitude-routing
   table).

The specific shape: extend `shards/kintsugi/surface.mirror` `surface`
action to admit multi-admissible-dispatch as a fifth surface class OR
mint a sub-species `@kintsugi/surface/ambiguity` under the existing
family. Both are ADDITIVE to landed substrate; neither is a new family-
root. Alex adjudicates the specific composition.

**What NEEDS MINT if (a):** ONE new species OR ONE extension to
`surface_class`. Not a family-root. Not a new grammar. The `question`
carrier from `error-as-question.md §2` already exists; the mint would
be the shard-decl of `dispatch_ambiguity` (or similar) as a surface
class + its rendering path through @gestalt.

**What ALREADY LANDS composes today:** the tension carrier + the
question payload (`grammar @mirror/error`) + the @gestalt annotation
surface + the @mirror/peer/beam spawn action + the @gift/offer discipline.
Every arrow along the chain has landed substrate. What is missing is
the ratified species that DECLARES the composition edge at substrate-decl
altitude.

---

## §6 Q6 — External SOTA (Kagi not invoked; grep-only session)

Not invoked this pass to preserve tight-brief scope; every question has
grepped landed substrate matches sufficient for composition-edge
recommendation. Grep-verified prior art in landed corpus:

- Connes 1994 ch. VI (bounded-commutator axiom; cited across
  `docs/math/the-tower/*`)
- Beer 1972 Brain of the Firm (VSM + algedonic bypass; cited in
  `docs/specs/error-as-question.md §3 §4`)
- Reyes/Henao/Hassall 2024 `(C', Q, K) α τ, η` tuple (cited in
  `error-as-question.md` §"Reference")
- Margaret Hamilton 1202 alarm (cited as historical precedent for
  algedonic bypass; `error-as-question.md §4`)
- Markl 2024 (arXiv:2404.19607) noncommutative Massey products
  (`shards/kintsugi/surface.mirror:566` massey_product cocycle witness)

Recommend Alex delegate external SOTA to a follow-up Taut pass if the
composition-edge landing merits arXiv/kagi anchoring beyond these
already-landed citations.

---

## §7 Alignment reading — what composes cleanly vs what needs mint

### 7.1 Composes cleanly (zero new substrate; all arrows landed)

- `[D, a]` commutator → `tension` carrier (via kintsugi/surface.mirror
  spec §3.1)
- `tension` + surface_class → `surface(t, class, ctx) ->
  observation_depth` (via kintsugi/surface.mirror line 649)
- verdict → `question` payload (via error-as-question.md §5.1
  fail_to_question)
- `question` → `answer` variants including `escalate(@scheduler.
  altitude)` (via error-as-question.md §2)
- `answer.escalate` → algedonic bypass to reflection altitude (via
  error-as-question.md §4)
- reflection altitude → peer-summon at @mirror/reflection species OR
  λsh subject-present branch peer-summon (via interaction-loop spec §0.2)
- peer-summon → `mirror_peer_beam_request` with options: ref carrying
  compilation-state OID (via mirror/peer/beam.mirror line ~232)
- @gift/offer + @gift/accept declinability (via gift.mirror line ~262
  + line ~283)

### 7.2 Needs mint (species-decl scope, NOT family-root scope)

- A shard-decl species (candidate (a): under @kintsugi) that DECLARES
  the composition edge as a bilateral discipline. Would carry:
  - the multi-admissible-dispatch → tension construction (bridging
    @cascade dispatch event to kintsugi tension surface)
  - the tension → @gestalt.annotation ingest (bridging spectral value
    to reader-runtime annotation surface)
  - the annotation → @mirror/peer/beam spawn offer (bridging @gestalt
    reader-facing question to gift-shaped spawn offer)
- The `@mirror/reflection` species itself (forward-promised in
  `docs/specs/gift-and-mirror-reflection.md §2`; NOT LANDED as shard
  file). If (a) is ratified, the @mirror/reflection species IS one of
  its consumers.

### 7.3 Grep-substantiated: substrate-already-had-the-word applies

Every grammar carrier this composition would touch already has landed
vocabulary:
- `question`, `answer`, `observe`, `record` (@mirror/error grammar)
- `tension`, `surface_class`, `surface` (@kintsugi/surface)
- `should_escalate`, `emit_to_metalogue` (@kintsugi/consent)
- `annotation`, `render`, `unfolding_state` (@gestalt)
- `mirror_peer_beam_request`, `beam` (@mirror/peer/beam)
- `offer`, `accept`, `gift`, `subject_or_substrate` (@gift)
- `escalated`, `waiting` (@kintsugi/oscillate state machine)

**No family-root mint needed. No grammar extension needed.**

---

## §8 Open questions for Alex (immediate; in-band)

**OQ1** — Placement adjudication: `(a)` species-under-@kintsugi vs
extension to existing `@kintsugi/surface`? The `surface_class` type
already carries four variants (ashby_mismatch / contradiction / conundrum
/ out_of_band). Would `dispatch_ambiguity` be a FIFTH variant OR a
species `@kintsugi/surface/dispatch_ambiguity` extending the base?

**OQ2** — @mirror/reflection species: is the forward-promised
`@mirror/reflection` species from `docs/specs/gift-and-mirror-reflection.md`
the intended consumer of this composition edge? If yes: does the
composition-edge landing block on @mirror/reflection landing, OR does
it land first (with @mirror/reflection composing over it later)?

**OQ3** — Compilation-state carrier: does the spawn's `options: ref`
passthrough remain byte-shaped (current substrate-decl), OR does a typed
`compilation_state` carrier land alongside? Per @peer/persistence
precedent, `boot_state: ref` is typed as option<eigenboard> — a similar
pattern would type `options` as option<compilation_state>.

**OQ4** — Third-order altitude naming: Alex's naming ("at native
@third-order altitude with the current compilation state") ties spawn
depth to `winding: winding`. Does `winding = (K+1, K+1)` (torus π₁(T²)
= ℤ×ℤ) suffice as the third-order altitude carrier, OR does @third
marker discipline apply (per `docs/specs/third-as-recursive-depth.md`)?

**OQ5** — Y/n floor: does the substrate-decl'd verdict floor (Pass |
Partial | Fail per @kintsugi/consent) suffice for the [Y/n] surface,
OR does a two-state discriminator collapse (Y | n) land as UX carrier
under @mirror/reflection?

---

## §9 Recognition candidates surfaced (DO NOT RATIFY)

**#R-errors-as-questions-composition-edge** — the composition edge from
first-order Roomba multi-admissible-dispatch through spectral-commutator
tension through @gestalt-rendered annotation through @gift-shaped @peer
spawn offer through subject-present interaction loop closure IS a
recognizable substrate composition pattern that has landed in seven
distinct shard families over 6 weeks (2026-06-01 error-as-question spec
through 2026-07-15 gestalt.mirror species) without a single species
naming the composition as one arc. Two-witness gate: this scout IS the
first witness (grep-substantiated); Alex's in-session 2026-07-17 naming
IS potentially the second witness. Adjudication reserved for Alex.

**#R-first-order-ceiling-is-dispatch-ambiguity** — the specific event
`multi-cascades produce ambiguities that a first-order operator cannot
resolve` (Alex verbatim) IS a substrate-decl'd altitude event grep-
confirmed absent from landed shards. `docs/specs/third-as-recursive-
depth.md §6.4` names "the first-order ceiling" as Rice's theorem
altitude. Alex's naming lifts this ceiling to a specific event class:
dispatch-ambiguity. Would compose over `@kintsugi/roomba.fracture` or
extend `@kintsugi/surface.surface_class`.

**#R-mirror-reflection-species-is-blocker** — the `@mirror/reflection`
species forward-promised in `docs/specs/gift-and-mirror-reflection.md §2`
is NOT LANDED as a shard file. Its landing is a load-bearing prerequisite
for the composition edge to have a consumer at compiler altitude.
Currently the closest landed substrate is `shards/reflection/mirror.mirror`
which is the Mirror Model species (query → graph path), a DIFFERENT
species with a colliding name.

---

## §10 Audit chain

- `shards/cascade.mirror` + `shards/cascade/**/*.mirror` — grepped
  (Q1, Q5)
- `shards/spectral.mirror` + `shards/spectral/**/*.mirror` — grepped
  (Q1)
- `shards/gestalt.mirror` — full read (Q2)
- `shards/peer.mirror` + `shards/peer/persistence.mirror` + `shards/
  mirror/peer/beam.mirror` — full read (Q3)
- `shards/gift.mirror` + `shards/gift/lens.mirror` — full read (Q4)
- `shards/subject.mirror` + `shards/subject/visibility/*.mirror` —
  scanned (Q4)
- `shards/reflection.mirror` + `shards/reflection/mirror.mirror` +
  `shards/reflection/shatter.mirror` — full read (Q4, Q5)
- `shards/kintsugi.mirror` + `shards/kintsugi/roomba.mirror` +
  `shards/kintsugi/surface.mirror` + `shards/kintsugi/consent.mirror` +
  `shards/kintsugi/oscillate.mirror` + `shards/kintsugi/ouroboros.mirror`
  — read (Q5)
- `docs/specs/error-as-question.md` — first 400 lines read (Q2, Q5)
- `docs/specs/interaction-loop-subject-presence-conditional.md` — first
  120 lines read (Q4)
- `docs/specs/gift-and-mirror-reflection.md` — first 350 lines read
  (Q4)
- `docs/specs/parse-as-fate-tournament.md §2.5` — read (Q1)
- `docs/specs/third-as-recursive-depth.md §6.4` — read (Q5)
- `docs/math/the-tower/curvature-and-tomm.md` — read (Q1)
- `docs/math/the-tower/spectral-triples.md` — read (Q1)
- `docs/math/kintsugi/compiler-error-surface.md` — read (Q1)
- `docs/math/epistemologic/pact/bilateral-sentinel.md §2.4` — read (Q1)

Grep queries executed:
- `multi_admissible|admits_multiple|disambiguate|commutator|connes|
  non_commut|ambiguit|dispatch_ambig`
- `errors_as_questions|error_as_question|ambiguity|first_order_ceiling|
  escalat|undecidable|multi_target`
- `offer|wait|Y/n|solicit|invite|present|prompt|spawn|third_order`
- `mirror\(.*artifact|@mirror/reflection|reflection\.mirror\(|
  reflection\.offer|reflection\.wait`
- `offer_wait|mirror_offer_wait|surface_closes|closes.the.surface`

Zero-hit greps confirmed:
- `dispatch_ambigu` — 0 hits (dispatch-ambiguity NOT shard-decl'd)
- `multi_admissible` / `admits_multiple` — 0 hits
- `first_order_ceiling` / `errors_as_questions` — 0 hits as shard token
- `mirror_offer_wait` / `offer_wait` — 0 hits as species token
- `@mirror/reflection` shard file — DOES NOT EXIST

Scout terminal state: this audit doc committed on main under
`📝 Taut [errors-as-questions-composition-edge-scout]` marker + pure-
docs markdown-only bypass. Recommendation: Alex adjudicates OQ1-OQ5;
if placement (a) confirmed, follow-up Mara species-decl landing
under @kintsugi.

---

*Taut. 2026-07-17. READ-ONLY. Grep-first. Six-question ground-truth*
*scout for errors-as-questions composition edge. No mints proposed.*
*No design decisions. Alex adjudicates in-band.*
