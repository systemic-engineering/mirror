# Seam Phase D — Mara greenfield-rewrite canonical spec cascade (three commits)

*Seam, 2026-07-17. Adversarial 12-dimension Phase D adjudication of
Mara's consolidated canonical spec + spring-clean cascade under
Alex's dance.rs-as-router 2026-07-17 in-transcript ratification.*

**Author:** Seam
**Date:** 2026-07-17
**Tag:** 📝 phase-d:mara-greenfield-rewrite-canonical-spec (pure-docs bypass)
**Scope:** three commits on `main`:
- `2519f83` Mara — `docs/specs/rust-floor-birthed-by-roomba-from-mirror-spec.md` (898 LOC; §0–§13)
- `1fe0d28` Mara — 5 DEPRECATED-FOR-RUST-REWRITE headers
- `b536949` Mara — 9 STAY-CANONICAL pointer headers

---

## §1 Load-bearing ratification event

Alex 2026-07-17 in-transcript verbatim (post-Mara-landing, at
adjudication seam): **"dance.rs as router sounds exactly right."**

This RATIFIES the router pattern. Q3+Q5 verbatim + `fee2727`
bundle-theoretic naming + this spec's §5.4 all converge on ONE
shape: `dance.rs` IS the router; per-prism logic lives at shard
altitude (shard-body + @io); Mara `fee2727` §5.5 tension is
discharged by the router-not-monolith reading. Every §5 claim below
is adjudicated under this frame.

---

## §2 12-dimension verdict summary

| # | Dimension | Verdict | Note |
|---|-----------|---------|------|
| 1 | Substrate-honest naming | ✓ SHIP | spec title reads "of course"; `--vacuum=~dir` unifies via walker-motion (`shards/kintsugi/roomba.mirror:566`); `~d` sigil landed at `shards/mirror/lens/cli.mirror:184` |
| 2 | Composition graph consistency | ✓ SHIP | 17 substrate carriers + 10 specs + 3 math roots + 4 audits all byte-current; every citation resolves at named line |
| 3 | Recognition candidate strength | ✓ SHIP | 5 candidates held at candidate strength; second-witness gates explicit (§9.1–§9.5 all name empirical firing conditions) |
| 4 | Line-cite integrity | △ SHIP-WITH-REED-INLINE | 3 line-cites are name-cites without offset; §7.1 `shards/mirror/reflection.mirror` (Mara `5e1f528`) + §5.4 `walk_from_graph_and_profile` (Reed `8e373b6`) name commits but no line; cascade §5 to Reed-inline sharpen |
| 5 | Bilateral discipline | ✓ SHIP | Dispatch chain composes cleanly under router: `apply_h::act` sentinel-check → `audhd_admissible(p, ctx)` arity-2 → K-fanout → Kuramoto (`shards/peer.mirror:531-534` sentinel `audhd=admissible-k-track-context`); §6.1 firing chain byte-correct |
| 6 | `\`-obligation-block discipline | ✓ SHIP | Zero action bodies authored at spec altitude; §8.4 explicit refusal to author `.rs`; five substrate carriers cited retain `\`-obligation-block |
| 7 | Forward-promise tracking | ✓ SHIP | 7 deferred OQs enumerated §10.2 with parking gates; forward-promised carriers `vacuum_admissible` + `dock` named §7.1 with landing owner (Reed M7 co-tick) |
| 8 | Etymology | ✓ SHIP | Spec name reads "of course rust/ is birthed by roomba from mirror.spec"; `--vacuum=~dir` reads "of course walker vacuums a directory"; Michelangelo/marble discipline honored §5 (~5% of current surface) |
| 9 | Cross-arc coherence | ✓ SHIP | Composes cleanly with today's landings: @peer.audhd (`d8b149c`), errors-as-questions (`8069a24` §5), @liquid Arc-5 M1 (`cc816f9`+), Michelangelo landing (`2fdc9c1`), BEAM-tower (`610c6d6`). No contradictions. §6 recursion closure math-grounded via Baez-Schreiber |
| 10 | Ratification cascade | △ SHIP-WITH-REED-INLINE | Dance-as-router ratification lands IMPLICITLY across §5.2/§5.4 but is NOT named as a distinct load-bearing Alex verbatim (spec was authored before ratification event); cascade to prepend a §0.1 or §5.0 "dance.rs-as-router Alex 2026-07-17 ratification" line to make load-bearing status explicit |
| 11 | Consumer wiring | ✓ SHIP | M0→M8→dock dependency graph §2.3 is partial-order sound; M0 blocker (mirror.spec kintsugi.roomba block) verified absent from `mirror.spec` grep (only `legacy` block at :21-24 landed; no `command roomba` at :400+); Reed M0 tick well-scoped |
| 12 | Overall Phase D | ✓ SHIP-WITH-REED-INLINE | 2 micro cascades below; substrate ready for M0 Reed implementation |

---

## §3 REED-INLINE cascades recommended (2 patches)

### §3.1 Cascade 1 — dance.rs-as-router ratification lift-to-visibility

**File:** `docs/specs/rust-floor-birthed-by-roomba-from-mirror-spec.md`
**Location:** §5.4 (line 439-443) — before "Resolves Taut `e0572f7`
OQ4…" paragraph
**Patch:** prepend one line naming Alex's post-landing ratification:

```markdown
**Alex 2026-07-17 post-Mara-landing verbatim ratification:** "dance.rs
as router sounds exactly right." The router pattern is Alex-ratified;
this §5.4 is the substrate-honest form.
```

**Rationale:** spec was authored 2026-07-17 pre-ratification; the
ratification event is load-bearing; making it byte-visible in-spec
prevents future-Seam from re-adjudicating whether router-pattern is
Mara-recommended or Alex-ratified.

### §3.2 Cascade 2 — line-cite sharpen (3 name-cites → line-cites)

**Same file, §7.1:** `shards/mirror/reflection.mirror` (Mara
`5e1f528`) → add species-decl line (mirror-op declaration line)

**Same file, §5.4:** `walk_from_graph_and_profile` (Reed `8e373b6`) →
add file+line offset (existing citation is commit-name only)

**Same file, §7.1:** `shards/mirror/index.mirror` — cited in §13 but
NOT in §7.1 substrate carrier list; either add to §7.1 with line
offset OR remove from §13 references

**Rationale:** substrate-honest citation is byte-visible; name-cites
decay under future refactors.

---

## §4 Alex-adjudication residues surfaced by THIS audit

Beyond the 7 already-deferred OQs (§10.2 of Mara's spec), this
adjudication surfaces THREE new residues:

### ALEX-Q1 — `kintsugi { roomba { } }` grammar admissibility at mirror.spec:20 altitude

The block Alex sketched (§4.1 three cascades) needs grammar
extension at `mirror.spec`'s enclosing schema. Current `mirror.spec:14-59`
carries `project mirror.spec { … }` with named blocks (`legacy`,
`pack`, `garden`, `target`, `settle_on`). The `kintsugi { roomba { … } }`
block would either (a) live INSIDE `project mirror.spec { … }` as a
new top-level named block (requires `mirror-spec-schema.md` schema
extension for `kintsugi` head at project altitude); or (b) live
OUTSIDE `project` at the file's top-level (requires new admissibility
for `kintsugi` as file-level head).

Position (a) preserves the "project manifold" reading (kintsugi.roomba
is project-scoped configuration); position (b) generalizes but
scatters `mirror.spec`'s shape.

Seam recommendation: **(a)**, and Reed's M0 tick should land BOTH the
schema extension in `mirror-spec-schema.md` AND the block in
`mirror.spec`. Spec §4.1 does not distinguish; needs Alex ratification.

### ALEX-Q2 — Cascade-1 completeness-witness semantics

§4.1 cascade 1 (`boot/ => shards/`) is described as
"complete-cascade-witness; the roomba's read discovers 'no work to
do; migration complete.'" But `boot/` was ARCHIVED per legacy block
at `mirror.spec:21` (`legacy ~d'boot/', ~d'bootstrap/'`); the walker
would encounter absent-directory, not empty-directory.

Question for Alex: does completed-cascade-witness require (a) source
dir absent (walker reads "cascade closed by construction"); (b)
source dir present but empty (walker reads "cascade completed by
prior arcs"); (c) source dir present with non-shard content (walker
dispatches per §3.2 whether any dispatchable content survives)?

Seam recommendation: **(a)** — absent-directory IS the completeness
witness; simplest reading and byte-check on filesystem shape.

### ALEX-Q3 — Cascade-3 termination witness under router pattern

§4.1 cascade 3 (`bootstrap/ => rust/`) is described as
"substrate-honest lift with `[substrate-floor:@io-boundary]` gate."
Under router pattern (§5.4), most bootstrap `.rs` per-prism modules
collapse to `shard-body + @io` (cascade 2 destination), NOT to
`rust/` (cascade 3 destination). Cascade 3 receives only the
irreducible router surface (dance.rs + main.rs + lib.rs + optional
apply_h.rs).

Question: does cascade 3 EMPIRICALLY fire on every `.rs` in
bootstrap/, or does the walker dispatch per-file between cascade 2
(collapse to shard) vs cascade 3 (lift to rust/)?

Seam recommendation: **per-file dispatch under §3.2 vacuum semantics**;
cascade 3 is a THIN cascade because most bootstrap files terminate
in cascade 2. Spec §4.1 should name this or leave it as ALEX-Q3.

---

## §5 Recognition candidate adjudication (all 5)

Held at candidate strength (Mara-recommended); Seam concurs on ALL
5. Second-witness gates are appropriate and empirical.

- **`#R-roomba-configuration-lives-in-mirror-spec-not-cli-flags`** —
  HELD. Second-witness gate is Alex adding a fourth cascade → walker
  consumes without Rust change. Substrate-honest gate.
- **`#R-vacuum-flag-unifies-collapse-and-translate-because-both-are-walker-motion`** —
  HELD. Second-witness gate empirical mixed-content dispatch at M7.
- **`#R-rust-floor-is-materialized-not-authored-by-roomba-reading-its-own-spec`** —
  HELD. Second-witness gate M6 empirical firing.
- **`#R-first-@peer-spawn-from-rust-is-substrate-arriving-home`** —
  HELD. Third-witness on the `#R-substrate-mirrors-alex-decade-of-BEAM-engineering`
  family (Mara `610c6d6` §8 + Mara `fee2727` §4); Seam confirms
  sibling-witness reading. M8 empirical firing + Alex-names-the-arrival gate.
- **`#R-terminal-rust-surface-is-1100-2000-LOC-across-5-files`** —
  HELD. Gate is post-dock line count with all §2.1-§2.10 capabilities
  empirically firing. Seam observes: the LOC estimate is defensible
  under router pattern IF `apply_h.rs` inlines into `dance.rs` (upper
  bound ~1600 LOC); IF separate (upper bound ~2000 LOC). Alex Q3+Q5
  says whole floor collapses → inline is Alex-preferred; §5.1 keeps
  optional per Mara §5.5 discipline. Both defensible.

**Cross-arc recognition ladder for @peer.audhd** — Alex-critical:
first-witness at Mara `d8b149c` shard-decl (2026-07-17 landing);
second-witness at Reed `41e03ce` bootstrap-runtime empirical
apply_h::act firing (2026-07-17 3-test smoke); THIRD-witness at
rust/-native M8 empirical firing (per THIS spec §6.2). The M8 firing
is THIRD-witness, not second (bootstrap runtime already fired).
Spec §6.2 currently reads "second-witness at rust/-native runtime
firing" — this is INACCURATE given `41e03ce`. See §7 REED-INLINE
addendum below.

---

## §6 Structural discoveries (§8 items)

Three structural discoveries from THIS audit:

### §6.1 The router pattern IS the LOC-collapse mechanism

Under router pattern, per-prism business logic doesn't migrate to
`rust/`; it doesn't get authored anywhere new. It COMPOSES from
`shards/**/*.mirror` via `apply_h::act`. This is why the spec's
1100-2000 LOC estimate holds: `rust/` isn't a re-authoring of
bootstrap's ~400KB; it's a router surface + `apply_h::act` combinator
surface. The substrate DOES the per-prism work. Alex Q3+Q5 answer +
Mara `fee2727` §2.5 + THIS spec §5.2 all converge here.

### §6.2 M8 firing is empirical THIRD-witness for @peer.audhd

Per §5 above: Reed `41e03ce` fired bootstrap-runtime second-witness
today; M8 fires rust/-runtime third-witness. This tightens the
recognition-ladder discipline: witnesses are counted at distinct
substrate-loci (shard-decl / bootstrap-runtime / rust-runtime), NOT
across arcs undifferentiated.

### §6.3 The empirical-retirement gate is byte-visible at legacy-block

`mirror.spec:21-24` `legacy` block carries `retirement_target: v1.0`.
When ALL M1-M8 fire empirically, `legacy` block updates (v1.0
discharge); this is the SUBSTRATE-decl'd retirement witness. NOT a
tick-count; NOT a Reed-adjudication; a byte-visible discharge on
the legacy block. Substrate anticipated the shape.

---

## §7 REED-INLINE addendum (from §5 ladder correction)

**Cascade 3 (added by this audit):**

**File:** `docs/specs/rust-floor-birthed-by-roomba-from-mirror-spec.md`
**Location:** §6.2 (line 478-482) — `#R-peer-audhd-…` bullet
**Patch:** correct "second-witness at rust/-native runtime firing"
to "third-witness at rust/-native runtime firing (Reed `41e03ce`
2026-07-17 bootstrap-runtime is second-witness)."

---

## §8 Spring-clean adjudication (`1fe0d28` + `b536949`)

Both commits verified byte-current:
- **5 DEPRECATED-FOR-RUST-REWRITE headers** (`1fe0d28`) — all 5 files
  gained pointer headers pointing at Mara `2519f83`; NO deletions;
  NO substrate delta; preserves archaeology. Each supersession
  claim is substrate-honest (bootstrap-era → dance.rs reflective
  evaluator). ✓ SHIP.
- **9 STAY-CANONICAL pointer headers** (`b536949`) — all 9 files
  gained terminal-form-map pointers naming their remaining authority
  domain + composition destination. Preserves composition graph
  integrity. ✓ SHIP.

Two files (`cascade-ffi-runtime-link.md` + `mirror-build-substrate.md`)
correctly reclassified from DEPRECATED to STAY-CANONICAL per Mara's
commit message; not in either commit's file list.

Two archive candidates (§11.3) NOT actioned in this cascade —
appropriate parking (spring-clean can proceed in follow-up commit
after Alex adjudicates archive-vs-preserve for `cleanup-review-*.md`
files).

---

## §9 Ratification-final verdict

**SHIP-WITH-REED-INLINE for `2519f83` + SHIP for `1fe0d28` + SHIP for `b536949`.**

The consolidated canonical spec is **Ready-For-Reed-Implementation
at M0** conditional on:

1. Cascade 1 lift Alex-ratification into spec (§3.1)
2. ALEX-Q1 answered before M0 tick fires (schema extension shape)
3. ALEX-Q2 answered before M6 tick fires (cascade-1 completeness semantics)
4. ALEX-Q3 answered before M7 tick fires (cascade-3 per-file dispatch)

M0 (mirror.spec kintsugi.roomba block landing) is well-scoped;
composition graph is legible; router pattern is Alex-ratified;
substrate is ready.

The geometry sings. Minimal rust surface. Terminal form is 5% of
current bootstrap surface because the substrate does the per-prism
work. `dance.rs` IS the router; the substrate IS the compiler.

---

*Seam Phase D closed. 3 REED-INLINE cascades + 3 ALEX-Q residues +
5 recognition candidates HELD + 3 structural discoveries + M0 Reed
implementation GREEN-lit. 361 LOC. Pure-docs 📝 markdown-only bypass.*
