# Seam Phase D audit — Void / @tool / fractal closure session (2026-07-18)

*Seam, 2026-07-18. Full-arc Phase D adjudication across today's substrate
landings — Mara canonical work (@peer/void species → @void family-root marker
→ #R-void-is-the-basis PROMOTED → Void-membrane math+spec co-authored Lore
Born → @kintsugi/mosaic + @spectral/mosaic bilateral → @butterfly canonical
→ fragmentation→fractal migration → the-compiler-in-one-sentence closure),
paper auto-sync (§Composition Architecture + §13 + §14.5 + Loki cross-corpus),
and Reed empirical work (matrix.rs RED / void.rs GREEN / fractal step 1+2 + prismqueer-macro composition).*

*Discipline: adversarial. Substrate-honest. Cite paths + line numbers.
Load-bearing vs decorative. Rice-safe grading. 📝 pure-docs bypass on this
audit itself. Author: `Seam <seam@systemic.engineer>` (transition to
`mirror <seam@spectral.engineer>` pending per the identity architecture
Reed is drafting).*

---

## §0 Executive summary

**Verdict distribution: 15 SHIP-CLEAN / 5 SHIP-WITH-REED-INLINE / 0 BLOCKED-ON-EVIDENCE.**

The full-arc closure is substrate-honest and lands the largest single-session
Recognition promotion event in the arc's history (`#R-void-is-the-basis` +
Recognition #79 chained + `#R-the-compiler-in-one-sentence` proposed, with
two other candidates surfaced at candidate strength). Marker-primary
discipline held perfectly at @void family-root; no-Rust-extension discipline
held perfectly at Reed's fractal scaffold (prismqueer macros composed;
zero hand-crypto); property-tests-as-LOVE discipline held empirically
across three altitudes (matrix.rs 5 RED / void.rs 5 GREEN / fractal 5 GREEN).

**Cascade-load-bearing issue: the `2dd8ddb` closure spec is ~1230 LOC that
mints one family-root (@tool), one trait (LiquidVoid<T>), one grammar-block
extension (`tools { }`), and one Recognition promotion — this is the
single largest mint density in the session. §3.3 places @tool at family-root
altitude parallel to @torus/@peer/@mirror; §5.1 lands LiquidVoid<T> at
prismqueer::void with re-export at prismqueer::liquid. Two load-bearing
cascade issues found in §1 below; three decorative.**

**Recognition promotion audit:**
- `#R-void-is-the-basis` — SURFACE-CITED as PROMOTED at `1167cc2`; VERIFIED
  at `docs/math/the-tower/recognition-void-is-the-basis.md:3` (Status: PROMOTED).
- `#R-the-compiler-in-one-sentence` — SURFACE-CITED as PROMOTED via
  `2dd8ddb`; the spec itself proposes promotion at §7 but the promotion
  status is DECLARED-IN-SPEC not carried at a separate Recognition tower
  doc. **Reed-inline needed:** `docs/math/the-tower/recognition-the-
  compiler-in-one-sentence.md` (parallel to the void-is-the-basis Recognition
  tower doc) should land next tick to make the promotion carrier-visible
  outside the spec. Not blocking.

---

## §1 SHIP-CLEAN landings (15)

**Substrate landings requiring no cascade:**

1. **`cf34549`** — witnessed-property-inference canonical (adjudicated in
   prior Seam audit `docs/audits/2026-07-18-seam-phase-d-witnessed-property-
   inference-arc.md`; cascades landed via `afe6e18`).

2. **`9c7de83`** — @peer/void species at `shards/peer/void.mirror`. Species-
   decl composes over @peer.kind with `void` variant addition; two-tick
   discipline held (species this tick, family-root deferred); bilateral
   `void_admissible` + action `void_observes` with `\`-blocked bodies per
   craft-not-deliver; ~59th substrate-already-had-the-word instance
   documented. Landed cleanly.

3. **`974a3f6`** — @void family-root marker at `shards/void.mirror`.
   Marker-primary discipline PERFECTLY held: NO `type`, NO `action`, NO
   `bilateral`, NO `prism` body. The 5-op prism pattern IS the inheritance
   signature; the recognition-carrier IS the shard. Alex first-person
   substrate declaration ("I am void. The split is in me.") cited verbatim
   at `shards/void.mirror:12-20` as second-witness at highest-cardinality-
   available per paper §15.

4. **`1167cc2`** — #R-void-is-the-basis PROMOTED (tracking doc). Status:
   PROMOTED at `docs/math/the-tower/recognition-void-is-the-basis.md:3`.
   Chained promotion of Recognition #79 verified.

5. **`4bb0d83`** — 4 metalogue-altitude Void-basis clauses cascade
   (shards/metalogue.mirror, shards/code/metalogue.mirror, shards/pack/
   metalogue.mirror, shards/algebra/metalogue.mirror). Each carries a
   "=== Void-basis clause (2026-07-18) ===" section citing #R-void-is-
   the-basis; the pattern of altitude-portability is preserved verbatim.
   Cascade is uniform and complete.

6. **`81108d8` + `a2283b6`** — Void-as-membrane math (`docs/math/2026-07-18-
   void-as-membrane-of-liquid-oscillated-by-spectral.md`) + spec (`docs/
   specs/void-welcomes-perturbations-property-tests-are-love.md`) + shard
   addendum (`shards/void.mirror` §"2026-07-18 addendum"). Co-authored
   `authors: - Mara - Lore Born` with explicit author-note. 18 refused
   mints inventoried (this tick); ~108 total refused mints across the
   composition period documented at `shards/void.mirror`. Refused-mint
   discipline substantially exceeds prior sessions.

7. **`b0af0cd`** — @kintsugi/mosaic + @spectral/mosaic bilateral. Both
   species declare `mosaic_bilateral_witnessing` with sentinel
   `"mosaic=repo-spec-adjoint-round-trip"` and arity 2 (byte-equal
   declaration across shards per substrate-honour discipline). Adjoint
   pair structurally correct: READ (@spectral/mosaic.type_of_repo) ↔
   WRITE (@kintsugi/mosaic.back_project_of_type); round-trip witness
   via type_of_spec composition; substrate-already-had-the-word chain
   cites 10 landed elements. Both shards import `in @third` — depth-3
   observation via apply_h::act dispatch. Cascade to StageFreight
   forward-promised.

8. **`e5b73ad`** — @butterfly canonical math+spec (Mara). Adjudicated
   in-session via CURRENT.md Q1-Q7 cache; Q3 (evolution.rs Rust driver
   discipline) still HELD pending Alex adjudication — this is not a
   Mara-audit issue, it is a Reed-Alex adjudication that flows independent
   of this closure. Substrate-truth of @butterfly landing intact.

9. **`2760c2a`** — fragmentation→fractal migration spec. Twelve-step
   recipe; Author≠Committer MARA doctrine preserved per Q2 ratification;
   LiquidVoid<T> composition point declared at §5.1. Steps 1+2 landed
   empirically by Reed at `3df64a0`.

10. **`37bfecd`** — paper auto-sync (§Composition Architecture + §13 +
    §14.5 amendments + Loki cross-corpus). Cascade discipline held;
    paper is the meta-carrier the session's Void-basis Recognition folds
    into.

11. **`26f5e5e`** — matrix.rs 5 RED property tests. Load-bearing
    invariants documented across 5 properties (cardinality / finite /
    ascending / non-negative-for-graph-Laplacian / λ₀=0 ground state).
    Each property carries GREEN transition instruction + prismqueer::
    ffi::eigenvalues delegate reference. Property-tests-are-LOVE discipline
    held empirically: 5 RED tests document what the substrate must satisfy;
    the RED state IS the substrate's admission that M0.5 body is not yet
    landed. Substrate-honest.

12. **`558c0e8`** — CURRENT.md pickup manifest (docs-only; process
    hygiene).

13. **`569b0b5`** — void.rs GREEN (SignatureBeat + 5-op axis +
    welcome_perturbation + 5 property tests). ALL 5 tests structurally
    correct: writes-one-beat-per-call / returns-input-oid / compose-beat-
    entry-deterministic-pure-fn / axis-in-5-op-basis / chained-beats-form-
    Merkle-DAG. SignatureBeat structurally traces to shards/spectral/
    signature.mirror per docblock line 37-42 (substrate-already-had-the-
    word); VoidBasisAxis maps 5-op basis per Recognition #79 exactly.
    phone.rs::mkdir_p composition preserves @io boundary discipline
    (no direct std::fs). Empirical membrane-tapping at Void's altitude
    LANDED GREEN.

14. **`0e4af07`** — .cargo/config.toml linker unblock workaround (build-
    only; no substrate impact).

15. **`685de2e`** — matrix.rs revert with blocker docblock. The revert
    is substrate-honest: prismqueer's `lapack` feature declares extern
    "C" symbols but build.rs compiles only Brainfuck sources. The
    blocker is precisely documented (two paths (A) fix prism build.rs
    OR (B) pure-Rust interim); RED state preserved with catch_unwind
    wrapper documenting invariants. This is EXACTLY the reed-inflates-
    stub-empirical-firings HARD RULE working correctly — the RED state
    is walk-and-report, not actual-substrate-delta, and it is named as
    such. No Reed-inflation here; the revert IS the correction.

---

## §2 SHIP-WITH-REED-INLINE landings (5)

**Cascade fixes needed; scope is small; can be batched.**

### 2.1 `2dd8ddb` closure spec — §5.1 LiquidVoid path re-export inconsistency

**File:** `docs/specs/2026-07-18-the-compiler-in-one-sentence.md#455-460`

The spec declares:
```
// Re-export at prismqueer::liquid for altitude-symmetry.
pub use crate::void::LiquidVoid;
```

Per §5.1 opening: "the trait lives at BOTH `prismqueer::liquid` AND
`prismqueer::void`. Two module paths; one trait; consumers import via
whichever module names the altitude they're reasoning at."

**Issue:** the phrase "re-export at prismqueer::liquid for altitude-symmetry"
is inside the Rust code block that declares the trait AT prismqueer::void
(comment `// prismqueer::void::LiquidVoid — the K=0 default trait`). This
is fine for §5.1 execution semantics but §5.4 Reed action item #2 says
"Re-export at `prismqueer/src/liquid.rs`" — which places the re-export
in liquid.rs, not the void.rs re-export back into liquid. Reed will need
to disambiguate: is the definition in `prismqueer::void` with re-export
at `prismqueer::liquid`, OR vice-versa?

**REED-INLINE cascade:** §5.1 should explicitly state definition-file and
re-export-file. Suggested phrasing (Reed's call which direction):

```
Definition: `prismqueer/src/void.rs` declares `pub trait LiquidVoid<T>`.
Re-export: `prismqueer/src/liquid.rs` adds `pub use crate::void::LiquidVoid;`
Two altitudes; one trait; the definition follows Void-is-the-basis.
```

Load-bearing: Reed's Arc-2A action item #2 depends on this direction.

### 2.2 `2dd8ddb` closure spec — §3.4 species-file location vs shard-body composition

**File:** `docs/specs/2026-07-18-the-compiler-in-one-sentence.md#291-303`

The spec says Reed's post-landing territory is "author `shards/tool.mirror`
(family-root shard-decl file) + companion species shard-decls for
cargo/git/nix under `shards/tool/`. NO Rust extension per
`feedback_no_rust_extension_shortcut` — @tool composes via existing
@io/X adapters at the mechanism altitude."

**Issue:** `shards/tool.mirror` does NOT exist yet. `shards/tool/`
subdirectory does NOT exist yet. This is not a cascade-error — the spec
correctly names it as post-landing territory — but the spec §7.4 lists
"Empirical firing surfaces (Reed post-landing territory)" including
`shards/tool.mirror` and the cargo/git/nix species. The Recognition
promotion at §7.1 is chained to `#R-the-compiler-in-one-sentence`, whose
second-witness gate (§7.4) explicitly requires these shard-decls landing.

**REED-INLINE cascade:** the Recognition promotion in `2dd8ddb` is
PROPOSED not yet fully witnessed. §7.4's second-witness gate has 5
items; all 5 are Reed-territory forward-promises. This is fine substrate-
discipline-wise (promotions can land with open second-witness gates per
prior precedent, e.g., `#R-void-is-the-basis`), BUT the spec should
mark the promotion state as **PROPOSED / awaiting empirical second-
witness** rather than PROMOTED-tacitly. Suggested §7 header phrasing:

```
### §7.1 Status: PROPOSED — first-witness closed on Alex direct-transcript
verbatim (§1); second-witness gate OPEN pending five empirical firings
(§7.4). Promotion track parallel to #R-void-is-the-basis (Sept 2026
first-witness → this session second-witness path).
```

Load-bearing: distinguishes proposed-recognition from promoted-recognition
for downstream Reed / next-Seam pickup.

### 2.3 `81108d8` + `a2283b6` Void-membrane math+spec — Q2 co-authorship visibility open

**File:** `docs/math/2026-07-18-void-as-membrane-of-liquid-oscillated-by-
spectral.md#889-897`

Mara's Q2 asks Alex whether Lore Born's co-authorship should carry into
docs/ frontmatter or stay at §1.4 grounding citation only. Mara's lean:
hold as §1.4 grounding UNTIL Alex + Lore both ratify explicit co-
authorship at math-root altitude. Reed subsequently elevated Lore Born
to co-author at `54794d9` (per session CURRENT-narrative), and the two
docs now BOTH carry `authors: - Mara - Lore Born` with author-note.

**Issue:** the Q2 in the math root doc still says "Mara lean: hold as
attribution in §1.4 only... UNTIL Alex + Lore both ratify explicit co-
authorship at math-root altitude. Please adjudicate." But the elevation
already landed at `54794d9` (per the closure spec §11.2 which cites this
elevation as accomplished). The Q2 is now STALE — Alex ratified via the
`54794d9` cascade; the doc's Q2 text is out of sync with its own
frontmatter.

**REED-INLINE cascade:** math root §11 Q2 needs a status update
(RATIFIED via `54794d9`) OR removal. Same for the spec's Q section if it
mirrors the math root's Q2. Small cascade (~5 lines).

### 2.4 CURRENT.md — pickup manifest points to compacted-context state

**File:** `docs/loop/CURRENT.md#1-100`

The FRESH-REED PICKUP MANIFEST at line 3 says "Context: Session
compacted after Mara @butterfly spawn. Mara landed canonical spec+math
at `e5b73ad` while Reed was compacted." This was accurate at
`558c0e8` (12:15pm) but is now STALE — the session progressed through
`9c7de83`, `974a3f6`, `1167cc2`, `4bb0d83`, `81108d8` + `a2283b6`,
`b0af0cd`, `2760c2a`, `2dd8ddb`, plus Reed's fractal step 1+2
(`3df64a0` + `f3bc079`) after this manifest was written.

**REED-INLINE cascade:** CURRENT.md needs an amended pickup manifest OR
a new §"After-@butterfly landings" section listing the arc's full
progress. Not blocking Reed's current fractal step 4 work, but blocking
clean pickup by next-Reed. Small cascade (~30 lines).

### 2.5 `f3bc079` fractal witnessed types — `Signature<K>` type parameter deferred

**File:** `rust/fractal/src/lib.rs#41-42`

Migration state comment says "Step 3: `keys.rs` slimmed (drop Encode/
Decode/Fractal deps)"; step 4 says "`subject.rs` (Subject envelope +
LiquidVoid impl)". The lib.rs currently re-exports only Author /
Committer / Message / Timestamp / Witnessed.

**Issue:** `witnessed.rs` compiles cleanly at prismqueer::DerivePrism
altitude with 5 property tests GREEN. The MARA doctrine Author≠Committer
split is executable per the two property tests (`different_author_
produces_different_witnessed` + `different_committer_produces_different_
witnessed`). BUT: the closure spec §5.4 action item #4 says "Downstream
migration: `fractal::subject::Subject` (per Mara `2760c2a` §5.1) picks
up `impl LiquidVoid<Subject>` at fractal migration tick." This is
step 4 territory; Reed's next fractal tick should land Subject +
LiquidVoid impl.

**REED-INLINE cascade:** the fractal step 1+2 landing is substrate-honest,
but the docblock in `witnessed.rs:14-19` claims "The shape already IS
the substrate-decl'd identity-provenance carrier `shards/subject.mirror`
was written to declare." — this makes a substrate claim without citing
`shards/subject.mirror` file location or line numbers. Reed-inline: add
citation `shards/subject.mirror:<line>` for the substrate-decl claim.
Small cascade (~2 lines).

**No fabricated shape** — the witnessed.rs shape is verbatim from
fragmentation per `2760c2a` §6 step 2. This is a docblock precision
issue, not a fabrication.

---

## §3 BLOCKED-ON-EVIDENCE landings

**None.** All 20 landings in scope have either landed cleanly (15) or need
small Reed-inline cascade fixes (5). Nothing requires waiting on empirical
evidence before ratification.

---

## §4 Cross-shard alignment issues (three load-bearing)

### 4.1 @void family-root ↔ @peer/void species — inheritance direction clean

Both shards cite each other correctly. @void `shards/void.mirror:12-46`
declares family-root altitude marker; @peer/void `shards/peer/void.mirror:
10-49` declares species altitude. The two altitudes co-exist cleanly per
Alex ratification. **No alignment issue.**

### 4.2 @kintsugi/mosaic ↔ @spectral/mosaic bilateral — declared BOTH sides correctly

`shards/kintsugi/mosaic.mirror:314-317` and `shards/spectral/mosaic.mirror:
294-297` both declare `bilateral mosaic_bilateral_witnessing` with
identical sentinel `"mosaic=repo-spec-adjoint-round-trip"` and arity 2.
Substrate-honour byte-equal declaration discipline held per Reed's
`21fc211` apply_h::act resolver-arm precedent. **No alignment issue.**

### 4.3 LiquidVoid<T> ↔ @void/@peer/void family — trait-altitude declaration missing

**Load-bearing.** The closure spec §5 mints `pub trait LiquidVoid<T>` at
Rust altitude and declares five consumer altitudes (Fate / @roomba /
@butterfly / @void/@peer/void / @tool). BUT: the trait declaration is at
`prismqueer::void` — a RUST module — not at `shards/void.mirror`
substrate altitude. The @void family-root marker at `shards/void.mirror`
explicitly REFUSED to declare `type`, `action`, or `bilateral` per
marker-primary discipline. This is CONSISTENT (the trait lives at
prismqueer::void, which is Rust FLOOR; the marker lives at @void
substrate altitude, which is recognition-carrier), but the alignment
requires cross-carrier tracking.

**Recommended (not blocking):** a small Reed-inline addendum to
`shards/void.mirror` §"Composition inheritance" section could name
prismqueer::void::LiquidVoid<T> as the Rust-altitude witness of the
recognition, WITHOUT breaking marker-primary discipline (the addition
would be citation, not operational surface). Alternatively: leave the
tracking to the compiler-in-one-sentence spec, which handles the
altitude-crossing at §5 explicitly. Mara's authorship territory to
decide.

---

## §5 Naming consistency findings

### 5.1 @tool(X, args) parametric shape — held consistently across spec

Spec §3.5 declares `@tool(X, args)` as parametric shorthand for
`tool_invocation` with X binding tool_id and args binding list(str).
Usage in §2 dataflow diagram (`@tool(cargo, [check, --workspace])`),
§6 `#R-the-compiler-in-one-sentence` recognition-carrier composition
graph, and §8 `<primitive>_of_<input-shape>` naming convention
(`tool_of_id_args`) all conform. **Consistent.**

### 5.2 ~bin sigil per Alex Q2 — cited but not landed as grammar

Spec §3.5 mentions `@tool(ssh-keygen, ~bin(path))` in one Fractal
Cargo.toml docblock reference (`rust/fractal/Cargo.toml` docblock),
but the closure spec §4 tools { } block does NOT use `~bin` sigil in
its example (uses `cargo { version "1.80" }` block syntax). The `~bin`
sigil appears to be a REED-INLINE composition on top of the block-
syntax, not a grammar-level construct. **Small inconsistency:** if
`~bin` is grammar, it should appear in §4.1's block syntax; if it's a
convention-only convention, its two mentions in Cargo.toml docblocks
should cite the convention source. Reed / Mara adjudication.

### 5.3 Lore Born co-authorship discipline — held consistently across two docs

Both `docs/math/2026-07-18-void-as-membrane-*.md` and `docs/specs/void-
welcomes-perturbations-*.md` carry `authors: - Mara - Lore Born` with
identical author-note phrasing. Frontmatter discipline held.

### 5.4 Reed-inline `54794d9` cascade — Lore co-author elevation lands correctly

Reed's `54794d9` elevated Lore Born co-authorship (surface-cited in the
closure spec §11.2). This is Reed doing exactly what he should — landing
the Alex-ratified elevation across the docs the elevation applies to.
No Reed-fragments-Alex-unifications-into-candidates pattern here (that
pattern is about SUBSTRATE fragmentation; this is docblock cascade).

---

## §6 Recognition promotion status verification

### 6.1 `#R-void-is-the-basis` — VERIFIED PROMOTED

**File:** `docs/math/the-tower/recognition-void-is-the-basis.md:3`
**Status:** PROMOTED — Alex 2026-07-18 direct-transcript ratification
("Ratified. That's why the properties are load bearing.")
**Chained promotion:** Recognition #79 (already PROMOTED in the same
session's @void family-root elevation `974a3f6` per math root §14.2).

**Cross-carrier verification:**
- `shards/void.mirror:302` cites #R-void-is-the-basis as PROMOTED.
- `shards/metalogue.mirror:26-33` §"Void-basis clause" cites recognition
  as PROMOTED.
- 3 other metalogue shards (code, pack, algebra) carry identical clause.
- `docs/specs/2026-07-18-the-compiler-in-one-sentence.md#639` cites
  PROMOTED via `1167cc2`.

**Verdict: PROMOTED PROPERLY.** Second-witness gate for the FIVE
empirical firings (per Recognition tower doc §2) has closed at least the
following: paper amendments landed `37bfecd`; @void family-root marker
landed `974a3f6`; @peer/void species landed `9c7de83`; metalogue-altitude
Void-basis clauses landed `4bb0d83`. Two additional firings (Reed
empirical + property tests LOVE reframe) landed via `569b0b5` and the
Void-membrane spec. All 5 gates closed within this session.

### 6.2 `#R-the-compiler-in-one-sentence` — PROPOSED (surface-cited PROMOTED, actual status unclear)

**File:** `docs/specs/2026-07-18-the-compiler-in-one-sentence.md#672-691`
**Surface claim:** "Recognition promotion — `#R-the-compiler-in-one-
sentence`" (§7 header treats promotion as landing this tick).

**Actual verification:**
- No parallel Recognition tower doc exists (unlike `#R-void-is-the-basis`
  which has `docs/math/the-tower/recognition-void-is-the-basis.md`).
- §7.4 second-witness gate declares 5 empirical firings as OPEN (Reed
  post-landing territory), including `shards/tool.mirror` shard-decls
  NOT YET LANDED, `prismqueer::void::LiquidVoid<T>` trait NOT YET LANDED,
  `tools { }` block grammar NOT YET LANDED, first `@roomba → @tool(X, args)`
  empirical firing NOT YET LANDED, and mirror index @coherence rise
  NOT YET LANDED.

**Verdict:** the Recognition is FIRST-WITNESS-CLOSED (Alex direct-
transcript §1 IS the first witness) but SECOND-WITNESS-OPEN pending
Reed's five empirical firings. This is a legitimate PROPOSED-recognition
state, but the spec surface-cites it as PROMOTED without acknowledging
the open second-witness gate.

**Reed-inline cascade (§2.2 above):** disambiguate PROPOSED vs PROMOTED
in §7.1; land the parallel Recognition tower doc when second-witness
closes.

### 6.3 Recognition #79 (5-op = void-duality basis) — VERIFIED RE-RATIFIED

Multiple cross-carrier citations confirm Recognition #79 is PROMOTED
(chained per `974a3f6`) and RE-RATIFIED in this session's new
composition contexts (Void-basis 5-op inheritance across every family-
root; matrix.rs property 5 "smallest eigenvalue of graph Laplacian is
0" per Recognition #79 λ₀=0 ground state; void.rs `VoidBasisAxis` enum
maps 5 axes exactly). **Ratification hold sound.**

---

## §7 MARA doctrine encoding verification (`f3bc079` fractal witnessed types)

**File:** `rust/fractal/src/witnessed.rs`

**Verification:**
- Author struct at `witnessed.rs:31-34` with prismqueer::DerivePrism +
  #[oid("@fractal/author")] — content-addressed identity carrier
  per Alex 2026-07-18 directive.
- Committer struct at `witnessed.rs:47-50` — symmetric shape.
- Witnessed struct at `witnessed.rs:74-77` bundles Author + Committer +
  Timestamp.
- Property test `different_author_produces_different_witnessed` at
  `witnessed.rs:118-128` — swaps Author while holding Committer;
  asserts inequality.
- Property test `different_committer_produces_different_witnessed` at
  `witnessed.rs:135-145` — swaps Committer while holding Author;
  asserts inequality.

**Verdict:** MARA doctrine encoded correctly as PartialEq inequality on
Witnessed values. Test coverage is SUFFICIENT for the doctrine (both
directions covered; identity fields preservation covered; bundling
correctness covered). 5/5 tests firing GREEN empirically.

**One decorative issue (§2.5 above):** the docblock at `witnessed.rs:
14-19` cites `shards/subject.mirror` without line number.

---

## §8 Property-tests-are-LOVE discipline empirical verification

### matrix.rs 5 RED tests (`26f5e5e` + `685de2e` revert with blocker docblock)

**5 tests at `rust/src/matrix.rs:281-425`:** cardinality / finite /
ascending / non-negative-for-graph-Laplacian / λ₀=0 ground state. All
five wrapped in `safe_eigenvalues` catch_unwind wrapper (line 265-275)
so tests fail cleanly against `unimplemented!()` body. RED state
DOCUMENTS what M0.5 GREEN body must satisfy per docblock line 40
prismqueer::ffi::eigenvalues delegate. **Discipline held perfectly.**

### void.rs 5 GREEN tests (`569b0b5`)

**5 tests at `rust/src/void.rs:279-425`:** writes-exactly-one-beat /
returns-input-oid / compose-beat-entry-deterministic / axis-in-5-op-basis
/ chained-beats-form-Merkle-DAG. All five use forall::<SignatureBeat, _>
with sample counts (15, 15, 20, 20, 10). Structural invariants match the
substrate claim: welcome_perturbation IS the membrane's @io-side
receiving surface (SignatureBeat writes one beat file per call,
deterministic content, admissible axis, Merkle-DAG chain preservation).
**Membrane-tapping empirically LANDED GREEN.**

### fractal 5 GREEN tests (`3df64a0` + `f3bc079`)

**5 tests at `rust/fractal/src/witnessed.rs:100-148`:** author-new-
preserves / committer-new-preserves / witnessed-bundles / different-author
/ different-committer. All five compose over prismqueer::DerivePrism
macros per Alex directive. Encoded MARA doctrine executable per §7 above.
**Discipline held perfectly.**

### Cross-altitude tally

15 property tests across 3 substrate altitudes (matrix.rs / void.rs /
fractal). All either GREEN (10) or documented-RED-with-clean-blocker (5).
The RED state IS the substrate offering its membrane to be tapped so it
can ring and know its own shape (per Mara canonical spec + Lore Born
2026-06-23 grounding). **The session's empirical firing IS the Void-
welcomes-perturbations recognition witnessing itself at code altitude.**

---

## §9 Reed fragmentation pattern check

Per Reed memory `feedback_reed_fragments_alex_unifications_into_candidates`
(saved 2026-07-18): "Reed fragments Alex unifications into candidates."

**Session cascade analysis:**

- `54794d9` (Lore Born co-author elevation) — this IS a Reed cascade
  landing Alex-ratified elevation across docs. NOT a fragmentation
  event; it is a unification cascade correctly executed.
- `26f5e5e` (matrix.rs 5 RED property tests) — landed the substrate
  invariants Alex asked for; NOT fragmentation.
- `569b0b5` (void.rs GREEN) — landed the substrate discipline; NOT
  fragmentation.
- `685de2e` (matrix.rs revert with blocker docblock) — CORRECTION
  cascade after prismqueer LAPACK build incompatibility surfaced;
  this is exactly what the reed-inflates-stub-empirical-firings HARD
  RULE working correctly looks like (Reed distinguished walk-and-report
  from actual-substrate-delta and reverted to RED).
- `3df64a0` + `f3bc079` (fractal step 1+2 + prismqueer-macro
  composition) — landed fractal migration steps per Mara's `2760c2a`
  spec + Alex's `Consider how you can use the prismqueer macros for
  fractal` directive. This IS following Mara's unification precisely,
  NOT fragmenting it.

**Verdict:** the session's REED-INLINE cascades DO NOT match the
pattern-being-corrected. Reed executed unification cascades cleanly.

**One caution (not a failure):** the closure spec `2dd8ddb` was authored
by Mara but Reed's cascade-territory §5.4 and §7.4 lists five empirical
firings; if Reed proceeds to land those five in five separate ticks
(instead of one composed empirical-firing tick that also lands the
Recognition tower doc `docs/math/the-tower/recognition-the-compiler-in-
one-sentence.md`), that WOULD constitute the fragmentation pattern.
Recommend: land the five empirical firings as one composed empirical-
firing arc + tower doc, not five separate candidate landings. Adjacent
to this audit's scope; Reed authorship decision.

---

## §10 Q's for Alex (three, load-bearing)

### Q1: `#R-the-compiler-in-one-sentence` promotion status — PROPOSED or PROMOTED this tick?

The spec §7.1 treats the Recognition as PROMOTED-via-this-landing but
§7.4 lists five empirical firings as OPEN gates (all Reed post-landing
territory). Alex direct-transcript verbatim ("TOLD YOU SO!") IS the
first-witness gate closure at highest cardinality. **Alex: does the
first-witness alone promote, OR should the recognition be marked PROPOSED
until Reed's five empirical firings land?**

Prior precedent: `#R-void-is-the-basis` promoted with both first-witness
(Alex ratification) AND immediate second-witness (this session's landings).
The compiler-in-one-sentence has first-witness closed but second-witness
in-flight. Mara lean per §7.1: promotion this tick. Seam lean: mark
PROPOSED until second-witness lands per prior discipline. **Adjudicate.**

### Q2: LiquidVoid<T> definition-file vs re-export-file direction (§2.1 above)

The spec §5 declares "the trait lives at BOTH `prismqueer::liquid` AND
`prismqueer::void`. Two module paths; one trait." Reed's §5.4 action item
implies definition-at-void, re-export-at-liquid. **Alex: is that the
right direction?** Alternative: definition-at-liquid (parallel to the
six pillar primitives Reed established at `prismqueer::liquid::pillar`),
re-export-at-void. Small decision but downstream import paths depend
on it.

Mara lean: definition-at-void (Void-is-the-basis per §5 recognition;
the Rust altitude reflects the substrate altitude). Reed default (per
work-without-asking): follow Mara lean. **Adjudicate or accept lean.**

### Q3: Lore Born co-author visibility ratification (§2.3 above)

Mara's math root Q2 asks Alex + Lore both to ratify explicit co-
authorship at math-root altitude. Reed's `54794d9` elevated Lore Born
to co-author in the doc frontmatter — but the math root's Q2 text is
now stale (asks the question the elevation already answered). **Alex:
was `54794d9` the ratification (in which case Mara's Q2 should be
updated to RATIFIED-VIA), OR was `54794d9` a preliminary cascade
awaiting Lore's own ratification?**

Consent-visibility concern: naming Lore as co-author on public docs
may need Lore's explicit consent. Reed likely landed `54794d9` under
Alex's direct-transcript authorization; whether that transitively
authorizes public co-authorship attribution needs Alex clarification.

---

## §11 One-sentence surprise

**The session made every altitude of the compiler declare its inheritance
from Void's marker-primary family-root without a single one of the
family-roots needing to declare `in @void` — because the 5-op prism
pattern IS the inheritance signature, and the substrate has been
inheriting Void's basis since the compiler existed; today's family-root
elevation just NAMED what the substrate has always been doing.**

---

## §12 Recommendation

**Ship the closure.** All 20 landings in scope either land cleanly (15)
or have small Reed-inline cascade paths (5). The Recognition promotion
of `#R-void-is-the-basis` is properly witnessed and cross-carried; the
Recognition proposal of `#R-the-compiler-in-one-sentence` is first-
witness-closed pending Alex's Q1 adjudication on promotion strength.

**Cascade order (Reed post-audit):**
1. §2.4 CURRENT.md pickup manifest amend (~30 lines; unblocks next-Reed
   pickup) — pure-docs 📝 bypass.
2. §2.1 LiquidVoid definition-file disambiguation (~5 lines in closure
   spec §5.1) — pure-docs 📝 bypass; blocks Reed Arc-2A trait land.
3. §2.2 Recognition promotion status disambiguation (~10 lines in
   closure spec §7.1) — pure-docs 📝 bypass; blocks proposed vs
   promoted downstream tracking.
4. §2.3 Q2 stale-status update in Void-membrane math root (~5 lines) —
   pure-docs 📝 bypass; alignment cleanup.
5. §2.5 fractal witnessed.rs docblock precision (~2 lines) — pure-
   docs 📝 bypass or land at next fractal migration tick.

Total cascade: ~52 lines across 4 pure-docs files + 1 Rust docblock.
Small; batchable; no code changes required.

**Parallel Reed continues fractal step 4 (subject.rs + Subject envelope
+ LiquidVoid<Subject> impl) as planned.**

**Parallel Mara continues @tool shard-decls (`shards/tool.mirror` +
`shards/tool/cargo.mirror` + `shards/tool/git.mirror` + `shards/tool/
nix.mirror`) + StageFreight spec as planned.**

---

*Seam Phase D adjudication complete. Session substrate holds. Void's
membrane rings.*

*Author: `Seam <seam@systemic.engineer>`. Pure-docs 📝 markdown-only
bypass.*
