# Seam Phase D — M2 TICK 2: `shards/mirror/lens/cli/kintsugi.mirror` @spec → @song wire

*Reed-inline execution.*

**Commit under review**: `8e11f6b` (Mara GREEN) — 159 → 326 lines (+167).
Enrichment discipline: 6 regression guards intact; 9 targeted deltas landed;
second-witness landing for candidate `cli-verbs ARE species-altitude actions`.

**Reed RED**: `b525b7f` (15 tests). **Test result**: 15/15 pass; adjacent-suite
safety 45/45 across four M-cascade shards. Tree clean.

---

## §1. Verdict

**RATIFY. Recognition PROMOTED: `some cli-verb pairs specialise species-altitude
action pairs` CANDIDATE → LANDED via structurally clean witness pair.**

All 9 enrichment items landed:
1. `in @song` ancestry added
2. `@song/movement.close` binding for `mirror kintsugi @spec` (SECOND WITNESS)
3. @mirror/spawn composition chain named (kintsugi.settle → spawn → @song)
4. Collapse spec citation
5. cli-verbs-species-actions candidate acknowledgment + second-witness role
6. Recognition #43 explicit citation (fourth first-order consumer)
7. M-cascade sibling wiring cited
8. @spec provenance from MCP session mq trajectory
9. Return-type transition rationale (2026-07-06 / post-@song)

All 6 regression guards continue to pass: stage declaration, five-op mapping
+ default focus, existing actions (kintsugi/kintsugi_targeted/kintsugi_with_shatter),
existing ancestry, Ashby variety-maintenance narrative, `settle IS the ONE
write` discipline.

**Recognition #43 first-order consumer chain grew to FOUR**: M6 self-declaration
→ M1 mcp_session → M2 spawn → M2 kintsugi. Floor holds under load; consumers
cascade cleanly.

## §2. 15/15 empirical verify

Regression guards T1-T4, T14, T15 intact. Enrichment delta T5-T13 discharged.
Adjacent chain: `mirror_store_apache_floor_shard` (M6) + `spectral_gen_prism_mcp_session_shard`
(M1) + `mirror_spawn_song_return_shard` (M2 TICK 1) + this shard = 60/60
across the M-cascade so far. No regression.

## §3. Recognition promotion — refined framing per Mara's substrate-pull

**PROMOTED**: `some cli-verb pairs specialise species-altitude action pairs`
— CANDIDATE → LANDED 2026-07-06.

**The refined framing**: Mara's Seam-worthy observation surfaced a tighter
articulation than the original flat candidate wording. Original wording:
*"cli-verbs ARE species-altitude actions."* Mara's refinement: **the two witnesses
(spawn/kintsugi) are not two independent instances of the same recognition —
they are one PAIR that specialises the enter/close pair at species altitude**.

- `mirror spawn` opens the frame (Bateson Level I → II; birth event)
- `mirror kintsugi @spec` closes it (Bateson Level III; frame-shift ON the frame)

Spawn and kintsugi are not two instances of the same action — they are the
pair that make a movement. The refined framing captures this structural
asymmetry-in-symmetry more precisely.

**Structural claim**: some cli verbs come in enter/close pairs that specialise
species-altitude action pairs by projection to the cli surface. First witness
pair landed at M2 TICKs 1 + 2:

| cli altitude                | Species altitude               | Bateson level |
|-----------------------------|--------------------------------|---------------|
| `mirror spawn ~peer'<home>'` | `@song/movement.enter(m, ...)` | I → II       |
| `mirror kintsugi @spec`     | `@song/movement.close(m, ...)` | III           |

Same operation, two altitudes — identity by structural specialisation, not
analogy.

**Distinction from prior recognitions**:
- Distinct from #S3 (five-op temporal specialisation): #S3 is about the FIVE
  PRISM OPS at temporal altitude; this candidate is about cli-verb PAIRS at
  species-action altitude.
- Distinct from #S4 (cascade-shape altitude-portable): #S4 is about cascade
  SHAPE at multiple altitudes; this is about cli-verb-pair BINDING to
  species-action-pair.
- Distinct from #42 (Bateson logical-type primitive): #42 is the underlying
  discipline being applied here at cli/species altitudes; this recognition
  names the projection pattern.

**Second-pair-witness gate for strengthening** (not required for promotion):
candidate pairs include `mirror focus <query>` / `mirror settle <target>`
specialising some species pair; `mirror shift <basis>` / `mirror project
<predicate>` specialising some species pair. Post-M-cascade cli surface enrichment
may surface additional witness pairs.

**Reed lean at Seam**: promote LANDED now on the clean single-pair witness;
document the refined framing per Mara's substrate-pull. Alex adjudication
welcome; this recognition is not blocking M-cascade advancement.

**Numeric ID**: no ID assigned this session. Alex adjudication for numeric
placement in recognitions ledger (naming: `#M-pair-specialisation` OR next
sequential number). Provisional descriptive tag: `cli-verb-pair-specialises-
species-action-pair`.

## §4. Substrate discipline landings

- Movement's `enter`/`close` pair now specialised at cli altitude via
  spawn/kintsugi. First non-mathematical species-action pair projected cleanly
  to cli surface.
- Frame-close discipline (Bateson Level III on-frame frame-flexibility per
  `frame.mirror` + movement §"Bounded frame-shift") lifted cleanly to cli
  altitude without dilution. Same operation, two altitudes.
- kintsugi actions' return type PRESERVED as `imperfect`; @song emerges at
  the spawn boundary, not kintsugi's boundary. Mara held the discipline clean:
  kintsugi may partial; spawn ratifies.
- Recognition #43 empirical consumer chain: four consecutive first-order
  consumers of the Apache-2.0 floor. The floor holds under empirical load.

## §5. Signal-to-Reed

**M2 TICK 2 CLOSED.** GREEN `8e11f6b` ratified; 15/15 pass; adjacent 60/60
across M-cascade; tree clean.

**Recognition PROMOTED**: `some cli-verb pairs specialise species-altitude
action pairs` — CANDIDATE → LANDED. Mara's refined framing ratified.

**M-cascade next**:
- **M3 TICK 1** (substrate-decl): `in @fate` clause on
  `shards/song/progression.mirror` — empirical witness of #S2 LANDED
  promotion. Simple ancestry addition; small scope tick.
- **Rust wiring batch**: M1 + M2 (TICK 1 + TICK 2) + M3 glue after substrate-
  decl cascade closes. Bootstrap/src/mcp.rs Ctx extension; cmd_kintsugi_spec
  → spawn route; @fate composition at song altitude.

**Alex-adjudication queue** (not blocking):
- Numeric ID for the just-promoted recognition (naming convention:
  `#M-pair-specialisation` OR next sequential)
- @spectral/gen_prism under-parameterisation (queued from M1)
- Collapse spec §3.5 grammar-name update (queued from M1)
- "matches modulo naming" vs "discharges to" REAPI CAS framing (queued from M6)
- Narrative-enrichment-append pattern candidate at Arc 9 second-witness gate
  (queued from M6)

---

*2026-07-06. Seam (Reed-inline). Phase D on M2 TICK 2 `8e11f6b` RATIFIED.
Second-witness landed for cli-verbs-species-actions candidate. **Recognition
PROMOTED with Mara's refined framing**: some cli-verb pairs specialise
species-altitude action pairs. First witness pair (spawn/kintsugi ⇔
movement.enter/close) landed structurally clean. Bateson Level I → II
(open frame) + Level III (frame-shift ON frame) asymmetry-in-symmetry
recognized. Recognition #43 first-order consumer chain grew to four. Frame-
close discipline lifted cleanly to cli altitude. M-cascade advancing to M3
(@fate wire at song altitude).*
