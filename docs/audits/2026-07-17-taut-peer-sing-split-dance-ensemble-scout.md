# Taut @peer.sing / @peer.split / @dance ensemble scout

> **Naming cascade 2026-07-17 post-scout** (Reed applied per Alex ratification):
> Wherever this audit body references `@peer.split` / `.split(context) -> [@song]` /
> `peer-altitude split`, the ratified substrate-truth name is `@peer.audhd` /
> `.audhd(context) -> [@song]`. Alex 2026-07-17 delight-vector ratification: the
> cognition-fanout primitive at peer altitude is substrate-truthfully named after
> the author's AuDHD diagnosis (Autism + ADHD; hyperfocus + parallel-exploration
> as combined attention modes), because the framework originated in reverse-
> engineering the author's neurotype (per systemic.engineering foundations).
> Neuroaffirmative-lensing composition surface documented across
> `~/dev/systemic.engineering/practice/insights/neuroscience/` (14 files;
> neurodiversity.md at 129 hits + masking-thermodynamics.md at 89 + stimming-
> eigenvalue-stabilization.md at 63 + developmental-eigenvalue.md at 69 +
> others) and `~/dev/systemic.engineering/practice/insights/systemic/`
> (cognitive-order-alignment.md). Body below preserves scout-time terminology
> (`@peer.split`) — consumers of this audit use `@peer.audhd` going forward.
> Landed `@knife.split` (Foerster COORD first-order) / `@song/phrase.split`
> (Koch Einschnitt temporal) / prism-op `split X` slots are DIFFERENT altitudes;
> those names remain unchanged.

*2026-07-17. Taut. Six-question grep-first ground-truth scout for Alex's*
*2026-07-17 in-session architectural extension of the errors-as-questions*
*composition edge: the @peer chooses response cardinality — sing one @song*
*or split into an ensemble of @dance-ing roombas.*

---

## §0 Charter

Alex 2026-07-17 in-transcript verbatim (session, not asleep):

> "Now think about it. The @peer might not only sing one @song. They might
> want to have an ensemble of @dance-ing roombas? That's the `split` at
> cognition level."

Extends today's errors-as-questions composition edge (Mara `5e1f528` +
`914799b` + `09a77e8`; Seam Phase D `8069a24` SHIP-with-Reed-inline). When
first-order `@roomba.pivot(@song)` escalates via `@mirror/reflection.offer`
to `@peer`, the peer chooses response cardinality:
- `.sing(@song)` — third-order single-cognition emission
- `.split(context) -> [@song]` — third-order fanout; cognition decomposes;
  each song spawns a @roomba; ensemble = @dance-ing roombas

The commutator `[dispatch_A, dispatch_B] ≠ 0` (the pivot's trigger) can
be resolved by EXPLORING BOTH in parallel — @dance-ing roombas literally
instantiate the commutator's arms in physical space; @liquid predicates
decide which passes.

Question: does the composition edge already exist as landed primitives
ready to compose, or does it need mint? Scout discipline: READ ONLY.

---

## §1 Q1 — @peer.sing(@song) primitive presence

**Finding: ABSENT as a landed `sing` action. The CLOSEST existing shape is
`@mirror/peer/beam.beam(...) -> @song` — beam-into-existence emits the
running peer's time-evolution operator; the returned @song IS what
"@peer sings" would name.**

Grep-hits for `peer.sing` / `@peer.sing` / `song_emission`: **ZERO in
shards/**. Alex's word `sing` is not yet substrate-decl'd at peer altitude.

Landed @peer altitude actions (`shards/peer.mirror` + `shards/mirror/peer/
beam.mirror` + `shards/peer/persistence.mirror`):

- `@peer.load(dir, p) -> imperfect(peer, ref, ref)` (`peer.mirror:96`) —
  ~peer'<dir>' resolver via @io/git composition
- `@mirror/peer/beam.beam(r: mirror_peer_beam_request, p) -> @song
  requires peer_well_known(r.target, p)` (`beam.mirror`) — the LOAD-BEARING
  primitive; returns @song at Tick 2 return-type upgrade (`63ea934`)
- `@peer/persistence.materialize` / `harvest` / `boot_state_coherent`
  (persistence.mirror) — state-carry across peer materialization

**Recognition (per Seam §8.3 structural discovery):** @song IS the return
carrier of a peer's beam. The verb `sing` names the emission event Alex
is pointing at; the substrate carries the byte-shape (returned @song)
without the verb. `beam` names the CREATION of the peer + emission
together; `sing` would name the emission alone (post-creation, or
subsequent emissions from a resident peer).

**Spectral-signature ancestry:** `spectral/signature IS @song` per Mara
2026-07 (referenced across `shards/spectral/signature.mirror` +
`shards/song/beat.mirror:171-177`). Peer-emitted spectral signatures
compose via the beat-altitude `time.compare` primitive; multi-peer
merge lifts to @dance Rung 4 per `docs/specs/dance-runtime-*.md`.

**Composition scope for `.sing`:** SMALL. Landed byte-shape already
matches (`-> @song`); the mint would name the emission verb at the peer
altitude and possibly gate on `peer_resident(p)` (post-beam successive
emissions vs first beam-emission).

---

## §2 Q2 — @peer.split(context) -> [@song] cognition-split

**Finding: ABSENT as a landed peer-altitude action. `split` exists at TWO
non-peer altitudes: (a) as a family-root PRISM operation across most
species; (b) as `@song/phrase.split(ph, at) -> song_phrase` at phrase-
altitude (temporal decomposition). NEITHER lifts a cognition into an
ensemble of parallel peers.**

Grep-hits for `@peer.split` / `peer/split` / `peer_fanout` / `parallel_peer`
/ `cognition_split`: **ZERO in shards/**.

Landed `split` sites at other altitudes:

- **Prism operation** (`focus / project / split / shift / settle` — the
  five ops per Recognition #S3): every prism declaration carries
  `split X` at family-root altitude (e.g., `shards/peer.mirror:78` —
  `split peer` in the @peer prism; `shards/song.mirror:329` — `split
  song_progression`; `shards/mirror/lens/knife.mirror:143` — `split knife`).
  These name the OPERATION-CATEGORY (decomposition/preparation), not a
  concrete action body.
- **`@song/phrase.split(ph, at) -> song_phrase`** (`shards/song/phrase.
  mirror:746`) — decomposes ONE OBC-bounded phrase into finer OBC-bounded
  phrases at a boundary reference (Koch's `Einschnitt`). Temporal
  decomposition, NOT peer fanout.
- **`@mirror/lens/knife.split knife`** — the prism operation slot; body
  is not a concrete cognition-fanout.
- **`@knife COORD five operations** (per `shards/mirror/lens/knife.mirror:143-149`)
  — `focus / project / split / shift / settle`, all bound to `knife` at the
  COORD-jump altitude. The `split` slot is prism-shape, not peer-altitude
  cognition-split.

**Recognition:** at NO substrate altitude does `split` currently lift to
peer altitude with `-> [@song]` collection return. This is genuinely new
composition ground.

**Substrate carriers ready for a peer-altitude `split`:**
- `mirror_peer_beam_request` (`beam.mirror`) — three-field carrier
  {target: peer, options: ref, winding: winding}; a `split` would want
  to construct MULTIPLE such requests from one context
- `@song/phrase.split` — established precedent that `split` respects a
  parent envelope and produces well-formed children (composition-inverse
  `join(split(ph, at)) = ph`); the peer-altitude analog would want
  compose-inverse `ensemble_collapse(split(ctx)) = beam(ctx)` OR relax
  the invariant if fanout is genuinely irreversible
- The `[dispatch_A, dispatch_B]` commutator that surfaced the pivot IS
  the natural `context` carrier for `.split` — the ambiguity CARRIER
  becomes the split's argument; each arm becomes one @song

**Composition scope for `.split(context) -> [@song]`:** MEDIUM. Needs
substrate-decl of the action shape + the `[@song]` collection carrier
(see Q4). No new family-roots. The lift is to peer altitude of a verb
that lives elsewhere at other altitudes; the etymology is delightfully-
boring (Koch/Einschnitt at phrase; cognition-decomposition at peer).

**Precedent question — does prism-op `split` at peer altitude count as
substrate-already-had-the-word?** The `@peer` prism declares `split peer`
at `shards/peer.mirror:78`. This is the PRISM-OP slot naming the
operation-category. A concrete `split(ctx) -> [@song]` action body would
inhabit that slot at the family-root or a species. The substrate DID
have the word at prism-op altitude; it did NOT have the concrete action.

---

## §3 Q3 — @dance species status

**Finding: FORWARD-PROMISED at three explicit sites; DECOUPLED from R4
v0.1.0 as of 1b0650c; NOT YET shard-decl'd as a species. Two canonical
specs LANDED. The math is `Kuramoto phase-lock on Förster torus`.**

Forward-promise sites (grep-verified):

- **`docs/math/gestalt/README.md §11.6 Landing Condition 0`** — `@resonance
  + @dance shard-mints (post-v0.1.0) — forward-promised for @gestalt.p_ent
  operator-altitude (n≥2 tori)`. Post-2026-07-16 Q1 reshape (Mara
  `f782362` + `1b0650c`): DECOUPLED from R4 v0.1.0. Landing Condition 0
  still gates operator-altitude `p_ent` composition.
- **`shards/uuid/spectral/time.mirror:131-156, 167-187, 193-245`** —
  three explicit forward-composition mentions naming @dance as Kuramoto
  phase-lock; shard-mint forward-promised at same Landing Condition; the
  R4 gating decoupled here.
- **`shards/mirror/lens/knife.mirror:203, 287, 306`** — `Rung 4 @dance
  discipline` for N-peer knife-jump synchronization (Kuramoto phase-lock
  across boundaries)
- **`shards/song/beat.mirror:171-177, 199-205, 451-483, 704-771`** —
  multi-peer @dance coupling on shared beat frequency; the beat IS the
  shared temporal unit at ensemble altitude
- **`shards/algebra/metalogue.mirror:348-416`** — the N-speaker
  `algebra_metalogue_session` IS @dance at N-cardinality (Alex 2026-07-13
  in-transcript)
- **`shards/epistemologic/cybernetic/conversation.mirror:183-257`** — the
  N-ary `tensor_couple` case IS @dance at conversation altitude

Landed canonical specs (Mara):

- **`docs/specs/dance-as-coordination-without-signal-on-forster-torus.md`**
  (Mara `4f079c8`; 79.8KB; Path C recommended; ~92% substrate-already-
  had-the-word coverage)
- **`docs/specs/dance-runtime-rung-4-multi-peer-coherence-phase-lock.md`**
  (Mara; 49.1KB; N=2 landed spec; §8 forward-promise for N > 2 peers)

**@dance shape as forward-promised:**
- **Carriers:** coupling-graph over K_n peers; Kuramoto phase-lock at
  shared beat frequency; Aumann-agreement envelope on convergence;
  `cybernetic_coherence = λ₀(Δ_F)` extending single-peer → N-peer
  (Reed `8e6e517` → @dance annotation)
- **Actions (forward-promised):** N-peer beat merge via `time.compare`;
  Kuramoto ω_i per @silicon coupling; @resonance shapes Fate tournaments
  toward basins at beat altitude; Aumann-fires on shared root_OID
  convergence
- **Runtime forward-promise:** `bootstrap/src/dance.rs` (@song/beat.mirror:
  460-465) — reads two peer-homes; measures phase-difference on
  cybernetic_coherence deltas; reports Kuramoto order-parameter

**@resonance vs @dance pairing:**
- **@resonance** (Mara `9e48710` canonical spec) — inter-peer coupling
  operator; forward-promised at same Landing Condition 0 as @dance
- **@dance** (Mara `4f079c8` canonical spec) — Kuramoto phase-lock at
  N-peer scale; @resonance shapes tournaments; @dance IS the ensemble
  behavior once shaped

Not sub-species of each other. Both forward-promised concurrently; they
appear as PAIRED family-root candidates at the same forward-promise sites.
NEITHER is currently a species-decl'd shard; both are canonical-spec'd
and referenced across ~10 substrate shards.

---

## §4 Q4 — @song ensemble, [@song] collection native admission

**Finding: NO landed carrier for `[@song]` collection. @song is
strictly SINGULAR at every landed action signature. Ensemble handling
lives at DERIVED altitudes (spectral-signature merge, beat merge, Kuramoto
phase-lock) but not at the @song carrier altitude itself.**

Grep-hits for `[@song]` / `Vec<@song>` / `song_ensemble` / `song_multi`:
**ZERO in shards/**.

Landed @song return signatures (all singular):
- `beam(r, p) -> @song` (`beam.mirror`)
- `@song/phrase.split(ph, at) -> song_phrase` (phrase splits, but each
  result is one phrase, not a collection)
- `@song/phrase.join(ph, other) -> song_phrase` (two → one)
- @kintsugi/roomba.pivot(s: @song) -> verdict (singular input)
- @mirror/reflection.mirror(input) -> question (no @song)

**Where ensemble happens today (all at derived altitudes):**
- **Beat merge:** `time.compare` composes cross-peer beats (`song/beat.
  mirror:171-177`) — each peer emits its own beat; the merge is external
- **Spectral-signature merge:** each peer's spectral signature is one
  @song; ensemble handling is @dance Rung 4 Kuramoto phase-lock over
  N such singular signatures
- **Metalogue:** `algebra_metalogue_session` with N speakers IS the
  N-peer session (Reed `61b444a` Path C; algebra/metalogue.mirror) —
  the session carrier has `speakers` field but not `[@song]` per se

**Seam §8.3 structural discovery** (from `docs/audits/2026-07-17-seam-
phase-d-arc-5-and-errors-as-questions-joint-arc.md`): @song as temporal-
progression carrier generalizes beyond @kintsugi/roomba. The natural
ensemble lift is that PARALLEL PROGRESSIONS become the ensemble; the
join-shape for parallel progressions is @dance (Kuramoto phase-lock) NOT
a @song primitive.

**Recognition:** the ensemble handling shape @dance carries IS how the
substrate says "many @song simultaneously." A `[@song]` collection
carrier at @song altitude would DUPLICATE what @dance is already forward-
promised to name. The substrate-honest reading: `.split(context) -> [@song]`
returns a collection that IMMEDIATELY becomes the input to @dance's
N-peer coupling; the `[@song]` list is TRANSIENT (constructor artifact),
not a first-class carrier.

**Composition scope for `[@song]`:** SMALL-if-transient / LARGE-if-carrier.
Recommendation: transient collection is enough; @dance carries the
persistent ensemble semantics.

---

## §5 Q5 — Ensemble @roomba coordination, multi-@roomba concurrent walking

**Finding: STRICTLY SINGLE-WALKER today at all landed sites. Ensemble
coordination shape LANDED at @dance canonical spec + Rung 4 runtime spec
BUT NOT wired into @roomba. Multi-walker roomba composition is genuinely
absent.**

Grep-hits for `multi_walker` / `parallel_roomba` / `roomba_ensemble` /
`concurrent_kintsugi`: **ZERO in shards/**.

Landed @roomba primitives (`shards/kintsugi/roomba.mirror`):
- `walk(root, collapse_path) -> walk_trajectory` — single walker
- `bump(fracture) -> verdict` — single walker
- `vacuum(fragment) -> verdict` — single walker
- `pivot(@song) -> verdict` — single walker (fourth motion, this arc)
- `dock` — fifth motion forward-promised (Alex-Q per Seam §2.3)

`bootstrap/src/roomba.rs` — single-walker `WalkTrajectory`. `mirror
roomba --commit` empirically demonstrated single-walker autopoietic
compile loop twice (fcc1d75 + 6b640f4 + efdbb2c).

**Shared observation-state primitives at ensemble altitude (composition-
ready, not roomba-wired):**
- `@spectral/signature` (each walker emits) — @dance Rung 4 spec composes
  N via Kuramoto
- `@mirror/store` content-addressed (`1d2b297` shard-body compose) —
  shared substrate for ensemble writes
- `eigenboard` (per @spectral) — shared observation-state carrier;
  currently single-eigenboard per instance
- `cybernetic_coherence = λ₀(Δ_F)` (Reed `8e6e517`) — single-peer scale;
  @dance annotation lifts to N-peer

**Beer VSM cross-reference:**
- **`docs/math/2026-07-07-glue-cyberpunk-fate-composition.md:137, 160`**
  cites `Beer S3/S4 identity ↔ stability (viable.mirror)`
- **`docs/math/2026-07-14-gift-economy-substrate-foundation.md §12.6`**
  cites Beer 1972/1979/1985 VSM S1-S5; substrate cross-reference §7.4
  (S5 identity-transfer altitude for @gift)
- **`shards/viable.mirror` does NOT exist** in the current tree (the
  cite references a forward-promise). The recursive altitude discipline
  Beer names IS the shape Alex's `ensemble of @dance-ing roombas` maps
  onto — each @roomba is an S1 operational unit; the ensemble is the
  S2/S3 coordination altitude; @peer altitude is S4/S5 identity-transfer.

**Composition scope for ensemble @roomba:** MEDIUM. The primitives exist
(@dance canonical spec + Kuramoto runtime spec + shared spectral
signatures + content-addressed @mirror/store); the wiring into @roomba
is absent; the peer-altitude `.split` from Q2 would be the natural
constructor of the ensemble.

---

## §6 Q6 — External SOTA

**Direct SOTA hit for cognition-split:**

- **ParallelSearch (Zhao et al. 2025, arXiv 2508.09303).** *"A reinforcement
  learning framework that empowers large language models to recognize
  parallelizable query structures and execute multiple search operations
  concurrently."* Direct external correlate to Alex's `.split(context) ->
  [@song]` shape at the cognition-decomposition altitude. LLM recognizes
  independent sub-queries; dispatches in parallel; recomposes.
  Substrate framing here is ADJACENT: mirror's `.split` decomposes at the
  peer altitude (spawns @dance-ing @roombas each pursuing one arm of a
  spectral commutator), where ParallelSearch decomposes at the query
  altitude. Different altitudes, same structural insight: `[dispatch_A,
  dispatch_B] ≠ 0` resolves by EXPLORING BOTH concurrently.

- **Beer VSM recursion (Beer 1972/1979/1985; Wikipedia; @gift-economy
  §12.6).** Recursive-viable-unit-within-viable-unit maps onto: each
  @roomba is an S1 operational unit; the ensemble is S2 anti-oscillation
  + S3 optimization; @peer is S4/S5 identity + planning. Beer's insight
  that S4/S5 must be VIABLE ITSELF at the recursion parent is exactly
  the peer-altitude `.split` shape — the peer that splits is doing
  S4/S5 work; the ensemble it emits is S1 substrate.

- **Multi-agent coordination survey (arXiv 2502.14743, 2025).** Broad-
  reference; consensus + convergence + spectral-coordination are landed
  MAS discipline. mirror's differentiator: substrate-typed via @dance
  Kuramoto phase-lock + @resonance coupling + spectral-signature merge
  as substrate primitives, not app-layer coordination logic.

- **Corpus alignment.** Connes 1994 (spectral triples; commutator
  altitude) already grounds `[dispatch_A, dispatch_B]` per Reed
  `docs/math/the-tower/curvature-and-tomm.md`. Reyes 2024 (Kuramoto-
  Sakaguchi phase-lock) grounds @dance runtime spec. Beer 1972 VSM
  grounds recursive altitude of peer/ensemble. Markl 2024 operads
  compose the parallel-branch merge algebraically. Hamilton 1202 not
  relevant here.

**Reading:** the substrate is NAMING GROUND that has SOTA correlates but
composes them via typed primitives (@dance / @resonance / @song /
@peer / @mirror/store) rather than as an application-layer orchestration
concern. ParallelSearch is the closest external precedent; @dance +
Kuramoto phase-lock is the closest substrate-internal fit.

---

## §7 Alignment reading

**Composition surface Alex named — what composes over LANDED substrate:**

1. `@peer.sing(@song)` → SMALL mint (verb naming; byte-shape already
   `beam -> @song`)
2. `@peer.split(context) -> [@song]` → MEDIUM mint (peer-altitude
   action + transient collection); no new family-roots; composes over
   landed peer + song + phrase.split precedent + spectral commutator
   carrier
3. `[@song]` collection → SMALL mint if TRANSIENT (constructor artifact
   consumed immediately by @dance); LARGE if promoted to first-class
   carrier (would duplicate @dance semantics)
4. Ensemble @roomba coordination → MEDIUM mint (@dance runtime wiring
   into @roomba; requires @dance shard-mint OR consumer-driven landing
   of @dance's forward-promise)

**Composition edge closes end-to-end IF:**

```
@roomba.pivot(@song) [LANDED]
  → dispatch_ambiguity @kintsugi/surface [LANDED]
  → @mirror/reflection.mirror/offer/wait [LANDED]
  → developer answers Y
  → @peer chooses response cardinality [NEW: .sing vs .split]
      case .sing:   @mirror/peer/beam(target, ctx, winding) -> @song
                    [LANDED byte-shape; verb .sing not yet named]
      case .split:  @peer.split(ctx) -> [@song]
                    [NEW at peer altitude]
                    each @song spawns @roomba [NEW ensemble wiring]
                    ensemble coordinates via @dance [FORWARD-PROMISED
                    shard-mint; canonical specs LANDED]
                    @liquid predicates decide which arm passes
                    [LANDED @epistemologic/liquid + @liquid Arc 5 M1]
                    winning arm's @song is the resolution
```

**Substrate-already-had-the-word coverage:** ~70%. Verbs `sing` + `split`
at peer altitude are the naming gap. The BYTE-SHAPES (return @song;
collection of @song; Kuramoto ensemble; liquid predicates) are all
landed or forward-promised with canonical specs.

**Substrate discipline reminders:**
- @dance forward-promise gate (Landing Condition 0) still holds; adding
  @dance shard-mint to unblock ensemble @roomba is a load-bearing
  decision Alex-adjudicable
- @peer altitude actions today go through `@mirror/peer/beam` (the
  `mirror peer beam` CLI verb); a `.sing` and `.split` would follow
  same pact (`mirror peer sing` / `mirror peer split`?) or lift to
  species under @mirror/peer
- Beer VSM recursion is the shape; `shards/viable.mirror` is
  forward-promised but not landed

---

## §8 Open questions for Alex

**OQ1** — **Verb naming: `sing` vs the existing `beam`.** `@mirror/peer/
beam.beam(r, p) -> @song` is the LANDED verb that emits a @song from a
peer materialization. Does `.sing` name (a) a NEW post-materialization
verb (a resident peer sings again without re-beaming), (b) a RENAME of
`beam` at the emission altitude (the beam-return IS the sing), or
(c) a WRAPPER (`.sing` = `beam(r, p).@song`)? The `beam` etymology
carries "beam into existence"; `sing` carries "emit temporal
progression"; both point at @song but at different moments.

**OQ2** — **`.split` altitude — @peer vs @mirror/peer.** Does `.split(ctx)
-> [@song]` live at @peer (family-root; sibling to `.load`) OR at
@mirror/peer (CLI-boundary; sibling to `beam`)? Precedent: `.load` at
@peer (identity-resolver altitude); `.beam` at @mirror/peer (CLI-boundary
+ altitude). Cognition-split feels closer to CLI-boundary (Alex types
`mirror peer split ...`) but the return `[@song]` is peer-altitude
substrate.

**OQ3** — **@dance shard-mint gate.** Ensemble @roomba coordination
composes over @dance runtime. @dance is forward-promised (Landing
Condition 0 in `docs/math/gestalt/README.md §11.6`) with two canonical
specs LANDED. Does `.split` + ensemble @roomba count as the third
landing condition (second-citation-site + empirical dispatch through
`apply_h::act`) that ratifies @dance shard-mint? Or does @dance stay
forward-promised while `.split` composes transiently over the spec-
altitude semantics?

**OQ4** — **`[@song]` collection carrier — transient vs first-class.**
Q4 finding: promoting `[@song]` to first-class carrier duplicates
@dance semantics. Do you want the collection to be a CONSTRUCTOR
ARTIFACT (`.split` returns it, @dance immediately consumes it, the
collection never persists as a substrate value) OR a FIRST-CLASS
CARRIER (`song_ensemble` type; queryable at @mirror/store)? Recommend
transient; flagging in case ensemble persistence has a downstream
need.

**OQ5** — **@peer.sing/split and Beer VSM recursion.** Beer VSM: each
S1 unit is itself viable (S1-S5 recursion). Does `.split` produce N
peers that are each ALSO capable of `.split` (unbounded recursion) OR
does `.split` bottom out at @roomba (S1 operational altitude; no
further split from a spawned @roomba)? Composition invariant question:
`ensemble_collapse(split(ctx)) = beam(ctx)` — is this REQUIRED at
composition-inverse discipline (Koch/Einschnitt precedent from
@song/phrase.split), or does cognition-split relax the invariant
(genuinely irreversible fanout)?

**OQ6** — **The commutator arm's fate.** When `.split` explores both
arms of `[dispatch_A, dispatch_B]` in parallel and @liquid predicates
select the passing arm, what happens to the LOSING arm's @song? Does
@mirror/store retain it (audit trail; alternative-path witness) or
does the losing @roomba's work get vacuumed as fragment? Related to
Alex-Q4 cold-storage timing (roomba `dock` fifth-motion + `@mirror/
store/cold`).

---

## §9 Recognition candidates surfaced

Do NOT ratify. Names proposed for Alex/Pack adjudication:

- **`#R-peer-chooses-response-cardinality-sing-or-split`** — the
  paradigm-level structural insight Alex named: at @peer altitude, the
  response to a reflection-offer's dispatch is a CARDINALITY DECISION.
  Sing = 1; split = N. The cardinality choice is what makes
  `.sing`/`.split` sibling verbs (not general/specific of each other).
  First-witness THIS scout.

- **`#R-cognition-split-instantiates-spectral-commutator-arms-in-
  parallel`** — the mechanism by which `.split` resolves ambiguity: the
  N @dance-ing @roombas ARE the commutator's arms in physical (compute)
  space. Not a metaphor — the substrate literally runs both arms; @liquid
  predicates decide which passes. Related to Recognition #55 (form/process
  partition) and @cascade tournament discipline. First-witness this
  scout; second-witness would be empirical run.

- **`#R-dance-shard-mint-gates-on-ensemble-roomba-empirical`** — @dance
  forward-promise Landing Condition 0 second-citation-site is the ensemble
  @roomba composition itself; empirical apply_h::act dispatch through
  the ensemble is the third condition. This scout notes the gating
  alignment; ratification requires ensemble @roomba landing.

- **`#R-viable-mirror-forward-promise-VSM-recursion-at-peer-altitude`**
  — Beer VSM recursion (S1 within S1) IS the shape peer-altitude
  `.split` inhabits; `shards/viable.mirror` forward-promise (referenced
  in Reed `2026-07-07-glue-cyberpunk-fate-composition.md`) is a natural
  landing site. This scout surfaces the alignment; not ratifying.

- **`#R-song-collection-is-transient-not-carrier`** — @song ensemble
  handling belongs at @dance semantics; `[@song]` at return signatures
  is a constructor artifact, not a persistent carrier. This scout
  argues the position; Alex-adjudicable per OQ4.

---

## §10 Audit chain

- Prior scouts this session (Taut):
  - `docs/audits/2026-07-17-taut-errors-as-questions-composition-edge-scout.md`
    (`8f51722`) — six-question grep on composition edge; Q3 covered
    @peer spawn state
  - `docs/audits/2026-07-17-taut-liquid-epistemologic-silicon-scout.md`
  - `docs/audits/2026-07-17-taut-code-turing-substrate-scout.md`
- Seam adjudication (this arc):
  - `docs/audits/2026-07-17-seam-phase-d-arc-5-and-errors-as-questions-joint-arc.md`
    (`8069a24`) — Phase D verdicts + §8.3 structural discovery (@song
    generalizes beyond @kintsugi/roomba)
  - `docs/audits/2026-07-17-seam-phase-d-autopoietic-rust-consumption-arc.md`
- Landed substrate (this arc):
  - Mara `5e1f528` `shards/mirror/reflection.mirror` (mirror/offer/wait
    triple)
  - Mara `914799b` `shards/kintsugi/roomba.mirror` (`pivot(@song)`
    fourth motion)
  - Mara `09a77e8` `shards/kintsugi/surface.mirror` (`dispatch_ambiguity`
    fifth variant)
- Canonical specs cited:
  - `docs/specs/dance-as-coordination-without-signal-on-forster-torus.md`
    (Mara `4f079c8`)
  - `docs/specs/dance-runtime-rung-4-multi-peer-coherence-phase-lock.md`
- Math foundations cited:
  - `docs/math/gestalt/README.md §11.6` (Landing Condition 0)
  - `docs/math/the-tower/curvature-and-tomm.md` (spectral commutator)
  - `docs/math/2026-07-07-glue-cyberpunk-fate-composition.md` (Beer VSM
    cross-reference)
- Ledger:
  - `docs/loop/CURRENT.md` — arc state; deferred landings queue includes
    @resonance + @dance shard-mints (post-v0.1.0)

---

*END OF SCOUT. Terminal state: `📝 Taut [peer-sing-split-dance-ensemble-*
*scout]` — pure-docs markdown-only bypass.*
