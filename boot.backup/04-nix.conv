in @beam

grammar @nix {
  type = flake | input | output | derivation

  type input = url | follows | path

  type output = package | shell | lib

  type derivation = build | check | dev

  action emit(target: flake)

  action build(target: derivation)
}
